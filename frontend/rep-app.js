(() => {
  const { useState, useEffect, useCallback, useRef, useMemo } = React;
  const repListNav = {};
  function repNavNeighbors(section, id) {
    const ids = repListNav[section] || [];
    const i = ids.indexOf(id);
    return {
      prev: i > 0 ? ids[i - 1] : null,
      next: i >= 0 && i < ids.length - 1 ? ids[i + 1] : null
    };
  }
  function RepPrevNext({ section, id, view, navigate, linkStyle }) {
    const { prev, next } = repNavNeighbors(section, id);
    const st = (on) => ({ ...linkStyle || {}, opacity: on ? 1 : 0.35, cursor: on ? "pointer" : "default", pointerEvents: on ? "auto" : "none" });
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("a", { "aria-disabled": !prev, style: st(prev), onClick: () => prev && navigate(view, prev) }, "\u2190", " Prev"), /* @__PURE__ */ React.createElement("a", { "aria-disabled": !next, style: st(next), onClick: () => next && navigate(view, next) }, "Next ", "\u2192"));
  }
  const rofDraftGuard = { current: null };
  const rofDraftStore = {
    k: (key) => "rofDraft:" + key,
    save(key, state) {
      try {
        localStorage.setItem(this.k(key), JSON.stringify({ savedAt: (/* @__PURE__ */ new Date()).toISOString(), state }));
      } catch (e) {
      }
    },
    load(key) {
      try {
        const raw = localStorage.getItem(this.k(key));
        return raw ? JSON.parse(raw) : null;
      } catch (e) {
        return null;
      }
    },
    clear(key) {
      try {
        localStorage.removeItem(this.k(key));
      } catch (e) {
      }
    }
  };
  function rofDraftOnLeave() {
    const g = rofDraftGuard.current;
    if (!g) return;
    rofDraftGuard.current = null;
    try {
      if (!g.isDirty()) return;
      if (confirm("Save your unfinished " + g.label + " as a draft?\n\nOK \u2014 save it to resume later\nCancel \u2014 discard it")) {
        rofDraftStore.save(g.key, g.getState());
      } else {
        rofDraftStore.clear(g.key);
      }
    } catch (e) {
    }
  }
  function useRofDraft(key, label, isDirty, getState) {
    const ref = useRef(null);
    ref.current = { isDirty, getState };
    useEffect(() => {
      if (!key) return;
      rofDraftGuard.current = {
        key,
        label,
        isDirty: () => {
          try {
            return !!ref.current.isDirty();
          } catch (e) {
            return false;
          }
        },
        getState: () => ref.current.getState()
      };
      const onBeforeUnload = (e) => {
        const g = rofDraftGuard.current;
        if (g && g.key === key && g.isDirty()) {
          rofDraftStore.save(key, g.getState());
          e.preventDefault();
          e.returnValue = "";
        }
      };
      window.addEventListener("beforeunload", onBeforeUnload);
      return () => {
        window.removeEventListener("beforeunload", onBeforeUnload);
        if (rofDraftGuard.current && rofDraftGuard.current.key === key) rofDraftGuard.current = null;
      };
    }, [key]);
  }
  function useRofDraftRestore(key, restore) {
    useEffect(() => {
      if (!key) return;
      const d = rofDraftStore.load(key);
      if (!d || !d.state) return;
      const when = d.savedAt ? new Date(d.savedAt).toLocaleString() : "";
      if (confirm("Resume your saved draft" + (when ? " from " + when : "") + "?\n\nOK \u2014 resume it\nCancel \u2014 discard it")) {
        try {
          restore(d.state);
        } catch (e) {
        }
      }
      rofDraftStore.clear(key);
    }, []);
  }
  const API = (() => {
    const h = window.location.hostname;
    if (h === "localhost" || h === "127.0.0.1") return "http://localhost:3001";
    return "";
  })();
  let CA_TAX_RATES = null;
  const loadTaxRates = () => fetch(API + "/api/tax-rates").then((r) => r.ok ? r.json() : null).then((d) => {
    CA_TAX_RATES = d;
  }).catch(() => {
  });
  (window.requestIdleCallback || ((cb) => setTimeout(cb, 1500)))(loadTaxRates);
  function caTaxRate(zip) {
    if (!zip || !zip.startsWith("9")) return 0;
    return CA_TAX_RATES && CA_TAX_RATES[zip.substring(0, 3)] || 0.0725;
  }
  function fmtQty(n) {
    if (n >= 1e4) {
      const k = n / 1e3;
      return (k >= 100 ? Math.round(k) : k.toFixed(1).replace(/\.0$/, "")) + "k";
    }
    return n.toLocaleString();
  }
  function availableQty(p) {
    const qty = parseInt(p.qty_on_hand);
    if (!(qty > 0)) return "";
    if (p.sell_by === "unit") return fmtQty(qty) + " units";
    if (p.sell_by === "roll") return "~" + fmtQty(qty) + " sf";
    const spb = parseFloat(p.sqft_per_box);
    if (spb > 0) return "~" + fmtQty(Math.round(qty * spb)) + " sf";
    return fmtQty(qty) + " bx";
  }
  function stockDisplay(p) {
    const MUTED = "#8a7e68";
    if (!p.vendor_has_inventory && (p.stock_status === "unknown" || p.stock_status === "out_of_stock" || !p.stock_status)) {
      return { l: "No inv. data", c: MUTED };
    }
    if (p.stock_status === "in_stock") {
      return { l: "In stock", c: "#3a7a4e", q: availableQty(p) };
    }
    if (p.stock_status === "low_stock") {
      const qty = parseInt(p.qty_on_hand);
      if (qty > 0) {
        const lbl = p.sell_by === "unit" ? "Only " + qty + " left" : p.sell_by === "box" ? "Only " + qty + " boxes" : p.sell_by === "roll" ? "Only ~" + fmtQty(qty) + " sf" : qty + " on hand";
        return { l: lbl, c: "#a87935" };
      }
      return { l: "Low stock", c: "#a87935" };
    }
    if (p.stock_status === "out_of_stock") return { l: "Out of stock", c: "#c54731" };
    return { l: "Unknown", c: MUTED };
  }
  function stashPendingAdd(target, sku, extra) {
    try {
      sessionStorage.setItem("rep_pending_add", JSON.stringify({ target, sku, ts: Date.now(), ...extra || {} }));
    } catch (e) {
    }
  }
  function consumePendingAdd(target) {
    try {
      const raw = sessionStorage.getItem("rep_pending_add");
      if (!raw) return null;
      const data = JSON.parse(raw);
      if (data.target !== target) return null;
      sessionStorage.removeItem("rep_pending_add");
      if (data.ts && Date.now() - data.ts > 10 * 60 * 1e3) return null;
      return data;
    } catch (e) {
      return null;
    }
  }
  function optimizeImg(url, width) {
    if (!url || typeof url !== "string") return url;
    try {
      if (url.includes("i8.amplience.net")) {
        const u = new URL(url);
        u.searchParams.set("w", width);
        u.searchParams.set("fmt", "auto");
        u.searchParams.set("qlt", "80");
        return u.toString();
      }
      if (url.includes("res.cloudinary.com") && url.includes("/upload/")) return url.replace("/upload/", `/upload/w_${width},f_auto,q_80/`);
      if (url.includes(".widen.net")) {
        const u = new URL(url);
        u.searchParams.set("w", width);
        u.searchParams.set("quality", "80");
        return u.toString();
      }
    } catch (e) {
    }
    return url;
  }
  function skuPriceSuffix(row) {
    row = row || {};
    const basis = row.price_basis;
    const soldPerUnit = row.sell_by ? row.sell_by === "unit" || row.sell_by === "piece" : basis === "per_unit";
    if (soldPerUnit) {
      if ((basis === "sqft" || basis === "per_sqft") && !(parseFloat(row.sqft_per_box) > 0) && !(parseFloat(row.slab_sqft) > 0)) return "/sqft";
      return isPerPieceRow(row) ? "/pc" : "/ea";
    }
    if (row.sell_by ? row.sell_by === "roll" : basis === "per_sqyd") return "/sqyd";
    return "/sqft";
  }
  function isPerPieceRow(row) {
    if (!row) return false;
    const basis = row.price_basis;
    return (row.sell_by === "unit" || row.sell_by === "piece") && (basis === "sqft" || basis === "per_sqft") && parseFloat(row.sqft_per_box) > 0;
  }
  function pieceRetail(row, rawPrice) {
    const price = parseFloat(rawPrice != null ? rawPrice : row && (row.retail_price != null ? row.retail_price : row.price)) || 0;
    return isPerPieceRow(row) ? price * (parseFloat(row.sqft_per_box) || 0) : price;
  }
  function formatLineItem(it) {
    it = it || {};
    const brand = it.brand_hidden ? (it.vendor_public_code != null ? String(it.vendor_public_code) : "") || it.custom_vendor || "" : it.brand_name || it.vendor_name || it.custom_vendor || "";
    const sku = it.vendor_sku || it.internal_sku || it.sku || "";
    if (it.display_name) return { brand, name: it.display_name, sku };
    if (it.is_custom_rug || (it.description || "").startsWith("Custom Area Rug")) {
      const fmtFt = (ft) => {
        const n = parseFloat(ft) || 0, w = Math.floor(n);
        let i = Math.round((n - w) * 12);
        if (i === 12) return w + 1 + "'";
        return i === 0 ? w + "'" : w + "'" + i + '"';
      };
      const dims = it.custom_width_ft && it.custom_length_ft ? fmtFt(it.custom_width_ft) + " \xD7 " + fmtFt(it.custom_length_ft) : (it.description || "").replace(/^Custom Area Rug\s*[—-]\s*/, "");
      const rawBrand = it.brand_name || it.vendor_name || "";
      const stripBrand = (s) => {
        let o = String(s || "").trim();
        if (rawBrand) o = o.replace(new RegExp("^" + rawBrand.replace(/[.*+?^${}()|[\]\\]/g, "\\$&") + "\\b\\s*", "i"), "").trim();
        return o;
      };
      const normKey = (s) => String(s || "").toLowerCase().replace(/[^a-z0-9]+/g, "");
      const style = stripBrand(it.product_name) || stripBrand(it.collection);
      const colorPart = it.color && !(style && normKey(style).includes(normKey(it.color))) ? it.color : "";
      return {
        brand,
        name: ["Custom Area Rug", dims, style, colorPart].filter(Boolean).join("  \xB7  "),
        sku
      };
    }
    const isAcc = (it.variant_type || "").toLowerCase() === "accessory";
    const accLabel = isAcc ? it.accessory_label || it.variant_name : null;
    const parentCollection = isAcc ? it.parent_collection : null;
    const parentColor = isAcc ? it.parent_color : null;
    let title, ordered;
    if (parentCollection) {
      title = parentCollection;
      ordered = [parentColor, accLabel || it.product_name];
    } else {
      title = it.collection || it.current_collection || it.product_name || "Product";
      const _leadEcho = (coll, val) => {
        if (!coll || !val) return val;
        const c = String(coll).trim().split(/\s+/), k = String(val).trim().split(/\s+/);
        if (c.some((t) => t === "&" || t.toLowerCase() === "and")) return val;
        const nz = (t) => t.toLowerCase().replace(/[.,]+$/, "");
        let a = 0;
        while (a < k.length - 1 && a < c.length && nz(k[a]) === nz(c[a])) a++;
        return a > 0 ? k.slice(a).join(" ") : val;
      };
      const design = _leadEcho(title, it.color) || (it.product_name && it.product_name !== title ? it.product_name : null);
      let sizeInfo = null;
      if (it.color && it.product_name) {
        let rem = String(it.product_name).trim();
        if (rem.toLowerCase().indexOf(String(title).toLowerCase()) === 0) rem = rem.slice(String(title).length);
        rem = rem.replace(new RegExp(String(it.color).replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "ig"), " ").replace(/^[\s,·\-–—]+|[\s,·\-–—]+$/g, "").replace(/\s+/g, " ").trim();
        if (rem && /\d\s*(?:[xX×]|["″”'’]|\s*(?:cm|mm|in\b))/.test(rem)) sizeInfo = rem;
      }
      let sizeDesc = null;
      const _ex = [title, design, sizeInfo, it.variant_name].filter(Boolean).join(" ");
      if (it.size && !/slab|jumbo|standard|panel|prefab|countertop/i.test(_ex)) {
        const sd = (String(it.size).match(/\d+(?:\.\d+)?\s*[xX×]\s*\d+(?:\.\d+)?/) || [])[0];
        const nz = (x) => x.toLowerCase().replace(/["″”'’\s]/g, "").replace(/×/g, "x");
        if (sd && !nz(_ex).includes(nz(sd))) sizeDesc = String(it.size).trim();
      }
      ordered = [design, sizeInfo, sizeDesc, it.variant_name, accLabel];
    }
    const _normSeg = (s) => " " + String(s).toLowerCase().replace(/(\d)\.(\d)/g, "$1\xA7$2").replace(/[^a-z0-9§]+/g, " ").replace(/§/g, ".").replace(/(\d(?:\.\d+)?)\s*x\s*(?=\d)/g, "$1x").replace(/\bmou?ldings?\b/g, "mold").replace(/\s+/g, " ").trim() + " ";
    let _acc = _normSeg(title);
    const _segsWhole = /* @__PURE__ */ new Set([_normSeg(title)]);
    const _stripCovered = (d) => {
      let toks = String(d).trim().split(/\s+/);
      const canStrip = (phrase, k) => {
        const pn = _normSeg(phrase);
        if (!pn.trim() || !_acc.includes(pn)) return false;
        return k >= 2 || /\d/.test(phrase) || _segsWhole.has(pn);
      };
      let changed = true;
      outer: while (changed && toks.length > 1) {
        changed = false;
        for (let k = toks.length - 1; k >= 1; k--) {
          for (let i = 0; i + k <= toks.length; i++) {
            if (toks.length - k < 1) break;
            if (canStrip(toks.slice(i, i + k).join(" "), k)) {
              toks = toks.slice(0, i).concat(toks.slice(i + k));
              changed = true;
              continue outer;
            }
          }
        }
      }
      return toks.join(" ");
    };
    const _noSpace = (s) => _normSeg(s).replace(/ /g, "");
    const _segsNS = /* @__PURE__ */ new Set([_noSpace(title)]);
    const descriptors = [];
    for (const v of ordered) {
      if (!v) continue;
      const p = _normSeg(v);
      if (p.trim() === "" || _acc.includes(p) || _segsNS.has(_noSpace(v))) continue;
      const kept = _stripCovered(v).replace(/^[\s·\-–—,]+|[\s·\-–—,]+$/g, "").trim();
      const kp = _normSeg(kept);
      if (kp.trim() === "" || _acc.includes(kp) || _segsNS.has(_noSpace(kept))) continue;
      descriptors.push(kept);
      _acc += kp;
      _segsWhole.add(kp);
      _segsNS.add(_noSpace(kept));
    }
    return {
      brand,
      name: title + (descriptors.length ? "  \xB7  " + descriptors.join("  \xB7  ") : ""),
      sku
    };
  }
  function repFetch(path, options = {}) {
    const token = sessionStorage.getItem("rep_token");
    return fetch(API + path, {
      ...options,
      headers: {
        "Content-Type": "application/json",
        "X-Rep-Token": token || "",
        ...options.headers || {}
      }
    }).then((r) => {
      if (r.status === 401) {
        sessionStorage.removeItem("rep_token");
        sessionStorage.removeItem("rep_info");
        window.location.reload();
        throw new Error("Unauthorized");
      }
      const ct = r.headers.get("content-type") || "";
      if (!ct.includes("application/json")) {
        throw new Error("Server returned non-JSON (status " + r.status + ")");
      }
      return r.json().then((data) => {
        if (!r.ok) {
          const e = new Error(data.error || "Request failed (status " + r.status + ")");
          e.status = r.status;
          e.data = data;
          if (data.needs_force) e.needs_force = true;
          throw e;
        }
        return data;
      });
    });
  }
  async function valorTerminalCharge({ amount, orderRef }) {
    const data = await repFetch("/api/rep/valor/charge", {
      method: "POST",
      body: JSON.stringify({ amount: parseFloat(amount), order_ref: orderRef || void 0 })
    });
    if (!data || !data.card_txn) throw new Error(data && data.error || "Terminal charge failed");
    return data.card_txn;
  }
  function repOpenDoc(path, newTab) {
    const token = sessionStorage.getItem("rep_token");
    return fetch(API + path, {
      headers: { "X-Rep-Token": token || "" }
    }).then((r) => {
      if (r.status === 401) {
        sessionStorage.removeItem("rep_token");
        sessionStorage.removeItem("rep_info");
        window.location.reload();
        throw new Error("Unauthorized");
      }
      if (!r.ok) throw new Error("Failed to load document");
      return r.blob();
    }).then((blob) => {
      const url = URL.createObjectURL(blob);
      if (newTab) {
        window.open(url, "_blank");
        setTimeout(() => URL.revokeObjectURL(url), 6e4);
      }
      return url;
    });
  }
  function printDocUrl(url, revoke) {
    return new Promise((resolve) => {
      const frame = document.createElement("iframe");
      frame.style.cssText = "position:fixed;right:0;bottom:0;width:0;height:0;border:0;";
      const finish = () => {
        setTimeout(() => {
          frame.remove();
          if (revoke) URL.revokeObjectURL(url);
        }, 1e3);
        resolve();
      };
      frame.onload = () => {
        setTimeout(() => {
          try {
            frame.contentWindow.focus();
            frame.contentWindow.print();
          } catch (e) {
            window.open(url, "_blank");
          }
          finish();
        }, 400);
      };
      frame.src = url;
      document.body.appendChild(frame);
    });
  }
  function repUpload(path, formData) {
    const token = sessionStorage.getItem("rep_token");
    return fetch(API + path, {
      method: "POST",
      headers: { "X-Rep-Token": token || "" },
      body: formData
    }).then((r) => {
      if (r.status === 401) {
        sessionStorage.removeItem("rep_token");
        window.location.reload();
        throw new Error("Unauthorized");
      }
      return r.json().then((data) => {
        if (!r.ok) throw new Error(data.error || "Upload failed (status " + r.status + ")");
        return data;
      });
    });
  }
  let stripeInstance = null;
  const loadStripe = async () => {
    if (typeof Stripe === "undefined") return;
    try {
      const r = await fetch(API + "/api/config/stripe-key");
      const data = await r.json();
      if (data.key) stripeInstance = Stripe(data.key);
    } catch (e) {
    }
  };
  (window.requestIdleCallback || ((cb) => setTimeout(cb, 1500)))(loadStripe);
  const timeAgo = (dateStr) => {
    const now = /* @__PURE__ */ new Date();
    const d = new Date(dateStr);
    const diffMs = now - d;
    const mins = Math.floor(diffMs / 6e4);
    if (mins < 1) return "just now";
    if (mins < 60) return mins + "m ago";
    const hours = Math.floor(mins / 60);
    if (hours < 24) return hours + "h ago";
    const days = Math.floor(hours / 24);
    if (days < 7) return days + "d ago";
    return d.toLocaleDateString();
  };
  const isValidEmail = (v) => /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(v);
  const isValidPhone = (v) => v.replace(/\D/g, "").length === 10;
  const formatPhone = (val) => {
    let d = (val || "").replace(/\D/g, "");
    let cc = "";
    if (d.length > 10 && d[0] === "1") {
      cc = "+1 ";
      d = d.slice(1);
    }
    const ext = d.slice(10);
    d = d.slice(0, 10);
    let out;
    if (d.length < 4) out = d;
    else if (d.length < 7) out = "(" + d.slice(0, 3) + ") " + d.slice(3);
    else out = "(" + d.slice(0, 3) + ") " + d.slice(3, 6) + "-" + d.slice(6);
    return (cc + out + (ext ? " x" + ext : "")).trim();
  };
  const NOTIF_CATEGORIES = [
    {
      key: "orders",
      label: "Orders",
      tone: "#5c6e94",
      glyph: "\u259A",
      types: ["new_order", "order_created", "order_status_changed", "order_assigned", "delivery_method_changed", "item_added", "item_removed", "price_adjusted"]
    },
    {
      key: "payments",
      label: "Payments",
      tone: "#3a7a4e",
      glyph: "$",
      types: ["payment_received", "payment_failed", "payment_request_sent", "refund_issued"]
    },
    {
      key: "purchasing",
      label: "Purchasing",
      tone: "#a87935",
      glyph: "\u2318",
      types: ["po_approved", "po_items_received"]
    },
    {
      key: "samples",
      label: "Samples",
      tone: "#8a7e68",
      glyph: "\u2317",
      types: ["sample_request_created", "sample_request_shipped"]
    },
    {
      key: "inquiries",
      label: "Inquiries",
      tone: "#8a5c74",
      glyph: "\u2302",
      types: ["installation_inquiry", "visit_quote_request"]
    }
  ];
  const notifCategoryFor = (type) => NOTIF_CATEGORIES.find((c) => c.types.includes(type)) || { key: "other", label: "Update", tone: "#8a7e68", glyph: "\u25C9" };
  const NOTIF_URGENT_TYPES = ["payment_failed"];
  const notifDeepLink = (n) => {
    if (!n.entity_id) return null;
    if (n.entity_type === "order") return { view: "order-detail", label: "Order" };
    if (n.entity_type === "quote") return { view: "quote-edit", label: "Quote" };
    if (n.entity_type === "sample_request") return { view: "sample-detail", label: "Samples" };
    if (n.entity_type === "purchase_order") return { view: "purchase-order-detail", label: "PO" };
    if (n.entity_type === "visit") return { view: "visit-detail", label: "Visit" };
    if (n.entity_type === "customer") return { view: "customer-detail", label: "Customer", id: "retail_" + n.entity_id };
    return null;
  };
  const notifDayGroup = (dateStr) => {
    const d = new Date(dateStr);
    const now = /* @__PURE__ */ new Date();
    const startOfDay = (x) => new Date(x.getFullYear(), x.getMonth(), x.getDate());
    const dayDiff = Math.round((startOfDay(now) - startOfDay(d)) / 864e5);
    if (dayDiff <= 0) return "Today";
    if (dayDiff === 1) return "Yesterday";
    if (dayDiff < 7) return "This week";
    return "Earlier";
  };
  function useAddressAutofill({ fieldMap, setForm, selectedCustomer, isReadonly }) {
    const addressInputRef = useRef(null);
    const [previousAddresses, setPreviousAddresses] = useState([]);
    const [showPrevAddresses, setShowPrevAddresses] = useState(false);
    useEffect(() => {
      setPreviousAddresses([]);
      setShowPrevAddresses(false);
      if (!selectedCustomer || !selectedCustomer.email) return;
      let cancelled = false;
      repFetch("/api/rep/customers/" + encodeURIComponent(selectedCustomer.email) + "/addresses").then((data) => {
        if (!cancelled) setPreviousAddresses(data.addresses || []);
      }).catch(() => {
      });
      return () => {
        cancelled = true;
      };
    }, [selectedCustomer && selectedCustomer.email]);
    const selectPreviousAddress = (addr) => {
      setForm((f) => ({
        ...f,
        [fieldMap.line1]: addr.line1 || "",
        [fieldMap.line2]: addr.line2 || "",
        [fieldMap.city]: addr.city || "",
        [fieldMap.state]: addr.state || "",
        [fieldMap.zip]: addr.zip || ""
      }));
      setShowPrevAddresses(false);
    };
    return { addressInputRef, previousAddresses, showPrevAddresses, setShowPrevAddresses, selectPreviousAddress };
  }
  function TapToPayModal({ amount, onSuccess, onCancel }) {
    const [tapStatus, setTapStatus] = useState("initializing");
    const [tapError, setTapError] = useState("");
    const cancelledRef = useRef(false);
    const runCharge = async () => {
      setTapError("");
      try {
        setTapStatus("ready");
        const txn = await valorTerminalCharge({ amount });
        if (cancelledRef.current) return;
        setTapStatus("success");
        setTimeout(() => {
          if (!cancelledRef.current) onSuccess({ card_txn: txn });
        }, 800);
      } catch (err) {
        if (!cancelledRef.current) {
          setTapStatus("error");
          setTapError(err.message || "Payment failed");
        }
      }
    };
    useEffect(() => {
      cancelledRef.current = false;
      runCharge();
      return () => {
        cancelledRef.current = true;
      };
    }, []);
    const handleCancel = () => {
      cancelledRef.current = true;
      onCancel();
    };
    const handleRetry = () => {
      cancelledRef.current = false;
      runCharge();
    };
    const statusMessages = {
      initializing: "Connecting to terminal...",
      ready: "Charge sent \u2014 tap/dip card on the terminal",
      processing: "Processing...",
      success: "Payment successful!",
      error: tapError || "Payment failed"
    };
    const iconClass = tapStatus === "ready" ? "ready" : tapStatus === "processing" ? "processing" : tapStatus === "success" ? "success" : tapStatus === "error" ? "error" : "processing";
    return /* @__PURE__ */ React.createElement("div", { className: "tap-modal-overlay" }, /* @__PURE__ */ React.createElement("div", { className: "tap-modal" }, /* @__PURE__ */ React.createElement("h3", null, "Valor Terminal"), /* @__PURE__ */ React.createElement("div", { className: "tap-modal-amount" }, "$", parseFloat(amount).toFixed(2)), /* @__PURE__ */ React.createElement("div", { className: "tap-modal-icon " + iconClass }, tapStatus === "success" ? /* @__PURE__ */ React.createElement("svg", { width: "40", height: "40", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ React.createElement("polyline", { points: "20 6 9 17 4 12" })) : tapStatus === "error" ? /* @__PURE__ */ React.createElement("svg", { width: "40", height: "40", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ React.createElement("line", { x1: "18", y1: "6", x2: "6", y2: "18" }), /* @__PURE__ */ React.createElement("line", { x1: "6", y1: "6", x2: "18", y2: "18" })) : /* @__PURE__ */ React.createElement("svg", { width: "40", height: "40", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ React.createElement("path", { d: "M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" }), /* @__PURE__ */ React.createElement("path", { d: "M2 8.5c1.5-1 3.5-1.5 5-1 1 .3 1.8 1 2.5 1.8", opacity: "0.6" }), /* @__PURE__ */ React.createElement("path", { d: "M2 5.5c2.5-1.5 5.5-2 8-1 1.5.6 2.8 1.7 3.8 3", opacity: "0.4" }))), /* @__PURE__ */ React.createElement("div", { className: "tap-modal-status", style: { color: tapStatus === "error" ? "var(--red)" : tapStatus === "success" ? "var(--green)" : void 0 } }, statusMessages[tapStatus]), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.75rem", justifyContent: "center", marginTop: "1rem" } }, tapStatus === "error" && /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary btn-sm", onClick: handleRetry }, "Retry"), tapStatus !== "success" && /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary btn-sm", onClick: handleCancel }, "Cancel"))));
  }
  function ManualCardEntryModal({ amount, onSuccess, onCancel }) {
    const [cardStatus, setCardStatus] = useState("idle");
    const [cardError, setCardError] = useState("");
    const [cardComplete, setCardComplete] = useState(false);
    const [focused, setFocused] = useState(false);
    const [invalid, setInvalid] = useState(false);
    const cardElRef = useRef(null);
    const elementsRef = useRef(null);
    const mountRef = useRef(null);
    useEffect(() => {
      if (!stripeInstance || !mountRef.current) return;
      const elements = stripeInstance.elements();
      const card = elements.create("card", {
        disableLink: true,
        style: {
          base: {
            fontSize: "15px",
            fontFamily: "'Inter', sans-serif",
            color: "#292524",
            "::placeholder": { color: "#a8a29e" }
          },
          invalid: { color: "#dc2626" }
        },
        hidePostalCode: false
      });
      card.mount(mountRef.current);
      card.on("change", (event) => {
        setCardComplete(event.complete);
        setInvalid(!!event.error);
        setCardError(event.error ? event.error.message : "");
      });
      card.on("focus", () => setFocused(true));
      card.on("blur", () => setFocused(false));
      cardElRef.current = card;
      elementsRef.current = elements;
      return () => {
        card.destroy();
      };
    }, []);
    const handleSubmit = async () => {
      if (!cardComplete || !stripeInstance || !cardElRef.current) return;
      setCardStatus("processing");
      setCardError("");
      try {
        const piData = await repFetch("/api/rep/card/create-payment-intent", {
          method: "POST",
          body: JSON.stringify({ amount, order_description: "In-store manual card entry" })
        });
        if (!piData.client_secret) {
          setCardStatus("error");
          setCardError(piData.error || "Failed to create payment");
          return;
        }
        const result = await stripeInstance.confirmCardPayment(piData.client_secret, {
          payment_method: { card: cardElRef.current }
        });
        if (result.error) {
          setCardStatus("error");
          setCardError(result.error.message);
          return;
        }
        setCardStatus("success");
        setTimeout(() => onSuccess(piData.payment_intent_id), 600);
      } catch (err) {
        setCardStatus("error");
        setCardError(err.message || "Payment failed");
      }
    };
    const wrapperClass = "stripe-element-wrapper" + (focused ? " focused" : "") + (invalid ? " invalid" : "");
    return /* @__PURE__ */ React.createElement("div", { className: "card-entry-overlay", onClick: cardStatus !== "processing" ? onCancel : void 0 }, /* @__PURE__ */ React.createElement("div", { className: "card-entry-modal", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("h3", null, "Enter Card Details"), /* @__PURE__ */ React.createElement("div", { className: "amount" }, "$", parseFloat(amount).toFixed(2)), cardStatus === "success" ? /* @__PURE__ */ React.createElement("div", { style: { textAlign: "center", padding: "2rem 0" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 64, height: 64, borderRadius: "50%", background: "#dcfce7", color: "var(--green)", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 1rem" } }, /* @__PURE__ */ React.createElement("svg", { width: "32", height: "32", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ React.createElement("polyline", { points: "20 6 9 17 4 12" }))), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1rem", fontWeight: 600, color: "var(--green)" } }, "Payment successful!")) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: wrapperClass }, /* @__PURE__ */ React.createElement("div", { ref: mountRef })), /* @__PURE__ */ React.createElement("div", { className: "card-entry-error" }, cardError), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.75rem" } }, /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "btn btn-secondary",
        style: { flex: 1 },
        onClick: onCancel,
        disabled: cardStatus === "processing"
      },
      "Cancel"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "btn btn-primary",
        style: { flex: 1 },
        onClick: handleSubmit,
        disabled: !cardComplete || cardStatus === "processing"
      },
      cardStatus === "processing" ? "Processing..." : "Pay $" + parseFloat(amount).toFixed(2)
    )))));
  }
  function CardMethodChoiceModal({ amount, terminalConnected, onTap, onManual, onCancel }) {
    return /* @__PURE__ */ React.createElement("div", { className: "card-choice-overlay", onClick: onCancel }, /* @__PURE__ */ React.createElement("div", { className: "card-choice-modal", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("h3", null, "Card Payment"), /* @__PURE__ */ React.createElement("div", { className: "amount" }, "$", parseFloat(amount).toFixed(2)), /* @__PURE__ */ React.createElement("button", { className: "card-choice-btn", onClick: onTap, disabled: !terminalConnected }, /* @__PURE__ */ React.createElement("div", { className: "choice-icon tap", style: !terminalConnected ? { opacity: 0.4 } : {} }, /* @__PURE__ */ React.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ React.createElement("path", { d: "M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" }), /* @__PURE__ */ React.createElement("path", { d: "M2 8.5c1.5-1 3.5-1.5 5-1 1 .3 1.8 1 2.5 1.8", opacity: "0.6" }), /* @__PURE__ */ React.createElement("path", { d: "M2 5.5c2.5-1.5 5.5-2 8-1 1.5.6 2.8 1.7 3.8 3", opacity: "0.4" }))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "choice-label" }, "Valor Terminal"), /* @__PURE__ */ React.createElement("div", { className: "choice-desc" }, terminalConnected ? "Tap, insert, or swipe on the terminal" : "No terminal set up"))), /* @__PURE__ */ React.createElement("button", { className: "card-choice-btn", onClick: onManual }, /* @__PURE__ */ React.createElement("div", { className: "choice-icon key" }, /* @__PURE__ */ React.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ React.createElement("rect", { x: "1", y: "4", width: "22", height: "16", rx: "2", ry: "2" }), /* @__PURE__ */ React.createElement("line", { x1: "1", y1: "10", x2: "23", y2: "10" }))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "choice-label" }, "Enter Card Details"), /* @__PURE__ */ React.createElement("div", { className: "choice-desc" }, "Type in card number manually"))), /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", style: { width: "100%", marginTop: "0.25rem" }, onClick: onCancel }, "Cancel")));
  }
  function AchPaymentModal({ amount, customerName, customerEmail, onSuccess, onCancel }) {
    const [step, setStep] = useState(1);
    const [achStatus, setAchStatus] = useState("idle");
    const [achError, setAchError] = useState("");
    const [achName, setAchName] = useState(customerName || "");
    const [achEmail, setAchEmail] = useState(customerEmail || "");
    const [idFile, setIdFile] = useState(null);
    const [checkFile, setCheckFile] = useState(null);
    const [idPreview, setIdPreview] = useState(null);
    const [checkPreview, setCheckPreview] = useState(null);
    const [uploading, setUploading] = useState(false);
    const [uploadError, setUploadError] = useState("");
    const [documentIds, setDocumentIds] = useState([]);
    const handleFileSelect = (file, type) => {
      if (!file) return;
      if (type === "id") {
        setIdFile(file);
        if (file.type.startsWith("image/")) {
          const reader = new FileReader();
          reader.onload = (e) => setIdPreview(e.target.result);
          reader.readAsDataURL(file);
        } else {
          setIdPreview(null);
        }
      } else {
        setCheckFile(file);
        if (file.type.startsWith("image/")) {
          const reader = new FileReader();
          reader.onload = (e) => setCheckPreview(e.target.result);
          reader.readAsDataURL(file);
        } else {
          setCheckPreview(null);
        }
      }
    };
    const handleUploadDocs = async () => {
      if (!idFile || !checkFile) {
        setUploadError("Both files are required");
        return;
      }
      setUploading(true);
      setUploadError("");
      try {
        const idForm = new FormData();
        idForm.append("file", idFile);
        idForm.append("doc_type", "customer_id");
        const idResult = await repUpload("/api/rep/orders/documents/upload", idForm);
        if (idResult.error) throw new Error(idResult.error);
        const checkForm = new FormData();
        checkForm.append("file", checkFile);
        checkForm.append("doc_type", "check_photo");
        const checkResult = await repUpload("/api/rep/orders/documents/upload", checkForm);
        if (checkResult.error) throw new Error(checkResult.error);
        setDocumentIds([idResult.document_id, checkResult.document_id]);
        setStep(2);
      } catch (err) {
        setUploadError(err.message || "Upload failed");
      }
      setUploading(false);
    };
    const handleSubmit = async () => {
      if (!achName.trim() || !achEmail.trim()) {
        setAchError("Name and email are required");
        return;
      }
      setAchStatus("processing");
      setAchError("");
      try {
        const piData = await repFetch("/api/rep/ach/create-payment-intent", {
          method: "POST",
          body: JSON.stringify({ amount, customer_name: achName, customer_email: achEmail })
        });
        if (!piData.client_secret) {
          setAchStatus("error");
          setAchError(piData.error || "Failed to create payment");
          return;
        }
        const result = await stripeInstance.collectBankAccountForPayment({
          clientSecret: piData.client_secret,
          params: {
            payment_method_type: "us_bank_account",
            payment_method_data: {
              billing_details: { name: achName, email: achEmail }
            }
          },
          expand: ["payment_method"]
        });
        if (result.error) {
          setAchStatus("error");
          setAchError(result.error.message);
          return;
        }
        if (result.paymentIntent.status === "requires_confirmation") {
          const confirmResult = await stripeInstance.confirmUsBankAccountPayment(piData.client_secret);
          if (confirmResult.error) {
            setAchStatus("error");
            setAchError(confirmResult.error.message);
            return;
          }
        }
        setAchStatus("success");
        setTimeout(() => onSuccess(piData.payment_intent_id, documentIds), 600);
      } catch (err) {
        setAchStatus("error");
        setAchError(err.message || "Bank transfer setup failed");
      }
    };
    return /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: achStatus !== "processing" && !uploading ? onCancel : void 0 }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation(), style: { maxWidth: "480px" } }, /* @__PURE__ */ React.createElement("h3", null, "Check Payment \u2014 ACH Transfer"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1.25rem", fontWeight: 600, textAlign: "center", margin: "0.5rem 0 1rem" } }, "$", parseFloat(amount).toFixed(2)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", marginBottom: "1.25rem", justifyContent: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.35rem" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 24, height: 24, borderRadius: "50%", background: step >= 1 ? "var(--gold)" : "var(--stone-200)", color: step >= 1 ? "#fff" : "var(--stone-500)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "0.75rem", fontWeight: 600 } }, "1"), /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.75rem", fontWeight: step === 1 ? 600 : 400, color: step === 1 ? "var(--stone-800)" : "var(--stone-500)" } }, "Documents")), /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 1, background: "var(--stone-300)", alignSelf: "center" } }), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.35rem" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 24, height: 24, borderRadius: "50%", background: step >= 2 ? "var(--gold)" : "var(--stone-200)", color: step >= 2 ? "#fff" : "var(--stone-500)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "0.75rem", fontWeight: 600 } }, "2"), /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.75rem", fontWeight: step === 2 ? 600 : 400, color: step === 2 ? "var(--stone-800)" : "var(--stone-500)" } }, "Bank Account"))), achStatus === "success" ? /* @__PURE__ */ React.createElement("div", { style: { textAlign: "center", padding: "1.5rem 0" } }, /* @__PURE__ */ React.createElement("div", { style: { fontSize: "2.5rem", marginBottom: "0.5rem" } }, "\u2705"), /* @__PURE__ */ React.createElement("div", { style: { fontWeight: 600, color: "#166534" } }, "Bank account connected"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.8125rem", color: "#78716c", marginTop: "0.25rem" } }, "Payment will clear in 4-5 business days")) : step === 1 ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement("label", { style: { fontSize: "0.8125rem", fontWeight: 500, display: "block", marginBottom: "0.35rem" } }, "Customer ID Photo *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "file",
        accept: "image/*,.pdf",
        onChange: (e) => handleFileSelect(e.target.files[0], "id"),
        style: { fontSize: "0.8125rem", width: "100%" }
      }
    ), idPreview && /* @__PURE__ */ React.createElement("img", { src: idPreview, alt: "ID preview", style: { marginTop: "0.5rem", maxWidth: "100%", maxHeight: 120, borderRadius: 6, border: "1px solid var(--stone-200)" } }), idFile && !idPreview && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.35rem", fontSize: "0.75rem", color: "var(--stone-500)" } }, idFile.name)), /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement("label", { style: { fontSize: "0.8125rem", fontWeight: 500, display: "block", marginBottom: "0.35rem" } }, "Check Photo *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "file",
        accept: "image/*,.pdf",
        onChange: (e) => handleFileSelect(e.target.files[0], "check"),
        style: { fontSize: "0.8125rem", width: "100%" }
      }
    ), checkPreview && /* @__PURE__ */ React.createElement("img", { src: checkPreview, alt: "Check preview", style: { marginTop: "0.5rem", maxWidth: "100%", maxHeight: 120, borderRadius: 6, border: "1px solid var(--stone-200)" } }), checkFile && !checkPreview && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.35rem", fontSize: "0.75rem", color: "var(--stone-500)" } }, checkFile.name)), /* @__PURE__ */ React.createElement("div", { style: { padding: "0.5rem 0.75rem", background: "#eff6ff", border: "1px solid #93c5fd", borderRadius: "6px", fontSize: "0.8rem", color: "#1e40af", marginBottom: "1rem" } }, "Upload a photo of the customer's ID and the check before proceeding to bank account setup."), uploadError && /* @__PURE__ */ React.createElement("div", { style: { color: "#dc2626", fontSize: "0.8125rem", marginBottom: "0.75rem" } }, uploadError), /* @__PURE__ */ React.createElement("div", { className: "modal-actions" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: onCancel, disabled: uploading }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: handleUploadDocs, disabled: uploading || !idFile || !checkFile }, uploading ? "Uploading..." : "Continue"))) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "form-group", style: { marginBottom: "0.75rem" } }, /* @__PURE__ */ React.createElement("label", { style: { fontSize: "0.8125rem", fontWeight: 500 } }, "Account Holder Name"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: achName,
        onChange: (e) => setAchName(e.target.value),
        placeholder: "Full name on bank account",
        disabled: achStatus === "processing",
        style: { width: "100%", padding: "0.6rem 0.75rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem", marginTop: "0.25rem" }
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "form-group", style: { marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement("label", { style: { fontSize: "0.8125rem", fontWeight: 500 } }, "Email Address"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        value: achEmail,
        onChange: (e) => setAchEmail(e.target.value),
        placeholder: "customer@email.com",
        disabled: achStatus === "processing",
        style: { width: "100%", padding: "0.6rem 0.75rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem", marginTop: "0.25rem" }
      }
    )), /* @__PURE__ */ React.createElement("div", { style: { padding: "0.5rem 0.75rem", background: "#eff6ff", border: "1px solid #93c5fd", borderRadius: "6px", fontSize: "0.8rem", color: "#1e40af", marginBottom: "1rem" } }, "Stripe will securely collect bank account details. Payment typically clears in 4-5 business days."), achError && /* @__PURE__ */ React.createElement("div", { style: { color: "#dc2626", fontSize: "0.8125rem", marginBottom: "0.75rem" } }, achError), /* @__PURE__ */ React.createElement("div", { className: "modal-actions" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setStep(1), disabled: achStatus === "processing" }, "Back"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: handleSubmit, disabled: achStatus === "processing" || !achName.trim() || !achEmail.trim() }, achStatus === "processing" ? "Connecting..." : "Connect Bank Account")))));
  }
  const repToastListeners = /* @__PURE__ */ new Set();
  function repToast(message) {
    repToastListeners.forEach((fn) => {
      try {
        fn(message);
      } catch (e) {
      }
    });
  }
  function RepToast() {
    const [msg, setMsg] = useState(null);
    const timer = useRef(null);
    useEffect(() => {
      const fn = (m) => {
        setMsg(m);
        if (timer.current) clearTimeout(timer.current);
        timer.current = setTimeout(() => setMsg(null), 2600);
      };
      repToastListeners.add(fn);
      return () => {
        repToastListeners.delete(fn);
        if (timer.current) clearTimeout(timer.current);
      };
    }, []);
    if (!msg) return null;
    return /* @__PURE__ */ React.createElement("div", { style: {
      position: "fixed",
      left: "50%",
      bottom: 92,
      transform: "translateX(-50%)",
      zIndex: 4e3,
      display: "flex",
      alignItems: "center",
      gap: 8,
      background: "#1c1917",
      color: "#ece5d8",
      padding: "11px 20px",
      boxShadow: "0 8px 30px rgba(28,25,23,0.28)",
      pointerEvents: "none",
      font: "500 10px/1 Inter, sans-serif",
      letterSpacing: "0.14em",
      textTransform: "uppercase"
    } }, /* @__PURE__ */ React.createElement("span", { style: { color: "#7fb98f", fontSize: 12 } }, "\u2713"), msg);
  }
  function App() {
    const [authed, setAuthed] = useState(false);
    const [checking, setChecking] = useState(true);
    useEffect(() => {
      const token = sessionStorage.getItem("rep_token");
      if (!token) {
        setChecking(false);
        return;
      }
      repFetch("/api/rep/me").then((data) => {
        if (data.rep) {
          sessionStorage.setItem("rep_info", JSON.stringify(data.rep));
          setAuthed(true);
        }
        setChecking(false);
      }).catch(() => {
        sessionStorage.removeItem("rep_token");
        sessionStorage.removeItem("rep_info");
        setChecking(false);
      });
    }, []);
    const handleLogin = (token, rep) => {
      sessionStorage.setItem("rep_token", token);
      sessionStorage.setItem("rep_info", JSON.stringify(rep));
      setAuthed(true);
    };
    const handleLogout = async () => {
      try {
        await repFetch("/api/rep/logout", { method: "POST" });
      } catch (e) {
      }
      sessionStorage.removeItem("rep_token");
      sessionStorage.removeItem("rep_info");
      setAuthed(false);
    };
    if (checking) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading...");
    if (!authed) return /* @__PURE__ */ React.createElement(LoginView, { onLogin: handleLogin });
    return /* @__PURE__ */ React.createElement(RepLayout, { onLogout: handleLogout });
  }
  function LoginView({ onLogin }) {
    const resetTokenFromUrl = React.useMemo(
      () => new URLSearchParams(window.location.search).get("reset_token"),
      []
    );
    const [mode, setMode] = useState(resetTokenFromUrl ? "reset" : "login");
    const [email, setEmail] = useState("");
    const [password, setPassword] = useState("");
    const [newPassword, setNewPassword] = useState("");
    const [confirmPassword, setConfirmPassword] = useState("");
    const [error, setError] = useState("");
    const [notice, setNotice] = useState("");
    const [loading, setLoading] = useState(false);
    const switchMode = (next) => {
      setMode(next);
      setError("");
      setNotice("");
      setPassword("");
      setNewPassword("");
      setConfirmPassword("");
    };
    const clearResetTokenFromUrl = () => {
      window.history.replaceState({}, "", window.location.pathname);
    };
    const handleSubmit = async (e) => {
      e.preventDefault();
      setError("");
      setLoading(true);
      try {
        const resp = await fetch(API + "/api/rep/login", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ email, password })
        });
        const data = await resp.json();
        if (!resp.ok) throw new Error(data.error || "Login failed");
        onLogin(data.token, data.rep);
      } catch (err) {
        setError(err.message);
        setLoading(false);
      }
    };
    const handleForgot = async (e) => {
      e.preventDefault();
      setError("");
      setNotice("");
      setLoading(true);
      try {
        const resp = await fetch(API + "/api/rep/forgot-password", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ email })
        });
        if (!resp.ok) throw new Error("Something went wrong. Try again.");
        setNotice("If that email belongs to a rep account, a reset link is on its way. Check your inbox \u2014 the link expires in 1 hour.");
      } catch (err) {
        setError(err.message);
      }
      setLoading(false);
    };
    const handleReset = async (e) => {
      e.preventDefault();
      setError("");
      if (newPassword.length < 8 || !/[A-Z]/.test(newPassword) || !/[0-9]/.test(newPassword)) {
        setError("Password must be at least 8 characters with 1 uppercase letter and 1 number.");
        return;
      }
      if (newPassword !== confirmPassword) {
        setError("Passwords don\u2019t match.");
        return;
      }
      setLoading(true);
      try {
        const resp = await fetch(API + "/api/rep/reset-password", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ token: resetTokenFromUrl, new_password: newPassword })
        });
        const data = await resp.json();
        if (!resp.ok) throw new Error(data.error || "Reset failed");
        clearResetTokenFromUrl();
        switchMode("login");
        setNotice("Password updated. Sign in with your new password.");
      } catch (err) {
        setError(err.message);
      }
      setLoading(false);
    };
    const formHandler = mode === "forgot" ? handleForgot : mode === "reset" ? handleReset : handleSubmit;
    return /* @__PURE__ */ React.createElement("div", { className: "rep-auth" }, /* @__PURE__ */ React.createElement("form", { className: "rep-auth-form", onSubmit: formHandler }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-header" }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", gap: 12 } }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-wordmark" }, "Roma"), /* @__PURE__ */ React.createElement("span", { className: "rep-auth-badge" }, "Staff portal")), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-version" }, "Internal")), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-body" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-body-inner" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-eyebrow" }, "Sales rep access"), /* @__PURE__ */ React.createElement("h1", { className: "rep-auth-title" }, mode === "forgot" ? "Reset your password" : mode === "reset" ? "Set a new password" : "Rep sign in")), error && /* @__PURE__ */ React.createElement("div", { className: "rep-auth-error" }, error), notice && /* @__PURE__ */ React.createElement("div", { className: "rep-auth-error", style: { color: "#7dd3a8", borderColor: "rgba(125,211,168,0.4)" } }, notice), mode === "login" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-fields" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-label" }, "Email address"), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-row" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        placeholder: "you@romaflooringdesigns.com",
        value: email,
        onChange: (e) => setEmail(e.target.value),
        required: true,
        autoComplete: "email"
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-label" }, "Password"), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-row" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "password",
        placeholder: "Enter password",
        value: password,
        onChange: (e) => setPassword(e.target.value),
        required: true,
        autoComplete: "current-password"
      }
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        className: "rep-auth-field-hint",
        style: { cursor: "pointer" },
        onClick: () => switchMode("forgot")
      },
      "Forgot password?"
    )))), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-actions" }, /* @__PURE__ */ React.createElement("button", { className: "rep-auth-cta", type: "submit", disabled: loading }, loading ? "Signing in..." : "Sign in to dashboard \u2192")), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-security" }, /* @__PURE__ */ React.createElement("span", { className: "rep-auth-security-icon" }, "!"), /* @__PURE__ */ React.createElement("span", null, "Roma monitors staff sign-ins. Don't share credentials. Locked out? Use ", /* @__PURE__ */ React.createElement("span", { style: { color: "#ece5d8", cursor: "pointer" }, onClick: () => switchMode("forgot") }, "Forgot password"), " or contact your admin."))), mode === "forgot" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-fields" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-label" }, "Email address"), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-row" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        placeholder: "you@romaflooringdesigns.com",
        value: email,
        onChange: (e) => setEmail(e.target.value),
        required: true,
        autoComplete: "email"
      }
    )))), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-actions" }, /* @__PURE__ */ React.createElement("button", { className: "rep-auth-cta", type: "submit", disabled: loading }, loading ? "Sending..." : "Email me a reset link \u2192")), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-security" }, /* @__PURE__ */ React.createElement("span", { className: "rep-auth-security-icon" }, "?"), /* @__PURE__ */ React.createElement("span", null, "We\u2019ll email a one-hour link to reset your password.", " ", /* @__PURE__ */ React.createElement("span", { style: { color: "#ece5d8", cursor: "pointer" }, onClick: () => switchMode("login") }, "\u2190 Back to sign in")))), mode === "reset" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-fields" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-label" }, "New password"), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-row" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "password",
        placeholder: "At least 8 characters",
        value: newPassword,
        onChange: (e) => setNewPassword(e.target.value),
        required: true,
        autoComplete: "new-password"
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-label" }, "Confirm new password"), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-field-row" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "password",
        placeholder: "Re-enter new password",
        value: confirmPassword,
        onChange: (e) => setConfirmPassword(e.target.value),
        required: true,
        autoComplete: "new-password"
      }
    )))), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-actions" }, /* @__PURE__ */ React.createElement("button", { className: "rep-auth-cta", type: "submit", disabled: loading }, loading ? "Saving..." : "Set new password \u2192")), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-security" }, /* @__PURE__ */ React.createElement("span", { className: "rep-auth-security-icon" }, "!"), /* @__PURE__ */ React.createElement("span", null, "Use 8+ characters with an uppercase letter and a number.", " ", /* @__PURE__ */ React.createElement("span", { style: { color: "#ece5d8", cursor: "pointer" }, onClick: () => {
      clearResetTokenFromUrl();
      switchMode("login");
    } }, "\u2190 Back to sign in")))))), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-footer" }, /* @__PURE__ */ React.createElement("span", null, "\xA9 2026 Roma Flooring Designs \xB7 Staff systems"), /* @__PURE__ */ React.createElement("a", { href: "/signin" }, "\u2190 Customer sign in"))), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-panel" }, /* @__PURE__ */ React.createElement("div", { className: "rep-auth-panel-bg" }), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-panel-overlay" }), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-panel-eyebrow" }, "Roma Flooring \xB7 Sales portal"), /* @__PURE__ */ React.createElement("div", { style: { maxWidth: 540 } }, /* @__PURE__ */ React.createElement("h2", { className: "rep-auth-panel-headline" }, "Your book, ", /* @__PURE__ */ React.createElement("em", null, "your numbers"), ", one login."), /* @__PURE__ */ React.createElement("p", { className: "rep-auth-panel-sub" }, "Quotes, estimates, orders, commissions, and your full customer book \u2014 all in one place. Manage sample requests, schedule visits, and process payments from the showroom floor."), /* @__PURE__ */ React.createElement("div", { className: "rep-auth-panel-attr" }, "Catalog \xB7 Orders \xB7 Quotes \xB7 Commissions"))));
  }
  const TA = {
    paper: "#f0eee9",
    card: "#f7f5f1",
    ink: "#1c1917",
    warm: "#e8e3da",
    accent: "#a87935",
    good: "#3a7a4e",
    hold: "#a87935",
    bad: "#c54731",
    serif: "'Cormorant Garamond', serif",
    sans: "'Inter', sans-serif",
    mono: "ui-monospace, monospace"
  };
  const tInk = (a) => `rgba(28,25,23,${a})`;
  const TA_CHK = {
    pass: { c: TA.good, g: "\u2713", t: "ON FILE" },
    wait: { c: TA.hold, g: "\xB7", t: "REVIEW" },
    none: { c: "rgba(28,25,23,0.35)", g: "\u2014", t: "NONE" }
  };
  function taAge(iso) {
    if (!iso) return "";
    const h = Math.floor((Date.now() - new Date(iso).getTime()) / 36e5);
    if (h < 1) return "just now";
    if (h < 24) return h + " hr" + (h > 1 ? "s" : "");
    const d = Math.floor(h / 24);
    return d + " day" + (d > 1 ? "s" : "");
  }
  const taMoney = (n) => "$" + (parseFloat(n) || 0).toLocaleString(void 0, { maximumFractionDigits: 0 });
  const taPct = (v) => `${parseFloat(v)}%`;
  const taDate = (d) => d ? new Date(d).toLocaleDateString(void 0, { month: "short", day: "numeric", year: "numeric" }) : "\u2014";
  function taRec(c, lowestTier) {
    if (!(c.ein || c.contractor_license)) return { key: "info", label: "NEEDS DOCUMENTS", c: TA.hold };
    return { key: "approve", label: "APPROVE \xB7 " + (lowestTier ? lowestTier.name : "Silver"), c: TA.good };
  }
  function RepTradeApplicationsView({ navigate }) {
    const rootRef = useRef(null);
    const [h, setH] = useState(560);
    const [customers, setCustomers] = useState([]);
    const [tiers, setTiers] = useState([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);
    const [sel, setSel] = useState(null);
    const [tierChoice, setTierChoice] = useState(null);
    const [docs, setDocs] = useState([]);
    const [docsLoading, setDocsLoading] = useState(false);
    const [note, setNote] = useState("");
    const [taxExempt, setTaxExempt] = useState(false);
    const [busy, setBusy] = useState(false);
    React.useLayoutEffect(() => {
      const content = rootRef.current && rootRef.current.closest(".rep-content");
      const layout = rootRef.current && rootRef.current.closest(".rep-layout");
      const prevContentMin = content ? content.style.minHeight : "";
      const prevLayout = layout ? { minHeight: layout.style.minHeight, height: layout.style.height, overflow: layout.style.overflow } : null;
      if (content) content.style.minHeight = "0";
      const measure = () => {
        if (layout) {
          layout.style.minHeight = "0";
          layout.style.overflow = "hidden";
          const top = layout.getBoundingClientRect().top;
          layout.style.height = Math.max(420, window.innerHeight - top) + "px";
        }
        if (rootRef.current) setH(Math.max(420, window.innerHeight - rootRef.current.getBoundingClientRect().top));
      };
      measure();
      window.addEventListener("resize", measure);
      return () => {
        window.removeEventListener("resize", measure);
        if (content) content.style.minHeight = prevContentMin;
        if (layout && prevLayout) {
          layout.style.minHeight = prevLayout.minHeight;
          layout.style.height = prevLayout.height;
          layout.style.overflow = prevLayout.overflow;
        }
      };
    }, []);
    const load = () => {
      setLoading(true);
      Promise.all([
        repFetch("/api/rep/trade-customers?status=pending"),
        repFetch("/api/rep/trade-tiers")
      ]).then(([cs, ts]) => {
        const list = cs.customers || [];
        setCustomers(list);
        setTiers(ts.tiers || []);
        setSel((s) => s && list.some((c) => c.id === s) ? s : null);
        setLoading(false);
      }).catch((e) => {
        setError(e.message);
        setLoading(false);
      });
    };
    useEffect(load, []);
    useEffect(() => {
      setTierChoice(null);
      setNote("");
      setDocs([]);
      setTaxExempt(false);
      if (!sel) return;
      setDocsLoading(true);
      repFetch("/api/rep/trade-customers/" + sel + "/documents").then((d) => {
        setDocs(d.documents || []);
        setDocsLoading(false);
      }).catch(() => setDocsLoading(false));
    }, [sel]);
    const lowestTier = tiers[0] || null;
    const a = customers.find((c) => c.id === sel) || null;
    const rec = a ? taRec(a, lowestTier) : null;
    const recTierId = rec && rec.key === "approve" && lowestTier ? lowestTier.id : "none";
    const chosen = tierChoice === null ? recTierId : tierChoice;
    const chosenTier = chosen === "none" ? null : tiers.find((t) => t.id === chosen);
    const decide = (verdict) => {
      if (!a || busy) return;
      setBusy(true);
      const path = verdict === "approve" ? "/approve" : "/deny";
      const body = verdict === "approve" ? { margin_tier_id: chosen, tax_exempt: taxExempt } : { denial_reason: note };
      repFetch("/api/rep/trade-customers/" + a.id + path, { method: "POST", body: JSON.stringify(body) }).then(() => {
        setBusy(false);
        setCustomers((prev) => prev.filter((c) => c.id !== a.id));
        setSel(null);
      }).catch((e) => {
        setBusy(false);
        alert(e.message);
      });
    };
    const hasResale = docs.some((d) => /resale|resell|seller/i.test((d.doc_type || "") + " " + (d.file_name || "")));
    const effSpend = a ? parseFloat(a.prior_spend != null ? a.prior_spend : a.total_spend) || 0 : 0;
    const checks = a ? [
      ["EIN / Tax ID", a.ein ? "pass" : "none", a.ein || "Not provided"],
      ["Contractor license", a.contractor_license ? "pass" : "none", a.contractor_license || "Not provided"],
      ["Resale certificate", hasResale ? "pass" : docs.length ? "wait" : "none", hasResale ? "Document uploaded" : docs.length ? "Not clearly labeled" : "No document"],
      ["Business documents", docs.length ? "pass" : "none", docs.length ? docs.length + " uploaded" : "None uploaded"],
      ["Business address", a.address_line1 || a.city ? "pass" : "none", [a.city, a.state].filter(Boolean).join(", ") || "Not provided"],
      ["Prior spend with Roma", effSpend > 0 ? "pass" : "none", effSpend > 0 ? taMoney(effSpend) + " trailing 12 mo" + (a.from_retail_upgrade ? " (rolls over)" : "") : "No order history"]
    ] : [];
    const passCount = checks.filter((c) => c[1] === "pass").length;
    const submitted = a ? [
      ["Business name", a.company_name || "\u2014"],
      ["Contact", a.contact_name || "\u2014"],
      ["Email", a.email || "\u2014"],
      ["Phone", a.phone || "Not provided"],
      ["Business type", a.business_type ? a.business_type.replace(/_/g, " ") : "Not stated"],
      ["EIN", a.ein || "Not provided"],
      ["License", a.contractor_license || "Not provided"],
      ["Address", [a.address_line1, [a.city, a.state].filter(Boolean).join(", "), a.zip].filter(Boolean).join(" \xB7 ") || "Not provided"],
      ["Applied", taDate(a.created_at)],
      ["Account", a.from_retail_upgrade ? "Upgraded from retail" + (a.retail_customer_since ? " \xB7 customer since " + taDate(a.retail_customer_since) : "") : "New applicant"],
      ["Trailing spend", taMoney(effSpend) + (a.from_retail_upgrade && effSpend > 0 ? " \xB7 rolls into tier" : "")]
    ] : [];
    const strip = { display: "flex", alignItems: "center", gap: 16, padding: "0 26px", height: 46, background: TA.card, borderBottom: `0.5px solid ${tInk(0.14)}`, flexShrink: 0 };
    return /* @__PURE__ */ React.createElement("div", { ref: rootRef, style: { margin: "-32px -40px -60px", height: h, display: "flex", flexDirection: "column", background: TA.paper, color: TA.ink, fontFamily: TA.sans, overflow: "hidden" } }, /* @__PURE__ */ React.createElement("div", { style: strip }, a ? /* @__PURE__ */ React.createElement("button", { onClick: () => setSel(null), style: { display: "flex", alignItems: "center", gap: 8, background: "none", border: "none", cursor: "pointer", padding: 0, font: `500 10px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: TA.accent } }, /* @__PURE__ */ React.createElement("span", { style: { font: `500 14px/1 ${TA.sans}` } }, "\u2190"), " All applications") : /* @__PURE__ */ React.createElement("span", { style: { font: `500 10px/1 ${TA.mono}`, letterSpacing: "0.18em", textTransform: "uppercase", color: TA.accent } }, "Trade applications"), /* @__PURE__ */ React.createElement("span", { style: { marginLeft: "auto", font: `500 10px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: tInk(0.5) } }, a ? a.company_name || a.contact_name : `${customers.length} awaiting review`)), error ? /* @__PURE__ */ React.createElement("div", { style: { padding: 30, color: TA.bad, font: `400 14px ${TA.sans}` } }, error) : loading ? /* @__PURE__ */ React.createElement("div", { style: { padding: 30, color: tInk(0.5), font: `400 14px ${TA.sans}` } }, "Loading applications", "\u2026") : a ? /* @__PURE__ */ React.createElement(TaReview, { a, rec, tiers, chosen, chosenTier, recTierId, setTierChoice, checks, passCount, submitted, docs, docsLoading, note, setNote, taxExempt, setTaxExempt, hasResale, busy, decide }) : customers.length === 0 ? /* @__PURE__ */ React.createElement("div", { style: { flex: 1, display: "grid", placeItems: "center", padding: 40, textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: `300 30px/1.3 ${TA.serif}`, color: TA.ink } }, "Queue clear."), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 10, font: `500 10px/1.6 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: tInk(0.45) } }, "No applications awaiting review"))) : /* @__PURE__ */ React.createElement(TaListPage, { list: customers, setSel, lowestTier }));
  }
  function TaListPage({ list, setSel, lowestTier }) {
    const COLS = "104px minmax(0,1fr) 132px 108px 92px";
    const hCell = { font: `500 9px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: tInk(0.4) };
    return /* @__PURE__ */ React.createElement("div", { style: { flex: 1, overflow: "auto", minHeight: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "26px 0 48px" } }, /* @__PURE__ */ React.createElement("div", { className: "ta-hide", style: { display: "grid", gridTemplateColumns: COLS, gap: 18, padding: "0 20px 11px" } }, /* @__PURE__ */ React.createElement("span", { style: hCell }, "Status"), /* @__PURE__ */ React.createElement("span", { style: hCell }, "Business"), /* @__PURE__ */ React.createElement("span", { style: hCell }, "Type"), /* @__PURE__ */ React.createElement("span", { style: { ...hCell, textAlign: "right" } }, "Trailing spend"), /* @__PURE__ */ React.createElement("span", { style: { ...hCell, textAlign: "right" } }, "Waiting")), /* @__PURE__ */ React.createElement("div", { style: { border: `0.5px solid ${tInk(0.12)}`, background: TA.card } }, list.map((a, i) => {
      const rec = taRec(a, lowestTier);
      const ready = rec.key === "approve";
      const loc = [a.city, a.state].filter(Boolean).join(", ");
      return /* @__PURE__ */ React.createElement("button", { key: a.id, onClick: () => setSel(a.id), className: "ta-row", style: {
        display: "grid",
        gridTemplateColumns: COLS,
        gap: 18,
        alignItems: "center",
        width: "100%",
        textAlign: "left",
        cursor: "pointer",
        padding: "16px 20px",
        background: "transparent",
        border: "none",
        borderTop: i === 0 ? "none" : `0.5px solid ${tInk(0.09)}`
      } }, /* @__PURE__ */ React.createElement("span", { className: "ta-hide", style: { display: "flex", alignItems: "center", gap: 8, minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { width: 7, height: 7, borderRadius: "50%", background: rec.c, flexShrink: 0 } }), /* @__PURE__ */ React.createElement("span", { style: { font: `500 9px/1.3 ${TA.mono}`, letterSpacing: "0.1em", textTransform: "uppercase", color: tInk(0.6) } }, ready ? "Ready" : "Docs")), /* @__PURE__ */ React.createElement("span", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", gap: 8, minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: `400 17px/1.25 ${TA.serif}`, color: TA.ink, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, a.company_name || a.contact_name), a.from_retail_upgrade && /* @__PURE__ */ React.createElement("span", { title: "Upgraded from an existing retail account", style: { flexShrink: 0, padding: "2px 6px", borderRadius: 3, background: tInk(0.06), font: `500 8px/1.4 ${TA.mono}`, letterSpacing: "0.09em", textTransform: "uppercase", color: TA.accent } }, "Existing customer")), /* @__PURE__ */ React.createElement("span", { style: { display: "block", marginTop: 3, font: `400 12px/1.4 ${TA.sans}`, color: tInk(0.55), overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, a.contact_name || "\u2014", loc ? " \xB7 " + loc : "")), /* @__PURE__ */ React.createElement("span", { className: "ta-hide", style: { font: `500 9px/1.5 ${TA.mono}`, letterSpacing: "0.1em", textTransform: "uppercase", color: tInk(0.55), overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, (a.business_type || "Trade").replace(/_/g, " ")), /* @__PURE__ */ React.createElement("span", { className: "ta-hide", style: { textAlign: "right", font: `400 15px/1.3 ${TA.serif}`, color: (parseFloat(a.prior_spend != null ? a.prior_spend : a.total_spend) || 0) > 0 ? TA.ink : tInk(0.4) } }, (parseFloat(a.prior_spend != null ? a.prior_spend : a.total_spend) || 0) > 0 ? taMoney(parseFloat(a.prior_spend != null ? a.prior_spend : a.total_spend)) : "New"), /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 11 } }, /* @__PURE__ */ React.createElement("span", { style: { font: `500 9px/1.3 ${TA.mono}`, letterSpacing: "0.08em", textTransform: "uppercase", color: tInk(0.4), whiteSpace: "nowrap" } }, taAge(a.created_at)), /* @__PURE__ */ React.createElement("span", { style: { color: tInk(0.35), font: `400 17px/1 ${TA.sans}` } }, "\u203A")));
    }))));
  }
  function TaReview(props) {
    const { a, rec, tiers, chosen, chosenTier, recTierId, setTierChoice, checks, passCount, submitted, docs, docsLoading, note, setNote, taxExempt, setTaxExempt, hasResale, busy, decide } = props;
    const isDecline = chosen === "none";
    const stats = [["Applied", taDate(a.created_at)], ["Trailing spend", taMoney(parseFloat(a.prior_spend != null ? a.prior_spend : a.total_spend) || 0)], ["Documents", String(docs.length)]];
    const readText = rec.key === "info" ? "No EIN or contractor license is on file yet. Request the missing documents before granting trade pricing \u2014 the account approves cleanly once identity is verified." : `Identity is on file and ${passCount} of ${checks.length} checks are satisfied. New trade accounts start at ${(tiers[0] || {}).name || "the base tier"} (${taPct((tiers[0] || {}).discount_percent || 0)} off); the tier rises automatically with trailing 12-month spend.`;
    const simFacts = isDecline ? [["Discount", "None \u2014 retail pricing"], ["Outcome", "No trade account created"], ["Note", "Applicant is emailed your reason"]] : [["Discount", taPct(chosenTier.discount_percent) + " off list"], ["Tier reached at", parseFloat(chosenTier.spend_threshold) > 0 ? taMoney(chosenTier.spend_threshold) + " / yr" : "Starting tier"], ["Progression", "Auto-adjusts on trailing 12-mo spend"]];
    return /* @__PURE__ */ React.createElement("section", { style: { flex: 1, display: "flex", flexDirection: "column", minWidth: 0, minHeight: 0, overflow: "hidden" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 30px 18px", borderBottom: `0.5px solid ${tInk(0.12)}`, flexShrink: 0, display: "flex", gap: 24, alignItems: "flex-start", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 14, font: `500 10px/1 ${TA.mono}`, letterSpacing: "0.16em", textTransform: "uppercase" } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", gap: 8, color: tInk(0.75) } }, /* @__PURE__ */ React.createElement("span", { style: { width: 7, height: 7, borderRadius: "50%", background: rec.c } }), rec.key === "approve" ? "Ready to approve" : "Needs documents"), /* @__PURE__ */ React.createElement("span", { style: { color: tInk(0.4) } }, "submitted ", taAge(a.created_at), " ago"), a.from_retail_upgrade && /* @__PURE__ */ React.createElement("span", { title: "Upgraded from an existing retail account", style: { display: "flex", alignItems: "center", gap: 6, padding: "2px 8px", borderRadius: 3, background: tInk(0.06), color: TA.accent } }, /* @__PURE__ */ React.createElement("span", { style: { width: 5, height: 5, borderRadius: "50%", background: TA.accent } }), "Existing customer", a.retail_customer_since ? " since " + taDate(a.retail_customer_since) : "")), /* @__PURE__ */ React.createElement("h1", { style: { margin: "13px 0 0", font: `300 34px/1 ${TA.serif}`, letterSpacing: "-0.018em" } }, a.company_name || a.contact_name), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 9, font: `400 14px/1.5 ${TA.sans}`, color: tInk(0.68) } }, a.contact_name, a.business_type ? " \xB7 " + a.business_type.replace(/_/g, " ") : "", a.city || a.state ? " \xB7 " + [a.city, a.state].filter(Boolean).join(", ") : "")), /* @__PURE__ */ React.createElement("div", { style: { marginLeft: "auto", display: "flex", gap: 26, flexShrink: 0 } }, stats.map((k) => /* @__PURE__ */ React.createElement("div", { key: k[0], style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: `500 9px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: tInk(0.45) } }, k[0]), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 8, font: `300 22px/1 ${TA.serif}` } }, k[1]))))), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, overflow: "auto", minHeight: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 30px 0" } }, /* @__PURE__ */ React.createElement("div", { style: { background: TA.warm, borderLeft: `3px solid ${rec.c}`, padding: "16px 20px 17px" } }, /* @__PURE__ */ React.createElement("div", { style: { font: `500 10px/1 ${TA.mono}`, letterSpacing: "0.18em", textTransform: "uppercase", color: rec.c } }, "Recommendation"), /* @__PURE__ */ React.createElement("p", { style: { margin: "11px 0 0", maxWidth: "68ch", font: `400 15px/1.6 ${TA.sans}`, textWrap: "pretty" } }, readText))), /* @__PURE__ */ React.createElement(TaBlock, { label: "What they get", note: chosen === recTierId ? "recommended" : "overridden" }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, flexWrap: "wrap" } }, [{ id: "none", name: "Decline", discount_percent: null }].concat(tiers).map((t) => {
      const on = t.id === chosen;
      return /* @__PURE__ */ React.createElement("button", { key: t.id, onClick: () => setTierChoice(t.id), style: {
        padding: "11px 16px",
        cursor: "pointer",
        textAlign: "left",
        background: on ? TA.ink : "transparent",
        color: on ? TA.paper : TA.ink,
        border: on ? "none" : `0.5px solid ${tInk(0.22)}`
      } }, /* @__PURE__ */ React.createElement("div", { style: { font: `500 11px/1 ${TA.mono}`, letterSpacing: "0.12em", textTransform: "uppercase" } }, t.name), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 6, font: `400 13px/1 ${TA.sans}`, opacity: on ? 0.85 : 0.6 } }, t.discount_percent === null ? "Retail pricing" : taPct(t.discount_percent) + " off"));
    })), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 16, display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(180px,1fr))", gap: 1, background: tInk(0.1) } }, simFacts.map((k) => /* @__PURE__ */ React.createElement("div", { key: k[0], style: { background: TA.card, padding: "12px 14px" } }, /* @__PURE__ */ React.createElement("div", { style: { font: `500 9px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: tInk(0.45) } }, k[0]), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 7, font: `400 13px/1.4 ${TA.sans}` } }, k[1])))), chosenTier && tiers[0] && chosenTier.tier_level > tiers[0].tier_level && /* @__PURE__ */ React.createElement("p", { style: { margin: "14px 0 0", maxWidth: "64ch", font: `400 13px/1.6 ${TA.sans}`, color: TA.bad, textWrap: "pretty" } }, "Above the recommended starting tier. Granting ", taPct(chosenTier.discount_percent), " before any order history exposes the full margin up front \u2014 the spend-based ladder reaches it automatically.")), /* @__PURE__ */ React.createElement(TaBlock, { label: `Verification \xB7 ${passCount} of ${checks.length} on file` }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 1, background: tInk(0.09) } }, checks.map((c) => {
      const k = TA_CHK[c[1]];
      return /* @__PURE__ */ React.createElement("div", { key: c[0], style: { background: TA.card, padding: "12px 14px", display: "grid", gridTemplateColumns: "20px minmax(120px,1fr) minmax(140px,1.4fr) 84px", gap: 14, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { width: 18, height: 18, borderRadius: "50%", border: `1px solid ${k.c}`, background: c[1] === "pass" ? k.c : "transparent", color: c[1] === "pass" ? TA.paper : k.c, display: "grid", placeItems: "center", font: `500 10px/1 ${TA.sans}` } }, k.g), /* @__PURE__ */ React.createElement("span", { style: { font: `400 14px/1.3 ${TA.sans}` } }, c[0]), /* @__PURE__ */ React.createElement("span", { style: { font: `400 12px/1.4 ${TA.sans}`, color: tInk(0.6) } }, c[2]), /* @__PURE__ */ React.createElement("span", { style: { font: `500 9px/1 ${TA.mono}`, letterSpacing: "0.12em", textTransform: "uppercase", color: k.c, textAlign: "right" } }, k.t));
    }))), /* @__PURE__ */ React.createElement("div", { className: "ta-cols" }, /* @__PURE__ */ React.createElement(TaBlock, { label: "What they submitted" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(150px,1fr))", gap: 16 } }, submitted.map((f) => /* @__PURE__ */ React.createElement("div", { key: f[0] }, /* @__PURE__ */ React.createElement("div", { style: { font: `500 9px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: tInk(0.45) } }, f[0]), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 6, font: `400 14px/1.35 ${TA.serif}`, color: /not (provided|stated)/i.test(f[1]) ? TA.bad : TA.ink } }, f[1]))))), /* @__PURE__ */ React.createElement(TaBlock, { label: docs.length ? `Documents \xB7 ${docs.length}` : "Documents" }, docsLoading ? /* @__PURE__ */ React.createElement("div", { style: { font: `400 13px/1.5 ${TA.sans}`, color: tInk(0.5) } }, "Loading", "\u2026") : docs.length === 0 ? /* @__PURE__ */ React.createElement("div", { style: { font: `500 10px/1.7 ${TA.mono}`, letterSpacing: "0.12em", textTransform: "uppercase", color: tInk(0.4) } }, "Nothing uploaded") : /* @__PURE__ */ React.createElement("div", null, docs.map((d, i) => /* @__PURE__ */ React.createElement("div", { key: d.id, style: { padding: "10px 0", borderBottom: i < docs.length - 1 ? `0.5px solid ${tInk(0.09)}` : "none", display: "grid", gridTemplateColumns: "1fr auto", gap: 12, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: `400 13px/1.3 ${TA.sans}`, textTransform: "capitalize" } }, (d.doc_type || "Document").replace(/_/g, " ")), /* @__PURE__ */ React.createElement("span", { style: { display: "block", marginTop: 4, font: `500 9px/1.3 ${TA.mono}`, letterSpacing: "0.1em", color: tInk(0.45), overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, d.file_name || d.file_key, d.file_size ? " \xB7 " + Math.round(d.file_size / 1024) + " KB" : "")), d.url && /* @__PURE__ */ React.createElement("a", { href: d.url, target: "_blank", rel: "noopener noreferrer", style: { font: `500 9px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: TA.accent, textDecoration: "none" } }, "Open ", "\u2197")))))), !isDecline && /* @__PURE__ */ React.createElement(TaBlock, { label: "Sales tax", note: taxExempt ? "exempt" : "charged" }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, flexWrap: "wrap" } }, [{ v: false, name: "Charge sales tax", sub: "Standard \u2014 CA materials tax applies" }, { v: true, name: "Tax exempt (resale)", sub: "No sales tax on materials" }].map((o) => {
      const on = taxExempt === o.v;
      return /* @__PURE__ */ React.createElement("button", { key: String(o.v), onClick: () => setTaxExempt(o.v), style: {
        padding: "11px 16px",
        cursor: "pointer",
        textAlign: "left",
        background: on ? TA.ink : "transparent",
        color: on ? TA.paper : TA.ink,
        border: on ? "none" : `0.5px solid ${tInk(0.22)}`
      } }, /* @__PURE__ */ React.createElement("div", { style: { font: `500 11px/1 ${TA.mono}`, letterSpacing: "0.12em", textTransform: "uppercase" } }, o.name), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 6, font: `400 13px/1 ${TA.sans}`, opacity: on ? 0.85 : 0.6 } }, o.sub));
    })), taxExempt && !hasResale && /* @__PURE__ */ React.createElement("p", { style: { margin: "14px 0 0", maxWidth: "64ch", font: `400 13px/1.6 ${TA.sans}`, color: TA.bad, textWrap: "pretty" } }, "No resale certificate is clearly on file. Only exempt this account once you have verified a valid resale certificate \u2014 Roma is liable for uncollected tax otherwise.")), /* @__PURE__ */ React.createElement(TaBlock, { label: "Note on the record" }, /* @__PURE__ */ React.createElement("textarea", { rows: 2, value: note, onChange: (e) => setNote(e.target.value), placeholder: "Visible to the applicant if you decline\u2026", style: { width: "100%", boxSizing: "border-box", resize: "vertical", padding: "11px 12px", background: TA.paper, border: `0.5px solid ${tInk(0.2)}`, color: TA.ink, font: `400 13px/1.5 ${TA.sans}`, outline: "none" } }))), /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 30px 18px", borderTop: `0.5px solid ${tInk(0.16)}`, background: TA.ink, color: TA.paper, flexShrink: 0, display: "flex", gap: 16, alignItems: "center", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 240, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { style: { font: `500 10px/1 ${TA.mono}`, letterSpacing: "0.18em", textTransform: "uppercase", color: "#caa97f" } }, "On this decision"), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 8, font: `300 17px/1.45 ${TA.serif}`, textWrap: "pretty", maxWidth: "70ch" } }, isDecline ? `${a.company_name || a.contact_name} is declined and emailed your reason. No trade pricing, no margin exposure.` : `${a.company_name || a.contact_name} becomes a ${chosenTier.name} account at ${taPct(chosenTier.discount_percent)} off, assigned to the trade program${taxExempt ? ", tax-exempt on materials (resale)" : ", sales tax charged"}.`)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("button", { disabled: busy, onClick: () => decide("deny"), style: { padding: "13px 16px", background: "transparent", border: `0.5px solid ${TA.bad}`, color: "#f6e7e3", font: `500 11px/1 ${TA.sans}`, letterSpacing: "0.12em", textTransform: "uppercase", cursor: "pointer" } }, busy ? "Working\u2026" : "Decline"), !isDecline && /* @__PURE__ */ React.createElement("button", { disabled: busy, onClick: () => decide("approve"), style: { padding: "13px 24px", background: "#caa97f", border: "none", color: TA.ink, font: `500 12px/1 ${TA.sans}`, letterSpacing: "0.14em", textTransform: "uppercase", cursor: "pointer", whiteSpace: "nowrap" } }, busy ? "Working\u2026" : `Approve as ${chosenTier.name} \u2192`))));
  }
  function TaBlock({ label, note, children }) {
    return /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 30px 24px", borderBottom: `0.5px solid ${tInk(0.09)}` } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", gap: 12, marginBottom: 14 } }, /* @__PURE__ */ React.createElement("span", { style: { font: `500 10px/1 ${TA.mono}`, letterSpacing: "0.18em", textTransform: "uppercase", color: tInk(0.45) } }, label), note && /* @__PURE__ */ React.createElement("span", { style: { marginLeft: "auto", font: `500 9px/1 ${TA.mono}`, letterSpacing: "0.14em", textTransform: "uppercase", color: TA.accent } }, note)), children);
  }
  function RepVendorsListView({ navigate }) {
    const [vendors, setVendors] = useState([]);
    const [loading, setLoading] = useState(true);
    const [search, setSearch] = useState("");
    useEffect(() => {
      repFetch("/api/rep/vendor-directory").then((d) => {
        setVendors(d.vendors || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, []);
    const initials = (name) => {
      if (!name) return "?";
      const p = name.trim().split(/\s+/);
      return (p.length === 1 ? p[0].slice(0, 2) : p[0][0] + p[p.length - 1][0]).toUpperCase();
    };
    const q = search.trim().toLowerCase();
    const visible = vendors.filter((v) => !q || (v.name || "").toLowerCase().includes(q) || (v.code || "").toLowerCase().includes(q) || (v.primary_contact_name || "").toLowerCase().includes(q));
    const totalSkus = vendors.reduce((s, v) => s + (v.active_product_count || 0), 0);
    const withCatalog = vendors.filter((v) => (v.active_product_count || 0) > 0).length;
    const compact = (n) => n >= 1e3 ? (n / 1e3).toFixed(1).replace(/\.0$/, "") + "K" : String(n);
    const ROW_COLS = "40px 1.7fr 1.5fr 130px 60px";
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cv-head" }, /* @__PURE__ */ React.createElement("div", { className: "cv-head-top" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cv-kicker" }, "Catalog ", "\xB7", " Vendor directory"), /* @__PURE__ */ React.createElement("h1", { className: "cv-title" }, /* @__PURE__ */ React.createElement("em", null, "Vendors")))), /* @__PURE__ */ React.createElement("div", { className: "cv-stats" }, /* @__PURE__ */ React.createElement("div", { className: "cv-stat" }, /* @__PURE__ */ React.createElement("div", { className: "cv-stat-l" }, "Vendors"), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-v" }, vendors.length), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-s" }, "active suppliers")), /* @__PURE__ */ React.createElement("div", { className: "cv-stat" }, /* @__PURE__ */ React.createElement("div", { className: "cv-stat-l" }, "Active SKUs"), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-v" }, compact(totalSkus)), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-s" }, "across the book")), /* @__PURE__ */ React.createElement("div", { className: "cv-stat" }, /* @__PURE__ */ React.createElement("div", { className: "cv-stat-l" }, "With catalog"), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-v" }, withCatalog), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-s" }, "have live products")), /* @__PURE__ */ React.createElement("div", { className: "cv-stat" }, /* @__PURE__ */ React.createElement("div", { className: "cv-stat-l" }, "Reference only"), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-v" }, "\u2014"), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-s" }, "managed by office")))), /* @__PURE__ */ React.createElement("div", { className: "cv-roster-head" }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)" } }, visible.length, " of ", vendors.length, " vendor", vendors.length !== 1 ? "s" : ""), /* @__PURE__ */ React.createElement("div", { className: "cv-roster-tools" }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search name, code, contact",
        value: search,
        onChange: (e) => setSearch(e.target.value)
      }
    )))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 4px" } }, "Loading vendors", "\u2026") : /* @__PURE__ */ React.createElement("div", null, visible.map((v) => /* @__PURE__ */ React.createElement("div", { key: v.id, className: "cv-row", style: { gridTemplateColumns: ROW_COLS }, onClick: () => {
      repListNav.vendors = visible.map((x) => x.id);
      navigate("vendor-detail", v.id);
    } }, /* @__PURE__ */ React.createElement("div", { className: "cl-avatar" }, initials(v.name)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cv-row-name" }, v.name, v.public_code ? /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)", fontWeight: 400 } }, " \xB7 ", v.public_code) : null), /* @__PURE__ */ React.createElement("div", { className: "cv-row-sub" }, v.code)), /* @__PURE__ */ React.createElement("div", { className: "cv-row-line" }, v.primary_contact_name || v.email || v.phone || (v.website || "").replace(/^https?:\/\//, "") || "No contact on file"), /* @__PURE__ */ React.createElement("div", { className: "cv-row-line", style: { font: '400 13px/1.4 "Inter", sans-serif' } }, (v.active_product_count || 0).toLocaleString(), " SKU", v.active_product_count !== 1 ? "s" : ""), /* @__PURE__ */ React.createElement("div", { className: "cv-row-last" }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2192")))), visible.length === 0 && /* @__PURE__ */ React.createElement("div", { className: "gs-empty", style: { padding: "40px 4px" } }, "No vendors match ", "\u201C", search, "\u201D"), /* @__PURE__ */ React.createElement("div", { className: "cv-roster-foot" }, /* @__PURE__ */ React.createElement("span", null, "Showing ", visible.length, " of ", vendors.length), /* @__PURE__ */ React.createElement("span", null, "Read-only ", "\xB7", " vendor records managed by the office"))));
  }
  function RepContactEditor({ form, setForm, onSave, onCancel, saving }) {
    const set = (k) => (e) => setForm((f) => ({ ...f, [k]: e.target.value }));
    return /* @__PURE__ */ React.createElement("div", { className: "rv-contact-edit" }, /* @__PURE__ */ React.createElement("div", { className: "rv-grid2" }, /* @__PURE__ */ React.createElement("input", { className: "rv-input", placeholder: "Name *", value: form.name, onChange: set("name") }), /* @__PURE__ */ React.createElement("input", { className: "rv-input", placeholder: "Role \u2014 e.g. Sales, AR", value: form.role, onChange: set("role") }), /* @__PURE__ */ React.createElement("input", { className: "rv-input", placeholder: "Email", value: form.email, onChange: set("email") }), /* @__PURE__ */ React.createElement("input", { className: "rv-input", placeholder: "Phone", value: form.phone, onChange: (e) => setForm((f) => ({ ...f, phone: formatPhone(e.target.value) })) })), /* @__PURE__ */ React.createElement("label", { className: "rv-check" }, /* @__PURE__ */ React.createElement("input", { type: "checkbox", checked: form.is_primary, onChange: (e) => setForm((f) => ({ ...f, is_primary: e.target.checked })) }), " Primary contact"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: onCancel, disabled: saving }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "cdv-btn primary", onClick: onSave, disabled: saving }, saving ? "Saving\u2026" : "Save")));
  }
  function RepVendorDetailView({ navigate, editId }) {
    const [data, setData] = useState(null);
    const [activity, setActivity] = useState([]);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState(null);
    const [editingCompany, setEditingCompany] = useState(false);
    const [companyForm, setCompanyForm] = useState({});
    const [savingCompany, setSavingCompany] = useState(false);
    const [editingContact, setEditingContact] = useState(null);
    const [contactForm, setContactForm] = useState({ name: "", role: "", email: "", phone: "", is_primary: false });
    const [savingContact, setSavingContact] = useState(false);
    const money = (v2) => "$" + (parseFloat(v2) || 0).toLocaleString(void 0, { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const fmtDate = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }) : "\u2014";
    const initials = (name) => {
      if (!name) return "?";
      const p = name.trim().split(/\s+/);
      return (p.length === 1 ? p[0].slice(0, 2) : p[0][0] + p[p.length - 1][0]).toUpperCase();
    };
    const load = () => {
      repFetch("/api/rep/vendors/" + editId).then((d) => {
        setData(d);
        setLoading(false);
      }).catch((err) => {
        setError(err.message);
        setLoading(false);
      });
    };
    useEffect(() => {
      setLoading(true);
      load();
      repFetch("/api/rep/vendors/" + editId + "/po-activity").then((d) => setActivity(d.activity || [])).catch(() => {
      });
    }, [editId]);
    const startEditCompany = () => {
      const vv = data && data.vendor || {};
      setCompanyForm({ name: vv.name || "", email: vv.email || "", phone: formatPhone(vv.phone || ""), website: vv.website || "", account_number: vv.account_number || "", address: vv.address || "", notes: vv.notes || "" });
      setEditingCompany(true);
    };
    const saveCompany = async () => {
      if (!companyForm.name.trim()) {
        alert("Vendor name is required");
        return;
      }
      setSavingCompany(true);
      try {
        await repFetch("/api/rep/vendors/" + editId, { method: "PUT", body: JSON.stringify(companyForm) });
        setEditingCompany(false);
        load();
      } catch (err) {
        alert("Save failed: " + err.message);
      } finally {
        setSavingCompany(false);
      }
    };
    const startNewContact = () => {
      setContactForm({ name: "", role: "", email: "", phone: "", is_primary: false });
      setEditingContact("new");
    };
    const startEditContact = (c) => {
      setContactForm({ name: c.name || "", role: c.role || "", email: c.email || "", phone: formatPhone(c.phone || ""), is_primary: !!c.is_primary });
      setEditingContact(c.id);
    };
    const saveContact = async () => {
      if (!contactForm.name.trim()) {
        alert("Contact name is required");
        return;
      }
      setSavingContact(true);
      try {
        const isNew = editingContact === "new";
        await repFetch("/api/rep/vendors/" + editId + "/contacts" + (isNew ? "" : "/" + editingContact), { method: isNew ? "POST" : "PUT", body: JSON.stringify(contactForm) });
        setEditingContact(null);
        load();
      } catch (err) {
        alert("Save failed: " + err.message);
      } finally {
        setSavingContact(false);
      }
    };
    const deleteContact = async (c) => {
      if (!confirm("Delete contact " + c.name + "?")) return;
      try {
        await repFetch("/api/rep/vendors/" + editId + "/contacts/" + c.id, { method: "DELETE" });
        load();
      } catch (err) {
        alert("Delete failed: " + err.message);
      }
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "cdv-empty", style: { padding: "48px 22px" } }, "Loading vendor", "\u2026");
    if (error || !data) return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumbs" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumb-trail" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("vendors") }, "Vendors"))), /* @__PURE__ */ React.createElement("div", { className: "cdv-empty", style: { padding: "48px 22px" } }, error || "Vendor not found"));
    const v = data.vendor;
    const stats = data.stats || {};
    const brands = data.brands || [];
    const pos = data.purchase_orders || [];
    const contacts = data.contacts || [];
    const primaryContact = contacts.find((c) => c.is_primary) || contacts[0] || null;
    const nameParts = (v.name || "Vendor").trim().split(/\s+/);
    const nameHead = nameParts.length > 1 ? nameParts.slice(0, -1).join(" ") : "";
    const nameTail = nameParts[nameParts.length - 1];
    const webHref = v.website ? /^https?:/.test(v.website) ? v.website : "https://" + v.website : null;
    const webLabel = v.website ? v.website.replace(/^https?:\/\//, "").replace(/\/$/, "") : null;
    const field = (k, val, sub, wide, href) => /* @__PURE__ */ React.createElement("div", { key: k, className: wide ? "cdv-field-wide" : "" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-field-k" }, k), /* @__PURE__ */ React.createElement("div", { className: "cdv-field-v" }, val ? href ? /* @__PURE__ */ React.createElement("a", { href, target: href.startsWith("http") ? "_blank" : void 0, rel: "noopener noreferrer", style: { color: "var(--stone-900)", textDecoration: "none", borderBottom: "1px solid var(--gold)" } }, val) : val : /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)" } }, "\u2014")), sub && /* @__PURE__ */ React.createElement("div", { className: "cdv-field-s" }, sub));
    const kpis = [
      { l: "Active SKUs", v: (stats.active_product_count || 0).toLocaleString(), s: (stats.product_count || 0).toLocaleString() + " total in catalog" },
      { l: "Brands", v: String(brands.length), s: brands.length ? brands.filter((b) => b.is_primary).map((b) => b.name).join(", ") || brands[0].name : "None linked" },
      { l: "Recent POs", v: String(pos.length) + (pos.length === 10 ? "+" : ""), s: pos.length ? "last " + fmtDate(pos[0].created_at) : "None on record" },
      { l: "Account #", v: v.account_number || "\u2014", s: v.account_number ? "Our customer ID" : "None on file" }
    ];
    const PO_COLS = "130px 1fr 130px 120px 24px";
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumbs" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumb-trail" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("vendors") }, "Vendors"), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-cur" }, v.name, v.public_code ? " \xB7 " + v.public_code : "")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 8 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 12 } }, /* @__PURE__ */ React.createElement(RepPrevNext, { section: "vendors", id: editId, view: "vendor-detail", navigate, linkStyle: { color: "var(--brass-muted)", textDecoration: "none" } })), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-status", style: { color: v.is_active ? "#3a7a4e" : "var(--brass-muted)" } }, "\u25CF", " ", v.is_active ? "Active supplier" : "Inactive"))), /* @__PURE__ */ React.createElement("div", { className: "cdv-head" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-head-id" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-avatar" }, initials(v.name)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-badge" }, "\u25CF", " ", v.code, v.public_code ? " \xB7 " + v.public_code : "", v.is_one_off ? " \xB7 One-off" : ""), /* @__PURE__ */ React.createElement("h1", { className: "cdv-name" }, nameHead ? /* @__PURE__ */ React.createElement(React.Fragment, null, nameHead, " ", /* @__PURE__ */ React.createElement("em", null, nameTail)) : /* @__PURE__ */ React.createElement("em", null, nameTail), v.public_code ? /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5em", color: "var(--brass-muted)", fontWeight: 400, letterSpacing: 0, marginLeft: "0.4em", verticalAlign: "middle" } }, v.public_code) : null), /* @__PURE__ */ React.createElement("div", { className: "cdv-contact" }, primaryContact && /* @__PURE__ */ React.createElement(React.Fragment, null, primaryContact.name, " ", "\xB7", " "), v.email && /* @__PURE__ */ React.createElement("a", { href: "mailto:" + v.email }, v.email), v.phone && /* @__PURE__ */ React.createElement(React.Fragment, null, " ", "\xB7", " ", v.phone), webLabel && /* @__PURE__ */ React.createElement(React.Fragment, null, " ", "\xB7", " ", /* @__PURE__ */ React.createElement("a", { href: webHref, target: "_blank", rel: "noopener noreferrer" }, webLabel))))), /* @__PURE__ */ React.createElement("div", { className: "cdv-head-actions" }, /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: () => navigate("vendors") }, "\u2190", " Back"), v.email && /* @__PURE__ */ React.createElement("a", { className: "cdv-btn", href: "mailto:" + v.email }, "Email"), webHref && /* @__PURE__ */ React.createElement("a", { className: "cdv-btn", href: webHref, target: "_blank", rel: "noopener noreferrer" }, "Website"))), /* @__PURE__ */ React.createElement("div", { className: "cdv-kpis", style: { gridTemplateColumns: "repeat(4, 1fr)" } }, kpis.map((k) => /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi", key: k.l }, /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi-l" }, k.l), /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi-v" }, k.v), /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi-s" }, k.s)))), /* @__PURE__ */ React.createElement("div", { className: "cdv-grid2" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card", style: { margin: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Company"), !editingCompany && /* @__PURE__ */ React.createElement("button", { className: "cdv-card-link", onClick: startEditCompany }, "Edit")), editingCompany ? /* @__PURE__ */ React.createElement("div", { className: "rv-form" }, /* @__PURE__ */ React.createElement("label", { className: "rv-full" }, /* @__PURE__ */ React.createElement("span", { className: "rv-label" }, "Vendor name"), /* @__PURE__ */ React.createElement("input", { className: "rv-input", value: companyForm.name, onChange: (e) => setCompanyForm((f) => ({ ...f, name: e.target.value })) })), /* @__PURE__ */ React.createElement("label", null, /* @__PURE__ */ React.createElement("span", { className: "rv-label" }, "Order / AR email"), /* @__PURE__ */ React.createElement("input", { className: "rv-input", value: companyForm.email, onChange: (e) => setCompanyForm((f) => ({ ...f, email: e.target.value })), placeholder: "orders@vendor.com" })), /* @__PURE__ */ React.createElement("label", null, /* @__PURE__ */ React.createElement("span", { className: "rv-label" }, "Phone"), /* @__PURE__ */ React.createElement("input", { className: "rv-input", value: companyForm.phone, onChange: (e) => setCompanyForm((f) => ({ ...f, phone: formatPhone(e.target.value) })), placeholder: "(714) 555-0100" })), /* @__PURE__ */ React.createElement("label", null, /* @__PURE__ */ React.createElement("span", { className: "rv-label" }, "Website"), /* @__PURE__ */ React.createElement("input", { className: "rv-input", value: companyForm.website, onChange: (e) => setCompanyForm((f) => ({ ...f, website: e.target.value })), placeholder: "https://\u2026" })), /* @__PURE__ */ React.createElement("label", null, /* @__PURE__ */ React.createElement("span", { className: "rv-label" }, "Account #"), /* @__PURE__ */ React.createElement("input", { className: "rv-input", value: companyForm.account_number, onChange: (e) => setCompanyForm((f) => ({ ...f, account_number: e.target.value })), placeholder: "Our customer ID" })), /* @__PURE__ */ React.createElement("label", { className: "rv-full" }, /* @__PURE__ */ React.createElement("span", { className: "rv-label" }, "Address"), /* @__PURE__ */ React.createElement("input", { className: "rv-input", value: companyForm.address, onChange: (e) => setCompanyForm((f) => ({ ...f, address: e.target.value })), placeholder: "Street, City, State ZIP" })), /* @__PURE__ */ React.createElement("label", { className: "rv-full" }, /* @__PURE__ */ React.createElement("span", { className: "rv-label" }, "Notes"), /* @__PURE__ */ React.createElement("textarea", { className: "rv-input", value: companyForm.notes, onChange: (e) => setCompanyForm((f) => ({ ...f, notes: e.target.value })), placeholder: "Internal notes about this vendor\u2026" })), /* @__PURE__ */ React.createElement("div", { className: "rv-actions" }, /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: () => setEditingCompany(false), disabled: savingCompany }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "cdv-btn primary", onClick: saveCompany, disabled: savingCompany }, savingCompany ? "Saving\u2026" : "Save"))) : /* @__PURE__ */ React.createElement("div", { className: "cdv-fields" }, field("Order / AR email", v.email || "\u2014", null, false, v.email ? "mailto:" + v.email : null), field("Phone", v.phone || "\u2014"), field("Website", webLabel || "\u2014", null, false, webHref), field("Account #", v.account_number || "\u2014"), field("Address", v.address || "\u2014", null, true))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card", style: { margin: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Catalog ", "\xB7", " brands")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body" }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 28, marginBottom: brands.length ? 18 : 0 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: '300 30px/1 "Cormorant Garamond", serif', color: "var(--stone-900)" } }, (stats.active_product_count || 0).toLocaleString()), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)", marginTop: 6 } }, "Active SKUs")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: '300 30px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, (stats.product_count || 0).toLocaleString()), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)", marginTop: 6 } }, "Total"))), brands.length > 0 ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 8 } }, brands.map((b) => /* @__PURE__ */ React.createElement("span", { key: b.id, style: { padding: "5px 11px", background: "var(--brass-warm)", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--stone-900)" } }, b.name, b.is_primary ? " \u2605" : ""))) : /* @__PURE__ */ React.createElement("div", { style: { font: '400 13px/1.4 "Inter", sans-serif', color: "var(--brass-muted)" } }, "No brands linked to this vendor")))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Contacts ", "\xB7", " ", contacts.length), editingContact === null && /* @__PURE__ */ React.createElement("button", { className: "cdv-card-link", onClick: startNewContact }, "+ Add contact")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body", style: { paddingTop: 6, paddingBottom: 6 } }, contacts.map((c, i) => editingContact === c.id ? /* @__PURE__ */ React.createElement(RepContactEditor, { key: c.id, form: contactForm, setForm: setContactForm, onSave: saveContact, onCancel: () => setEditingContact(null), saving: savingContact }) : /* @__PURE__ */ React.createElement("div", { key: c.id, style: { display: "flex", justifyContent: "space-between", gap: 16, alignItems: "baseline", padding: "11px 0", borderBottom: i < contacts.length - 1 ? "0.5px solid rgba(28,25,23,0.08)" : "none" } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 16px/1.2 "Cormorant Garamond", serif', color: "var(--stone-900)" } }, c.name), c.is_primary && /* @__PURE__ */ React.createElement("span", { style: { marginLeft: 8, font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--gold)" } }, "\u25CF", " Primary"), c.role && /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--brass-muted)", marginTop: 3 } }, c.role), /* @__PURE__ */ React.createElement("div", { className: "rv-row-actions" }, /* @__PURE__ */ React.createElement("button", { className: "rv-edit", onClick: () => startEditContact(c) }, "Edit"), /* @__PURE__ */ React.createElement("button", { className: "rv-del", onClick: () => deleteContact(c) }, "Delete"))), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right", font: '400 13px/1.5 "Inter", sans-serif', whiteSpace: "nowrap" } }, c.email && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("a", { href: "mailto:" + c.email, style: { color: "var(--stone-900)", textDecoration: "none", borderBottom: "1px solid var(--gold)" } }, c.email)), c.phone && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--brass-muted)" } }, /* @__PURE__ */ React.createElement("a", { href: "tel:" + c.phone, style: { color: "var(--brass-muted)", textDecoration: "none" } }, c.phone))))), editingContact === "new" && /* @__PURE__ */ React.createElement(RepContactEditor, { form: contactForm, setForm: setContactForm, onSave: saveContact, onCancel: () => setEditingContact(null), saving: savingContact }), contacts.length === 0 && editingContact !== "new" && /* @__PURE__ */ React.createElement("div", { className: "cdv-empty", style: { padding: "14px 0" } }, "No contacts on file"))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Recent purchase orders ", "\xB7", " ", pos.length, pos.length === 10 ? "+" : "")), pos.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No purchase orders with this vendor yet") : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-head", style: { gridTemplateColumns: PO_COLS } }, /* @__PURE__ */ React.createElement("span", null, "PO"), /* @__PURE__ */ React.createElement("span", null, "Status"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Subtotal"), /* @__PURE__ */ React.createElement("span", null, "Created"), /* @__PURE__ */ React.createElement("span", null)), pos.map((po) => /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: po.id, style: { gridTemplateColumns: PO_COLS }, onClick: () => navigate("purchase-order-detail", po.id) }, /* @__PURE__ */ React.createElement("span", { className: "cdv-o-id" }, po.po_number), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-status", style: { color: po.status === "fulfilled" ? "#3a7a4e" : po.status === "cancelled" ? "#c54731" : po.status === "draft" ? "var(--brass-muted)" : "#a87935" } }, "\u25CF", " ", po.status), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-total" }, money(po.subtotal)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date" }, fmtDate(po.created_at)), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", color: "var(--brass-muted)", font: '400 14px/1 "Cormorant Garamond", serif' } }, "\u2192"))))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Recent PO activity")), activity.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No recent activity") : /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body", style: { paddingTop: 6, paddingBottom: 6 } }, activity.map((a, i) => /* @__PURE__ */ React.createElement("div", { key: a.id || i, className: "cdv-tl-item", style: { display: "flex", justifyContent: "space-between", gap: 16, padding: "11px 0", borderBottom: i < activity.length - 1 ? "0.5px solid rgba(28,25,23,0.08)" : "none" } }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1.3 "Inter", sans-serif', color: "var(--stone-900)" } }, a.po_number && /* @__PURE__ */ React.createElement("span", { className: "cdv-o-id", style: { marginRight: 8 } }, a.po_number), a.action || "Activity", a.performer_name && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)" } }, " ", "\xB7", " ", a.performer_name)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date", style: { whiteSpace: "nowrap" } }, fmtDate(a.created_at)))))), v.notes && /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Notes")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body", style: { whiteSpace: "pre-wrap", font: '400 14px/1.6 "Inter", sans-serif', color: "var(--stone-900)" } }, v.notes)));
  }
  function RepCustomersListView({ navigate }) {
    const [customers, setCustomers] = useState([]);
    const [summary, setSummary] = useState(null);
    const [attention, setAttention] = useState([]);
    const [loading, setLoading] = useState(true);
    const [search, setSearch] = useState("");
    const [sortCol, setSortCol] = useState("spent");
    const [sortDir, setSortDir] = useState("desc");
    const [activeView, setActiveView] = useState("all");
    const [page, setPage] = useState(1);
    const [total, setTotal] = useState(0);
    const limit = 50;
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const repName = ((repInfo.first_name || "") + " " + (repInfo.last_name || "")).trim();
    const load = useCallback(() => {
      setLoading(true);
      let url = "/api/rep/customers?page=" + page + "&limit=" + limit + "&sort=" + sortCol + "&dir=" + sortDir + "&type=all";
      if (search) url += "&search=" + encodeURIComponent(search);
      repFetch(url).then((data) => {
        setCustomers(data.customers || []);
        setTotal(data.total || 0);
        setSummary(data.summary || null);
        setAttention(data.attention || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [page, sortCol, sortDir, search]);
    useEffect(load, [load]);
    const daysAgo = (d) => d ? Math.floor((Date.now() - new Date(d).getTime()) / 864e5) : null;
    const viewFilters = {
      "all": () => true,
      "trade": (c) => c.customer_type === "trade",
      "active": (c) => c.order_count > 0 && daysAgo(c.last_order_date) !== null && daysAgo(c.last_order_date) <= 90,
      "lapsed": (c) => c.order_count > 0 && (daysAgo(c.last_order_date) === null || daysAgo(c.last_order_date) > 180),
      "new": (c) => daysAgo(c.created_at) !== null && daysAgo(c.created_at) <= 30
    };
    const filteredCustomers = useMemo(() => {
      const fn = viewFilters[activeView] || (() => true);
      return customers.filter(fn);
    }, [customers, activeView]);
    const viewCounts = useMemo(() => ({
      "all": total,
      "trade": customers.filter(viewFilters["trade"]).length,
      "active": customers.filter(viewFilters["active"]).length,
      "lapsed": customers.filter(viewFilters["lapsed"]).length,
      "new": customers.filter(viewFilters["new"]).length
    }), [customers, total]);
    const totalPages = Math.ceil(total / limit) || 1;
    const displayCustomers = filteredCustomers;
    const maxLtv = Math.max(1, ...displayCustomers.map((c) => parseFloat(c.total_spent) || 0));
    const getInitials = (name) => {
      if (!name) return "??";
      const parts = name.trim().split(/\s+/);
      if (parts.length === 1) return parts[0].substring(0, 2).toUpperCase();
      return (parts[0][0] + parts[parts.length - 1][0]).toUpperCase();
    };
    const getLastTouch = (c) => {
      const days = daysAgo(c.last_order_date);
      if (days === null) return { label: "No orders", tone: "var(--brass-muted)" };
      if (days === 0) return { label: "Today", tone: "#3a7a4e" };
      if (days === 1) return { label: "Yesterday", tone: "#3a7a4e" };
      if (days < 7) return { label: days + " days", tone: "#3a7a4e" };
      if (days < 30) return { label: days + " days", tone: "var(--brass-muted)" };
      if (days < 90) return { label: days + "d", tone: "#a87935" };
      return { label: days + "d", tone: "#c54731" };
    };
    const deriveLine = (c) => {
      const open = [];
      if (c.open_orders) open.push(c.open_orders + " order" + (c.open_orders !== 1 ? "s" : "") + " in flight");
      if (c.open_quotes) open.push(c.open_quotes + " quote" + (c.open_quotes !== 1 ? "s" : "") + " open");
      let line;
      if (open.length) line = open.join(" \xB7 ");
      else {
        const d = daysAgo(c.last_order_date);
        if (d === null) line = "No orders yet";
        else if (d > 180) line = "Lapsed \xB7 no open work";
        else line = c.order_count + " order" + (c.order_count !== 1 ? "s" : "") + " \xB7 last " + getLastTouch(c).label.toLowerCase();
      }
      return line.charAt(0).toUpperCase() + line.slice(1);
    };
    const tierLabel = (c) => c.customer_type === "trade" ? c.tier_name || "Trade Pro" : c.customer_type === "guest" ? "Guest" : "Retail";
    const compact = (v) => {
      v = parseFloat(v) || 0;
      if (v >= 1e6) return "$" + (v / 1e6).toFixed(1) + "M";
      if (v >= 1e3) return "$" + Math.round(v / 1e3) + "K";
      return "$" + Math.round(v).toLocaleString();
    };
    const median = (arr) => {
      if (!arr.length) return 0;
      const a = [...arr].sort((x, y) => x - y);
      const m = Math.floor(a.length / 2);
      return a.length % 2 ? a[m] : Math.round((a[m - 1] + a[m]) / 2);
    };
    const s = summary || {};
    const accts = s.account_count || 0;
    const kpis = [
      { l: "Book value", v: compact(s.book_value || 0), sub: "lifetime \xB7 " + accts.toLocaleString() + " account" + (accts !== 1 ? "s" : "") },
      { l: "Open work", v: compact(s.open_work_value || 0), sub: (s.open_orders || 0) + " order" + ((s.open_orders || 0) !== 1 ? "s" : "") + " \xB7 " + (s.open_quotes || 0) + " quote" + ((s.open_quotes || 0) !== 1 ? "s" : "") },
      { l: "Touched this week", v: String(s.touched_week || 0), sub: (accts ? Math.round((s.touched_week || 0) / accts * 100) : 0) + "% of book" },
      { l: "Going cold", v: String(s.going_cold || 0), sub: "no contact 180d" }
    ];
    const VIEWS = [
      { key: "all", label: "All mine" },
      { key: "trade", label: "Trade Pro" },
      { key: "active", label: "Active project" },
      { key: "lapsed", label: "Lapsed" },
      { key: "new", label: "New this month" }
    ];
    const avgLtv = accts ? (s.book_value || 0) / accts : 0;
    const medOrders = median(displayCustomers.map((c) => c.order_count || 0));
    const sortValue = sortCol + ":" + sortDir;
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cv-head" }, /* @__PURE__ */ React.createElement("div", { className: "cv-head-top" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cv-kicker" }, "Your book", repName ? " \xB7 " + repName : ""), /* @__PURE__ */ React.createElement("h1", { className: "cv-title" }, /* @__PURE__ */ React.createElement("em", null, "Customers"))), /* @__PURE__ */ React.createElement("div", { className: "cv-actions" }, /* @__PURE__ */ React.createElement("button", { className: "cv-btn primary", onClick: () => navigate("customer-create") }, "+ New customer"))), /* @__PURE__ */ React.createElement("div", { className: "cv-stats" }, kpis.map((k) => /* @__PURE__ */ React.createElement("div", { key: k.l, className: "cv-stat" }, /* @__PURE__ */ React.createElement("div", { className: "cv-stat-l" }, k.l), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-v" }, k.v), /* @__PURE__ */ React.createElement("div", { className: "cv-stat-s" }, k.sub))))), attention.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "cv-attention" }, /* @__PURE__ */ React.createElement("div", { className: "cv-attention-head" }, /* @__PURE__ */ React.createElement("h2", { className: "cv-attention-title" }, "Needs you today"), /* @__PURE__ */ React.createElement("span", { className: "cv-attention-count" }, attention.length, " of ", accts.toLocaleString())), /* @__PURE__ */ React.createElement("div", { className: "cv-cards" }, attention.map((a) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: a.id,
        className: "cv-card",
        style: { borderTopColor: a.tone },
        onClick: () => navigate("customer-detail", a.id)
      },
      /* @__PURE__ */ React.createElement("div", { className: "cv-card-kicker", style: { color: a.tone } }, a.kicker),
      /* @__PURE__ */ React.createElement("div", { className: "cv-card-name" }, a.name),
      /* @__PURE__ */ React.createElement("div", { className: "cv-card-body" }, a.body),
      /* @__PURE__ */ React.createElement("div", { className: "cv-card-foot" }, /* @__PURE__ */ React.createElement("span", { className: "cv-card-meta" }, a.meta), /* @__PURE__ */ React.createElement("span", { className: "cv-card-cta", onClick: (e) => {
        e.stopPropagation();
        navigate("customer-detail", a.id);
      } }, a.cta))
    )))), /* @__PURE__ */ React.createElement("div", { className: "cv-roster-head" }, /* @__PURE__ */ React.createElement("div", { className: "cv-tabs" }, VIEWS.map((v) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: v.key,
        className: "cv-tab" + (activeView === v.key ? " active" : ""),
        onClick: () => {
          setActiveView(v.key);
          setPage(1);
        }
      },
      v.label,
      /* @__PURE__ */ React.createElement("span", { className: "cv-tab-count" }, viewCounts[v.key])
    ))), /* @__PURE__ */ React.createElement("div", { className: "cv-roster-tools" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        value: sortValue,
        onChange: (e) => {
          const [col, dir] = e.target.value.split(":");
          setSortCol(col);
          setSortDir(dir);
          setPage(1);
        },
        style: { padding: "6px 24px 6px 11px", background: "var(--brass-warm)", border: "0.5px solid rgba(168,121,53,0.33)", font: '400 12px/1 "Inter", sans-serif', color: "var(--stone-900)", cursor: "pointer", appearance: "auto" }
      },
      /* @__PURE__ */ React.createElement("option", { value: "spent:desc" }, "Sort: LTV, high to low"),
      /* @__PURE__ */ React.createElement("option", { value: "last_order:desc" }, "Sort: Recent activity"),
      /* @__PURE__ */ React.createElement("option", { value: "orders:desc" }, "Sort: Most orders"),
      /* @__PURE__ */ React.createElement("option", { value: "name:asc" }, "Sort: Name A-Z")
    ), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search name, email, phone",
        value: search,
        onChange: (e) => {
          setSearch(e.target.value);
          setPage(1);
        }
      }
    )))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 4px" } }, "Loading customers", "\u2026") : /* @__PURE__ */ React.createElement("div", null, displayCustomers.map((c) => {
      const lt = getLastTouch(c);
      const name = c.company_name || c.name || "\u2014";
      const ltv = parseFloat(c.total_spent) || 0;
      return /* @__PURE__ */ React.createElement("div", { key: c.id, className: "cv-row", onClick: () => {
        repListNav.customers = displayCustomers.map((x) => x.id);
        navigate("customer-detail", c.id);
      } }, /* @__PURE__ */ React.createElement("div", { className: "cl-avatar" }, getInitials(name)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cv-row-name" }, name), /* @__PURE__ */ React.createElement("div", { className: "cv-row-sub" }, tierLabel(c), " ", "\xB7", " ", c.city || "\u2014")), /* @__PURE__ */ React.createElement("div", { className: "cv-row-line" }, deriveLine(c)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cv-row-ltvwrap" }, /* @__PURE__ */ React.createElement("span", { className: "cv-row-ltv" }, "$", ltv.toLocaleString(void 0, { maximumFractionDigits: 0 })), /* @__PURE__ */ React.createElement("span", { className: "cv-row-ord" }, c.order_count, " ord")), /* @__PURE__ */ React.createElement("div", { className: "cv-bar" }, /* @__PURE__ */ React.createElement("div", { className: "cv-bar-fill", style: { width: ltv / maxLtv * 100 + "%" } }))), /* @__PURE__ */ React.createElement("div", { className: "cv-row-last" }, /* @__PURE__ */ React.createElement("span", { className: "cv-row-lastdot", style: { color: lt.tone } }, "\u25CF", " ", lt.label), /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2192")));
    }), displayCustomers.length === 0 && /* @__PURE__ */ React.createElement("div", { className: "gs-empty", style: { padding: "40px 4px" } }, "No customers in this view"), /* @__PURE__ */ React.createElement("div", { className: "cv-roster-foot" }, /* @__PURE__ */ React.createElement("span", null, "Showing ", displayCustomers.length, " of ", activeView === "all" ? total : displayCustomers.length, " ", "\xB7", " sorted by ", sortCol === "spent" ? "LTV" : sortCol === "last_order" ? "activity" : sortCol === "orders" ? "orders" : "name"), accts > 0 && /* @__PURE__ */ React.createElement("span", null, "Avg ", compact(avgLtv), " ", "\xB7", " median ", medOrders, " order", medOrders !== 1 ? "s" : "")), totalPages > 1 && activeView === "all" && /* @__PURE__ */ React.createElement("div", { className: "ol-pagination" }, /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, limit, " per page"), /* @__PURE__ */ React.createElement("div", { className: "ol-page-btns" }, /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page <= 1, onClick: () => setPage((p) => p - 1) }, "\u2190"), Array.from({ length: Math.min(totalPages, 5) }, (_, i) => i + 1).map((p) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: p,
        className: "ol-page-btn" + (page === p ? " active" : ""),
        onClick: () => setPage(p)
      },
      p
    )), /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page >= totalPages, onClick: () => setPage((p) => p + 1) }, "\u2192")))));
  }
  function RepCustomerCreateView({ navigate }) {
    const [form, setForm] = useState({ first_name: "", middle_initial: "", last_name: "", email: "", phone: "", company_name: "", address_line1: "", address_line2: "", city: "", state: "", zip: "" });
    const [saving, setSaving] = useState(false);
    const [error, setError] = useState("");
    const set = (k) => (e) => setForm((f) => ({ ...f, [k]: e.target.value }));
    const canSave = !!form.first_name.trim() && !!form.last_name.trim() && !!form.email.trim() && isValidEmail(form.email.trim()) && !!form.phone.trim() && isValidPhone(form.phone);
    const submit = () => {
      setError("");
      if (!form.first_name.trim() || !form.last_name.trim() || !form.email.trim()) {
        setError("First name, last name, and email are required.");
        return;
      }
      if (!isValidEmail(form.email.trim())) {
        setError("Enter a valid email.");
        return;
      }
      if (!form.phone.trim() || !isValidPhone(form.phone)) {
        setError("A valid 10-digit phone is required.");
        return;
      }
      setSaving(true);
      repFetch("/api/rep/customers", { method: "POST", body: JSON.stringify(form) }).then((data) => {
        navigate("customer-detail", "retail_" + data.id);
      }).catch((err) => {
        setError(err && err.message || "Could not create customer.");
        setSaving(false);
      });
    };
    const inputStyle = { width: "100%", padding: "9px 12px", border: "0.5px solid var(--stone-300)", background: "#fff", font: "400 14px/1.4 Inter, sans-serif", color: "var(--stone-900)", boxSizing: "border-box" };
    const fieldWrap = (label, node, required, wide) => /* @__PURE__ */ React.createElement("div", { style: wide ? { gridColumn: "1 / -1" } : void 0 }, /* @__PURE__ */ React.createElement("label", { className: "cdv-field-k", style: { display: "block", marginBottom: 6 } }, label, required && /* @__PURE__ */ React.createElement("span", { style: { color: "#c54731" } }, " *")), node);
    const onKey = (e) => {
      if (e.key === "Enter") submit();
    };
    return /* @__PURE__ */ React.createElement("div", { style: { maxWidth: 720 } }, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumbs" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumb-trail" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("customers") }, "Customers"), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-cur" }, "New customer"))), /* @__PURE__ */ React.createElement("h1", { className: "cdv-name", style: { marginBottom: 16 } }, "New ", /* @__PURE__ */ React.createElement("em", null, "customer")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "flex-start", gap: 10, padding: "11px 14px", marginBottom: 24, background: "rgba(184,134,74,0.08)", border: "0.5px solid rgba(184,134,74,0.45)", font: "400 13px/1.5 Inter, sans-serif", color: "var(--stone-800)" } }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)", fontSize: 15, lineHeight: "19px" } }, "\u2605"), /* @__PURE__ */ React.createElement("span", null, "Added to ", /* @__PURE__ */ React.createElement("strong", null, "your book"), " as a dedicated customer. Their orders, invoices, and commission are yours \u2014 and it stays that way unless a manager reassigns them.")), error && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 14px", marginBottom: 16, background: "rgba(197,71,49,0.08)", border: "0.5px solid rgba(197,71,49,0.4)", color: "#c54731", font: "400 13px/1.4 Inter, sans-serif" } }, error), /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Contact")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body", style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 } }, fieldWrap("First name", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.first_name, onChange: set("first_name"), onKeyDown: onKey, autoFocus: true }), true), fieldWrap("Last name", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.last_name, onChange: set("last_name"), onKeyDown: onKey }), true), fieldWrap("Middle initial (optional)", /* @__PURE__ */ React.createElement("input", { style: inputStyle, maxLength: 4, value: form.middle_initial, onChange: set("middle_initial"), onKeyDown: onKey })), fieldWrap("Email", /* @__PURE__ */ React.createElement("input", { type: "email", style: inputStyle, value: form.email, onChange: set("email"), onKeyDown: onKey }), true, true), fieldWrap("Phone", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.phone, onChange: (e) => setForm((f) => ({ ...f, phone: formatPhone(e.target.value) })), onKeyDown: onKey }), true), fieldWrap("Company (optional)", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.company_name, onChange: set("company_name"), onKeyDown: onKey })))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Address ", "\xB7", " optional")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body", style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 } }, fieldWrap("Street address", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.address_line1, onChange: set("address_line1"), onKeyDown: onKey }), false, true), fieldWrap("Apt / suite", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.address_line2, onChange: set("address_line2"), onKeyDown: onKey }), false, true), fieldWrap("City", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.city, onChange: set("city"), onKeyDown: onKey })), fieldWrap("State", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.state, onChange: set("state"), onKeyDown: onKey })), fieldWrap("ZIP", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.zip, onChange: set("zip"), onKeyDown: onKey })))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 10, marginTop: 20 } }, /* @__PURE__ */ React.createElement("button", { className: "cdv-btn primary", onClick: submit, disabled: saving || !canSave }, saving ? "Creating\u2026" : "Create customer"), /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: () => navigate("customers"), disabled: saving }, "Cancel")), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 16, font: "400 12px/1.5 Inter, sans-serif", color: "var(--brass-muted)" } }, "Creates a retail account. For trade accounts, use the trade application flow."));
  }
  function ReassignRepModal({ title, subtitle, currentRepId, currentRepName, saving, onClose, onConfirm }) {
    const [reps, setReps] = useState(null);
    const [repId, setRepId] = useState("");
    const [err, setErr] = useState("");
    useEffect(() => {
      repFetch("/api/rep/reps").then((d) => setReps(d.reps || [])).catch(() => {
        setReps([]);
        setErr("Could not load reps");
      });
    }, []);
    const rrmInitials = (name) => {
      if (!name) return "?";
      const p = String(name).trim().split(/\s+/);
      return (p.length === 1 ? p[0].slice(0, 2) : p[0][0] + p[p.length - 1][0]).toUpperCase();
    };
    const avatar = (name, size, sel) => /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "rrm-avatar" + (sel ? " sel" : ""),
        style: { width: size, height: size, fontSize: Math.round(size * 0.42) }
      },
      rrmInitials(name)
    );
    const selected = reps && reps.find((r) => r.id === repId);
    const canSubmit = repId && repId !== currentRepId && !saving;
    return /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => !saving && onClose() }, /* @__PURE__ */ React.createElement("div", { className: "modal-box rrm-box", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("div", { className: "rrm-head" }, /* @__PURE__ */ React.createElement("div", { className: "rrm-eyebrow" }, "Reassign rep"), /* @__PURE__ */ React.createElement("h3", { className: "rrm-title" }, title), subtitle && /* @__PURE__ */ React.createElement("p", { className: "rrm-sub" }, subtitle)), /* @__PURE__ */ React.createElement("div", { className: "rrm-transition" }, avatar(currentRepName || "\u2014", 34, false), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rrm-tlabel" }, "Currently"), /* @__PURE__ */ React.createElement("div", { className: "rrm-tname" }, currentRepName || "Unassigned")), selected && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rrm-arrow" }, "\u2192"), avatar(selected.name, 34, true), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rrm-tlabel", style: { color: "var(--gold)" } }, "New rep"), /* @__PURE__ */ React.createElement("div", { className: "rrm-tname", style: { color: "var(--stone-900)" } }, selected.name)))), /* @__PURE__ */ React.createElement("div", { className: "rrm-list" }, reps === null ? /* @__PURE__ */ React.createElement("div", { style: { padding: "1.75rem", textAlign: "center", color: "var(--stone-400)", fontSize: 13 } }, "Loading reps", "\u2026") : reps.length === 0 ? /* @__PURE__ */ React.createElement("div", { style: { padding: "1.75rem", textAlign: "center", color: "var(--stone-400)", fontSize: 13 } }, "No reps available") : reps.map((r) => {
      const isCurrent = r.id === currentRepId;
      const isSel = r.id === repId;
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          key: r.id,
          type: "button",
          disabled: isCurrent || saving,
          className: "rrm-row" + (isSel ? " sel" : ""),
          onClick: () => setRepId(r.id)
        },
        avatar(r.name, 38, isSel),
        /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "rrm-name" }, r.name, r.is_manager && /* @__PURE__ */ React.createElement("span", { className: "rrm-badge" }, "Manager")), /* @__PURE__ */ React.createElement("div", { className: "rrm-email" }, r.email)),
        isCurrent ? /* @__PURE__ */ React.createElement("span", { className: "rrm-current-tag" }, "Current") : isSel ? /* @__PURE__ */ React.createElement("span", { className: "rrm-check" }, "\u2713") : /* @__PURE__ */ React.createElement("span", { className: "rrm-radio" })
      );
    })), err && /* @__PURE__ */ React.createElement("div", { className: "rrm-err" }, err), /* @__PURE__ */ React.createElement("div", { className: "rrm-foot" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: onClose, disabled: saving }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: () => onConfirm(repId), disabled: !canSubmit }, saving ? "Reassigning\u2026" : selected ? "Reassign to " + selected.name.split(/\s+/)[0] : "Reassign"))));
  }
  function RepCustomerEditModal({ customer, onClose, onSaved }) {
    const [form, setForm] = useState({
      first_name: customer.first_name || "",
      middle_initial: customer.middle_initial || "",
      last_name: customer.last_name || "",
      email: customer.email || "",
      phone: customer.phone || "",
      company_name: customer.company_name || "",
      address_line1: customer.address_line1 || "",
      address_line2: customer.address_line2 || "",
      city: customer.city || "",
      state: customer.state || "",
      zip: customer.zip || ""
    });
    const [saving, setSaving] = useState(false);
    const [error, setError] = useState("");
    const set = (k) => (e) => setForm((f) => ({ ...f, [k]: e.target.value }));
    const canSave = !!form.first_name.trim() && !!form.last_name.trim() && !!form.email.trim() && isValidEmail(form.email.trim()) && !!form.phone.trim() && isValidPhone(form.phone);
    const submit = () => {
      setError("");
      if (!form.first_name.trim() || !form.last_name.trim() || !form.email.trim()) {
        setError("First name, last name, and email are required.");
        return;
      }
      if (!isValidEmail(form.email.trim())) {
        setError("Enter a valid email.");
        return;
      }
      if (!form.phone.trim() || !isValidPhone(form.phone)) {
        setError("A valid 10-digit phone is required.");
        return;
      }
      setSaving(true);
      repFetch("/api/rep/customers/" + encodeURIComponent(customer.id), { method: "PUT", body: JSON.stringify(form) }).then(() => {
        onSaved();
      }).catch((err) => {
        setError(err && err.message || "Could not save changes.");
        setSaving(false);
      });
    };
    const inputStyle = { width: "100%", padding: "9px 12px", border: "0.5px solid var(--stone-300)", background: "#fff", font: "400 14px/1.4 Inter, sans-serif", color: "var(--stone-900)", boxSizing: "border-box" };
    const fieldWrap = (label, node, required, wide) => /* @__PURE__ */ React.createElement("div", { style: wide ? { gridColumn: "1 / -1" } : void 0 }, /* @__PURE__ */ React.createElement("label", { className: "cdv-field-k", style: { display: "block", marginBottom: 6 } }, label, required && /* @__PURE__ */ React.createElement("span", { style: { color: "#c54731" } }, " *")), node);
    const onKey = (e) => {
      if (e.key === "Enter") submit();
    };
    return /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => !saving && onClose() }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", style: { maxWidth: 640, width: "92%" }, onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 22px 0" } }, /* @__PURE__ */ React.createElement("div", { className: "rrm-eyebrow" }, "Edit customer"), /* @__PURE__ */ React.createElement("h3", { className: "rrm-title", style: { marginBottom: 4 } }, "Edit ", /* @__PURE__ */ React.createElement("em", null, [customer.first_name, customer.last_name].filter(Boolean).join(" ") || "customer"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 22px 0", maxHeight: "70vh", overflowY: "auto" } }, error && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 14px", marginBottom: 16, background: "rgba(197,71,49,0.08)", border: "0.5px solid rgba(197,71,49,0.4)", color: "#c54731", font: "400 13px/1.4 Inter, sans-serif" } }, error), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 18 } }, fieldWrap("First name", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.first_name, onChange: set("first_name"), onKeyDown: onKey, autoFocus: true }), true), fieldWrap("Last name", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.last_name, onChange: set("last_name"), onKeyDown: onKey }), true), fieldWrap("Middle initial (optional)", /* @__PURE__ */ React.createElement("input", { style: inputStyle, maxLength: 4, value: form.middle_initial, onChange: set("middle_initial"), onKeyDown: onKey })), fieldWrap("Email", /* @__PURE__ */ React.createElement("input", { type: "email", style: inputStyle, value: form.email, onChange: set("email"), onKeyDown: onKey }), true, true), fieldWrap("Phone", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.phone, onChange: (e) => setForm((f) => ({ ...f, phone: formatPhone(e.target.value) })), onKeyDown: onKey }), true), fieldWrap("Company (optional)", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.company_name, onChange: set("company_name"), onKeyDown: onKey }))), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 } }, fieldWrap("Street address", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.address_line1, onChange: set("address_line1"), onKeyDown: onKey }), false, true), fieldWrap("Apt / suite", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.address_line2, onChange: set("address_line2"), onKeyDown: onKey }), false, true), fieldWrap("City", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.city, onChange: set("city"), onKeyDown: onKey })), fieldWrap("State", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.state, onChange: set("state"), onKeyDown: onKey })), fieldWrap("ZIP", /* @__PURE__ */ React.createElement("input", { style: inputStyle, value: form.zip, onChange: set("zip"), onKeyDown: onKey })))), /* @__PURE__ */ React.createElement("div", { className: "rrm-foot", style: { padding: "18px 22px" } }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: onClose, disabled: saving }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: submit, disabled: saving || !canSave }, saving ? "Saving\u2026" : "Save changes"))));
  }
  function RepCustomerDetailView({ navigate, editId }) {
    const [customer, setCustomer] = useState(null);
    const [orders, setOrders] = useState([]);
    const [notes, setNotes] = useState([]);
    const [stats, setStats] = useState(null);
    const [loading, setLoading] = useState(true);
    const [quotes, setQuotes] = useState([]);
    const [estimates, setEstimates] = useState([]);
    const [samples, setSamples] = useState([]);
    const [visits, setVisits] = useState([]);
    const [transactions, setTransactions] = useState([]);
    const [paymentRequests, setPaymentRequests] = useState([]);
    const [timeline, setTimeline] = useState([]);
    const [timelineLoading, setTimelineLoading] = useState(true);
    const [timelineFilter, setTimelineFilter] = useState("all");
    const [creditOpen, setCreditOpen] = useState(false);
    const [creditEntries, setCreditEntries] = useState(null);
    const [creditLoading, setCreditLoading] = useState(false);
    const [activeTab, setActiveTab] = useState("overview");
    const [ordersFilter, setOrdersFilter] = useState("all");
    const [showAllActivity, setShowAllActivity] = useState(false);
    const [materials, setMaterials] = useState([]);
    const [uniqueSkus, setUniqueSkus] = useState(0);
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const [showReassign, setShowReassign] = useState(false);
    const [reassignSaving, setReassignSaving] = useState(false);
    const [showEdit, setShowEdit] = useState(false);
    const [reloadKey, setReloadKey] = useState(0);
    const parseId = (compositeId) => {
      if (!compositeId) return { type: null, ref: null };
      const idx = compositeId.indexOf("_");
      if (idx === -1) return { type: null, ref: null };
      const type2 = compositeId.substring(0, idx);
      const ref2 = compositeId.substring(idx + 1);
      return { type: type2, ref: ref2 };
    };
    const { type, ref } = parseId(editId);
    useEffect(() => {
      if (!type || !ref) return;
      setLoading(true);
      setTimelineLoading(true);
      Promise.all([
        repFetch("/api/rep/customers/" + encodeURIComponent(ref) + "?type=" + type),
        repFetch("/api/rep/customers/" + encodeURIComponent(ref) + "/timeline?type=" + type),
        repFetch("/api/rep/customers/" + encodeURIComponent(ref) + "/materials?type=" + type).catch(() => ({ materials: [], unique_skus: 0 }))
      ]).then(([data, tlData, matData]) => {
        setCustomer(data.customer);
        setOrders(data.orders || []);
        setNotes(data.notes || []);
        setQuotes(data.quotes || []);
        setEstimates(data.estimates || []);
        setSamples(data.samples || []);
        setVisits(data.visits || []);
        setTransactions(data.transactions || []);
        setPaymentRequests(data.payment_requests || []);
        setStats(data.stats || {});
        setTimeline(tlData.timeline || []);
        setMaterials(matData && matData.materials || []);
        setUniqueSkus(matData && matData.unique_skus || 0);
        setLoading(false);
        setTimelineLoading(false);
      }).catch(() => {
        setLoading(false);
        setTimelineLoading(false);
      });
    }, [editId, reloadKey]);
    const submitReassign = async (repId) => {
      setReassignSaving(true);
      try {
        const data = await repFetch("/api/rep/customers/" + encodeURIComponent(ref) + "/assign-rep", {
          method: "PUT",
          body: JSON.stringify({ rep_id: repId })
        });
        setShowReassign(false);
        const n = data.reassigned_orders || 0;
        alert("Customer reassigned" + (n ? " \xB7 " + n + " open order" + (n !== 1 ? "s" : "") + " moved too" : " \xB7 no open orders to move"));
        setReloadKey((k) => k + 1);
      } catch (err) {
        alert(err.message || "Failed to reassign customer");
      } finally {
        setReassignSaving(false);
      }
    };
    const toggleCredit = () => {
      const next = !creditOpen;
      setCreditOpen(next);
      if (next && creditEntries === null && type && ref) {
        setCreditLoading(true);
        repFetch("/api/rep/store-credit?type=" + type + "&id=" + encodeURIComponent(ref)).then((data) => {
          setCreditEntries(data.entries || []);
          setCreditLoading(false);
        }).catch(() => {
          setCreditEntries([]);
          setCreditLoading(false);
        });
      }
    };
    const timelineConfig = {
      order_placed: { label: "Order", dotColor: "#2563eb", badgeBg: "#dbeafe", badgeText: "#1e40af", icon: "M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2", clickable: true },
      status_change: { label: "Status", dotColor: "var(--stone-400)", badgeBg: "var(--stone-100)", badgeText: "var(--stone-600)", icon: "M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15", clickable: true },
      payment: { label: "Payment", dotColor: "var(--green)", badgeBg: "var(--green-light)", badgeText: "var(--green)", icon: "M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z", clickable: true },
      sample_request: { label: "Sample", dotColor: "#d97706", badgeBg: "#fef3c7", badgeText: "#92400e", icon: "M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4", clickable: false },
      standalone_sample: { label: "Sample", dotColor: "#d97706", badgeBg: "#fef3c7", badgeText: "#92400e", icon: "M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4", clickable: true },
      quote: { label: "Quote", dotColor: "#7c3aed", badgeBg: "#ede9fe", badgeText: "#6d28d9", icon: "M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z", clickable: true },
      note: { label: "Note", dotColor: "var(--stone-500)", badgeBg: "var(--stone-100)", badgeText: "var(--stone-600)", icon: "M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z", clickable: false },
      showroom_visit: { label: "Visit", dotColor: "#0d9488", badgeBg: "#ccfbf1", badgeText: "#0f766e", icon: "M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4", clickable: true },
      installation_inquiry: { label: "Install", dotColor: "#ea580c", badgeBg: "#fff7ed", badgeText: "#c2410c", icon: "M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z M15 12a3 3 0 11-6 0 3 3 0 016 0z", clickable: false }
    };
    const handleTimelineClick = (evt) => {
      if (evt.entity_type === "order") navigate("order-detail", evt.entity_id);
      else if (evt.entity_type === "quote") navigate("quote-edit", evt.entity_id);
      else if (evt.entity_type === "visit") navigate("visit-detail", evt.entity_id);
      else if (evt.entity_type === "sample") navigate("sample-detail", evt.entity_id);
    };
    const timelineFilterGroups = {
      all: null,
      orders: ["order_placed", "status_change", "payment"],
      quotes: ["quote"],
      samples: ["sample_request", "standalone_sample"],
      visits: ["showroom_visit"],
      installs: ["installation_inquiry"],
      notes: ["note"]
    };
    const filteredTimeline = timelineFilter === "all" ? timeline : timeline.filter((evt) => (timelineFilterGroups[timelineFilter] || []).includes(evt.event_type));
    const pendingPaymentOrderIds = new Set(paymentRequests.map((pr) => pr.order_id));
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "cdv-empty", style: { padding: "48px 22px" } }, "Loading customer", "\u2026");
    if (!customer) return /* @__PURE__ */ React.createElement("div", { className: "cdv-empty", style: { padding: "48px 22px" } }, "Customer not found");
    const money = (v) => "$" + (parseFloat(v) || 0).toLocaleString(void 0, { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const money0 = (v) => "$" + Math.round(parseFloat(v) || 0).toLocaleString();
    const fmtDate = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }) : "\u2014";
    const fmtMY = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", year: "numeric" }) : "\u2014";
    const initials = (name) => {
      if (!name) return "?";
      const p = name.trim().split(/\s+/);
      return (p.length === 1 ? p[0].slice(0, 2) : p[0][0] + p[p.length - 1][0]).toUpperCase();
    };
    const isTerminal = (s) => ["delivered", "cancelled", "refunded"].includes((s || "").toLowerCase());
    const statusTone = (s) => {
      s = (s || "").toLowerCase();
      if (s === "delivered") return "#3a7a4e";
      if (s === "cancelled" || s === "refunded") return "#c54731";
      if (["shipped", "confirmed", "processing", "pending"].includes(s)) return "#a87935";
      return "var(--brass-muted)";
    };
    const st = stats || {};
    const activeOrders = orders.filter((o) => !isTerminal(o.status));
    const displayName = customer.name || customer.email || "Customer";
    const nameParts = displayName.trim().split(/\s+/);
    const nameHead = nameParts.length > 1 ? nameParts.slice(0, -1).join(" ") : "";
    const nameTail = nameParts[nameParts.length - 1];
    const isTrade = customer.customer_type === "trade";
    const typeLabel = isTrade ? customer.tier_name ? customer.tier_name : "Trade Pro" : customer.customer_type === "guest" ? "Guest" : "Retail";
    const yrs = customer.created_at ? Math.floor((Date.now() - new Date(customer.created_at).getTime()) / (365 * 864e5)) : null;
    const badgeBits = [typeLabel, isTrade && customer.business_type, yrs >= 1 && yrs + "-yr customer"].filter(Boolean);
    const creditTotal = (parseFloat(st.available_credit) || 0) + (parseFloat(st.store_credit_balance) || 0);
    const openBal = parseFloat(st.open_balance) || 0;
    const kpis = [
      { l: "Lifetime value", v: money0(st.total_spent), s: "Avg " + money0(st.avg_order_value) + " \xB7 last " + fmtDate(st.last_order_date) },
      { l: "Orders", v: (st.total_orders || 0) + (activeOrders.length ? " \xB7 " + activeOrders.length + " active" : ""), s: "Since " + fmtDate(st.first_order_date) },
      { l: "Open balance", v: money0(openBal), s: st.pending_payments_count > 0 ? st.pending_payments_count + " payment req \xB7 " + money0(st.pending_payments_total) : openBal > 0.01 ? "No request sent yet" : "Nothing outstanding", tone: openBal > 0.01 ? "#c54731" : "#3a7a4e" },
      { l: "Open quotes", v: String(st.open_quotes_count || 0), s: st.open_quotes_count > 0 ? money0(st.open_quotes_value) + " in play" : "None open" },
      { l: "Account credit", v: money0(creditTotal), s: "Store " + money0(st.store_credit_balance) + " \xB7 overpay " + money0(st.available_credit), tone: creditTotal > 0.01 ? "#3a7a4e" : void 0 },
      isTrade ? { l: "Trade tier", v: (customer.tier_name || "No tier") + (customer.discount_percent ? " \xB7 " + parseFloat(customer.discount_percent) + "%" : ""), s: "Status: " + (customer.status || "\u2014"), tone: "var(--gold)" } : { l: "Touchpoints", v: String(samples.length + visits.length), s: samples.length + " sample" + (samples.length !== 1 ? "s" : "") + " \xB7 " + visits.length + " visit" + (visits.length !== 1 ? "s" : "") }
    ];
    const TABS = [
      { k: "overview", l: "Overview" },
      { k: "orders", l: "Orders \xB7 " + orders.length },
      { k: "quotes", l: "Quotes \xB7 " + quotes.length },
      { k: "estimates", l: "Estimates \xB7 " + estimates.length },
      { k: "samples", l: "Samples \xB7 " + samples.length },
      { k: "visits", l: "Visits \xB7 " + visits.length },
      { k: "payments", l: "Payments" }
    ];
    const matImg = (url) => !url ? null : url.indexOf("http") === 0 ? "/api/img?url=" + encodeURIComponent(url) + "&w=240&h=240&fit=cover" : url;
    const orderFilters = { all: () => true, active: (o) => !isTerminal(o.status), delivered: (o) => (o.status || "").toLowerCase() === "delivered", refunded: (o) => ["cancelled", "refunded"].includes((o.status || "").toLowerCase()) };
    const shownOrders = orders.filter(orderFilters[ordersFilter] || (() => true));
    const field = (k, v, s, wide) => /* @__PURE__ */ React.createElement("div", { key: k, className: wide ? "cdv-field-wide" : "" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-field-k" }, k), /* @__PURE__ */ React.createElement("div", { className: "cdv-field-v" }, v), s && /* @__PURE__ */ React.createElement("div", { className: "cdv-field-s" }, s));
    const TL_STYLE = {
      order_placed: { g: "\u25A4", t: "var(--gold)" },
      status_change: { g: "\u25E6", t: "var(--brass-muted)" },
      payment: { g: "$", t: "#3a7a4e" },
      sample_request: { g: "\u25C7", t: "#a87935" },
      standalone_sample: { g: "\u25C7", t: "#a87935" },
      quote: { g: "\u25A7", t: "#5c6e94" },
      showroom_visit: { g: "\u25C9", t: "#0d9488" },
      installation_inquiry: { g: "\u2691", t: "#c2410c" },
      note: { g: "\u270E", t: "var(--brass-muted)" }
    };
    const tlTime = (ts) => {
      const d = new Date(ts);
      const days = Math.floor((Date.now() - d.getTime()) / 864e5);
      if (days <= 0) return "Today";
      if (days === 1) return "Yesterday";
      if (days < 7) return days + " days ago";
      return d.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });
    };
    const timelineCard = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Activity", !timelineLoading ? " \xB7 " + filteredTimeline.length + " event" + (filteredTimeline.length !== 1 ? "s" : "") : "")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body" }, timelineLoading ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "Loading timeline", "\u2026") : filteredTimeline.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, timelineFilter === "all" ? "No activity yet" : "No " + timelineFilter + " events") : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "qw-thread-list", style: { padding: 0 } }, (showAllActivity ? filteredTimeline : filteredTimeline.slice(0, 8)).map((evt, i) => {
      const cfg = timelineConfig[evt.event_type] || { label: evt.event_type, clickable: false };
      const sty = TL_STYLE[evt.event_type] || { g: "\xB7", t: "var(--brass-muted)" };
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "qw-msg",
          key: i,
          onClick: () => cfg.clickable && handleTimelineClick(evt),
          style: cfg.clickable ? { cursor: "pointer" } : void 0
        },
        /* @__PURE__ */ React.createElement("div", { className: "qw-msg-avatar", style: { background: "transparent", border: "0.5px solid rgba(28,25,23,0.18)", color: sty.t, font: "400 14px/1 'Cormorant Garamond', serif" } }, sty.g),
        /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "qw-msg-head" }, /* @__PURE__ */ React.createElement("span", { className: "qw-msg-tag" }, cfg.label), /* @__PURE__ */ React.createElement("span", { className: "qw-msg-time" }, tlTime(evt.timestamp))), /* @__PURE__ */ React.createElement("div", { className: "qw-msg-body sys" }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--stone-900)" } }, evt.title), evt.description && /* @__PURE__ */ React.createElement("span", null, " ", "\xB7", " ", evt.description), cfg.clickable && /* @__PURE__ */ React.createElement("span", { className: "cdv-tl-view" }, "View ", "\u2192")))
      );
    })), filteredTimeline.length > 8 && /* @__PURE__ */ React.createElement("button", { className: "cdv-card-link", style: { marginTop: 12 }, onClick: () => setShowAllActivity((v) => !v) }, showAllActivity ? "\u2191 Show recent only" : "Full history \xB7 " + filteredTimeline.length + " events \u2192"))));
    const ownerCard = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Sales rep")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body" }, customer.rep_name || customer.assigned_rep_id ? /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, (customer.rep_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name" }, customer.rep_name || "Assigned rep", customer.assigned_rep_id === repInfo.id && /* @__PURE__ */ React.createElement("span", { className: "ws-you-tag" }, "You")), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, customer.assigned_rep_id === repInfo.id && repInfo.email ? repInfo.email : "Dedicated rep for this customer"))) : /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar", style: { background: "var(--brass-warm, #d8cdb6)", color: "var(--brass-muted, #8a7e68)" } }, "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name", style: { color: "var(--rod-muted)" } }, "Unassigned"), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "No rep owns this customer yet")))));
    const addr = [customer.address_line1, customer.address_line2, [customer.city, customer.state, customer.zip].filter(Boolean).join(", ")].filter(Boolean).join(" \xB7 ");
    const gridBlock = /* @__PURE__ */ React.createElement("div", { className: "cdv-grid2" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card", style: { margin: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Profile + ship-to")), /* @__PURE__ */ React.createElement("div", { className: "cdv-fields" }, field("Contact", customer.name || "\u2014", customer.email), field("Phone", customer.phone || "\u2014"), isTrade && field("Business", customer.company_name || "\u2014", customer.business_type), field(isTrade ? "Trade since" : "Customer since", fmtDate(customer.created_at), yrs >= 1 ? yrs + " years" : "New this year"), customer.rep_name && field("Rep", customer.rep_name), isTrade && customer.contractor_license && field("License", customer.contractor_license), field("Default ship-to", addr || "\u2014", null, true))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card", style: { margin: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Payment + balance")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-pay-balance", style: { marginTop: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--gold)", marginBottom: 6 } }, "Outstanding balance"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "300 26px/1 Cormorant Garamond, serif", color: openBal > 0.01 ? "#c54731" : "var(--stone-900)" } }, money(openBal)), st.pending_payments_count > 0 && /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--brass-muted)" } }, st.pending_payments_count, " request pending"))), /* @__PURE__ */ React.createElement("div", { className: "cdv-pay-rows" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-pay-row" }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)" } }, "Lifetime paid"), /* @__PURE__ */ React.createElement("span", { style: { color: "var(--stone-900)" } }, money(st.total_spent))), /* @__PURE__ */ React.createElement("div", { className: "cdv-pay-row" }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)" } }, "Available credit"), /* @__PURE__ */ React.createElement("span", { style: { color: (parseFloat(st.available_credit) || 0) > 0 ? "#3a7a4e" : "var(--stone-900)" } }, money(st.available_credit))), /* @__PURE__ */ React.createElement("div", { className: "cdv-pay-row" }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)" } }, "Store credit"), /* @__PURE__ */ React.createElement("span", { style: { color: (parseFloat(st.store_credit_balance) || 0) > 0 ? "#3a7a4e" : "var(--stone-900)" } }, money(st.store_credit_balance)))))));
    const materialsCard = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Material palette ", "\xB7", " what they buy"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)", marginTop: 4 } }, uniqueSkus, " unique product", uniqueSkus !== 1 ? "s" : "", " ", "\xB7", " across ", orders.length, " order", orders.length !== 1 ? "s" : "")), /* @__PURE__ */ React.createElement("button", { className: "cdv-card-link", onClick: () => navigate("catalog") }, "Browse catalog ", "\u2192")), materials.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No purchased materials yet") : /* @__PURE__ */ React.createElement("div", { className: "cdv-mats" }, materials.map((m) => /* @__PURE__ */ React.createElement("div", { className: "cdv-mat", key: m.product_id, onClick: () => navigate("product-detail", m.sku_id || m.product_id) }, matImg(m.image) ? /* @__PURE__ */ React.createElement("img", { className: "cdv-mat-face", src: matImg(m.image), alt: "", loading: "lazy", onError: (e) => {
      e.currentTarget.removeAttribute("src");
    } }) : /* @__PURE__ */ React.createElement("div", { className: "cdv-mat-face" }), /* @__PURE__ */ React.createElement("div", { className: "cdv-mat-badge" }, "\xD7", m.order_count), /* @__PURE__ */ React.createElement("div", { className: "cdv-mat-label" }, m.product_name), /* @__PURE__ */ React.createElement("div", { className: "cdv-mat-sub" }, "Last ", fmtMY(m.last_purchased))))));
    const ordersTab = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Orders ", "\xB7", " ", orders.length, " lifetime"), /* @__PURE__ */ React.createElement("div", { className: "cdv-filters" }, ["all", "active", "delivered", "refunded"].map((f) => /* @__PURE__ */ React.createElement("button", { key: f, className: "cdv-filter" + (ordersFilter === f ? " active" : ""), onClick: () => setOrdersFilter(f) }, f)))), shownOrders.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No orders in this view") : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-head" }, /* @__PURE__ */ React.createElement("span", null, "Order"), /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null, "Detail"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Total"), /* @__PURE__ */ React.createElement("span", null, "Status"), /* @__PURE__ */ React.createElement("span", null)), shownOrders.map((o) => {
      const bal = (parseFloat(o.total) || 0) - (parseFloat(o.amount_paid) || 0);
      return /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: o.id, onClick: () => navigate("order-detail", o.id) }, /* @__PURE__ */ React.createElement("span", { className: "cdv-o-id" }, o.order_number), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date" }, fmtDate(o.created_at)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-proj" }, (o.item_count || 0) + " item" + (o.item_count !== 1 ? "s" : ""), bal > 0.01 ? " \xB7 " + money0(bal) + " due" : ""), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-total" }, money(o.total)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-status", style: { color: statusTone(o.status) } }, "\u25CF", " ", o.status, pendingPaymentOrderIds.has(o.id) ? " \xB7 pay req" : ""), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", color: "var(--brass-muted)", font: "400 14px/1 Cormorant Garamond, serif" } }, "\u2192"));
    })));
    const quotesTab = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Quotes ", "\xB7", " ", quotes.length)), quotes.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No quotes yet") : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-head" }, /* @__PURE__ */ React.createElement("span", null, "Quote"), /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null, "Detail"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Total"), /* @__PURE__ */ React.createElement("span", null, "Status"), /* @__PURE__ */ React.createElement("span", null)), quotes.map((q) => /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: q.id, onClick: () => navigate("quote-edit", q.id) }, /* @__PURE__ */ React.createElement("span", { className: "cdv-o-id" }, q.quote_number), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date" }, fmtDate(q.created_at)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-proj" }, (q.item_count || 0) + " item" + (q.item_count !== 1 ? "s" : ""), q.expires_at ? " \xB7 exp " + fmtDate(q.expires_at) : ""), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-total" }, money(q.total)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-status", style: { color: "#5c6e94" } }, "\u25CF", " ", q.status), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", color: "var(--brass-muted)", font: "400 14px/1 Cormorant Garamond, serif" } }, "\u2192")))));
    const txnLabel = (t) => String(t.payment_method || t.payment_type || "Payment").replace(/_/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());
    const paymentsTab = /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-pay-grid" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card", style: { margin: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Balance")), /* @__PURE__ */ React.createElement("div", { className: "cdv-card-body" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-bal" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-bal-label" }, "Outstanding balance"), /* @__PURE__ */ React.createElement("div", { className: "cdv-bal-amt", style: { color: openBal > 0.01 ? "#c54731" : "#3a7a4e" } }, money(openBal)), /* @__PURE__ */ React.createElement("div", { className: "cdv-bal-note" }, openBal > 0.01 ? st.pending_payments_count > 0 ? st.pending_payments_count + " payment request" + (st.pending_payments_count !== 1 ? "s" : "") + " pending \xB7 " + money(st.pending_payments_total) : "No payment request sent yet" : "Account is current \u2014 nothing owed")), /* @__PURE__ */ React.createElement("div", { className: "cdv-bal-rows" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-bal-row" }, /* @__PURE__ */ React.createElement("span", { className: "k" }, "Lifetime paid"), /* @__PURE__ */ React.createElement("span", { className: "v" }, money(st.total_spent))), /* @__PURE__ */ React.createElement("div", { className: "cdv-bal-row" }, /* @__PURE__ */ React.createElement("span", { className: "k" }, "Available credit"), /* @__PURE__ */ React.createElement("span", { className: "v", style: { color: (parseFloat(st.available_credit) || 0) > 0.01 ? "#3a7a4e" : void 0 } }, money(st.available_credit))), /* @__PURE__ */ React.createElement("div", { className: "cdv-bal-row" }, /* @__PURE__ */ React.createElement("span", { className: "k" }, "Store credit"), /* @__PURE__ */ React.createElement("span", { className: "v", style: { color: (parseFloat(st.store_credit_balance) || 0) > 0.01 ? "#3a7a4e" : void 0 } }, money(st.store_credit_balance)))))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card", style: { margin: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Payment requests ", "\xB7", " ", paymentRequests.length, " pending")), paymentRequests.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No pending payment requests") : /* @__PURE__ */ React.createElement("div", null, paymentRequests.map((pr) => /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: pr.id, style: { gridTemplateColumns: "1fr auto", cursor: "pointer" }, onClick: () => navigate("order-detail", pr.order_id) }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-o-id" }, pr.order_number), /* @__PURE__ */ React.createElement("div", { className: "cdv-o-date" }, "Sent ", fmtDate(pr.created_at), pr.expires_at ? " \xB7 exp " + fmtDate(pr.expires_at) : "")), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-total" }, money(pr.amount))))))), /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Transactions ", "\xB7", " ", transactions.length)), transactions.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No payments recorded yet") : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-head", style: { gridTemplateColumns: "120px 1fr 130px 120px" } }, /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null, "Method"), /* @__PURE__ */ React.createElement("span", null, "Order"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Amount")), transactions.map((t) => {
      const refund = t.is_refund;
      return /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: t.id, style: { gridTemplateColumns: "120px 1fr 130px 120px" }, onClick: () => navigate("order-detail", t.order_id) }, /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date" }, fmtDate(t.created_at)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-proj" }, refund ? "Refund" : txnLabel(t), t.description ? " \xB7 " + t.description : ""), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-id" }, t.order_number), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-total", style: { color: refund ? "#c54731" : "#3a7a4e" } }, refund ? "\u2212" : "", money(Math.abs(parseFloat(t.amount) || 0))));
    }))));
    const estStatusTone = (s) => {
      s = (s || "").toLowerCase();
      if (s === "accepted") return "#3a7a4e";
      if (s === "declined" || s === "expired") return "#c54731";
      if (s === "sent") return "#a87935";
      return "var(--brass-muted)";
    };
    const estimatesTab = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Estimates ", "\xB7", " ", estimates.length), /* @__PURE__ */ React.createElement("button", { className: "cdv-card-link", onClick: () => navigate("estimate-create") }, "+ New estimate")), estimates.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No estimates yet") : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-head" }, /* @__PURE__ */ React.createElement("span", null, "Estimate"), /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null, "Project"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Total"), /* @__PURE__ */ React.createElement("span", null, "Status"), /* @__PURE__ */ React.createElement("span", null)), estimates.map((e) => {
      const es = e.effective_status || e.status;
      return /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: e.id, onClick: () => navigate("estimate-edit", e.id) }, /* @__PURE__ */ React.createElement("span", { className: "cdv-o-id" }, e.estimate_number), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date" }, fmtDate(e.created_at)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-proj" }, e.project_name || (e.item_count || 0) + " item" + (e.item_count !== 1 ? "s" : ""), e.expires_at && es === "sent" ? " \xB7 exp " + fmtDate(e.expires_at) : ""), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-total" }, money(e.total)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-status", style: { color: estStatusTone(es) } }, "\u25CF", " ", es, e.converted_order_id ? " \xB7 ordered" : e.converted_quote_id ? " \xB7 quoted" : ""), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", color: "var(--brass-muted)", font: "400 14px/1 Cormorant Garamond, serif" } }, "\u2192"));
    })));
    const sampleTone = (s) => {
      s = (s || "").toLowerCase();
      if (s === "delivered") return "#3a7a4e";
      if (s === "cancelled") return "#c54731";
      if (s === "shipped") return "#5c6e94";
      return "#a87935";
    };
    const samplesTab = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Sample requests ", "\xB7", " ", samples.length), /* @__PURE__ */ React.createElement("button", { className: "cdv-card-link", onClick: () => navigate("sample-create") }, "+ New sample")), samples.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No sample requests yet") : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-head", style: { gridTemplateColumns: "130px 110px 1fr 150px 24px" } }, /* @__PURE__ */ React.createElement("span", null, "Request"), /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null, "Detail"), /* @__PURE__ */ React.createElement("span", null, "Status"), /* @__PURE__ */ React.createElement("span", null)), samples.map((s) => /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: s.id, style: { gridTemplateColumns: "130px 110px 1fr 150px 24px" }, onClick: () => navigate("sample-detail", s.id) }, /* @__PURE__ */ React.createElement("span", { className: "cdv-o-id" }, s.request_number), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date" }, fmtDate(s.created_at)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-proj" }, (s.item_count || 0) + " sample" + (s.item_count !== 1 ? "s" : ""), s.delivery_method === "pickup" ? " \xB7 In-store pickup" : s.delivery_method ? " \xB7 " + s.delivery_method.charAt(0).toUpperCase() + s.delivery_method.slice(1) : "", s.tracking_number ? " \xB7 " + s.tracking_number : ""), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-status", style: { color: sampleTone(s.status) } }, "\u25CF", " ", s.status), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", color: "var(--brass-muted)", font: "400 14px/1 Cormorant Garamond, serif" } }, "\u2192")))));
    const visitTone = (s) => {
      s = (s || "").toLowerCase();
      if (s === "carted" || s === "quote_requested") return "#3a7a4e";
      if (s === "opened") return "#5c6e94";
      if (s === "expired") return "#c54731";
      return "#a87935";
    };
    const visitsTab = /* @__PURE__ */ React.createElement("div", { className: "cdv-card" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-card-head" }, /* @__PURE__ */ React.createElement("h3", { className: "cdv-card-title" }, "Showroom visits ", "\xB7", " ", visits.length), /* @__PURE__ */ React.createElement("button", { className: "cdv-card-link", onClick: () => navigate("visit-create") }, "+ New visit")), visits.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "cdv-empty" }, "No showroom visits yet") : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-head", style: { gridTemplateColumns: "150px 1fr 150px 24px" } }, /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null, "Detail"), /* @__PURE__ */ React.createElement("span", null, "Status"), /* @__PURE__ */ React.createElement("span", null)), visits.map((v) => /* @__PURE__ */ React.createElement("div", { className: "cdv-otable-row", key: v.id, style: { gridTemplateColumns: "150px 1fr 150px 24px" }, onClick: () => navigate("visit-detail", v.id) }, /* @__PURE__ */ React.createElement("span", { className: "cdv-o-date" }, fmtDate(v.created_at)), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-proj" }, (v.item_count || 0) + " item" + (v.item_count !== 1 ? "s" : ""), v.status === "carted" ? " \xB7 added to cart" : "", v.quote_requested_at ? " \xB7 quote requested" : ""), /* @__PURE__ */ React.createElement("span", { className: "cdv-o-status", style: { color: visitTone(v.status) } }, "\u25CF", " ", v.status), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", color: "var(--brass-muted)", font: "400 14px/1 Cormorant Garamond, serif" } }, "\u2192")))));
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumbs" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-crumb-trail" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("customers") }, "Customers"), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-cur" }, displayName)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 8 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 12 } }, /* @__PURE__ */ React.createElement(RepPrevNext, { section: "customers", id: editId, view: "customer-detail", navigate, linkStyle: { color: "var(--brass-muted)", textDecoration: "none" } })), /* @__PURE__ */ React.createElement("span", { className: "cdv-crumb-status", style: { color: activeOrders.length ? "#3a7a4e" : "var(--brass-muted)" } }, "\u25CF", " ", typeLabel, activeOrders.length ? " \xB7 " + activeOrders.length + " active order" + (activeOrders.length !== 1 ? "s" : "") : " \xB7 no open work"))), /* @__PURE__ */ React.createElement("div", { className: "cdv-head" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-head-id" }, /* @__PURE__ */ React.createElement("div", { className: "cdv-avatar" }, initials(displayName)), /* @__PURE__ */ React.createElement("div", null, badgeBits.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "cdv-badge" }, "\u25CF", " ", badgeBits.join(" \xB7 ")), /* @__PURE__ */ React.createElement("h1", { className: "cdv-name" }, nameHead ? /* @__PURE__ */ React.createElement(React.Fragment, null, nameHead, " ", /* @__PURE__ */ React.createElement("em", null, nameTail)) : /* @__PURE__ */ React.createElement("em", null, nameTail)), /* @__PURE__ */ React.createElement("div", { className: "cdv-contact" }, isTrade && customer.company_name && /* @__PURE__ */ React.createElement(React.Fragment, null, customer.company_name, " ", "\xB7", " "), customer.city && /* @__PURE__ */ React.createElement(React.Fragment, null, customer.city, " ", "\xB7", " "), customer.email && /* @__PURE__ */ React.createElement("a", { href: "mailto:" + customer.email }, customer.email), customer.phone && /* @__PURE__ */ React.createElement(React.Fragment, null, " ", "\xB7", " ", customer.phone)))), /* @__PURE__ */ React.createElement("div", { className: "cdv-head-actions" }, /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: () => navigate("customers") }, "\u2190", " Back"), type === "retail" && customer.id && /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: () => setShowEdit(true) }, "Edit"), customer.email && /* @__PURE__ */ React.createElement("a", { className: "cdv-btn", href: "mailto:" + customer.email }, "Email"), /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: () => navigate("estimate-create") }, "+ Estimate"), repInfo.is_manager && type === "retail" && /* @__PURE__ */ React.createElement("button", { className: "cdv-btn", onClick: () => setShowReassign(true) }, "Reassign rep"), /* @__PURE__ */ React.createElement(
      AddTaskButton,
      {
        navigate,
        className: "cdv-btn",
        label: "+ Task",
        prefill: { title: "Follow up with " + displayName, linked_customer_type: type, linked_customer_ref: ref, customer_name: customer.name, customer_email: customer.email, customer_phone: customer.phone, context_label: displayName }
      }
    ), /* @__PURE__ */ React.createElement("button", { className: "cdv-btn primary", onClick: () => navigate("quote-create") }, "+ New quote"))), showReassign && /* @__PURE__ */ React.createElement(
      ReassignRepModal,
      {
        title: "Reassign " + displayName,
        subtitle: "Moves this customer to the chosen rep. Their " + activeOrders.length + " open order" + (activeOrders.length !== 1 ? "s" : "") + " move too; closed orders keep their current rep.",
        currentRepId: customer.assigned_rep_id || "",
        currentRepName: customer.rep_name,
        saving: reassignSaving,
        onClose: () => setShowReassign(false),
        onConfirm: submitReassign
      }
    ), showEdit && /* @__PURE__ */ React.createElement(
      RepCustomerEditModal,
      {
        customer,
        onClose: () => setShowEdit(false),
        onSaved: () => {
          setShowEdit(false);
          setReloadKey((k) => k + 1);
        }
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "cdv-kpis" }, kpis.map((k) => /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi", key: k.l }, /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi-l" }, k.l), /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi-v", style: k.tone ? { color: k.tone } : void 0 }, k.v), /* @__PURE__ */ React.createElement("div", { className: "cdv-kpi-s" }, k.s)))), /* @__PURE__ */ React.createElement("div", { className: "cdv-tabs" }, TABS.map((t) => /* @__PURE__ */ React.createElement("button", { key: t.k, className: "cdv-tab" + (activeTab === t.k ? " active" : ""), onClick: () => setActiveTab(t.k) }, t.l))), activeTab === "overview" && /* @__PURE__ */ React.createElement("div", null, ownerCard, timelineCard, gridBlock, materialsCard), activeTab === "orders" && ordersTab, activeTab === "quotes" && quotesTab, activeTab === "estimates" && estimatesTab, activeTab === "samples" && samplesTab, activeTab === "visits" && visitsTab, activeTab === "payments" && paymentsTab);
  }
  function parseHash() {
    const hash = window.location.hash.replace(/^#\/?/, "");
    if (!hash) return { view: "dashboard", id: null };
    const parts = hash.split("/");
    return { view: parts[0] || "dashboard", id: parts[1] || null };
  }
  function buildHash(view, id) {
    if (view === "dashboard") return "";
    return id ? "#" + view + "/" + id : "#" + view;
  }
  function RepLayout({ onLogout }) {
    const initialRoute = parseHash();
    const [view, setView] = useState(initialRoute.view);
    const [editId, setEditId] = useState(initialRoute.id);
    const [unreadCount, setUnreadCount] = useState(0);
    const [showNotifDropdown, setShowNotifDropdown] = useState(false);
    const [notifList, setNotifList] = useState([]);
    const [notifLoading, setNotifLoading] = useState(false);
    const [showCost, setShowCost] = useState(true);
    const [sidebarOpen, setSidebarOpen] = useState(false);
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const notifRef = useRef(null);
    const [showSearch, setShowSearch] = useState(false);
    const [searchQuery, setSearchQuery] = useState("");
    const terminalRef = useRef(null);
    const [terminalStatus, setTerminalStatus] = useState("disconnected");
    useEffect(() => {
      setTerminalStatus("connected");
    }, []);
    const connectReader = () => {
    };
    const disconnectReader = () => {
    };
    useEffect(() => {
      const mq = window.matchMedia("(max-width: 768px)");
      const handler = (e) => {
        if (!e.matches) setSidebarOpen(false);
      };
      mq.addEventListener("change", handler);
      history.replaceState({ view: initialRoute.view, id: initialRoute.id }, "");
      const onKeyDown = (e) => {
        if ((e.metaKey || e.ctrlKey) && e.key === "k") {
          e.preventDefault();
          setShowSearch(true);
        }
      };
      window.addEventListener("keydown", onKeyDown);
      return () => {
        mq.removeEventListener("change", handler);
        window.removeEventListener("keydown", onKeyDown);
      };
    }, []);
    const [releaseIntent, setReleaseIntent] = useState(null);
    const navigate = (v, id, opts) => {
      rofDraftOnLeave();
      setView(v);
      setEditId(id || null);
      setReleaseIntent(v === "release-flow" && opts && opts.method ? opts.method : null);
      const hash = buildHash(v, id);
      if (window.location.hash !== (hash || "#")) {
        const method = opts && opts.replace ? "replaceState" : "pushState";
        history[method]({ view: v, id: id || null }, "", hash || window.location.pathname);
      }
      window.scrollTo(0, 0);
      setShowNotifDropdown(false);
      setSidebarOpen(false);
    };
    useEffect(() => {
      const onPopState = () => {
        rofDraftOnLeave();
        const route = parseHash();
        setView(route.view);
        setEditId(route.id);
        window.scrollTo(0, 0);
        setShowNotifDropdown(false);
        setSidebarOpen(false);
      };
      window.addEventListener("popstate", onPopState);
      return () => window.removeEventListener("popstate", onPopState);
    }, []);
    const [tasksDueCount, setTasksDueCount] = useState(0);
    const [tasksOverdueCount, setTasksOverdueCount] = useState(0);
    const pollBadges = useCallback(() => {
      repFetch("/api/rep/notifications/count").then((d) => setUnreadCount(d.unread_count || 0)).catch(() => {
      });
      repFetch("/api/rep/tasks/count").then((d) => {
        setTasksDueCount(d.due_count || 0);
        setTasksOverdueCount(d.overdue_count || 0);
      }).catch(() => {
      });
    }, []);
    useEffect(() => {
      pollBadges();
      const interval = setInterval(pollBadges, 45e3);
      return () => clearInterval(interval);
    }, [pollBadges]);
    useEffect(() => {
      const handler = (e) => {
        if (notifRef.current && !notifRef.current.contains(e.target)) {
          setShowNotifDropdown(false);
        }
      };
      document.addEventListener("mousedown", handler);
      return () => document.removeEventListener("mousedown", handler);
    }, []);
    const openNotifDropdown = () => {
      if (showNotifDropdown) {
        setShowNotifDropdown(false);
        return;
      }
      setShowNotifDropdown(true);
      setNotifLoading(true);
      repFetch("/api/rep/notifications?limit=15").then((d) => {
        setNotifList(d.notifications || []);
        setUnreadCount(d.unread_count || 0);
        setNotifLoading(false);
      }).catch(() => setNotifLoading(false));
    };
    const markAllRead = () => {
      repFetch("/api/rep/notifications/read-all", { method: "PUT" }).then(() => {
        setUnreadCount(0);
        setNotifList((prev) => prev.map((n) => ({ ...n, is_read: true })));
      }).catch(() => {
      });
    };
    const handleNotifClick = (n) => {
      if (!n.is_read) {
        repFetch("/api/rep/notifications/" + n.id + "/read", { method: "PUT" }).catch(() => {
        });
        setNotifList((prev) => prev.map((x) => x.id === n.id ? { ...x, is_read: true } : x));
        setUnreadCount((prev) => Math.max(0, prev - 1));
      }
      const link = notifDeepLink(n);
      if (link) {
        navigate(link.view, link.id || n.entity_id);
      } else {
        setShowNotifDropdown(false);
      }
    };
    const navSections = [
      { label: "Workspace", items: [
        { key: "dashboard", name: "Dashboard", glyph: "\u25E7" },
        { key: "tasks", name: "Tasks", glyph: "\u25C7" }
      ] },
      { label: "Field", items: [
        { key: "catalog", name: "SKU lookup", glyph: "\u229E" },
        { key: "visits", name: "Visits", glyph: "\u25CE" }
      ] },
      { label: "Sales", items: [
        { key: "orders", name: "Orders", glyph: "\u259A" },
        { key: "purchase-orders", name: "Purchase orders", glyph: "\u2318" },
        { key: "quotes", name: "Quotes", glyph: "\u2398" },
        { key: "estimates", name: "Construction Estimates", glyph: "\u2637" },
        { key: "samples", name: "Sample requests", glyph: "\u2317" }
      ] },
      { label: "Directory", items: [
        { key: "customers", name: "Customers", glyph: "\u25C9" },
        { key: "vendors", name: "Vendors", glyph: "\u25C6" }
      ] },
      // Managers only: review and approve/reject trade membership applications.
      ...repInfo.is_manager ? [{ label: "Trade", items: [
        { key: "trade-applications", name: "Trade applications", glyph: "\u2691" }
      ] }] : [],
      { label: "Reports", items: [
        { key: "commissions", name: "Commission", glyph: "$" },
        // Managers only: view any rep's commission ledger.
        ...repInfo.is_manager ? [{ key: "team-commissions", name: "Team commissions", glyph: "\u25CE" }] : [],
        { key: "cash-drawer", name: "Cash drawer", glyph: "\u2338" }
      ] }
    ];
    const activeBase = view === "commissions" ? "commissions" : view === "team-commissions" ? "team-commissions" : view === "cash-drawer" ? "cash-drawer" : view === "tasks" ? "tasks" : view === "purchase-orders" || view === "purchase-order-detail" ? "purchase-orders" : view === "visits" || view === "visit-create" || view === "visit-detail" ? "visits" : view === "samples" || view === "sample-create" || view === "sample-detail" ? "samples" : view.split("-")[0] === "product" ? "catalog" : view === "catalog" ? "catalog" : view === "vendors" || view === "vendor-detail" ? "vendors" : view.split("-")[0] === "order" ? "orders" : view.split("-")[0] === "quote" ? "quotes" : view.split("-")[0] === "estimate" ? "estimates" : view.split("-")[0] === "customer" ? "customers" : view === "search-results" ? "search-results" : view;
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("header", { className: "rep-topbar" }, /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-left" }, /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-wordmark" }, "Roma"), /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-badge" }, "Rep portal")), /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-search", onClick: () => setShowSearch(true) }, /* @__PURE__ */ React.createElement("span", { className: "rep-topbar-search-icon" }, "\u2315"), /* @__PURE__ */ React.createElement("input", { placeholder: "Search customers, orders, SKUs, quotes\u2026", readOnly: true, tabIndex: -1 }), /* @__PURE__ */ React.createElement("span", { className: "rep-topbar-search-kbd" }, "\u2318", " K")), /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-right" }, /* @__PURE__ */ React.createElement("button", { className: "rep-topbar-btn", onClick: () => navigate("quote-create") }, /* @__PURE__ */ React.createElement("span", { className: "glyph" }, "\u271A"), " New"), /* @__PURE__ */ React.createElement("button", { className: "rep-topbar-btn", onClick: () => navigate("tasks") }, /* @__PURE__ */ React.createElement("span", { className: "glyph" }, "\u25C7"), " Tasks", tasksDueCount > 0 && /* @__PURE__ */ React.createElement("span", { className: "rep-topbar-count", style: tasksOverdueCount > 0 ? { background: "var(--rod-red)" } : void 0 }, tasksDueCount)), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" }, ref: notifRef }, /* @__PURE__ */ React.createElement("button", { className: "rep-topbar-btn", onClick: openNotifDropdown }, /* @__PURE__ */ React.createElement("span", { className: "glyph" }, "\u25C9"), " Notifications", unreadCount > 0 && /* @__PURE__ */ React.createElement("span", { className: "rep-topbar-dot" })), showNotifDropdown && /* @__PURE__ */ React.createElement("div", { className: "notification-dropdown", onClick: (e) => e.stopPropagation(), style: { top: "100%", right: 0, marginTop: 4 } }, /* @__PURE__ */ React.createElement("div", { className: "notification-dropdown-header" }, /* @__PURE__ */ React.createElement("span", null, "Notifications", unreadCount > 0 && /* @__PURE__ */ React.createElement("span", { className: "nin-header-unread" }, " \xB7 " + unreadCount + " unread")), unreadCount > 0 && /* @__PURE__ */ React.createElement("button", { className: "nin-text-btn", onClick: markAllRead }, "Mark all read")), /* @__PURE__ */ React.createElement("div", { className: "notification-dropdown-list" }, notifLoading ? /* @__PURE__ */ React.createElement("div", { className: "nin-empty" }, "Loading\u2026") : notifList.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "nin-empty" }, "You're all caught up.") : notifList.map((n) => {
      const cat = notifCategoryFor(n.type);
      return /* @__PURE__ */ React.createElement("div", { key: n.id, className: "notification-item" + (n.is_read ? "" : " unread"), onClick: () => handleNotifClick(n) }, /* @__PURE__ */ React.createElement("div", { className: "nin-glyph", style: { color: cat.tone } }, cat.glyph), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "notification-item-title" }, n.title), n.message && /* @__PURE__ */ React.createElement("div", { className: "notification-item-message" }, n.message)), /* @__PURE__ */ React.createElement("div", { className: "notification-item-time" }, timeAgo(n.created_at)));
    })), /* @__PURE__ */ React.createElement("div", { className: "notification-dropdown-footer", onClick: () => navigate("notifications") }, "View all ", "\u2192"))), /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-user" }, /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-avatar" }, (repInfo.first_name || "R").charAt(0), (repInfo.last_name || "").charAt(0)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-user-name" }, repInfo.first_name, " ", repInfo.last_name), /* @__PURE__ */ React.createElement("div", { className: "rep-topbar-user-role" }, "Sales rep"))))), /* @__PURE__ */ React.createElement("div", { className: "mobile-topbar" }, /* @__PURE__ */ React.createElement("button", { className: "hamburger-btn", onClick: () => setSidebarOpen((s) => !s) }, /* @__PURE__ */ React.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2" }, sidebarOpen ? /* @__PURE__ */ React.createElement("path", { d: "M6 18L18 6M6 6l12 12" }) : /* @__PURE__ */ React.createElement("path", { d: "M3 12h18M3 6h18M3 18h18" }))), /* @__PURE__ */ React.createElement("span", { className: "logo-text", "aria-label": "Roma Flooring Designs" }, /* @__PURE__ */ React.createElement("span", { className: "logo-wordmark" }, "ROMA ", /* @__PURE__ */ React.createElement("em", null, "FLOORING")), /* @__PURE__ */ React.createElement("span", { className: "logo-script" }, "Designs")), /* @__PURE__ */ React.createElement("div", { style: { width: 24 } })), /* @__PURE__ */ React.createElement("div", { className: "sidebar-backdrop" + (sidebarOpen ? " visible" : ""), onClick: () => setSidebarOpen(false) }), /* @__PURE__ */ React.createElement("div", { className: "rep-layout" }, /* @__PURE__ */ React.createElement("div", { className: "rep-sidebar" + (sidebarOpen ? " open" : "") }, /* @__PURE__ */ React.createElement("div", { className: "sidebar-nav" }, navSections.map((sect) => /* @__PURE__ */ React.createElement("div", { key: sect.label }, /* @__PURE__ */ React.createElement("div", { className: "sidebar-section-label" }, sect.label), /* @__PURE__ */ React.createElement("div", { className: "sidebar-section-items" }, sect.items.map((item) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: item.key,
        className: "sidebar-item" + (activeBase === item.key ? " active" : ""),
        onClick: () => navigate(item.key)
      },
      /* @__PURE__ */ React.createElement("span", { className: "sidebar-item-left" }, /* @__PURE__ */ React.createElement("span", { className: "sidebar-glyph" }, item.glyph), item.name)
    )))))), /* @__PURE__ */ React.createElement("div", { className: "terminal-status" }, /* @__PURE__ */ React.createElement("span", { className: "terminal-dot " + terminalStatus }), /* @__PURE__ */ React.createElement("span", { style: { flex: 1 } }, "Valor terminal \xB7 cloud")), /* @__PURE__ */ React.createElement("div", { className: "sidebar-logout" }, /* @__PURE__ */ React.createElement("div", { className: "sidebar-item", onClick: onLogout, style: { margin: 0, padding: "0.5rem 0" } }, /* @__PURE__ */ React.createElement("span", { className: "sidebar-item-left" }, /* @__PURE__ */ React.createElement("span", { className: "sidebar-glyph" }, "\u2192"), "Logout")))), /* @__PURE__ */ React.createElement("div", { className: "rep-content" }, view === "dashboard" && /* @__PURE__ */ React.createElement(RepDashboardView, { navigate }), view === "tasks" && /* @__PURE__ */ React.createElement(RepTasksListView, { navigate, onChanged: pollBadges }), view === "catalog" && /* @__PURE__ */ React.createElement(RepCatalogView, { navigate, showCost, setShowCost }), view === "vendors" && /* @__PURE__ */ React.createElement(RepVendorsListView, { navigate }), view === "vendor-detail" && /* @__PURE__ */ React.createElement(RepVendorDetailView, { navigate, editId }), view === "product-detail" && /* @__PURE__ */ React.createElement(RepProductDetailView, { navigate, editId, showCost, setShowCost }), view === "orders" && /* @__PURE__ */ React.createElement(RepOrdersListView, { navigate }), view === "order-detail" && /* @__PURE__ */ React.createElement(RepOrderDetailView, { navigate, editId, terminalRef, terminalStatus, connectReader, showCost }), view === "order-create" && /* @__PURE__ */ React.createElement(RepOrderWizard, { navigate, terminalRef, terminalStatus, connectReader, showCost }), view === "quotes" && /* @__PURE__ */ React.createElement(RepQuotesListView, { navigate }), view === "quote-create" && /* @__PURE__ */ React.createElement(RepQuoteFormView, { navigate, terminalRef, terminalStatus, connectReader, showCost }), view === "quote-edit" && /* @__PURE__ */ React.createElement(RepQuoteFormView, { navigate, editId, terminalRef, terminalStatus, connectReader, showCost }), view === "estimates" && /* @__PURE__ */ React.createElement(RepEstimatesListView, { navigate }), view === "estimate-create" && /* @__PURE__ */ React.createElement(RepEstimateFormView, { navigate, showCost }), view === "estimate-edit" && /* @__PURE__ */ React.createElement(RepEstimateFormView, { navigate, editId, showCost }), view === "customers" && /* @__PURE__ */ React.createElement(RepCustomersListView, { navigate }), view === "customer-detail" && /* @__PURE__ */ React.createElement(RepCustomerDetailView, { navigate, editId }), view === "customer-create" && /* @__PURE__ */ React.createElement(RepCustomerCreateView, { navigate }), view === "commissions" && /* @__PURE__ */ React.createElement(RepCommissionsView, { navigate }), view === "team-commissions" && repInfo.is_manager && /* @__PURE__ */ React.createElement(RepTeamCommissionsView, { navigate }), view === "trade-applications" && /* @__PURE__ */ React.createElement(RepTradeApplicationsView, { navigate }), view === "visits" && /* @__PURE__ */ React.createElement(RepVisitsListView, { navigate }), view === "visit-create" && /* @__PURE__ */ React.createElement(RepVisitBuilderView, { navigate }), view === "visit-detail" && /* @__PURE__ */ React.createElement(RepVisitDetailView, { navigate, editId }), view === "samples" && /* @__PURE__ */ React.createElement(RepSampleRequestsListView, { navigate }), view === "sample-create" && /* @__PURE__ */ React.createElement(RepSampleRequestCreateView, { navigate }), view === "sample-detail" && /* @__PURE__ */ React.createElement(RepSampleRequestDetailView, { navigate, editId, terminalRef, terminalStatus, connectReader }), view === "notifications" && /* @__PURE__ */ React.createElement(RepNotificationsListView, { navigate, onUnreadChange: pollBadges }), view === "cash-drawer" && /* @__PURE__ */ React.createElement(RepCashDrawerView, { navigate }), view === "purchase-orders" && /* @__PURE__ */ React.createElement(RepPurchaseOrdersListView, { navigate }), view === "purchase-order-detail" && /* @__PURE__ */ React.createElement(RepPurchaseOrderDetailView, { navigate, editId }), view === "search-results" && /* @__PURE__ */ React.createElement(RepSearchResultsView, { navigate, initialQuery: searchQuery, onOpenPalette: () => setShowSearch(true) }), view === "return-flow" && repInfo.is_manager && /* @__PURE__ */ React.createElement(RepReturnFlowView, { navigate, editId, showCost, terminalRef, terminalStatus, connectReader }), view === "change-order" && /* @__PURE__ */ React.createElement(RepChangeOrderView, { navigate, editId, showCost, terminalRef, terminalStatus, connectReader }), view === "release-flow" && /* @__PURE__ */ React.createElement(RepReleaseFlowView, { navigate, editId, initialMethod: releaseIntent }))), showSearch && /* @__PURE__ */ React.createElement(
      GlobalSearchPalette,
      {
        onClose: () => setShowSearch(false),
        navigate,
        onSeeAll: (q) => {
          navigate("search-results");
          setSearchQuery(q);
        }
      }
    ), /* @__PURE__ */ React.createElement(RepToast, null));
  }
  const GS_ICONS = { customers: "\u25C9", orders: "\u25E7", quotes: "\u2263", estimates: "\u2261", products: "\u25D0" };
  const GS_TYPE_LABELS = { customers: "Customers", orders: "Orders", quotes: "Quotes", estimates: "Estimates", products: "Products" };
  const GS_TYPE_ORDER = ["customers", "orders", "quotes", "estimates", "products"];
  function gsRowTitle(row) {
    switch (row._type) {
      case "customers":
        return row.title || "";
      case "orders":
        return (row.order_number || "") + " \xB7 " + (row.customer_name || "");
      case "quotes":
        return (row.quote_number || "") + " \xB7 " + (row.customer_name || "");
      case "estimates":
        return (row.estimate_number || "") + " \xB7 " + (row.customer_name || "");
      case "products":
        return formatLineItem(row).name || row.product_name || "";
      default:
        return "";
    }
  }
  function gsRowSub(row) {
    switch (row._type) {
      case "customers": {
        const p = [row.customer_type === "trade" ? "Trade" : "Retail"];
        if (row.city) p.push(row.city);
        if (row.email) p.push(row.email);
        return p.join(" \xB7 ");
      }
      case "orders": {
        const p = [row.status || ""];
        if (row.total) p.push("$" + parseFloat(row.total).toFixed(2));
        if (row.item_count) p.push(row.item_count + " items");
        if (row.created_at) p.push(new Date(row.created_at).toLocaleDateString());
        return p.join(" \xB7 ");
      }
      case "quotes": {
        const p = [row.status || ""];
        if (row.total) p.push("$" + parseFloat(row.total).toFixed(2));
        if (row.expires_at) p.push("expires " + new Date(row.expires_at).toLocaleDateString());
        return p.join(" \xB7 ");
      }
      case "estimates": {
        const p = [row.status || ""];
        if (row.project_name) p.push(row.project_name);
        if (row.total) p.push("$" + parseFloat(row.total).toFixed(2));
        return p.join(" \xB7 ");
      }
      case "products": {
        const p = [];
        if (row.internal_sku) p.push(row.internal_sku);
        if (row.brand_name) p.push(row.brand_name);
        if (row.sell_by) p.push(row.sell_by === "unit" ? "per unit" : "per sqft");
        return p.join(" \xB7 ");
      }
      default:
        return "";
    }
  }
  function gsNavigateToRow(row, navigate) {
    switch (row._type) {
      case "customers":
        navigate("customer-detail", row.id);
        break;
      case "orders":
        navigate("order-detail", row.id);
        break;
      case "quotes":
        navigate("quote-edit", row.id);
        break;
      case "estimates":
        navigate("estimate-edit", row.id);
        break;
      case "products":
        navigate("product-detail", row.sku_id || row.product_id);
        break;
    }
  }
  function gsFlattenResults(results, filterType) {
    const rows = [];
    for (const key of GS_TYPE_ORDER) {
      const items = results[key];
      if (!items || items.length === 0) continue;
      if (filterType !== "all" && filterType !== key) continue;
      for (const item of items) rows.push({ ...item, _type: key });
    }
    return rows;
  }
  function gsGroupRows(flatRows) {
    const groups = [];
    let currentType = null;
    for (let i = 0; i < flatRows.length; i++) {
      const row = flatRows[i];
      if (row._type !== currentType) {
        currentType = row._type;
        groups.push({ type: "label", key: currentType, count: flatRows.filter((r) => r._type === currentType).length });
      }
      groups.push({ type: "row", row, flatIndex: i });
    }
    return groups;
  }
  function RepSearchResultsView({ navigate, initialQuery, onOpenPalette }) {
    const [query, setQuery] = useState(initialQuery || "");
    const [results, setResults] = useState({});
    const [loading, setLoading] = useState(false);
    const [typeFilter, setTypeFilter] = useState("all");
    const searchTimer = useRef(null);
    const inputRef = useRef(null);
    const doSearch = useCallback(async (q) => {
      if (!q || q.trim().length < 2) {
        setResults({});
        return;
      }
      setLoading(true);
      try {
        const data = await repFetch("/api/rep/global-search?q=" + encodeURIComponent(q.trim()) + "&limit=20");
        setResults(data.results || {});
      } catch (e) {
        console.error("Search results error:", e);
        setResults({});
      } finally {
        setLoading(false);
      }
    }, []);
    useEffect(() => {
      if (initialQuery && initialQuery.trim().length >= 2) doSearch(initialQuery);
    }, [initialQuery]);
    useEffect(() => {
      if (searchTimer.current) clearTimeout(searchTimer.current);
      if (!query || query.trim().length < 2) {
        setResults({});
        return;
      }
      searchTimer.current = setTimeout(() => doSearch(query), 300);
      return () => {
        if (searchTimer.current) clearTimeout(searchTimer.current);
      };
    }, [query, doSearch]);
    const flatRows = useMemo(() => gsFlattenResults(results, typeFilter), [results, typeFilter]);
    const typeCounts = useMemo(() => {
      const counts = {};
      let total = 0;
      for (const key of GS_TYPE_ORDER) {
        const items = results[key];
        const n = items ? items.length : 0;
        counts[key] = n;
        total += n;
      }
      counts.all = total;
      return counts;
    }, [results]);
    const handleKeyDown = (e) => {
      if ((e.metaKey || e.ctrlKey) && e.key === "k") {
        e.preventDefault();
        onOpenPalette();
      }
    };
    return /* @__PURE__ */ React.createElement("div", { onKeyDown: handleKeyDown }, /* @__PURE__ */ React.createElement("div", { className: "sr-search-input-row" }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 20px/1 "Cormorant Garamond", serif', color: "var(--gold, #c9a668)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        ref: inputRef,
        value: query,
        onChange: (e) => setQuery(e.target.value),
        placeholder: "Search customers, orders, SKUs, quotes\u2026",
        autoFocus: true
      }
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)", cursor: "pointer" },
        onClick: onOpenPalette
      },
      "\u2318",
      "K palette"
    )), query.trim().length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "sr-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "sr-header-label" }, "Search results"), /* @__PURE__ */ React.createElement("div", { className: "sr-header-title" }, /* @__PURE__ */ React.createElement("em", null, '"', query, '"'), " ", "\xB7", " ", typeCounts.all, " result", typeCounts.all !== 1 ? "s" : ""))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 22px" } }, "Searching", "\u2026") : query.trim().length >= 2 && typeCounts.all === 0 ? /* @__PURE__ */ React.createElement("div", { className: "gs-empty", style: { padding: "60px 22px", background: "#ece5d8", border: "0.5px solid rgba(28,25,23,0.14)" } }, 'No results for "', query, '"') : query.trim().length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "sr-grid" }, /* @__PURE__ */ React.createElement("div", { className: "sr-facets" }, /* @__PURE__ */ React.createElement("h3", { className: "sr-facet-heading" }, "Filter"), /* @__PURE__ */ React.createElement("div", { className: "sr-facet-group" }, /* @__PURE__ */ React.createElement("div", { className: "sr-facet-group-label" }, "Type"), /* @__PURE__ */ React.createElement(
      "div",
      {
        className: "sr-facet-item" + (typeFilter === "all" ? " active" : ""),
        onClick: () => setTypeFilter("all")
      },
      /* @__PURE__ */ React.createElement("span", null, typeFilter === "all" ? "\u25CF " : "\u25CB ", "All"),
      /* @__PURE__ */ React.createElement("span", { className: "sr-facet-count" }, typeCounts.all)
    ), GS_TYPE_ORDER.map((key) => {
      if (!typeCounts[key]) return null;
      const isActive = typeFilter === key;
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key,
          className: "sr-facet-item" + (isActive ? " active" : ""),
          onClick: () => setTypeFilter(isActive ? "all" : key)
        },
        /* @__PURE__ */ React.createElement("span", null, isActive ? "\u25CF " : "\u25CB ", GS_TYPE_LABELS[key]),
        /* @__PURE__ */ React.createElement("span", { className: "sr-facet-count" }, typeCounts[key])
      );
    }))), /* @__PURE__ */ React.createElement("div", { className: "sr-results" }, flatRows.map((row, i) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: row._type + "-" + (row.id || row.sku_id || i),
        className: "sr-result-row",
        onClick: () => gsNavigateToRow(row, navigate)
      },
      /* @__PURE__ */ React.createElement("span", { className: "sr-result-icon" }, GS_ICONS[row._type] || "\u25CB"),
      /* @__PURE__ */ React.createElement("span", { className: "sr-result-badge" }, GS_TYPE_LABELS[row._type] || row._type),
      /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "sr-result-title" }, gsRowTitle(row)), /* @__PURE__ */ React.createElement("div", { className: "sr-result-sub" }, gsRowSub(row)))
    )), flatRows.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "sr-pagination" }, /* @__PURE__ */ React.createElement("span", { className: "sr-pagination-count" }, "Showing ", flatRows.length, " of ", typeCounts.all)))));
  }
  function GlobalSearchPalette({ onClose, navigate, onSeeAll }) {
    const [query, setQuery] = useState("");
    const [results, setResults] = useState({});
    const [loading, setLoading] = useState(false);
    const [filter, setFilter] = useState("all");
    const [selectedIdx, setSelectedIdx] = useState(0);
    const inputRef = useRef(null);
    const searchTimer = useRef(null);
    const startTime = useRef(null);
    const [elapsed, setElapsed] = useState(0);
    const FILTERS = ["All", "Customers", "Orders", "Quotes", "Estimates", "Products"];
    useEffect(() => {
      if (inputRef.current) inputRef.current.focus();
    }, []);
    useEffect(() => {
      if (searchTimer.current) clearTimeout(searchTimer.current);
      if (!query || query.trim().length < 2) {
        setResults({});
        setSelectedIdx(0);
        return;
      }
      setLoading(true);
      startTime.current = performance.now();
      searchTimer.current = setTimeout(async () => {
        try {
          const typeParam = filter !== "all" ? "&type=" + filter : "";
          const data = await repFetch("/api/rep/global-search?q=" + encodeURIComponent(query.trim()) + typeParam);
          setResults(data.results || {});
          setElapsed(Math.round(performance.now() - startTime.current));
        } catch (e) {
          console.error("Global search error:", e);
          setResults({});
        } finally {
          setLoading(false);
          setSelectedIdx(0);
        }
      }, 200);
      return () => {
        if (searchTimer.current) clearTimeout(searchTimer.current);
      };
    }, [query, filter]);
    const flatRows = useMemo(() => gsFlattenResults(results, filter), [results, filter]);
    const groupedDisplay = useMemo(() => gsGroupRows(flatRows), [flatRows]);
    const totalCount = flatRows.length;
    const handleKeyDown = (e) => {
      if (e.key === "Escape") {
        onClose();
        return;
      }
      if (e.key === "ArrowDown") {
        e.preventDefault();
        setSelectedIdx((i) => Math.min(i + 1, flatRows.length - 1));
        return;
      }
      if (e.key === "ArrowUp") {
        e.preventDefault();
        setSelectedIdx((i) => Math.max(i - 1, 0));
        return;
      }
      if (e.key === "Enter" && flatRows.length > 0) {
        e.preventDefault();
        gsNavigateToRow(flatRows[selectedIdx], navigate);
        onClose();
        return;
      }
    };
    const handleSeeAll = () => {
      if (query.trim().length >= 2) {
        onSeeAll(query.trim());
        onClose();
      }
    };
    return ReactDOM.createPortal(
      /* @__PURE__ */ React.createElement("div", { className: "gs-backdrop", onClick: (e) => {
        if (e.target === e.currentTarget) onClose();
      }, onKeyDown: handleKeyDown }, /* @__PURE__ */ React.createElement("div", { className: "gs-palette" }, /* @__PURE__ */ React.createElement("div", { className: "gs-input-row" }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 18px/1 "Cormorant Garamond", serif', color: "var(--gold, #c9a668)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
        "input",
        {
          ref: inputRef,
          value: query,
          onChange: (e) => setQuery(e.target.value),
          placeholder: "Search customers, orders, SKUs, quotes\u2026",
          autoFocus: true
        }
      ), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)" } }, "esc to close")), /* @__PURE__ */ React.createElement("div", { className: "gs-chips" }, FILTERS.map((f) => {
        const key = f.toLowerCase();
        return /* @__PURE__ */ React.createElement("button", { key: f, className: "gs-chip" + (filter === key ? " active" : ""), onClick: () => setFilter(key) }, f);
      })), /* @__PURE__ */ React.createElement("div", { className: "gs-results" }, loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading" }, "Searching", "\u2026") : query.trim().length >= 2 && totalCount === 0 ? /* @__PURE__ */ React.createElement("div", { className: "gs-empty" }, 'No results for "', query, '"') : groupedDisplay.map((item, idx) => {
        if (item.type === "label") {
          return /* @__PURE__ */ React.createElement("div", { key: "label-" + item.key, className: "gs-group-label" }, (GS_TYPE_LABELS[item.key] || item.key) + " \xB7 " + item.count);
        }
        const row = item.row;
        const isSel = item.flatIndex === selectedIdx;
        return /* @__PURE__ */ React.createElement(
          "div",
          {
            key: row._type + "-" + (row.id || row.sku_id || idx),
            className: "gs-row" + (isSel ? " selected" : ""),
            onClick: () => {
              gsNavigateToRow(row, navigate);
              onClose();
            },
            onMouseEnter: () => setSelectedIdx(item.flatIndex)
          },
          /* @__PURE__ */ React.createElement("span", { className: "gs-icon" }, GS_ICONS[row._type] || "\u25CB"),
          /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "gs-title" }, gsRowTitle(row)), /* @__PURE__ */ React.createElement("div", { className: "gs-sub" }, gsRowSub(row))),
          isSel && /* @__PURE__ */ React.createElement("span", { className: "gs-kbd" }, "\u21B5")
        );
      })), query.trim().length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "gs-footer" }, /* @__PURE__ */ React.createElement("div", { className: "gs-footer-hints" }, /* @__PURE__ */ React.createElement("span", null, "\u2191\u2193", " navigate"), /* @__PURE__ */ React.createElement("span", null, "\u21B5", " open"), /* @__PURE__ */ React.createElement("span", null, "esc close")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 16 } }, totalCount > 0 && /* @__PURE__ */ React.createElement("span", { className: "gs-footer-see-all", onClick: handleSeeAll }, "See all results ", "\u2192"), /* @__PURE__ */ React.createElement("span", { className: "gs-footer-count" }, totalCount, " result", totalCount !== 1 ? "s" : "", " ", "\xB7", " ", elapsed, "ms"))))),
      document.body
    );
  }
  function ImageLightbox({ images, startIndex, onClose }) {
    const [index, setIndex] = useState(startIndex || 0);
    useEffect(() => {
      const onKey = (e) => {
        if (e.key === "Escape") onClose();
        if (e.key === "ArrowRight") setIndex((i) => Math.min(i + 1, images.length - 1));
        if (e.key === "ArrowLeft") setIndex((i) => Math.max(i - 1, 0));
      };
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [images.length, onClose]);
    if (!images || images.length === 0) return null;
    return ReactDOM.createPortal(
      /* @__PURE__ */ React.createElement("div", { className: "lightbox-overlay", onClick: onClose }, /* @__PURE__ */ React.createElement("img", { src: images[index], alt: "", className: "lightbox-img", onClick: (e) => e.stopPropagation() }), /* @__PURE__ */ React.createElement("button", { className: "lightbox-close", onClick: onClose }, "\xD7"), index > 0 && /* @__PURE__ */ React.createElement("button", { className: "lightbox-arrow prev", onClick: (e) => {
        e.stopPropagation();
        setIndex((i) => i - 1);
      } }, "\u2039"), index < images.length - 1 && /* @__PURE__ */ React.createElement("button", { className: "lightbox-arrow next", onClick: (e) => {
        e.stopPropagation();
        setIndex((i) => i + 1);
      } }, "\u203A"), /* @__PURE__ */ React.createElement("div", { className: "lightbox-counter" }, index + 1, " / ", images.length)),
      document.body
    );
  }
  const REP_NON_SELECTABLE = /* @__PURE__ */ new Set(["pei_rating", "shade_variation", "water_absorption", "dcof", "material", "material_class", "country", "application", "edge", "look", "color", "color_code", "style_code", "price_list", "companion_skus", "species", "subcategory", "upc", "msrp", "weight", "top_ref_sku", "sink_ref_sku", "optional_accessories", "group_number", "width", "size", "height", "depth", "hardware_finish", "num_drawers", "num_doors", "num_shelves", "num_sinks", "soft_close", "sink_material", "sink_type", "vanity_type", "bowl_shape", "style", "origin", "countertop_material", "construction", "sub_line", "collection", "brand", "surface_texture", "wear_layer", "ac_rating", "edge_treatment", "plank_width", "plank_length", "composition", "install_method", "features", "technology", "product_line", "color_family", "breaking_strength", "mohs_hardness", "color_generic", "pattern", "projection", "clearance", "overall_length", "diameter", "center_to_center"]);
  function repBuildVariantSelectors(current, siblings) {
    const all = [current, ...siblings];
    const attrsOf = (s) => (s.attributes || []).reduce((m, a) => {
      m[a.slug] = a.value;
      return m;
    }, {});
    const curAttrs = attrsOf(current);
    if (!curAttrs.color) return null;
    const normColor = (v) => (v || "").replace(/\s*\d+\.?\d*\s*[xX]+\s*\d+\.?\d*\s*/g, " ").replace(/\s*-?\s*\d+m[mi]l?\b/gi, "").replace(/^\s*-\s*/, "").replace(/\s+/g, " ").trim();
    const normSize = (v) => (v || "").toLowerCase().replace(/["″\s]/g, "");
    const numSort = (x, y) => {
      const nx = parseFloat(x), ny = parseFloat(y);
      if (!isNaN(nx) && !isNaN(ny) && nx !== ny) return nx - ny;
      return String(x).localeCompare(String(y));
    };
    const byColor = /* @__PURE__ */ new Map();
    all.forEach((s) => {
      const a = attrsOf(s);
      const c = normColor(a.color);
      if (!c) return;
      const isCur = s.sku_id === current.sku_id;
      const sizeMatch = !!(curAttrs.size && a.size && normSize(a.size) === normSize(curAttrs.size));
      const ex = byColor.get(c);
      if (!ex || isCur || sizeMatch && !ex._sm && !ex.is_current) {
        byColor.set(c, { sku_id: s.sku_id, label: c, primary_image: s.primary_image || null, stock_status: s.stock_status, qty_on_hand: s.qty_on_hand, sell_by: s.sell_by, is_current: isCur, _sm: sizeMatch });
      }
    });
    const colorItems = [...byColor.values()].sort((a, b) => a.label.localeCompare(b.label));
    const bySize = /* @__PURE__ */ new Map();
    all.forEach((s) => {
      const a = attrsOf(s);
      if (!a.size) return;
      const k = normSize(a.size);
      const isCur = s.sku_id === current.sku_id;
      const colorMatch = !!(curAttrs.color && a.color && normColor(a.color) === normColor(curAttrs.color));
      const ex = bySize.get(k);
      if (!ex || isCur || colorMatch && !ex._cm && !ex._cur) {
        bySize.set(k, { sku_id: s.sku_id, label: a.size, is_current: k === normSize(curAttrs.size), _cm: colorMatch, _cur: isCur });
      }
    });
    const sizeItems = [...bySize.values()].sort((a, b) => numSort(a.label, b.label));
    const localCounts = {}, colorGroups = {}, colorSizeGroups = {}, nameOf = {};
    all.forEach((s) => {
      const a = attrsOf(s);
      const cKey = normColor(a.color) || s.variant_name || "";
      const csKey = cKey + "||" + normSize(a.size || "");
      (s.attributes || []).forEach((at) => {
        nameOf[at.slug] = at.name || at.slug;
        (localCounts[at.slug] = localCounts[at.slug] || /* @__PURE__ */ new Set()).add(at.value);
        const cg = colorGroups[at.slug] = colorGroups[at.slug] || {};
        (cg[cKey] = cg[cKey] || /* @__PURE__ */ new Set()).add(at.value);
        const csg = colorSizeGroups[at.slug] = colorSizeGroups[at.slug] || {};
        (csg[csKey] = csg[csKey] || /* @__PURE__ */ new Set()).add(at.value);
      });
    });
    const sizeSelectorShown = sizeItems.length > 1;
    const RECT_SHAPES = /* @__PURE__ */ new Set(["rectangle", "square", "plank"]);
    const shapeMirror = localCounts.shape && [...localCounts.shape].every((v) => RECT_SHAPES.has(v.toLowerCase().trim()));
    const hasJunk = (slug) => [...localCounts[slug]].some((v) => v.includes(";"));
    const independent = (slug) => {
      const g = slug === "shape" || !sizeSelectorShown ? colorGroups[slug] : colorSizeGroups[slug];
      return !!g && Object.values(g).some((vals) => vals.size > 1);
    };
    let slugs = Object.keys(localCounts).filter(
      (slug) => localCounts[slug].size > 1 && !REP_NON_SELECTABLE.has(slug) && !(slug === "shape" && shapeMirror) && !hasJunk(slug) && independent(slug)
    ).sort((a, b) => a === "finish" ? -1 : b === "finish" ? 1 : 0);
    if (slugs.length > 1) {
      const rank = (slug) => slug === "finish" ? 0 : slug === "shape" ? 1 : 2;
      const locked = (a, b) => {
        const fwd = /* @__PURE__ */ new Map(), rev = /* @__PURE__ */ new Map();
        for (const s of all) {
          const at = attrsOf(s);
          if (at[a] === void 0 || at[b] === void 0) return false;
          if (fwd.has(at[a]) && fwd.get(at[a]) !== at[b]) return false;
          if (rev.has(at[b]) && rev.get(at[b]) !== at[a]) return false;
          fwd.set(at[a], at[b]);
          rev.set(at[b], at[a]);
        }
        return fwd.size > 0;
      };
      const drop = /* @__PURE__ */ new Set();
      for (let i = 0; i < slugs.length; i++) for (let j = i + 1; j < slugs.length; j++) {
        const a = slugs[i], b = slugs[j];
        if (drop.has(a) || drop.has(b)) continue;
        if (locked(a, b)) drop.add(rank(a) <= rank(b) ? b : a);
      }
      if (drop.size) slugs = slugs.filter((s) => !drop.has(s));
    }
    const attrPills = slugs.map((slug) => {
      const values = [...localCounts[slug]].sort(numSort);
      const items = values.map((val) => {
        let best = null, bestScore = -1;
        all.forEach((s) => {
          const a = attrsOf(s);
          if (a[slug] !== val) return;
          let score = 0;
          if (curAttrs.color && a.color && normColor(a.color) === normColor(curAttrs.color)) score += 10;
          if (curAttrs.size && a.size && normSize(a.size) === normSize(curAttrs.size)) score += 10;
          slugs.forEach((o) => {
            if (o !== slug && curAttrs[o] && a[o] === curAttrs[o]) score += 1;
          });
          if (score > bestScore) {
            bestScore = score;
            best = s;
          }
        });
        return { value: val, sku_id: best ? best.sku_id : null, is_current: curAttrs[slug] === val };
      });
      return { slug, name: nameOf[slug] || slug, items };
    });
    const hasShape = slugs.includes("shape");
    return { colorItems, sizeItems: hasShape ? [] : sizeItems, attrPills };
  }
  class QuickViewErrorBoundary extends React.Component {
    constructor(props) {
      super(props);
      this.state = { hasError: false };
    }
    static getDerivedStateFromError() {
      return { hasError: true };
    }
    componentDidCatch(err) {
      console.error("QuickView render error:", err);
    }
    componentDidUpdate(prevProps) {
      if (this.props.productId !== prevProps.productId) this.setState({ hasError: false });
    }
    render() {
      if (this.state.hasError) {
        document.body.style.overflow = "";
        setTimeout(() => this.props.onClose(), 0);
        return null;
      }
      return this.props.children;
    }
  }
  function RepQuickViewModal({ productId, initialSkuId, products, showCost, navigate, onClose, onAddTo }) {
    const TH = { ink: "#1c1917", paper: "#ece5d8", accent: "#a87935", muted: "#8a7e68", warm: "#d8cdb6" };
    const [productData, setProductData] = useState(null);
    const [skuData, setSkuData] = useState(null);
    const [loading, setLoading] = useState(true);
    const [skuLoading, setSkuLoading] = useState(false);
    const [mainImgIdx, setMainImgIdx] = useState(0);
    const [lightbox, setLightbox] = useState(null);
    const [specsOpen, setSpecsOpen] = useState(true);
    const [calcPrice, setCalcPrice] = useState(null);
    const [coverSqft, setCoverSqft] = useState(200);
    const loadProduct = useCallback((id) => {
      setLoading(true);
      setMainImgIdx(0);
      setSkuData(null);
      repFetch("/api/rep/products/" + id).then((d) => {
        setProductData(d);
        setLoading(false);
        const skus = d.skus || [];
        const firstSkuId = skus.length > 0 ? skus[0].id : null;
        const targetSkuId = initialSkuId && skus.some((s) => s.id === initialSkuId) ? initialSkuId : firstSkuId;
        if (targetSkuId) loadSku(targetSkuId);
      }).catch(() => setLoading(false));
    }, [initialSkuId]);
    const loadSku = useCallback((skuId) => {
      setSkuLoading(true);
      setMainImgIdx(0);
      repFetch("/api/rep/skus/" + skuId).then((d) => {
        setSkuData(d);
        setSkuLoading(false);
        setCalcPrice(null);
      }).catch(() => setSkuLoading(false));
    }, []);
    useEffect(() => {
      if (productId) loadProduct(productId);
    }, [productId, loadProduct]);
    useEffect(() => {
      const onKey = (e) => {
        if (e.key === "Escape") onClose();
      };
      document.addEventListener("keydown", onKey);
      document.body.style.overflow = "hidden";
      return () => {
        document.removeEventListener("keydown", onKey);
        document.body.style.overflow = "";
      };
    }, [onClose]);
    const activeSku = skuData ? skuData.sku : {};
    const product = skuData ? skuData.product : productData ? productData.product : null;
    const skuMedia = skuData ? (skuData.media || []).filter((m) => m.asset_type !== "spec_pdf") : [];
    const siblings = skuData ? skuData.siblings || [] : [];
    const accessories = skuData ? skuData.accessories || [] : [];
    const allSkus = productData ? productData.skus || [] : [];
    const allVariants = useMemo(() => {
      if (!activeSku.sku_id) return [];
      const current = {
        sku_id: activeSku.sku_id,
        variant_name: activeSku.variant_name,
        primary_image: skuMedia.length > 0 ? skuMedia[0].url : null,
        retail_price: activeSku.retail_price,
        cost: activeSku.cost,
        margin_pct: activeSku.margin_pct,
        stock_status: activeSku.stock_status,
        qty_on_hand: activeSku.qty_on_hand,
        sqft_per_box: activeSku.sqft_per_box,
        sell_by: activeSku.sell_by
      };
      return [current, ...siblings].sort(
        (a, b) => (a.variant_name || "").localeCompare(b.variant_name || "") || String(a.sku_id).localeCompare(String(b.sku_id))
      );
    }, [activeSku.sku_id, siblings, skuMedia]);
    const variantSelectors = useMemo(() => {
      if (!activeSku.sku_id) return null;
      const cur = {
        sku_id: activeSku.sku_id,
        variant_name: activeSku.variant_name,
        attributes: activeSku.attributes || [],
        primary_image: skuMedia.length > 0 ? skuMedia[0].url : null,
        stock_status: activeSku.stock_status,
        qty_on_hand: activeSku.qty_on_hand,
        sell_by: activeSku.sell_by
      };
      return repBuildVariantSelectors(cur, siblings);
    }, [activeSku.sku_id, siblings, skuMedia]);
    const showStructured = !!(variantSelectors && (variantSelectors.colorItems.length >= 2 || variantSelectors.sizeItems.length >= 2 || variantSelectors.attrPills.length > 0));
    const displayMedia = skuMedia;
    const mono = (c) => ({ font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: c });
    const sectionTitle = { font: "500 10px/1 var(--roma-sans)", letterSpacing: "0.16em", textTransform: "uppercase", color: TH.ink, margin: "0 0 12px" };
    const fmtMoney = (n) => "$" + parseFloat(n).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const marginCol = (pct) => pct == null ? TH.muted : pct >= 30 ? "#3a7a4e" : pct >= 15 ? "#a87935" : "#c54731";
    const retail = activeSku.retail_price ? parseFloat(activeSku.retail_price) : 0;
    const cost = activeSku.cost ? parseFloat(activeSku.cost) : 0;
    const mPct = activeSku.margin_pct;
    const isCarpet = activeSku.sell_by === "roll" || activeSku.price_basis === "per_sqyd";
    const unitLabel = skuPriceSuffix(activeSku);
    const price = calcPrice !== null ? calcPrice : retail;
    const calcMargin = price > 0 ? (price - cost) / price * 100 : 0;
    const sqftPerBox = activeSku.sqft_per_box ? parseFloat(activeSku.sqft_per_box) : null;
    const boxes = sqftPerBox ? Math.ceil(coverSqft * 1.1 / sqftPerBox) : 0;
    const carpetSqyd = isCarpet ? coverSqft * 1.1 / 9 : 0;
    const coverTotal = isCarpet ? carpetSqyd * price : sqftPerBox ? boxes * sqftPerBox * price : 0;
    const step = retail >= 100 ? 25 : 0.25;
    const calcMin = +(cost * 1.05).toFixed(2) || 0.01;
    const calcMax = +(retail * 1.4).toFixed(2) || 100;
    const clampPrice = (v) => Math.max(calcMin, Math.min(calcMax, +v.toFixed(2)));
    const tiers = retail > 0 ? [
      { label: "Retail", mult: 1 },
      { label: "Silver", mult: 0.875 },
      { label: "Gold", mult: 0.8125 },
      { label: "Platinum", mult: 0.78125 }
    ].map((t) => ({ ...t, price: retail * t.mult, margin: cost > 0 ? (retail * t.mult - cost) / (retail * t.mult) * 100 : 0 })) : [];
    return ReactDOM.createPortal(
      /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { onClick: onClose, style: {
        position: "fixed",
        inset: 0,
        background: "rgba(28,25,23,0.4)",
        backdropFilter: "blur(1.5px)",
        zIndex: 1e3,
        animation: "rcatFadeIn .2s ease"
      } }), /* @__PURE__ */ React.createElement("aside", { style: {
        position: "fixed",
        top: 0,
        right: 0,
        bottom: 0,
        width: 552,
        maxWidth: "100vw",
        background: TH.paper,
        zIndex: 1001,
        boxShadow: "-24px 0 70px rgba(28,25,23,0.25)",
        display: "flex",
        flexDirection: "column",
        animation: "rcatSlideIn .28s cubic-bezier(.22,.61,.36,1)"
      } }, /* @__PURE__ */ React.createElement("div", { style: {
        display: "flex",
        justifyContent: "space-between",
        alignItems: "flex-start",
        padding: "16px 20px",
        borderBottom: "0.5px solid rgba(28,25,23,0.1)",
        flexShrink: 0
      } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: mono(TH.accent) }, "Quick view \xB7 spec + economics"), activeSku.sku_id && /* @__PURE__ */ React.createElement("div", { style: { ...mono(TH.muted), marginTop: 7 } }, "SKU ", activeSku.sku_id)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, flexShrink: 0 } }, productId && /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => repOpenDoc("/api/rep/labels?format=image&productIds=" + productId, true).catch((err) => alert(err.message || "Failed to generate labels")),
          title: "Print showroom sample labels (one per color) \u2014 4\xD72 for the label printer",
          style: {
            height: 30,
            padding: "0 12px",
            border: "0.5px solid rgba(28,25,23,0.2)",
            background: "transparent",
            color: TH.ink,
            cursor: "pointer",
            font: "500 12px/1 var(--roma-sans)"
          }
        },
        "Labels"
      ), /* @__PURE__ */ React.createElement("button", { onClick: onClose, style: {
        width: 30,
        height: 30,
        border: "0.5px solid rgba(28,25,23,0.2)",
        background: "transparent",
        color: TH.ink,
        cursor: "pointer",
        font: "400 15px/1 var(--roma-sans)"
      } }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, overflowY: "auto" } }, loading ? /* @__PURE__ */ React.createElement("div", { style: { padding: 40, textAlign: "center", color: TH.muted } }, "Loading...") : !product ? /* @__PURE__ */ React.createElement("div", { style: { padding: 40, textAlign: "center", color: TH.muted } }, "Product not found") : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { style: { position: "relative", height: 224, background: "var(--stone-100)", overflow: "hidden" } }, displayMedia.length > 0 ? /* @__PURE__ */ React.createElement(
        "img",
        {
          src: optimizeImg(displayMedia[mainImgIdx] ? displayMedia[mainImgIdx].url : displayMedia[0].url, 800),
          alt: "",
          style: { width: "100%", height: "100%", objectFit: "cover", opacity: skuLoading ? 0.5 : 1, transition: "opacity 0.15s", cursor: "pointer" },
          onClick: () => setLightbox({ images: displayMedia.map((m) => m.url), startIndex: mainImgIdx })
        }
      ) : /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "center", height: "100%", color: TH.muted } }, "No Image"), activeSku.stock_status && (() => {
        const qvSk = stockDisplay(activeSku);
        return /* @__PURE__ */ React.createElement("div", { style: {
          position: "absolute",
          top: 12,
          left: 12,
          padding: "5px 9px",
          background: TH.paper,
          color: qvSk.c,
          ...mono(qvSk.c)
        } }, "\u25CF", " ", qvSk.l, qvSk.q ? " \xB7 " + qvSk.q : "");
      })(), showCost && mPct > 0 && /* @__PURE__ */ React.createElement("div", { style: {
        position: "absolute",
        top: 12,
        right: 12,
        padding: "5px 9px",
        background: marginCol(mPct),
        color: "#fff",
        ...mono("#fff")
      } }, mPct.toFixed(0), "% margin")), displayMedia.length > 1 && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, padding: "8px 20px", overflowX: "auto", borderBottom: "0.5px solid rgba(28,25,23,0.1)" } }, displayMedia.map((m, i) => /* @__PURE__ */ React.createElement(
        "img",
        {
          key: m.id || i,
          src: optimizeImg(m.url, 80),
          alt: "",
          onClick: () => setMainImgIdx(i),
          style: { width: 44, height: 44, objectFit: "cover", border: i === mainImgIdx ? "2px solid " + TH.ink : "2px solid transparent", cursor: "pointer", opacity: i === mainImgIdx ? 1 : 0.6, flexShrink: 0 }
        }
      ))), /* @__PURE__ */ React.createElement("div", { style: { padding: "20px", display: "grid", gap: 22 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, [product.category_name, product.brand_name || product.vendor_name, product.collection].filter(Boolean).join(" \xB7 ")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 26px/1.05 var(--roma-serif)", letterSpacing: "-0.012em", color: TH.ink, marginTop: 8 } }, activeSku.display_name || product.name), !activeSku.display_name && /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 var(--roma-sans)", color: TH.ink + "99", marginTop: 4 } }, [activeSku.variant_name, activeSku.size_display].filter(Boolean).join(" \xB7 ")), product.description_short && /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.5 var(--roma-sans)", color: TH.muted, marginTop: 6 } }, product.description_short)), showStructured && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 18 } }, variantSelectors.colorItems.length >= 2 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", marginBottom: 10 } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, "Color"), /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, variantSelectors.colorItems.length, " colors")), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(74px, 1fr))", gap: 10 } }, variantSelectors.colorItems.map((v) => {
        const isActive = v.is_current;
        const st = v.stock_status ? stockDisplay({ ...v, vendor_has_inventory: product.vendor_has_inventory }) : null;
        return /* @__PURE__ */ React.createElement(
          "button",
          {
            key: v.sku_id,
            onClick: () => {
              if (!isActive) loadSku(v.sku_id);
            },
            title: [v.label, st && st.l].filter(Boolean).join(" \xB7 "),
            style: { display: "flex", flexDirection: "column", gap: 6, padding: 0, cursor: isActive ? "default" : "pointer", background: "transparent", border: "none", textAlign: "left" }
          },
          /* @__PURE__ */ React.createElement("div", { style: {
            position: "relative",
            width: "100%",
            aspectRatio: "1 / 1",
            overflow: "hidden",
            border: isActive ? "2px solid " + TH.ink : "1px solid rgba(28,25,23,0.15)",
            outline: isActive ? "2px solid " + TH.ink : "none",
            outlineOffset: 2,
            background: "var(--stone-100)"
          } }, v.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(v.primary_image, 120), alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", justifyContent: "center", width: "100%", height: "100%", font: "400 10px/1.2 var(--roma-sans)", color: TH.muted, textAlign: "center", padding: 4 } }, v.label), st && /* @__PURE__ */ React.createElement("span", { title: st.l, style: { position: "absolute", top: 5, right: 5, width: 9, height: 9, borderRadius: "50%", background: st.c, boxShadow: "0 0 0 2px " + TH.paper } })),
          /* @__PURE__ */ React.createElement("span", { style: { font: (isActive ? "600" : "400") + " 11px/1.25 var(--roma-sans)", color: isActive ? TH.ink : TH.muted, display: "-webkit-box", WebkitLineClamp: 2, WebkitBoxOrient: "vertical", overflow: "hidden" } }, v.label)
        );
      }))), [
        ...variantSelectors.sizeItems.length >= 2 ? [{ slug: "_size", name: "Size", items: variantSelectors.sizeItems.map((s) => ({ value: s.label, sku_id: s.sku_id, is_current: s.is_current })) }] : [],
        ...variantSelectors.attrPills
      ].map((group) => /* @__PURE__ */ React.createElement("div", { key: group.slug }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", marginBottom: 10 } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, group.name), /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, (group.items.find((i) => i.is_current) || {}).value || "")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 8 } }, group.items.map((it) => /* @__PURE__ */ React.createElement(
        "button",
        {
          key: it.value,
          onClick: () => {
            if (!it.is_current && it.sku_id) loadSku(it.sku_id);
          },
          style: {
            padding: "7px 13px",
            font: "500 12px/1 var(--roma-sans)",
            border: it.is_current ? "1.5px solid " + TH.ink : "0.5px solid rgba(28,25,23,0.25)",
            background: it.is_current ? TH.ink : "transparent",
            color: it.is_current ? TH.paper : TH.ink,
            cursor: it.is_current ? "default" : "pointer"
          }
        },
        it.value
      )))))), !showStructured && allVariants.length > 1 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", marginBottom: 10 } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, "Colors / Variants"), /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, allVariants.length, " options")), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(74px, 1fr))", gap: 10 } }, allVariants.map((v) => {
        const isActive = v.sku_id === activeSku.sku_id;
        const st = v.stock_status ? stockDisplay({ ...v, vendor_has_inventory: product.vendor_has_inventory }) : null;
        return /* @__PURE__ */ React.createElement(
          "button",
          {
            key: v.sku_id,
            onClick: () => {
              if (!isActive) loadSku(v.sku_id);
            },
            title: [v.variant_name, st && st.l].filter(Boolean).join(" \xB7 "),
            style: { display: "flex", flexDirection: "column", gap: 6, padding: 0, cursor: isActive ? "default" : "pointer", background: "transparent", border: "none", textAlign: "left" }
          },
          /* @__PURE__ */ React.createElement("div", { style: {
            position: "relative",
            width: "100%",
            aspectRatio: "1 / 1",
            overflow: "hidden",
            border: isActive ? "2px solid " + TH.ink : "1px solid rgba(28,25,23,0.15)",
            outline: isActive ? "2px solid " + TH.ink : "none",
            outlineOffset: 2,
            background: "var(--stone-100)"
          } }, v.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(v.primary_image, 120), alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", justifyContent: "center", width: "100%", height: "100%", font: "400 10px/1.2 var(--roma-sans)", color: TH.muted, textAlign: "center", padding: 4 } }, v.variant_name || "\u2014"), st && /* @__PURE__ */ React.createElement("span", { title: st.l, style: { position: "absolute", top: 5, right: 5, width: 9, height: 9, borderRadius: "50%", background: st.c, boxShadow: "0 0 0 2px " + TH.paper } })),
          /* @__PURE__ */ React.createElement("span", { style: { font: (isActive ? "600" : "400") + " 11px/1.25 var(--roma-sans)", color: isActive ? TH.ink : TH.muted, display: "-webkit-box", WebkitLineClamp: 2, WebkitBoxOrient: "vertical", overflow: "hidden" } }, v.variant_name || "\u2014")
        );
      }))), retail > 0 && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(3, 1fr)", border: "0.5px solid rgba(28,25,23,0.13)" } }, [
        ["Retail", fmtMoney(retail) + unitLabel, TH.ink],
        showCost ? ["Your cost", fmtMoney(cost) + unitLabel, "#c54731"] : ["Unit", unitLabel.replace("/", ""), TH.muted],
        showCost && mPct > 0 ? ["Margin", mPct.toFixed(1) + "%", marginCol(mPct)] : ["SKUs", allSkus.length.toString(), TH.ink]
      ].map(([l, v, c], i) => /* @__PURE__ */ React.createElement("div", { key: l, style: { padding: 14, borderLeft: i ? "0.5px solid rgba(28,25,23,0.1)" : "none" } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, l), /* @__PURE__ */ React.createElement("div", { style: { font: "400 22px/1.1 var(--roma-serif)", color: c, marginTop: 6, letterSpacing: "-0.01em" } }, v)))), (sqftPerBox || isCarpet) && activeSku.sell_by !== "unit" && /* @__PURE__ */ React.createElement("div", { style: { background: TH.warm, padding: 16, border: "0.5px solid rgba(28,25,23,0.06)" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 12 } }, /* @__PURE__ */ React.createElement("h4", { style: sectionTitle }, "Coverage calculator"), /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, "at ", fmtMoney(price), unitLabel)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 10, marginBottom: 14 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1 var(--roma-sans)", color: TH.muted } }, "Area"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          value: coverSqft,
          onChange: (e) => setCoverSqft(Math.max(0, +e.target.value)),
          style: { width: 80, padding: "8px 10px", border: "0.5px solid rgba(28,25,23,0.2)", background: TH.paper, color: TH.ink, font: "400 14px/1 var(--roma-sans)", outline: "none" }
        }
      ), /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1 var(--roma-sans)", color: TH.muted } }, "sf \xB7 incl. 10% waste")), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 1, background: "rgba(28,25,23,0.1)", border: "0.5px solid rgba(28,25,23,0.1)" } }, (isCarpet ? [["Sq yds", carpetSqyd.toFixed(1)], ["Covers", (coverSqft * 1.1).toFixed(0) + " sf"], ["Est. total", fmtMoney(coverTotal)]] : [["Boxes", boxes], ["Covers", (boxes * sqftPerBox).toFixed(0) + " sf"], ["Est. total", fmtMoney(coverTotal)]]).map(([l, v]) => /* @__PURE__ */ React.createElement("div", { key: l, style: { background: TH.paper, padding: "11px 12px" } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, l), /* @__PURE__ */ React.createElement("div", { style: { font: "400 19px/1.1 var(--roma-serif)", color: TH.ink, marginTop: 5 } }, v)))), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.4 var(--roma-sans)", color: TH.muted, marginTop: 9 } }, isCarpet ? "9 sf per sq yd \xB7 cut from roll \xB7 " + fmtMoney(price / 9) + "/sf equivalent" : sqftPerBox + " sf / box \xB7 sold by full box")), showCost && tiers.length > 0 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h4", { style: sectionTitle }, "Customer pricing tiers"), /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.13)" } }, tiers.map((t, i) => /* @__PURE__ */ React.createElement("div", { key: t.label, style: {
        display: "grid",
        gridTemplateColumns: "1.2fr 1fr 1fr",
        alignItems: "center",
        padding: "11px 14px",
        borderTop: i ? "0.5px solid rgba(28,25,23,0.08)" : "none",
        background: t.label === "Trade" ? TH.accent + "1f" : "transparent"
      } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-sans)", color: TH.ink, display: "flex", alignItems: "center", gap: 7 } }, t.label === "Trade" && /* @__PURE__ */ React.createElement("span", { style: { width: 6, height: 6, borderRadius: "50%", background: TH.accent } }), t.label), /* @__PURE__ */ React.createElement("span", { style: { font: "400 14px/1 var(--roma-serif)", color: TH.ink, textAlign: "right" } }, fmtMoney(t.price), unitLabel), /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", color: marginCol(t.margin), textAlign: "right" } }, t.margin.toFixed(0), "% mgn"))))), (() => {
        const HIDE = ["color_code", "style_code", "upc", "price_list", "material_class", "collection", "brand", "subcategory"];
        const specs = (activeSku.attributes || []).filter((a) => a.value && String(a.value).trim() && !HIDE.includes(a.slug));
        if (!specs.length) return null;
        return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h4", { style: sectionTitle }, "Specifications"), /* @__PURE__ */ React.createElement("div", null, specs.map((a) => /* @__PURE__ */ React.createElement("div", { key: a.slug, style: { display: "flex", justifyContent: "space-between", gap: 16, padding: "10px 0", borderTop: "0.5px solid rgba(28,25,23,0.08)" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1.3 var(--roma-sans)", color: TH.muted } }, a.name), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1.3 var(--roma-sans)", color: TH.ink, textAlign: "right" } }, a.value)))));
      })(), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h4", { style: sectionTitle }, "Packaging & specifications"), /* @__PURE__ */ React.createElement("div", null, [
        ["Sqft / Box", sqftPerBox ? sqftPerBox.toFixed(1) : "\u2014"],
        ["Pieces / Box", activeSku.pieces_per_box || "\u2014"],
        ["Weight / Box", activeSku.weight_per_box_lbs ? parseFloat(activeSku.weight_per_box_lbs).toFixed(1) + " lbs" : "\u2014"],
        ["Boxes / Pallet", activeSku.boxes_per_pallet || "\u2014"],
        ["Sell by", activeSku.sell_by === "unit" ? "Per unit" : activeSku.sell_by === "box" ? "Per sqft (full box)" : activeSku.sell_by === "roll" ? "Per sq yd (cut from roll)" : "\u2014"]
      ].map(([k, v]) => /* @__PURE__ */ React.createElement("div", { key: k, style: { display: "flex", justifyContent: "space-between", padding: "10px 0", borderTop: "0.5px solid rgba(28,25,23,0.08)" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1.3 var(--roma-sans)", color: TH.muted } }, k), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1.3 var(--roma-sans)", color: TH.ink, textAlign: "right" } }, v))))), accessories.length > 0 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 12 } }, /* @__PURE__ */ React.createElement("h4", { style: { ...sectionTitle, margin: 0 } }, "Accessories \u2014 complete the install"), /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, accessories.length, " suggested")), /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.13)" } }, accessories.map((a, i) => /* @__PURE__ */ React.createElement("div", { key: a.sku_id, style: {
        display: "grid",
        gridTemplateColumns: "40px 1fr auto",
        gap: 12,
        alignItems: "center",
        padding: "11px 12px",
        borderTop: i ? "0.5px solid rgba(28,25,23,0.08)" : "none"
      } }, a.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(a.primary_image, 80), alt: "", style: { width: 40, height: 40, objectFit: "cover", border: "0.5px solid rgba(28,25,23,0.06)" } }) : /* @__PURE__ */ React.createElement("div", { style: { width: 40, height: 40, background: "var(--stone-200)", border: "0.5px solid rgba(28,25,23,0.06)" } }), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.2 var(--roma-sans)", color: TH.ink } }, a.accessory_label || a.variant_name || "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.3 var(--roma-sans)", color: TH.muted, marginTop: 2 } }, a.sell_by === "box" ? "per sqft" : "per unit")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 15px/1 var(--roma-serif)", color: TH.ink } }, a.retail_price ? fmtMoney(a.retail_price) : "\u2014"), showCost && a.cost && /* @__PURE__ */ React.createElement("div", { style: { font: "400 10px/1 var(--roma-sans)", color: TH.muted, marginTop: 3 } }, "cost ", fmtMoney(a.cost))))))), showCost && retail > 0 && cost > 0 && /* @__PURE__ */ React.createElement("div", { style: { background: TH.warm, padding: 16, border: "0.5px solid rgba(28,25,23,0.06)" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 14 } }, /* @__PURE__ */ React.createElement("h4", { style: sectionTitle }, "Price & margin calculator"), price < retail && /* @__PURE__ */ React.createElement("span", { style: { ...mono("#c54731"), color: "#c54731" } }, "\u2212", ((retail - price) / retail * 100).toFixed(0), "% off retail")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 10, marginBottom: 14 } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => setCalcPrice(clampPrice((calcPrice || retail) - step)),
          style: { width: 38, height: 38, border: "0.5px solid rgba(28,25,23,0.27)", background: "transparent", color: TH.ink, cursor: "pointer", font: "400 20px/1 var(--roma-sans)", flexShrink: 0 }
        },
        "\u2212"
      ), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 32px/1 var(--roma-serif)", color: TH.ink, letterSpacing: "-0.01em" } }, fmtMoney(price), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-sans)", color: TH.muted } }, unitLabel))), /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => setCalcPrice(clampPrice((calcPrice || retail) + step)),
          style: { width: 38, height: 38, border: "0.5px solid rgba(28,25,23,0.27)", background: "transparent", color: TH.ink, cursor: "pointer", font: "400 20px/1 var(--roma-sans)", flexShrink: 0 }
        },
        "+"
      )), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "range",
          min: calcMin,
          max: calcMax,
          step,
          value: price,
          onChange: (e) => setCalcPrice(clampPrice(+e.target.value)),
          style: { width: "100%", accentColor: TH.accent, marginBottom: 14 }
        }
      ), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 1, background: "rgba(28,25,23,0.1)", border: "0.5px solid rgba(28,25,23,0.1)" } }, /* @__PURE__ */ React.createElement("div", { style: { background: TH.paper, padding: "12px 14px" } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, "Margin at this price"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 26px/1 var(--roma-serif)", color: marginCol(calcMargin), marginTop: 6 } }, calcMargin.toFixed(1), "%")), /* @__PURE__ */ React.createElement("div", { style: { background: TH.paper, padding: "12px 14px" } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, "Profit ", unitLabel), /* @__PURE__ */ React.createElement("div", { style: { font: "400 26px/1 var(--roma-serif)", color: TH.ink, marginTop: 6 } }, fmtMoney(price - cost))))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h4", { style: sectionTitle }, "Sourcing & availability"), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 } }, [
        ["Vendor", (product.brand_name || product.vendor_name || "\u2014") + (product.vendor_public_code ? " \xB7 " + product.vendor_public_code : "")],
        ["Collection", product.collection || "\u2014"],
        ["On hand", (() => {
          const noData = !product.vendor_has_inventory && (activeSku.stock_status === "unknown" || activeSku.stock_status === "out_of_stock" || !activeSku.stock_status);
          if (noData) return "Not tracked";
          return availableQty(activeSku) || "\u2014";
        })()],
        ["Availability", (() => {
          const noData = !product.vendor_has_inventory && (activeSku.stock_status === "unknown" || activeSku.stock_status === "out_of_stock" || !activeSku.stock_status);
          if (noData) return "Confirm with vendor";
          if (activeSku.stock_status === "in_stock") return "Stocked \xB7 ships now";
          if (activeSku.stock_status === "low_stock") return "Low \xB7 confirm before quoting";
          if (activeSku.stock_status === "out_of_stock") return "Backordered \xB7 confirm with vendor";
          return "Confirm with vendor";
        })()]
      ].map(([l, v]) => /* @__PURE__ */ React.createElement("div", { key: l, style: { padding: "12px 14px", border: "0.5px solid rgba(28,25,23,0.06)" } }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, l), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.3 var(--roma-sans)", color: TH.ink, marginTop: 5 } }, v))))), allSkus.length > 1 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { onClick: () => setSpecsOpen(!specsOpen), style: { display: "flex", alignItems: "center", justifyContent: "space-between", cursor: "pointer", userSelect: "none" } }, /* @__PURE__ */ React.createElement("h4", { style: { ...sectionTitle, margin: 0 } }, "All Variants (", allSkus.length, ")"), /* @__PURE__ */ React.createElement("span", { style: { color: TH.muted, font: "400 11px/1 var(--roma-sans)", transform: specsOpen ? "rotate(180deg)" : "none", transition: "transform .15s" } }, "\u25BE")), specsOpen && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12, border: "0.5px solid rgba(28,25,23,0.13)", maxHeight: 200, overflowY: "auto" } }, /* @__PURE__ */ React.createElement("table", { style: { width: "100%", borderCollapse: "collapse", fontSize: "0.75rem" } }, /* @__PURE__ */ React.createElement("thead", null, /* @__PURE__ */ React.createElement("tr", { style: { background: TH.warm } }, /* @__PURE__ */ React.createElement("th", { style: { textAlign: "left", padding: "6px 8px", ...mono(TH.muted), position: "sticky", top: 0, background: TH.warm } }, "Variant"), /* @__PURE__ */ React.createElement("th", { style: { textAlign: "right", padding: "6px 8px", ...mono(TH.muted), position: "sticky", top: 0, background: TH.warm } }, "Retail"), showCost && /* @__PURE__ */ React.createElement("th", { style: { textAlign: "right", padding: "6px 8px", ...mono(TH.muted), position: "sticky", top: 0, background: TH.warm } }, "Margin"), /* @__PURE__ */ React.createElement("th", { style: { textAlign: "right", padding: "6px 8px", ...mono(TH.muted), position: "sticky", top: 0, background: TH.warm } }, "Stock"))), /* @__PURE__ */ React.createElement("tbody", null, allSkus.map((s) => /* @__PURE__ */ React.createElement(
        "tr",
        {
          key: s.id,
          onClick: () => {
            if (s.id !== activeSku.sku_id) loadSku(s.id);
          },
          style: { cursor: "pointer", background: s.id === activeSku.sku_id ? TH.accent + "14" : "transparent", borderBottom: "0.5px solid rgba(28,25,23,0.06)" }
        },
        /* @__PURE__ */ React.createElement("td", { style: { padding: "8px", font: "400 12px/1.2 var(--roma-sans)", color: TH.ink, fontWeight: s.id === activeSku.sku_id ? 600 : 400 } }, s.variant_name || "\u2014"),
        /* @__PURE__ */ React.createElement("td", { style: { padding: "8px", font: "400 13px/1 var(--roma-serif)", color: TH.ink, textAlign: "right" } }, s.retail_price ? "$" + parseFloat(s.retail_price).toFixed(2) : "\u2014"),
        showCost && /* @__PURE__ */ React.createElement("td", { style: { padding: "8px", font: "500 11px/1 ui-monospace, monospace", color: marginCol(s.margin_pct || 0), textAlign: "right" } }, s.margin_pct ? s.margin_pct.toFixed(0) + "%" : "\u2014"),
        /* @__PURE__ */ React.createElement("td", { style: { padding: "8px", textAlign: "right" } }, (() => {
          const vs = stockDisplay({ ...s, vendor_has_inventory: product.vendor_has_inventory });
          return /* @__PURE__ */ React.createElement("span", { style: mono(vs.c) }, "\u25CF", " ", vs.l);
        })())
      ))))))))), product && /* @__PURE__ */ React.createElement("div", { style: {
        flexShrink: 0,
        padding: "14px 20px",
        borderTop: "0.5px solid rgba(28,25,23,0.1)",
        display: "flex",
        gap: 8,
        background: TH.paper
      } }, /* @__PURE__ */ React.createElement("a", { onClick: () => {
        if (onAddTo && activeSku.sku_id) {
          onAddTo({ skuId: activeSku.sku_id, summary: {
            name: product ? product.name : "",
            variant: activeSku.variant_name,
            image: skuMedia.length > 0 ? skuMedia[0].url : null,
            price: activeSku.retail_price,
            sell_by: activeSku.sell_by,
            vendor_sku: activeSku.vendor_sku,
            vendor: product ? product.vendor_name : "",
            category: product ? product.category_name : ""
          } });
        } else {
          onClose();
          navigate("quote-create");
        }
      }, style: {
        flex: 2,
        padding: "13px 8px",
        textAlign: "center",
        background: TH.ink,
        color: TH.paper,
        font: "500 11px/1 var(--roma-sans)",
        letterSpacing: "0.08em",
        textTransform: "uppercase",
        cursor: "pointer",
        textDecoration: "none"
      } }, "+ Add to\u2026"))), lightbox && /* @__PURE__ */ React.createElement(ImageLightbox, { images: lightbox.images, startIndex: lightbox.startIndex, onClose: () => setLightbox(null) })),
      document.body
    );
  }
  function RepPagination({ page, totalPages, onPageChange }) {
    if (totalPages <= 1) return null;
    const maxVisible = 7;
    let start = Math.max(1, page - Math.floor(maxVisible / 2));
    let end = start + maxVisible - 1;
    if (end > totalPages) {
      end = totalPages;
      start = Math.max(1, end - maxVisible + 1);
    }
    const pages = [];
    if (start > 1) {
      pages.push(1);
      if (start > 2) pages.push("...1");
    }
    for (let i = start; i <= end; i++) pages.push(i);
    if (end < totalPages) {
      if (end < totalPages - 1) pages.push("...2");
      pages.push(totalPages);
    }
    return React.createElement(
      "nav",
      { className: "rc-pagination" },
      React.createElement("button", { className: "rc-page-btn", disabled: page <= 1, onClick: () => onPageChange(page - 1) }, "\u2190 Prev"),
      React.createElement(
        "div",
        { style: { display: "flex", alignItems: "center", gap: "0.25rem" } },
        pages.map(
          (p) => typeof p === "string" ? React.createElement("span", { key: p, className: "rc-page-ellipsis" }, "\u2026") : React.createElement("button", { key: p, className: "rc-page-num" + (p === page ? " active" : ""), onClick: () => onPageChange(p) }, p)
        )
      ),
      React.createElement("button", { className: "rc-page-btn", disabled: page >= totalPages, onClick: () => onPageChange(page + 1) }, "Next \u2192")
    );
  }
  const ADDTO_TH = { ink: "#1c1917", paper: "#ece5d8", accent: "#a87935", muted: "#8a7e68", warm: "#d8cdb6" };
  const addtoMoney = (n) => "$" + parseFloat(n).toLocaleString("en-US", { minimumFractionDigits: parseFloat(n) >= 1e3 ? 0 : 2, maximumFractionDigits: 2 });
  const addtoRelTime = (d) => {
    if (!d) return "";
    const s = (Date.now() - new Date(d).getTime()) / 1e3;
    if (s < 3600) return Math.max(1, Math.floor(s / 60)) + "m ago";
    if (s < 86400) return Math.floor(s / 3600) + "h ago";
    if (s < 604800) return Math.floor(s / 86400) + "d ago";
    return Math.floor(s / 604800) + "w ago";
  };
  function AddToDestMark({ type, color, image }) {
    const line = { background: color, display: "block" };
    if (type === "quote") {
      return /* @__PURE__ */ React.createElement("div", { style: { width: 17, height: 21, border: "1.25px solid " + color, padding: "4px 3px", display: "flex", flexDirection: "column", gap: 2.5, justifyContent: "flex-start", boxSizing: "border-box" } }, /* @__PURE__ */ React.createElement("span", { style: { ...line, height: 1.25, width: "65%" } }), /* @__PURE__ */ React.createElement("span", { style: { ...line, height: 1.25, width: "100%" } }), /* @__PURE__ */ React.createElement("span", { style: { ...line, height: 1.25, width: "100%" } }), /* @__PURE__ */ React.createElement("span", { style: { ...line, height: 1.25, width: "45%" } }));
    }
    if (type === "order") {
      return /* @__PURE__ */ React.createElement("div", { style: { width: 21, height: 19, border: "1.25px solid " + color, position: "relative", boxSizing: "border-box" } }, /* @__PURE__ */ React.createElement("span", { style: { ...line, position: "absolute", top: "33%", left: 0, right: 0, height: 1.25 } }), /* @__PURE__ */ React.createElement("span", { style: { ...line, position: "absolute", top: 0, bottom: "67%", left: "50%", width: 1.25, transform: "translateX(-50%)" } }));
    }
    return /* @__PURE__ */ React.createElement("div", { style: { position: "relative", width: 21, height: 21 } }, /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", top: 0, right: 0, width: 13, height: 13, border: "1.25px solid " + color, opacity: 0.5, boxSizing: "border-box" } }), /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", bottom: 0, left: 0, width: 14, height: 14, border: "1.25px solid " + color, overflow: "hidden", boxSizing: "border-box", background: "var(--stone-100)" } }, image && /* @__PURE__ */ React.createElement("img", { src: optimizeImg(image, 40), alt: "", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } })));
  }
  function AddToDestRow({ mark, label, hint, primary, disabled, onClick, image }) {
    const TH = ADDTO_TH;
    const [hover, setHover] = useState(false);
    const fg = primary ? TH.paper : TH.ink;
    const lit = hover && !disabled;
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick,
        disabled,
        onMouseEnter: () => setHover(true),
        onMouseLeave: () => setHover(false),
        style: {
          display: "flex",
          alignItems: "center",
          gap: 14,
          width: "100%",
          textAlign: "left",
          padding: "15px 18px",
          background: primary ? TH.ink : "transparent",
          color: fg,
          border: primary ? "none" : "0.5px solid " + TH.ink + "2e",
          outline: lit ? "1.5px solid " + TH.accent : "1.5px solid transparent",
          outlineOffset: -1.5,
          cursor: disabled ? "wait" : "pointer",
          opacity: disabled ? 0.55 : 1,
          transition: "outline-color .12s, transform .12s",
          transform: lit ? "translateX(2px)" : "none"
        }
      },
      /* @__PURE__ */ React.createElement("span", { style: {
        width: 38,
        height: 38,
        flexShrink: 0,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        border: "0.5px solid " + (primary ? TH.paper + "3a" : TH.ink + "22"),
        background: primary ? TH.paper + "12" : TH.paper + "80"
      } }, /* @__PURE__ */ React.createElement(AddToDestMark, { type: mark, color: fg, image: mark === "sample" ? image : null })),
      /* @__PURE__ */ React.createElement("span", { style: { flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 13.5px/1.2 var(--roma-sans)", letterSpacing: "0.01em" } }, label), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 11px/1.3 var(--roma-sans)", color: primary ? TH.paper + "aa" : TH.muted, marginTop: 2 } }, hint)),
      /* @__PURE__ */ React.createElement("span", { style: { font: "400 16px/1 var(--roma-sans)", color: lit ? TH.accent : primary ? TH.paper + "99" : TH.muted, transition: "color .12s", flexShrink: 0 } }, "\u2192")
    );
  }
  function AddToQuoteRow({ q, disabled, onClick }) {
    const TH = ADDTO_TH;
    const [hover, setHover] = useState(false);
    const sent = q.status === "sent";
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick,
        disabled,
        onMouseEnter: () => setHover(true),
        onMouseLeave: () => setHover(false),
        style: {
          display: "grid",
          gridTemplateColumns: "1fr auto",
          alignItems: "center",
          gap: 12,
          width: "100%",
          textAlign: "left",
          padding: "11px 14px",
          background: hover && !disabled ? TH.warm : "transparent",
          border: "none",
          borderBottom: "0.5px solid " + TH.ink + "12",
          cursor: disabled ? "wait" : "pointer",
          opacity: disabled ? 0.55 : 1,
          transition: "background .1s"
        }
      },
      /* @__PURE__ */ React.createElement("span", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 16px/1 var(--roma-serif)", color: TH.ink } }, q.quote_number), /* @__PURE__ */ React.createElement("span", { style: {
        font: "500 8px/1 ui-monospace, monospace",
        letterSpacing: "0.14em",
        textTransform: "uppercase",
        color: sent ? TH.accent : TH.muted,
        border: "0.5px solid " + (sent ? TH.accent + "88" : TH.ink + "2e"),
        padding: "2.5px 5px"
      } }, q.status)), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 12px/1.2 var(--roma-sans)", color: TH.ink + "b0", marginTop: 4, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, [q.customer_name, addtoRelTime(q.updated_at)].filter(Boolean).join(" \xB7 ") || "\u2014")),
      /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", whiteSpace: "nowrap", flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 15px/1 var(--roma-serif)", color: TH.ink } }, q.total && parseFloat(q.total) > 0 ? addtoMoney(q.total) : "\u2014"), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8.5px/1 ui-monospace, monospace", letterSpacing: "0.1em", color: TH.muted, marginTop: 4 } }, q.item_count, " item", q.item_count !== 1 ? "s" : ""))
    );
  }
  function AddToTargetModal({ skuId, summary, navigate, onClose }) {
    const TH = ADDTO_TH;
    const monoLabel = (extra) => ({ font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: TH.muted, ...extra || {} });
    const [sku, setSku] = useState(null);
    const [loadErr, setLoadErr] = useState(false);
    const [done, setDone] = useState(null);
    const [quoteSearch, setQuoteSearch] = useState("");
    const [existingQuotes, setExistingQuotes] = useState([]);
    const [quotesLoading, setQuotesLoading] = useState(true);
    const quoteSearchTimer = useRef(null);
    const quoteSearchCounter = useRef(0);
    const goTimer = useRef(null);
    const loadQuotes = (term) => {
      setQuotesLoading(true);
      const thisSearch = ++quoteSearchCounter.current;
      const url = term ? "/api/rep/quotes?search=" + encodeURIComponent(term) : "/api/rep/quotes?status=draft";
      repFetch(url).then((d) => {
        if (thisSearch !== quoteSearchCounter.current) return;
        const editable = (d.quotes || []).filter((q2) => ["draft", "sent"].includes(q2.status));
        setExistingQuotes(editable.slice(0, 6));
        setQuotesLoading(false);
      }).catch(() => {
        if (thisSearch === quoteSearchCounter.current) setQuotesLoading(false);
      });
    };
    const onQuoteSearch = (v) => {
      setQuoteSearch(v);
      if (quoteSearchTimer.current) clearTimeout(quoteSearchTimer.current);
      quoteSearchTimer.current = setTimeout(() => loadQuotes(v.trim()), 300);
    };
    useEffect(() => {
      repFetch("/api/rep/skus/search?sku_id=" + skuId).then((d) => {
        const r = (d.results || [])[0];
        if (r) setSku(r);
        else setLoadErr(true);
      }).catch(() => setLoadErr(true));
      loadQuotes("");
    }, [skuId]);
    useEffect(() => {
      const onKey = (e) => {
        if (e.key === "Escape") onClose();
      };
      document.addEventListener("keydown", onKey);
      return () => {
        document.removeEventListener("keydown", onKey);
        if (goTimer.current) clearTimeout(goTimer.current);
      };
    }, [onClose]);
    const go = (target, view, id, extra, title, sub) => {
      if (!sku || done) return;
      setDone({ title, sub });
      goTimer.current = setTimeout(() => {
        stashPendingAdd(target, sku, extra);
        onClose();
        navigate(view, id);
      }, 850);
    };
    const price = summary && summary.price ? parseFloat(summary.price) : 0;
    const unit = summary && summary.sell_by === "unit" ? "ea" : summary && summary.sell_by === "roll" ? "sqyd" : "sqft";
    const ready = !!sku && !done;
    const q = quoteSearch.trim();
    const dests = [
      {
        mark: "quote",
        label: "New quote",
        hint: "Start a quote with this line already added",
        primary: true,
        act: () => go("quote", "quote-create", void 0, void 0, "Added to a new quote", "Opening the quote builder\u2026")
      },
      {
        mark: "order",
        label: "New order",
        hint: "Begin an order \u2014 running total updates live",
        primary: false,
        act: () => go("order", "order-create", void 0, void 0, "Added to a new order", "Opening the order wizard\u2026")
      },
      {
        mark: "sample",
        label: "Sample request",
        hint: "Send a physical sample \xB7 max 5 per request",
        primary: false,
        act: () => go("sample", "sample-create", void 0, void 0, "Added to sample request", "Opening the sample form\u2026")
      }
    ];
    return ReactDOM.createPortal(
      /* @__PURE__ */ React.createElement("div", { onClick: onClose, style: {
        position: "fixed",
        inset: 0,
        zIndex: 1300,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: TH.ink + "5c",
        backdropFilter: "blur(3px)",
        padding: 24,
        animation: "rcatFadeIn .15s ease"
      } }, /* @__PURE__ */ React.createElement("div", { onClick: (e) => e.stopPropagation(), style: {
        width: 460,
        maxWidth: "100%",
        maxHeight: "92vh",
        background: TH.paper,
        color: TH.ink,
        border: "0.5px solid " + TH.ink + "33",
        boxShadow: "0 30px 80px " + TH.ink + "55",
        display: "flex",
        flexDirection: "column",
        overflow: "hidden",
        animation: "rofPop .22s cubic-bezier(.22,.61,.36,1)"
      } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "14px 20px 13px", borderBottom: "0.5px solid " + TH.ink + "1a", flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: TH.accent } }, "Add to\u2026"), /* @__PURE__ */ React.createElement("button", { onClick: onClose, style: { width: 26, height: 26, border: "0.5px solid " + TH.ink + "2e", background: "transparent", color: TH.ink, cursor: "pointer", font: "400 13px/1 var(--roma-sans)", padding: 0 } }, "\u2715")), done ? (
        /* success state */
        /* @__PURE__ */ React.createElement("div", { style: { padding: "48px 30px 52px", textAlign: "center", display: "flex", flexDirection: "column", alignItems: "center", gap: 4 } }, /* @__PURE__ */ React.createElement("div", { style: { width: 46, height: 46, borderRadius: "50%", border: "1.5px solid " + TH.accent, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 14, color: TH.accent, font: "400 22px/1 var(--roma-sans)", animation: "rofPopCheck .3s ease" } }, "\u2713"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 23px/1.15 var(--roma-serif)", letterSpacing: "-0.01em" } }, done.title), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.4 var(--roma-sans)", color: TH.muted, marginTop: 2 } }, done.sub), summary && /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.3 var(--roma-sans)", color: TH.ink + "cc", marginTop: 18, padding: "9px 14px", background: TH.warm, border: "0.5px solid " + TH.ink + "1a", maxWidth: "90%" } }, summary.name, summary.variant ? /* @__PURE__ */ React.createElement("span", { style: { color: TH.muted } }, " \xB7 ", summary.variant) : null))
      ) : loadErr ? /* @__PURE__ */ React.createElement("div", { style: { padding: "32px 24px", font: "400 12.5px/1.5 var(--roma-sans)", color: "#c54731" } }, "Could not load this SKU. Close and try again.") : /* @__PURE__ */ React.createElement("div", { style: { overflowY: "auto", flex: 1 } }, summary && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 14, padding: "16px 22px", background: TH.warm, borderBottom: "0.5px solid " + TH.ink + "1a" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 52, height: 52, flexShrink: 0, border: "0.5px solid " + TH.ink + "22", overflow: "hidden", background: "var(--stone-100)" } }, summary.image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(summary.image, 120), alt: "", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }) : null), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0, flex: 1 } }, (summary.vendor || summary.category) && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: TH.muted, marginBottom: 5 } }, [summary.vendor, summary.category].filter(Boolean).join(" \xB7 ")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 19px/1.1 var(--roma-serif)", letterSpacing: "-0.01em", color: TH.ink, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, summary.name), summary.variant && /* @__PURE__ */ React.createElement("div", { style: { font: "400 11.5px/1.2 var(--roma-sans)", color: TH.ink + "99", marginTop: 3, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, summary.variant)), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right", flexShrink: 0 } }, price > 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "400 20px/1 var(--roma-serif)", color: TH.ink } }, addtoMoney(price)), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1 ui-monospace, monospace", letterSpacing: "0.1em", color: TH.muted, marginTop: 4 } }, price > 0 ? "/" + unit + " \xB7 " : "", summary.vendor_sku || ""))), /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 22px 8px" } }, /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel({ marginBottom: 11 }) } }, "Create new"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: 8 } }, dests.map((d) => /* @__PURE__ */ React.createElement(AddToDestRow, { key: d.label, mark: d.mark, label: d.label, hint: d.hint, primary: d.primary, disabled: !ready, onClick: d.act, image: summary ? summary.image : null })))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 12, padding: "16px 22px 6px" } }, /* @__PURE__ */ React.createElement("span", { style: { flex: 1, height: 0.5, background: TH.ink + "22" } }), /* @__PURE__ */ React.createElement("span", { style: monoLabel() }, "Or add to existing"), /* @__PURE__ */ React.createElement("span", { style: { flex: 1, height: 0.5, background: TH.ink + "22" } })), /* @__PURE__ */ React.createElement("div", { style: { padding: "8px 22px 0" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid " + TH.ink + "33", background: TH.paper } }, /* @__PURE__ */ React.createElement("span", { style: { padding: "0 10px", color: TH.muted, font: "400 14px/1 var(--roma-sans)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
        "input",
        {
          value: quoteSearch,
          onChange: (e) => onQuoteSearch(e.target.value),
          placeholder: "Search quotes \u2014 number, customer, email\u2026",
          style: { flex: 1, border: "none", background: "transparent", outline: "none", font: "400 12.5px/1 var(--roma-sans)", color: TH.ink, padding: "10px 8px 10px 0" }
        }
      ), quoteSearch && /* @__PURE__ */ React.createElement("span", { onClick: () => onQuoteSearch(""), style: { padding: "0 12px", cursor: "pointer", color: TH.muted, font: "400 14px/1 var(--roma-sans)" } }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 22px 20px" } }, !q && existingQuotes.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: TH.muted, margin: "2px 2px 8px" } }, "Recent \xB7 editable"), quotesLoading ? /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 12px", textAlign: "center", font: "400 12.5px/1.5 var(--roma-sans)", color: TH.muted, border: "0.5px dashed " + TH.ink + "2a" } }, "Loading quotes\u2026") : existingQuotes.length > 0 ? /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid " + TH.ink + "1f" } }, existingQuotes.map((qt) => /* @__PURE__ */ React.createElement(
        AddToQuoteRow,
        {
          key: qt.id,
          q: qt,
          disabled: !ready,
          onClick: () => go(
            "quote",
            "quote-edit",
            qt.id,
            { quoteId: qt.id },
            "Added to " + qt.quote_number,
            qt.customer_name ? "Opening " + qt.customer_name + "\u2019s quote\u2026" : "Opening the quote\u2026"
          )
        }
      ))) : /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 12px", textAlign: "center", font: "400 12.5px/1.5 var(--roma-sans)", color: TH.muted, border: "0.5px dashed " + TH.ink + "2a" } }, q ? "No editable quotes match \u201C" + quoteSearch + "\u201D." : "No draft quotes yet \u2014 search to find one."))))),
      document.body
    );
  }
  function BulkAddToQuoteModal({ items, navigate, onClose, onCommitted }) {
    const TH = ADDTO_TH;
    const monoLabel = (extra) => ({ font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: TH.muted, ...extra || {} });
    const [done, setDone] = useState(null);
    const [busy, setBusy] = useState(false);
    const [err, setErr] = useState(null);
    const [quoteSearch, setQuoteSearch] = useState("");
    const [existingQuotes, setExistingQuotes] = useState([]);
    const [quotesLoading, setQuotesLoading] = useState(true);
    const quoteSearchTimer = useRef(null);
    const quoteSearchCounter = useRef(0);
    const goTimer = useRef(null);
    const loadQuotes = (term) => {
      setQuotesLoading(true);
      const thisSearch = ++quoteSearchCounter.current;
      const url = term ? "/api/rep/quotes?search=" + encodeURIComponent(term) : "/api/rep/quotes?status=draft";
      repFetch(url).then((d) => {
        if (thisSearch !== quoteSearchCounter.current) return;
        const editable = (d.quotes || []).filter((q2) => ["draft", "sent"].includes(q2.status));
        setExistingQuotes(editable.slice(0, 6));
        setQuotesLoading(false);
      }).catch(() => {
        if (thisSearch === quoteSearchCounter.current) setQuotesLoading(false);
      });
    };
    const onQuoteSearch = (v) => {
      setQuoteSearch(v);
      if (quoteSearchTimer.current) clearTimeout(quoteSearchTimer.current);
      quoteSearchTimer.current = setTimeout(() => loadQuotes(v.trim()), 300);
    };
    useEffect(() => {
      loadQuotes("");
    }, []);
    useEffect(() => {
      const onKey = (e) => {
        if (e.key === "Escape" && !busy) onClose();
      };
      document.addEventListener("keydown", onKey);
      return () => {
        document.removeEventListener("keydown", onKey);
        if (goTimer.current) clearTimeout(goTimer.current);
      };
    }, [onClose, busy]);
    const commit = async (quoteId, view, navId, title, sub) => {
      if (busy || done) return;
      setBusy(true);
      setErr(null);
      try {
        const skus = (await Promise.all(items.map(
          (it) => repFetch("/api/rep/skus/search?sku_id=" + it.sku_id).then((d) => (d.results || [])[0]).catch(() => null)
        ))).filter(Boolean);
        if (!skus.length) {
          setBusy(false);
          setErr("Could not load the selected SKUs. Close and try again.");
          return;
        }
        stashPendingAdd("quote", skus[0], { skus, ...quoteId ? { quoteId } : {} });
        setDone({ title, sub });
        goTimer.current = setTimeout(() => {
          if (onCommitted) onCommitted();
          onClose();
          navigate(view, navId);
        }, 850);
      } catch (e) {
        setBusy(false);
        setErr("Something went wrong. Try again.");
      }
    };
    const q = quoteSearch.trim();
    const total = items.reduce((s, it) => s + (it.price ? parseFloat(it.price) : 0), 0);
    const preview = items.slice(0, 4);
    return ReactDOM.createPortal(
      /* @__PURE__ */ React.createElement("div", { onClick: () => {
        if (!busy) onClose();
      }, style: {
        position: "fixed",
        inset: 0,
        zIndex: 1400,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: TH.ink + "5c",
        backdropFilter: "blur(3px)",
        padding: 24,
        animation: "rcatFadeIn .15s ease"
      } }, /* @__PURE__ */ React.createElement("div", { onClick: (e) => e.stopPropagation(), style: {
        width: 460,
        maxWidth: "100%",
        maxHeight: "92vh",
        background: TH.paper,
        color: TH.ink,
        border: "0.5px solid " + TH.ink + "33",
        boxShadow: "0 30px 80px " + TH.ink + "55",
        display: "flex",
        flexDirection: "column",
        overflow: "hidden",
        animation: "rofPop .22s cubic-bezier(.22,.61,.36,1)"
      } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "14px 20px 13px", borderBottom: "0.5px solid " + TH.ink + "1a", flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: TH.accent } }, "Add ", items.length, " item", items.length === 1 ? "" : "s", " to\u2026"), /* @__PURE__ */ React.createElement("button", { onClick: () => {
        if (!busy) onClose();
      }, style: { width: 26, height: 26, border: "0.5px solid " + TH.ink + "2e", background: "transparent", color: TH.ink, cursor: "pointer", font: "400 13px/1 var(--roma-sans)", padding: 0 } }, "\u2715")), done ? /* @__PURE__ */ React.createElement("div", { style: { padding: "48px 30px 52px", textAlign: "center", display: "flex", flexDirection: "column", alignItems: "center", gap: 4 } }, /* @__PURE__ */ React.createElement("div", { style: { width: 46, height: 46, borderRadius: "50%", border: "1.5px solid " + TH.accent, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 14, color: TH.accent, font: "400 22px/1 var(--roma-sans)", animation: "rofPopCheck .3s ease" } }, "\u2713"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 23px/1.15 var(--roma-serif)", letterSpacing: "-0.01em" } }, done.title), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.4 var(--roma-sans)", color: TH.muted, marginTop: 2 } }, done.sub)) : /* @__PURE__ */ React.createElement("div", { style: { overflowY: "auto", flex: 1 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 22px", background: TH.warm, borderBottom: "0.5px solid " + TH.ink + "1a" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 6, marginBottom: preview.length ? 12 : 0 } }, preview.map((it) => /* @__PURE__ */ React.createElement("div", { key: it.sku_id, style: { width: 42, height: 42, flexShrink: 0, border: "0.5px solid " + TH.ink + "22", overflow: "hidden", background: "var(--stone-100)" } }, it.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(it.primary_image, 100), alt: "", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }) : null)), items.length > preview.length && /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1 var(--roma-serif)", color: TH.ink + "99" } }, "+", items.length - preview.length), /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }), total > 0 && /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 18px/1 var(--roma-serif)", color: TH.ink } }, addtoMoney(total)), /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel(), marginTop: 3 } }, "Retail total")))), err && /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 22px", font: "400 12px/1.4 var(--roma-sans)", color: "#c54731" } }, err), /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 22px 8px" } }, /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel({ marginBottom: 11 }) } }, "Create new"), /* @__PURE__ */ React.createElement(
        AddToDestRow,
        {
          mark: "quote",
          label: "New quote",
          hint: "Start a quote with all " + items.length + " lines added",
          primary: true,
          disabled: busy,
          onClick: () => commit(null, "quote-create", void 0, "Added to a new quote", "Opening the quote builder\u2026"),
          image: preview[0] ? preview[0].primary_image : null
        }
      )), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 12, padding: "16px 22px 6px" } }, /* @__PURE__ */ React.createElement("span", { style: { flex: 1, height: 0.5, background: TH.ink + "22" } }), /* @__PURE__ */ React.createElement("span", { style: monoLabel() }, "Or add to existing"), /* @__PURE__ */ React.createElement("span", { style: { flex: 1, height: 0.5, background: TH.ink + "22" } })), /* @__PURE__ */ React.createElement("div", { style: { padding: "8px 22px 0" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid " + TH.ink + "33", background: TH.paper } }, /* @__PURE__ */ React.createElement("span", { style: { padding: "0 10px", color: TH.muted, font: "400 14px/1 var(--roma-sans)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
        "input",
        {
          value: quoteSearch,
          onChange: (e) => onQuoteSearch(e.target.value),
          placeholder: "Search quotes \u2014 number, customer, email\u2026",
          style: { flex: 1, border: "none", background: "transparent", outline: "none", font: "400 12.5px/1 var(--roma-sans)", color: TH.ink, padding: "10px 8px 10px 0" }
        }
      ), quoteSearch && /* @__PURE__ */ React.createElement("span", { onClick: () => onQuoteSearch(""), style: { padding: "0 12px", cursor: "pointer", color: TH.muted, font: "400 14px/1 var(--roma-sans)" } }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 22px 20px" } }, !q && existingQuotes.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: TH.muted, margin: "2px 2px 8px" } }, "Recent \xB7 editable"), quotesLoading ? /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 12px", textAlign: "center", font: "400 12.5px/1.5 var(--roma-sans)", color: TH.muted, border: "0.5px dashed " + TH.ink + "2a" } }, "Loading quotes\u2026") : existingQuotes.length > 0 ? /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid " + TH.ink + "1f" } }, existingQuotes.map((qt) => /* @__PURE__ */ React.createElement(
        AddToQuoteRow,
        {
          key: qt.id,
          q: qt,
          disabled: busy,
          onClick: () => commit(
            qt.id,
            "quote-edit",
            qt.id,
            "Added to " + qt.quote_number,
            qt.customer_name ? "Opening " + qt.customer_name + "\u2019s quote\u2026" : "Opening the quote\u2026"
          )
        }
      ))) : /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 12px", textAlign: "center", font: "400 12.5px/1.5 var(--roma-sans)", color: TH.muted, border: "0.5px dashed " + TH.ink + "2a" } }, q ? "No editable quotes match \u201C" + quoteSearch + "\u201D." : "No draft quotes yet \u2014 search to find one."))))),
      document.body
    );
  }
  function RepCatalogView({ navigate, showCost, setShowCost }) {
    const TH = { ink: "#1c1917", paper: "#ece5d8", accent: "#a87935", muted: "#8a7e68", warm: "#d8cdb6" };
    const mono = (c) => ({ font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: c });
    const fmtMoney = (n) => "$" + parseFloat(n).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const marginCol = (pct) => pct == null ? TH.muted : pct >= 30 ? "#3a7a4e" : pct >= 15 ? "#a87935" : "#c54731";
    const fmtPct = (pct) => pct == null ? "\u2014" : pct.toFixed(1) + "%";
    const fmtSize = (s) => String(s).replace(/(\d)\s*[xX]\s*(\d)/g, "$1\xD7$2");
    const skuVariantText = (p) => {
      const nameLower = (p.name || "").toLowerCase();
      const parts = [];
      const base = p.variant_type === "accessory" ? p.accessory_label || p.variant_name : p.variant_name;
      if (base && !/^(standard|default)$/i.test(base.trim())) {
        const bl = base.trim().toLowerCase();
        if (!(nameLower === bl || nameLower.endsWith(" " + bl) || nameLower.endsWith("- " + bl))) {
          parts.push(base.trim());
        }
      }
      for (const extra of [p.size, p.finish]) {
        if (!extra) continue;
        const el = String(extra).trim();
        const ell = el.toLowerCase();
        if (!ell || /^(standard|default|n\/a|none)$/i.test(ell)) continue;
        const seen = (parts.join(" ") + " " + nameLower).toLowerCase();
        if (seen.includes(ell) || parts.some((x) => x.toLowerCase() === ell)) continue;
        parts.push(extra === p.size ? fmtSize(el) : el);
      }
      return parts.join(" \xB7 ");
    };
    const [products, setProducts] = useState([]);
    const [loading, setLoading] = useState(true);
    const [search, setSearch] = useState("");
    const [searchInput, setSearchInput] = useState("");
    const [category, setCategory] = useState("");
    const [vendor, setVendor] = useState("");
    const [collection, setCollection] = useState("");
    const [stockStatus, setStockStatus] = useState("");
    const [minPrice, setMinPrice] = useState("");
    const [maxPrice, setMaxPrice] = useState("");
    const [categories, setCategories] = useState([]);
    const [vendors, setVendors] = useState([]);
    const [collections, setCollections] = useState([]);
    const [filterableAttrs, setFilterableAttrs] = useState([]);
    const [attrFilters, setAttrFilters] = useState({});
    const [page, setPage] = useState(1);
    const [total, setTotal] = useState(0);
    const [lastUpdated, setLastUpdated] = useState(null);
    const [viewMode, setViewMode] = useState("grid");
    const [sortBy, setSortBy] = useState("margin_desc");
    const [perPage, setPerPage] = useState(24);
    const [quickView, setQuickView] = useState(null);
    const [addTo, setAddTo] = useState(null);
    const [includeAccessories, setIncludeAccessories] = useState(false);
    const [lightbox, setLightbox] = useState(null);
    const [selected, setSelected] = useState({});
    const [bulkOpen, setBulkOpen] = useState(false);
    const searchTimer = useRef(null);
    const userSorted = useRef(false);
    useEffect(() => {
      if (searchTimer.current) clearTimeout(searchTimer.current);
      searchTimer.current = setTimeout(() => {
        const q = searchInput.trim();
        setSearch(searchInput);
        setPage(1);
        if (q && !userSorted.current) setSortBy("relevance");
        if (!q) {
          userSorted.current = false;
          setSortBy((sb) => sb === "relevance" ? "margin_desc" : sb);
        }
      }, 300);
      return () => {
        if (searchTimer.current) clearTimeout(searchTimer.current);
      };
    }, [searchInput]);
    const [railSections, setRailSections] = useState({ category: false, vendor: false, price: false, stock: false, accessories: false, collection: false, attributes: true });
    const toggleRailSection = (key) => setRailSections((s) => ({ ...s, [key]: !s[key] }));
    useEffect(() => {
      document.body.style.overflow = "";
      return () => {
        document.body.style.overflow = "";
      };
    }, []);
    useEffect(() => {
      fetch(API + "/api/categories").then((r) => r.json()).then((d) => setCategories(d.categories || [])).catch(() => {
      });
      repFetch("/api/rep/vendors").then((d) => setVendors(d.vendors || [])).catch(() => {
      });
    }, []);
    useEffect(() => {
      setAttrFilters({});
      if (!category) {
        setFilterableAttrs([]);
        return;
      }
      repFetch("/api/rep/filterable-attributes?category=" + encodeURIComponent(category)).then((d) => setFilterableAttrs(d.attributes || [])).catch(() => setFilterableAttrs([]));
    }, [category]);
    useEffect(() => {
      const url = vendor ? API + "/api/collections?vendor=" + vendor : API + "/api/collections";
      fetch(url).then((r) => r.json()).then((d) => {
        setCollections(d.collections || []);
        if (vendor && collection) {
          const slugs = (d.collections || []).map((c) => c.slug);
          if (!slugs.includes(collection)) setCollection("");
        }
      }).catch(() => {
      });
    }, [vendor]);
    const STALE_MS = 6e4;
    const lastLoadRef = useRef(0);
    const load = useCallback((background) => {
      if (!background) setLoading(true);
      const params = new URLSearchParams();
      if (search) params.set("search", search);
      if (category) params.set("category", category);
      if (vendor) params.set("vendor", vendor);
      if (collection) params.set("collection", collection);
      if (stockStatus) params.set("stock_status", stockStatus);
      if (minPrice) params.set("min_price", minPrice);
      if (maxPrice) params.set("max_price", maxPrice);
      if (includeAccessories) params.set("include_accessories", "1");
      Object.entries(attrFilters).forEach(([slug, val]) => {
        if (val) params.set(slug, val);
      });
      params.set("page", page);
      params.set("limit", perPage);
      params.set("sort", sortBy);
      repFetch("/api/rep/products?" + params.toString()).then((d) => {
        setProducts(d.products || []);
        setTotal(d.total || 0);
        const now = /* @__PURE__ */ new Date();
        setLastUpdated(now);
        lastLoadRef.current = now.getTime();
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [search, category, vendor, collection, stockStatus, minPrice, maxPrice, includeAccessories, attrFilters, page, perPage, sortBy]);
    useEffect(load, [load]);
    useEffect(() => {
      const iv = setInterval(() => {
        if (Date.now() - lastLoadRef.current > STALE_MS) load(true);
      }, 12e4);
      return () => clearInterval(iv);
    }, [load]);
    useEffect(() => {
      const fn = () => {
        if (Date.now() - lastLoadRef.current > STALE_MS) load(true);
      };
      window.addEventListener("focus", fn);
      return () => window.removeEventListener("focus", fn);
    }, [load]);
    const topRef = useRef(null);
    const pageMountRef = useRef(false);
    useEffect(() => {
      if (!pageMountRef.current) {
        pageMountRef.current = true;
        return;
      }
      if (topRef.current) topRef.current.scrollIntoView({ block: "start", behavior: "smooth" });
    }, [page]);
    const totalPages = Math.ceil(total / perPage) || 1;
    const startIdx = (page - 1) * perPage + 1;
    const endIdx = Math.min(page * perPage, total);
    const openAddTo = (p, variantText) => setAddTo({
      skuId: p.sku_id,
      summary: {
        name: p.display_name || p.name,
        variant: p.display_name ? "" : variantText !== void 0 ? variantText : p.variant_name,
        image: p.primary_image,
        price: p.price,
        sell_by: p.sell_by,
        vendor_sku: p.vendor_sku || p.internal_sku,
        vendor: p.brand_name || p.vendor_name,
        category: p.category_name
      }
    });
    const clearAllFilters = () => {
      setCategory("");
      setVendor("");
      setCollection("");
      setStockStatus("");
      setMinPrice("");
      setMaxPrice("");
      setIncludeAccessories(false);
      setAttrFilters({});
      setPage(1);
    };
    const selectedList = Object.values(selected);
    const selectedCount = selectedList.length;
    const isSelected = (id) => Object.prototype.hasOwnProperty.call(selected, id);
    const toggleSelect = (p) => setSelected((prev) => {
      const next = { ...prev };
      if (next[p.sku_id]) delete next[p.sku_id];
      else next[p.sku_id] = p;
      return next;
    });
    const selectPage = () => setSelected((prev) => {
      const next = { ...prev };
      products.forEach((p) => {
        next[p.sku_id] = p;
      });
      return next;
    });
    const clearSelected = () => setSelected({});
    const allPageSelected = products.length > 0 && products.every((p) => isSelected(p.sku_id));
    const printSelectedLabels = () => {
      const ids = selectedList.map((p) => p.sku_id).filter(Boolean);
      if (!ids.length) return;
      repOpenDoc("/api/rep/labels?format=image&skuIds=" + ids.join(","), true).catch((err) => alert(err.message || "Failed to generate labels"));
    };
    const SelectBox = ({ on, onToggle, size, style }) => /* @__PURE__ */ React.createElement("button", { onClick: (e) => {
      e.stopPropagation();
      onToggle();
    }, title: on ? "Deselect" : "Select", style: {
      width: size || 22,
      height: size || 22,
      borderRadius: 4,
      cursor: "pointer",
      flexShrink: 0,
      padding: 0,
      border: "1px solid " + (on ? TH.accent : TH.ink + "55"),
      background: on ? TH.accent : TH.paper,
      color: "#fff",
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      font: "600 12px/1 var(--roma-sans)",
      boxShadow: "0 1px 3px " + TH.ink + "22",
      ...style || {}
    } }, on ? "\u2713" : "");
    const chips = [];
    if (category) {
      const cat = (categories || []).flatMap((c) => [c, ...c.children || []]).find((c) => c.slug === category);
      chips.push({ key: "cat", label: cat ? cat.name : category, clear: () => {
        setCategory("");
        setPage(1);
      } });
    }
    if (vendor) {
      const v = vendors.find((x) => String(x.id) === String(vendor));
      chips.push({ key: "vendor", label: v ? v.name + (v.public_code ? " \xB7 " + v.public_code : "") : "Vendor", clear: () => {
        setVendor("");
        setPage(1);
      } });
    }
    if (collection) {
      const col = collections.find((c) => c.slug === collection);
      chips.push({ key: "coll", label: col ? col.name : collection, clear: () => {
        setCollection("");
        setPage(1);
      } });
    }
    if (stockStatus) {
      const labels = { in_stock: "In Stock", low_stock: "Low Stock", out_of_stock: "Out of Stock" };
      chips.push({ key: "stock", label: labels[stockStatus] || stockStatus, clear: () => {
        setStockStatus("");
        setPage(1);
      } });
    }
    if (minPrice) chips.push({ key: "minP", label: "Min $" + minPrice, clear: () => {
      setMinPrice("");
      setPage(1);
    } });
    if (maxPrice) chips.push({ key: "maxP", label: "Max $" + maxPrice, clear: () => {
      setMaxPrice("");
      setPage(1);
    } });
    if (includeAccessories) chips.push({ key: "acc", label: "Incl. accessories", clear: () => {
      setIncludeAccessories(false);
      setPage(1);
    } });
    Object.entries(attrFilters).forEach(([slug, val]) => {
      if (val) {
        const attr = filterableAttrs.find((a) => a.slug === slug);
        chips.push({ key: slug, label: (attr ? attr.name + ": " : "") + val, clear: () => {
          setAttrFilters((prev) => ({ ...prev, [slug]: "" }));
          setPage(1);
        } });
      }
    });
    const handleSort = (field) => {
      if (sortBy === field + "_asc") setSortBy(field + "_desc");
      else setSortBy(field + "_asc");
      setPage(1);
    };
    const flatCats = [];
    (categories || []).forEach((c) => {
      flatCats.push({ slug: c.slug, name: c.name });
      if (c.children) c.children.forEach((ch) => flatCats.push({ slug: ch.slug, name: ch.name }));
    });
    const priceBands = [
      { label: "Under $5", min: "", max: "5" },
      { label: "$5 \u2013 $10", min: "5", max: "10" },
      { label: "$10 \u2013 $20", min: "10", max: "20" },
      { label: "Over $20", min: "20", max: "" }
    ];
    const activeBand = priceBands.findIndex((b) => b.min === minPrice && b.max === maxPrice);
    const sortOptions = [
      ...search ? [{ value: "relevance", label: "Relevance" }] : [],
      { value: "margin_desc", label: "Margin \xB7 high \u2192 low" },
      { value: "margin_asc", label: "Margin \xB7 low \u2192 high" },
      { value: "price_desc", label: "Price \xB7 high \u2192 low" },
      { value: "price_asc", label: "Price \xB7 low \u2192 high" },
      { value: "name_asc", label: "Name \xB7 A \u2192 Z" },
      { value: "name_desc", label: "Name \xB7 Z \u2192 A" },
      { value: "newest", label: "Newest first" },
      { value: "stock_desc", label: "Stock level" }
    ];
    const RailSection = ({ id, title, count, children }) => {
      const open = railSections[id];
      return /* @__PURE__ */ React.createElement("div", { style: { borderTop: "0.5px solid rgba(28,25,23,0.1)", padding: "14px 0" } }, /* @__PURE__ */ React.createElement("button", { onClick: () => toggleRailSection(id), style: {
        width: "100%",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        background: "transparent",
        border: "none",
        cursor: "pointer",
        padding: 0
      } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 var(--roma-sans)", letterSpacing: "0.1em", textTransform: "uppercase", color: TH.ink } }, title), count > 0 && /* @__PURE__ */ React.createElement("span", { style: { ...mono("#fff"), color: "#fff", background: TH.accent, padding: "2px 6px", fontSize: 9 } }, count)), /* @__PURE__ */ React.createElement("span", { style: { color: TH.muted, font: "400 11px/1 var(--roma-sans)", transform: open ? "rotate(180deg)" : "none", transition: "transform .15s" } }, "\u25BE")), open && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12 } }, children));
    };
    const CheckRow = ({ label, on, onToggle }) => /* @__PURE__ */ React.createElement("label", { onClick: onToggle, style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", gap: 12, cursor: "pointer" } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "baseline", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { width: 13, height: 13, border: "1px solid " + (on ? TH.accent : TH.ink + "44"), background: on ? TH.accent : "transparent", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0, transform: "translateY(2px)", color: "#fff", font: "400 9px/1 var(--roma-sans)" } }, on ? "\u2713" : ""), /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1.3 var(--roma-sans)", color: on ? TH.ink : TH.ink + "cc" } }, label)));
    return /* @__PURE__ */ React.createElement("div", { ref: topRef }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "flex-end", gap: 24, marginBottom: 20 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 10, marginBottom: 12 } }, /* @__PURE__ */ React.createElement("span", { style: mono(TH.accent) }, "Catalog \xB7 SKU lookup")), /* @__PURE__ */ React.createElement("h1", { style: { font: "300 44px/1 var(--roma-serif)", letterSpacing: "-0.018em", margin: 0, color: TH.ink } }, "Product ", /* @__PURE__ */ React.createElement("em", { style: { color: TH.accent, fontStyle: "italic" } }, "catalog")), /* @__PURE__ */ React.createElement("p", { style: { font: "400 13px/1.5 var(--roma-sans)", color: TH.ink + "b3", margin: "6px 0 0" } }, total.toLocaleString(), " SKUs \xB7 ", vendors.length, " vendors \xB7 live cost & margin \xB7 trade-tier pricing")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, alignItems: "center", font: "500 11px/1 var(--roma-sans)", letterSpacing: "0.06em", textTransform: "uppercase" } }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("quote-create"), style: { padding: "10px 14px", border: "0.5px solid " + TH.ink + "66", color: TH.ink, cursor: "pointer", textDecoration: "none" } }, "Open quote builder \u2192"))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", gap: 16, marginBottom: 14, flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid " + TH.ink + "33", background: TH.paper, flex: 1, maxWidth: 440 } }, /* @__PURE__ */ React.createElement("span", { style: { padding: "9px 12px", color: TH.muted, font: "400 14px/1 var(--roma-sans)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        value: searchInput,
        onChange: (e) => setSearchInput(e.target.value),
        placeholder: "Search SKU, vendor, name, collection\u2026",
        style: { flex: 1, border: "none", background: "transparent", outline: "none", font: "400 13px/1 var(--roma-sans)", color: TH.ink, padding: "9px 8px 9px 0" }
      }
    ), searchInput && /* @__PURE__ */ React.createElement("span", { onClick: () => {
      setSearchInput("");
      setSearch("");
      setPage(1);
    }, style: { padding: "0 12px", cursor: "pointer", color: TH.muted, font: "400 14px/1 var(--roma-sans)" } }, "\xD7")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 10, font: "500 11px/1 var(--roma-sans)" } }, /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, total > 0 ? startIdx + "\u2013" + endIdx + " of " + total.toLocaleString() : "0 SKUs"), /* @__PURE__ */ React.createElement("span", { style: { width: 1, height: 18, background: TH.ink + "22" } }), /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, "Sort"), /* @__PURE__ */ React.createElement("select", { value: sortBy, onChange: (e) => {
      userSorted.current = e.target.value !== "relevance";
      setSortBy(e.target.value);
      setPage(1);
    }, style: {
      padding: "7px 8px",
      border: "0.5px solid " + TH.ink + "33",
      background: TH.paper,
      color: TH.ink,
      font: "500 10px/1 ui-monospace, monospace",
      letterSpacing: "0.06em",
      textTransform: "uppercase",
      cursor: "pointer",
      outline: "none"
    } }, sortOptions.map((o) => /* @__PURE__ */ React.createElement("option", { key: o.value, value: o.value }, o.label))), /* @__PURE__ */ React.createElement("span", { style: { width: 1, height: 18, background: TH.ink + "22" } }), /* @__PURE__ */ React.createElement("label", { onClick: () => setShowCost((s) => !s), style: { display: "flex", alignItems: "center", gap: 7, cursor: "pointer" } }, /* @__PURE__ */ React.createElement("span", { style: { width: 32, height: 18, background: showCost ? TH.accent : TH.ink + "33", borderRadius: 999, position: "relative", transition: "background .15s" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", top: 2, left: showCost ? 16 : 2, width: 14, height: 14, borderRadius: "50%", background: TH.paper, transition: "left .15s" } })), /* @__PURE__ */ React.createElement("span", { style: mono(showCost ? TH.ink : TH.muted) }, showCost ? "\u25CF Cost visible" : "\u25CB Cost hidden")), /* @__PURE__ */ React.createElement("span", { style: { width: 1, height: 18, background: TH.ink + "22" } }), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", border: "0.5px solid " + TH.ink + "33" } }, [["grid", "Grid"], ["compact", "Compact"], ["spec", "Spec"]].map(([v, l], i) => /* @__PURE__ */ React.createElement("button", { key: v, onClick: () => setViewMode(v), style: {
      padding: "7px 12px",
      background: viewMode === v ? TH.ink : "transparent",
      color: viewMode === v ? TH.paper : TH.ink,
      border: "none",
      borderLeft: i ? "0.5px solid " + TH.ink + "22" : "none",
      cursor: "pointer",
      font: "500 10px/1 ui-monospace, monospace",
      letterSpacing: "0.12em",
      textTransform: "uppercase"
    } }, l))), /* @__PURE__ */ React.createElement("select", { value: perPage, onChange: (e) => {
      setPerPage(parseInt(e.target.value));
      setPage(1);
    }, style: {
      padding: "7px 8px",
      border: "0.5px solid " + TH.ink + "33",
      background: TH.paper,
      color: TH.ink,
      font: "500 10px/1 ui-monospace, monospace",
      cursor: "pointer",
      outline: "none"
    } }, [24, 48, 96].map((n) => /* @__PURE__ */ React.createElement("option", { key: n, value: n }, n, " / page"))))), chips.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center", marginBottom: 18 } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), marginRight: 4 } }, "Refined by"), chips.map((c) => /* @__PURE__ */ React.createElement("span", { key: c.key, onClick: c.clear, style: {
      display: "inline-flex",
      alignItems: "center",
      gap: 6,
      padding: "5px 8px",
      background: TH.accent + "1f",
      border: "0.5px solid " + TH.accent + "66",
      cursor: "pointer",
      font: "400 11px/1 var(--roma-sans)",
      color: TH.ink
    } }, c.label, " ", /* @__PURE__ */ React.createElement("span", { style: { color: TH.accent } }, "\xD7"))), /* @__PURE__ */ React.createElement("a", { onClick: clearAllFilters, style: {
      ...mono(TH.accent),
      color: TH.accent,
      marginLeft: 8,
      cursor: "pointer",
      textDecoration: "none",
      borderBottom: "1px solid " + TH.accent,
      paddingBottom: 2
    } }, "Clear all")), /* @__PURE__ */ React.createElement("div", { className: "rep-catalog-main" }, /* @__PURE__ */ React.createElement("aside", { style: { display: "flex", flexDirection: "column" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", paddingBottom: 12 } }, /* @__PURE__ */ React.createElement("h3", { style: { font: "400 19px/1 var(--roma-serif)", color: TH.ink, margin: 0 } }, "Filters"), /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, total, " match", total === 1 ? "" : "es")), /* @__PURE__ */ React.createElement(RailSection, { id: "vendor", title: "Vendor", count: vendor ? 1 : 0 }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 7, maxHeight: 240, overflowY: "auto" } }, vendors.map((v) => /* @__PURE__ */ React.createElement(CheckRow, { key: v.id, label: v.name + (v.public_code ? " \xB7 " + v.public_code : ""), on: String(vendor) === String(v.id), onToggle: () => {
      setVendor(String(vendor) === String(v.id) ? "" : String(v.id));
      setPage(1);
    } })), vendors.length === 0 && /* @__PURE__ */ React.createElement("span", { style: { font: "400 11px/1.4 var(--roma-sans)", color: TH.muted } }, "Loading..."))), /* @__PURE__ */ React.createElement(RailSection, { id: "category", title: "Category", count: category ? 1 : 0 }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 7, maxHeight: 240, overflowY: "auto" } }, flatCats.map((c) => /* @__PURE__ */ React.createElement(CheckRow, { key: c.slug, label: c.name, on: category === c.slug, onToggle: () => {
      setCategory(category === c.slug ? "" : c.slug);
      setPage(1);
    } })), flatCats.length === 0 && /* @__PURE__ */ React.createElement("span", { style: { font: "400 11px/1.4 var(--roma-sans)", color: TH.muted } }, "Loading..."))), category && filterableAttrs.length > 0 && /* @__PURE__ */ React.createElement(RailSection, { id: "attributes", title: "Attributes", count: Object.values(attrFilters).filter(Boolean).length }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 10 } }, filterableAttrs.map((attr) => /* @__PURE__ */ React.createElement("div", { key: attr.slug, style: { display: "grid", gap: 4 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 var(--roma-sans)", letterSpacing: "0.08em", textTransform: "uppercase", color: TH.muted } }, attr.name), /* @__PURE__ */ React.createElement("select", { value: attrFilters[attr.slug] || "", onChange: (e) => {
      const v = e.target.value;
      setAttrFilters((prev) => ({ ...prev, [attr.slug]: v }));
      setPage(1);
    }, style: {
      width: "100%",
      padding: "7px 8px",
      background: "transparent",
      cursor: "pointer",
      border: "0.5px solid " + TH.ink + "33",
      color: TH.ink,
      font: "400 12px/1 var(--roma-sans)"
    } }, /* @__PURE__ */ React.createElement("option", { value: "" }, "Any"), (attr.values || []).map((v) => /* @__PURE__ */ React.createElement("option", { key: v, value: v }, v))))))), /* @__PURE__ */ React.createElement(RailSection, { id: "price", title: "Price", count: minPrice || maxPrice ? 1 : 0 }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 } }, priceBands.map((b, i) => {
      const on = activeBand === i;
      return /* @__PURE__ */ React.createElement("button", { key: b.label, onClick: () => {
        if (on) {
          setMinPrice("");
          setMaxPrice("");
        } else {
          setMinPrice(b.min);
          setMaxPrice(b.max);
        }
        setPage(1);
      }, style: {
        padding: "7px 11px",
        cursor: "pointer",
        background: on ? TH.ink : "transparent",
        color: on ? TH.paper : TH.ink,
        border: on ? "none" : "0.5px solid " + TH.ink + "33",
        font: "400 12px/1 var(--roma-sans)"
      } }, b.label);
    }))), /* @__PURE__ */ React.createElement(RailSection, { id: "stock", title: "Stock", count: stockStatus ? 1 : 0 }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", border: "0.5px solid " + TH.ink + "33" } }, [["in_stock", "In stock"], ["low_stock", "Low"], ["out_of_stock", "Out"]].map(([id, lbl], i) => {
      const on = stockStatus === id;
      return /* @__PURE__ */ React.createElement("button", { key: id, onClick: () => {
        setStockStatus(on ? "" : id);
        setPage(1);
      }, style: {
        flex: 1,
        padding: "8px 6px",
        cursor: "pointer",
        borderLeft: i ? "0.5px solid " + TH.ink + "22" : "none",
        background: on ? TH.ink : "transparent",
        color: on ? TH.paper : TH.ink,
        border: "none",
        font: "500 10px/1 var(--roma-sans)",
        letterSpacing: "0.04em",
        textTransform: "uppercase"
      } }, lbl);
    }))), /* @__PURE__ */ React.createElement(RailSection, { id: "accessories", title: "Accessories", count: includeAccessories ? 1 : 0 }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 7 } }, /* @__PURE__ */ React.createElement(CheckRow, { label: "Show trims, moldings & accessories", on: includeAccessories, onToggle: () => {
      setIncludeAccessories((v) => !v);
      setPage(1);
    } }))), /* @__PURE__ */ React.createElement(RailSection, { id: "collection", title: "Collection", count: collection ? 1 : 0 }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 7, maxHeight: 200, overflowY: "auto" } }, collections.map((c) => /* @__PURE__ */ React.createElement(CheckRow, { key: c.slug, label: c.name, on: collection === c.slug, onToggle: () => {
      setCollection(collection === c.slug ? "" : c.slug);
      setPage(1);
    } })), collections.length === 0 && /* @__PURE__ */ React.createElement("span", { style: { font: "400 11px/1.4 var(--roma-sans)", color: TH.muted } }, "Select a vendor first")))), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, loading ? /* @__PURE__ */ React.createElement("div", { style: { padding: "60px 0", textAlign: "center", font: "400 14px/1.5 var(--roma-sans)", color: TH.ink + "99" } }, "Loading SKUs...") : products.length === 0 ? /* @__PURE__ */ React.createElement("div", { style: { padding: "60px 0", textAlign: "center", font: "400 14px/1.5 var(--roma-sans)", color: TH.ink + "99" } }, "No SKUs match these filters.") : (
      /* Grid view */
      viewMode === "grid" ? /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(240px, 1fr))", gap: 12 } }, products.map((p) => {
        const price = p.price ? pieceRetail(p, p.price) : 0;
        const cost = p.cost ? parseFloat(p.cost) : 0;
        const mPct = p.margin_pct;
        const mc = marginCol(mPct);
        const unit = skuPriceSuffix(p);
        const sk = stockDisplay(p);
        const variantText = skuVariantText(p);
        return /* @__PURE__ */ React.createElement("div", { key: p.sku_id, onClick: () => setQuickView({ id: p.id, skuId: p.sku_id }), style: {
          background: TH.paper,
          border: isSelected(p.sku_id) || quickView && quickView.skuId === p.sku_id ? "1.5px solid " + TH.accent : "0.5px solid " + TH.ink + "22",
          boxShadow: isSelected(p.sku_id) ? "0 0 0 1px " + TH.accent : quickView && quickView.skuId === p.sku_id ? "0 8px 24px " + TH.ink + "1f" : "none",
          display: "flex",
          flexDirection: "column",
          cursor: "pointer",
          position: "relative",
          transition: "border-color .12s, box-shadow .12s"
        } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative", height: 166, overflow: "hidden", flexShrink: 0, background: "var(--stone-100)" } }, p.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(p.primary_image, 500), alt: "", loading: "lazy", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }) : /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "center", height: "100%", color: TH.muted, fontSize: "0.8rem" } }, "No Image"), /* @__PURE__ */ React.createElement(SelectBox, { on: isSelected(p.sku_id), onToggle: () => toggleSelect(p), style: { position: "absolute", top: 10, left: 10, zIndex: 2 } }), /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", top: 10, right: 10, maxWidth: "calc(100% - 56px)", padding: "4px 8px", background: TH.paper, ...mono(sk.c), color: sk.c, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, "\u25CF", " ", sk.l, sk.q ? " \xB7 " + sk.q : "")), /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 14px 8px", display: "grid", gap: 4, flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", gap: 8, ...mono(TH.muted), fontSize: 9, minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", minWidth: 0 } }, p.brand_name || p.vendor_name || "", p.vendor_public_code ? " \xB7 " + p.vendor_public_code : "", p.category_name ? " \xB7 " + p.category_name : "")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 16px/1.2 var(--roma-serif)", color: TH.ink, letterSpacing: "-0.008em", minWidth: 0, overflowWrap: "anywhere" } }, p.display_name || p.name, !p.display_name && variantText ? " \xB7 " + variantText : ""), (p.vendor_sku || p.internal_sku || p.variant_type === "accessory" || p.accessory_count > 0) && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", gap: 8, minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), fontSize: 10, minWidth: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, p.vendor_sku || p.internal_sku || ""), p.variant_type === "accessory" ? /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.accent), fontSize: 9, flexShrink: 0 } }, "Accessory") : p.accessory_count > 0 ? /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.accent), fontSize: 9, flexShrink: 0 } }, "\u229E", " ", p.accessory_count, " accessor", p.accessory_count !== 1 ? "ies" : "y") : null)), showCost && price > 0 ? /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 14px", background: TH.warm, display: "grid", gridTemplateColumns: "repeat(" + (sk.q ? 4 : 3) + ", auto)", justifyContent: "space-between", gap: 8, alignItems: "baseline" } }, [["Retail", fmtMoney(price), TH.ink], ["Cost", cost > 0 ? fmtMoney(cost) : "\u2014", "#c54731"], ["Margin", fmtPct(mPct), mc]].concat(sk.q ? [["Avail", sk.q, sk.c]] : []).map(([l, v, c]) => /* @__PURE__ */ React.createElement("div", { key: l }, /* @__PURE__ */ React.createElement("div", { style: mono(TH.muted) }, l), /* @__PURE__ */ React.createElement("div", { style: { font: "400 15px/1.2 var(--roma-serif)", color: c, marginTop: 3, whiteSpace: "nowrap" } }, v)))) : /* @__PURE__ */ React.createElement("div", { style: { padding: "11px 14px", background: TH.warm, display: "flex", justifyContent: "space-between", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 18px/1 var(--roma-serif)", color: TH.ink } }, price > 0 ? fmtMoney(price) : "\u2014", /* @__PURE__ */ React.createElement("span", { style: { font: "400 11px/1 var(--roma-sans)", color: TH.muted } }, unit)), sk.q && /* @__PURE__ */ React.createElement("span", { style: { ...mono(sk.c), color: sk.c } }, sk.q, " avail")), /* @__PURE__ */ React.createElement("div", { style: { padding: "8px 14px", display: "flex", gap: 4, borderTop: "0.5px solid " + TH.ink + "18" } }, [
          { l: "View", primary: true, act: () => setQuickView({ id: p.id, skuId: p.sku_id }) },
          { l: "Add", primary: false, act: () => openAddTo(p, variantText) },
          { l: "Labels", primary: false, act: () => repOpenDoc("/api/rep/labels?format=image&productIds=" + p.id, true).catch((err) => alert(err.message || "Failed to generate labels")) }
        ].map((b) => /* @__PURE__ */ React.createElement("a", { key: b.l, onClick: (e) => {
          e.stopPropagation();
          b.act();
        }, style: {
          flex: 1,
          minWidth: 0,
          padding: "6px 4px",
          textAlign: "center",
          background: b.primary ? TH.ink : "transparent",
          color: b.primary ? TH.paper : TH.ink,
          border: b.primary ? "none" : "0.5px solid " + TH.ink + "33",
          font: "500 10px/1 var(--roma-sans)",
          letterSpacing: "0.06em",
          textTransform: "uppercase",
          cursor: "pointer",
          textDecoration: "none",
          whiteSpace: "nowrap",
          overflow: "hidden",
          textOverflow: "ellipsis"
        } }, b.l))));
      })) : (
        /* Compact view */
        viewMode === "compact" ? /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(330px, 1fr))", gap: 12 } }, products.map((p) => {
          const price = p.price ? pieceRetail(p, p.price) : 0;
          const cost = p.cost ? parseFloat(p.cost) : 0;
          const mPct = p.margin_pct;
          const mc = marginCol(mPct);
          const unit = skuPriceSuffix(p);
          const sk = stockDisplay(p);
          const variantText = skuVariantText(p);
          return /* @__PURE__ */ React.createElement("div", { key: p.sku_id, onClick: () => setQuickView({ id: p.id, skuId: p.sku_id }), style: {
            background: TH.paper,
            border: isSelected(p.sku_id) || quickView && quickView.skuId === p.sku_id ? "1.5px solid " + TH.accent : "0.5px solid " + TH.ink + "22",
            boxShadow: isSelected(p.sku_id) ? "0 0 0 1px " + TH.accent : quickView && quickView.skuId === p.sku_id ? "0 8px 24px " + TH.ink + "1f" : "none",
            display: "grid",
            gridTemplateColumns: "108px 1fr",
            cursor: "pointer",
            transition: "border-color .12s, box-shadow .12s",
            minHeight: 132
          } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative", overflow: "hidden", background: "var(--stone-100)" } }, p.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(p.primary_image, 250), alt: "", loading: "lazy", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }) : /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "center", height: "100%", color: TH.muted, fontSize: "0.7rem" } }, "No img"), /* @__PURE__ */ React.createElement(SelectBox, { on: isSelected(p.sku_id), onToggle: () => toggleSelect(p), size: 20, style: { position: "absolute", top: 8, left: 8, zIndex: 2 } })), /* @__PURE__ */ React.createElement("div", { style: { padding: "11px 13px", display: "flex", flexDirection: "column", gap: 5, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, p.brand_name || p.vendor_name, p.vendor_public_code ? " \xB7 " + p.vendor_public_code : ""), /* @__PURE__ */ React.createElement("span", { style: { ...mono(sk.c), color: sk.c, flexShrink: 0 } }, "\u25CF", " ", sk.l, sk.q ? " \xB7 " + sk.q : "")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 16px/1.15 var(--roma-serif)", color: TH.ink, letterSpacing: "-0.008em" } }, p.display_name || p.name, !p.display_name && variantText ? " \xB7 " + variantText : ""), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.3 var(--roma-sans)", color: TH.ink + "99" } }, [p.collection, p.category_name].filter(Boolean).join(" \xB7 "), p.variant_type === "accessory" ? /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.accent), fontSize: 9, marginLeft: 8 } }, "Accessory") : p.accessory_count > 0 ? /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.accent), fontSize: 9, marginLeft: 8 } }, p.accessory_count, " accessor", p.accessory_count !== 1 ? "ies" : "y") : null), /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 16px/1 var(--roma-serif)", color: TH.ink } }, price > 0 ? fmtMoney(price) : "\u2014", /* @__PURE__ */ React.createElement("span", { style: { font: "400 10px/1 var(--roma-sans)", color: TH.muted } }, unit)), showCost && (cost > 0 ? /* @__PURE__ */ React.createElement("span", { style: mono(mc) }, fmtMoney(cost), " \xB7 ", mPct.toFixed(0), "%") : /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, "\u2014")))));
        })) : (
          /* Spec (table) view */
          (() => {
            const cols = showCost ? "34px 52px minmax(180px,1.6fr) 0.9fr 0.9fr 1.1fr 0.7fr 0.7fr 1.1fr 0.8fr" : "34px 52px minmax(180px,1.7fr) 1fr 1fr 1.3fr 0.8fr 0.9fr";
            const heads = showCost ? [["", null], ["", null], ["Product", "name"], ["Vendor", null], ["Collection", null], ["Stock", null], ["Retail", "price"], ["Cost", null], ["Margin", "margin"], ["", null]] : [["", null], ["", null], ["Product", "name"], ["Vendor", null], ["Collection", null], ["Stock", null], ["Retail", "price"], ["", null]];
            return /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid " + TH.ink + "22", background: TH.paper } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: cols, gap: 12, padding: "11px 16px", background: TH.warm, borderBottom: "0.5px solid " + TH.ink + "22", alignItems: "center" } }, heads.map(([l, sortKey], i) => i === 0 ? /* @__PURE__ */ React.createElement(SelectBox, { key: "selall", on: allPageSelected, size: 18, onToggle: () => allPageSelected ? clearSelected() : selectPage() }) : /* @__PURE__ */ React.createElement("div", { key: i, onClick: () => sortKey && handleSort(sortKey), style: {
              ...mono(TH.muted),
              cursor: sortKey ? "pointer" : "default",
              textAlign: i >= 6 && i < heads.length - 1 ? "right" : "left",
              color: sortKey && (sortBy === sortKey + "_asc" || sortBy === sortKey + "_desc") ? TH.ink : TH.muted
            } }, l, sortKey && (sortBy === sortKey + "_desc" ? " \u25BE" : sortBy === sortKey + "_asc" ? " \u25B4" : " \u21C5")))), products.map((p, ri) => {
              const price = p.price ? pieceRetail(p, p.price) : 0;
              const cost = p.cost ? parseFloat(p.cost) : 0;
              const mPct = p.margin_pct;
              const mc = marginCol(mPct);
              const unit = skuPriceSuffix(p);
              const sk = stockDisplay(p);
              const variantText = skuVariantText(p);
              return /* @__PURE__ */ React.createElement("div", { key: p.sku_id, onClick: () => setQuickView({ id: p.id, skuId: p.sku_id }), style: {
                display: "grid",
                gridTemplateColumns: cols,
                gap: 12,
                padding: "12px 16px",
                alignItems: "center",
                borderBottom: ri < products.length - 1 ? "0.5px solid " + TH.ink + "12" : "none",
                cursor: "pointer",
                background: isSelected(p.sku_id) ? TH.accent + "1f" : quickView && quickView.skuId === p.sku_id ? TH.accent + "14" : "transparent"
              } }, /* @__PURE__ */ React.createElement(SelectBox, { on: isSelected(p.sku_id), size: 18, onToggle: () => toggleSelect(p) }), /* @__PURE__ */ React.createElement("div", { style: { width: 52, height: 40, overflow: "hidden", border: "0.5px solid " + TH.ink + "1a", background: "var(--stone-100)" } }, p.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(p.primary_image, 100), alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : null), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 15px/1.15 var(--roma-serif)", color: TH.ink, letterSpacing: "-0.008em", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, p.display_name || p.name, !p.display_name && variantText ? " \xB7 " + variantText : ""), /* @__PURE__ */ React.createElement("div", { style: { ...mono(TH.muted), fontSize: 9, marginTop: 3 } }, p.vendor_sku || p.internal_sku || "\u2014", p.variant_type === "accessory" ? " \xB7 Accessory" : p.accessory_count > 0 ? " \xB7 " + p.accessory_count + " accessor" + (p.accessory_count !== 1 ? "ies" : "y") : "")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.3 var(--roma-sans)", color: TH.ink + "cc" } }, p.brand_name || p.vendor_name, p.vendor_public_code ? " \xB7 " + p.vendor_public_code : ""), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.3 var(--roma-sans)", color: TH.ink + "cc" } }, p.collection || "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: 2, minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(sk.c), color: sk.c, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, "\u25CF", " ", sk.l), sk.q && /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, sk.q)), /* @__PURE__ */ React.createElement("div", { style: { font: "400 15px/1 var(--roma-serif)", color: TH.ink, textAlign: "right" } }, price > 0 ? fmtMoney(price) : "\u2014", /* @__PURE__ */ React.createElement("span", { style: { font: "400 9px/1 var(--roma-sans)", color: TH.muted } }, unit)), showCost && /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1 var(--roma-serif)", color: "#c54731", textAlign: "right" } }, cost > 0 ? fmtMoney(cost) : "\u2014"), showCost && /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1 var(--roma-serif)", color: mc, marginBottom: 5 } }, fmtPct(mPct)), mPct > 0 && /* @__PURE__ */ React.createElement("div", { style: { height: 4, background: TH.ink + "12", width: "100%" } }, /* @__PURE__ */ React.createElement("div", { style: { height: "100%", width: Math.min(mPct, 100) + "%", background: mc } }))), /* @__PURE__ */ React.createElement("div", { onClick: (e) => e.stopPropagation(), style: { display: "flex", justifyContent: "flex-end", gap: 4 } }, /* @__PURE__ */ React.createElement("a", { onClick: () => repOpenDoc("/api/rep/labels?format=image&productIds=" + p.id, true).catch((err) => alert(err.message || "Failed to generate labels")), style: { padding: "6px 10px", background: "transparent", color: TH.ink, border: "0.5px solid " + TH.ink + "33", font: "500 10px/1 var(--roma-sans)", letterSpacing: "0.06em", textTransform: "uppercase", cursor: "pointer", textDecoration: "none", whiteSpace: "nowrap" } }, "Labels"), /* @__PURE__ */ React.createElement("a", { onClick: () => setQuickView({ id: p.id, skuId: p.sku_id }), style: { padding: "6px 10px", background: TH.ink, color: TH.paper, font: "500 10px/1 var(--roma-sans)", letterSpacing: "0.06em", textTransform: "uppercase", cursor: "pointer", textDecoration: "none", whiteSpace: "nowrap" } }, "View")));
            }));
          })()
        )
      )
    ), React.createElement(RepPagination, { page, totalPages, onPageChange: setPage }))), quickView && React.createElement(
      QuickViewErrorBoundary,
      { productId: quickView.id, onClose: () => setQuickView(null) },
      /* @__PURE__ */ React.createElement(RepQuickViewModal, { productId: quickView.id, initialSkuId: quickView.skuId, products, showCost, navigate, onClose: () => setQuickView(null), onAddTo: setAddTo })
    ), lightbox && /* @__PURE__ */ React.createElement(ImageLightbox, { images: lightbox.images, startIndex: lightbox.startIndex, onClose: () => setLightbox(null) }), addTo && /* @__PURE__ */ React.createElement(AddToTargetModal, { skuId: addTo.skuId, summary: addTo.summary, navigate, onClose: () => setAddTo(null) }), selectedCount > 0 && /* @__PURE__ */ React.createElement("div", { style: {
      position: "fixed",
      left: "50%",
      bottom: 24,
      transform: "translateX(-50%)",
      zIndex: 1200,
      display: "flex",
      alignItems: "center",
      gap: 16,
      padding: "11px 14px 11px 18px",
      background: TH.ink,
      color: TH.paper,
      boxShadow: "0 12px 40px " + TH.ink + "66",
      maxWidth: "calc(100vw - 32px)"
    } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 15px/1 var(--roma-serif)", whiteSpace: "nowrap" } }, selectedCount, " selected"), /* @__PURE__ */ React.createElement("span", { style: { width: 1, height: 18, background: TH.paper + "33" } }), /* @__PURE__ */ React.createElement("a", { onClick: clearSelected, style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: TH.paper + "cc", cursor: "pointer", whiteSpace: "nowrap" } }, "Clear"), /* @__PURE__ */ React.createElement("a", { onClick: selectPage, style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: TH.paper + "cc", cursor: "pointer", whiteSpace: "nowrap" } }, "Select page"), /* @__PURE__ */ React.createElement("a", { onClick: () => setBulkOpen(true), style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: TH.paper + "cc", cursor: "pointer", whiteSpace: "nowrap" } }, "Add to quote"), /* @__PURE__ */ React.createElement("button", { onClick: printSelectedLabels, style: {
      padding: "9px 16px",
      background: TH.accent,
      color: "#fff",
      border: "none",
      cursor: "pointer",
      font: "500 11px/1 var(--roma-sans)",
      letterSpacing: "0.06em",
      textTransform: "uppercase",
      whiteSpace: "nowrap"
    } }, "Print ", selectedCount, " label", selectedCount === 1 ? "" : "s", " \u2192")), bulkOpen && /* @__PURE__ */ React.createElement(BulkAddToQuoteModal, { items: selectedList, navigate, onClose: () => setBulkOpen(false), onCommitted: clearSelected }));
  }
  function RepProductDetailView({ navigate, editId, showCost, setShowCost }) {
    const [product, setProduct] = useState(null);
    const [skus, setSkus] = useState([]);
    const [media, setMedia] = useState([]);
    const [loading, setLoading] = useState(true);
    const [skuLoading, setSkuLoading] = useState(false);
    const [selectedImage, setSelectedImage] = useState(null);
    const [activeSku, setActiveSku] = useState(null);
    const [accessories, setAccessories] = useState([]);
    const [variantSkus, setVariantSkus] = useState(null);
    const loadedPidRef = useRef(null);
    const loadSku = useCallback(() => {
      if (!editId) return;
      setSkuLoading(true);
      repFetch("/api/rep/skus/" + editId).then((sd) => {
        setActiveSku(sd.sku);
        setAccessories(sd.accessories || []);
        const skuMedia = (sd.media || []).filter((m) => m.asset_type !== "spec_pdf");
        setMedia(skuMedia);
        setSelectedImage((skuMedia.find((m) => m.asset_type === "primary") || skuMedia[0] || {}).url || null);
        const cur = {
          sku_id: sd.sku.sku_id,
          variant_name: sd.sku.variant_name,
          attributes: sd.sku.attributes || [],
          primary_image: (skuMedia[0] || {}).url || null,
          stock_status: sd.sku.stock_status,
          qty_on_hand: sd.sku.qty_on_hand,
          sell_by: sd.sku.sell_by
        };
        const sibs = (sd.siblings || []).map((s) => ({
          sku_id: s.sku_id,
          variant_name: s.variant_name,
          attributes: s.attributes || [],
          primary_image: s.primary_image || null,
          stock_status: s.stock_status,
          qty_on_hand: s.qty_on_hand,
          sell_by: s.sell_by
        }));
        setVariantSkus([cur, ...sibs]);
        const pid = sd.sku.product_id;
        if (loadedPidRef.current === pid) {
          setSkuLoading(false);
          setLoading(false);
          return null;
        }
        return repFetch("/api/rep/products/" + pid).then((d) => {
          loadedPidRef.current = pid;
          setProduct(d.product);
          setSkus(d.skus || []);
        }).finally(() => {
          setSkuLoading(false);
          setLoading(false);
        });
      }).catch((err) => {
        if (err && err.status === 404) {
          repFetch("/api/rep/products/" + editId).then((d) => {
            const first = (d.skus || []).find((s) => !s.is_sample) || (d.skus || [])[0];
            if (first) {
              navigate("product-detail", first.id, { replace: true });
            } else {
              setProduct(d.product || null);
              setSkus(d.skus || []);
              setLoading(false);
              setSkuLoading(false);
            }
          }).catch(() => {
            setLoading(false);
            setSkuLoading(false);
          });
        } else {
          setLoading(false);
          setSkuLoading(false);
        }
      });
    }, [editId, navigate]);
    useEffect(loadSku, [loadSku]);
    useEffect(() => {
      const onFocus = () => loadSku();
      window.addEventListener("focus", onFocus);
      return () => window.removeEventListener("focus", onFocus);
    }, [loadSku]);
    const variantSelectors = useMemo(() => {
      if (!variantSkus) return null;
      const cur = variantSkus.find((s) => s.sku_id === editId);
      if (!cur) return null;
      return repBuildVariantSelectors(cur, variantSkus.filter((s) => s.sku_id !== editId));
    }, [variantSkus, editId]);
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading product...");
    if (!product) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Product not found");
    const mainSku = activeSku && activeSku.sku_id === editId ? activeSku : skus.find((s) => s.id === editId) || skus.find((s) => !s.is_sample) || skus[0];
    const showVariants = !!(variantSelectors && (variantSelectors.colorItems.length >= 2 || variantSelectors.sizeItems.length >= 2 || variantSelectors.attrPills.length > 0));
    const selectVariant = (skuId) => {
      if (skuId && skuId !== editId) navigate("product-detail", skuId);
    };
    const vpLabel = { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--brass-muted)" };
    const retailPrice = mainSku && mainSku.retail_price ? parseFloat(mainSku.retail_price) : null;
    const costPrice = mainSku && mainSku.cost ? parseFloat(mainSku.cost) : null;
    const marginPct = mainSku && mainSku.margin_pct ? mainSku.margin_pct : null;
    const sellBy = mainSku ? mainSku.sell_by : "sqft";
    const priceUnit = sellBy === "unit" ? "/ea" : sellBy === "roll" ? "/yd\xB2" : "/ft\xB2";
    const sqftPerBox = mainSku && mainSku.sqft_per_box ? parseFloat(mainSku.sqft_per_box) : null;
    const weightPerBox = mainSku && mainSku.weight_per_box_lbs ? parseFloat(mainSku.weight_per_box_lbs) : null;
    const inStockCount = skus.filter((s) => s.stock_status === "in_stock" && !s.is_sample).length;
    const totalSkuCount = skus.filter((s) => !s.is_sample).length;
    const stockColor = inStockCount === totalSkuCount ? "#3a7a4e" : inStockCount > 0 ? "#a87935" : "#c54731";
    const stockLabel = inStockCount === totalSkuCount ? "Active" : inStockCount > 0 ? "Partial stock" : "Out of stock";
    const activeStock = mainSku ? stockDisplay({ ...mainSku, vendor_has_inventory: mainSku.vendor_has_inventory != null ? mainSku.vendor_has_inventory : product.vendor_has_inventory }) : { l: "\u2014", c: "#8a7e68" };
    const titleName = (mainSku && mainSku.display_name || product.display_name || product.name || "").trim();
    const titleColl = (product.collection || "").trim();
    let titleNode;
    if (titleColl && titleName.toLowerCase().startsWith(titleColl.toLowerCase() + " ")) {
      titleNode = /* @__PURE__ */ React.createElement(React.Fragment, null, titleColl, " ", /* @__PURE__ */ React.createElement("em", null, titleName.slice(titleColl.length).trim()));
    } else {
      titleNode = titleName || titleColl;
    }
    const pricingRows = [];
    if (retailPrice) {
      const TRADE_TIERS = [
        { label: "Retail \xB7 walk-in", pct: 0 },
        { label: "Silver", pct: 12.5 },
        { label: "Gold", pct: 18.75, highlight: true },
        { label: "Platinum", pct: 21.875 }
      ];
      TRADE_TIERS.forEach((t) => {
        const price = retailPrice * (1 - t.pct / 100);
        pricingRows.push({
          label: t.label,
          mult: t.pct ? "\u2212" + t.pct + "%" : "list",
          price: "$" + price.toFixed(2),
          margin: marginPct ? ((1 - (costPrice || 0) / price) * 100).toFixed(0) + "%" : "\u2014",
          highlight: t.highlight
        });
      });
      if (showCost && costPrice) {
        pricingRows.push({ label: "Floor \xB7 vendor cost", mult: (costPrice / retailPrice * 100).toFixed(0) + "%", price: "$" + costPrice.toFixed(2), margin: "0%", muted: true });
      }
    }
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-breadcrumb" }, /* @__PURE__ */ React.createElement("div", { className: "pd-breadcrumb-trail" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("catalog") }, "Catalog"), /* @__PURE__ */ React.createElement("span", { className: "pd-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("catalog") }, product.category_name || "Products"), /* @__PURE__ */ React.createElement("span", { className: "pd-sep" }), /* @__PURE__ */ React.createElement("span", { className: "pd-current" }, mainSku ? mainSku.internal_sku : product.name)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 10 } }, /* @__PURE__ */ React.createElement("span", { className: "pd-breadcrumb-status", style: { color: activeStock.c } }, "\u25CF", " ", activeStock.l, activeStock.q ? " \xB7 " + activeStock.q : ""), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8 } }, /* @__PURE__ */ React.createElement("button", { className: "td-btn primary", onClick: () => navigate("quote-create") }, "+ Add to quote"), /* @__PURE__ */ React.createElement("button", { className: "td-btn", onClick: () => navigate("order-create") }, "+ Add to order")))), /* @__PURE__ */ React.createElement("div", { className: "pd-header" }, /* @__PURE__ */ React.createElement("div", { className: "pd-image" }, selectedImage ? /* @__PURE__ */ React.createElement(
      "img",
      {
        src: optimizeImg(selectedImage, 800),
        alt: product.name,
        style: { opacity: skuLoading ? 0.55 : 1, transition: "opacity 0.15s" }
      }
    ) : /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)", font: "400 13px/1 Inter, sans-serif" } }, "No image"), mainSku && /* @__PURE__ */ React.createElement("div", { className: "pd-image-sku" }, mainSku.internal_sku)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-meta-tag" }, "\u25CF", " ", product.category_name || "Product", " ", "\xB7", " ", product.vendor_name, product.vendor_public_code ? " \xB7 " + product.vendor_public_code : ""), /* @__PURE__ */ React.createElement("h1", { className: "pd-title" }, titleNode), /* @__PURE__ */ React.createElement("div", { className: "pd-description" }, product.description_short || product.description || ""))), media.length > 1 && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, marginBottom: 22, overflowX: "auto", paddingBottom: 4 } }, media.map((m) => /* @__PURE__ */ React.createElement(
      "img",
      {
        key: m.id,
        src: optimizeImg(m.url, 120),
        alt: "",
        style: {
          width: 64,
          height: 64,
          objectFit: "cover",
          cursor: "pointer",
          flexShrink: 0,
          border: m.url === selectedImage ? "2px solid var(--gold)" : "0.5px solid rgba(28,25,23,0.13)"
        },
        onClick: () => setSelectedImage(m.url),
        loading: "lazy"
      }
    ))), showVariants && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 18, marginBottom: 22 } }, variantSelectors.colorItems.length >= 2 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", marginBottom: 10 } }, /* @__PURE__ */ React.createElement("div", { style: vpLabel }, "Color"), /* @__PURE__ */ React.createElement("div", { style: vpLabel }, variantSelectors.colorItems.length, " colors")), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(84px, 1fr))", gap: 10 } }, variantSelectors.colorItems.map((v) => {
      const isActive = v.is_current;
      const st = v.stock_status ? stockDisplay({ ...v, vendor_has_inventory: product.vendor_has_inventory }) : null;
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          key: v.sku_id,
          onClick: () => selectVariant(v.sku_id),
          title: [v.label, st && st.l].filter(Boolean).join(" \xB7 "),
          style: { display: "flex", flexDirection: "column", gap: 6, padding: 0, cursor: isActive ? "default" : "pointer", background: "transparent", border: "none", textAlign: "left" }
        },
        /* @__PURE__ */ React.createElement("div", { style: {
          position: "relative",
          width: "100%",
          aspectRatio: "1 / 1",
          overflow: "hidden",
          border: isActive ? "2px solid var(--stone-900)" : "1px solid rgba(28,25,23,0.15)",
          outline: isActive ? "2px solid var(--stone-900)" : "none",
          outlineOffset: 2,
          background: "var(--stone-100)"
        } }, v.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(v.primary_image, 160), alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", justifyContent: "center", width: "100%", height: "100%", font: "400 10px/1.2 Inter, sans-serif", color: "var(--brass-muted)", textAlign: "center", padding: 4 } }, v.label), st && /* @__PURE__ */ React.createElement("span", { title: st.l, style: { position: "absolute", top: 5, right: 5, width: 9, height: 9, borderRadius: "50%", background: st.c, boxShadow: "0 0 0 2px #fff" } })),
        /* @__PURE__ */ React.createElement("span", { style: { font: (isActive ? "600" : "400") + " 11px/1.25 Inter, sans-serif", color: isActive ? "var(--stone-900)" : "var(--brass-muted)", display: "-webkit-box", WebkitLineClamp: 2, WebkitBoxOrient: "vertical", overflow: "hidden" } }, v.label)
      );
    }))), [
      ...variantSelectors.sizeItems.length >= 2 ? [{ slug: "_size", name: "Size", items: variantSelectors.sizeItems.map((s) => ({ value: s.label, sku_id: s.sku_id, is_current: s.is_current })) }] : [],
      ...variantSelectors.attrPills
    ].map((group) => /* @__PURE__ */ React.createElement("div", { key: group.slug }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", marginBottom: 10 } }, /* @__PURE__ */ React.createElement("div", { style: vpLabel }, group.name), /* @__PURE__ */ React.createElement("div", { style: vpLabel }, (group.items.find((i) => i.is_current) || {}).value || "")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 8 } }, group.items.map((it) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: it.value,
        onClick: () => selectVariant(it.sku_id),
        disabled: !it.sku_id,
        style: {
          padding: "7px 13px",
          font: "500 12px/1 Inter, sans-serif",
          border: it.is_current ? "1.5px solid var(--stone-900)" : "0.5px solid rgba(28,25,23,0.25)",
          background: it.is_current ? "var(--stone-900)" : "transparent",
          color: it.is_current ? "#fff" : "var(--stone-900)",
          cursor: it.is_current || !it.sku_id ? "default" : "pointer",
          opacity: it.sku_id ? 1 : 0.4
        }
      },
      it.value
    )))))), /* @__PURE__ */ React.createElement("div", { className: "pd-stats-strip" }, costPrice && showCost && /* @__PURE__ */ React.createElement("div", { className: "pd-stat-cell" }, /* @__PURE__ */ React.createElement("div", { className: "pd-stat-label" }, "Vendor cost"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-value" }, "$", costPrice.toFixed(2)), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-sub" }, "per ", sellBy === "unit" ? "unit" : sellBy === "roll" ? "yd\xB2" : "ft\xB2")), retailPrice && /* @__PURE__ */ React.createElement("div", { className: "pd-stat-cell" }, /* @__PURE__ */ React.createElement("div", { className: "pd-stat-label" }, "List ", "\xB7", " retail"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-value" }, "$", retailPrice.toFixed(2)), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-sub" }, priceUnit)), sqftPerBox && /* @__PURE__ */ React.createElement("div", { className: "pd-stat-cell" }, /* @__PURE__ */ React.createElement("div", { className: "pd-stat-label" }, "Coverage"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-value" }, sqftPerBox.toFixed(1), " ft", "\xB2"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-sub" }, "per box/carton")), weightPerBox && /* @__PURE__ */ React.createElement("div", { className: "pd-stat-cell" }, /* @__PURE__ */ React.createElement("div", { className: "pd-stat-label" }, "Weight"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-value" }, weightPerBox.toFixed(1), " lbs"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-sub" }, "per box")), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-cell" }, /* @__PURE__ */ React.createElement("div", { className: "pd-stat-label" }, "SKUs"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-value" }, totalSkuCount), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-sub" }, inStockCount, " in stock")), marginPct && showCost && /* @__PURE__ */ React.createElement("div", { className: "pd-stat-cell" }, /* @__PURE__ */ React.createElement("div", { className: "pd-stat-label" }, "Avg margin"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-value" }, marginPct.toFixed(1), "%"), /* @__PURE__ */ React.createElement("div", { className: "pd-stat-sub" }, "At retail price"))), /* @__PURE__ */ React.createElement("div", { className: "pd-two-col" }, pricingRows.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "td-panel", style: { order: 2 } }, /* @__PURE__ */ React.createElement("div", { className: "td-panel-header" }, /* @__PURE__ */ React.createElement("h3", null, "Pricing ", "\xB7", " per channel"), /* @__PURE__ */ React.createElement("label", { className: "cost-toggle", onClick: () => setShowCost((s) => !s), style: { cursor: "pointer" } }, /* @__PURE__ */ React.createElement("div", { className: "cost-toggle-track" + (showCost ? " active" : "") }), "Show cost")), pricingRows.map((p, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "pd-pricing-row" + (p.highlight ? " highlight" : "") }, /* @__PURE__ */ React.createElement("span", { className: "pd-pricing-label", style: { color: p.muted ? "var(--brass-muted)" : "var(--stone-900)" } }, p.label), /* @__PURE__ */ React.createElement("span", { className: "pd-pricing-mult" }, p.mult), /* @__PURE__ */ React.createElement("span", { className: "pd-pricing-price", style: { color: p.muted ? "var(--brass-muted)" : "var(--stone-900)" } }, p.price), /* @__PURE__ */ React.createElement("span", { className: "pd-pricing-margin", style: {
      color: p.muted ? "var(--brass-muted)" : p.highlight ? "var(--gold)" : "#3a7a4e"
    } }, p.margin)))), /* @__PURE__ */ React.createElement("div", { className: "td-panel", style: { order: 1 } }, /* @__PURE__ */ React.createElement("div", { className: "td-panel-header" }, /* @__PURE__ */ React.createElement("h3", null, "Specs + details")), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-grid" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, "Collection"), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value" }, product.collection || "\u2014")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, "Vendor"), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value" }, product.vendor_name, product.vendor_public_code ? " \xB7 " + product.vendor_public_code : "")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, "Category"), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value" }, product.category_name || "\u2014")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, "Sell by"), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value" }, sellBy === "unit" ? "Per unit" : sellBy === "roll" ? "Per sq yd (roll)" : "Per sq ft")), mainSku && mainSku.pieces_per_box && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, "Pieces / box"), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value" }, mainSku.pieces_per_box)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, "Status"), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value", style: { color: stockColor } }, "\u25CF", " ", product.status)), (mainSku && mainSku.attributes || []).filter((a) => a.value && String(a.value).trim() && !["color_code", "style_code", "upc", "price_list", "material_class", "collection", "brand", "subcategory", "msrp", "group_number", "top_ref_sku", "sink_ref_sku", "optional_accessories"].includes(a.slug)).map((a) => /* @__PURE__ */ React.createElement("div", { key: a.slug }, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, a.name), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value" }, a.value))), product.description && product.description !== product.description_short && /* @__PURE__ */ React.createElement("div", { className: "pd-spec-wide" }, /* @__PURE__ */ React.createElement("div", { className: "pd-spec-label" }, "Full description"), /* @__PURE__ */ React.createElement("div", { className: "pd-spec-value" }, product.description))))), accessories.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "td-panel", style: { marginBottom: 16 } }, /* @__PURE__ */ React.createElement("div", { className: "td-panel-header" }, /* @__PURE__ */ React.createElement("h3", null, "Accessories ", "\xB7", " complete the install"), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)" } }, accessories.length, " suggested")), accessories.map((a, i) => /* @__PURE__ */ React.createElement("div", { key: a.sku_id, style: { display: "grid", gridTemplateColumns: "44px 1fr auto", gap: 14, alignItems: "center", padding: "11px 22px", borderTop: i ? "0.5px solid rgba(28,25,23,0.07)" : "none" } }, a.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(a.primary_image, 88), alt: "", style: { width: 44, height: 44, objectFit: "cover", border: "0.5px solid rgba(28,25,23,0.06)" } }) : /* @__PURE__ */ React.createElement("div", { style: { width: 44, height: 44, background: "var(--stone-200)", border: "0.5px solid rgba(28,25,23,0.06)" } }), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.2 Inter, sans-serif", color: "var(--stone-900)" } }, a.accessory_label || a.variant_name || "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.3 Inter, sans-serif", color: "var(--brass-muted)", marginTop: 2 } }, a.sell_by === "box" ? "per sqft" : "per unit")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: '300 18px/1 "Cormorant Garamond", serif', color: "var(--stone-900)" } }, a.retail_price ? "$" + parseFloat(a.retail_price).toFixed(2) : "\u2014"), showCost && a.cost && /* @__PURE__ */ React.createElement("div", { style: { font: "400 10px/1 Inter, sans-serif", color: "var(--brass-muted)", marginTop: 3 } }, "cost $", parseFloat(a.cost).toFixed(2)))))), /* @__PURE__ */ React.createElement("div", { className: "td-panel", style: { marginBottom: 16 } }, /* @__PURE__ */ React.createElement("div", { className: "td-panel-header" }, /* @__PURE__ */ React.createElement("h3", null, "SKU inventory ", "\xB7", " ", skus.length, " total ", "\xB7", " ", inStockCount, " in stock")), skus.map((s, i) => {
      const detSk = stockDisplay({ ...s, vendor_has_inventory: product.vendor_has_inventory, sell_by: s.sell_by });
      const sColor = detSk.c;
      const sLabel = detSk.l;
      return /* @__PURE__ */ React.createElement("div", { key: s.id, onClick: () => !s.is_sample && selectVariant(s.id), style: {
        padding: "12px 22px",
        borderBottom: i < skus.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none",
        display: "grid",
        gridTemplateColumns: "1.2fr 120px 120px 100px 100px 100px",
        gap: 14,
        alignItems: "center",
        background: s.id === editId ? "var(--stone-100)" : "transparent",
        cursor: s.is_sample ? "default" : "pointer"
      } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.2 Inter, sans-serif", color: "var(--stone-900)" } }, s.variant_name || "Default", s.is_sample && /* @__PURE__ */ React.createElement("span", { style: { marginLeft: 8, padding: "2px 6px", background: "rgba(28,25,23,0.05)", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)" } }, "Sample")), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.3 ui-monospace, monospace", letterSpacing: "0.06em", color: "var(--brass-muted)", marginTop: 3 } }, s.internal_sku)), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.06em", color: "var(--brass-muted)" } }, s.vendor_sku), /* @__PURE__ */ React.createElement("span", { style: { font: '300 18px/1 "Cormorant Garamond", serif', color: "var(--stone-900)" } }, s.retail_price ? "$" + parseFloat(s.retail_price).toFixed(2) : "\u2014"), showCost ? /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 Inter, sans-serif", color: "var(--brass-muted)" } }, s.cost ? "$" + parseFloat(s.cost).toFixed(2) : "\u2014") : /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: sColor, textTransform: "uppercase" } }, "\u25CF", " ", sLabel), /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--stone-900)", textAlign: "right" } }, s.qty_on_hand != null ? s.qty_on_hand + " qty" : "\u2014"));
    }), skus.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center", color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif" } }, "No SKUs found")));
  }
  const ROF_STEPS = [
    { num: "01", label: "Order", sub: "Customer + items + fulfillment", key: "order" },
    { num: "02", label: "Payment", sub: "Collect & process", key: "payment" },
    { num: "03", label: "Send POs", sub: "Vendor transmission", key: "pos" },
    { num: "04", label: "Done", sub: "Receipt & next steps", key: "done" }
  ];
  function RofPayCardModal({ amount, customerName, customerEmail, orderNumber, tenderIdx, tenderCount, onSuccess, onCancel, terminalRef, terminalStatus, connectReader }) {
    const [phase, setPhase] = useState("ready");
    const [procStep, setProcStep] = useState(0);
    const timers = useRef([]);
    const [cardLast4, setCardLast4] = useState("");
    const [cardBrand, setCardBrand] = useState("CARD");
    const [piId, setPiId] = useState("");
    const [declineReason, setDeclineReason] = useState("");
    const [chargeError, setChargeError] = useState("");
    const [savedCards, setSavedCards] = useState(null);
    const [stripeCustomerId, setStripeCustomerId] = useState(null);
    const [selectedPm, setSelectedPm] = useState(null);
    const [cardComplete, setCardComplete] = useState(false);
    const [cardError, setCardError] = useState("");
    const [saveNewCard, setSaveNewCard] = useState(true);
    const cardElRef = useRef(null);
    const mountRef = useRef(null);
    const lastInputTab = useRef("newcard");
    const valorTxnRef = useRef(null);
    const chargeIdemRef = useRef(null);
    const takeChargeIdemKey = () => {
      if (!chargeIdemRef.current) {
        chargeIdemRef.current = "rp" + Date.now().toString(36) + Math.random().toString(36).slice(2, 14);
      }
      return chargeIdemRef.current;
    };
    useEffect(() => () => timers.current.forEach(clearTimeout), []);
    useEffect(() => {
      let cancelled = false;
      if (!customerEmail) {
        setSavedCards([]);
        setPhase((p) => p === "ready" ? "newcard" : p);
        return;
      }
      repFetch("/api/rep/customers/payment-methods?email=" + encodeURIComponent(customerEmail)).then((d) => {
        if (cancelled) return;
        setSavedCards(d.cards || []);
        setStripeCustomerId(d.stripe_customer_id || null);
        if (d.cards && d.cards.length) setSelectedPm(d.cards[0].id);
        else setPhase((p) => p === "ready" ? "newcard" : p);
      }).catch(() => {
        if (cancelled) return;
        setSavedCards([]);
        setPhase((p) => p === "ready" ? "newcard" : p);
      });
      return () => {
        cancelled = true;
      };
    }, [customerEmail]);
    useEffect(() => {
      if (phase !== "newcard" || !stripeInstance || !mountRef.current) return;
      const elements = stripeInstance.elements();
      const card = elements.create("card", {
        disableLink: true,
        style: {
          base: {
            fontSize: "15px",
            fontFamily: "'Inter', sans-serif",
            color: "#1c1917",
            "::placeholder": { color: "#8a7e68" }
          },
          invalid: { color: "#b3402e" }
        },
        hidePostalCode: false
      });
      card.mount(mountRef.current);
      card.on("change", (event) => {
        setCardComplete(event.complete);
        setCardError(event.error ? event.error.message : "");
        if (event.brand && event.brand !== "unknown") setCardBrand(event.brand.toUpperCase());
      });
      cardElRef.current = card;
      return () => {
        card.destroy();
        cardElRef.current = null;
        setCardComplete(false);
      };
    }, [phase]);
    const payMoney = (n) => "$" + parseFloat(n).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const PROC_STEPS = lastInputTab.current === "terminal" ? [
      { l: "Sending to terminal", m: "Valor Connect \xB7 secure" },
      { l: "Waiting for card", m: "tap \xB7 insert \xB7 swipe" },
      { l: "Authorizing " + payMoney(amount), m: "card network" }
    ] : [
      { l: "Contacting Stripe", m: "TLS \xB7 tokenizing card" },
      { l: "Authorizing " + payMoney(amount), m: "Card network \xB7 AVS + CVC" },
      { l: "Capturing funds", m: "auth \u2192 capture" }
    ];
    const chargeDescription = orderNumber ? "Charge for " + orderNumber : "In-store card charge";
    const chargeSaved = async () => {
      const card = (savedCards || []).find((c) => c.id === selectedPm);
      if (!card || !stripeCustomerId) return;
      lastInputTab.current = "ready";
      setCardBrand((card.brand || "card").toUpperCase());
      setCardLast4(card.last4 || "");
      setPhase("processing");
      setProcStep(0);
      setChargeError("");
      timers.current.push(setTimeout(() => setProcStep(1), 700));
      try {
        const r = await repFetch("/api/rep/card/charge-saved", {
          method: "POST",
          body: JSON.stringify({
            amount: parseFloat(amount),
            description: chargeDescription,
            stripe_customer_id: stripeCustomerId,
            payment_method_id: card.id,
            idempotency_key: takeChargeIdemKey()
          })
        });
        chargeIdemRef.current = null;
        setProcStep(2);
        if (r.brand) setCardBrand(r.brand.toUpperCase());
        if (r.last4) setCardLast4(r.last4);
        setPiId(r.payment_intent_id);
        setPhase("approved");
      } catch (err) {
        if (err && err.status) chargeIdemRef.current = null;
        setDeclineReason(err.message || "Charge failed");
        setPhase("declined");
      }
    };
    const chargeNewCard = async () => {
      if (!stripeInstance || !cardElRef.current || !cardComplete) return;
      lastInputTab.current = "newcard";
      setChargeError("");
      let pmId;
      try {
        const pmResult = await stripeInstance.createPaymentMethod({ type: "card", card: cardElRef.current });
        if (pmResult.error) {
          setCardError(pmResult.error.message);
          return;
        }
        pmId = pmResult.paymentMethod.id;
        if (pmResult.paymentMethod.card) {
          setCardBrand((pmResult.paymentMethod.card.brand || "card").toUpperCase());
          setCardLast4(pmResult.paymentMethod.card.last4 || "");
        }
      } catch (err) {
        setCardError(err.message || "Card tokenization failed");
        return;
      }
      setPhase("processing");
      setProcStep(0);
      try {
        const wantSave = !!(saveNewCard && customerEmail);
        const r = await repFetch("/api/rep/card/create-payment-intent", {
          method: "POST",
          body: JSON.stringify({
            amount: parseFloat(amount),
            order_description: chargeDescription,
            customer_email: wantSave ? customerEmail : void 0,
            save_card: wantSave
          })
        });
        if (!r.client_secret) throw new Error(r.error || "Failed to create payment");
        setProcStep(1);
        const result = await stripeInstance.confirmCardPayment(r.client_secret, { payment_method: pmId });
        if (result.error) throw new Error(result.error.message);
        setProcStep(2);
        setPiId(result.paymentIntent.id);
        setPhase("approved");
      } catch (err) {
        setDeclineReason(err.message || "Payment failed");
        setPhase("declined");
      }
    };
    const chargeTerminal = async () => {
      lastInputTab.current = "terminal";
      setCardBrand("CARD");
      setCardLast4("");
      setPhase("processing");
      setProcStep(1);
      setChargeError("");
      valorTxnRef.current = null;
      try {
        const txn = await valorTerminalCharge({ amount, orderRef: orderNumber || void 0 });
        setProcStep(2);
        valorTxnRef.current = txn;
        if (txn.brand) setCardBrand(txn.brand.toUpperCase());
        setCardLast4((txn.masked_pan || "").replace(/\D/g, "").slice(-4) || "****");
        setPiId(txn.tran_no ? "Valor " + txn.tran_no : "");
        setPhase("approved");
      } catch (err) {
        setDeclineReason(err.message || "Terminal payment failed");
        setPhase("declined");
      }
    };
    const handleCharge = () => {
      if (phase === "terminal") {
        chargeTerminal();
        return;
      }
      if (phase === "ready") {
        chargeSaved();
        return;
      }
      chargeNewCard();
    };
    const handleDone = () => {
      if (valorTxnRef.current) onSuccess({ card_txn: valorTxnRef.current });
      else if (piId) onSuccess({ payment_intent_id: piId });
      else onCancel();
    };
    if (phase === "processing") {
      const pct = Math.round((procStep + 0.5) / PROC_STEPS.length * 100);
      return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay" }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-pop" }, /* @__PURE__ */ React.createElement("div", { style: { background: "#1c1917", color: "#ece5d8", padding: "24px 24px 0" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#a87935", marginBottom: 12, display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { className: "rof-pulse", style: { width: 7, height: 7, borderRadius: "50%", background: "#a87935" } }), "Authorizing"), /* @__PURE__ */ React.createElement("div", { style: { font: '300 38px/1 "Cormorant Garamond", serif', letterSpacing: "-0.015em" } }, payMoney(amount)), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", color: "rgba(236,229,216,0.6)", textTransform: "uppercase", marginTop: 6 } }, cardBrand, " ", "\u2022\u2022\u2022\u2022", " ", cardLast4 || "****"), /* @__PURE__ */ React.createElement("div", { style: { height: 3, background: "rgba(236,229,216,0.12)", marginTop: 18 } }, /* @__PURE__ */ React.createElement("div", { style: { height: "100%", width: pct + "%", background: "#a87935", transition: "width .6s cubic-bezier(.4,0,.2,1)" } }))), /* @__PURE__ */ React.createElement("div", { style: { padding: "8px 0 10px" } }, PROC_STEPS.map((s, i) => {
        const st = i < procStep ? "done" : i === procStep ? "running" : "queued";
        return /* @__PURE__ */ React.createElement("div", { key: i, style: { padding: "12px 24px", display: "grid", gridTemplateColumns: "20px 1fr", gap: 13, alignItems: "center", background: st === "running" ? "rgba(216,205,182,0.53)" : "transparent", opacity: st === "queued" ? 0.5 : 1, transition: "opacity .3s, background .3s" } }, st === "done" ? /* @__PURE__ */ React.createElement("span", { style: { width: 20, height: 20, borderRadius: "50%", background: "#3a7a4e", color: "#fff", display: "flex", alignItems: "center", justifyContent: "center", font: "500 11px/1 Inter, sans-serif" } }, "\u2713") : st === "running" ? /* @__PURE__ */ React.createElement("span", { className: "rof-spin", style: { width: 20, height: 20, borderRadius: "50%", border: "2px solid rgba(168,121,53,0.33)", borderTopColor: "#a87935" } }) : /* @__PURE__ */ React.createElement("span", { style: { width: 20, height: 20, borderRadius: "50%", border: "1.5px solid rgba(28,25,23,0.2)" } }), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 Inter, sans-serif", color: "#1c1917" } }, s.l), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.13em", color: "#8a7e68", textTransform: "uppercase", marginTop: 3 } }, st === "running" ? "Working\u2026" : s.m)));
      })), /* @__PURE__ */ React.createElement("div", { style: { padding: "13px 24px", borderTop: "0.5px solid rgba(28,25,23,0.09)", background: "#d8cdb6", display: "flex", alignItems: "center", gap: 10, font: "500 10px/1.4 ui-monospace, monospace", letterSpacing: "0.13em", color: "#8a7e68", textTransform: "uppercase" } }, /* @__PURE__ */ React.createElement("span", { className: "rof-spin", style: { width: 13, height: 13, borderRadius: "50%", border: "2px solid rgba(28,25,23,0.2)", borderTopColor: "#1c1917" } }), "Do not close ", "\u2014", " authorizing with the network")));
    }
    if (phase === "approved") {
      const rows = [
        ["Method", lastInputTab.current === "ready" ? "Card on file" : lastInputTab.current === "terminal" ? "Valor terminal" : "Keyed entry"],
        ["Network", (cardBrand || "CARD") + " \u2022\u2022\u2022\u2022 " + (cardLast4 || "****")],
        ["Status", "Captured"],
        ["Transaction", piId ? piId.slice(0, 24) : "\u2014"]
      ];
      return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay" }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-pop" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "30px 24px 20px", textAlign: "center", borderBottom: "0.5px solid rgba(28,25,23,0.09)", position: "relative" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-pop-check", style: { width: 50, height: 50, borderRadius: "50%", background: "#3a7a4e", color: "#fff", display: "flex", alignItems: "center", justifyContent: "center", font: "500 23px/1 Inter, sans-serif", margin: "0 auto 14px" } }, "\u2713"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#3a7a4e", marginBottom: 9 } }, "Approved"), /* @__PURE__ */ React.createElement("div", { style: { font: '300 32px/1 "Cormorant Garamond", serif', letterSpacing: "-0.015em", color: "#1c1917" } }, payMoney(amount), " captured"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.5 Inter, sans-serif", color: "#8a7e68", marginTop: 7 } }, "Charged to ", cardBrand, " ", "\u2022\u2022\u2022\u2022", " ", cardLast4 || "****", tenderCount > 1 ? " \xB7 Tender " + tenderIdx + " of " + tenderCount : "")), /* @__PURE__ */ React.createElement("div", { style: { padding: "6px 24px 4px" } }, rows.map(([l, v], i) => /* @__PURE__ */ React.createElement("div", { key: l, style: { display: "flex", justifyContent: "space-between", padding: "11px 0", borderBottom: i < rows.length - 1 ? "0.5px solid rgba(28,25,23,0.05)" : "none" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, l), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 ui-monospace, monospace", color: "#1c1917" } }, v)))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 24px", borderTop: "0.5px solid rgba(28,25,23,0.09)", display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { marginRight: "auto" } }), /* @__PURE__ */ React.createElement("button", { onClick: handleDone, style: { padding: "12px 22px", background: "#a87935", color: "#ece5d8", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", border: "none" } }, "Done"))));
    }
    if (phase === "declined") {
      return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay" }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-pop" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "30px 24px 20px", textAlign: "center", borderBottom: "0.5px solid rgba(28,25,23,0.09)", position: "relative" } }, /* @__PURE__ */ React.createElement("button", { onClick: onCancel, style: { position: "absolute", top: 14, right: 16, width: 26, height: 26, border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", color: "#1c1917", cursor: "pointer", font: "400 13px/1 Inter, sans-serif" } }, "\u2715"), /* @__PURE__ */ React.createElement("div", { style: { width: 50, height: 50, borderRadius: "50%", background: "#b3402e", color: "#fff", display: "flex", alignItems: "center", justifyContent: "center", font: "500 23px/1 Inter, sans-serif", margin: "0 auto 14px" } }, "\u2715"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#b3402e", marginBottom: 9 } }, "Declined"), /* @__PURE__ */ React.createElement("div", { style: { font: '300 30px/1 "Cormorant Garamond", serif', letterSpacing: "-0.015em", color: "#1c1917" } }, "Charge not approved")), /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 24px" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 14px", background: "rgba(179,64,46,0.07)", marginBottom: 6 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8f3022", marginBottom: 5 } }, "Reason"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "#8f3022" } }, /* @__PURE__ */ React.createElement("strong", null, declineReason || "Card declined"), " ", "\u2014", " the issuing bank declined this charge. No funds were captured. Try another card or use a different tender."))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 24px", borderTop: "0.5px solid rgba(28,25,23,0.09)", display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("button", { onClick: onCancel, style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68", cursor: "pointer", background: "none", border: "none", marginRight: "auto" } }, "\u2190", " Back"), /* @__PURE__ */ React.createElement("button", { onClick: () => setPhase(lastInputTab.current === "ready" && savedCards && savedCards.length ? "ready" : lastInputTab.current === "terminal" ? "terminal" : "newcard"), style: { padding: "12px 22px", background: "#1c1917", color: "#ece5d8", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", border: "none" } }, "\u21BB", " Try again"))));
    }
    const tabs = [
      { id: "ready", l: "Card on file" },
      { id: "newcard", l: "New card" },
      { id: "terminal", l: "Valor terminal" }
    ];
    return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay" }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-pop" }, /* @__PURE__ */ React.createElement("div", { style: { background: "#1c1917", color: "#ece5d8", padding: "20px 24px", position: "relative" } }, /* @__PURE__ */ React.createElement("button", { onClick: onCancel, style: { position: "absolute", top: 16, right: 18, width: 26, height: 26, border: "0.5px solid rgba(236,229,216,0.27)", background: "transparent", color: "#ece5d8", cursor: "pointer", font: "400 13px/1 Inter, sans-serif" } }, "\u2715"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#a87935", marginBottom: 12, display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { className: "rof-pulse", style: { width: 7, height: 7, borderRadius: "50%", background: "#a87935" } }), "Card payment ", "\xB7", " Stripe"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("div", { style: { font: '300 38px/1 "Cormorant Garamond", serif', letterSpacing: "-0.015em" } }, payMoney(amount)), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right", font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", color: "rgba(236,229,216,0.6)", textTransform: "uppercase" } }, orderNumber || "New order", tenderCount > 1 && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("br", null), "Tender ", tenderIdx, " of ", tenderCount)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", borderBottom: "0.5px solid rgba(28,25,23,0.1)" } }, tabs.map((t) => {
      const on = phase === t.id;
      return /* @__PURE__ */ React.createElement("button", { key: t.id, onClick: () => setPhase(t.id), style: { flex: 1, padding: "13px 8px", background: "transparent", border: "none", borderBottom: on ? "2px solid #a87935" : "2px solid transparent", marginBottom: -0.5, cursor: "pointer", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: on ? "#1c1917" : "#8a7e68" } }, t.l);
    })), /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 24px" } }, phase === "ready" && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 14 } }, savedCards === null && /* @__PURE__ */ React.createElement("div", { style: { padding: "28px 0", textAlign: "center", font: "500 10px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, "Checking for cards on file", "\u2026"), savedCards && savedCards.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 16px", textAlign: "center", border: "0.5px dashed rgba(28,25,23,0.3)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 15px/1.3 "Cormorant Garamond", serif', color: "#1c1917" } }, "No card on file", customerEmail ? "" : " \u2014 no customer email"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.5 Inter, sans-serif", color: "#8a7e68", marginTop: 6 } }, customerEmail ? "Charge a new card with \u201CSave card\u201D checked and it will appear here next time." : "Select a customer with an email to use saved cards.")), savedCards && savedCards.length > 0 && (() => {
      const sel = savedCards.find((c) => c.id === selectedPm) || savedCards[0];
      const expStr = String(sel.exp_month).padStart(2, "0") + " / " + String(sel.exp_year).slice(-2);
      return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { style: { position: "relative", padding: "18px 20px", borderRadius: 10, background: "linear-gradient(135deg, #2b2723 0%, #3a342d 100%)", color: "#f0ead9", boxShadow: "0 8px 22px rgba(28,25,23,0.2)", overflow: "hidden" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "flex-start" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 22, borderRadius: 4, background: "linear-gradient(135deg,#d8b878,#b8923f)" } }), /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "center", justifyContent: "center", padding: "3px 7px", borderRadius: 3, background: "#1a1f71", color: "#fff", font: "700 11px/1 Inter, sans-serif", letterSpacing: "0.06em", fontStyle: "italic" } }, (sel.brand || "CARD").toUpperCase())), /* @__PURE__ */ React.createElement("div", { style: { font: '400 19px/1 "Cormorant Garamond", serif', letterSpacing: "0.14em", marginTop: 18 } }, "\u2022\u2022\u2022\u2022", "  ", "\u2022\u2022\u2022\u2022", "  ", "\u2022\u2022\u2022\u2022", "  ", sel.last4), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", marginTop: 14, font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: "rgba(240,234,217,0.67)", textTransform: "uppercase" } }, /* @__PURE__ */ React.createElement("span", null, customerName), /* @__PURE__ */ React.createElement("span", null, "Exp ", expStr)), savedCards[0].id === sel.id && /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", top: 14, right: 70, font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.14em", color: "#a87935", textTransform: "uppercase" } }, "Default")), savedCards.length > 1 && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 6 } }, savedCards.map((c, i) => {
        const on = c.id === sel.id;
        return /* @__PURE__ */ React.createElement("button", { key: c.id, onClick: () => setSelectedPm(c.id), style: { padding: "10px 13px", display: "flex", alignItems: "center", gap: 10, cursor: "pointer", textAlign: "left", background: on ? "#d8cdb6" : "transparent", border: "0.5px solid " + (on ? "#1c1917" : "rgba(28,25,23,0.2)") } }, /* @__PURE__ */ React.createElement("span", { style: { width: 13, height: 13, borderRadius: "50%", flexShrink: 0, border: "1px solid " + (on ? "#1c1917" : "rgba(28,25,23,0.35)"), background: on ? "#1c1917" : "transparent", display: "flex", alignItems: "center", justifyContent: "center" } }, on && /* @__PURE__ */ React.createElement("span", { style: { width: 5, height: 5, borderRadius: "50%", background: "#ece5d8" } })), /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "#1c1917" } }, (c.brand || "card").toUpperCase(), " ", "\u2022\u2022\u2022\u2022", " ", c.last4), /* @__PURE__ */ React.createElement("span", { style: { marginLeft: "auto", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.1em", color: "#8a7e68", textTransform: "uppercase" } }, String(c.exp_month).padStart(2, "0"), "/", String(c.exp_year).slice(-2), i === 0 ? " \xB7 default" : ""));
      })), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 7, font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", color: "#8a7e68", textTransform: "uppercase" } }, "\u{1F512}", " Charged off-session ", "\xB7", " card data held by Stripe ", "\xB7", " never touches Roma"));
    })()), phase === "newcard" && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 14 } }, /* @__PURE__ */ React.createElement("label", null, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 7, display: "block" } }, "Card details"), /* @__PURE__ */ React.createElement("div", { style: { padding: "13px", border: "0.5px solid rgba(28,25,23,0.25)", background: "#f6f1e7", borderRadius: 4 } }, /* @__PURE__ */ React.createElement("div", { ref: mountRef }))), !stripeInstance && /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.4 Inter, sans-serif", color: "#b3402e" } }, "Stripe failed to load \\u2014 refresh and try again, or use the terminal."), cardError && /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.4 Inter, sans-serif", color: "#b3402e" } }, cardError), customerEmail && /* @__PURE__ */ React.createElement("label", { style: { display: "flex", alignItems: "center", gap: 10, cursor: "pointer" } }, /* @__PURE__ */ React.createElement("span", { onClick: (e) => {
      e.preventDefault();
      setSaveNewCard((v) => !v);
    }, style: { width: 16, height: 16, borderRadius: 3, flexShrink: 0, background: saveNewCard ? "#a87935" : "transparent", border: saveNewCard ? "none" : "1px solid rgba(28,25,23,0.35)", color: "#ece5d8", display: "flex", alignItems: "center", justifyContent: "center", font: "500 10px/1 Inter, sans-serif" } }, saveNewCard ? "\u2713" : ""), /* @__PURE__ */ React.createElement("span", { onClick: (e) => {
      e.preventDefault();
      setSaveNewCard((v) => !v);
    }, style: { font: "400 12px/1.4 Inter, sans-serif", color: "rgba(28,25,23,0.8)" } }, "Save card to ", customerEmail, " for future charges")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 7, font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", color: "#8a7e68", textTransform: "uppercase" } }, "\u{1F512}", " PCI-compliant ", "\xB7", " card data tokenized by Stripe ", "\xB7", " never touches Roma")), phase === "terminal" && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 18, justifyItems: "center", textAlign: "center", padding: "6px 0 2px" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 132, height: 180, borderRadius: 16, background: "linear-gradient(160deg,#26221e,#3b342c)", boxShadow: "0 14px 30px rgba(28,25,23,0.2)", padding: 12, display: "flex", flexDirection: "column", alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { width: "100%", height: 84, borderRadius: 8, background: "#0d0c0b", display: "flex", alignItems: "center", justifyContent: "center", flexDirection: "column", gap: 6 } }, /* @__PURE__ */ React.createElement("span", { className: "rof-pulse", style: { font: '300 22px/1 "Cormorant Garamond", serif', color: "#f0ead9" } }, payMoney(amount)), /* @__PURE__ */ React.createElement("span", { style: { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.14em", color: "#a87935", textTransform: "uppercase" } }, "Tap ", "\xB7", " Insert ", "\xB7", " Swipe")), /* @__PURE__ */ React.createElement("div", { style: { marginTop: "auto", width: "70%", height: 5, borderRadius: 3, background: "#0d0c0b" } })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: '400 16px/1.3 "Cormorant Garamond", serif', color: "#1c1917" } }, "Charge on the Valor terminal"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.5 Inter, sans-serif", color: "#8a7e68", marginTop: 4 } }, "Press Charge, then have the customer tap, insert, or swipe on the terminal.")), /* @__PURE__ */ React.createElement("div", { style: { display: "inline-flex", alignItems: "center", gap: 8, padding: "7px 13px", background: "#d8cdb6", borderRadius: 20, font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: terminalStatus === "connected" ? "#3a7a4e" : "#8a7e68" } }, /* @__PURE__ */ React.createElement("span", { style: { width: 7, height: 7, borderRadius: "50%", background: terminalStatus === "connected" ? "#3a7a4e" : "#8a7e68" } }), terminalStatus === "connected" ? "Valor terminal ready" : "Terminal connecting\u2026"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 24px", borderTop: "0.5px solid rgba(28,25,23,0.09)", display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("button", { onClick: onCancel, style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68", cursor: "pointer", background: "none", border: "none", marginRight: "auto" } }, "Cancel"), phase === "terminal" ? /* @__PURE__ */ React.createElement("button", { onClick: chargeTerminal, style: { padding: "12px 22px", background: "#a87935", color: "#ece5d8", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", border: "none" } }, "Charge ", payMoney(amount), " on terminal ", "\u2192") : (() => {
      const chargeDisabled = phase === "ready" ? !(savedCards && savedCards.length && selectedPm) : !(stripeInstance && cardComplete);
      return /* @__PURE__ */ React.createElement("button", { onClick: handleCharge, disabled: chargeDisabled, style: { padding: "12px 22px", background: "#a87935", color: "#ece5d8", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: chargeDisabled ? "default" : "pointer", border: "none", opacity: chargeDisabled ? 0.45 : 1 } }, "Charge ", payMoney(amount), " ", "\u2192");
    })())));
  }
  function RofProcessOrderModal({ data, setData, onComplete, onCancel }) {
    const [statuses, setStatuses] = useState([]);
    const [phase, setPhase] = useState("running");
    const [errorMsg, setErrorMsg] = useState("");
    const [createdOrder, setCreatedOrder] = useState(null);
    const [transmissionLog, setTransmissionLog] = useState([]);
    const timers = useRef([]);
    const payMoney = (n) => "$" + parseFloat(n).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const customerName = data.customer ? data.customer.name : data.newCustomerName || "";
    const customerEmail = data.customer ? data.customer.email : data.newCustomerEmail || "";
    const phone = data.customer ? data.customer.phone : data.newCustomerPhone || "";
    const companyName = data.customer ? data.customer.company_name || "" : data.newCustomerCompany || "";
    const itemCount = (data.items || []).length;
    const vendorCount = new Set(
      (data.items || []).filter((i) => !i.is_sample && (i.vendor_id || i.custom_vendor)).map((i) => i.vendor_id || "oneoff:" + String(i.custom_vendor || "").trim().toLowerCase())
    ).size;
    const itemsSubtotal = (data.items || []).reduce((s, i) => s + i.subtotal, 0);
    const discount = data.promoResult ? data.promoResult.discount_amount : 0;
    const taxableSub = Math.max(0, itemsSubtotal - discount);
    const destZip = data.deliveryMethod !== "shipping" ? "92806" : data.shippingAddress && data.shippingAddress.zip ? data.shippingAddress.zip : "92806";
    const taxRate = caTaxRate(destZip);
    const estTax = parseFloat((itemsSubtotal * taxRate).toFixed(2));
    const modalShipping = data.deliveryMethod === "shipping" ? parseFloat(data.estShipping) || 0 : 0;
    const total = taxableSub + estTax + modalShipping;
    const paymentMethods = data.paymentMethods || [];
    const inStoreTenders = paymentMethods.filter((pm) => pm.method !== "stripe");
    const primaryTender = inStoreTenders[0] || paymentMethods[0] || null;
    const capturedNow = inStoreTenders.reduce((s, p) => s + (parseFloat(p.amount) || 0), 0);
    const linkPending = paymentMethods.filter((p) => p.method === "stripe").reduce((s, p) => s + (parseFloat(p.amount) || 0), 0);
    const balanceRemaining = Math.max(0, parseFloat((total - capturedNow - linkPending).toFixed(2)));
    const tasks = useMemo(() => {
      const t = [];
      paymentMethods.forEach((pm, i) => {
        if (pm.method === "card") t.push({ key: "card-" + i, l: "Capture card payment", m: pm.card_txn ? "Valor terminal" : "Stripe \xB7 2.9% + 30\xA2", amt: payMoney(pm.amount), pay: true });
        else if (pm.method === "ach") t.push({ key: "ach-" + i, l: "Process ACH transfer", m: "Bank transfer \xB7 4\u20135 days", amt: payMoney(pm.amount), pay: true });
        else if (pm.method === "cash") t.push({ key: "cash-" + i, l: "Log cash payment", m: "Cash drawer", amt: payMoney(pm.amount), pay: true });
        else if (pm.method === "check") t.push({ key: "check-" + i, l: "Log check #" + (pm.checkNumber || ""), m: "AP \xB7 clears in 3 days", amt: payMoney(pm.amount), pay: true });
        else if (pm.method === "stripe") t.push({ key: "stripe-" + i, l: "Send payment request", m: "Email link to customer", amt: payMoney(pm.amount), pay: true });
      });
      t.push({ key: "invoice", l: "Issue invoice", m: "PDF generated", amt: null });
      t.push({ key: "receipt", l: "Email confirmation + receipt", m: customerEmail, amt: null });
      t.push({ key: "state", l: "Move order to processed", m: vendorCount > 0 ? vendorCount + " vendor PO" + (vendorCount !== 1 ? "s" : "") + " queued \xB7 step 3" : "No vendor POs needed", amt: null });
      if (data.deliveryMethod === "shipping") t.push({ key: "freight", l: "Schedule freight", m: "Delivery to " + (data.shippingAddress ? data.shippingAddress.city : ""), amt: null });
      return t;
    }, []);
    useEffect(() => {
      setStatuses(tasks.map(() => "queued"));
      runProcess();
      return () => timers.current.forEach(clearTimeout);
    }, []);
    const runProcess = async () => {
      const log = [];
      const now = () => (/* @__PURE__ */ new Date()).toLocaleTimeString("en-US", { hour: "2-digit", minute: "2-digit", second: "2-digit" });
      const setTaskStatus = (idx, status) => setStatuses((s) => {
        const n = s.slice();
        n[idx] = status;
        return n;
      });
      try {
        let taskIdx = 0;
        for (const pm of paymentMethods) {
          setTaskStatus(taskIdx, "running");
          await new Promise((r) => setTimeout(r, 400));
          log.push({ ok: true, message: (pm.method === "card" ? "Card" : pm.method === "check" ? "Check #" + (pm.checkNumber || "") : pm.method.toUpperCase()) + " payment: " + payMoney(pm.amount), time: now() });
          setTaskStatus(taskIdx, "done");
          await new Promise((r) => setTimeout(r, 200));
          taskIdx++;
        }
        setTaskStatus(taskIdx, "running");
        await new Promise((r) => setTimeout(r, 300));
        let result;
        if (data.createdOrder && data.createdOrder.id) {
          const oid = data.createdOrder.id;
          const toAttach = paymentMethods.filter((pm) => ["cash", "card", "check"].includes(pm.method) && !pm.persisted);
          for (const pm of toAttach) {
            try {
              await repFetch("/api/rep/orders/" + oid + "/collect-payment", {
                method: "POST",
                body: JSON.stringify({ payment_method: pm.method, amount: pm.amount, card_txn: pm.card_txn || void 0, stripe_payment_intent_id: pm.stripePaymentIntentId || void 0, check_number: pm.checkNumber || void 0, defer_confirm: true })
              });
              log.push({ ok: true, message: "Attached " + payMoney(pm.amount) + " " + pm.method, time: now() });
            } catch (e) {
              log.push({ ok: false, message: "Attach payment failed: " + e.message, time: now() });
            }
          }
          result = await repFetch("/api/rep/orders/" + oid + "/finalize", { method: "POST", body: JSON.stringify({}) });
          if (!result.order) throw new Error(result.error || "Order finalize failed");
        } else {
          const body = buildRepOrderBody(data, primaryTender);
          result = await repFetch("/api/rep/orders", { method: "POST", body: JSON.stringify(body) });
          if (!result.order) throw new Error(result.error || "Order creation failed");
        }
        log.push({ ok: true, message: "Invoice " + (result.order.order_number || "#" + result.order.id) + " issued", time: now() });
        setTaskStatus(taskIdx, "done");
        taskIdx++;
        await new Promise((r) => setTimeout(r, 200));
        setTaskStatus(taskIdx, "running");
        await new Promise((r) => setTimeout(r, 500));
        log.push({ ok: true, message: "Confirmation email queued to " + customerEmail, time: now() });
        setTaskStatus(taskIdx, "done");
        taskIdx++;
        await new Promise((r) => setTimeout(r, 200));
        const additionalPayments = data.createdOrder && data.createdOrder.id ? [] : inStoreTenders.filter((pm) => pm !== primaryTender);
        for (const pm of additionalPayments) {
          try {
            await repFetch("/api/rep/orders/" + result.order.id + "/collect-payment", {
              method: "POST",
              body: JSON.stringify({
                payment_method: pm.method,
                amount: pm.amount,
                card_txn: pm.card_txn || void 0,
                stripe_payment_intent_id: pm.stripePaymentIntentId || void 0,
                check_number: pm.checkNumber || void 0
              })
            });
            log.push({ ok: true, message: "Split payment: " + payMoney(pm.amount) + " " + pm.method, time: now() });
          } catch (e) {
            log.push({ ok: false, message: "Split payment failed: " + e.message, time: now() });
          }
        }
        if (paymentMethods.some((pm) => pm.method === "stripe")) {
          try {
            await repFetch("/api/rep/orders/" + result.order.id + "/payment-request", {
              method: "POST",
              body: JSON.stringify({})
            });
            log.push({ ok: true, message: "Payment link + invoice PDF emailed to " + customerEmail, time: now() });
          } catch (e) {
            log.push({ ok: false, message: "Payment link failed: " + e.message, time: now() });
          }
        }
        setTaskStatus(taskIdx, "running");
        await new Promise((r) => setTimeout(r, 400));
        if ((data.items || []).some((i) => i.sku_id || i.vendor_id || i.custom_vendor)) {
          log.push({ ok: true, message: "Draft purchase orders created", time: now() });
        }
        log.push({ ok: true, message: "Order " + (result.order.order_number || "#" + result.order.id) + " processed", time: now() });
        setTaskStatus(taskIdx, "done");
        taskIdx++;
        await new Promise((r) => setTimeout(r, 200));
        if (taskIdx < tasks.length) {
          setTaskStatus(taskIdx, "running");
          await new Promise((r) => setTimeout(r, 400));
          log.push({ ok: true, message: "Freight scheduled", time: now() });
          setTaskStatus(taskIdx, "done");
          taskIdx++;
          await new Promise((r) => setTimeout(r, 200));
        }
        setCreatedOrder(result.order);
        setTransmissionLog(log);
        setPhase("done");
      } catch (err) {
        log.push({ ok: false, message: err.message || "Processing failed", time: now() });
        setTransmissionLog(log);
        setErrorMsg(err.message || "Order processing failed");
        setStatuses((s) => s.map((st) => st === "running" ? "failed" : st));
        setPhase("error");
      }
    };
    const handleContinue = () => {
      if (createdOrder) {
        setData((d) => ({ ...d, createdOrder, transmissionLog }));
        onComplete();
      }
    };
    const doneCount = statuses.filter((s) => s === "done").length;
    const pct = tasks.length > 0 ? Math.round(doneCount / tasks.length * 100) : 0;
    return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay" }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-modal-wide rof-pop" }, phase === "done" ? /* @__PURE__ */ React.createElement("div", { style: { padding: "30px 26px 22px", borderBottom: "0.5px solid rgba(28,25,23,0.09)", textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-pop-check", style: { width: 52, height: 52, borderRadius: "50%", background: "#3a7a4e", color: "#fff", display: "flex", alignItems: "center", justifyContent: "center", font: "500 24px/1 Inter, sans-serif", margin: "0 auto 16px" } }, "\u2713"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#3a7a4e", marginBottom: 10 } }, "Order processed ", "\xB7", " live"), /* @__PURE__ */ React.createElement("h2", { style: { margin: 0, font: '300 30px/1.1 "Cormorant Garamond", serif', letterSpacing: "-0.014em", color: "#1c1917" } }, createdOrder ? createdOrder.order_number || "R-" + createdOrder.id : "Order", " is committed."), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "#8a7e68", marginTop: 8, maxWidth: 440, marginLeft: "auto", marginRight: "auto" } }, /* @__PURE__ */ React.createElement("strong", { style: { color: "#1c1917" } }, payMoney(capturedNow)), " captured", balanceRemaining > 0.01 ? /* @__PURE__ */ React.createElement(React.Fragment, null, " ", "\xB7", " ", /* @__PURE__ */ React.createElement("strong", { style: { color: "#a87935" } }, payMoney(balanceRemaining)), " balance due") : "", " \xB7", " invoice emailed to ", customerName.split(" ")[0], vendorCount > 0 ? " \xB7 " + vendorCount + " vendor PO" + (vendorCount !== 1 ? "s" : "") + " queued for step 3" : "", ".")) : /* @__PURE__ */ React.createElement("div", { style: { background: "#1c1917", color: "#ece5d8", padding: "22px 26px 0" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "flex-start" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: phase === "error" ? "#b3402e" : "#a87935", marginBottom: 9, display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { className: phase === "error" ? "" : "rof-pulse", style: { width: 7, height: 7, borderRadius: "50%", background: phase === "error" ? "#b3402e" : "#a87935" } }), phase === "error" ? "Payment declined" : "Processing order"), /* @__PURE__ */ React.createElement("div", { style: { font: '300 26px/1.05 "Cormorant Garamond", serif', letterSpacing: "-0.012em" } }, "New Order"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", color: "rgba(236,229,216,0.6)", textTransform: "uppercase", marginTop: 6 } }, customerName, " ", "\xB7", " ", itemCount, " line", itemCount !== 1 ? "s" : "", " ", "\xB7", " ", vendorCount, " PO", vendorCount !== 1 ? "s" : "", " queued")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "rgba(236,229,216,0.47)", marginBottom: 6 } }, phase === "error" ? "Nothing finalized" : balanceRemaining > 0.01 ? "Capturing deposit" : "Capturing"), /* @__PURE__ */ React.createElement("div", { style: { font: '300 30px/1 "Cormorant Garamond", serif', letterSpacing: "-0.014em" } }, payMoney(capturedNow)))), /* @__PURE__ */ React.createElement("div", { style: { height: 3, background: "rgba(236,229,216,0.12)", marginTop: 20 } }, /* @__PURE__ */ React.createElement("div", { style: { height: "100%", width: (phase === "error" ? 8 : pct) + "%", background: phase === "error" ? "#b3402e" : "#a87935", transition: "width .5s cubic-bezier(.4,0,.2,1)" } }))), /* @__PURE__ */ React.createElement("div", { style: { overflowY: "auto", padding: "6px 0" } }, tasks.map((task, i) => {
      const st = statuses[i] || "queued";
      const active = st === "running";
      return /* @__PURE__ */ React.createElement("div", { key: task.key, style: { padding: "11px 26px", display: "grid", gridTemplateColumns: "20px 1fr auto", gap: 14, alignItems: "center", background: active ? "rgba(216,205,182,0.53)" : "transparent", opacity: st === "queued" ? 0.5 : 1, transition: "opacity .3s, background .3s", borderBottom: i < tasks.length - 1 ? "0.5px solid rgba(28,25,23,0.05)" : "none" } }, st === "done" ? /* @__PURE__ */ React.createElement("span", { style: { width: 20, height: 20, borderRadius: "50%", background: "#3a7a4e", color: "#fff", display: "flex", alignItems: "center", justifyContent: "center", font: "500 11px/1 Inter, sans-serif" } }, "\u2713") : st === "failed" ? /* @__PURE__ */ React.createElement("span", { style: { width: 20, height: 20, borderRadius: "50%", background: "#b3402e", color: "#fff", display: "flex", alignItems: "center", justifyContent: "center", font: "500 11px/1 Inter, sans-serif" } }, "\u2715") : st === "running" ? /* @__PURE__ */ React.createElement("span", { className: "rof-spin", style: { width: 20, height: 20, borderRadius: "50%", border: "2px solid rgba(168,121,53,0.33)", borderTopColor: "#a87935" } }) : /* @__PURE__ */ React.createElement("span", { style: { width: 20, height: 20, borderRadius: "50%", border: "1.5px solid rgba(28,25,23,0.2)" } }), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 Inter, sans-serif", color: st === "failed" ? "#b3402e" : "#1c1917" } }, task.l), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.13em", color: "#8a7e68", textTransform: "uppercase", marginTop: 3 } }, st === "failed" ? "Failed" : st === "running" ? "Working\u2026" : task.m)), task.amt && /* @__PURE__ */ React.createElement("div", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: st === "failed" ? "#b3402e" : st === "done" ? "#1c1917" : "#8a7e68", letterSpacing: "-0.01em" } }, task.amt));
    })), phase === "running" && /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 26px", borderTop: "0.5px solid rgba(28,25,23,0.09)", background: "#d8cdb6", display: "flex", alignItems: "center", gap: 10, font: "500 10px/1.4 ui-monospace, monospace", letterSpacing: "0.13em", color: "#8a7e68", textTransform: "uppercase" } }, /* @__PURE__ */ React.createElement("span", { className: "rof-spin", style: { width: 13, height: 13, borderRadius: "50%", border: "2px solid rgba(28,25,23,0.2)", borderTopColor: "#1c1917" } }), "Working ", "\u2014", " keep this window open. Vendor POs do not send yet."), phase === "done" && /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 26px", borderTop: "0.5px solid rgba(28,25,23,0.09)", display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { marginRight: "auto" } }), /* @__PURE__ */ React.createElement("button", { onClick: handleContinue, style: { padding: "12px 22px", background: "#a87935", color: "#ece5d8", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", border: "none" } }, "Continue to send POs ", "\u2192")), phase === "error" && /* @__PURE__ */ React.createElement("div", { style: { borderTop: "0.5px solid rgba(28,25,23,0.09)" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 26px", background: "rgba(179,64,46,0.07)", font: "400 12px/1.5 Inter, sans-serif", color: "#8f3022" } }, /* @__PURE__ */ React.createElement("strong", null, "Processing failed ", "\u2014", " nothing was finalized."), " ", /* @__PURE__ */ React.createElement("span", { style: { color: "#8a7e68" } }, errorMsg || "No invoice was issued and the order is still a draft. Try again or edit the payment method.")), /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 26px", display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { marginRight: "auto" } }), /* @__PURE__ */ React.createElement("button", { onClick: onCancel, style: { padding: "12px 18px", border: "0.5px solid rgba(28,25,23,0.2)", color: "#1c1917", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", background: "transparent" } }, "Edit payment"), /* @__PURE__ */ React.createElement("button", { onClick: onCancel, style: { padding: "12px 22px", background: "#1c1917", color: "#ece5d8", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", border: "none" } }, "\u21BB", " Close")))));
  }
  function materialFace(kind, tone) {
    const T = tone || {};
    const A = T.a || "#c8b094";
    const B = T.b || "#7a6850";
    const C = T.c || "#3a3127";
    switch ((kind || "").toLowerCase()) {
      case "wood":
      case "hardwood":
      case "laminate":
        return {
          background: `repeating-linear-gradient(92deg, ${A} 0 7px, ${B} 7px 9px, ${A} 9px 22px, ${C}55 22px 23px, ${A} 23px 41px, ${B} 41px 43px), linear-gradient(180deg, ${A}, ${B})`,
          backgroundBlendMode: "multiply"
        };
      case "marble":
        return {
          background: `radial-gradient(120% 80% at 30% 20%, ${A} 0%, ${A} 30%, ${B}88 55%, ${A} 70%), radial-gradient(80% 60% at 70% 80%, ${C}55, transparent 60%), linear-gradient(135deg, ${A}, ${B}44)`
        };
      case "tile":
      case "porcelain":
      case "ceramic":
        return {
          background: `repeating-linear-gradient(0deg, ${C}22 0 0.5px, transparent 0.5px 60px), repeating-linear-gradient(90deg, ${C}22 0 0.5px, transparent 0.5px 60px), radial-gradient(60% 80% at 40% 30%, ${A}, ${B})`
        };
      case "stone":
      case "travertine":
      case "slate":
        return {
          background: `radial-gradient(40% 60% at 25% 30%, ${A}, ${B} 80%), radial-gradient(30% 30% at 70% 60%, ${A}88, transparent), radial-gradient(20% 20% at 50% 80%, ${C}55, transparent), ${B}`
        };
      case "lvp":
      case "lvt":
      case "vinyl":
      case "spc":
        return {
          background: `repeating-linear-gradient(95deg, ${A} 0 14px, ${B}77 14px 15px, ${A} 15px 30px), linear-gradient(180deg, ${A}, ${B})`,
          backgroundBlendMode: "multiply"
        };
      case "quartz":
        return {
          background: `radial-gradient(2px 2px at 20% 30%, ${C}55, transparent), radial-gradient(2px 2px at 60% 20%, ${C}55, transparent), radial-gradient(3px 3px at 80% 60%, ${C}77, transparent), radial-gradient(2px 2px at 30% 70%, ${C}55, transparent), radial-gradient(2px 2px at 50% 50%, ${C}55, transparent), linear-gradient(135deg, ${A}, ${B}44)`
        };
      case "carpet":
        return {
          background: `repeating-linear-gradient(90deg, ${A} 0 2px, ${B}44 2px 3px, ${A} 3px 5px), repeating-linear-gradient(0deg, ${A} 0 2px, ${B}33 2px 3px, ${A} 3px 5px), linear-gradient(135deg, ${A}, ${B})`,
          backgroundBlendMode: "multiply"
        };
      default:
        return { background: A };
    }
  }
  function WizRepCard({ title, actions, children, style }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rof-card", style }, title && /* @__PURE__ */ React.createElement("div", { className: "rof-card-header" }, /* @__PURE__ */ React.createElement("span", { className: "rof-card-title" }, title), actions && /* @__PURE__ */ React.createElement("div", { className: "rof-card-actions" }, actions)), /* @__PURE__ */ React.createElement("div", { className: "rof-card-body" }, children));
  }
  function RofStepRail({ step, setStep }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rof-rail" }, ROF_STEPS.map((s, i) => {
      const stepNum = i + 1;
      const isActive = step === stepNum;
      const isCompleted = step > stepNum;
      const canJump = isCompleted;
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: s.key,
          className: "rof-rail-step" + (isActive ? " active" : "") + (isCompleted ? " completed" : "") + (!isActive && !isCompleted ? " future" : ""),
          onClick: () => canJump ? setStep(stepNum) : null
        },
        /* @__PURE__ */ React.createElement("span", { className: "rof-rail-num" }, isCompleted ? "\u2713" : s.num),
        /* @__PURE__ */ React.createElement("div", { className: "rof-rail-text" }, /* @__PURE__ */ React.createElement("div", { className: "rof-rail-label" }, s.label), /* @__PURE__ */ React.createElement("div", { className: "rof-rail-sub" }, s.sub)),
        /* @__PURE__ */ React.createElement("span", { className: "rof-rail-chev" }, isCompleted ? "\u2713" : "\u203A")
      );
    }));
  }
  function RofActionBar({ step, setStep, canContinue, onProcess, processing, navigate, total, orderNumber, itemCount, customerName, deliveryMethod }) {
    if (step === 4) return null;
    const labels = { 1: "Continue to Payment", 2: "Process Order", 3: "Continue" };
    const nextLabel = labels[step] || "Continue";
    const context = [
      customerName,
      itemCount > 0 ? itemCount + " line" + (itemCount !== 1 ? "s" : "") : null,
      deliveryMethod ? deliveryMethod === "pickup" ? "Pickup" : deliveryMethod === "will_call" ? "Will-call" : "Freight" : null
    ].filter(Boolean).join(" \xB7 ");
    return /* @__PURE__ */ React.createElement("div", { className: "rof-action-bar" }, /* @__PURE__ */ React.createElement("div", { className: "rof-action-left" }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)" } }, orderNumber || "New order", " ", "\xB7", " Step ", step, " of 4"), /* @__PURE__ */ React.createElement("span", { className: "rof-action-total", style: { display: "block", marginTop: 4 } }, "$", total.toFixed(2), /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.4375rem/1 ui-monospace, monospace", opacity: 0.5, marginLeft: "0.3rem" } }, "incl. tax"))), context && /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "rgba(245,240,232,0.6)", marginLeft: "1rem" } }, context)), /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => navigate("orders") }, "Save & Close"), step > 1 && step < 4 && /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => setStep(step - 1) }, "Back"), step === 1 && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", disabled: !canContinue, onClick: () => setStep(2) }, nextLabel), step === 2 && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", disabled: !canContinue || processing, onClick: onProcess }, processing ? "Processing..." : nextLabel), step === 3 && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: () => setStep(4) }, nextLabel));
  }
  function RofSectionAnchors({ active, onJump }) {
    const anchors = [
      { id: "customer", label: "01a Customer" },
      { id: "items", label: "01b Items" },
      { id: "fulfillment", label: "01c Fulfillment" }
    ];
    return /* @__PURE__ */ React.createElement("div", { className: "rof-anchors" }, anchors.map((a) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: a.id,
        className: "rof-anchor" + (active === a.id ? " active" : ""),
        onClick: () => onJump(a.id)
      },
      /* @__PURE__ */ React.createElement("span", { className: "rof-anchor-dot" }, "\u2022"),
      a.label
    )));
  }
  function RofSectionHeader({ num, label, id }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rof-section-hdr", id: "rof-section-" + id }, /* @__PURE__ */ React.createElement("span", { className: "rof-section-num" }, num), /* @__PURE__ */ React.createElement("span", { className: "rof-section-label" }, label));
  }
  function RofCustomerSection({ data, setData }) {
    const [search, setSearch] = useState("");
    const [results, setResults] = useState([]);
    const [searching, setSearching] = useState(false);
    const searchTimer = useRef(null);
    const [emailError, setEmailError] = useState("");
    const [phoneError, setPhoneError] = useState("");
    const [customerMatch, setCustomerMatch] = useState(null);
    const customerMode = data.customerMode || "search";
    const setCustomerMode = (m) => setData((d) => ({ ...d, customerMode: m }));
    const doSearch = (q) => {
      setSearch(q);
      if (searchTimer.current) clearTimeout(searchTimer.current);
      if (q.trim().length < 2) {
        setResults([]);
        setSearching(false);
        return;
      }
      setSearching(true);
      searchTimer.current = setTimeout(async () => {
        try {
          const d = await repFetch("/api/rep/customers/search?q=" + encodeURIComponent(q.trim()));
          setResults(d.results || []);
        } catch (e) {
          setResults([]);
        }
        setSearching(false);
      }, 300);
    };
    const repriceItems = (items, discount) => {
      return (items || []).map((item) => {
        if (!item.sku_id || !item.retail_price) return item;
        const newPrice = rofTradeUnit(item.retail_price, item.retail_locked, discount);
        const isUnit = item.sell_by === "unit" || item.sell_by === "piece";
        const isCarpet = item.price_basis === "per_sqyd";
        let sub;
        if (isUnit) sub = newPrice * item.num_boxes;
        else if (isCarpet && item.sqft_needed) sub = newPrice * (parseFloat(item.sqft_needed) / 9);
        else if (item.sqft_needed) sub = newPrice * parseFloat(item.sqft_needed);
        else sub = newPrice * item.num_boxes;
        return { ...item, unit_price: newPrice, subtotal: sub };
      });
    };
    const selectCustomer = (c) => {
      const discount = c.type === "trade" ? parseFloat(c.discount_percent || 0) : 0;
      setData((d) => ({
        ...d,
        customer: c,
        items: repriceItems(d.items, discount),
        newCustomerName: "",
        newCustomerFirst: "",
        newCustomerMiddle: "",
        newCustomerLast: "",
        newCustomerEmail: "",
        newCustomerPhone: "",
        newCustomerCompany: "",
        dupMatch: null,
        // Only take the customer's address when they have one — otherwise keep
        // whatever was already entered in the fulfillment form
        shippingAddress: c.address_line1 ? {
          line1: c.address_line1,
          line2: c.address_line2 || "",
          city: c.city || "",
          state: c.state || "",
          zip: c.zip || ""
        } : d.shippingAddress
      }));
      setSearch("");
      setResults([]);
      setCustomerMatch(null);
    };
    const clearCustomer = () => {
      setData((d) => ({ ...d, customer: null, items: repriceItems(d.items, 0), dupMatch: null }));
      setCustomerMatch(null);
      setEmailError("");
      setPhoneError("");
    };
    const checkMatch = async () => {
      if (data.customer) return;
      const email = data.newCustomerEmail || "";
      const phone = data.newCustomerPhone || "";
      const hasEmail = email.trim().length >= 3;
      const hasPhone = phone.replace(/\D/g, "").length >= 10;
      if (!hasEmail && !hasPhone) {
        setCustomerMatch(null);
        setData((d) => ({ ...d, dupMatch: null }));
        return;
      }
      try {
        const r = await repFetch("/api/rep/customers/check-duplicate?email=" + encodeURIComponent(hasEmail ? email : "") + "&phone=" + encodeURIComponent(hasPhone ? phone : ""));
        const m = r.match || null;
        setCustomerMatch(m);
        setData((d) => ({ ...d, dupMatch: m }));
      } catch (e) {
        setCustomerMatch(null);
        setData((d) => ({ ...d, dupMatch: null }));
      }
    };
    return /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Customer",
        actions: data.customer ? /* @__PURE__ */ React.createElement("button", { className: "rof-card-action", onClick: clearCustomer }, "Change") : null
      },
      !data.customer && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light" + (customerMode === "search" ? " primary" : ""),
          onClick: () => {
            setCustomerMode("search");
            setCustomerMatch(null);
          },
          style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
        },
        "Search Existing"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light" + (customerMode === "new" ? " primary" : ""),
          onClick: () => {
            setCustomerMode("new");
            setResults([]);
            setSearch("");
          },
          style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
        },
        "+ New Customer"
      )),
      customerMode === "search" && !data.customer && /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Search"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: search,
          onChange: (e) => doSearch(e.target.value),
          placeholder: "Name, email, or phone..."
        }
      )), searching && results.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "Searching..."), !searching && search.trim().length >= 2 && results.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "No customers found"), results.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, results.map((c, i) => /* @__PURE__ */ React.createElement("div", { key: c.id || i, className: "rof-search-item", onMouseDown: (e) => {
        e.preventDefault();
        selectCustomer(c);
      } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, c.name, c.company_name ? " \u2014 " + c.company_name : ""), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, c.email, c.phone ? " \xB7 " + c.phone : "")), /* @__PURE__ */ React.createElement("span", { className: "rof-search-tag " + (c.type === "trade" ? "trade" : c.type === "order" ? "order" : "new") }, c.type === "order" ? "prev order" : c.type))))),
      data.customer && (() => {
        const c = data.customer;
        const isTrade = c.type === "trade";
        const disc = parseFloat(c.discount_percent || 0);
        const addr = [c.address_line1, [c.city, c.state].filter(Boolean).join(", "), c.zip].filter(Boolean).join(" \xB7 ");
        const initials = (c.name || "?").split(" ").map((w) => w[0]).slice(0, 2).join("").toUpperCase();
        return /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.13)", background: "#ece5d8", padding: "12px 16px 14px" } }, /* @__PURE__ */ React.createElement("div", { className: "qw-identity", style: { marginTop: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, initials), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line", style: { font: "500 13px/1.25 'Inter', sans-serif" } }, c.name, c.company_name ? " \u2014 " + c.company_name : ""), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, [c.email, c.phone, isTrade ? "Trade" + (c.tier_name ? " \xB7 " + c.tier_name : "") : "Retail"].filter(Boolean).join(" \xB7 ")))), addr && /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "#8a7e68", marginTop: 10 } }, addr), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "#8a7e68", marginTop: addr ? 4 : 10 } }, [
          isTrade && disc > 0 ? "Trade \xB7 " + disc + "% off" : isTrade ? "Trade \xB7 list" : "Retail \xB7 list",
          "$" + parseFloat(c.lifetime_value || 0).toLocaleString("en-US", { maximumFractionDigits: 0 }) + " lifetime",
          (c.order_count || 0) + " order" + ((c.order_count || 0) !== 1 ? "s" : ""),
          (c.order_count || 0) > 0 ? "avg $" + (parseFloat(c.lifetime_value || 0) / c.order_count).toLocaleString("en-US", { maximumFractionDigits: 0 }) : null
        ].filter(Boolean).join(" \xB7 ")), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68", marginTop: 10, paddingTop: 8, borderTop: "0.5px solid rgba(28,25,23,0.1)" } }, "\u25CF", " Drives discount, tax & terms on this order"));
      })(),
      customerMode === "new" && customerMatch && !data.customer && /* @__PURE__ */ React.createElement("div", { className: "rof-alert error", style: { display: "flex", alignItems: "center", gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }, "This ", customerMatch.type === "trade" ? "trade account" : "customer", " already exists: ", /* @__PURE__ */ React.createElement("strong", null, customerMatch.name), customerMatch.email ? " (" + customerMatch.email + ")" : "", ". Use their account or change the email/phone to continue."), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light",
          onClick: () => {
            selectCustomer(customerMatch);
            setCustomerMode("search");
          },
          style: { padding: "0.25rem 0.5rem", fontSize: "0.5rem", flexShrink: 0, whiteSpace: "nowrap" }
        },
        "Use their account"
      )),
      customerMode === "new" && !data.customer && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
        CustomerNameFields,
        {
          first: data.newCustomerFirst,
          middle: data.newCustomerMiddle,
          last: data.newCustomerLast,
          onFirst: (v) => setData((d) => ({ ...d, newCustomerFirst: v, newCustomerName: composeCustomerName(v, d.newCustomerMiddle, d.newCustomerLast) })),
          onMiddle: (v) => setData((d) => ({ ...d, newCustomerMiddle: v, newCustomerName: composeCustomerName(d.newCustomerFirst, v, d.newCustomerLast) })),
          onLast: (v) => setData((d) => ({ ...d, newCustomerLast: v, newCustomerName: composeCustomerName(d.newCustomerFirst, d.newCustomerMiddle, v) }))
        }
      ), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Email *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "email",
          value: data.newCustomerEmail || "",
          onChange: (e) => {
            setData((d) => ({ ...d, newCustomerEmail: e.target.value, dupMatch: null }));
            setCustomerMatch(null);
            if (emailError) setEmailError("");
          },
          onBlur: () => {
            if (data.newCustomerEmail && !isValidEmail(data.newCustomerEmail)) setEmailError("Enter a valid email");
            else setEmailError("");
            checkMatch("email");
          },
          placeholder: "email@example.com",
          style: emailError ? { borderColor: "var(--red)" } : {}
        }
      ), emailError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, emailError)), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Phone *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: data.newCustomerPhone || "",
          onChange: (e) => {
            const v = formatPhone(e.target.value);
            setData((d) => ({ ...d, newCustomerPhone: v, dupMatch: null }));
            setCustomerMatch(null);
            if (phoneError) setPhoneError("");
          },
          onBlur: () => {
            if (data.newCustomerPhone && !isValidPhone(data.newCustomerPhone)) setPhoneError("Enter a valid 10-digit phone");
            else setPhoneError("");
            checkMatch("phone");
          },
          placeholder: "(555) 123-4567",
          style: phoneError ? { borderColor: "var(--red)" } : {}
        }
      ), phoneError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, phoneError))), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Company (optional)"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: data.newCustomerCompany || "",
          onChange: (e) => setData((d) => ({ ...d, newCustomerCompany: e.target.value })),
          placeholder: "Company name"
        }
      )))
    );
  }
  function composeCustomerName(first, middle, last) {
    return [first, middle, last].map((s) => (s || "").trim()).filter(Boolean).join(" ");
  }
  function CustomerNameFields({ first, middle, last, onFirst, onMiddle, onLast }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rof-field-row-name" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "First name *"), /* @__PURE__ */ React.createElement("input", { type: "text", value: first || "", onChange: (e) => onFirst(e.target.value), placeholder: "John" })), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "MI"), /* @__PURE__ */ React.createElement("input", { type: "text", maxLength: 4, value: middle || "", onChange: (e) => onMiddle(e.target.value), placeholder: "A" })), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Last name *"), /* @__PURE__ */ React.createElement("input", { type: "text", value: last || "", onChange: (e) => onLast(e.target.value), placeholder: "Smith" })));
  }
  function SidemarkSelect({ value, onChange, customerId, email, disabled, className, style, placeholder }) {
    const [suggestions, setSuggestions] = useState([]);
    const [manual, setManual] = useState(false);
    useEffect(() => {
      let alive = true;
      const t = setTimeout(() => {
        const params = [];
        if (customerId) params.push("customer_id=" + encodeURIComponent(customerId));
        if (email) params.push("email=" + encodeURIComponent(email));
        repFetch("/api/rep/sidemarks" + (params.length ? "?" + params.join("&") : "")).then((d) => {
          if (alive) setSuggestions(Array.isArray(d && d.sidemarks) ? d.sidemarks : []);
        }).catch(() => {
          if (alive) setSuggestions([]);
        });
      }, 300);
      return () => {
        alive = false;
        clearTimeout(t);
      };
    }, [customerId, email]);
    const useInput = manual || suggestions.length === 0;
    if (useInput) {
      return /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          className,
          style,
          maxLength: 200,
          value: value || "",
          disabled,
          placeholder: placeholder || "e.g. Smith Residence \u2014 Master Bath",
          onChange: (e) => onChange(e.target.value)
        }
      ), suggestions.length > 0 && !disabled && /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: () => setManual(false),
          style: { background: "none", border: "none", cursor: "pointer", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.08em", textTransform: "uppercase", color: "var(--rod-muted, #8a7e68)", whiteSpace: "nowrap" }
        },
        "\u21A9 List"
      ));
    }
    const known = value && suggestions.includes(value);
    return /* @__PURE__ */ React.createElement(
      "select",
      {
        className,
        style,
        disabled,
        value: value || "",
        onChange: (e) => {
          if (e.target.value === "__add__") {
            setManual(true);
            onChange("");
          } else onChange(e.target.value);
        }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, "\u2014 None \u2014"),
      value && !known && /* @__PURE__ */ React.createElement("option", { value }, value),
      suggestions.map((s) => /* @__PURE__ */ React.createElement("option", { key: s, value: s }, s)),
      /* @__PURE__ */ React.createElement("option", { value: "__add__" }, "+ Add new\u2026")
    );
  }
  function RepCustomerEntry({ selected, onSelect, onClear, name, email, phone, onName, onEmail, onPhone, requirePhone, company, onCompany }) {
    const [mode, setMode] = useState("search");
    const [search, setSearch] = useState("");
    const [results, setResults] = useState([]);
    const [searching, setSearching] = useState(false);
    const [emailError, setEmailError] = useState("");
    const [phoneError, setPhoneError] = useState("");
    const [match, setMatch] = useState(null);
    const [firstName, setFirstName] = useState("");
    const [middleName, setMiddleName] = useState("");
    const [lastName, setLastName] = useState("");
    const timer = useRef(null);
    const setNamePart = (part, val) => {
      const parts = { first: firstName, middle: middleName, last: lastName, [part]: val };
      if (part === "first") setFirstName(val);
      else if (part === "middle") setMiddleName(val);
      else setLastName(val);
      onName(composeCustomerName(parts.first, parts.middle, parts.last));
    };
    const resetName = () => {
      setFirstName("");
      setMiddleName("");
      setLastName("");
    };
    const doSearch = (q) => {
      setSearch(q);
      if (timer.current) clearTimeout(timer.current);
      if (q.trim().length < 2) {
        setResults([]);
        setSearching(false);
        return;
      }
      setSearching(true);
      timer.current = setTimeout(async () => {
        try {
          const d = await repFetch("/api/rep/customers/search?q=" + encodeURIComponent(q.trim()));
          setResults(d.results || []);
        } catch (e) {
          setResults([]);
        }
        setSearching(false);
      }, 300);
    };
    const pick = (c) => {
      onSelect(c);
      setSearch("");
      setResults([]);
      setMatch(null);
      setEmailError("");
      setPhoneError("");
      resetName();
    };
    const checkMatch = async (val) => {
      if (selected) return;
      if (!val || val.trim().length < 3) {
        setMatch(null);
        return;
      }
      try {
        const r = await repFetch("/api/rep/customers/search?q=" + encodeURIComponent(val));
        setMatch(r.results && r.results.length > 0 ? r.results[0] : null);
      } catch (e) {
        setMatch(null);
      }
    };
    if (selected) {
      const c = selected;
      const initials = (c.name || "?").split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase();
      const missingEmail = !c.email;
      const missingPhone = requirePhone && !c.phone;
      return /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.13)", background: "#ece5d8", padding: "0.875rem 1rem", display: "flex", alignItems: "center", gap: "0.875rem" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 44, height: 44, background: "#d8cdb6", display: "flex", alignItems: "center", justifyContent: "center", font: '400 17px/1 "Cormorant Garamond", serif', color: "#1c1917", flexShrink: 0 } }, initials), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 17px/1.2 "Cormorant Garamond", serif', color: "#1c1917" } }, c.name, c.company_name ? " \u2014 " + c.company_name : ""), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.5 Inter, sans-serif", color: "rgba(28,25,23,0.7)" } }, [email || c.email, phone || c.phone].filter(Boolean).join(" \xB7 ") || "No contact info on file")), /* @__PURE__ */ React.createElement("span", { className: "rof-search-tag " + (c.type === "trade" ? "trade" : c.type === "order" ? "order" : "new") }, c.type === "order" ? "prev order" : c.type || "customer"), /* @__PURE__ */ React.createElement("button", { className: "rof-card-action", onClick: () => {
        onClear();
        setMode("search");
        resetName();
      } }, "Change")), (missingEmail || missingPhone) && /* @__PURE__ */ React.createElement("div", { className: "rof-field-row", style: { marginTop: "0.75rem" } }, missingEmail && /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Email * ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", letterSpacing: 0, color: "#a87935" } }, "\u2014 not on file, please add")), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "email",
          value: email || "",
          onChange: (e) => {
            onEmail(e.target.value);
            if (emailError) setEmailError("");
          },
          onBlur: () => setEmailError(email && !isValidEmail(email) ? "Enter a valid email" : ""),
          placeholder: "email@example.com",
          style: emailError ? { borderColor: "var(--red)" } : {}
        }
      ), emailError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, emailError)), missingPhone && /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Phone * ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", letterSpacing: 0, color: "#a87935" } }, "\u2014 not on file, please add")), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: phone || "",
          onChange: (e) => {
            onPhone(formatPhone(e.target.value));
            if (phoneError) setPhoneError("");
          },
          onBlur: () => setPhoneError(phone && !isValidPhone(phone) ? "Enter a valid 10-digit phone" : ""),
          placeholder: "(555) 123-4567",
          style: phoneError ? { borderColor: "var(--red)" } : {}
        }
      ), phoneError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, phoneError))));
    }
    return /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light" + (mode === "search" ? " primary" : ""),
        onClick: () => {
          setMode("search");
          setMatch(null);
          resetName();
        },
        style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
      },
      "Search Existing"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light" + (mode === "new" ? " primary" : ""),
        onClick: () => {
          setMode("new");
          setResults([]);
          setSearch("");
        },
        style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
      },
      "+ New Customer"
    )), mode === "search" && /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Search"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: search,
        onChange: (e) => doSearch(e.target.value),
        placeholder: "Name, email, or phone..."
      }
    )), searching && !results.length && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "Searching..."), !searching && search.trim().length >= 2 && results.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "No customers found"), results.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, results.map((c, i) => /* @__PURE__ */ React.createElement("div", { key: c.id || i, className: "rof-search-item", onMouseDown: (e) => {
      e.preventDefault();
      pick(c);
    } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, c.name, c.company_name ? " \u2014 " + c.company_name : ""), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, c.email, c.phone ? " \xB7 " + c.phone : "")), /* @__PURE__ */ React.createElement("span", { className: "rof-search-tag " + (c.type === "trade" ? "trade" : c.type === "order" ? "order" : "new") }, c.type === "order" ? "prev order" : c.type))))), mode === "new" && match && /* @__PURE__ */ React.createElement("div", { className: "rof-alert error", style: { display: "flex", alignItems: "center", gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }, "Customer already exists: ", /* @__PURE__ */ React.createElement("strong", null, match.name), " (", match.email, ")"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light",
        onClick: () => {
          pick(match);
          setMode("search");
        },
        style: { padding: "0.25rem 0.5rem", fontSize: "0.5rem", flexShrink: 0, whiteSpace: "nowrap" }
      },
      "Use this customer"
    )), mode === "new" && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
      CustomerNameFields,
      {
        first: firstName,
        middle: middleName,
        last: lastName,
        onFirst: (v) => setNamePart("first", v),
        onMiddle: (v) => setNamePart("middle", v),
        onLast: (v) => setNamePart("last", v)
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Email *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        value: email || "",
        onChange: (e) => {
          onEmail(e.target.value);
          if (emailError) setEmailError("");
        },
        onBlur: () => {
          setEmailError(email && !isValidEmail(email) ? "Enter a valid email" : "");
          checkMatch(email);
        },
        placeholder: "email@example.com",
        style: emailError ? { borderColor: "var(--red)" } : {}
      }
    ), emailError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, emailError)), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, requirePhone ? "Phone *" : "Phone"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: phone || "",
        onChange: (e) => {
          onPhone(formatPhone(e.target.value));
          if (phoneError) setPhoneError("");
        },
        onBlur: () => {
          setPhoneError(phone && !isValidPhone(phone) ? "Enter a valid 10-digit phone" : "");
          checkMatch(phone);
        },
        placeholder: "(555) 123-4567",
        style: phoneError ? { borderColor: "var(--red)" } : {}
      }
    ), phoneError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, phoneError))), onCompany && /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Company ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", letterSpacing: 0, color: "#8a7e68" } }, "(optional)")), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: company || "",
        onChange: (e) => onCompany(e.target.value),
        placeholder: "Company or trade business name"
      }
    ))));
  }
  function rofCatKind(p) {
    const cat = (p.category_name || p.collection || "").toLowerCase();
    if (/wood|hardwood|bamboo/.test(cat)) return "wood";
    if (/marble/.test(cat)) return "marble";
    if (/tile|porcelain|ceramic/.test(cat)) return "tile";
    if (/stone|travertine|slate/.test(cat)) return "stone";
    if (/lvp|vinyl|laminate|spc/.test(cat)) return "lvp";
    if (/quartz/.test(cat)) return "quartz";
    if (/carpet/.test(cat)) return "carpet";
    return "tile";
  }
  function rofCatExt(p, qty, price) {
    const sellBy = p.sell_by || "box";
    if (sellBy === "unit" || sellBy === "piece") return price * qty;
    const spb = parseFloat(p.sqft_per_box || 0);
    if (!spb) return price * qty;
    if (p.price_basis === "per_sqyd") return price * (spb * qty / 9);
    return price * spb * qty;
  }
  function rofCatExtOv(p, qty, price, overage) {
    const base = rofCatExt(p, qty, price);
    const isUnit = (p.sell_by || "box") === "unit" || p.sell_by === "piece";
    const hasSqft = p.price_basis === "per_sqyd" || parseFloat(p.sqft_per_box || 0) > 0;
    return overage && !isUnit && hasSqft ? base * 1.1 : base;
  }
  function rofTradeUnit(retail, retailLocked, discount) {
    const d = discount > 0 ? retailLocked ? Math.min(discount, 10) : discount : 0;
    return d > 0 ? retail * (1 - d / 100) : retail;
  }
  const RUG_BINDING_PER_FT = 6;
  const RUG_SETUP_FEE = 50;
  const RUG_BINDING_COST_PER_FT = 2.5;
  const RUG_FABRICATION_COST = 25;
  const RUG_DEFAULT_ROLL_WIDTH_FT = 12;
  function repRugQuote(widthFt, lengthFt, rollWidthFt, cutPricePerSqyd, materialCostPerSqyd) {
    const w = parseFloat(widthFt) || 0, l = parseFloat(lengthFt) || 0;
    const roll = parseFloat(rollWidthFt) > 0 ? parseFloat(rollWidthFt) : RUG_DEFAULT_ROLL_WIDTH_FT;
    const price = parseFloat(cutPricePerSqyd) || 0, mcost = parseFloat(materialCostPerSqyd) || 0;
    if (w <= 0 || l <= 0) return { valid: false, oversized: false, rollWidthFt: roll };
    const a = Math.max(w, l), b = Math.min(w, l);
    let lf;
    if (a <= roll) lf = b;
    else if (b <= roll) lf = a;
    else return { valid: false, oversized: true, rollWidthFt: roll };
    const cutArea = roll * lf, perim = 2 * (w + l);
    const material = cutArea / 9 * price, binding = perim * RUG_BINDING_PER_FT;
    const perRug = material + binding + RUG_SETUP_FEE;
    const costPerRug = cutArea / 9 * mcost + perim * RUG_BINDING_COST_PER_FT + RUG_FABRICATION_COST;
    return { valid: true, oversized: false, rollWidthFt: roll, linearFeet: lf, cutAreaSqft: cutArea, perimeterFt: perim, material, binding, setup: RUG_SETUP_FEE, perRug, costPerRug };
  }
  function repRugDims(w, l) {
    const f = (ft) => {
      const n = parseFloat(ft) || 0, wh = Math.floor(n);
      let i = Math.round((n - wh) * 12);
      if (i === 12) return wh + 1 + "'";
      return i === 0 ? wh + "'" : wh + "'" + i + '"';
    };
    return f(w) + " \xD7 " + f(l);
  }
  const repIsCarpetRow = (r) => r && (r.price_basis === "per_sqyd" || r.sell_by === "roll" || r.cut_price != null);
  function RugConfigModal({ carpet: carpetProp, showCost, onClose, onAdd }) {
    const [form, setForm] = useState({ widthFt: "", widthIn: "", lengthFt: "", lengthIn: "", qty: 1 });
    const [fetched, setFetched] = useState(null);
    useEffect(() => {
      if (carpetProp && carpetProp.cut_price == null && carpetProp.sku_id) {
        repFetch("/api/rep/skus/search?sku_id=" + encodeURIComponent(carpetProp.sku_id)).then((d) => {
          const r = (d.results || [])[0];
          if (r) setFetched(r);
        }).catch(() => {
        });
      }
    }, [carpetProp && carpetProp.sku_id]);
    const carpet = fetched ? {
      ...carpetProp,
      cut_price: fetched.cut_price,
      cut_cost: fetched.cut_cost,
      roll_width_ft: fetched.roll_width_ft,
      vendor_cost: fetched.vendor_cost,
      retail_price: carpetProp.retail_price != null ? carpetProp.retail_price : fetched.retail_price
    } : carpetProp;
    const wFt = (parseFloat(form.widthFt) || 0) + (parseFloat(form.widthIn) || 0) / 12;
    const lFt = (parseFloat(form.lengthFt) || 0) + (parseFloat(form.lengthIn) || 0) / 12;
    const q = repRugQuote(
      wFt,
      lFt,
      carpet.roll_width_ft,
      carpet.cut_price != null ? carpet.cut_price : carpet.retail_price,
      carpet.cut_cost != null ? carpet.cut_cost : carpet.vendor_cost != null ? carpet.vendor_cost : carpet.cost
    );
    const qty = Math.max(1, parseInt(form.qty) || 1);
    const add = () => {
      if (!q.valid) return;
      onAdd({
        _key: Date.now(),
        is_custom_rug: true,
        sku_id: carpet.sku_id,
        product_id: carpet.product_id,
        product_name: carpet.product_name || "",
        collection: carpet.collection || "",
        color: carpet.color || "",
        vendor_id: carpet.vendor_id,
        vendor_name: carpet.vendor_name,
        vendor_sku: carpet.vendor_sku || carpet.internal_sku || "",
        custom_width_ft: wFt.toFixed(2),
        custom_length_ft: lFt.toFixed(2),
        num_boxes: qty,
        unit_price: q.perRug,
        subtotal: q.perRug * qty,
        cost: q.costPerRug,
        sell_by: "unit",
        price_basis: null,
        description: "Custom Area Rug \u2014 " + repRugDims(wFt, lFt),
        primary_image: carpet.primary_image || null,
        category_name: carpet.category_name || "Carpet"
      });
    };
    const inp = { width: "100%", padding: "7px 8px", border: "0.5px solid rgba(28,25,23,0.2)", background: "#ece5d8", font: "400 13px/1 Inter, sans-serif", boxSizing: "border-box" };
    const lbl = { display: "block", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 5 };
    return /* @__PURE__ */ React.createElement("div", { onClick: onClose, style: { position: "fixed", inset: 0, background: "rgba(28,25,23,0.5)", zIndex: 1e3, display: "flex", alignItems: "center", justifyContent: "center", padding: "1rem" } }, /* @__PURE__ */ React.createElement("div", { onClick: (e) => e.stopPropagation(), style: { background: "#f4efe6", width: "100%", maxWidth: 440, maxHeight: "90vh", overflowY: "auto", border: "0.5px solid rgba(28,25,23,0.25)", boxShadow: "0 20px 60px rgba(0,0,0,0.3)" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "flex-start", padding: "1rem 1.25rem", borderBottom: "0.5px solid rgba(28,25,23,0.12)" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 0.5625rem/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 4 } }, "Custom Area Rug"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.9375rem/1.3 Inter, sans-serif", color: "#1c1917" } }, formatLineItem(carpet).name), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.75rem/1.3 Inter, sans-serif", color: "#8a7e68", marginTop: 2 } }, (carpet.roll_width_ft ? parseFloat(carpet.roll_width_ft) : 12) + "' roll \xB7 $" + parseFloat(carpet.cut_price != null ? carpet.cut_price : carpet.retail_price || 0).toFixed(2) + "/sqyd")), /* @__PURE__ */ React.createElement("button", { onClick: onClose, style: { background: "none", border: "none", font: "400 1.5rem/1 Inter", color: "#8a7e68", cursor: "pointer", padding: 0 } }, "\xD7")), /* @__PURE__ */ React.createElement("div", { style: { padding: "1.25rem" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.75rem", marginBottom: "0.875rem" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }, /* @__PURE__ */ React.createElement("label", { style: lbl }, "Width"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 5 } }, /* @__PURE__ */ React.createElement("input", { type: "number", min: "0", placeholder: "ft", value: form.widthFt, onChange: (e) => setForm((f) => ({ ...f, widthFt: e.target.value })), style: inp }), /* @__PURE__ */ React.createElement("input", { type: "number", min: "0", max: "11", placeholder: "in", value: form.widthIn, onChange: (e) => setForm((f) => ({ ...f, widthIn: e.target.value })), style: inp }))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "flex-end", paddingBottom: 8, color: "#a89e86" } }, "\xD7"), /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }, /* @__PURE__ */ React.createElement("label", { style: lbl }, "Length"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 5 } }, /* @__PURE__ */ React.createElement("input", { type: "number", min: "0", placeholder: "ft", value: form.lengthFt, onChange: (e) => setForm((f) => ({ ...f, lengthFt: e.target.value })), style: inp }), /* @__PURE__ */ React.createElement("input", { type: "number", min: "0", max: "11", placeholder: "in", value: form.lengthIn, onChange: (e) => setForm((f) => ({ ...f, lengthIn: e.target.value })), style: inp })))), q.oversized && /* @__PURE__ */ React.createElement("div", { style: { padding: "0.625rem 0.75rem", background: "#faf3e4", border: "0.5px solid var(--gold, #c9a668)", font: "400 0.75rem/1.4 Inter, sans-serif", color: "#8a6d3b", marginBottom: "0.875rem" } }, "Both sides exceed the ", q.rollWidthFt, "' roll width \u2014 this rug needs seaming. Call it in or add it as a Custom Item."), q.valid && /* @__PURE__ */ React.createElement("div", { style: { background: "rgba(28,25,23,0.03)", border: "0.5px solid rgba(28,25,23,0.08)", padding: "0.75rem", marginBottom: "0.875rem", font: "400 0.8125rem/1.6 Inter, sans-serif", color: "#4a4441" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("span", null, "Cut"), /* @__PURE__ */ React.createElement("span", null, q.rollWidthFt, "' \xD7 ", q.linearFeet.toFixed(1), "' (", (q.cutAreaSqft / 9).toFixed(1), " sqyd)")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("span", null, "Material"), /* @__PURE__ */ React.createElement("span", null, "$", q.material.toFixed(2))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("span", null, "Binding (", q.perimeterFt.toFixed(1), "' \xD7 $", RUG_BINDING_PER_FT, ")"), /* @__PURE__ */ React.createElement("span", null, "$", q.binding.toFixed(2))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("span", null, "Fabrication"), /* @__PURE__ */ React.createElement("span", null, "$", q.setup.toFixed(2))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", fontWeight: 500, color: "#1c1917", borderTop: "0.5px solid rgba(28,25,23,0.12)", marginTop: 5, paddingTop: 5 } }, /* @__PURE__ */ React.createElement("span", null, "Per rug"), /* @__PURE__ */ React.createElement("span", null, "$", q.perRug.toFixed(2))), showCost && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", font: "400 0.6875rem/1.4 ui-monospace, monospace", color: "#8a7e68", marginTop: 3 } }, /* @__PURE__ */ React.createElement("span", null, "cost / rug"), /* @__PURE__ */ React.createElement("span", null, "$", q.costPerRug.toFixed(2)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("label", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, "Qty"), /* @__PURE__ */ React.createElement("input", { type: "number", min: "1", value: form.qty, onChange: (e) => setForm((f) => ({ ...f, qty: e.target.value })), style: { ...inp, width: 56, textAlign: "center" } })), /* @__PURE__ */ React.createElement("button", { onClick: add, disabled: !q.valid, style: { flex: 1, padding: "10px 14px", background: q.valid ? "#1c1917" : "#b5ada0", color: "#ece5d8", border: "none", font: "500 13px/1 Inter, sans-serif", cursor: q.valid ? "pointer" : "default" } }, q.valid ? "Add Rug \u2014 $" + (q.perRug * qty).toFixed(2) : "Enter dimensions")))));
  }
  function RofCatalogModal({ data, showCost, orderNumber, onClose, onAdd, onAddRug, contextLabel, overage, onOverageChange }) {
    const TH = { ink: "#1c1917", paper: "#ece5d8", accent: "#a87935", muted: "#8a7e68", warm: "#d8cdb6" };
    const [rugFor, setRugFor] = useState(null);
    const mono = (c) => ({ font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: c });
    const fmtMoney = (n) => "$" + parseFloat(n || 0).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const customer = data.customer;
    const customerName = customer ? customer.name : data.newCustomerName || "Walk-in";
    const discount = customer && customer.type === "trade" ? parseFloat(customer.discount_percent || 0) : 0;
    const [mode, setMode] = useState("catalog");
    const [query, setQuery] = useState("");
    const [vendorId, setVendorId] = useState("");
    const [products, setProducts] = useState([]);
    const [total, setTotal] = useState(0);
    const [page, setPage] = useState(1);
    const PER_PAGE = 48;
    const [loading, setLoading] = useState(true);
    const fetchTimer = useRef(null);
    const fetchCounter = useRef(0);
    const gridRef = useRef(null);
    const pgBtn = (active, disabled) => ({
      minWidth: 30,
      padding: "5px 9px",
      borderRadius: 6,
      cursor: disabled ? "default" : "pointer",
      border: "1px solid " + (active ? TH.accent : TH.warm),
      background: active ? TH.accent : "#fff",
      color: active ? "#fff" : TH.ink,
      font: "600 12px/1 var(--roma-sans)",
      opacity: disabled ? 0.4 : 1
    });
    const [showFilters, setShowFilters] = useState(false);
    const [category, setCategory] = useState("");
    const [stockStatus, setStockStatus] = useState("");
    const [minPrice, setMinPrice] = useState("");
    const [maxPrice, setMaxPrice] = useState("");
    const [categories, setCategories] = useState([]);
    const activeFilterCount = (category ? 1 : 0) + (stockStatus ? 1 : 0) + (minPrice || maxPrice ? 1 : 0);
    const clearFilters = () => {
      setCategory("");
      setStockStatus("");
      setMinPrice("");
      setMaxPrice("");
      setPage(1);
    };
    const filterPill = (on) => ({
      padding: "6px 11px",
      cursor: "pointer",
      whiteSpace: "nowrap",
      border: "0.5px solid " + (on ? TH.ink : "rgba(28,25,23,0.16)"),
      background: on ? TH.ink : "transparent",
      color: on ? TH.paper : TH.ink,
      font: "500 11px/1 var(--roma-sans)"
    });
    const [added, setAdded] = useState({});
    const [accAdded, setAccAdded] = useState({});
    const [expanded, setExpanded] = useState(null);
    const [accCache, setAccCache] = useState({});
    const [customList, setCustomList] = useState([]);
    const [rugList, setRugList] = useState([]);
    const [customVendors, setCustomVendors] = useState([]);
    useEffect(() => {
      const onKey = (e) => {
        if (e.key === "Escape") onClose();
      };
      window.addEventListener("keydown", onKey);
      const prevOverflow = document.body.style.overflow;
      document.body.style.overflow = "hidden";
      return () => {
        window.removeEventListener("keydown", onKey);
        document.body.style.overflow = prevOverflow;
      };
    }, []);
    useEffect(() => {
      repFetch("/api/rep/vendors").then((d) => setCustomVendors(d.vendors || [])).catch(() => {
      });
      fetch(API + "/api/categories").then((r) => r.json()).then((d) => setCategories(d.categories || [])).catch(() => {
      });
    }, []);
    useEffect(() => {
      const thisFetch = ++fetchCounter.current;
      setLoading(true);
      if (fetchTimer.current) clearTimeout(fetchTimer.current);
      fetchTimer.current = setTimeout(() => {
        const params = new URLSearchParams();
        if (query.trim()) params.set("search", query.trim());
        if (vendorId) params.set("vendor", vendorId);
        if (category) params.set("category", category);
        if (stockStatus) params.set("stock_status", stockStatus);
        if (minPrice) params.set("min_price", minPrice);
        if (maxPrice) params.set("max_price", maxPrice);
        params.set("limit", PER_PAGE);
        params.set("page", page);
        params.set("sort", query.trim() ? "relevance" : "margin_desc");
        repFetch("/api/rep/products?" + params.toString()).then((d) => {
          if (thisFetch !== fetchCounter.current) return;
          setProducts(d.products || []);
          setTotal(d.total || 0);
          setLoading(false);
          if (gridRef.current) gridRef.current.scrollTop = 0;
        }).catch(() => {
          if (thisFetch === fetchCounter.current) setLoading(false);
        });
      }, query.trim() ? 250 : 0);
      return () => clearTimeout(fetchTimer.current);
    }, [query, vendorId, page, category, stockStatus, minPrice, maxPrice]);
    const setQty = (p, qty) => setAdded((a) => {
      const n = { ...a };
      if (qty <= 0) delete n[p.sku_id];
      else n[p.sku_id] = { item: p, qty };
      return n;
    });
    const bumpQty = (p, delta) => {
      const cur = added[p.sku_id] ? added[p.sku_id].qty : 0;
      setQty(p, Math.max(0, cur + delta));
    };
    const toggleExpand = (p) => {
      const skuId = p.sku_id;
      setExpanded((e) => e === skuId ? null : skuId);
      if (!accCache[skuId]) {
        setAccCache((c) => ({ ...c, [skuId]: "loading" }));
        repFetch("/api/rep/skus/" + skuId).then((d) => setAccCache((c) => ({ ...c, [skuId]: d.accessories || [] }))).catch(() => setAccCache((c) => ({ ...c, [skuId]: [] })));
      }
    };
    const toggleAcc = (acc, parent) => setAccAdded((a) => {
      const n = { ...a };
      if (n[acc.sku_id]) delete n[acc.sku_id];
      else n[acc.sku_id] = { acc, parent, qty: 1 };
      return n;
    });
    const setAccQty = (acc, parent, qty) => setAccAdded((a) => {
      const n = { ...a };
      if (qty <= 0) delete n[acc.sku_id];
      else n[acc.sku_id] = { acc, parent, qty };
      return n;
    });
    const lineList = Object.values(added);
    const accList = Object.values(accAdded);
    const itemCount = lineList.length + accList.length + customList.length + rugList.length;
    const catalogRetail = lineList.reduce((t, { item, qty }) => t + rofCatExtOv(item, qty, parseFloat(item.price || 0), overage), 0) + accList.reduce((t, { acc, qty }) => t + parseFloat(acc.retail_price || 0) * qty, 0);
    const catalogTrade = lineList.reduce((t, { item, qty }) => t + rofCatExtOv(item, qty, rofTradeUnit(parseFloat(item.price || 0), item.retail_locked, discount), overage), 0) + accList.reduce((t, { acc, qty }) => t + rofTradeUnit(parseFloat(acc.retail_price || 0), acc.retail_locked, discount) * qty, 0);
    const customSubtotal = customList.reduce((t, c) => t + c.subtotal, 0);
    const rugSubtotal = rugList.reduce((t, r) => t + (parseFloat(r.subtotal) || 0), 0);
    const retailSubtotal = catalogRetail + customSubtotal + rugSubtotal;
    const discountedTotal = catalogTrade + customSubtotal + rugSubtotal;
    const confirm2 = () => {
      if (itemCount === 0) return;
      onAdd({ lines: lineList, accessories: accList, customs: customList });
      if (onAddRug) rugList.forEach((r) => onAddRug(r));
      onClose();
    };
    return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay", onMouseDown: (e) => {
      if (e.target === e.currentTarget) onClose();
    } }, /* @__PURE__ */ React.createElement("div", { className: "rof-cat-dialog rof-pop", style: { position: "relative" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 24px", borderBottom: "0.5px solid rgba(28,25,23,0.12)", display: "flex", alignItems: "center", gap: 20, flexShrink: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { flexShrink: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { ...mono(TH.accent), fontSize: 10, letterSpacing: "0.18em", marginBottom: 8 } }, contextLabel || /* @__PURE__ */ React.createElement(React.Fragment, null, "Add to order ", "\xB7", " ", orderNumber || "New order")), /* @__PURE__ */ React.createElement("div", { style: { font: "300 28px/1 var(--roma-serif)", letterSpacing: "-0.015em", color: TH.ink } }, "Product ", /* @__PURE__ */ React.createElement("em", { style: { color: TH.accent } }, "catalog"))), /* @__PURE__ */ React.createElement("div", { className: "rof-cat-header-chip", style: { padding: "8px 14px", border: "0.5px solid rgba(28,25,23,0.13)", background: TH.warm, display: "flex", flexDirection: "column", gap: 3, flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), fontSize: 9 } }, "Pricing for"), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-sans)", color: TH.ink } }, customerName, " \xB7 ", /* @__PURE__ */ React.createElement("span", { style: { color: TH.accent } }, discount > 0 ? "Trade \u2212" + discount + "%" : "Retail"))), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", background: TH.paper, minWidth: 120 } }, /* @__PURE__ */ React.createElement("span", { style: { padding: "11px 14px", color: TH.muted, font: "400 15px/1 var(--roma-sans)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        autoFocus: true,
        value: query,
        onChange: (e) => {
          setQuery(e.target.value);
          setPage(1);
        },
        placeholder: "Search SKU, material, vendor, collection\u2026 or scan a barcode",
        style: { flex: 1, border: "none", background: "transparent", outline: "none", font: "400 14px/1 var(--roma-sans)", color: TH.ink, padding: "11px 0", minWidth: 0 }
      }
    ), query && /* @__PURE__ */ React.createElement("span", { onClick: () => {
      setQuery("");
      setPage(1);
    }, style: { padding: "0 12px", cursor: "pointer", color: TH.muted, font: "400 15px/1 var(--roma-sans)" } }, "\xD7")), /* @__PURE__ */ React.createElement("button", { onClick: onClose, style: { width: 34, height: 34, flexShrink: 0, border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", color: TH.ink, cursor: "pointer", font: "400 16px/1 var(--roma-sans)" } }, "\u2715")), /* @__PURE__ */ React.createElement("div", { style: { padding: "0 24px", borderBottom: "0.5px solid rgba(28,25,23,0.1)", flexShrink: 0, display: "flex", gap: 0, alignItems: "stretch" } }, [["catalog", "\u25A6", "Catalog"], ["custom", "\u270E", "Custom item"]].map(([id, icon, label]) => {
      const on = mode === id;
      return /* @__PURE__ */ React.createElement("button", { key: id, onClick: () => setMode(id), style: {
        padding: "13px 20px",
        cursor: "pointer",
        background: "transparent",
        border: "none",
        display: "flex",
        alignItems: "center",
        gap: 8,
        font: "500 11px/1 var(--roma-sans)",
        letterSpacing: "0.1em",
        textTransform: "uppercase",
        color: on ? TH.ink : TH.muted,
        borderBottom: on ? "2px solid " + TH.accent : "2px solid transparent",
        marginBottom: -0.5
      } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 14px/1 var(--roma-serif)" } }, icon), label);
    }), /* @__PURE__ */ React.createElement("span", { style: { marginLeft: "auto", alignSelf: "center", ...mono(TH.muted), fontSize: 9, lineHeight: 1.4, letterSpacing: "0.12em" } }, mode === "catalog" ? "From stocked SKUs" : "Off-catalog \xB7 manual line")), mode === "catalog" && /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 24px", borderBottom: "0.5px solid rgba(28,25,23,0.1)", flexShrink: 0, display: "flex", gap: 14, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0, display: "flex", gap: 7, alignItems: "center", overflowX: "auto", scrollbarWidth: "thin", paddingBottom: 2 } }, [{ id: "", name: "All vendors" }].concat(customVendors).map((v) => {
      const on = String(v.id || "") === vendorId;
      return /* @__PURE__ */ React.createElement("button", { key: v.id || "all", onClick: () => {
        setVendorId(String(v.id || ""));
        setPage(1);
      }, style: {
        padding: "7px 13px",
        cursor: "pointer",
        flexShrink: 0,
        whiteSpace: "nowrap",
        background: on ? TH.ink : "transparent",
        color: on ? TH.paper : TH.ink,
        border: on ? "0.5px solid " + TH.ink : "0.5px solid rgba(28,25,23,0.16)",
        font: "500 11px/1 var(--roma-sans)",
        letterSpacing: "0.02em"
      } }, v.name, v.public_code ? " \xB7 " + v.public_code : "");
    })), /* @__PURE__ */ React.createElement("button", { onClick: () => setShowFilters(true), style: {
      flexShrink: 0,
      display: "flex",
      alignItems: "center",
      gap: 7,
      padding: "7px 13px",
      cursor: "pointer",
      background: activeFilterCount ? TH.ink : "transparent",
      color: activeFilterCount ? TH.paper : TH.ink,
      border: "0.5px solid " + (activeFilterCount ? TH.ink : "rgba(28,25,23,0.2)"),
      font: "500 11px/1 var(--roma-sans)",
      letterSpacing: "0.02em"
    } }, /* @__PURE__ */ React.createElement("svg", { width: "13", height: "13", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2" }, /* @__PURE__ */ React.createElement("line", { x1: "4", y1: "6", x2: "20", y2: "6" }), /* @__PURE__ */ React.createElement("line", { x1: "7", y1: "12", x2: "17", y2: "12" }), /* @__PURE__ */ React.createElement("line", { x1: "10", y1: "18", x2: "14", y2: "18" })), "Filters", activeFilterCount > 0 ? " \xB7 " + activeFilterCount : ""), /* @__PURE__ */ React.createElement("span", { style: { flexShrink: 0, ...mono(TH.muted), fontSize: 10 } }, loading ? "Loading\u2026" : total.toLocaleString() + (total === 1 ? " result" : " results") + (showCost ? " \xB7 cost & margin visible" : ""))), /* @__PURE__ */ React.createElement("div", { className: "rof-cat-body" }, mode === "catalog" ? /* @__PURE__ */ React.createElement("div", { className: "rof-cat-grid", ref: gridRef }, loading && products.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { gridColumn: "1 / -1", padding: "60px 0", textAlign: "center", color: TH.muted, font: "400 15px/1.5 var(--roma-sans)" } }, "Loading SKUs\u2026"), products.map((p) => /* @__PURE__ */ React.createElement(
      RofCatCard,
      {
        key: p.sku_id,
        p,
        showCost,
        discount,
        qty: added[p.sku_id] ? added[p.sku_id].qty : 0,
        onBump: (d) => bumpQty(p, d),
        onSet: (v) => setQty(p, v),
        expanded: expanded === p.sku_id,
        onToggleExpand: () => toggleExpand(p),
        accessories: accCache[p.sku_id],
        accAdded,
        onToggleAcc: (acc) => toggleAcc(acc, p),
        onSetAccQty: (acc, q) => setAccQty(acc, p, q),
        onRug: onAddRug ? setRugFor : null
      }
    )), !loading && products.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { gridColumn: "1 / -1", padding: "60px 0", textAlign: "center", color: TH.muted, font: "400 15px/1.5 var(--roma-sans)" } }, "No products match", query.trim() ? " \u201C" + query.trim() + "\u201D" : " these filters", ".", /* @__PURE__ */ React.createElement("br", null), "Try another term or clear the category filter."), !loading && total > PER_PAGE && (() => {
      const totalPages = Math.ceil(total / PER_PAGE);
      const cur = Math.min(page, totalPages);
      const from = (cur - 1) * PER_PAGE + 1;
      const to = Math.min(cur * PER_PAGE, total);
      const nums = [];
      for (let i = 1; i <= totalPages; i++) {
        if (i === 1 || i === totalPages || i >= cur - 2 && i <= cur + 2) nums.push(i);
        else if (nums[nums.length - 1] !== "\u2026") nums.push("\u2026");
      }
      return /* @__PURE__ */ React.createElement("div", { style: { gridColumn: "1 / -1", display: "flex", alignItems: "center", justifyContent: "center", flexWrap: "wrap", gap: 6, padding: "16px 0 4px" } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), fontSize: 9, marginRight: 8 } }, from.toLocaleString(), "\u2013", to.toLocaleString(), " of ", total.toLocaleString()), /* @__PURE__ */ React.createElement("button", { onClick: () => setPage((p) => Math.max(1, p - 1)), disabled: cur === 1, style: pgBtn(false, cur === 1) }, "\u2039 Prev"), nums.map(
        (n, idx) => n === "\u2026" ? /* @__PURE__ */ React.createElement("span", { key: "e" + idx, style: { color: TH.muted, padding: "0 2px" } }, "\u2026") : /* @__PURE__ */ React.createElement("button", { key: n, onClick: () => setPage(n), style: pgBtn(n === cur, false) }, n)
      ), /* @__PURE__ */ React.createElement("button", { onClick: () => setPage((p) => Math.min(totalPages, p + 1)), disabled: cur === totalPages, style: pgBtn(false, cur === totalPages) }, "Next \u203A"));
    })()) : /* @__PURE__ */ React.createElement(
      RofCatCustomForm,
      {
        showCost,
        vendors: customVendors,
        onAdd: (line) => setCustomList((l) => [...l, line])
      }
    ), /* @__PURE__ */ React.createElement(
      RofCatTray,
      {
        discount,
        overage,
        onOverageChange,
        lineList,
        accList,
        customList,
        rugList,
        itemCount,
        retailSubtotal,
        discountedTotal,
        onRemoveItem: (skuId) => setAdded((a) => {
          const n = { ...a };
          delete n[skuId];
          return n;
        }),
        onRemoveAcc: (skuId) => setAccAdded((a) => {
          const n = { ...a };
          delete n[skuId];
          return n;
        }),
        onRemoveCustom: (cid) => setCustomList((l) => l.filter((c) => c._cid !== cid)),
        onRemoveRug: (cid) => setRugList((l) => l.filter((r) => r._cid !== cid)),
        onClose,
        onConfirm: confirm2
      }
    )), showFilters && /* @__PURE__ */ React.createElement("div", { onClick: () => setShowFilters(false), style: { position: "absolute", inset: 0, background: "rgba(28,25,23,0.25)", zIndex: 5 } }), /* @__PURE__ */ React.createElement("div", { style: {
      position: "absolute",
      top: 0,
      right: 0,
      bottom: 0,
      width: 344,
      maxWidth: "90%",
      background: TH.paper,
      borderLeft: "0.5px solid rgba(28,25,23,0.2)",
      boxShadow: "-24px 0 60px rgba(28,25,23,0.22)",
      zIndex: 6,
      transform: showFilters ? "translateX(0)" : "translateX(105%)",
      transition: "transform 0.28s cubic-bezier(.22,.61,.36,1)",
      display: "flex",
      flexDirection: "column"
    } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 20px", borderBottom: "0.5px solid rgba(28,25,23,0.12)", display: "flex", alignItems: "center", justifyContent: "space-between", flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "300 20px/1 var(--roma-serif)", color: TH.ink } }, "Filters"), /* @__PURE__ */ React.createElement("button", { onClick: () => setShowFilters(false), style: { width: 30, height: 30, border: "none", background: "transparent", cursor: "pointer", color: TH.ink, font: "400 16px/1 var(--roma-sans)" } }, "\u2715")), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, overflowY: "auto", padding: "18px 20px", display: "flex", flexDirection: "column", gap: 22 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...mono(TH.muted), fontSize: 9, marginBottom: 10 } }, "Category"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 7 } }, [{ slug: "", name: "All" }].concat(categories).map((c) => /* @__PURE__ */ React.createElement("button", { key: c.slug || "all", onClick: () => {
      setCategory(c.slug || "");
      setPage(1);
    }, style: filterPill((c.slug || "") === category) }, c.name)))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...mono(TH.muted), fontSize: 9, marginBottom: 10 } }, "Availability"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 7 } }, [["", "Any"], ["in_stock", "In stock"], ["low_stock", "Low stock"], ["out_of_stock", "Out of stock"]].map(([v, l]) => /* @__PURE__ */ React.createElement("button", { key: v || "any", onClick: () => {
      setStockStatus(v);
      setPage(1);
    }, style: filterPill(v === stockStatus) }, l)))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...mono(TH.muted), fontSize: 9, marginBottom: 10 } }, "Price per unit"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 10 } }, [["Min", minPrice, setMinPrice], ["Max", maxPrice, setMaxPrice]].map(([ph, val, setter]) => /* @__PURE__ */ React.createElement("div", { key: ph, style: { flex: 1, display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", background: "#fff" } }, /* @__PURE__ */ React.createElement("span", { style: { padding: "9px 2px 9px 10px", color: TH.muted, font: "400 13px/1 var(--roma-sans)" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "number",
        min: "0",
        placeholder: ph,
        value: val,
        onChange: (e) => {
          setter(e.target.value);
          setPage(1);
        },
        style: { flex: 1, width: "100%", border: "none", outline: "none", background: "transparent", font: "400 13px/1 var(--roma-sans)", color: TH.ink, padding: "9px 8px 9px 4px", minWidth: 0 }
      }
    )))))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "0.5px solid rgba(28,25,23,0.12)", display: "flex", gap: 10, flexShrink: 0 } }, /* @__PURE__ */ React.createElement("button", { onClick: clearFilters, disabled: activeFilterCount === 0, style: { flex: 1, padding: "11px", cursor: activeFilterCount ? "pointer" : "default", border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", color: TH.ink, font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", opacity: activeFilterCount ? 1 : 0.4 } }, "Clear all"), /* @__PURE__ */ React.createElement("button", { onClick: () => setShowFilters(false), style: { flex: 1, padding: "11px", cursor: "pointer", border: "0.5px solid " + TH.ink, background: TH.ink, color: TH.paper, font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase" } }, "Done")))), rugFor && /* @__PURE__ */ React.createElement(
      RugConfigModal,
      {
        carpet: {
          sku_id: rugFor.sku_id,
          product_id: rugFor.product_id || rugFor.id,
          product_name: rugFor.display_name || rugFor.name || rugFor.product_name || "",
          collection: rugFor.collection || "",
          color: "",
          vendor_id: rugFor.vendor_id,
          vendor_name: rugFor.brand_name || rugFor.vendor_name,
          vendor_sku: rugFor.vendor_sku || rugFor.internal_sku || "",
          primary_image: rugFor.primary_image || null,
          category_name: rugFor.category_name || "Carpet",
          price_basis: rugFor.price_basis,
          sell_by: rugFor.sell_by,
          cut_price: rugFor.cut_price != null ? rugFor.cut_price : null,
          roll_width_ft: rugFor.roll_width_ft != null ? rugFor.roll_width_ft : null
        },
        showCost,
        onClose: () => setRugFor(null),
        onAdd: (line) => {
          setRugList((l) => [...l, { ...line, _cid: "rug" + (l.length + 1) + "-" + line.sku_id }]);
          setRugFor(null);
        }
      }
    ));
  }
  function RofCatCard({ p, showCost, discount, qty, onBump, onSet, expanded, onToggleExpand, accessories, accAdded, onToggleAcc, onSetAccQty, onRug }) {
    const TH = { ink: "#1c1917", paper: "#ece5d8", accent: "#a87935", muted: "#8a7e68", warm: "#d8cdb6" };
    const mono = (c) => ({ font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: c });
    const fmtMoney = (n) => "$" + parseFloat(n || 0).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const marginColor = (pct) => pct >= 30 ? "#3a7a4e" : pct >= 15 ? "#a87935" : "#c54731";
    const price = p.price ? pieceRetail(p, p.price) : 0;
    const tradeUnit = rofTradeUnit(price, p.retail_locked, discount || 0);
    const discounted = price > 0 && tradeUnit < price;
    const _baseMargin = p.margin_pct != null ? parseFloat(p.margin_pct) : null;
    const _effUnit = discounted ? tradeUnit : price;
    const mPct = _baseMargin != null && price > 0 && _effUnit > 0 ? (1 - price * (1 - _baseMargin / 100) / _effUnit) * 100 : _baseMargin || 0;
    const sk = stockDisplay(p);
    const active = qty > 0;
    const isUnit = p.sell_by === "unit" || p.sell_by === "piece";
    const isCarpet = p.price_basis === "per_sqyd" || p.sell_by === "roll";
    const qtyUnit = isCarpet ? "sq yd" : isPerPieceRow(p) ? "pcs" : isUnit ? "units" : "boxes";
    const priceUnit = skuPriceSuffix(p);
    const hasAcc = (p.accessory_count || 0) > 0;
    const accLoading = accessories === "loading" || accessories === void 0;
    const accList = Array.isArray(accessories) ? accessories : [];
    const accCount = accList.filter((a) => accAdded[a.sku_id]).length;
    const stepBtn = { width: 32, height: 34, border: "none", background: "transparent", color: TH.ink, cursor: "pointer", font: "400 17px/1 var(--roma-sans)" };
    return /* @__PURE__ */ React.createElement("div", { style: {
      border: active ? "1.5px solid " + TH.accent : "0.5px solid rgba(28,25,23,0.13)",
      background: TH.paper,
      display: "flex",
      flexDirection: "column",
      boxShadow: active ? "0 6px 20px rgba(28,25,23,0.08)" : "none",
      transition: "border-color .12s, box-shadow .12s"
    } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "96px 1fr" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative", minHeight: 96, overflow: "hidden", background: "var(--stone-100)" } }, p.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(p.primary_image, 200), alt: "", loading: "lazy", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", inset: 0, ...materialFace(rofCatKind(p)) } }), /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", top: 6, left: 6, padding: "3px 6px", background: TH.paper, ...mono(sk.c), fontSize: 8, letterSpacing: "0.1em" } }, "\u25CF", " ", sk.l)), /* @__PURE__ */ React.createElement("div", { style: { padding: "11px 13px", display: "flex", flexDirection: "column", gap: 3, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), fontSize: 9, lineHeight: 1.3, letterSpacing: "0.12em", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, [p.brand_name || p.vendor_name, p.vendor_public_code, p.category_name].filter(Boolean).join(" \xB7 ") || (p.vendor_sku || p.internal_sku || "")), showCost && mPct > 0 && /* @__PURE__ */ React.createElement("span", { style: { padding: "2px 6px", background: marginColor(mPct), color: "#fff", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.08em", whiteSpace: "nowrap", flexShrink: 0 } }, mPct.toFixed(0), "%")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 17px/1.15 var(--roma-serif)", color: TH.ink, letterSpacing: "-0.008em" } }, p.display_name || p.name), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginTop: 2, gap: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(sk.q ? sk.c : TH.muted), fontSize: 9, letterSpacing: "0.1em", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, sk.q ? sk.q + " avail" : p.vendor_sku || p.internal_sku || ""), /* @__PURE__ */ React.createElement("span", { style: { whiteSpace: "nowrap", textAlign: "right", display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 1 } }, price > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, discounted && /* @__PURE__ */ React.createElement("span", { style: { font: "400 10px/1 var(--roma-sans)", color: TH.muted, textDecoration: "line-through" } }, fmtMoney(price), priceUnit), /* @__PURE__ */ React.createElement("span", { style: { font: "400 16px/1 var(--roma-serif)", color: discounted ? TH.accent : TH.ink } }, fmtMoney(tradeUnit), /* @__PURE__ */ React.createElement("span", { style: { font: "400 10px/1 var(--roma-sans)", color: TH.muted } }, priceUnit))) : "\u2014")))), /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 13px", borderTop: "0.5px solid rgba(28,25,23,0.08)", display: "flex", alignItems: "center", gap: 10 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)" } }, /* @__PURE__ */ React.createElement("button", { onClick: () => onBump(-1), style: stepBtn }, "\u2212"), /* @__PURE__ */ React.createElement("input", { value: qty, onChange: (e) => onSet(Math.max(0, parseInt(e.target.value) || 0)), style: {
      width: 52,
      textAlign: "center",
      border: "none",
      borderLeft: "0.5px solid rgba(28,25,23,0.13)",
      borderRight: "0.5px solid rgba(28,25,23,0.13)",
      background: "transparent",
      outline: "none",
      font: "400 15px/1 var(--roma-serif)",
      color: TH.ink,
      padding: "7px 0"
    } }), /* @__PURE__ */ React.createElement("button", { onClick: () => onBump(1), style: stepBtn }, "+")), /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), fontSize: 9, letterSpacing: "0.12em" } }, qtyUnit, !isUnit && !isCarpet && parseFloat(p.sqft_per_box || 0) > 0 ? " \xB7 " + +parseFloat(p.sqft_per_box).toFixed(2) + " sf/box" : "", isCarpet ? qty > 0 ? " \xB7 \u2248" + Math.round(qty * 9) + " sf" : "" : !isUnit && parseFloat(p.sqft_per_box || 0) > 0 && qty > 0 ? " \xB7 \u2248" + Math.round(parseFloat(p.sqft_per_box) * qty) + " sf" : ""), /* @__PURE__ */ React.createElement("button", { onClick: () => onBump(1), style: {
      marginLeft: "auto",
      padding: "8px 16px",
      cursor: "pointer",
      background: active ? TH.accent : TH.ink,
      color: TH.paper,
      border: "none",
      font: "500 11px/1 var(--roma-sans)",
      letterSpacing: "0.06em",
      textTransform: "uppercase"
    } }, active ? "\u2713 Added" : "+ Add")), onRug && repIsCarpetRow(p) && /* @__PURE__ */ React.createElement("button", { onClick: () => onRug(p), style: {
      padding: "9px 13px",
      borderTop: "0.5px solid rgba(28,25,23,0.08)",
      background: "transparent",
      cursor: "pointer",
      border: "none",
      borderTopStyle: "solid",
      borderTopWidth: "0.5px",
      borderTopColor: "rgba(28,25,23,0.08)",
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      width: "100%",
      font: "500 10px/1 ui-monospace, monospace",
      letterSpacing: "0.12em",
      textTransform: "uppercase",
      color: TH.accent
    } }, /* @__PURE__ */ React.createElement("span", null, "+ Custom Area Rug ", "\xB7", " bind to any size"), /* @__PURE__ */ React.createElement("span", null, "\u2317")), hasAcc && /* @__PURE__ */ React.createElement("button", { onClick: onToggleExpand, style: {
      padding: "9px 13px",
      borderTop: "0.5px solid rgba(28,25,23,0.08)",
      background: expanded ? TH.warm : "transparent",
      cursor: "pointer",
      border: "none",
      borderTopStyle: "solid",
      borderTopWidth: "0.5px",
      borderTopColor: "rgba(28,25,23,0.08)",
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      width: "100%",
      font: "500 10px/1 ui-monospace, monospace",
      letterSpacing: "0.12em",
      textTransform: "uppercase",
      color: accCount ? TH.accent : TH.muted
    } }, /* @__PURE__ */ React.createElement("span", null, accCount ? "\u2713 " + accCount + " accessor" + (accCount !== 1 ? "ies" : "y") + " added" : "+ " + p.accessory_count + " accessor" + (p.accessory_count !== 1 ? "ies" : "y"), " ", "\xB7", " complete the install"), /* @__PURE__ */ React.createElement("span", { style: { transition: "transform .15s", transform: expanded ? "rotate(180deg)" : "none" } }, "\u2304")), hasAcc && expanded && /* @__PURE__ */ React.createElement("div", { className: "rof-pop", style: { borderTop: "0.5px solid rgba(28,25,23,0.08)", background: "rgba(216,205,182,0.5)" } }, accLoading && /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 13px", ...mono(TH.muted), fontSize: 9, lineHeight: 1.4 } }, "Loading accessories\u2026"), !accLoading && accList.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 13px", ...mono(TH.muted), fontSize: 9, lineHeight: 1.4 } }, "No color-matched accessories for this SKU."), !accLoading && accList.map((a, i) => {
      const on = !!accAdded[a.sku_id];
      const aQty = on ? accAdded[a.sku_id].qty : 0;
      const aPrice = parseFloat(a.retail_price || 0);
      const aTrade = rofTradeUnit(aPrice, a.retail_locked, discount || 0);
      const aDiscounted = aPrice > 0 && aTrade < aPrice;
      const aUnit = skuPriceSuffix(a);
      const accStepBtn = { width: 26, height: 28, border: "none", background: "transparent", color: TH.ink, cursor: "pointer", font: "400 15px/1 var(--roma-sans)" };
      return /* @__PURE__ */ React.createElement("div", { key: a.sku_id, style: {
        display: "grid",
        gridTemplateColumns: "30px 1fr auto auto",
        gap: 10,
        alignItems: "center",
        padding: "9px 13px",
        borderTop: i ? "0.5px solid rgba(28,25,23,0.06)" : "none",
        background: on ? "rgba(168,121,53,0.08)" : "transparent"
      } }, a.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(a.primary_image, 80), alt: "", loading: "lazy", style: { width: 30, height: 30, objectFit: "cover", border: "0.5px solid rgba(28,25,23,0.1)" } }) : /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 30, ...materialFace(rofCatKind(p)), border: "0.5px solid rgba(28,25,23,0.1)" } }), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.2 var(--roma-sans)", color: TH.ink, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, a.accessory_label || a.variant_name), /* @__PURE__ */ React.createElement("div", { style: { font: "400 10px/1.3 var(--roma-sans)", color: TH.muted, marginTop: 1, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, [a.variant_name && a.variant_name !== (a.accessory_label || "") ? a.variant_name : null, a.vendor_sku].filter(Boolean).join(" \xB7 "))), /* @__PURE__ */ React.createElement("span", { style: { whiteSpace: "nowrap", textAlign: "right", display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 1 } }, aPrice > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, aDiscounted && /* @__PURE__ */ React.createElement("span", { style: { font: "400 9px/1 var(--roma-sans)", color: TH.muted, textDecoration: "line-through" } }, fmtMoney(aPrice), aUnit), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-serif)", color: aDiscounted ? TH.accent : TH.ink } }, fmtMoney(aTrade), /* @__PURE__ */ React.createElement("span", { style: { font: "400 9px/1 var(--roma-sans)", color: TH.muted } }, aUnit))) : "\u2014"), on ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid " + TH.accent, flexShrink: 0 } }, /* @__PURE__ */ React.createElement("button", { onClick: () => onSetAccQty(a, aQty - 1), style: accStepBtn }, "\u2212"), /* @__PURE__ */ React.createElement("input", { value: aQty, onChange: (e) => onSetAccQty(a, Math.max(0, parseInt(e.target.value) || 0)), style: {
        width: 34,
        textAlign: "center",
        border: "none",
        borderLeft: "0.5px solid rgba(28,25,23,0.13)",
        borderRight: "0.5px solid rgba(28,25,23,0.13)",
        background: "transparent",
        outline: "none",
        font: "400 13px/1 var(--roma-serif)",
        color: TH.ink,
        padding: "6px 0"
      } }), /* @__PURE__ */ React.createElement("button", { onClick: () => onSetAccQty(a, aQty + 1), style: accStepBtn }, "+")) : /* @__PURE__ */ React.createElement("button", { onClick: () => onToggleAcc(a), style: {
        width: 28,
        height: 28,
        flexShrink: 0,
        cursor: "pointer",
        background: "transparent",
        color: TH.ink,
        border: "0.5px solid rgba(28,25,23,0.2)",
        font: "400 15px/1 var(--roma-sans)"
      } }, "+"));
    })));
  }
  function OneOffVendorPicker({ vendors, vendorId, customVendor, customVendorEmail, onChange, emailOptional, selectStyle, inputStyle, selectClassName, inputClassName }) {
    const [oneOff, setOneOff] = useState(!vendorId && !!customVendor);
    const selValue = vendorId ? String(vendorId) : oneOff ? "__oneoff" : "";
    return /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement(
      "select",
      {
        className: selectClassName,
        value: selValue,
        style: selectStyle,
        onChange: (e) => {
          const val = e.target.value;
          if (val === "__oneoff") {
            setOneOff(true);
            onChange("", customVendor || "", customVendorEmail || "");
          } else {
            setOneOff(false);
            onChange(val, "", "");
          }
        }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, "Select vendor\u2026"),
      /* @__PURE__ */ React.createElement("option", { value: "__oneoff" }, "\u270E", " One-off vendor (not in system)\u2026"),
      vendors.map((v) => /* @__PURE__ */ React.createElement("option", { key: v.id, value: v.id }, v.name, v.public_code ? " \xB7 " + v.public_code : ""))
    ), oneOff && !vendorId && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      "input",
      {
        className: inputClassName,
        type: "text",
        maxLength: 120,
        value: customVendor || "",
        autoFocus: true,
        onChange: (e) => onChange("", e.target.value, customVendorEmail || ""),
        placeholder: "Vendor name \u2014 e.g. Joe\u2019s Stone Yard",
        style: inputStyle
      }
    ), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: inputClassName,
        type: "email",
        maxLength: 200,
        value: customVendorEmail || "",
        onChange: (e) => onChange("", customVendor || "", e.target.value),
        placeholder: emailOptional ? "Vendor email (needed to send a PO)" : "Vendor email * \u2014 their PO is sent here",
        style: inputStyle
      }
    )));
  }
  const isValidVendorEmail = (e) => /^\S+@\S+\.\S+$/.test(String(e || "").trim());
  function RofCatCustomForm({ showCost, vendors, onAdd }) {
    const TH = { ink: "#1c1917", paper: "#ece5d8", accent: "#a87935", muted: "#8a7e68", warm: "#d8cdb6" };
    const fmtMoney = (n) => "$" + parseFloat(n || 0).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const blank = { product_name: "", vendor_id: "", custom_vendor: "", custom_vendor_email: "", sell_by: "unit", num_boxes: 1, unit_price: "", cost: "", description: "" };
    const [f, setF] = useState(blank);
    const set = (k, v) => setF((prev) => ({ ...prev, [k]: v }));
    const price = parseFloat(f.unit_price) || 0;
    const cost = parseFloat(f.cost) || 0;
    const qty = parseInt(f.num_boxes) || 0;
    const ext = price * qty;
    const margin = price > 0 && cost > 0 ? (price - cost) / price * 100 : null;
    const oneOffName = f.custom_vendor.trim();
    const valid = f.product_name.trim() && (f.vendor_id || oneOffName && isValidVendorEmail(f.custom_vendor_email)) && price > 0 && qty > 0;
    const submit = () => {
      if (!valid) return;
      const vendor = vendors.find((v) => String(v.id) === String(f.vendor_id));
      onAdd({
        _cid: Date.now() + Math.random(),
        product_name: f.product_name.trim(),
        vendor_id: f.vendor_id || null,
        custom_vendor: f.vendor_id ? null : oneOffName,
        custom_vendor_email: f.vendor_id ? null : f.custom_vendor_email.trim(),
        vendor_name: vendor ? vendor.name : oneOffName,
        sell_by: f.sell_by,
        num_boxes: qty,
        unit_price: price,
        cost,
        description: f.description.trim(),
        subtotal: ext
      });
      setF(blank);
    };
    const lbl = { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: TH.muted, marginBottom: 7, display: "block" };
    const field = { width: "100%", padding: "10px 12px", border: "0.5px solid rgba(28,25,23,0.2)", background: TH.paper, color: TH.ink, font: "400 14px/1.2 var(--roma-sans)", outline: "none", boxSizing: "border-box" };
    return /* @__PURE__ */ React.createElement("div", { style: { overflowY: "auto", padding: "22px 28px" } }, /* @__PURE__ */ React.createElement("div", { style: { maxWidth: 620 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 20px/1.2 var(--roma-serif)", color: TH.ink, letterSpacing: "-0.01em", marginBottom: 4 } }, "Add a ", /* @__PURE__ */ React.createElement("em", { style: { color: TH.accent } }, "custom"), " line"), /* @__PURE__ */ React.createElement("p", { style: { font: "400 12.5px/1.5 var(--roma-sans)", color: TH.muted, margin: "0 0 22px" } }, "For anything off-catalog ", "\u2014", " special-order material from a vendor we stock or a one-off vendor, a freight or labor charge, a one-off cut. Priced manually; it flows onto the order like any other line and lands on the vendor", "\u2019", "s PO. One-off vendors need an email ", "\u2014", " that", "\u2019", "s where their PO is sent."), /* @__PURE__ */ React.createElement("div", { style: { marginBottom: 16 } }, /* @__PURE__ */ React.createElement("label", { style: lbl }, "Description *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        maxLength: 200,
        value: f.product_name,
        onChange: (e) => set("product_name", e.target.value),
        placeholder: "e.g. Special-order Nero Marquina slab \xB7 book-matched pair",
        style: field
      }
    )), /* @__PURE__ */ React.createElement("div", { style: { marginBottom: 16 } }, /* @__PURE__ */ React.createElement("label", { style: lbl }, "Vendor / source *"), /* @__PURE__ */ React.createElement(
      OneOffVendorPicker,
      {
        vendors,
        vendorId: f.vendor_id,
        customVendor: f.custom_vendor,
        customVendorEmail: f.custom_vendor_email,
        onChange: (vid, cv, cve) => setF((prev) => ({ ...prev, vendor_id: vid, custom_vendor: cv, custom_vendor_email: cve })),
        selectStyle: { ...field, cursor: "pointer" },
        inputStyle: field
      }
    )), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: showCost ? "1fr 1fr 1fr" : "1fr 1fr", gap: 14, marginBottom: 18 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: lbl }, "Quantity *"), /* @__PURE__ */ React.createElement("input", { type: "number", min: "1", value: f.num_boxes, onChange: (e) => set("num_boxes", e.target.value), style: field })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: lbl }, "Unit price *"), /* @__PURE__ */ React.createElement("input", { type: "number", min: "0", step: "0.01", value: f.unit_price, onChange: (e) => set("unit_price", e.target.value), placeholder: "0.00", style: field })), showCost && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: lbl }, "Your cost"), /* @__PURE__ */ React.createElement("input", { type: "number", min: "0", step: "0.01", value: f.cost, onChange: (e) => set("cost", e.target.value), placeholder: "0.00", style: field }))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "stretch", border: "0.5px solid rgba(28,25,23,0.13)", marginBottom: 18 } }, [
      ["Ext. price", ext > 0 ? fmtMoney(ext) : "\u2014", TH.ink],
      ...showCost ? [["Margin", margin != null ? margin.toFixed(0) + "%" : "\u2014 add cost", margin != null ? margin >= 30 ? "#3a7a4e" : margin >= 15 ? "#a87935" : "#c54731" : TH.muted]] : [],
      ["Line", (qty || 0) + " \xD7 " + (price ? fmtMoney(price) : "$0.00"), TH.muted]
    ].map(([l, v, c], i) => /* @__PURE__ */ React.createElement("div", { key: l, style: { flex: 1, padding: "13px 16px", borderLeft: i ? "0.5px solid rgba(28,25,23,0.1)" : "none" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: TH.muted } }, l), /* @__PURE__ */ React.createElement("div", { style: { font: "400 18px/1.1 var(--roma-serif)", color: c, marginTop: 6, letterSpacing: "-0.01em" } }, v)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 10 } }, /* @__PURE__ */ React.createElement("button", { onClick: submit, disabled: !valid, style: {
      padding: "13px 24px",
      cursor: valid ? "pointer" : "not-allowed",
      background: valid ? TH.ink : "rgba(28,25,23,0.2)",
      color: TH.paper,
      border: "none",
      font: "500 11px/1 var(--roma-sans)",
      letterSpacing: "0.08em",
      textTransform: "uppercase"
    } }, "+ Add custom line"), /* @__PURE__ */ React.createElement("button", { onClick: () => setF(blank), style: {
      padding: "13px 20px",
      cursor: "pointer",
      background: "transparent",
      color: TH.ink,
      border: "0.5px solid rgba(28,25,23,0.2)",
      font: "500 11px/1 var(--roma-sans)",
      letterSpacing: "0.06em",
      textTransform: "uppercase"
    } }, "Clear"))));
  }
  function RofCatTray({ discount, overage, onOverageChange, lineList, accList, customList, rugList = [], itemCount, retailSubtotal, discountedTotal, onRemoveItem, onRemoveAcc, onRemoveCustom, onRemoveRug, onClose, onConfirm }) {
    const TH = { ink: "#1c1917", paper: "#ece5d8", accent: "#a87935", muted: "#8a7e68", warm: "#d8cdb6" };
    const mono = (c) => ({ font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: c });
    const fmtMoney = (n) => "$" + parseFloat(n || 0).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const empty = itemCount === 0;
    const xBtn = { width: 18, height: 18, border: "none", background: "transparent", color: TH.muted, cursor: "pointer", font: "400 16px/1 var(--roma-sans)", padding: 0 };
    return /* @__PURE__ */ React.createElement("div", { className: "rof-cat-tray" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 20px 12px", borderBottom: "0.5px solid rgba(28,25,23,0.1)", flexShrink: 0, display: "flex", justifyContent: "space-between", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.ink), letterSpacing: "0.16em" } }, "Adding to order"), /* @__PURE__ */ React.createElement("span", { style: mono(TH.muted) }, itemCount, " ", itemCount === 1 ? "line" : "lines")), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, overflowY: "auto", padding: empty ? 0 : "8px 0" } }, empty && /* @__PURE__ */ React.createElement("div", { style: { padding: "48px 24px", textAlign: "center", color: TH.muted, font: "400 13px/1.6 var(--roma-sans)" } }, "Nothing selected yet.", /* @__PURE__ */ React.createElement("br", null), "Set a quantity on any product ", "\u2014", " accessories tuck in underneath it."), lineList.map(({ item, qty }) => {
      const tUnit = rofTradeUnit(parseFloat(item.price || 0), item.retail_locked, discount);
      return /* @__PURE__ */ React.createElement("div", { key: item.sku_id, style: { padding: "10px 20px", display: "grid", gridTemplateColumns: "30px 1fr auto 18px", gap: 10, alignItems: "center" } }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 80), alt: "", loading: "lazy", style: { width: 30, height: 30, objectFit: "cover", border: "0.5px solid rgba(28,25,23,0.1)" } }) : /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 30, ...materialFace(rofCatKind(item)), border: "0.5px solid rgba(28,25,23,0.1)" } }), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.2 var(--roma-sans)", color: TH.ink, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, item.display_name || item.name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: TH.muted } }, qty, " ", item.price_basis === "per_sqyd" || item.sell_by === "roll" ? "sq yd" : item.sell_by === "unit" || item.sell_by === "piece" ? "units" : "bx", " ", "\xD7", " ", fmtMoney(tUnit))), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-serif)", color: TH.ink, whiteSpace: "nowrap" } }, fmtMoney(rofCatExtOv(item, qty, tUnit, overage))), /* @__PURE__ */ React.createElement("button", { onClick: () => onRemoveItem(item.sku_id), style: xBtn }, "\xD7"));
    }), accList.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "6px 20px 4px", ...mono(TH.muted), fontSize: 9, marginTop: 4 } }, "Accessories"), accList.map(({ acc, qty }) => {
      const aTUnit = rofTradeUnit(parseFloat(acc.retail_price || 0), acc.retail_locked, discount);
      return /* @__PURE__ */ React.createElement("div", { key: acc.sku_id, style: { padding: "8px 20px", display: "grid", gridTemplateColumns: "30px 1fr auto 18px", gap: 10, alignItems: "center" } }, acc.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(acc.primary_image, 80), alt: "", loading: "lazy", style: { width: 30, height: 30, objectFit: "cover", border: "0.5px solid rgba(28,25,23,0.1)" } }) : /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 30, background: TH.paper, border: "0.5px solid rgba(28,25,23,0.1)" } }), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.2 var(--roma-sans)", color: "rgba(28,25,23,0.87)", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, acc.accessory_label || acc.variant_name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: TH.muted } }, qty, " ", "\xD7", " ", fmtMoney(aTUnit))), /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1 var(--roma-serif)", color: TH.ink, whiteSpace: "nowrap" } }, fmtMoney(aTUnit * qty)), /* @__PURE__ */ React.createElement("button", { onClick: () => onRemoveAcc(acc.sku_id), style: xBtn }, "\xD7"));
    }), customList.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "6px 20px 4px", ...mono(TH.muted), fontSize: 9, marginTop: 4 } }, "Custom lines"), customList.map((c) => /* @__PURE__ */ React.createElement("div", { key: c._cid, style: { padding: "8px 20px", display: "grid", gridTemplateColumns: "30px 1fr auto 18px", gap: 10, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 30, border: "0.5px dashed rgba(28,25,23,0.27)", display: "flex", alignItems: "center", justifyContent: "center", font: "400 13px/1 var(--roma-serif)", color: TH.muted } }, "\u270E"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.2 var(--roma-sans)", color: TH.ink, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, c.product_name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: TH.muted } }, c.vendor_name ? c.vendor_name + (c.vendor_public_code ? " \xB7 " + c.vendor_public_code : "") + " \xB7 " : "", c.num_boxes, " ", "\xD7", " ", fmtMoney(c.unit_price))), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-serif)", color: TH.ink, whiteSpace: "nowrap" } }, fmtMoney(c.subtotal)), /* @__PURE__ */ React.createElement("button", { onClick: () => onRemoveCustom(c._cid), style: xBtn }, "\xD7"))), rugList.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "6px 20px 4px", ...mono(TH.muted), fontSize: 9, marginTop: 4 } }, "Custom rugs"), rugList.map((r) => /* @__PURE__ */ React.createElement("div", { key: r._cid, style: { padding: "8px 20px", display: "grid", gridTemplateColumns: "30px 1fr auto 18px", gap: 10, alignItems: "center" } }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(r.primary_image, 80), alt: "", loading: "lazy", style: { width: 30, height: 30, objectFit: "cover", border: "0.5px solid rgba(28,25,23,0.1)" } }) : /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 30, border: "0.5px dashed rgba(28,25,23,0.27)", display: "flex", alignItems: "center", justifyContent: "center", font: "400 13px/1 var(--roma-serif)", color: TH.muted } }, "\u2317"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.2 var(--roma-sans)", color: TH.ink, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, [r.collection, r.color].filter(Boolean).join(" ") || r.product_name, " ", "\xB7", " Rug"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: TH.muted } }, repRugDims(r.custom_width_ft, r.custom_length_ft), r.num_boxes > 1 ? " \xD7 " + r.num_boxes : "", " ", "\xD7", " ", fmtMoney(r.unit_price))), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-serif)", color: TH.ink, whiteSpace: "nowrap" } }, fmtMoney(r.subtotal)), /* @__PURE__ */ React.createElement("button", { onClick: () => onRemoveRug(r._cid), style: xBtn }, "\xD7")))), /* @__PURE__ */ React.createElement("div", { style: { flexShrink: 0, borderTop: "0.5px solid rgba(28,25,23,0.13)", padding: "14px 20px", background: TH.paper } }, typeof onOverageChange === "function" && /* @__PURE__ */ React.createElement("label", { onClick: () => onOverageChange(!overage), role: "switch", "aria-checked": !!overage, style: { display: "flex", alignItems: "center", justifyContent: "space-between", gap: 10, marginBottom: 10, cursor: "pointer" } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { className: "rof-switch" + (overage ? " on" : "") }), /* @__PURE__ */ React.createElement("span", { style: { ...mono(TH.muted), fontSize: 10, letterSpacing: "0.1em" } }, "10% overage")), /* @__PURE__ */ React.createElement("span", { style: { font: "400 11px/1.3 var(--roma-sans)", color: TH.muted, textAlign: "right" } }, "Adds waste factor to sqft materials")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", font: "400 12px/1 var(--roma-sans)", color: TH.muted, marginBottom: 7 } }, /* @__PURE__ */ React.createElement("span", null, "Retail subtotal", overage ? " \xB7 incl. 10% overage" : ""), /* @__PURE__ */ React.createElement("span", null, fmtMoney(retailSubtotal))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", marginBottom: 14 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1 var(--roma-sans)", color: TH.ink } }, discount > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Trade price ", /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", color: TH.accent } }, "\u2212", discount, "%")) : "Total"), /* @__PURE__ */ React.createElement("span", { style: { font: "400 22px/1 var(--roma-serif)", color: TH.ink, letterSpacing: "-0.01em" } }, fmtMoney(discountedTotal))), /* @__PURE__ */ React.createElement("button", { disabled: empty, onClick: onConfirm, style: {
      width: "100%",
      padding: "13px 0",
      marginBottom: 8,
      cursor: empty ? "not-allowed" : "pointer",
      background: empty ? "rgba(28,25,23,0.2)" : TH.ink,
      color: TH.paper,
      border: "none",
      font: "500 12px/1 var(--roma-sans)",
      letterSpacing: "0.08em",
      textTransform: "uppercase"
    } }, empty ? "Select items to add" : "Add " + itemCount + " " + (itemCount === 1 ? "line" : "lines") + " to order \u2192"), /* @__PURE__ */ React.createElement("button", { onClick: onClose, style: {
      width: "100%",
      padding: "11px 0",
      cursor: "pointer",
      background: "transparent",
      color: TH.ink,
      border: "0.5px solid rgba(28,25,23,0.2)",
      font: "500 11px/1 var(--roma-sans)",
      letterSpacing: "0.06em",
      textTransform: "uppercase"
    } }, "Cancel")));
  }
  function RofItemsSection({ data, setData, showCost }) {
    const [skuSearch, setSkuSearch] = useState("");
    const [skuResults, setSkuResults] = useState([]);
    const [skuSearching, setSkuSearching] = useState(false);
    const skuSearchTimer = useRef(null);
    const skuSearchCounter = useRef(0);
    const [addBoxes, setAddBoxes] = useState(1);
    const [rugModalItem, setRugModalItem] = useState(null);
    const [editingKey, setEditingKey] = useState(null);
    const [editField, setEditField] = useState(null);
    const [editValue, setEditValue] = useState("");
    const [catalogOpen, setCatalogOpen] = useState(false);
    const items = data.items || [];
    const qtyLabel = (sellBy) => sellBy === "unit" ? "units" : sellBy === "piece" ? "pcs" : sellBy === "roll" ? "sq yd" : "boxes";
    const slabNeedsSize = (r) => (r.price_basis === "per_sqft" || r.price_basis === "sqft") && (r.sell_by === "unit" || r.sell_by === "piece") && !(parseFloat(r.sqft_per_box) > 0);
    const parseSlabSqft = (raw) => {
      const s = String(raw == null ? "" : raw).trim();
      const m = s.match(/([\d.]+)\s*[x×]\s*([\d.]+)/i);
      if (m) return parseFloat(m[1]) * parseFloat(m[2]) / 144;
      const n = parseFloat(s);
      return n > 0 ? n : 0;
    };
    const searchSku = (q) => {
      setSkuSearch(q);
      if (skuSearchTimer.current) clearTimeout(skuSearchTimer.current);
      const thisSearch = ++skuSearchCounter.current;
      if (q.length < 2) {
        setSkuResults([]);
        setSkuSearching(false);
        return;
      }
      setSkuSearching(true);
      skuSearchTimer.current = setTimeout(async () => {
        try {
          const d = await repFetch("/api/rep/skus/search?q=" + encodeURIComponent(q));
          if (thisSearch !== skuSearchCounter.current) return;
          setSkuResults(d.results || []);
        } catch (e) {
          if (thisSearch !== skuSearchCounter.current) return;
        }
        if (thisSearch === skuSearchCounter.current) setSkuSearching(false);
      }, 250);
    };
    const addSkuItem = (r) => {
      const numBoxes = parseInt(addBoxes) || 1;
      const ppMult = isPerPieceRow(r) ? parseFloat(r.sqft_per_box) || 0 : 1;
      const retailPrice = parseFloat(r.retail_price || 0) * ppMult;
      const rawCost = parseFloat(r.vendor_cost || r.cost || 0) * ppMult;
      const discount = data.customer && data.customer.type === "trade" ? parseFloat(data.customer.discount_percent || 0) : 0;
      const unitPrice = rofTradeUnit(retailPrice, r.retail_locked, discount);
      const sqftPerBox = parseFloat(r.sqft_per_box || 0);
      const sellBy = r.sell_by || "box";
      const isUnit = sellBy === "unit" || sellBy === "piece";
      const isCarpet = r.price_basis === "per_sqyd" || sellBy === "roll";
      const sqftNeeded = isCarpet ? numBoxes * 9 : sqftPerBox ? sqftPerBox * numBoxes : null;
      const needsSlab = slabNeedsSize(r);
      let subtotal;
      if (needsSlab) subtotal = 0;
      else if (isUnit) subtotal = unitPrice * numBoxes;
      else if (isCarpet) subtotal = unitPrice * numBoxes;
      else if (sqftNeeded) subtotal = unitPrice * sqftNeeded;
      else subtotal = unitPrice * numBoxes;
      const newItem = {
        _key: Date.now(),
        sku_id: r.sku_id,
        collection: r.collection || "",
        product_name: r.product_name || "",
        color: r.color || "",
        variant_name: r.variant_name || "",
        variant_type: r.variant_type || "",
        accessory_label: r.accessory_label || "",
        vendor_id: r.vendor_id,
        vendor_name: r.vendor_name,
        vendor_sku: r.vendor_sku || r.internal_sku || "",
        retail_price: needsSlab ? 0 : retailPrice,
        retail_locked: !!r.retail_locked,
        unit_price: needsSlab ? 0 : unitPrice,
        num_boxes: numBoxes,
        subtotal,
        sqft_per_box: sqftPerBox,
        sqft_needed: sqftNeeded,
        _sqft_per_box: sqftPerBox,
        is_sample: false,
        sell_by: sellBy,
        price_basis: r.price_basis || null,
        cost: needsSlab ? 0 : rawCost,
        vendor_cost: parseFloat(r.vendor_cost || 0) * ppMult || null,
        // Carpet-only: kept so the "Custom Area Rug" popup can price the cut
        // (per-sqyd price/cost + roll width). Harmless on non-carpet lines.
        cut_price: r.cut_price != null ? r.cut_price : null,
        cut_cost: r.cut_cost != null ? r.cut_cost : null,
        roll_width_ft: r.roll_width_ft != null ? r.roll_width_ft : null,
        category_name: r.category_name || "",
        primary_image: r.primary_image || null,
        // Slab-size entry: per-sqft cost/retail held here until the rep enters the
        // slab's area, which converts both to per-slab. [[slab-size-entry]]
        _needsSlabSize: needsSlab,
        _perSqftRetail: needsSlab ? retailPrice : null,
        _perSqftCost: needsSlab ? rawCost : null,
        slab_sqft: null
      };
      setData((d) => ({ ...d, items: [...d.items || [], newItem], promoResult: null, promoCode: "" }));
      setSkuSearch("");
      setSkuResults([]);
      setAddBoxes(1);
    };
    useEffect(() => {
      const pending = consumePendingAdd("order");
      if (pending && pending.sku && !items.find((i) => i.sku_id === pending.sku.sku_id)) {
        addSkuItem(pending.sku);
      }
    }, []);
    const openRugModal = (carpetLine) => setRugModalItem(carpetLine);
    const addRugLine = (line) => {
      setData((d) => ({ ...d, items: [...d.items || [], line], promoResult: null, promoCode: "" }));
      setRugModalItem(null);
    };
    const addFromCatalog = ({ lines, accessories, customs }) => {
      const discount = data.customer && data.customer.type === "trade" ? parseFloat(data.customer.discount_percent || 0) : 0;
      setData((d) => {
        const newItems = [...d.items || []];
        let key = Date.now();
        for (const { item: p, qty } of lines) {
          const ppMult = isPerPieceRow(p) ? parseFloat(p.sqft_per_box) || 0 : 1;
          const retailPrice = parseFloat(p.price || 0) * ppMult;
          const rawCost = parseFloat(p.cost || 0) * ppMult;
          const unitPrice = rofTradeUnit(retailPrice, p.retail_locked, discount);
          const sqftPerBox = parseFloat(p.sqft_per_box || 0);
          const sellBy = p.sell_by || "box";
          const isUnit = sellBy === "unit" || sellBy === "piece";
          const isCarpet = p.price_basis === "per_sqyd" || sellBy === "roll";
          const sqftNeeded = isCarpet ? qty * 9 : sqftPerBox ? sqftPerBox * qty : null;
          const needsSlab = slabNeedsSize(p);
          let subtotal;
          if (needsSlab) subtotal = 0;
          else if (isUnit) subtotal = unitPrice * qty;
          else if (isCarpet) subtotal = unitPrice * qty;
          else if (sqftNeeded) subtotal = unitPrice * sqftNeeded;
          else subtotal = unitPrice * qty;
          newItems.push({
            _key: key++,
            sku_id: p.sku_id,
            collection: p.collection || "",
            product_name: p.display_name || p.name || "",
            color: "",
            variant_name: p.variant_name || "",
            vendor_id: p.vendor_id,
            vendor_name: p.brand_name || p.vendor_name,
            vendor_sku: p.vendor_sku || p.internal_sku || "",
            retail_price: needsSlab ? 0 : retailPrice,
            retail_locked: !!p.retail_locked,
            unit_price: needsSlab ? 0 : unitPrice,
            num_boxes: qty,
            subtotal,
            sqft_per_box: sqftPerBox,
            sqft_needed: sqftNeeded,
            _sqft_per_box: sqftPerBox,
            is_sample: false,
            sell_by: sellBy,
            price_basis: p.price_basis || null,
            cost: needsSlab ? 0 : rawCost,
            vendor_cost: parseFloat(p.cost || 0) * ppMult || null,
            category_name: p.category_name || "",
            primary_image: p.primary_image || null,
            _needsSlabSize: needsSlab,
            _perSqftRetail: needsSlab ? retailPrice : null,
            _perSqftCost: needsSlab ? rawCost : null,
            slab_sqft: null
          });
        }
        for (const { acc, parent, qty } of accessories) {
          const retailPrice = parseFloat(acc.retail_price || 0);
          const unitPrice = rofTradeUnit(retailPrice, acc.retail_locked, discount);
          newItems.push({
            _key: key++,
            sku_id: acc.sku_id,
            collection: acc.collection || parent.collection || "",
            product_name: parent.display_name || parent.product_name || parent.name || acc.product_name || "",
            color: acc.color || "",
            variant_name: acc.variant_name || "",
            variant_type: "accessory",
            accessory_label: acc.accessory_label || "",
            vendor_id: parent.vendor_id,
            vendor_name: parent.brand_name || parent.vendor_name,
            vendor_sku: acc.vendor_sku || acc.internal_sku || "",
            retail_price: retailPrice,
            retail_locked: !!acc.retail_locked,
            unit_price: unitPrice,
            num_boxes: qty,
            subtotal: unitPrice * qty,
            sqft_per_box: 0,
            sqft_needed: null,
            is_sample: false,
            sell_by: acc.sell_by || "unit",
            cost: parseFloat(acc.cost || 0),
            vendor_cost: acc.cost || null,
            category_name: acc.category_name || "",
            primary_image: acc.primary_image || null
          });
        }
        for (const c of customs) {
          newItems.push({
            _key: key++,
            sku_id: null,
            collection: "",
            product_name: c.product_name,
            color: "",
            variant_name: "",
            vendor_id: c.vendor_id || null,
            custom_vendor: c.custom_vendor || null,
            custom_vendor_email: c.custom_vendor_email || null,
            vendor_name: c.vendor_name || "",
            vendor_sku: "",
            unit_price: c.unit_price,
            num_boxes: c.num_boxes,
            subtotal: c.subtotal,
            description: c.description || null,
            sell_by: c.sell_by,
            is_custom: true,
            cost: c.cost || 0
          });
        }
        return { ...d, items: newItems, promoResult: null, promoCode: "" };
      });
    };
    const removeItem = (key) => {
      setData((d) => ({ ...d, items: (d.items || []).filter((i) => i._key !== key), promoResult: null, promoCode: "" }));
    };
    const startEdit = (key, field, val) => {
      setEditingKey(key);
      setEditField(field);
      setEditValue(String(val));
    };
    const cancelEdit = () => {
      setEditingKey(null);
      setEditField(null);
      setEditValue("");
    };
    const commitEdit = (itemKey, field, rawValue) => {
      setData((d) => {
        const newItems = (d.items || []).map((item) => {
          if (item._key !== itemKey) return item;
          if (field === "price") {
            const price = parseFloat(rawValue);
            if (isNaN(price) || price < 0) return item;
            const isUnit = item.sell_by === "unit" || item.sell_by === "piece";
            const isCarpet = item.price_basis === "per_sqyd" || item.sell_by === "roll";
            let sub;
            if (isUnit) sub = price * item.num_boxes;
            else if (isCarpet) sub = price * item.num_boxes;
            else if (item.sqft_needed) sub = price * parseFloat(item.sqft_needed);
            else sub = price * item.num_boxes;
            return { ...item, unit_price: price, subtotal: sub };
          } else if (field === "cost") {
            const cost = parseFloat(rawValue);
            if (isNaN(cost) || cost < 0) return item;
            return { ...item, cost };
          } else if (field === "slabsize") {
            const area = parseSlabSqft(rawValue);
            if (!(area > 0)) return item;
            const disc = data.customer && data.customer.type === "trade" ? parseFloat(data.customer.discount_percent || 0) : 0;
            const perSlabRetail = (parseFloat(item._perSqftRetail) || 0) * area;
            const unit = rofTradeUnit(perSlabRetail, item.retail_locked, disc);
            const cost = (parseFloat(item._perSqftCost) || 0) * area;
            return {
              ...item,
              slab_sqft: area,
              retail_price: perSlabRetail,
              unit_price: unit,
              cost,
              subtotal: unit * item.num_boxes,
              _needsSlabSize: false
            };
          } else if (field === "qty") {
            const qty = Math.max(1, Math.round(parseFloat(rawValue) || 1));
            const isUnit = item.sell_by === "unit" || item.sell_by === "piece";
            const isCarpet = item.price_basis === "per_sqyd" || item.sell_by === "roll";
            const sqft = isCarpet ? qty * 9 : item.sqft_per_box ? item.sqft_per_box * qty : 0;
            let sub;
            if (isUnit) sub = item.unit_price * qty;
            else if (isCarpet) sub = item.unit_price * qty;
            else if (sqft) sub = item.unit_price * sqft;
            else sub = item.unit_price * qty;
            return { ...item, num_boxes: qty, sqft_needed: sqft || item.sqft_needed, subtotal: sub };
          }
          return item;
        });
        return { ...d, items: newItems, promoResult: null, promoCode: "" };
      });
      cancelEdit();
    };
    const guessKind = (item) => {
      const cat = (item.category_name || item.collection || "").toLowerCase();
      if (/wood|hardwood|bamboo/.test(cat)) return "wood";
      if (/marble/.test(cat)) return "marble";
      if (/tile|porcelain|ceramic/.test(cat)) return "tile";
      if (/stone|travertine|slate/.test(cat)) return "stone";
      if (/lvp|vinyl|laminate|spc/.test(cat)) return "lvp";
      if (/quartz/.test(cat)) return "quartz";
      if (/carpet/.test(cat)) return "carpet";
      return "tile";
    };
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(WizRepCard, { title: "Line Items" }, /* @__PURE__ */ React.createElement("button", { className: "rof-cat-launch", onClick: () => setCatalogOpen(true) }, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-icon" }, "\u229E"), /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-title" }, "Browse the full catalog"), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-sub" }, "Filter by vendor, set quantities & add accessories in one pass")), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-cta" }, "Open catalog ", "\u2192")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.625rem", marginBottom: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap", style: { flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: skuSearch,
        placeholder: "Quick add \u2014 search SKU or product name...",
        onBlur: () => setTimeout(() => {
          setSkuResults([]);
          setSkuSearching(false);
        }, 200),
        onChange: (e) => searchSku(e.target.value),
        style: { width: "100%", padding: "0.5625rem 0.75rem", border: "0.5px solid rgba(28,25,23,0.18)", background: "#ece5d8", font: "400 0.8125rem/1.4 Inter, sans-serif" }
      }
    ), skuSearching && skuResults.length === 0 && skuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results", style: { padding: "0.75rem", color: "#8a7e68", fontSize: "0.8125rem" } }, "Searching..."), !skuSearching && skuResults.length === 0 && skuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results", style: { padding: "0.75rem", color: "#8a7e68", fontSize: "0.8125rem" } }, "No results"), skuResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, skuResults.slice(0, 20).map((r, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "rof-search-item", onMouseDown: () => addSkuItem(r) }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(r.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover", flexShrink: 0 }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(guessKind({ category_name: r.category_name, collection: r.collection }), 50), flexShrink: 0 } }), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0 } }, formatLineItem(r).brand && /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, formatLineItem(r).brand), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, formatLineItem(r).name), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, formatLineItem(r).sku, r.retail_price ? (formatLineItem(r).sku ? "  \xB7  " : "") + "$" + pieceRetail(r).toFixed(2) + skuPriceSuffix(r) : "")))))), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "number",
        min: "1",
        value: addBoxes,
        onChange: (e) => setAddBoxes(parseInt(e.target.value) || 1),
        placeholder: "Qty",
        style: { width: "60px", padding: "0.5625rem 0.375rem", border: "0.5px solid rgba(28,25,23,0.18)", background: "#ece5d8", font: "400 0.8125rem/1 Inter, sans-serif", textAlign: "center" }
      }
    )), items.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-items-header" + (showCost ? "" : " rof-nocost") }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Product"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Qty"), showCost && /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Cost"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, data.customer && data.customer.type === "trade" ? "Trade Price" : "Price"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Subtotal")), items.map((item) => /* @__PURE__ */ React.createElement("div", { key: item._key, className: "rof-item-row" + (showCost ? "" : " rof-nocost") }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-swatch" }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover" }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(guessKind(item), 50) } })), /* @__PURE__ */ React.createElement("div", { className: "rof-item-info" }, item.is_custom ? (item.vendor_name || item.custom_vendor) && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, item.vendor_name || item.custom_vendor, item.vendor_public_code ? " \xB7 " + item.vendor_public_code : "") : formatLineItem(item).brand && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, formatLineItem(item).brand), /* @__PURE__ */ React.createElement("div", { className: "rof-item-name" }, formatLineItem(item).name), item.is_custom ? /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, "Custom") : formatLineItem(item).sku && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, formatLineItem(item).sku), item._perSqftRetail != null && (editingKey === item._key && editField === "slabsize" ? /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "center", gap: 4, marginTop: 4 } }, /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rof-edit-input",
        type: "text",
        autoFocus: true,
        placeholder: "sq ft or 126x63",
        value: editValue,
        onChange: (e) => setEditValue(e.target.value),
        onKeyDown: (e) => {
          if (e.key === "Enter") commitEdit(item._key, "slabsize", editValue);
          if (e.key === "Escape") cancelEdit();
        },
        onBlur: () => commitEdit(item._key, "slabsize", editValue),
        style: { width: 96 }
      }
    ), /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5625rem", color: "#8a7e68" } }, "@ $", parseFloat(item._perSqftRetail).toFixed(2), "/sf")) : /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-edit-cell",
        onClick: () => startEdit(item._key, "slabsize", item.slab_sqft ? item.slab_sqft.toFixed(1) : ""),
        style: { marginTop: 4, font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.06em", textTransform: "uppercase", padding: "3px 7px", border: "0.5px solid " + (item.slab_sqft ? "rgba(28,25,23,0.2)" : "var(--gold, #c9a668)"), color: item.slab_sqft ? "#8a7e68" : "#8a6d3b", background: item.slab_sqft ? "transparent" : "#faf3e4" }
      },
      item.slab_sqft ? `Slab ${item.slab_sqft.toFixed(1)} sf \xB7 $${parseFloat(item._perSqftRetail).toFixed(2)}/sf` : "\u26A0 Enter slab size"
    ))), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, editingKey === item._key && editField === "qty" ? /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rof-edit-input",
        type: "text",
        inputMode: "numeric",
        autoFocus: true,
        value: editValue,
        onChange: (e) => {
          if (e.target.value === "" || /^\d+$/.test(e.target.value)) setEditValue(e.target.value);
        },
        onKeyDown: (e) => {
          if (e.key === "Enter") commitEdit(item._key, "qty", editValue);
          if (e.key === "Escape") cancelEdit();
        },
        onBlur: () => commitEdit(item._key, "qty", editValue)
      }
    ) : /* @__PURE__ */ React.createElement("button", { className: "rof-edit-cell", onClick: () => startEdit(item._key, "qty", item.num_boxes) }, item.num_boxes, " ", /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5625rem", color: "#8a7e68", marginLeft: "0.1rem" } }, qtyLabel(item.sell_by)))), showCost && /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, editingKey === item._key && editField === "cost" ? /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.8125rem", color: "#8a7e68", marginRight: "0.1rem" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rof-edit-input",
        type: "text",
        inputMode: "decimal",
        autoFocus: true,
        value: editValue,
        onChange: (e) => {
          if (e.target.value === "" || /^\d*\.?\d{0,2}$/.test(e.target.value)) setEditValue(e.target.value);
        },
        onKeyDown: (e) => {
          if (e.key === "Enter") commitEdit(item._key, "cost", editValue);
          if (e.key === "Escape") cancelEdit();
        },
        onBlur: () => commitEdit(item._key, "cost", editValue)
      }
    )) : /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-edit-cell",
        onClick: () => startEdit(item._key, "cost", (item.cost || 0).toFixed(2)),
        style: { color: "#8a7e68" }
      },
      "$",
      (item.cost || 0).toFixed(2)
    )), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, editingKey === item._key && editField === "price" ? /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.8125rem", color: "#8a7e68", marginRight: "0.1rem" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rof-edit-input",
        type: "text",
        inputMode: "decimal",
        autoFocus: true,
        value: editValue,
        onChange: (e) => {
          if (e.target.value === "" || /^\d*\.?\d{0,2}$/.test(e.target.value)) setEditValue(e.target.value);
        },
        onKeyDown: (e) => {
          if (e.key === "Enter") commitEdit(item._key, "price", editValue);
          if (e.key === "Escape") cancelEdit();
        },
        onBlur: () => commitEdit(item._key, "price", editValue)
      }
    )) : /* @__PURE__ */ React.createElement("button", { className: "rof-edit-cell", onClick: () => startEdit(item._key, "price", item.unit_price.toFixed(2)) }, "$", item.unit_price.toFixed(2), /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5rem", color: "#8a7e68", marginLeft: "0.15rem" } }, skuPriceSuffix(item)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.375rem", justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("span", { className: "rof-item-price" }, "$", item.subtotal.toFixed(2)), /* @__PURE__ */ React.createElement("button", { className: "rof-remove-btn", onClick: () => removeItem(item._key), title: "Remove" }, "\u2715")))), items.length === 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-items-empty" }, "No items yet. Search products or add a custom item above.")), catalogOpen && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data,
        showCost,
        orderNumber: data.createdOrder ? data.createdOrder.order_number || "R-" + data.createdOrder.id : null,
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog,
        onAddRug: addRugLine
      }
    ), rugModalItem && /* @__PURE__ */ React.createElement(
      RugConfigModal,
      {
        carpet: rugModalItem,
        showCost,
        onClose: () => setRugModalItem(null),
        onAdd: addRugLine
      }
    ));
  }
  function RofFulfillmentSection({ data, setData }) {
    const [addrForm, setAddrForm] = useState({
      shipping_line1: (data.shippingAddress || {}).line1 || "",
      shipping_line2: (data.shippingAddress || {}).line2 || "",
      shipping_city: (data.shippingAddress || {}).city || "",
      shipping_state: (data.shippingAddress || {}).state || "",
      shipping_zip: (data.shippingAddress || {}).zip || ""
    });
    useEffect(() => {
      setData((d) => ({
        ...d,
        shippingAddress: {
          line1: addrForm.shipping_line1,
          line2: addrForm.shipping_line2,
          city: addrForm.shipping_city,
          state: addrForm.shipping_state,
          zip: addrForm.shipping_zip
        }
      }));
    }, [addrForm.shipping_line1, addrForm.shipping_line2, addrForm.shipping_city, addrForm.shipping_state, addrForm.shipping_zip]);
    const extAddr = data.shippingAddress || {};
    useEffect(() => {
      setAddrForm((f) => {
        if (f.shipping_line1 === (extAddr.line1 || "") && f.shipping_line2 === (extAddr.line2 || "") && f.shipping_city === (extAddr.city || "") && f.shipping_state === (extAddr.state || "") && f.shipping_zip === (extAddr.zip || "")) return f;
        return {
          shipping_line1: extAddr.line1 || "",
          shipping_line2: extAddr.line2 || "",
          shipping_city: extAddr.city || "",
          shipping_state: extAddr.state || "",
          shipping_zip: extAddr.zip || ""
        };
      });
    }, [extAddr.line1, extAddr.line2, extAddr.city, extAddr.state, extAddr.zip]);
    const { addressInputRef, previousAddresses, showPrevAddresses, setShowPrevAddresses, selectPreviousAddress } = useAddressAutofill({
      fieldMap: { line1: "shipping_line1", line2: "shipping_line2", city: "shipping_city", state: "shipping_state", zip: "shipping_zip" },
      setForm: setAddrForm,
      selectedCustomer: data.customer,
      isReadonly: false
    });
    const deliveryMethod = data.deliveryMethod || "pickup";
    const INK = "#1c1917", MUTED = "#8a7e68", ACCENT = "#a87935", WARM = "#d8cdb6", PAPER = "#ece5d8";
    const estShipping = parseFloat(data.estShipping) || 0;
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(WizRepCard, { title: "How is the customer receiving these items?" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: "0.625rem", marginBottom: "0.875rem" } }, [
      {
        id: "pickup",
        icon: "\u25EB",
        label: "In-store pickup",
        sub: "Collect at the Anaheim showroom.",
        cost: "No freight"
      },
      {
        id: "will_call",
        icon: "\u22A1",
        label: "Will-call at distributor",
        sub: "Customer picks up at the distributor.",
        cost: "No freight"
      },
      {
        id: "shipping",
        icon: "\u2933",
        label: "Freight / delivery",
        sub: "Roma truck (local) or LTL freight.",
        cost: estShipping > 0 ? "Est. $" + estShipping.toFixed(2) : "Estimate below"
      }
    ].map((opt) => {
      const on = deliveryMethod === opt.id;
      return /* @__PURE__ */ React.createElement("button", { key: opt.id, onClick: () => setData((d) => ({ ...d, deliveryMethod: opt.id })), style: {
        padding: "0.875rem",
        cursor: "pointer",
        textAlign: "left",
        background: on ? WARM : "transparent",
        border: "0.5px solid " + (on ? INK : "rgba(28,25,23,0.2)"),
        display: "flex",
        flexDirection: "column",
        gap: "0.5rem",
        minHeight: 138,
        transition: "background 120ms, border-color 120ms"
      } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("span", { style: {
        width: 38,
        height: 38,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        font: "400 19px/1 var(--roma-serif)",
        color: INK,
        border: "0.5px solid " + (on ? "rgba(28,25,23,0.4)" : "rgba(28,25,23,0.2)")
      } }, opt.icon), /* @__PURE__ */ React.createElement("span", { style: {
        width: 15,
        height: 15,
        borderRadius: "50%",
        flexShrink: 0,
        border: "1px solid " + (on ? INK : "rgba(28,25,23,0.33)"),
        background: on ? INK : "transparent",
        display: "flex",
        alignItems: "center",
        justifyContent: "center"
      } }, on && /* @__PURE__ */ React.createElement("span", { style: { width: 5, height: 5, borderRadius: "50%", background: PAPER } }))), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 16px/1.2 var(--roma-serif)", color: INK, letterSpacing: "-0.012em" } }, opt.label), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 11.5px/1.5 var(--roma-sans)", color: "rgba(28,25,23,0.7)" } }, opt.sub), /* @__PURE__ */ React.createElement("span", { style: {
        marginTop: "auto",
        paddingTop: "0.25rem",
        font: "500 9px/1 ui-monospace, monospace",
        letterSpacing: "0.16em",
        textTransform: "uppercase",
        color: on ? ACCENT : MUTED
      } }, "\u25CF", " ", opt.cost));
    })), deliveryMethod === "pickup" && /* @__PURE__ */ React.createElement("div", { style: { padding: "0.75rem 1rem", border: "0.5px solid rgba(28,25,23,0.08)", background: "rgba(28,25,23,0.02)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: "0.45rem" } }, "Pickup location"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 0.8125rem/1.3 Inter, sans-serif", color: "#1c1917" } }, "Roma Flooring Designs"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.8125rem/1.5 Inter, sans-serif", color: "#8a7e68", marginTop: "0.2rem" } }, "1440 S. State College Blvd., Suite 6M", /* @__PURE__ */ React.createElement("br", null), "Anaheim, CA 92806")), deliveryMethod === "will_call" && /* @__PURE__ */ React.createElement("div", { style: { padding: "0.75rem 1rem", border: "0.5px solid rgba(28,25,23,0.08)", background: "rgba(28,25,23,0.02)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: "0.45rem" } }, "How will-call works"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.8125rem/1.5 Inter, sans-serif", color: "#8a7e68" } }, "No freight is charged. Once the order is paid and the distributor PO is sent, open the order and use ", /* @__PURE__ */ React.createElement("b", { style: { color: "#1c1917" } }, "Release materials"), " to print the customer\u2019s will-call form \u2014 it carries the PO number the distributor releases against.")), deliveryMethod === "shipping" && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-field full" }, /* @__PURE__ */ React.createElement("label", null, "Address Line 1 *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        ref: addressInputRef,
        type: "text",
        placeholder: "Street address",
        value: addrForm.shipping_line1,
        onChange: (e) => setAddrForm((f) => ({ ...f, shipping_line1: e.target.value }))
      }
    ), previousAddresses.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.3rem" } }, /* @__PURE__ */ React.createElement(
      "span",
      {
        style: { font: "400 0.6875rem/1 Inter, sans-serif", color: "#8a7e68", cursor: "pointer", textDecoration: "underline" },
        onClick: () => setShowPrevAddresses((s) => !s)
      },
      showPrevAddresses ? "Hide" : "Use",
      " previous address (",
      previousAddresses.length,
      ")"
    ), showPrevAddresses && /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.12)", marginTop: "0.3rem" } }, previousAddresses.map((addr, i) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: i,
        style: { padding: "0.4rem 0.75rem", cursor: "pointer", borderBottom: "0.5px solid rgba(28,25,23,0.06)" },
        onClick: () => selectPreviousAddress(addr),
        onMouseEnter: (e) => e.currentTarget.style.background = "rgba(28,25,23,0.04)",
        onMouseLeave: (e) => e.currentTarget.style.background = "transparent"
      },
      /* @__PURE__ */ React.createElement("div", { style: { font: "500 0.8125rem/1.3 Inter, sans-serif" } }, addr.line1),
      /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.6875rem/1.3 Inter, sans-serif", color: "#8a7e68" } }, [addr.city, addr.state, addr.zip].filter(Boolean).join(", "))
    ))))), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Address Line 2"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: addrForm.shipping_line2,
        onChange: (e) => setAddrForm((f) => ({ ...f, shipping_line2: e.target.value }))
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row-3" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "City *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: addrForm.shipping_city,
        onChange: (e) => setAddrForm((f) => ({ ...f, shipping_city: e.target.value }))
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "State *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: addrForm.shipping_state,
        maxLength: 2,
        onChange: (e) => setAddrForm((f) => ({ ...f, shipping_state: e.target.value }))
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "ZIP *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: addrForm.shipping_zip,
        maxLength: 10,
        onChange: (e) => setAddrForm((f) => ({ ...f, shipping_zip: e.target.value }))
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Delivery Window"), /* @__PURE__ */ React.createElement("select", { value: data.deliveryWindow || "", onChange: (e) => setData((d) => ({ ...d, deliveryWindow: e.target.value })) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "Anytime"), /* @__PURE__ */ React.createElement("option", { value: "Morning \xB7 8a\u201312p" }, "Morning \xB7 8a\u201312p"), /* @__PURE__ */ React.createElement("option", { value: "Afternoon \xB7 12p\u20134p" }, "Afternoon \xB7 12p\u20134p"), /* @__PURE__ */ React.createElement("option", { value: "Evening \xB7 4p\u20137p" }, "Evening \xB7 4p\u20137p"))), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Preferred Carrier"), /* @__PURE__ */ React.createElement("select", { value: data.carrierPreference || "", onChange: (e) => setData((d) => ({ ...d, carrierPreference: e.target.value })) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "No preference"), /* @__PURE__ */ React.createElement("option", { value: "fedex_freight" }, "FedEx Freight"), /* @__PURE__ */ React.createElement("option", { value: "ups_freight" }, "UPS Freight"), /* @__PURE__ */ React.createElement("option", { value: "estes" }, "Estes Express"), /* @__PURE__ */ React.createElement("option", { value: "old_dominion" }, "Old Dominion"), /* @__PURE__ */ React.createElement("option", { value: "xpo" }, "XPO Logistics"), /* @__PURE__ */ React.createElement("option", { value: "roma_truck" }, "Roma Truck"), /* @__PURE__ */ React.createElement("option", { value: "other" }, "Other")))), /* @__PURE__ */ React.createElement("div", { style: {
      margin: "0.25rem -1.25rem -1.25rem",
      padding: "0.75rem 1.25rem",
      background: WARM,
      borderTop: "0.5px solid rgba(28,25,23,0.12)",
      display: "flex",
      justifyContent: "space-between",
      alignItems: "center",
      gap: "1rem"
    } }, /* @__PURE__ */ React.createElement("span", { style: {
      font: "500 9px/1.4 ui-monospace, monospace",
      letterSpacing: "0.14em",
      textTransform: "uppercase",
      color: MUTED
    } }, "\u25CF", " ", data.deliveryWindow ? "Delivery window \xB7 " + data.deliveryWindow : "Charged to the customer \xB7 added to the order total"), /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "baseline", gap: "0.625rem", flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: MUTED } }, "Freight estimate"), /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 16px/1 var(--roma-serif)", color: INK } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        inputMode: "decimal",
        value: data.estShipping || "",
        placeholder: "0.00",
        onChange: (e) => {
          if (e.target.value === "" || /^\d*\.?\d{0,2}$/.test(e.target.value)) setData((d) => ({ ...d, estShipping: e.target.value }));
        },
        style: {
          width: 76,
          border: "none",
          borderBottom: "0.5px solid rgba(28,25,23,0.33)",
          background: "transparent",
          outline: "none",
          font: "400 18px/1 var(--roma-serif)",
          color: INK,
          padding: "0 0 2px",
          textAlign: "right"
        }
      }
    )))))));
  }
  function RofPromoField({ data, setData, onCancel }) {
    const [code, setCode] = useState(data.promoCode || "");
    const [err, setErr] = useState("");
    const [loading, setLoading] = useState(false);
    const items = data.items || [];
    const apply = async () => {
      if (!code.trim() || items.length === 0) return;
      setLoading(true);
      setErr("");
      try {
        const promoItems = items.map((i) => ({ sku_id: i.sku_id, product_id: i.product_id || null, subtotal: i.subtotal, is_sample: false }));
        const customerEmail = data.customer ? data.customer.email : data.newCustomerEmail || "";
        const r = await repFetch("/api/rep/promo-codes/validate", {
          method: "POST",
          body: JSON.stringify({ code, items: promoItems, customer_email: customerEmail })
        });
        if (r.valid) {
          setData((d) => ({ ...d, promoResult: r, promoCode: code }));
          setErr("");
        } else setErr(r.error || "Invalid promo code");
      } catch (e) {
        setErr(e.message);
      }
      setLoading(false);
    };
    const remove = () => {
      setData((d) => ({ ...d, promoResult: null, promoCode: "" }));
      setCode("");
      setErr("");
      if (onCancel) onCancel();
    };
    return /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label", style: { marginBottom: 0 } }, "Promo code"), onCancel && !data.promoResult && /* @__PURE__ */ React.createElement("button", { onClick: onCancel, style: { background: "none", border: "none", padding: 0, cursor: "pointer", font: "500 0.5rem/1 ui-monospace, monospace", letterSpacing: "0.06em", textTransform: "uppercase", color: "#8a7e68" } }, "\u2190 Back")), data.promoResult ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", gap: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "600 0.6875rem/1 ui-monospace, monospace", color: "var(--green)", letterSpacing: "0.04em" } }, data.promoResult.code), /* @__PURE__ */ React.createElement("span", { style: { font: '300 0.9rem/1 "Cormorant Garamond", serif', color: "var(--green)" } }, "-$", data.promoResult.discount_amount.toFixed(2)), /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light danger", onClick: remove, style: { padding: "0.2rem 0.45rem", fontSize: "0.5rem" } }, "Remove")) : /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6 } }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 50,
        value: code,
        onChange: (e) => {
          setCode(e.target.value.toUpperCase());
          setErr("");
        },
        onKeyDown: (e) => e.key === "Enter" && apply(),
        placeholder: "Enter code",
        style: { flex: 1, minWidth: 0, padding: "6px 8px", border: "0.5px solid rgba(28,25,23,0.3)", background: "#fff", font: "400 0.75rem/1 Inter, sans-serif", color: "var(--rod-ink)", outline: "none" }
      }
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light primary",
        onClick: apply,
        disabled: loading || !code.trim() || items.length === 0,
        style: { padding: "0.4rem 0.6rem" }
      },
      loading ? "..." : "Apply"
    )), err && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red)", fontSize: "0.6875rem", marginTop: "0.35rem" } }, err));
  }
  function RofDiscountField({ data, setData, onCancel }) {
    const itemsSubtotal = (data.items || []).reduce((s, i) => s + (i.subtotal || 0), 0);
    const type = data.manualDiscountType || "fixed";
    const raw = parseFloat(data.manualDiscountValue);
    const amount = !isNaN(raw) && raw > 0 ? Math.min(type === "percent" ? itemsSubtotal * Math.min(raw, 100) / 100 : raw, itemsSubtotal) : 0;
    const back = () => {
      setData((d) => ({ ...d, manualDiscountValue: "" }));
      if (onCancel) onCancel();
    };
    if (data.promoResult) {
      return /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label" }, "Manual discount"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.6875rem", color: "#8a7e68" } }, "Remove the promo code to enter a manual discount."));
    }
    return /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label", style: { marginBottom: 0 } }, "Manual discount"), onCancel && /* @__PURE__ */ React.createElement("button", { onClick: back, style: { background: "none", border: "none", padding: 0, cursor: "pointer", font: "500 0.5rem/1 ui-monospace, monospace", letterSpacing: "0.06em", textTransform: "uppercase", color: "#8a7e68" } }, "\u2190 Back")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, marginTop: 6 } }, /* @__PURE__ */ React.createElement(
      "select",
      {
        value: type,
        onChange: (e) => setData((d) => ({ ...d, manualDiscountType: e.target.value })),
        style: { flex: "0 0 52px", padding: "6px 4px", border: "0.5px solid rgba(28,25,23,0.3)", background: "#fff", font: "400 0.75rem/1 Inter, sans-serif", color: "var(--rod-ink)", outline: "none" }
      },
      /* @__PURE__ */ React.createElement("option", { value: "fixed" }, "$"),
      /* @__PURE__ */ React.createElement("option", { value: "percent" }, "%")
    ), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "number",
        min: "0",
        step: "0.01",
        value: data.manualDiscountValue || "",
        onChange: (e) => setData((d) => ({ ...d, manualDiscountValue: e.target.value })),
        placeholder: type === "percent" ? "% off subtotal" : "$ off total",
        style: { flex: 1, minWidth: 0, padding: "6px 8px", border: "0.5px solid rgba(28,25,23,0.3)", background: "#fff", font: "400 0.75rem/1 Inter, sans-serif", color: "var(--rod-ink)", outline: "none" }
      }
    )), amount > 0 && /* @__PURE__ */ React.createElement("div", { style: { font: '300 0.9rem/1 "Cormorant Garamond", serif', color: "var(--green)", marginTop: 4, textAlign: "right" } }, "-$", amount.toFixed(2)));
  }
  function RofDiscountControls({ data, setData }) {
    const hasManual = !data.promoResult && parseFloat(data.manualDiscountValue) > 0;
    const [mode, setMode] = useState(data.promoResult ? "promo" : hasManual ? "manual" : null);
    useEffect(() => {
      if (data.promoResult) setMode("promo");
    }, [data.promoResult]);
    if (mode === "promo") return /* @__PURE__ */ React.createElement(RofPromoField, { data, setData, onCancel: () => setMode(null) });
    if (mode === "manual") return /* @__PURE__ */ React.createElement(RofDiscountField, { data, setData, onCancel: () => setMode(null) });
    return /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label" }, "Discount"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6 } }, /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", onClick: () => setMode("promo"), style: { flex: 1, padding: "0.4rem 0.6rem" } }, "Promo code"), /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", onClick: () => setMode("manual"), style: { flex: 1, padding: "0.4rem 0.6rem" } }, "Manual discount")));
  }
  function RofRunningSummary({ data, setData, step }) {
    const itemsSubtotal = (data.items || []).reduce((sum, i) => sum + (i.subtotal || 0), 0);
    const manualRaw = parseFloat(data.manualDiscountValue);
    const manualDiscount = !data.promoResult && !isNaN(manualRaw) && manualRaw > 0 ? Math.min(data.manualDiscountType === "percent" ? itemsSubtotal * Math.min(manualRaw, 100) / 100 : manualRaw, itemsSubtotal) : 0;
    const discount = data.promoResult ? data.promoResult.discount_amount : manualDiscount;
    const taxableSubtotal = Math.max(0, itemsSubtotal - discount);
    const destZip = data.deliveryMethod === "pickup" ? "92806" : data.shippingAddress && data.shippingAddress.zip ? data.shippingAddress.zip : "92806";
    const taxRate = caTaxRate(destZip);
    const estTax = parseFloat((taxableSubtotal * taxRate).toFixed(2));
    const estShipping = data.deliveryMethod === "shipping" ? parseFloat(data.estShipping) || 0 : 0;
    const total = taxableSubtotal + estTax + estShipping;
    const paymentCollected = (data.paymentMethods || []).reduce((sum, p) => sum + (parseFloat(p.amount) || 0), 0);
    const statusLabel = step < 3 ? "Draft" : "Live";
    const statusClass = step < 3 ? "draft" : "live";
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-summary" }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-title" }, "Order so far"), (data.customer || data.newCustomerName) && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label" }, "Customer"), /* @__PURE__ */ React.createElement("div", { className: "rof-summary-value" }, data.customer ? data.customer.name : data.newCustomerName || "\u2014", (data.customer ? data.customer.email : data.newCustomerEmail) && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.6875rem", color: "#8a7e68" } }, data.customer ? data.customer.email : data.newCustomerEmail), data.customer && data.customer.type === "trade" && /* @__PURE__ */ React.createElement("span", { className: "rof-search-tag trade", style: { display: "inline-block", marginTop: "0.2rem", fontSize: "0.4375rem" } }, "Trade"), !data.customer && data.newCustomerName && /* @__PURE__ */ React.createElement("span", { style: { font: "500 0.4375rem/1 ui-monospace, monospace", letterSpacing: "0.06em", textTransform: "uppercase", color: "#8a7e68", background: "rgba(28,25,23,0.06)", padding: "0.1rem 0.3rem", display: "inline-block", marginTop: "0.2rem" } }, "New"))), data.items && data.items.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label" }, data.items.length, " Item", data.items.length !== 1 ? "s" : ""), data.items.slice(0, 5).map((item, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", { style: { minWidth: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", paddingRight: "0.5rem" } }, formatLineItem(item).name || "Item"), /* @__PURE__ */ React.createElement("span", { style: { flexShrink: 0, font: '300 0.875rem/1 "Cormorant Garamond", serif' } }, "$", item.subtotal.toFixed(2)))), data.items.length > 5 && /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.6875rem/1 Inter, sans-serif", color: "#8a7e68", paddingTop: "0.2rem" } }, "+", data.items.length - 5, " more")), step >= 1 && data.deliveryMethod && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label" }, "Delivery"), /* @__PURE__ */ React.createElement("div", { className: "rof-summary-value" }, data.deliveryMethod === "will_call" ? "Will-call at distributor" : data.deliveryMethod === "pickup" ? "In-store pickup" : data.deliveryMethod === "shipping" ? "Freight / delivery" : data.deliveryMethod)), step >= 2 && data.paymentMethods && data.paymentMethods.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section" }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-label" }, "Payment"), data.paymentMethods.map((pm, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", { style: { textTransform: "capitalize" } }, pm.method), /* @__PURE__ */ React.createElement("span", null, "$", parseFloat(pm.amount).toFixed(2))))), data.items && data.items.length > 0 && step < 3 && /* @__PURE__ */ React.createElement(RofDiscountControls, { data, setData }), /* @__PURE__ */ React.createElement("div", { className: "rof-summary-section", style: { marginBottom: 0 } }, itemsSubtotal > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", null, "Subtotal"), /* @__PURE__ */ React.createElement("span", null, "$", itemsSubtotal.toFixed(2))), discount > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line", style: { color: "var(--green)" } }, /* @__PURE__ */ React.createElement("span", null, "Discount"), /* @__PURE__ */ React.createElement("span", null, "-$", discount.toFixed(2))), estShipping > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", null, "Freight"), /* @__PURE__ */ React.createElement("span", null, "$", estShipping.toFixed(2))), taxableSubtotal > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", null, "Est. Tax ", /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5rem", color: "#8a7e68" } }, "(", (taxRate * 100).toFixed(2), "%)")), /* @__PURE__ */ React.createElement("span", null, "$", estTax.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "rof-summary-total" }, /* @__PURE__ */ React.createElement("span", { className: "rof-summary-total-label" }, "Est. Total"), /* @__PURE__ */ React.createElement("span", { className: "rof-summary-total-amount" }, "$", total.toFixed(2))), /* @__PURE__ */ React.createElement("span", { className: "rof-summary-pill " + statusClass }, statusLabel))), /* @__PURE__ */ React.createElement("div", { className: "rof-context" }, step === 1 && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rof-context-title" }, "What the customer sees"), /* @__PURE__ */ React.createElement("div", { className: "rof-context-text" }, "Nothing yet. The order is a draft until payment is processed in Step 2.")), step === 2 && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rof-context-title" }, "Balance"), /* @__PURE__ */ React.createElement("div", { className: "rof-context-text" }, paymentCollected >= total - 0.01 ? "Fully covered. Ready to process." : `$${(total - paymentCollected).toFixed(2)} remaining to collect.`)), step === 3 && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rof-context-title" }, "Margin check"), /* @__PURE__ */ React.createElement("div", { className: "rof-context-text" }, (() => {
      const cost = (data.items || []).reduce((s, i) => {
        const c = i.cost || 0;
        if (i.sell_by === "unit" || i.sell_by === "piece") return s + c * i.num_boxes;
        return s + c * (parseFloat(i.sqft_needed) || i.num_boxes);
      }, 0);
      const margin = total > 0 ? (total - cost) / total * 100 : 0;
      return `Overall margin: ${margin.toFixed(1)}%`;
    })()))));
  }
  function buildRepOrderBody(data, primaryTender) {
    const customerName = data.customer ? data.customer.name : data.newCustomerName || "";
    const customerEmail = data.customer ? data.customer.email : data.newCustomerEmail || "";
    const phone = data.customer ? data.customer.phone : data.newCustomerPhone || "";
    const companyName = data.customer ? data.customer.company_name || "" : data.newCustomerCompany || "";
    const orderItems = (data.items || []).map((i) => {
      if (i.is_custom_rug && i.sku_id) return { sku_id: i.sku_id, num_boxes: i.num_boxes, is_custom_rug: true, custom_width_ft: i.custom_width_ft, custom_length_ft: i.custom_length_ft };
      if (i.sku_id) return { sku_id: i.sku_id, num_boxes: i.num_boxes, sqft_needed: i.sqft_needed, unit_price: i.unit_price, cost: i.cost != null ? i.cost : void 0, sell_by: i.sell_by, slab_sqft: i.slab_sqft || void 0 };
      return { product_name: i.product_name, unit_price: i.unit_price, vendor_id: i.vendor_id || null, custom_vendor: i.custom_vendor || void 0, custom_vendor_email: i.custom_vendor_email || void 0, cost: i.cost || void 0, num_boxes: i.num_boxes, description: i.description, sell_by: i.sell_by };
    });
    const body = {
      customer_name: customerName,
      customer_email: customerEmail,
      phone: phone || void 0,
      company_name: companyName || void 0,
      delivery_method: data.deliveryMethod,
      payment_method: primaryTender ? primaryTender.method : "card",
      payment_amount: primaryTender ? primaryTender.amount : void 0,
      items: orderItems,
      promo_code: data.promoResult ? data.promoResult.code : void 0,
      stripe_payment_intent_id: primaryTender && primaryTender.stripePaymentIntentId || data.stripePaymentIntentId || void 0,
      card_txn: primaryTender && primaryTender.card_txn || void 0,
      document_ids: data.documentIds || void 0,
      check_number: primaryTender && primaryTender.checkNumber || void 0,
      notes: data.notes || void 0,
      invoice_notes: (data.invoiceNotes || "").trim() || void 0,
      job_name: (data.jobName || "").trim() || void 0,
      manual_discount_type: !data.promoResult && parseFloat(data.manualDiscountValue) > 0 ? data.manualDiscountType || "fixed" : void 0,
      manual_discount_value: !data.promoResult && parseFloat(data.manualDiscountValue) > 0 ? parseFloat(data.manualDiscountValue) : void 0
    };
    if (data.deliveryMethod === "shipping" && data.shippingAddress) {
      body.shipping_address = {
        line1: data.shippingAddress.line1,
        line2: data.shippingAddress.line2 || void 0,
        city: data.shippingAddress.city,
        state: data.shippingAddress.state,
        zip: data.shippingAddress.zip
      };
      const freight = parseFloat(data.estShipping) || 0;
      if (freight > 0) body.shipping_cost = freight;
      const addonLabels = { liftgate: "Liftgate", insideDelivery: "Inside delivery", callBefore: "Call before delivery", appointmentRequired: "Appointment required" };
      const dn = [];
      if (data.deliveryWindow) dn.push("Window: " + data.deliveryWindow);
      if (data.carrierPreference) dn.push("Carrier: " + data.carrierPreference.replace(/_/g, " "));
      const addons = Object.entries(data.fulfillmentAddons || {}).filter(([, v]) => v).map(([k]) => addonLabels[k] || k);
      if (addons.length) dn.push("Add-ons: " + addons.join(", "));
      if (data.deliveryNotes) dn.push(data.deliveryNotes);
      if (dn.length) body.delivery_notes = dn.join(" \xB7 ");
    }
    return body;
  }
  function RofPaymentStep({ data, setData, setStep, terminalRef, terminalStatus, connectReader, onAutoPlace }) {
    const [showPayCard, setShowPayCard] = useState(false);
    const [showAchModal, setShowAchModal] = useState(false);
    const [cashDrawerOpen, setCashDrawerOpen] = useState(null);
    const [collectNow, setCollectNow] = useState("");
    const [pendingCardAmount, setPendingCardAmount] = useState(0);
    useEffect(() => {
      repFetch("/api/rep/cash-drawer/current").then((d) => setCashDrawerOpen(!!d.drawer)).catch(() => setCashDrawerOpen(false));
    }, []);
    const paymentMethod = data.paymentMethod || "";
    const itemsSubtotal = (data.items || []).reduce((s, i) => s + i.subtotal, 0);
    const discount = data.promoResult ? data.promoResult.discount_amount : 0;
    const taxableSubtotal = Math.max(0, itemsSubtotal - discount);
    const destZip = data.deliveryMethod !== "shipping" ? "92806" : data.shippingAddress && data.shippingAddress.zip ? data.shippingAddress.zip : "92806";
    const taxRate = caTaxRate(destZip);
    const estTax = parseFloat((itemsSubtotal * taxRate).toFixed(2));
    const estShipping = data.deliveryMethod === "shipping" ? parseFloat(data.estShipping) || 0 : 0;
    const total = taxableSubtotal + estTax + estShipping;
    const paymentMethods = data.paymentMethods || [];
    const collectedAmount = paymentMethods.reduce((s, p) => s + (parseFloat(p.amount) || 0), 0);
    const remaining = total - collectedAmount;
    const defaultCollect = remaining > 0 ? parseFloat(remaining.toFixed(2)) : 0;
    const collectAmt = (() => {
      if (collectNow === "") return defaultCollect;
      const v = parseFloat(collectNow);
      if (isNaN(v) || v <= 0) return 0;
      return Math.min(parseFloat(v.toFixed(2)), defaultCollect);
    })();
    const isDeposit = collectAmt > 0.01 && collectAmt < defaultCollect - 0.01;
    const selectMethod = (method) => setData((d) => ({ ...d, paymentMethod: method }));
    const handleCardPayment = () => {
      setPendingCardAmount(collectAmt);
      setShowPayCard(true);
    };
    const createdOrderRef = useRef(data.createdOrder ? data.createdOrder.id : null);
    const orderKeyRef = useRef(null);
    const orderKey = () => {
      if (!orderKeyRef.current) orderKeyRef.current = "wiz" + Date.now().toString(36) + Math.random().toString(36).slice(2, 10);
      return orderKeyRef.current;
    };
    const persistTender = async (tender) => {
      const existingId = createdOrderRef.current;
      if (existingId) {
        if (!["cash", "card", "check"].includes(tender.method)) return { ...tender, persisted: false };
        await repFetch("/api/rep/orders/" + existingId + "/collect-payment", {
          method: "POST",
          body: JSON.stringify({
            payment_method: tender.method,
            amount: tender.amount,
            card_txn: tender.card_txn || void 0,
            stripe_payment_intent_id: tender.stripePaymentIntentId || void 0,
            check_number: tender.checkNumber || void 0,
            defer_confirm: true
          })
        });
        return { ...tender, persisted: true };
      }
      const body = buildRepOrderBody(data, tender);
      body.finalize = false;
      body.idempotency_key = orderKey();
      const result = await repFetch("/api/rep/orders", { method: "POST", body: JSON.stringify(body) });
      if (!result.order) throw new Error(result.error || "Could not save the order");
      createdOrderRef.current = result.order.id;
      setData((d) => ({ ...d, createdOrder: result.order }));
      return { ...tender, persisted: true };
    };
    const handleCardSuccess = async (res) => {
      const amount = pendingCardAmount || (remaining > 0 ? remaining : total);
      setCollectNow("");
      let tender = { method: "card", amount };
      if (res && res.card_txn) tender.card_txn = res.card_txn;
      else if (res && res.payment_intent_id) tender.stripePaymentIntentId = res.payment_intent_id;
      try {
        tender = await persistTender(tender);
      } catch (e) {
        alert("Card captured, but saving it to the order failed: " + (e.message || "") + "\nIt will be retried when you Process the order.");
      }
      setData((d) => ({
        ...d,
        paymentMethods: [...d.paymentMethods || [], tender],
        stripePaymentIntentId: res && res.payment_intent_id || d.stripePaymentIntentId || null
      }));
      if (onAutoPlace && amount >= remaining - 0.01 && (data.items || []).length > 0) {
        onAutoPlace();
      }
    };
    const handleAchSuccess = (piId, documentIds) => {
      const amount = collectAmt > 0 ? collectAmt : remaining > 0 ? remaining : total;
      setCollectNow("");
      setData((d) => ({
        ...d,
        paymentMethods: [...d.paymentMethods || [], { method: "ach", amount, stripePaymentIntentId: piId }],
        stripePaymentIntentId: piId,
        documentIds
      }));
      setShowAchModal(false);
    };
    const setCashPayment = async () => {
      if (cashDrawerOpen === false || collectAmt <= 0) return;
      const amount = collectAmt;
      setCollectNow("");
      let tender = { method: "cash", amount };
      try {
        tender = await persistTender(tender);
      } catch (e) {
        alert("Saving the cash payment to the order failed: " + (e.message || ""));
      }
      setData((d) => ({ ...d, paymentMethods: [...d.paymentMethods || [], tender] }));
    };
    const setCheckPayment = async () => {
      if (!data.checkNumber || !data.checkNumber.trim() || collectAmt <= 0) return;
      const amount = collectAmt;
      const checkNumber = data.checkNumber;
      setCollectNow("");
      let tender = { method: "check", amount, checkNumber };
      try {
        tender = await persistTender(tender);
      } catch (e) {
        alert("Saving the check payment to the order failed: " + (e.message || ""));
      }
      setData((d) => ({ ...d, paymentMethods: [...d.paymentMethods || [], tender] }));
    };
    const setStripeRequest = () => {
      if (collectAmt <= 0) return;
      const amount = collectAmt;
      setCollectNow("");
      setData((d) => ({ ...d, paymentMethods: [...d.paymentMethods || [], { method: "stripe", amount }] }));
    };
    const removePayment = (idx) => {
      const pm = (data.paymentMethods || [])[idx];
      if (pm && pm.persisted) {
        alert("This payment was already captured and recorded on the order. To reverse it, refund it from the order\u2019s Payments section.");
        return;
      }
      setData((d) => ({ ...d, paymentMethods: (d.paymentMethods || []).filter((_, i) => i !== idx), stripePaymentIntentId: null }));
    };
    const customerName = data.customer ? data.customer.name : data.newCustomerName || "";
    const customerEmail = data.customer ? data.customer.email : data.newCustomerEmail || "";
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Reviewing payment for",
        actions: /* @__PURE__ */ React.createElement("button", { className: "rof-card-action", onClick: () => setStep && setStep(1) }, "Edit order ", "\u2192")
      },
      /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1.3fr 1fr 1fr", gap: "1.25rem", alignItems: "flex-start" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "46px 1fr", gap: "0.75rem", alignItems: "center", minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: {
        width: 46,
        height: 46,
        background: "#d8cdb6",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        font: "400 17px/1 var(--roma-serif)",
        color: "#1c1917"
      } }, (customerName || "?").split(" ").map((w) => w[0]).slice(0, 2).join("").toUpperCase()), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 16px/1.2 var(--roma-serif)", color: "#1c1917", letterSpacing: "-0.012em" } }, customerName), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: "#8a7e68", textTransform: "uppercase", marginTop: 2, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, data.customer && data.customer.type === "trade" ? "Trade" : "Retail", " ", "\xB7", " ", customerEmail))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", color: "#8a7e68", textTransform: "uppercase", marginBottom: 4 } }, "Items ", "\xB7", " ", (data.items || []).length), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.5 var(--roma-sans)", color: "#1c1917", letterSpacing: "-0.003em" } }, (data.items || []).slice(0, 5).map((i) => i.num_boxes + "\xD7" + (i.vendor_sku || i.collection || i.product_name || "item")).join(" \xB7 "), (data.items || []).length > 5 ? " \xB7 +" + ((data.items || []).length - 5) + " more" : "")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", color: "#8a7e68", textTransform: "uppercase", marginBottom: 4 } }, "Fulfillment"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.5 var(--roma-sans)", color: "#1c1917" } }, data.deliveryMethod === "pickup" ? "Pickup \xB7 Roma Flooring, Anaheim" : data.shippingAddress && data.shippingAddress.city ? "Freight \xB7 " + data.shippingAddress.city + (data.deliveryWindow ? " \xB7 " + data.deliveryWindow : "") : "Freight", estShipping > 0 ? " \xB7 $" + estShipping.toFixed(2) : ""))),
      /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.875rem", paddingTop: "0.625rem", borderTop: "0.5px solid rgba(28,25,23,0.1)" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", null, "Subtotal"), /* @__PURE__ */ React.createElement("span", { style: { font: '300 0.875rem/1 "Cormorant Garamond", serif' } }, "$", itemsSubtotal.toFixed(2))), discount > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line", style: { color: "var(--green)" } }, /* @__PURE__ */ React.createElement("span", null, "Discount"), /* @__PURE__ */ React.createElement("span", { style: { font: '300 0.875rem/1 "Cormorant Garamond", serif' } }, "-$", discount.toFixed(2))), estShipping > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", null, "Freight"), /* @__PURE__ */ React.createElement("span", { style: { font: '300 0.875rem/1 "Cormorant Garamond", serif' } }, "$", estShipping.toFixed(2))), estTax > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-summary-line" }, /* @__PURE__ */ React.createElement("span", null, "Est. Tax ", /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5rem", color: "#8a7e68" } }, "(", (taxRate * 100).toFixed(2), "%)")), /* @__PURE__ */ React.createElement("span", { style: { font: '300 0.875rem/1 "Cormorant Garamond", serif' } }, "$", estTax.toFixed(2)))),
      /* @__PURE__ */ React.createElement("div", { className: "rof-summary-total", style: { marginTop: "0.375rem" } }, /* @__PURE__ */ React.createElement("span", { className: "rof-summary-total-label" }, "Order Total"), /* @__PURE__ */ React.createElement("span", { className: "rof-summary-total-amount" }, "$", total.toFixed(2)))
    ), /* @__PURE__ */ React.createElement(WizRepCard, { title: "How is the customer paying?" }, /* @__PURE__ */ React.createElement("div", { className: "rof-pay-grid" }, [
      { key: "card", icon: "\u25A3", label: "Card \xB7 Terminal", sub: "Valor terminal or keyed entry" },
      { key: "stripe", icon: "\u2197", label: "Send payment link", sub: "Emailed \xB7 customer pays by card" },
      { key: "cash", icon: "$", label: "Cash", sub: "Drawer logs \xB7 printed slip" },
      { key: "check", icon: "\u270E", label: "Check", sub: "Hold for 3 day clearing" }
    ].map((m) => {
      const on = paymentMethod === m.key;
      return /* @__PURE__ */ React.createElement("button", { key: m.key, onClick: () => selectMethod(m.key), style: {
        padding: "0.75rem",
        cursor: "pointer",
        textAlign: "left",
        background: on ? "#d8cdb6" : "transparent",
        border: "0.5px solid " + (on ? "#1c1917" : "rgba(28,25,23,0.2)"),
        display: "grid",
        gridTemplateColumns: "30px 1fr 14px",
        gap: "0.625rem",
        alignItems: "center"
      } }, /* @__PURE__ */ React.createElement("span", { style: {
        width: 28,
        height: 28,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        font: "400 14px/1 var(--roma-serif)",
        color: "#1c1917",
        border: "0.5px solid rgba(28,25,23,0.2)"
      } }, m.icon), /* @__PURE__ */ React.createElement("span", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 12px/1.2 var(--roma-sans)", color: "#1c1917", letterSpacing: "-0.005em", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, m.label), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: "#8a7e68", textTransform: "uppercase", marginTop: 3, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" } }, m.sub)), /* @__PURE__ */ React.createElement("span", { style: {
        width: 13,
        height: 13,
        border: "1px solid #1c1917",
        flexShrink: 0,
        background: on ? "#1c1917" : "transparent",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#ece5d8",
        font: "500 8px/1 var(--roma-sans)"
      } }, on ? "\u2713" : ""));
    })), paymentMethod && remaining > 0.01 && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.25rem" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: "0.5rem" } }, /* @__PURE__ */ React.createElement("label", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", null, "Amount to collect now"), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: () => setCollectNow(""),
        style: { font: "500 8.5px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "#8a7e68", background: "none", border: "none", cursor: "pointer", padding: 0 }
      },
      "Full balance $",
      defaultCollect.toFixed(2)
    )), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: "50%", transform: "translateY(-50%)", font: "400 14px/1 var(--roma-serif)", color: "#8a7e68", pointerEvents: "none" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "number",
        min: "0",
        step: "0.01",
        value: collectNow,
        onChange: (e) => setCollectNow(e.target.value),
        placeholder: defaultCollect.toFixed(2),
        style: { paddingLeft: 22 }
      }
    )), isDeposit && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "#a87935", marginTop: 5 } }, "Deposit ", "\xB7", " $", (defaultCollect - collectAmt).toFixed(2), " balance stays due")), paymentMethod === "card" && /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light primary", onClick: handleCardPayment, disabled: collectAmt <= 0, style: { width: "100%" } }, "Charge $", collectAmt.toFixed(2), " by Card"), paymentMethod === "stripe" && /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light primary", onClick: setStripeRequest, disabled: collectAmt <= 0, style: { width: "100%" } }, "Send payment link for $", collectAmt.toFixed(2)), paymentMethod === "cash" && /* @__PURE__ */ React.createElement("div", null, cashDrawerOpen === false && /* @__PURE__ */ React.createElement("div", { className: "rof-alert warn" }, "No cash drawer open. Open one from the Cash Drawer tab first."), cashDrawerOpen !== false && /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light primary", onClick: setCashPayment, disabled: collectAmt <= 0, style: { width: "100%" } }, "Record $", collectAmt.toFixed(2), " Cash Payment")), paymentMethod === "check" && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Check Number *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 20,
        value: data.checkNumber || "",
        onChange: (e) => setData((d) => ({ ...d, checkNumber: e.target.value })),
        placeholder: "Enter check number"
      }
    )), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light primary",
        onClick: setCheckPayment,
        disabled: !data.checkNumber || !data.checkNumber.trim() || collectAmt <= 0,
        style: { width: "100%" }
      },
      "Record $",
      collectAmt.toFixed(2),
      " Check Payment"
    )))), paymentMethods.length > 0 && /* @__PURE__ */ React.createElement(WizRepCard, { title: "Split tenders" }, paymentMethods.map((pm, i) => {
      const meta = {
        card: { icon: "\u25A3", col: "#3a5a7a", label: pm.card_txn ? "Card \xB7 Valor terminal" : "Card \xB7 Stripe", status: "\u25CF Captured", ref: pm.card_txn ? (pm.card_txn.brand || "CARD") + " " + (String(pm.card_txn.masked_pan || "").replace(/\D/g, "").slice(-4) || "terminal") : pm.stripePaymentIntentId ? String(pm.stripePaymentIntentId).slice(0, 18) + "\u2026" : "Stripe" },
        ach: { icon: "\u296F", col: "#5c6e94", label: "ACH transfer", status: "\u25CB Pending clearance", ref: "Bank transfer \xB7 3\u20135 days" },
        cash: { icon: "$", col: "#3a7a4e", label: "Cash", status: "\u25CF In drawer", ref: "Cash drawer" },
        check: { icon: "\u270E", col: "#a87935", label: "Check", status: "\u25CB Pending clearance", ref: pm.checkNumber ? "Check #" + pm.checkNumber : "Check" },
        stripe: { icon: "\u2197", col: "#8a7e68", label: "Payment link", status: "\u25D0 Sends on Process", ref: "Emailed to customer" }
      }[pm.method] || { icon: "\xB7", col: "#8a7e68", label: pm.method, status: "", ref: "" };
      return /* @__PURE__ */ React.createElement("div", { key: i, style: {
        padding: "0.75rem 0",
        display: "grid",
        gridTemplateColumns: "32px 1.4fr 1fr 100px 22px",
        gap: "0.75rem",
        alignItems: "center",
        borderBottom: i < paymentMethods.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none"
      } }, /* @__PURE__ */ React.createElement("span", { style: {
        width: 30,
        height: 30,
        background: meta.col,
        color: "#ece5d8",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        font: "400 14px/1 var(--roma-serif)"
      } }, meta.icon), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 var(--roma-sans)", color: "#1c1917", letterSpacing: "-0.005em" } }, meta.label), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: "#8a7e68", textTransform: "uppercase", marginTop: 2, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, meta.ref)), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: meta.col, textTransform: "uppercase" } }, meta.status), /* @__PURE__ */ React.createElement("span", { style: { font: "400 17px/1 var(--roma-serif)", color: "#1c1917", textAlign: "right", letterSpacing: "-0.012em" } }, "$", parseFloat(pm.amount).toFixed(2)), /* @__PURE__ */ React.createElement("button", { className: "rof-remove-btn", onClick: () => removePayment(i) }, "\u2715"));
    }), /* @__PURE__ */ React.createElement("div", { style: {
      margin: "0.75rem -1.25rem -1.25rem",
      padding: "0.875rem 1.25rem",
      background: "#d8cdb6",
      borderTop: "0.5px solid rgba(28,25,23,0.13)",
      display: "grid",
      gridTemplateColumns: "repeat(3, 1fr)",
      gap: "1.25rem"
    } }, [
      { l: "Order total", v: "$" + total.toFixed(2), col: "#1c1917" },
      { l: "Tendered", v: "$" + collectedAmount.toFixed(2), col: "#1c1917" },
      {
        l: remaining <= 0.01 && remaining >= -0.01 ? "\u25CF Balanced" : remaining > 0 ? "\u25CF Balance due" : "\u25CF Overage",
        v: "$" + Math.abs(remaining <= 0.01 && remaining >= -0.01 ? 0 : remaining).toFixed(2),
        col: remaining <= 0.01 && remaining >= -0.01 ? "#3a7a4e" : remaining > 0 ? "#a87935" : "#c54731"
      }
    ].map((m) => /* @__PURE__ */ React.createElement("div", { key: m.l }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: m.col, marginBottom: 5 } }, m.l), /* @__PURE__ */ React.createElement("div", { style: { font: "300 22px/1 var(--roma-serif)", color: m.col, letterSpacing: "-0.014em" } }, m.v))))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "What Process Order does" }, [
      { text: "Create order with status confirmed", badge: "Instant" },
      { text: "Send confirmation email to " + customerEmail, badge: "~5 sec" },
      { text: "Create draft purchase orders for vendors", badge: "Instant" },
      { text: "Record payment transaction(s)", badge: "Instant" },
      ...(data.paymentMethods || []).some((p) => p.method === "stripe") ? [{ text: "Email payment link to customer", badge: "~10 sec" }] : []
    ].map((row, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "rof-checklist-row" }, /* @__PURE__ */ React.createElement("span", { className: "rof-checklist-dot" }), /* @__PURE__ */ React.createElement("span", { className: "rof-checklist-text" }, row.text), /* @__PURE__ */ React.createElement("span", { className: "rof-checklist-badge" }, row.badge)))), showPayCard && /* @__PURE__ */ React.createElement(
      RofPayCardModal,
      {
        amount: pendingCardAmount > 0 ? pendingCardAmount : remaining > 0 ? remaining : total,
        customerName,
        customerEmail,
        orderNumber: null,
        tenderIdx: paymentMethods.filter((p) => p.method === "card").length + 1,
        tenderCount: paymentMethods.length + 1,
        onSuccess: (res) => {
          setShowPayCard(false);
          handleCardSuccess(res);
        },
        onCancel: () => setShowPayCard(false),
        terminalRef,
        terminalStatus,
        connectReader
      }
    ));
  }
  function RofSendPOsStep({ data, setData, showCost, setStep }) {
    const createdItems = data.createdOrder && data.createdOrder.items || [];
    const oneOffVendorId = (i) => {
      const m = createdItems.find((ci) => !ci.sku_id && ci.vendor_id && String(ci.product_name || "") === String(i.product_name || ""));
      return m ? m.vendor_id : null;
    };
    const items = (data.items || []).filter((i) => !i.is_sample).map((i) => {
      if (i.sku_id || i.vendor_id) return i;
      const vid = i.custom_vendor ? oneOffVendorId(i) : null;
      return vid ? { ...i, vendor_id: vid, vendor_name: i.vendor_name || i.custom_vendor } : i;
    }).filter((i) => i.sku_id || i.vendor_id);
    const vendorGroups = {};
    items.forEach((item) => {
      const vKey = item.vendor_id || "_unknown";
      if (!vendorGroups[vKey]) vendorGroups[vKey] = { vendor_name: item.vendor_name || item.custom_vendor || "Unknown Vendor", vendor_public_code: item.vendor_public_code || null, items: [] };
      vendorGroups[vKey].items.push(item);
    });
    const order = data.createdOrder;
    const total = (data.items || []).reduce((s, i) => s + i.subtotal, 0) - (data.promoResult ? data.promoResult.discount_amount : 0);
    const captured = (data.paymentMethods || []).reduce((s, p) => s + (parseFloat(p.amount) || 0), 0) || total;
    const totalCost = items.reduce((s, i) => {
      const c = i.cost || 0;
      if (i.sell_by === "unit" || i.sell_by === "piece") return s + c * i.num_boxes;
      return s + c * (parseFloat(i.sqft_needed) || i.num_boxes);
    }, 0);
    const margin = total > 0 ? (total - totalCost) / total * 100 : 0;
    const poCount = Object.keys(vendorGroups).length;
    const [poChannels, setPoChannels] = useState(() => {
      const ch = {};
      Object.keys(vendorGroups).forEach((vKey) => {
        ch[vKey] = "email";
      });
      return ch;
    });
    const [poSentMap, setPoSentMap] = useState({});
    const [sendingPO, setSendingPO] = useState(null);
    const [batchSending, setBatchSending] = useState(false);
    const [poInfo, setPoInfo] = useState(null);
    const [savingCost, setSavingCost] = useState({});
    const [savingNote, setSavingNote] = useState({});
    useEffect(() => {
      const oid = data.createdOrder && data.createdOrder.id;
      if (!oid) return;
      repFetch("/api/rep/orders/" + oid + "/purchase-orders").then((res) => {
        const pos = res && res.purchase_orders || [];
        const repEmail = res && res.rep_email || "";
        const edi = {}, poId = {}, status = {}, poItemsByVendor = {};
        const vendorEmail = {}, contactEmail = {}, recipient = {}, cc = {}, notes = {};
        pos.forEach((po) => {
          edi[po.vendor_id] = !!po.edi_enabled;
          poId[po.vendor_id] = po.id;
          status[po.vendor_id] = po.status;
          vendorEmail[po.vendor_id] = po.vendor_email || "";
          contactEmail[po.vendor_id] = po.vendor_contact_email || "";
          recipient[po.vendor_id] = po.recipient_email || "";
          cc[po.vendor_id] = Array.isArray(po.cc_emails) ? po.cc_emails : [];
          notes[po.vendor_id] = po.notes || "";
          poItemsByVendor[po.vendor_id] = (po.items || []).map((it) => ({ id: it.id, sku_id: it.sku_id, product_name: it.product_name, _used: false }));
        });
        const line = {};
        Object.entries(vendorGroups).forEach(([vKey, group]) => {
          const avail = poItemsByVendor[vKey] || [];
          group.items.forEach((item) => {
            const m = item.sku_id ? avail.find((pi) => !pi._used && String(pi.sku_id) === String(item.sku_id)) : avail.find((pi) => !pi._used && !pi.sku_id && String(pi.product_name || "") === String(item.product_name || ""));
            if (m) {
              m._used = true;
              line[item._key] = m.id;
            }
          });
        });
        setPoInfo({ edi, poId, status, line, vendorEmail, contactEmail, recipient, cc, notes, repEmail });
        setPoChannels((prev) => {
          const next = { ...prev };
          Object.keys(vendorGroups).forEach((vKey) => {
            next[vKey] = edi[vKey] ? "edi+email" : "email";
          });
          return next;
        });
      }).catch(() => setPoInfo({ edi: {}, poId: {}, status: {}, line: {}, vendorEmail: {}, contactEmail: {}, recipient: {}, cc: {}, notes: {}, repEmail: "" }));
    }, [data.createdOrder && data.createdOrder.id]);
    const defaultTo = (vKey) => poInfo && (poInfo.recipient[vKey] || poInfo.vendorEmail[vKey]) || "";
    const defaultCc = (vKey) => {
      if (!poInfo) return [];
      if (poInfo.cc[vKey] && poInfo.cc[vKey].length) return poInfo.cc[vKey];
      return [poInfo.repEmail].filter(Boolean);
    };
    const defaultNotes = (vKey) => poInfo && poInfo.notes && poInfo.notes[vKey] || "";
    const updateLineCost = async (vKey, item, raw) => {
      const newCost = parseFloat(raw);
      if (isNaN(newCost) || newCost < 0 || newCost === (item.cost || 0)) return;
      setData((d) => ({ ...d, items: (d.items || []).map((it) => it._key === item._key ? { ...it, cost: newCost } : it) }));
      const poItemId = poInfo && poInfo.line && poInfo.line[item._key];
      const poId = poInfo && poInfo.poId && poInfo.poId[vKey];
      if (!poItemId || !poId) return;
      setSavingCost((s) => ({ ...s, [item._key]: true }));
      try {
        await repFetch("/api/rep/purchase-orders/" + poId + "/items/" + poItemId, {
          method: "PUT",
          body: JSON.stringify({ cost: newCost })
        });
      } catch (e) {
      }
      setSavingCost((s) => ({ ...s, [item._key]: false }));
    };
    const setPoNote = (vKey, text) => {
      setPoInfo((pi) => pi ? { ...pi, notes: { ...pi.notes, [vKey]: text } } : pi);
    };
    const savePoNote = async (vKey) => {
      const poId = poInfo && poInfo.poId && poInfo.poId[vKey];
      if (!poId) return;
      setSavingNote((s) => ({ ...s, [vKey]: true }));
      try {
        await repFetch("/api/rep/purchase-orders/" + poId, {
          method: "PUT",
          body: JSON.stringify({ notes: (poInfo.notes && poInfo.notes[vKey] || "").trim() })
        });
      } catch (e) {
      }
      setSavingNote((s) => ({ ...s, [vKey]: false }));
    };
    const [emailModal, setEmailModal] = useState(null);
    const sendableKeys = Object.keys(vendorGroups).filter((vKey) => !!(poInfo && poInfo.poId && poInfo.poId[vKey]));
    const allSent = sendableKeys.length > 0 && sendableKeys.every((vKey) => poSentMap[vKey]);
    const nowStamp = () => (/* @__PURE__ */ new Date()).toLocaleTimeString("en-US", { hour: "2-digit", minute: "2-digit", second: "2-digit" });
    const doSendPO = async (vKey, group, overrides) => {
      const poId = poInfo && poInfo.poId && poInfo.poId[vKey];
      if (!poId) {
        setData((d) => ({ ...d, transmissionLog: [...d.transmissionLog || [], { ok: false, message: `No draft PO to send for ${group.vendor_name}`, time: nowStamp() }] }));
        return false;
      }
      setSendingPO(vKey);
      let ok = false;
      try {
        const res = await repFetch("/api/rep/purchase-orders/" + poId + "/approve", {
          method: "POST",
          body: JSON.stringify({
            method: overrides && (overrides.method === "phone" || overrides.method === "online") ? overrides.method : void 0,
            recipient_email: overrides && overrides.recipient_email || void 0,
            cc_emails: overrides && overrides.cc_emails || void 0,
            notes: overrides && typeof overrides.notes === "string" ? overrides.notes : void 0,
            line_notes: overrides && overrides.line_notes || void 0
          })
        });
        ok = true;
        const sv = res && res.sent_via;
        const via = sv === "edi" ? "EDI 850" : sv === "phone" ? "phone" : sv === "online" ? "online order" : res && res.email_sent ? "email" : "email (queued)";
        const manualVia = sv === "phone" || sv === "online";
        const toShown = manualVia ? "" : overrides && overrides.recipient_email || poInfo && poInfo.vendorEmail[vKey] || "";
        setPoSentMap((prev) => ({ ...prev, [vKey]: true }));
        setData((d) => ({ ...d, transmissionLog: [...d.transmissionLog || [], { ok: true, message: `PO for ${group.vendor_name} ${manualVia ? "placed via " + via : "sent via " + via}${toShown ? " \u2192 " + toShown : ""}`, time: nowStamp() }] }));
      } catch (e) {
        setData((d) => ({ ...d, transmissionLog: [...d.transmissionLog || [], { ok: false, message: `PO to ${group.vendor_name} failed: ${e.message}`, time: nowStamp() }] }));
      }
      setSendingPO(null);
      return ok;
    };
    const sendSinglePO = (vKey, group) => setEmailModal({ vKey, group });
    const sendAllPOs = async () => {
      setBatchSending(true);
      for (const [vKey, group] of Object.entries(vendorGroups)) {
        if (!poSentMap[vKey] && poInfo && poInfo.poId[vKey]) {
          await doSendPO(vKey, group, { recipient_email: defaultTo(vKey), cc_emails: defaultCc(vKey) });
        }
      }
      setBatchSending(false);
    };
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-hero", style: { textAlign: "left", display: "grid", gridTemplateColumns: "1fr auto", gap: "1.5rem", alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#3a7a4e", marginBottom: 8 } }, "\u25CF", " Order processed ", "\xB7", " ", order ? order.order_number || "#" + order.id : "order", " is live"), /* @__PURE__ */ React.createElement("h2", { style: { font: "300 26px/1.15 var(--roma-serif)", letterSpacing: "-0.014em", margin: 0, color: "#f5f0e8" } }, "Now ship ", /* @__PURE__ */ React.createElement("em", { style: { color: "#a87935" } }, poCount, " purchase order", poCount !== 1 ? "s" : ""), " to the vendor", poCount !== 1 ? "s" : "", "."), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.5 var(--roma-sans)", color: "rgba(245,240,232,0.8)", marginTop: 8, maxWidth: 560 } }, "Payment captured ", "\xB7", " invoice sent ", "\xB7", " POs are pre-drafted at vendor cost ", "\u2014", " review each, then send all in a batch or one at a time.")), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(" + (showCost ? 3 : 2) + ", auto)", gap: "1.125rem", padding: "0.875rem 1.25rem", border: "0.5px solid rgba(245,240,232,0.2)" } }, [
      { l: "Captured", v: "$" + captured.toFixed(2) },
      { l: "POs out", v: showCost ? "$" + totalCost.toFixed(2) : String(poCount) },
      ...showCost ? [{ l: "Margin", v: margin.toFixed(1) + "%" }] : []
    ].map((s) => /* @__PURE__ */ React.createElement("div", { key: s.l }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)" } }, s.l), /* @__PURE__ */ React.createElement("div", { style: { font: "300 19px/1 var(--roma-serif)", color: "#f5f0e8", letterSpacing: "-0.012em", marginTop: 4, whiteSpace: "nowrap" } }, s.v))))), poCount > 0 && /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Vendor purchase orders",
        actions: /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.625rem/1 ui-monospace, monospace", color: "#8a7e68" } }, poCount, " PO", poCount !== 1 ? "s" : "", " ", allSent ? "\u2014 all sent" : "")
      },
      /* @__PURE__ */ React.createElement("div", { className: "rof-po-batch" }, /* @__PURE__ */ React.createElement("span", { className: "rof-po-batch-label" }, !poInfo ? "Loading purchase orders\u2026" : allSent ? `All ${sendableKeys.length} PO${sendableKeys.length !== 1 ? "s" : ""} sent` : `${sendableKeys.filter((k) => poSentMap[k]).length} of ${sendableKeys.length} sent`), !allSent && /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light primary",
          onClick: sendAllPOs,
          disabled: batchSending || !poInfo || sendableKeys.length === 0,
          style: { padding: "0.375rem 0.875rem" }
        },
        batchSending ? "Sending..." : "Send All POs"
      )),
      Object.entries(vendorGroups).map(([vKey, group], gi) => {
        const lineCost = (i) => {
          const c = i.cost || 0;
          if (i.sell_by === "unit" || i.sell_by === "piece") return c * i.num_boxes;
          return c * (parseFloat(i.sqft_needed) || i.num_boxes);
        };
        const poCost = group.items.reduce((sum, i) => sum + lineCost(i), 0);
        const poRetail = group.items.reduce((sum, i) => sum + i.subtotal, 0);
        const poMargin = poRetail > 0 ? (poRetail - poCost) / poRetail * 100 : 0;
        const isSent = poSentMap[vKey];
        const isSending = sendingPO === vKey;
        const ediOn = !!(poInfo && poInfo.edi && poInfo.edi[vKey]);
        const cardCanEditCost = showCost && !isSent && !!(poInfo && poInfo.poId && poInfo.poId[vKey]) && poInfo.status[vKey] === "draft";
        const channel = ediOn ? poChannels[vKey] || "edi+email" : "email";
        const INK = "#1c1917", MUTED = "#8a7e68", PAPER = "#ece5d8", WARM = "#d8cdb6";
        return /* @__PURE__ */ React.createElement("div", { key: vKey, style: { border: "0.5px solid rgba(28,25,23,0.13)", background: PAPER, marginBottom: "0.875rem", opacity: isSent ? 0.65 : 1 } }, /* @__PURE__ */ React.createElement("div", { style: {
          padding: "0.875rem 1.125rem",
          display: "grid",
          gridTemplateColumns: showCost ? "1.3fr 1fr auto" : "1.3fr 1fr",
          gap: "1.125rem",
          alignItems: "center",
          borderBottom: "0.5px solid rgba(28,25,23,0.1)"
        } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: {
          display: "flex",
          alignItems: "center",
          gap: 8,
          marginBottom: 4,
          font: "500 9px/1 ui-monospace, monospace",
          letterSpacing: "0.16em",
          textTransform: "uppercase",
          color: isSent ? "var(--green)" : MUTED
        } }, /* @__PURE__ */ React.createElement("span", null, "PO ", gi + 1, " of ", poCount, " ", "\xB7", " ", isSent ? "Sent \u2713" : "Queued"), channel !== "email" && /* @__PURE__ */ React.createElement("span", { style: { padding: "2px 6px", background: INK, color: PAPER, letterSpacing: "0.12em" } }, "EDI 850")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 18px/1.2 var(--roma-serif)", color: INK, letterSpacing: "-0.012em" } }, group.vendor_name, group.vendor_public_code ? /* @__PURE__ */ React.createElement("span", { style: { color: MUTED, fontSize: 14 } }, " \xB7 ", group.vendor_public_code) : null), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: MUTED, textTransform: "uppercase", marginTop: 2 } }, group.items.length, " line", group.items.length !== 1 ? "s" : "", " ", "\xB7", " drafted at vendor cost")), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: MUTED } }, "Transmit via"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 4, flexWrap: "wrap" } }, (ediOn ? [{ id: "edi+email", label: "EDI + Email" }, { id: "edi", label: "EDI only" }, { id: "email", label: "Email" }] : [{ id: "email", label: "Email" }]).map((opt) => {
          const on = opt.id === channel;
          return /* @__PURE__ */ React.createElement(
            "button",
            {
              key: opt.id,
              disabled: isSent || !ediOn && opt.id === "email",
              onClick: () => setPoChannels((ch) => ({ ...ch, [vKey]: opt.id })),
              style: {
                padding: "5px 9px",
                cursor: isSent || !ediOn ? "default" : "pointer",
                font: "500 8px/1 ui-monospace, monospace",
                letterSpacing: "0.12em",
                textTransform: "uppercase",
                border: "0.5px solid " + (on ? INK : "rgba(28,25,23,0.2)"),
                background: on ? INK : "transparent",
                color: on ? PAPER : MUTED
              }
            },
            opt.label
          );
        })), poInfo && !ediOn && /* @__PURE__ */ React.createElement("div", { style: { font: "500 7.5px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: MUTED } }, vKey === "_unknown" ? "No vendor \xB7 email only" : "No EDI for this vendor \xB7 email only")), showCost && /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: MUTED } }, "Vendor cost"), /* @__PURE__ */ React.createElement("div", { style: { font: "300 22px/1 var(--roma-serif)", color: INK, letterSpacing: "-0.012em", marginTop: 4 } }, "$", poCost.toFixed(2)), poRetail > 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.1em", marginTop: 4, color: poMargin >= 30 ? "#3a7a4e" : poMargin >= 15 ? "#a87935" : "#c54731" } }, poMargin.toFixed(1), "% margin"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "0.25rem 1.125rem" } }, group.items.map((item, li) => /* @__PURE__ */ React.createElement("div", { key: item._key, style: {
          padding: "0.5rem 0",
          display: "grid",
          gridTemplateColumns: showCost ? "32px 1fr 70px 76px 80px" : "32px 1fr 70px",
          gap: "0.75rem",
          alignItems: "center",
          borderBottom: li < group.items.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none"
        } }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 80), alt: "", loading: "lazy", style: { width: 32, height: 32, objectFit: "cover" } }) : /* @__PURE__ */ React.createElement("div", { style: { width: 32, height: 32, ...materialFace(rofCatKind(item)) } }), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, formatLineItem(item).brand && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: MUTED, textTransform: "uppercase", marginBottom: 2 } }, formatLineItem(item).brand), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.2 var(--roma-sans)", color: INK, letterSpacing: "-0.005em", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, formatLineItem(item).name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: MUTED, textTransform: "uppercase", marginTop: 2 } }, formatLineItem(item).sku || "\u2014")), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "400 12px/1.2 var(--roma-serif)", color: INK } }, item.num_boxes, " ", item.sell_by === "unit" || item.sell_by === "piece" ? "ea" : "bx"), showCost && (() => {
          const lineEditable = cardCanEditCost && !!(poInfo && poInfo.line && poInfo.line[item._key]);
          if (!lineEditable) {
            return /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "400 12px/1 var(--roma-serif)", color: MUTED } }, "$", (item.cost || 0).toFixed(2));
          }
          return /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 2, position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 11px/1 var(--roma-serif)", color: MUTED } }, "$"), /* @__PURE__ */ React.createElement(
            "input",
            {
              type: "number",
              min: "0",
              step: "0.01",
              defaultValue: (item.cost || 0).toFixed(2),
              title: "Adjust vendor cost",
              onKeyDown: (e) => {
                if (e.key === "Enter") e.target.blur();
              },
              onBlur: (e) => updateLineCost(vKey, item, e.target.value),
              style: {
                width: 56,
                textAlign: "right",
                padding: "3px 4px",
                font: "400 12px/1 var(--roma-serif)",
                color: INK,
                border: "0.5px solid rgba(28,25,23,0.28)",
                background: "#fff",
                opacity: savingCost[item._key] ? 0.5 : 1
              }
            }
          ));
        })(), showCost && /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "400 13px/1 var(--roma-serif)", color: INK } }, "$", lineCost(item).toFixed(2))))), /* @__PURE__ */ React.createElement("div", { style: { padding: "0.5rem 1.125rem 0.75rem", borderTop: "0.5px solid rgba(28,25,23,0.07)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: MUTED, marginBottom: 6 } }, "Note to vendor ", "\xB7", " printed on this PO"), isSent ? /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.5 var(--roma-sans)", color: poInfo && poInfo.notes && poInfo.notes[vKey] ? INK : MUTED, whiteSpace: "pre-wrap" } }, poInfo && poInfo.notes && poInfo.notes[vKey] || "No note added") : /* @__PURE__ */ React.createElement(
          "textarea",
          {
            value: poInfo && poInfo.notes && poInfo.notes[vKey] || "",
            onChange: (e) => setPoNote(vKey, e.target.value),
            onBlur: () => savePoNote(vKey),
            rows: 2,
            maxLength: 2e3,
            placeholder: "e.g. Confirm lead time \xB7 deliver to will-call \xB7 hold for pickup Fri",
            style: {
              width: "100%",
              padding: "7px 9px",
              resize: "vertical",
              boxSizing: "border-box",
              font: "400 12px/1.5 var(--roma-sans)",
              color: INK,
              border: "0.5px solid rgba(28,25,23,0.2)",
              background: "#fff",
              opacity: savingNote[vKey] ? 0.6 : 1
            }
          }
        )), /* @__PURE__ */ React.createElement("div", { style: {
          padding: "0.625rem 1.125rem",
          background: WARM,
          display: "grid",
          gridTemplateColumns: "1fr auto",
          gap: "0.875rem",
          alignItems: "center"
        } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: MUTED, textTransform: "uppercase" } }, isSent ? "Transmitted via " + (channel === "email" ? "PDF email" : channel === "edi" ? "EDI 850" : "EDI 850 + PDF email") : channel === "email" ? "PDF emailed only \xB7 no EDI for this send" : channel === "edi" ? "EDI 850 will send \xB7 no email copy" : "EDI 850 will send + PDF emailed"), !isSent && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.375rem" } }, /* @__PURE__ */ React.createElement(
          "button",
          {
            className: "rof-btn-light primary",
            onClick: () => sendSinglePO(vKey, group),
            disabled: isSending || !(poInfo && poInfo.poId && poInfo.poId[vKey]),
            style: { padding: "0.375rem 0.875rem" }
          },
          isSending ? "Sending..." : "Review & send \u2192"
        ))));
      })
    ), poCount === 0 && /* @__PURE__ */ React.createElement(WizRepCard, { title: "Vendor purchase orders" }, /* @__PURE__ */ React.createElement("div", { className: "rof-items-empty" }, "No PO-eligible items in this order.")), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Need to hold off?" }, /* @__PURE__ */ React.createElement("div", { className: "rof-context-text", style: { marginBottom: "0.625rem" } }, "POs are created as drafts. You can skip sending now and transmit them later from the order detail page."), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light",
        style: { padding: "0.375rem 0.875rem" },
        onClick: () => {
          if (setStep) setStep(4);
        }
      },
      "Hold all POs for now"
    )), emailModal && /* @__PURE__ */ React.createElement(
      RofPoEmailModal,
      {
        vendorName: emailModal.group.vendor_name,
        ediOn: !!(poInfo && poInfo.edi && poInfo.edi[emailModal.vKey]),
        defaultTo: defaultTo(emailModal.vKey),
        defaultCc: defaultCc(emailModal.vKey),
        defaultNotes: defaultNotes(emailModal.vKey),
        lines: (emailModal.group.items || []).map((item) => ({
          id: poInfo && poInfo.line && poInfo.line[item._key],
          label: (formatLineItem(item).sku ? formatLineItem(item).sku + " \xB7 " : "") + (formatLineItem(item).name || item.product_name || "Line"),
          note: ""
        })).filter((l) => l.id),
        sending: sendingPO === emailModal.vKey,
        onCancel: () => setEmailModal(null),
        onSend: async (overrides) => {
          const ok = await doSendPO(emailModal.vKey, emailModal.group, overrides);
          if (ok) setEmailModal(null);
        }
      }
    ));
  }
  function RofPoEmailModal({ vendorName, ediOn, isResend, defaultTo, defaultCc, defaultNotes, lines, sending, onCancel, onSend }) {
    const [to, setTo] = useState(defaultTo || "");
    const [ccText, setCcText] = useState((defaultCc || []).join(", "));
    const [notes, setNotes] = useState(defaultNotes || "");
    const [method, setMethod] = useState("transmit");
    const manual = !isResend && (method === "phone" || method === "online");
    const noEmailOnFile = !(defaultTo && String(defaultTo).trim());
    const emailRe = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    const ccList = ccText.split(",").map((s) => s.trim()).filter(Boolean);
    const toValid = emailRe.test((to || "").trim());
    const ccAllValid = ccList.every((e) => emailRe.test(e));
    const canSend = manual ? !sending : toValid && ccAllValid && !sending;
    const INK = "#1c1917", MUTED = "#8a7e68", PAPER = "#ece5d8";
    return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay", onMouseDown: (e) => {
      if (e.target === e.currentTarget && !sending) onCancel();
    } }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-pop", style: { maxWidth: 520 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 24px 16px", borderBottom: "0.5px solid rgba(28,25,23,0.1)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: MUTED, marginBottom: 8 } }, manual ? "Record purchase order" : isResend ? "Resend purchase order" : "Send purchase order"), /* @__PURE__ */ React.createElement("h2", { style: { margin: 0, font: '300 24px/1.15 "Cormorant Garamond", serif', letterSpacing: "-0.012em", color: INK } }, manual ? "Record PO for " : isResend ? "Resend PO to " : "Email PO to ", /* @__PURE__ */ React.createElement("em", null, vendorName)), ediOn && !manual && !isResend && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "#a87935", marginTop: 8 } }, "EDI vendor \xB7 transmits via EDI 850; email is the fallback / paper copy"), isResend && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "#a87935", marginTop: 8 } }, "Re-emails the PO PDF to the vendor \xB7 does not re-transmit EDI"), manual && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "#a87935", marginTop: 8 } }, "You placed this order ", method === "phone" ? "over the phone" : "on the vendor\u2019s website", " \xB7 nothing is sent to the vendor")), /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 24px" } }, !isResend && /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "How are you placing this order?"), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 6, marginTop: 2 } }, [
      { id: "transmit", label: ediOn ? "EDI / Email" : "Email" },
      { id: "phone", label: "By phone" },
      { id: "online", label: "Online" }
    ].map((opt) => {
      const on = method === opt.id;
      return /* @__PURE__ */ React.createElement("button", { key: opt.id, type: "button", onClick: () => setMethod(opt.id), style: {
        padding: "8px 6px",
        cursor: "pointer",
        textAlign: "center",
        font: "500 11px/1.2 Inter, sans-serif",
        color: on ? INK : MUTED,
        background: on ? "#d8cdb6" : "transparent",
        border: "0.5px solid " + (on ? INK : "rgba(28,25,23,0.22)")
      } }, opt.label);
    }))), !manual && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "To *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        value: to,
        onChange: (e) => setTo(e.target.value),
        placeholder: "vendor@example.com",
        style: { borderColor: to && !toValid || noEmailOnFile && !to.trim() ? "#c54731" : void 0 }
      }
    ), to && !toValid && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1.4 ui-monospace, monospace", letterSpacing: "0.08em", color: "#c54731", marginTop: 4, textTransform: "uppercase" } }, "Enter a valid email"), noEmailOnFile && !to.trim() && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1.4 ui-monospace, monospace", letterSpacing: "0.08em", color: "#a87935", marginTop: 4, textTransform: "uppercase" } }, "No email on file for this vendor \u2014 enter one to send")), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 4 } }, /* @__PURE__ */ React.createElement("label", null, "CC ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", color: MUTED } }, "\xB7 comma-separated")), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: ccText,
        onChange: (e) => setCcText(e.target.value),
        placeholder: "rep@romaflooringdesigns.com, contact@vendor.com",
        style: { borderColor: ccList.length && !ccAllValid ? "#c54731" : void 0 }
      }
    ), ccList.length > 0 && !ccAllValid && /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1.4 ui-monospace, monospace", letterSpacing: "0.08em", color: "#c54731", marginTop: 4, textTransform: "uppercase" } }, "One or more CC emails are invalid"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", color: MUTED, marginTop: 6, textTransform: "uppercase" } }, "The PO PDF is attached \xB7 reply-to is your rep email"))), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Notes to vendor ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", color: MUTED } }, "\xB7 printed on the PO", manual ? "" : " + email")), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        value: notes,
        onChange: (e) => setNotes(e.target.value),
        rows: 3,
        maxLength: 2e3,
        placeholder: "e.g. Confirm lead time \xB7 deliver to will-call \xB7 hold for pickup Fri"
      }
    ))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 24px", borderTop: "0.5px solid rgba(28,25,23,0.1)", display: "flex", gap: 8, justifyContent: "flex-end", background: "#d8cdb6" } }, /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", onClick: onCancel, disabled: sending, style: { padding: "0.5rem 1rem" } }, "Cancel"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light primary",
        onClick: () => onSend(manual ? { method, notes } : { recipient_email: to.trim(), cc_emails: ccList, notes }),
        disabled: !canSend,
        style: { padding: "0.5rem 1.25rem" }
      },
      sending ? manual ? "Saving\u2026" : "Sending\u2026" : manual ? "Mark as ordered" : isResend ? "Resend PO" : "Send PO"
    ))));
  }
  function RofDoneStep({ data, navigate, showCost, onNewOrder }) {
    const order = data.createdOrder;
    const log = data.transmissionLog || [];
    const total = (data.items || []).reduce((s, i) => s + i.subtotal, 0) - (data.promoResult ? data.promoResult.discount_amount : 0);
    const captured = (data.paymentMethods || []).reduce((s, p) => s + (parseFloat(p.amount) || 0), 0) || total;
    const totalCost = (data.items || []).filter((i) => i.sku_id).reduce((s, i) => {
      const c = i.cost || 0;
      if (i.sell_by === "unit" || i.sell_by === "piece") return s + c * i.num_boxes;
      return s + c * (parseFloat(i.sqft_needed) || i.num_boxes);
    }, 0);
    const margin = total > 0 ? (total - totalCost) / total * 100 : 0;
    const grossProfit = total - totalCost;
    const commission = grossProfit * 0.1;
    const orderNum = order ? order.order_number || "#" + order.id : "Order";
    const customerName = data.customer ? data.customer.name : data.newCustomerName || "";
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-hero", style: { textAlign: "left", display: "grid", gridTemplateColumns: "1fr auto", gap: "1.75rem", alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#a87935", marginBottom: 12 } }, "\u25CF", " Closed ", "\xB7", " all systems go"), /* @__PURE__ */ React.createElement("h1", { style: { font: "300 32px/1.05 var(--roma-serif)", letterSpacing: "-0.018em", margin: 0, color: "#f5f0e8" } }, /* @__PURE__ */ React.createElement("em", { style: { color: "#a87935" } }, orderNum), " ", "\xB7", " order delivered to every system."), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.6 var(--roma-sans)", color: "rgba(245,240,232,0.8)", marginTop: 12, maxWidth: 560 } }, customerName ? customerName + " has been emailed the receipt. " : "Receipt emailed. ", data.deliveryMethod === "pickup" ? "Will-call pickup at the Anaheim showroom." : data.deliveryMethod === "will_call" ? "Will-call at the distributor \u2014 print the release form once the PO is sent." : "Freight is on the schedule.", " ", "The order detail page is ready ", "\u2014", " go close another one.")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)", marginBottom: 6 } }, "Processed ", "\xB7", " ", (data.paymentMethods || []).length, " tender", (data.paymentMethods || []).length !== 1 ? "s" : ""), /* @__PURE__ */ React.createElement("div", { style: { font: "300 44px/1 var(--roma-serif)", color: "#f5f0e8", letterSpacing: "-0.018em" } }, "$", captured.toFixed(2)), showCost && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", color: "#a87935", textTransform: "uppercase", marginTop: 10 } }, "\u25CF", " $", grossProfit.toFixed(2), " margin (", margin.toFixed(1), "%) ", "\xB7", " $", commission.toFixed(2), " commission accruing"))), log.length > 0 && /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Transmission log",
        actions: /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.5625rem/1 ui-monospace, monospace", color: "#8a7e68" } }, log.length, " events")
      },
      log.map((entry, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "rof-log-row" }, /* @__PURE__ */ React.createElement("span", { className: "rof-log-dot " + (entry.ok ? "ok" : "fail") }), /* @__PURE__ */ React.createElement("span", { className: "rof-log-time" }, entry.time || ""), /* @__PURE__ */ React.createElement("span", { className: "rof-log-msg" }, entry.message)))
    ), /* @__PURE__ */ React.createElement("div", { className: "rof-next-grid" }, /* @__PURE__ */ React.createElement(WizRepCard, { title: "What's next" }, [
      "Customer receives confirmation email",
      "Review and send vendor POs",
      data.deliveryMethod === "shipping" ? "Track shipment & delivery" : "Notify customer for pickup",
      "Close out when fulfilled"
    ].map((text, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "rof-timeline-item" }, /* @__PURE__ */ React.createElement("span", { className: "rof-timeline-dot" }), /* @__PURE__ */ React.createElement("span", { className: "rof-timeline-text" }, text)))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Quick actions" }, order && /* @__PURE__ */ React.createElement("button", { className: "rof-quick-btn", onClick: () => navigate("order-detail", order.id) }, "View Order Details"), order && /* @__PURE__ */ React.createElement("button", { className: "rof-quick-btn", onClick: () => window.open("/api/rep/orders/" + order.id + "/receipt", "_blank") }, "Print Receipt"), /* @__PURE__ */ React.createElement("button", { className: "rof-quick-btn", onClick: () => navigate("orders") }, "All Orders"), /* @__PURE__ */ React.createElement("button", { className: "rof-quick-btn", onClick: () => onNewOrder ? onNewOrder() : navigate("order-create") }, "+ New Order"))));
  }
  function makeInitialOrderWizardData() {
    return {
      // Step 1 — Order
      customer: null,
      customerMode: "search",
      newCustomerName: "",
      newCustomerEmail: "",
      newCustomerPhone: "",
      items: [],
      promoCode: "",
      promoResult: null,
      deliveryMethod: "pickup",
      shippingAddress: { line1: "", line2: "", city: "", state: "", zip: "" },
      deliveryNotes: "",
      fulfillmentAddons: {},
      // Step 2 — Payment
      paymentMethod: "",
      paymentMethods: [],
      checkNumber: "",
      stripePaymentIntentId: null,
      documentIds: null,
      // Step 3 — POs
      posSent: false,
      // Step 4 — Done
      notes: "",
      createdOrder: null,
      transmissionLog: []
    };
  }
  function RepOrderWizard({ navigate, terminalRef, terminalStatus, connectReader, showCost }) {
    const [step, setStep] = useState(1);
    const [processing, setProcessing] = useState(false);
    const [showProcessModal, setShowProcessModal] = useState(false);
    const [activeAnchor, setActiveAnchor] = useState("customer");
    const [data, setData] = useState(makeInitialOrderWizardData);
    useRofDraft(
      "order-wizard",
      "order",
      () => !data.createdOrder && step < 4 && ((data.items || []).length > 0 || !!data.customer || !!((data.newCustomerName || "").trim() || (data.newCustomerEmail || "").trim() || (data.newCustomerPhone || "").trim())),
      () => ({ step: Math.min(step, 3), data: { ...data, createdOrder: null, transmissionLog: [], stripePaymentIntentId: null } })
    );
    useRofDraftRestore("order-wizard", (s) => {
      if (s.data) setData((d) => ({ ...d, ...s.data, createdOrder: null, transmissionLog: [] }));
      if (s.step) setStep(Math.min(parseInt(s.step) || 1, 3));
    });
    useEffect(() => {
      if (data.createdOrder) rofDraftStore.clear("order-wizard");
    }, [data.createdOrder]);
    const step1Valid = !!(data.customer && data.customer.name && data.customer.email || data.newCustomerName && data.newCustomerName.trim().split(/\s+/).length >= 2 && data.newCustomerEmail && isValidEmail(data.newCustomerEmail) && data.newCustomerPhone && isValidPhone(data.newCustomerPhone) && !data.dupMatch) && (data.items || []).length > 0 && (data.deliveryMethod === "pickup" || data.deliveryMethod === "will_call" || data.shippingAddress && data.shippingAddress.line1 && data.shippingAddress.city && data.shippingAddress.state && data.shippingAddress.zip);
    const itemsSubtotal = (data.items || []).reduce((s, i) => s + i.subtotal, 0);
    const discountAmt = data.promoResult ? data.promoResult.discount_amount : 0;
    const taxableSub = Math.max(0, itemsSubtotal - discountAmt);
    const wizDestZip = data.deliveryMethod !== "shipping" ? "92806" : data.shippingAddress && data.shippingAddress.zip ? data.shippingAddress.zip : "92806";
    const wizTaxRate = caTaxRate(wizDestZip);
    const wizEstTax = parseFloat((itemsSubtotal * wizTaxRate).toFixed(2));
    const wizShipping = data.deliveryMethod === "shipping" ? parseFloat(data.estShipping) || 0 : 0;
    const total = taxableSub + wizEstTax + wizShipping;
    const collectedAmount = (data.paymentMethods || []).reduce((s, p) => s + (parseFloat(p.amount) || 0), 0);
    const step2Valid = (data.paymentMethods || []).length > 0 && collectedAmount > 0.01;
    const canContinue = step === 1 ? step1Valid : step === 2 ? step2Valid : true;
    const orderNumber = data.createdOrder ? data.createdOrder.order_number || "R-" + data.createdOrder.id : null;
    const jumpToSection = (id) => {
      setActiveAnchor(id);
      const el = document.getElementById("rof-section-" + id);
      if (el) el.scrollIntoView({ behavior: "smooth", block: "start" });
    };
    const processOrder = () => setShowProcessModal(true);
    const startNewOrder = () => {
      rofDraftStore.clear("order-wizard");
      setData(makeInitialOrderWizardData());
      setActiveAnchor("customer");
      setShowProcessModal(false);
      setProcessing(false);
      setStep(1);
      window.scrollTo(0, 0);
    };
    return /* @__PURE__ */ React.createElement("div", { className: "rof-container" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar", style: { paddingTop: "1rem" } }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("orders") }, "Orders"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, orderNumber || "New"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", null, ROF_STEPS[step - 1] ? ROF_STEPS[step - 1].label : "Done"))), (() => {
      const custName = data.customer ? data.customer.name : data.newCustomerName || "";
      const custEmail = data.customer ? data.customer.email : data.newCustomerEmail || "";
      const custPhone = data.customer ? data.customer.phone : data.newCustomerPhone || "";
      const custInitials = (custName || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?";
      return /* @__PURE__ */ React.createElement("div", { className: "ws-head", style: { marginBottom: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, orderNumber ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Order ", /* @__PURE__ */ React.createElement("em", null, orderNumber)) : /* @__PURE__ */ React.createElement(React.Fragment, null, "New ", /* @__PURE__ */ React.createElement("em", null, "order")))), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, custInitials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, custName || "No customer yet"), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, custEmail || custPhone ? [custEmail, custPhone].filter(Boolean).join(" \xB7 ") : "Pick an existing customer or enter details below")))));
    })(), /* @__PURE__ */ React.createElement(RofStepRail, { step, setStep }), step <= 3 ? /* @__PURE__ */ React.createElement("div", { className: "rof-body" }, /* @__PURE__ */ React.createElement("div", { className: "rof-main" }, step === 1 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(RofSectionAnchors, { active: activeAnchor, onJump: jumpToSection }), /* @__PURE__ */ React.createElement(RofSectionHeader, { num: "01a", label: "Customer", id: "customer" }), /* @__PURE__ */ React.createElement(RofCustomerSection, { data, setData }), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Job / Sidemark" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement(
      SidemarkSelect,
      {
        value: data.jobName || "",
        onChange: (v) => setData((d) => ({ ...d, jobName: v })),
        customerId: data.customer && data.customer.id,
        email: data.customer && data.customer.email || data.newCustomerEmail,
        placeholder: "e.g. Smith residence \xB7 Unit 4B \xB7 Lobby remodel \u2014 optional, printed on vendor POs"
      }
    ))), /* @__PURE__ */ React.createElement(RofSectionHeader, { num: "01b", label: "Items", id: "items" }), /* @__PURE__ */ React.createElement(RofItemsSection, { data, setData, showCost }), /* @__PURE__ */ React.createElement(RofSectionHeader, { num: "01c", label: "Fulfillment", id: "fulfillment" }), /* @__PURE__ */ React.createElement(RofFulfillmentSection, { data, setData }), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Invoice note" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Note to customer ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", color: "#8a7e68" } }, "\xB7 printed on the invoice")), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        value: data.invoiceNotes || "",
        onChange: (e) => setData((d) => ({ ...d, invoiceNotes: e.target.value })),
        rows: 3,
        maxLength: 2e3,
        placeholder: "e.g. Thank you for your business \xB7 balance due on delivery \xB7 lead time 2\u20133 weeks \u2014 optional, printed on the invoice"
      }
    )))), step === 2 && /* @__PURE__ */ React.createElement(RofPaymentStep, { data, setData, setStep, terminalRef, terminalStatus, connectReader, onAutoPlace: processOrder }), step === 3 && /* @__PURE__ */ React.createElement(RofSendPOsStep, { data, setData, showCost, setStep })), /* @__PURE__ */ React.createElement("div", { className: "rof-sidebar" }, /* @__PURE__ */ React.createElement(RofRunningSummary, { data, setData, step }))) : /* @__PURE__ */ React.createElement(RofDoneStep, { data, navigate, showCost, onNewOrder: startNewOrder }), /* @__PURE__ */ React.createElement(
      RofActionBar,
      {
        step,
        setStep,
        canContinue,
        onProcess: processOrder,
        processing,
        navigate,
        total,
        orderNumber,
        itemCount: (data.items || []).length,
        customerName: data.customer ? data.customer.name : data.newCustomerName || "",
        deliveryMethod: data.deliveryMethod
      }
    ), showProcessModal && /* @__PURE__ */ React.createElement(
      RofProcessOrderModal,
      {
        data,
        setData,
        onComplete: () => {
          setShowProcessModal(false);
          setStep(3);
        },
        onCancel: () => setShowProcessModal(false)
      }
    ));
  }
  function ScopeToggle({ scope, setScope, onChange }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rep-scope-toggle", role: "group", "aria-label": "Ownership scope" }, [["mine", "Mine"], ["team", "Team"]].map(([k, label]) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: k,
        type: "button",
        className: scope === k ? "active" : "",
        onClick: () => {
          if (scope !== k) {
            setScope(k);
            onChange && onChange();
          }
        }
      },
      label
    )));
  }
  function RepSampleRequestsListView({ navigate }) {
    const [requests, setRequests] = useState([]);
    const [loading, setLoading] = useState(true);
    const [statusFilter, setStatusFilter] = useState("");
    const [search, setSearch] = useState("");
    const [activeView, setActiveView] = useState("all");
    const [scope, setScope] = useState("team");
    const [selected, setSelected] = useState(/* @__PURE__ */ new Set());
    const [page, setPage] = useState(1);
    const perPage = 50;
    const load = useCallback(() => {
      setLoading(true);
      const params = new URLSearchParams();
      if (statusFilter) params.set("status", statusFilter);
      if (search.trim()) params.set("search", search.trim());
      if (scope === "team") params.set("scope", "team");
      const qs = params.toString() ? "?" + params.toString() : "";
      repFetch("/api/rep/sample-requests" + qs).then((d) => {
        setRequests(d.sample_requests || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [statusFilter, search, scope]);
    useEffect(load, [load]);
    const viewFilters = {
      "open": (r) => r.status === "requested",
      "transit": (r) => r.status === "shipped",
      "delivered": (r) => r.status === "delivered",
      "cancelled": (r) => r.status === "cancelled",
      "all": () => true
    };
    const filtered = useMemo(() => {
      const fn = viewFilters[activeView] || (() => true);
      return requests.filter(fn);
    }, [requests, activeView]);
    const viewCounts = useMemo(() => ({
      "open": requests.filter(viewFilters["open"]).length,
      "transit": requests.filter(viewFilters["transit"]).length,
      "delivered": requests.filter(viewFilters["delivered"]).length,
      "cancelled": requests.filter(viewFilters["cancelled"]).length,
      "all": requests.length
    }), [requests]);
    const totalPages = Math.ceil(filtered.length / perPage);
    const pageRequests = filtered.slice((page - 1) * perPage, page * perPage);
    const toggleSelect = (id) => {
      setSelected((prev) => {
        const n = new Set(prev);
        n.has(id) ? n.delete(id) : n.add(id);
        return n;
      });
    };
    const toggleAll = () => {
      if (selected.size === pageRequests.length) setSelected(/* @__PURE__ */ new Set());
      else setSelected(new Set(pageRequests.map((r) => r.id)));
    };
    const getAge = (d) => {
      if (!d) return "\u2014";
      const days = Math.floor((Date.now() - new Date(d).getTime()) / 864e5);
      if (days === 0) return "today";
      return days + "d";
    };
    const fmtDay = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "";
    const getStatus = (r) => {
      const pickup = r.delivery_method === "pickup";
      if (r.status === "requested") return {
        label: "Requested",
        tone: "#a87935",
        step: pickup ? "Prepare for pickup" : "Needs shipment"
      };
      if (r.status === "shipped") return pickup ? { label: "Ready for pickup", tone: "#3a7a4e", step: "Customer notified" } : { label: "In transit", tone: "#3a7a4e", step: r.tracking_number ? "Trk " + r.tracking_number : "Shipped " + fmtDay(r.shipped_at) };
      if (r.status === "delivered") return {
        label: pickup ? "Picked up" : "Delivered",
        tone: "var(--brass-muted)",
        step: r.delivered_at ? fmtDay(r.delivered_at) : "Closed"
      };
      if (r.status === "cancelled") return {
        label: "Cancelled",
        tone: "#c54731",
        step: r.cancelled_at ? fmtDay(r.cancelled_at) : ""
      };
      return { label: r.status, tone: "var(--brass-muted)", step: "" };
    };
    const getFlagColor = (r) => {
      if (r.status !== "requested") return "transparent";
      const days = Math.floor((Date.now() - new Date(r.created_at).getTime()) / 864e5);
      return days >= 3 ? "#c54731" : "#a87935";
    };
    const VIEWS = [
      { key: "all", label: "All" },
      { key: "open", label: "To fulfill", tone: "#c54731" },
      { key: "transit", label: "In transit" },
      { key: "delivered", label: "Delivered" },
      { key: "cancelled", label: "Cancelled" }
    ];
    const STATUSES = [
      { value: "", label: "Status: Any" },
      { value: "requested", label: "Requested" },
      { value: "shipped", label: "Shipped" },
      { value: "delivered", label: "Delivered" },
      { value: "cancelled", label: "Cancelled" }
    ];
    const totalSamples = filtered.reduce((s, r) => s + (r.item_count || 0), 0);
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25CF", " Sample requests ", "\xB7", " ", requests.filter((r) => r.status === "requested" || r.status === "shipped").length, " active ", "\xB7", " ", requests.length, " total"), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Samples"))), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: () => navigate("sample-create") }, "+ New sample request"))), /* @__PURE__ */ React.createElement("div", { className: "ol-saved-views" }, VIEWS.map((v) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: v.key,
        className: "ol-view-tab" + (activeView === v.key ? " active" : ""),
        onClick: () => {
          setActiveView(v.key);
          setPage(1);
          setSelected(/* @__PURE__ */ new Set());
        }
      },
      v.label,
      /* @__PURE__ */ React.createElement("span", { className: "ol-tab-count", style: v.tone && viewCounts[v.key] > 0 ? { background: v.tone + "1a", color: v.tone } : void 0 }, viewCounts[v.key])
    )), /* @__PURE__ */ React.createElement(ScopeToggle, { scope, setScope, onChange: () => {
      setPage(1);
      setSelected(/* @__PURE__ */ new Set());
    } })), /* @__PURE__ */ React.createElement("div", { className: "ol-filter-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-filter-chips" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        className: "ol-chip",
        value: statusFilter,
        onChange: (e) => {
          setStatusFilter(e.target.value);
          setPage(1);
        },
        style: { appearance: "auto", paddingRight: 24 }
      },
      STATUSES.map((s) => /* @__PURE__ */ React.createElement("option", { key: s.value, value: s.value }, s.label))
    ), statusFilter && /* @__PURE__ */ React.createElement("span", { className: "ol-chip active", onClick: () => setStatusFilter("") }, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-label" }, "Status:"), " ", statusFilter, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-remove" }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search samples, customers",
        "aria-label": "Search sample requests",
        value: search,
        onChange: (e) => {
          setSearch(e.target.value);
          setPage(1);
        }
      }
    )), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, filtered.length, " rows"))), selected.size > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-label" }, "\u25CF", " ", selected.size, " request", selected.size !== 1 ? "s" : "", " selected"), /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: () => setSelected(/* @__PURE__ */ new Set()) }, "\u2715", " Clear"))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 22px", background: "#ece5d8", border: "0.5px solid rgba(28,25,23,0.14)" } }, "Loading sample requests", "\u2026") : /* @__PURE__ */ React.createElement("div", { className: "sl-table-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "sl-table-head" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "checkbox",
        style: { margin: 0 },
        "aria-label": "Select all sample requests on page",
        checked: pageRequests.length > 0 && selected.size === pageRequests.length,
        onChange: toggleAll
      }
    ), /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Request"), /* @__PURE__ */ React.createElement("span", null, "Customer"), /* @__PURE__ */ React.createElement("span", null, "Items"), /* @__PURE__ */ React.createElement("span", null, "Method"), /* @__PURE__ */ React.createElement("span", null, "State ", "\xB7", " step"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Age"), /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null)), pageRequests.map((r) => {
      const st = getStatus(r);
      const isSel = selected.has(r.id);
      const pickup = r.delivery_method === "pickup";
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: r.id,
          className: "sl-table-row" + (isSel ? " selected" : ""),
          role: "button",
          tabIndex: 0,
          onClick: () => {
            repListNav.samples = filtered.map((x) => x.id);
            navigate("sample-detail", r.id);
          },
          onKeyDown: (e) => {
            if (e.key === "Enter" || e.key === " ") {
              e.preventDefault();
              repListNav.samples = filtered.map((x) => x.id);
              navigate("sample-detail", r.id);
            }
          }
        },
        /* @__PURE__ */ React.createElement(
          "input",
          {
            type: "checkbox",
            style: { margin: 0 },
            checked: isSel,
            "aria-label": "Select sample request " + r.request_number,
            onChange: () => toggleSelect(r.id),
            onClick: (e) => e.stopPropagation(),
            onKeyDown: (e) => e.stopPropagation()
          }
        ),
        /* @__PURE__ */ React.createElement("span", { className: "ol-flag", style: { background: getFlagColor(r) } }),
        /* @__PURE__ */ React.createElement("span", { className: "sl-id" }, r.request_number),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "sl-cust-name" }, r.customer_name), r.customer_email && /* @__PURE__ */ React.createElement("div", { className: "sl-cust-sub" }, r.customer_email)),
        /* @__PURE__ */ React.createElement("span", { className: "sl-items" }, r.item_count, " item", r.item_count !== 1 ? "s" : ""),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "sl-method" }, pickup ? "Pickup" : "Shipping"), /* @__PURE__ */ React.createElement("div", { className: "sl-method-sub" }, pickup ? "Anaheim showroom" : [r.shipping_city, r.shipping_state].filter(Boolean).join(", ") || "\u2014")),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-state-label", style: { color: st.tone } }, "\u25CF", " ", st.label), st.step && /* @__PURE__ */ React.createElement("div", { className: "ol-state-step" }, st.step)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-age" }, getAge(r.created_at)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-install" }, fmtDay(r.created_at)),
        /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)", textAlign: "right" } }, "\u2192")
      );
    }), pageRequests.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif", marginBottom: 14 } }, "No sample requests match this view."), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: () => navigate("sample-create") }, "+ New sample request")), filtered.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-footer" }, /* @__PURE__ */ React.createElement("span", null, "Showing ", Math.min((page - 1) * perPage + 1, filtered.length), "\u2013", Math.min(page * perPage, filtered.length), " of ", filtered.length), /* @__PURE__ */ React.createElement("span", null, totalSamples, " sample", totalSamples !== 1 ? "s" : "", " across ", filtered.length, " request", filtered.length !== 1 ? "s" : "")), totalPages > 1 && /* @__PURE__ */ React.createElement("div", { className: "ol-pagination" }, /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, "Page ", page, " of ", totalPages), /* @__PURE__ */ React.createElement("div", { className: "ol-page-btns" }, /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page <= 1, onClick: () => setPage((p) => p - 1) }, "\u2190"), Array.from({ length: Math.min(totalPages, 5) }, (_, i) => i + 1).map((p) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: p,
        className: "ol-page-btn" + (page === p ? " active" : ""),
        onClick: () => setPage(p)
      },
      p
    )), totalPages > 5 && /* @__PURE__ */ React.createElement("span", { className: "ol-meta", style: { padding: "4px 6px" } }, "\xB7\xB7\xB7"), /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page >= totalPages, onClick: () => setPage((p) => p + 1) }, "\u2192")))));
  }
  function RepSampleRequestCreateView({ navigate }) {
    const [pickedCustomer, setPickedCustomer] = useState(null);
    const [customerName, setCustomerName] = useState("");
    const [customerEmail, setCustomerEmail] = useState("");
    const [customerPhone, setCustomerPhone] = useState("");
    const [companyName, setCompanyName] = useState("");
    const [sidemark, setSidemark] = useState("");
    const [addrLine1, setAddrLine1] = useState("");
    const [addrLine2, setAddrLine2] = useState("");
    const [addrCity, setAddrCity] = useState("");
    const [addrState, setAddrState] = useState("");
    const [addrZip, setAddrZip] = useState("");
    const [deliveryMethod, setDeliveryMethod] = useState("pickup");
    const [notes, setNotes] = useState("");
    const [stagedNotes, setStagedNotes] = useState([]);
    const [items, setItems] = useState([]);
    const [skuSearch, setSkuSearch] = useState("");
    const [skuResults, setSkuResults] = useState([]);
    const [skuSearching, setSkuSearching] = useState(false);
    const skuSearchTimer = useRef(null);
    const skuSearchCounter = useRef(0);
    const [saving, setSaving] = useState(false);
    const sampleSavedRef = useRef(false);
    useRofDraft(
      "sample-create",
      "sample request",
      () => !sampleSavedRef.current && (items.length > 0 || !!pickedCustomer || !!(customerName.trim() || customerEmail.trim() || customerPhone.trim() || notes.trim())),
      () => ({ pickedCustomer, customerName, customerEmail, customerPhone, companyName, sidemark, addrLine1, addrLine2, addrCity, addrState, addrZip, deliveryMethod, notes, stagedNotes, items })
    );
    useRofDraftRestore("sample-create", (s) => {
      if (s.pickedCustomer) setPickedCustomer(s.pickedCustomer);
      setCustomerName(s.customerName || "");
      setCustomerEmail(s.customerEmail || "");
      setCustomerPhone(s.customerPhone || "");
      setCompanyName(s.companyName || "");
      setSidemark(s.sidemark || "");
      setAddrLine1(s.addrLine1 || "");
      setAddrLine2(s.addrLine2 || "");
      setAddrCity(s.addrCity || "");
      setAddrState(s.addrState || "");
      setAddrZip(s.addrZip || "");
      if (s.deliveryMethod) setDeliveryMethod(s.deliveryMethod);
      setNotes(s.notes || "");
      if (Array.isArray(s.stagedNotes)) setStagedNotes(s.stagedNotes);
      if (Array.isArray(s.items)) setItems(s.items);
    });
    const searchSku = (q) => {
      setSkuSearch(q);
      if (skuSearchTimer.current) clearTimeout(skuSearchTimer.current);
      const thisSearch = ++skuSearchCounter.current;
      if (q.length < 2) {
        setSkuResults([]);
        setSkuSearching(false);
        return;
      }
      setSkuSearching(true);
      skuSearchTimer.current = setTimeout(async () => {
        try {
          const data = await repFetch("/api/rep/skus/search?q=" + encodeURIComponent(q));
          if (thisSearch !== skuSearchCounter.current) return;
          setSkuResults(data.results || []);
        } catch (e) {
          if (thisSearch !== skuSearchCounter.current) return;
        }
        if (thisSearch === skuSearchCounter.current) setSkuSearching(false);
      }, 250);
    };
    const addProduct = (r) => {
      if (items.length >= 5) return alert("Maximum 5 items per sample request");
      if (items.find((i) => i.product_id === r.product_id)) return alert("This product is already added");
      setItems((prev) => [...prev, {
        _key: Date.now(),
        product_id: r.product_id || null,
        sku_id: r.sku_id,
        product_name: r.product_name,
        collection: r.collection,
        variant_name: r.variant_name,
        vendor_name: r.vendor_name
      }]);
      setSkuSearch("");
      setSkuResults([]);
    };
    const [catalogOpen, setCatalogOpen] = useState(false);
    const addFromCatalog = ({ lines, accessories, customs }) => {
      const incoming = [
        ...(lines || []).map(({ item }) => ({
          product_id: item.id || null,
          sku_id: item.sku_id,
          product_name: item.display_name || item.name || "",
          collection: item.collection || "",
          variant_name: item.variant_name || "",
          vendor_name: item.brand_name || item.vendor_name || ""
        })),
        ...(accessories || []).map(({ acc }) => ({
          product_id: acc.product_id || null,
          sku_id: acc.sku_id,
          product_name: acc.display_name || acc.name || acc.product_name || "",
          collection: acc.collection || "",
          variant_name: acc.variant_name || "",
          vendor_name: acc.brand_name || acc.vendor_name || ""
        }))
      ];
      let skipped = (customs || []).length;
      setItems((prev) => {
        const next = [...prev];
        let key = Date.now();
        for (const it of incoming) {
          if (next.length >= 5 || next.find((x) => x.sku_id === it.sku_id)) {
            skipped++;
            continue;
          }
          next.push({ _key: key++, ...it });
        }
        return next;
      });
      if (skipped) setTimeout(() => alert(skipped + " selection" + (skipped !== 1 ? "s" : "") + " skipped \u2014 5-sample limit, duplicates, or custom lines"), 50);
    };
    useEffect(() => {
      const pending = consumePendingAdd("sample");
      if (pending && pending.sku) addProduct(pending.sku);
    }, []);
    const removeItem = (key) => setItems((prev) => prev.filter((i) => i._key !== key));
    const submitRequest = async () => {
      if (customerName.trim().split(/\s+/).length < 2) return alert("Customer first and last name are required");
      if (!customerEmail.trim() || !isValidEmail(customerEmail.trim())) return alert("A valid customer email is required");
      if (!customerPhone.trim() || !isValidPhone(customerPhone)) return alert("A valid 10-digit customer phone is required");
      if (deliveryMethod === "shipping") {
        if (!addrLine1.trim()) return alert("Shipping address is required");
        if (!addrCity.trim()) return alert("City is required");
        if (!addrState.trim()) return alert("State is required");
        if (!addrZip.trim()) return alert("ZIP code is required");
      }
      if (!items.length) return alert("Add at least one product");
      setSaving(true);
      try {
        const body = {
          customer_name: customerName.trim(),
          customer_email: customerEmail.trim() || null,
          customer_phone: customerPhone.trim() || null,
          company_name: companyName.trim() || null,
          sidemark: sidemark.trim() || null,
          delivery_method: deliveryMethod,
          shipping_address_line1: deliveryMethod === "shipping" ? addrLine1.trim() || null : null,
          shipping_address_line2: deliveryMethod === "shipping" ? addrLine2.trim() || null : null,
          shipping_city: deliveryMethod === "shipping" ? addrCity.trim() || null : null,
          shipping_state: deliveryMethod === "shipping" ? addrState.trim() || null : null,
          shipping_zip: deliveryMethod === "shipping" ? addrZip.trim() || null : null,
          notes: notes.trim() || null,
          internal_notes: stagedNotes.map((n) => n.note).filter((t) => t && t.trim()),
          items: items.map((i) => ({ product_id: i.product_id, sku_id: i.sku_id }))
        };
        const created = await repFetch("/api/rep/sample-requests", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(body)
        });
        sampleSavedRef.current = true;
        rofDraftStore.clear("sample-create");
        repToast("Sample request saved");
        navigate("sample-detail", created.sample_request.id);
      } catch (err) {
        alert("Error: " + (err.message || "Failed to create"));
      }
      setSaving(false);
    };
    const scLabel = { display: "block", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 7 };
    const scField = { width: "100%", boxSizing: "border-box", padding: "10px 12px", border: "0.5px solid rgba(28,25,23,0.3)", background: "rgba(255,255,255,0.35)", color: "var(--rod-ink)", font: "400 14px/1.3 Inter, sans-serif", outline: "none" };
    return /* @__PURE__ */ React.createElement("div", { className: "sc-create" }, /* @__PURE__ */ React.createElement("div", { className: "rod-breadcrumb" }, /* @__PURE__ */ React.createElement("div", { className: "rod-breadcrumb-trail" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "rod-breadcrumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("samples") }, "Samples"), /* @__PURE__ */ React.createElement("span", { className: "rod-breadcrumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "rod-current" }, "New request"))), /* @__PURE__ */ React.createElement("div", { className: "rod-header" }, /* @__PURE__ */ React.createElement("div", { className: "rod-header-left" }, /* @__PURE__ */ React.createElement("div", { className: "rod-status-pill", style: { background: "rgba(168,121,53,0.12)", color: "var(--rod-accent)" } }, "\u25CF Draft \xB7 not saved"), /* @__PURE__ */ React.createElement("h1", null, "New ", /* @__PURE__ */ React.createElement("em", null, "sample request")), /* @__PURE__ */ React.createElement("div", { className: "rod-header-subtitle" }, "Up to 5 free samples \xB7 in-store pickup is free \xB7 $12 flat-rate shipping")), /* @__PURE__ */ React.createElement("div", { className: "rod-header-money" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-header-money-label" }, "Samples"), /* @__PURE__ */ React.createElement("div", { className: "rod-header-money-value" }, items.length, /* @__PURE__ */ React.createElement("span", { style: { font: '400 15px/1 "Cormorant Garamond", serif', color: "var(--rod-muted)" } }, " / 5"))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-header-money-label" }, "Shipping"), /* @__PURE__ */ React.createElement("div", { className: "rod-header-money-value", style: deliveryMethod === "pickup" ? { color: "var(--rod-green)" } : void 0 }, deliveryMethod === "pickup" ? "Free" : "$12.00")))), /* @__PURE__ */ React.createElement("div", { className: "rod-body" }, /* @__PURE__ */ React.createElement("div", { className: "rod-col" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-header" }, /* @__PURE__ */ React.createElement("span", { className: "rod-card-title" }, "Customer"), /* @__PURE__ */ React.createElement("div", { className: "rod-card-actions" }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--rod-muted)" } }, "Drives the vendor request & notifications"))), /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded" }, /* @__PURE__ */ React.createElement(
      RepCustomerEntry,
      {
        selected: pickedCustomer,
        onSelect: (c) => {
          setPickedCustomer(c);
          setCustomerName(c.name || "");
          setCustomerEmail(c.email || "");
          setCustomerPhone(c.phone || "");
          setCompanyName(c.company_name || "");
          if (c.address_line1) {
            setAddrLine1(c.address_line1);
            setAddrLine2(c.address_line2 || "");
            setAddrCity(c.city || "");
            setAddrState(c.state || "");
            setAddrZip(c.zip || "");
          }
        },
        onClear: () => {
          setPickedCustomer(null);
          setCustomerName("");
          setCustomerEmail("");
          setCustomerPhone("");
          setCompanyName("");
        },
        name: customerName,
        email: customerEmail,
        phone: customerPhone,
        onName: setCustomerName,
        onEmail: setCustomerEmail,
        onPhone: setCustomerPhone,
        company: companyName,
        onCompany: setCompanyName,
        requirePhone: true
      }
    ), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14 } }, /* @__PURE__ */ React.createElement("label", { style: scLabel }, "Sidemark ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", letterSpacing: 0, color: "var(--rod-muted)" } }, "\xB7 job reference (optional)")), /* @__PURE__ */ React.createElement(
      SidemarkSelect,
      {
        value: sidemark,
        onChange: setSidemark,
        style: scField,
        customerId: pickedCustomer && pickedCustomer.id,
        email: customerEmail,
        placeholder: "e.g. Smith Residence \u2014 Master Bath"
      }
    )))), /* @__PURE__ */ React.createElement("div", { className: "rod-card" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-header" }, /* @__PURE__ */ React.createElement("span", { className: "rod-card-title" }, "Samples \xB7 ", items.length, " of 5"), /* @__PURE__ */ React.createElement("div", { className: "rod-card-actions" }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--rod-green)" } }, "\u25CF Free samples"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px 0" } }, /* @__PURE__ */ React.createElement("button", { onClick: () => setCatalogOpen(true), style: { width: "100%", padding: "14px 16px", cursor: "pointer", textAlign: "left", border: "0.5px solid var(--rod-ink)", background: "var(--rod-ink)", color: "#ece5d8", display: "grid", gridTemplateColumns: "40px 1fr auto", gap: 14, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { width: 38, height: 38, display: "flex", alignItems: "center", justifyContent: "center", border: "0.5px solid rgba(236,229,216,0.27)", font: '400 20px/1 "Cormorant Garamond", serif' } }, "\u25A6"), /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: '400 17px/1.15 "Cormorant Garamond", serif', letterSpacing: "-0.01em" } }, "Browse the full catalog"), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "rgba(236,229,216,0.67)", marginTop: 4 } }, "Filter by vendor ", "\xB7", " pick up to 5 free samples ", "\xB7", " accessories too")), /* @__PURE__ */ React.createElement("span", { style: { padding: "7px 14px", border: "0.5px solid rgba(236,229,216,0.33)", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase" } }, "Open catalog ", "\u2192"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderBottom: "0.5px solid var(--rod-border)" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 12, top: 10, font: '400 15px/1 "Cormorant Garamond", serif', color: "var(--rod-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        style: { ...scField, paddingLeft: 34 },
        value: skuSearch,
        onBlur: () => setTimeout(() => {
          setSkuResults([]);
          setSkuSearching(false);
        }, 200),
        onChange: (e) => searchSku(e.target.value),
        placeholder: "Quick add \u2014 search by product name, SKU, or collection\u2026"
      }
    ), skuSearching && skuResults.length === 0 && skuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, /* @__PURE__ */ React.createElement("div", { className: "search-loading-indicator" }, "Searching...")), !skuSearching && skuResults.length === 0 && skuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, /* @__PURE__ */ React.createElement("div", { className: "search-no-results" }, "No results found")), skuResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, skuResults.map((r) => /* @__PURE__ */ React.createElement("div", { key: r.sku_id, className: "search-result-item", onClick: () => addProduct(r) }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { className: "sku-thumb", src: optimizeImg(r.primary_image, 96), alt: "", loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { className: "sku-thumb-placeholder" }, /* @__PURE__ */ React.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5" }, /* @__PURE__ */ React.createElement("rect", { x: "3", y: "3", width: "18", height: "18", rx: "2" }), /* @__PURE__ */ React.createElement("circle", { cx: "8.5", cy: "8.5", r: "1.5" }), /* @__PURE__ */ React.createElement("path", { d: "M21 15l-5-5L5 21" }))), /* @__PURE__ */ React.createElement("div", { className: "sku-info" }, formatLineItem(r).brand && /* @__PURE__ */ React.createElement("div", { className: "product-meta" }, formatLineItem(r).brand), /* @__PURE__ */ React.createElement("div", { className: "sku-name" }, formatLineItem(r).name), formatLineItem(r).sku && /* @__PURE__ */ React.createElement("div", { className: "product-meta" }, formatLineItem(r).sku))))))), items.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "36px 20px", textAlign: "center", color: "var(--rod-muted)", font: "400 13px/1.5 Inter, sans-serif" } }, "Nothing selected yet.", /* @__PURE__ */ React.createElement("br", null), "Search the catalog above \u2014 each request carries up to 5 free samples."), items.map((item, i) => /* @__PURE__ */ React.createElement("div", { key: item._key, style: {
      padding: "12px 20px",
      display: "grid",
      gridTemplateColumns: "44px 1fr auto 24px",
      gap: 14,
      alignItems: "center",
      borderBottom: i < items.length - 1 ? "0.5px solid rgba(28,25,23,0.06)" : "none"
    } }, /* @__PURE__ */ React.createElement("div", { style: { width: 44, height: 40, background: "var(--rod-warm)", display: "flex", alignItems: "center", justifyContent: "center", font: '400 16px/1 "Cormorant Garamond", serif', color: "var(--rod-muted)" } }, "\u25EB"), /* @__PURE__ */ React.createElement("div", null, formatLineItem(item).brand && /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta" }, formatLineItem(item).brand), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-name" }, formatLineItem(item).name), formatLineItem(item).sku && /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta" }, formatLineItem(item).sku)), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--rod-green)" } }, "Free"), /* @__PURE__ */ React.createElement("button", { onClick: () => removeItem(item._key), "aria-label": "Remove sample", style: { width: 24, height: 24, border: "none", background: "transparent", color: "var(--rod-muted)", cursor: "pointer", font: "400 16px/1 Inter, sans-serif", padding: 0 } }, "\xD7")))), /* @__PURE__ */ React.createElement("div", { className: "rod-card" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-header" }, /* @__PURE__ */ React.createElement("span", { className: "rod-card-title" }, "How is the customer receiving the samples?")), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 } }, [
      { id: "pickup", icon: "\u25EB", label: "In-store pickup", sub: "Customer collects at the Anaheim showroom", cost: "Free", on: deliveryMethod === "pickup" },
      { id: "shipping", icon: "\u293F", label: "Ship to customer", sub: "USPS / parcel to the address below", cost: "$12 flat rate", on: deliveryMethod === "shipping" }
    ].map((opt) => /* @__PURE__ */ React.createElement("button", { key: opt.id, onClick: () => setDeliveryMethod(opt.id), style: {
      padding: 16,
      background: opt.on ? "var(--rod-warm)" : "transparent",
      border: "0.5px solid " + (opt.on ? "var(--rod-ink)" : "rgba(28,25,23,0.2)"),
      cursor: "pointer",
      textAlign: "left",
      display: "grid",
      gridTemplateColumns: "40px 1fr 18px",
      gap: 12,
      alignItems: "flex-start"
    } }, /* @__PURE__ */ React.createElement("span", { style: { width: 38, height: 38, display: "flex", alignItems: "center", justifyContent: "center", font: '400 19px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)", border: "0.5px solid rgba(28,25,23,0.2)" } }, opt.icon), /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: '400 16px/1.2 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, opt.label), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 12px/1.5 Inter, sans-serif", color: "rgba(28,25,23,0.7)", marginTop: 3 } }, opt.sub), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: opt.on ? "var(--rod-accent)" : "var(--rod-muted)", marginTop: 7 } }, "\u25CF ", opt.cost)), /* @__PURE__ */ React.createElement("span", { style: { width: 15, height: 15, borderRadius: "50%", marginTop: 3, border: "1px solid " + (opt.on ? "var(--rod-ink)" : "rgba(28,25,23,0.35)"), background: opt.on ? "var(--rod-ink)" : "transparent", display: "flex", alignItems: "center", justifyContent: "center" } }, opt.on && /* @__PURE__ */ React.createElement("span", { style: { width: 5, height: 5, borderRadius: "50%", background: "#ece5d8" } }))))), deliveryMethod === "shipping" && /* @__PURE__ */ React.createElement("div", { style: { padding: "4px 20px 18px", display: "grid", gap: 14 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: scLabel }, "Address line 1 *"), /* @__PURE__ */ React.createElement("input", { style: scField, value: addrLine1, onChange: (e) => setAddrLine1(e.target.value), placeholder: "Street address", required: true })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: scLabel }, "Address line 2"), /* @__PURE__ */ React.createElement("input", { style: scField, value: addrLine2, onChange: (e) => setAddrLine2(e.target.value), placeholder: "Apt, suite, unit\u2026" })), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "2fr 1fr 1fr", gap: 14 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: scLabel }, "City *"), /* @__PURE__ */ React.createElement("input", { style: scField, value: addrCity, onChange: (e) => setAddrCity(e.target.value), placeholder: "City", required: true })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: scLabel }, "State *"), /* @__PURE__ */ React.createElement("input", { style: scField, value: addrState, onChange: (e) => setAddrState(e.target.value), placeholder: "CA", maxLength: 2, required: true })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { style: scLabel }, "ZIP *"), /* @__PURE__ */ React.createElement("input", { style: scField, value: addrZip, onChange: (e) => setAddrZip(e.target.value), placeholder: "90210", maxLength: 10, required: true })))), deliveryMethod === "pickup" && /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 20px", background: "var(--rod-warm)", borderTop: "0.5px solid var(--rod-border)", font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--rod-muted)" } }, "\u25CF Will-call at 1440 S State College Blvd #6M, Anaheim \xB7 no shipping charge"))), /* @__PURE__ */ React.createElement("div", { className: "rod-col", style: { position: "sticky", top: 24 } }, /* @__PURE__ */ React.createElement("div", { className: "rod-card" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-header" }, /* @__PURE__ */ React.createElement("span", { className: "rod-card-title" }, "Request so far")), /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 12 } }, /* @__PURE__ */ React.createElement("div", { style: { paddingBottom: 10, borderBottom: "0.5px solid rgba(28,25,23,0.08)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 15px/1.3 Inter, sans-serif", color: customerName.trim() ? "var(--rod-ink)" : "var(--rod-muted)" } }, customerName.trim() || "No customer yet"), customerEmail.trim() && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--rod-muted)", marginTop: 3 } }, customerEmail.trim())), [
      ["Samples", items.length + " of 5"],
      ...sidemark.trim() ? [["Sidemark", sidemark.trim()]] : [],
      ["Delivery", deliveryMethod === "pickup" ? "In-store pickup" : "Ship to customer"],
      ["Shipping fee", deliveryMethod === "pickup" ? "Free" : "$12.00 flat rate"]
    ].map(([l, v]) => /* @__PURE__ */ React.createElement("div", { key: l, style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", paddingBottom: 6, borderBottom: "0.5px solid rgba(28,25,23,0.06)", font: "400 13px/1.3 Inter, sans-serif" } }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)" } }, l), /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, v))), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rod-btn rod-btn-primary",
        disabled: saving,
        onClick: submitRequest,
        style: { width: "100%", justifyContent: "center", padding: "12px 0", marginTop: 4, background: "var(--rod-ink)", color: "#fff", border: "0.5px solid var(--rod-ink)", cursor: saving ? "default" : "pointer", font: "500 12px/1 Inter, sans-serif", letterSpacing: "0.06em", textTransform: "uppercase", display: "flex", alignItems: "center" }
      },
      saving ? "Creating\u2026" : "Create sample request \u2192"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick: () => navigate("samples"),
        style: { width: "100%", padding: "10px 0", background: "transparent", color: "var(--rod-ink)", border: "0.5px solid rgba(28,25,23,0.25)", cursor: "pointer", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.06em", textTransform: "uppercase" }
      },
      "Cancel"
    ))), /* @__PURE__ */ React.createElement(InternalNotesCard, { staged: stagedNotes, onStaged: setStagedNotes, bg: "#f5f0e8" }), /* @__PURE__ */ React.createElement("div", { className: "rod-card" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-header" }, /* @__PURE__ */ React.createElement("span", { className: "rod-card-title" }, "What happens next")), /* @__PURE__ */ React.createElement("div", { style: { padding: "6px 0" } }, [
      ["01", "Vendors get an email request", "One per vendor \xB7 grouped automatically"],
      ["02", "Samples marked ready", "Track per-sample readiness on the request"],
      ["03", deliveryMethod === "pickup" ? "Customer notified for pickup" : "Shipped with tracking", deliveryMethod === "pickup" ? "Will-call at the showroom" : "$12 flat rate \xB7 collected up front"],
      ["04", "Delivered \xB7 follow up", "Turn the samples into a quote"]
    ].map(([n, l, d], i, arr) => /* @__PURE__ */ React.createElement("div", { key: n, style: { padding: "10px 20px", display: "grid", gridTemplateColumns: "26px 1fr", gap: 12, alignItems: "flex-start", borderBottom: i < arr.length - 1 ? "0.5px solid rgba(28,25,23,0.06)" : "none" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1.6 ui-monospace, monospace", letterSpacing: "0.1em", color: "var(--rod-muted)" } }, n), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 Inter, sans-serif", color: "var(--rod-ink)" } }, l), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--rod-muted)", marginTop: 2 } }, d)))))))), catalogOpen && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data: { customer: null, newCustomerName: customerName.trim() || "Sample request" },
        showCost: false,
        contextLabel: "Add samples \xB7 new request",
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog
      }
    ));
  }
  function RepSampleRequestDetailView({ navigate, editId, terminalRef, terminalStatus, connectReader }) {
    const [sr, setSr] = useState(null);
    const [items, setItems] = useState([]);
    const [loading, setLoading] = useState(true);
    const [trackingInput, setTrackingInput] = useState("");
    const [showTrackingInput, setShowTrackingInput] = useState(false);
    const [acting, setActing] = useState(false);
    const [sampleTasks, setSampleTasks] = useState([]);
    const load = useCallback(() => {
      setLoading(true);
      repFetch("/api/rep/sample-requests/" + editId).then((d) => {
        setSr(d.sample_request);
        setItems(d.items || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [editId]);
    const loadSampleTasks = useCallback(() => {
      repFetch("/api/rep/tasks?linked_sample_request_id=" + editId).then((d) => setSampleTasks(d.tasks || [])).catch(() => {
      });
    }, [editId]);
    useEffect(load, [load]);
    useEffect(loadSampleTasks, [loadSampleTasks]);
    const toggleTaskDone = async (t) => {
      try {
        await repFetch("/api/rep/tasks/" + t.id + "/" + (t.status === "completed" ? "reopen" : "complete"), { method: "PUT" });
        loadSampleTasks();
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
    };
    const markShipped = async () => {
      setActing(true);
      try {
        await repFetch("/api/rep/sample-requests/" + editId + "/ship", {
          method: "PUT",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ tracking_number: trackingInput.trim() || null })
        });
        setShowTrackingInput(false);
        const wasPickup = sr && sr.delivery_method === "pickup";
        repToast(sr && sr.customer_email ? "Customer notified" : wasPickup ? "Marked ready for pickup" : "Marked shipped");
        load();
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
      setActing(false);
    };
    const markDelivered = async () => {
      setActing(true);
      try {
        await repFetch("/api/rep/sample-requests/" + editId + "/deliver", { method: "PUT" });
        repToast("Marked delivered");
        load();
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
      setActing(false);
    };
    const cancelRequest = async () => {
      if (!confirm("Cancel this sample request?")) return;
      setActing(true);
      try {
        await repFetch("/api/rep/sample-requests/" + editId + "/cancel", { method: "PUT" });
        repToast("Sample request cancelled");
        load();
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
      setActing(false);
    };
    const [editingNoteId, setEditingNoteId] = useState(null);
    const [noteDraft, setNoteDraft] = useState("");
    const [paymentActing, setPaymentActing] = useState(false);
    const [showPayModal, setShowPayModal] = useState(false);
    const [readyPrompt, setReadyPrompt] = useState(false);
    const [notifying, setNotifying] = useState(false);
    const [removeTarget, setRemoveTarget] = useState(null);
    const [removing, setRemoving] = useState(false);
    const confirmRemoveItem = async () => {
      if (!removeTarget) return;
      setRemoving(true);
      try {
        await repFetch("/api/rep/sample-requests/" + editId + "/items/" + removeTarget.id + "/status", {
          method: "PUT",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ status: "cancelled" })
        });
        setItems((prev) => prev.map((it) => it.id === removeTarget.id ? { ...it, status: "cancelled" } : it));
        repToast("Sample removed");
        setRemoveTarget(null);
      } catch (err) {
        alert("Error: " + (err.message || "Failed to remove"));
      }
      setRemoving(false);
    };
    const notifyReady = async () => {
      setNotifying(true);
      try {
        const data = await repFetch("/api/rep/sample-requests/" + editId + "/notify-ready", { method: "POST" });
        setSr((prev) => ({ ...prev, all_ready_notified_at: data && data.notified_at || (/* @__PURE__ */ new Date()).toISOString(), status: data && data.status || prev.status }));
        repToast(data && data.sent ? "Ready email sent to the customer" : "Marked notified \u2014 no customer email on file");
        setReadyPrompt(false);
      } catch (err) {
        if (err && (err.already_sent || /already been sent/i.test(err.message || ""))) {
          setSr((prev) => ({ ...prev, all_ready_notified_at: (/* @__PURE__ */ new Date()).toISOString() }));
          setReadyPrompt(false);
        } else {
          alert("Error: " + (err && err.message || "Failed to send"));
        }
      }
      setNotifying(false);
    };
    const markPaymentCollected = async (collected, method, res) => {
      setPaymentActing(true);
      try {
        await repFetch("/api/rep/sample-requests/" + editId + "/payment-status", {
          method: "PUT",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ collected, method: method || null, stripe_payment_intent_id: res && res.payment_intent_id || null })
        });
        load();
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
      setPaymentActing(false);
    };
    const refundSampleShipping = async () => {
      if (!window.confirm("Refund the sample shipping fee to the customer\u2019s card?")) return;
      setPaymentActing(true);
      try {
        const data = await repFetch("/api/rep/sample-requests/" + editId + "/refund", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({})
        });
        repToast("Refunded $" + (data && data.refunded != null ? Number(data.refunded).toFixed(2) : ""));
        load();
      } catch (err) {
        alert("Error: " + (err.message || "Refund failed"));
      }
      setPaymentActing(false);
    };
    const [addSkuSearch, setAddSkuSearch] = useState("");
    const [addSkuResults, setAddSkuResults] = useState([]);
    const [addSkuSearching, setAddSkuSearching] = useState(false);
    const addSkuSearchTimer = useRef(null);
    const [addingItem, setAddingItem] = useState(false);
    const searchSkuToAdd = (q) => {
      setAddSkuSearch(q);
      if (addSkuSearchTimer.current) clearTimeout(addSkuSearchTimer.current);
      if (q.length < 2) {
        setAddSkuResults([]);
        setAddSkuSearching(false);
        return;
      }
      setAddSkuSearching(true);
      addSkuSearchTimer.current = setTimeout(async () => {
        try {
          const data = await repFetch("/api/rep/skus/search?q=" + encodeURIComponent(q));
          setAddSkuResults(data.results || []);
        } catch (e) {
        }
        setAddSkuSearching(false);
      }, 250);
    };
    const addItemToRequest = async (result) => {
      const activeCount = items.filter((i) => i.status !== "cancelled").length;
      if (activeCount >= 5) return alert("Maximum 5 active items per sample request");
      if (items.find((i) => i.product_id === result.product_id && i.status !== "cancelled")) return alert("This product is already in the request");
      setAddingItem(true);
      try {
        const data = await repFetch("/api/rep/sample-requests/" + editId + "/add-items", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ items: [{ product_id: result.product_id, sku_id: result.sku_id }] })
        });
        setAddSkuSearch("");
        setAddSkuResults([]);
        load();
      } catch (err) {
        alert("Error: " + (err.message || "Failed to add item"));
      }
      setAddingItem(false);
    };
    const [catalogOpen, setCatalogOpen] = useState(false);
    const addFromCatalog = async ({ lines, accessories, customs }) => {
      const incoming = [
        ...(lines || []).map(({ item }) => ({ product_id: item.id || null, sku_id: item.sku_id })),
        ...(accessories || []).map(({ acc }) => ({ product_id: acc.product_id || null, sku_id: acc.sku_id }))
      ];
      const active = items.filter((i) => i.status !== "cancelled");
      const existing = new Set(active.map((i) => i.sku_id));
      const room = Math.max(0, 5 - active.length);
      const fresh = incoming.filter((i) => i.sku_id && !existing.has(i.sku_id)).slice(0, room);
      const skipped = incoming.length - fresh.length + (customs || []).length;
      if (!fresh.length) {
        alert("Nothing added \u2014 5-sample limit reached, duplicates, or custom lines");
        return;
      }
      setAddingItem(true);
      try {
        await repFetch("/api/rep/sample-requests/" + editId + "/add-items", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ items: fresh })
        });
        load();
        if (skipped) setTimeout(() => alert(skipped + " selection" + (skipped !== 1 ? "s" : "") + " skipped \u2014 5-sample limit, duplicates, or custom lines"), 50);
      } catch (err) {
        alert("Error: " + (err.message || "Failed to add items"));
      }
      setAddingItem(false);
    };
    const [vendorShipTo, setVendorShipTo] = useState({});
    const [sendingVendorId, setSendingVendorId] = useState(null);
    const [vendorSendResults, setVendorSendResults] = useState({});
    const [sendVendorTarget, setSendVendorTarget] = useState(null);
    const [sendTo, setSendTo] = useState("");
    const [sendCc, setSendCc] = useState("");
    const confirmSendToVendor = async () => {
      if (!sendVendorTarget) return;
      const vendorId = sendVendorTarget.vendor_id;
      const shipTo = vendorShipTo[vendorId] || "store";
      setSendingVendorId(vendorId);
      try {
        const data = await repFetch("/api/rep/sample-requests/" + editId + "/send-to-vendor", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            vendor_id: vendorId,
            ship_to: shipTo,
            recipient_email: sendTo.trim(),
            cc_emails: sendCc.split(",").map((s) => s.trim()).filter(Boolean)
          })
        });
        setVendorSendResults((prev) => ({ ...prev, [vendorId]: data }));
        setSendVendorTarget(null);
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
      setSendingVendorId(null);
    };
    const toggleItemReady = async (itemId, currentStatus) => {
      const newStatus = currentStatus === "ready" ? "pending" : "ready";
      try {
        const data = await repFetch("/api/rep/sample-requests/" + editId + "/items/" + itemId + "/status", {
          method: "PUT",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ status: newStatus })
        });
        const nextItems = items.map((it) => it.id === itemId ? { ...it, status: data.item.status } : it);
        setItems(nextItems);
        if (newStatus === "ready" && !sr.all_ready_notified_at) {
          const active = nextItems.filter((i) => i.status !== "cancelled");
          if (active.length > 0 && active.every((i) => i.status === "ready")) setReadyPrompt(true);
        }
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
    };
    const saveItemNote = async (itemId) => {
      try {
        const data = await repFetch("/api/rep/sample-requests/" + editId + "/items/" + itemId + "/status", {
          method: "PUT",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ notes: noteDraft })
        });
        setItems((prev) => prev.map((it) => it.id === itemId ? { ...it, notes: data.item.notes } : it));
        setEditingNoteId(null);
      } catch (err) {
        alert("Error: " + (err.message || "Failed"));
      }
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading sample request...");
    if (!sr) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Sample request not found");
    const addressParts = [sr.shipping_address_line1, sr.shipping_address_line2].filter(Boolean);
    const cityLine = [sr.shipping_city, sr.shipping_state].filter(Boolean).join(", ");
    if (sr.shipping_zip && cityLine) addressParts.push(cityLine + " " + sr.shipping_zip);
    else if (cityLine) addressParts.push(cityLine);
    else if (sr.shipping_zip) addressParts.push(sr.shipping_zip);
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const activeItems = items.filter((i) => i.status !== "cancelled");
    const readyCount = activeItems.filter((i) => i.status === "ready").length;
    const cancelledCount = items.length - activeItems.length;
    const isPickup = sr.delivery_method === "pickup";
    const isShipping = !isPickup;
    const isOpen = sr.status === "requested";
    const shipRefunded = parseFloat(sr.shipping_refunded_amount || 0);
    const shipRefundable = parseFloat((parseFloat(sr.shipping_amount_paid || 0) - shipRefunded).toFixed(2));
    const shipOnlinePaid = !!sr.shipping_payment_intent_id;
    const steps = [];
    if (isShipping) {
      steps.push({
        label: "Shipping payment collected",
        done: !!sr.shipping_payment_collected,
        detail: shipRefunded > 0 ? "Refunded $" + shipRefunded.toFixed(2) : sr.shipping_payment_collected ? ["Collected", sr.shipping_payment_method, sr.shipping_payment_collected_at ? new Date(sr.shipping_payment_collected_at).toLocaleDateString() : null].filter(Boolean).join(" \xB7 ") : "$12 flat rate",
        action: !sr.shipping_payment_collected ? shipRefunded > 0 ? null : "collect_payment" : shipOnlinePaid ? null : "uncollect_payment"
      });
    }
    const vendorsTotal = new Set(activeItems.map((i) => i.vendor_id || "unknown")).size;
    const vendorsNotified = new Set(activeItems.filter((i) => i.vendor_notified_at).map((i) => i.vendor_id || "unknown")).size;
    steps.push({
      label: "Vendor emails sent",
      done: activeItems.length > 0 && vendorsNotified === vendorsTotal,
      detail: vendorsTotal > 0 ? vendorsNotified + " of " + vendorsTotal + " vendor" + (vendorsTotal !== 1 ? "s" : "") + " notified" : "No items"
    });
    const allSamplesReady = activeItems.length > 0 && readyCount === activeItems.length;
    steps.push({
      label: isPickup ? "Ready for pickup" : "All samples ready",
      done: allSamplesReady,
      detail: sr.all_ready_notified_at ? "Customer emailed" : activeItems.length > 0 ? readyCount + " of " + activeItems.length + " ready" : "No items"
    });
    const isFinalStep = sr.status === "shipped" || sr.status === "delivered";
    if (isShipping) {
      steps.push({
        label: "Order shipped",
        done: isFinalStep,
        detail: isFinalStep ? sr.shipped_at ? new Date(sr.shipped_at).toLocaleDateString() : sr.status === "delivered" ? "Delivered" : "Shipped" : null
      });
    }
    const vendorMap = {};
    items.forEach((item) => {
      const vid = item.vendor_id || "unknown";
      if (!vendorMap[vid]) {
        vendorMap[vid] = { vendor_id: item.vendor_id, vendor_name: item.vendor_name || "Unknown Vendor", vendor_public_code: item.vendor_public_code || null, vendor_email: item.vendor_email || null, items: [] };
      }
      vendorMap[vid].items.push(item);
    });
    const vendorGroups = Object.values(vendorMap);
    const pill = sr.status === "delivered" ? { bg: "rgba(58,122,78,0.12)", c: "var(--rod-green)", l: isPickup ? "Picked up" : "Delivered" } : sr.status === "shipped" ? { bg: "rgba(58,122,78,0.12)", c: "var(--rod-green)", l: isPickup ? "Ready for pickup" : "In transit" } : sr.status === "cancelled" ? { bg: "rgba(197,71,49,0.12)", c: "var(--rod-red)", l: "Cancelled" } : isPickup && allSamplesReady ? { bg: "rgba(58,122,78,0.12)", c: "var(--rod-green)", l: "Ready for pickup" } : { bg: "rgba(168,121,53,0.12)", c: "var(--rod-accent)", l: "Requested" };
    const sdMono = { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--rod-muted)" };
    const sdBadge = (bg, c) => ({ padding: "3px 8px", display: "inline-block", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", whiteSpace: "nowrap", background: bg, color: c });
    const sdSmallBtn = { padding: "6px 11px", background: "transparent", color: "var(--rod-ink)", border: "0.5px solid rgba(28,25,23,0.25)", cursor: "pointer", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.04em" };
    const sdField = { width: "100%", boxSizing: "border-box", padding: "10px 12px", border: "0.5px solid rgba(28,25,23,0.3)", background: "rgba(255,255,255,0.35)", color: "var(--rod-ink)", font: "400 14px/1.3 Inter, sans-serif", outline: "none" };
    const fmtTs = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }) + " \xB7 " + new Date(d).toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }).toLowerCase().replace(" ", "") : null;
    const fmtD = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "";
    const initials = (sr.customer_name || "?").split(" ").map((w) => w[0]).filter(Boolean).slice(0, 2).join("").toUpperCase();
    const cancelled = sr.status === "cancelled";
    const readyMeta = activeItems.length > 0 ? readyCount + "/" + activeItems.length + " ready" : "\u2014";
    const lifecycle = [
      { label: "Requested", done: true, meta: fmtD(sr.created_at) },
      { label: "Sent to vendor", done: activeItems.length > 0 && vendorsNotified === vendorsTotal, meta: vendorsTotal > 0 ? vendorsNotified + "/" + vendorsTotal + " sent" : "\u2014" }
    ];
    if (isPickup) {
      lifecycle.push({ label: "Ready for pickup", done: allSamplesReady, meta: readyMeta });
      lifecycle.push({ label: "Picked up", done: sr.status === "delivered", meta: sr.delivered_at ? fmtD(sr.delivered_at) : "" });
    } else {
      lifecycle.push({ label: "Prepared", done: allSamplesReady, meta: readyMeta });
      lifecycle.push({ label: "Shipped", done: isFinalStep, meta: sr.shipped_at ? fmtD(sr.shipped_at) : "" });
      lifecycle.push({ label: "Delivered", done: sr.status === "delivered", meta: sr.delivered_at ? fmtD(sr.delivered_at) : "" });
    }
    const lifeFirstUndone = lifecycle.findIndex((s) => !s.done);
    const lifeStall = lifeFirstUndone === -1 ? lifecycle.length - 1 : lifeFirstUndone;
    const lifeState = (i) => {
      if (lifecycle[i].done) return "done";
      if (cancelled) return i === lifeStall ? "dead" : "next";
      return i === lifeStall ? "now" : "next";
    };
    const firstPendingStep = steps.findIndex((s) => !s.done);
    return /* @__PURE__ */ React.createElement("div", { className: "sd-detail" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("samples") }, "Samples"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, sr.request_number)), /* @__PURE__ */ React.createElement("div", { className: "qw-meta-right", style: { alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", null, "Requested ", fmtD(sr.created_at)), /* @__PURE__ */ React.createElement(RepPrevNext, { section: "samples", id: editId, view: "sample-detail", navigate, linkStyle: { color: "var(--brass-muted)", textDecoration: "none" } }))), /* @__PURE__ */ React.createElement("div", { className: "ws-head" }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, "Sample request ", /* @__PURE__ */ React.createElement("em", null, sr.request_number)), /* @__PURE__ */ React.createElement("span", { className: "rov-chip", style: { background: pill.bg, color: pill.c } }, "\u25CF", " ", pill.l)), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, initials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, sr.customer_name), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, [sr.customer_email, sr.customer_phone].filter(Boolean).join(" \xB7 ") || (isPickup ? "In-store pickup" : "Shipping"))))), /* @__PURE__ */ React.createElement("div", { className: "ws-head-actions" }, /* @__PURE__ */ React.createElement(
      AddTaskButton,
      {
        navigate,
        className: "ol-header-btn",
        label: "+ Task",
        prefill: {
          linked_sample_request_id: sr.id,
          context_label: sr.request_number + (sr.customer_name ? " \xB7 " + sr.customer_name : ""),
          customer_name: sr.customer_name,
          customer_email: sr.customer_email,
          customer_phone: sr.customer_phone,
          linked_customer_type: sr.customer_id ? "retail" : null,
          linked_customer_ref: sr.customer_id || sr.customer_email || null
        },
        onSaved: loadSampleTasks
      }
    ), allSamplesReady && !sr.all_ready_notified_at && isOpen && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", disabled: notifying, onClick: () => setReadyPrompt(true) }, "Email customer"), isPickup && allSamplesReady && isOpen && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", disabled: acting, onClick: markDelivered }, acting ? "Updating\u2026" : "Mark picked up"), isShipping && isOpen && !showTrackingInput && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", disabled: acting, onClick: () => setShowTrackingInput(true) }, "Mark shipped"), sr.status === "shipped" && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", disabled: acting, onClick: markDelivered }, acting ? "Updating\u2026" : isPickup ? "Mark picked up" : "Mark delivered"), isOpen && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", style: { color: "#c54731" }, disabled: acting, onClick: cancelRequest }, "Cancel request"))), /* @__PURE__ */ React.createElement("div", { className: "qw-stepper cols-" + lifecycle.length }, lifecycle.map((s, i) => {
      const state = lifeState(i);
      return /* @__PURE__ */ React.createElement("div", { key: s.label, className: "qw-step " + state }, /* @__PURE__ */ React.createElement("div", { className: "qw-step-label" }, /* @__PURE__ */ React.createElement("span", { className: "qw-step-mark" }, state === "done" ? "\u2713" : state === "dead" ? "\u2715" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", null, s.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-step-meta" }, s.meta));
    })), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Samples"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, activeItems.length), cancelledCount > 0 && /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-s" }, cancelledCount, " cancelled")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Ready"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: activeItems.length > 0 && readyCount === activeItems.length ? { color: "var(--rod-green)" } : void 0 }, readyCount, " / ", activeItems.length)), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Vendors notified"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, vendorsNotified, " / ", vendorsTotal)), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Shipping"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: isPickup ? { color: "var(--rod-green)" } : void 0 }, isPickup ? "Free" : "$12"), isShipping && /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-s" }, sr.shipping_payment_collected ? "Collected" : "Not collected")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Requested"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, fmtD(sr.created_at)))), showTrackingInput && /* @__PURE__ */ React.createElement("div", { className: "rof-overlay", onMouseDown: (e) => {
      if (e.target === e.currentTarget && !acting) setShowTrackingInput(false);
    } }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-pop", style: { width: 460 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 24px 16px", borderBottom: "0.5px solid rgba(28,25,23,0.1)" } }, /* @__PURE__ */ React.createElement("div", { style: { ...sdMono, letterSpacing: "0.2em", marginBottom: 8 } }, "Notify customer"), /* @__PURE__ */ React.createElement("h2", { style: { margin: 0, font: '300 24px/1.15 "Cormorant Garamond", serif', letterSpacing: "-0.012em", color: "var(--rod-ink)" } }, isPickup ? "Ready for pickup" : "Confirm shipment")), /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 24px", display: "grid", gap: 14 } }, readyCount < activeItems.length && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 12px", background: "rgba(168,121,53,0.12)", border: "0.5px solid rgba(168,121,53,0.4)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--rod-accent)", marginBottom: 4 } }, "Not all samples ready"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.45 Inter, sans-serif", color: "var(--rod-ink)" } }, "Only ", readyCount, " of ", activeItems.length, " samples are marked ready. You can still send, but the customer will be told everything is ", isPickup ? "ready for pickup" : "on the way", ".")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.5 Inter, sans-serif", color: "var(--rod-ink)" } }, sr.customer_email ? /* @__PURE__ */ React.createElement(React.Fragment, null, "This emails ", /* @__PURE__ */ React.createElement("strong", null, sr.customer_name), " at ", sr.customer_email, " that their samples are ", isPickup ? "ready for pickup" : "on the way", ".") : /* @__PURE__ */ React.createElement(React.Fragment, null, "No email is on file for ", sr.customer_name, ", so no notification is sent \u2014 this only updates the status.")), !isPickup && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...sdMono, marginBottom: 6 } }, "Tracking number \xB7 optional"), /* @__PURE__ */ React.createElement(
      "input",
      {
        style: sdField,
        placeholder: "Tracking number",
        value: trackingInput,
        onChange: (e) => setTrackingInput(e.target.value)
      }
    ))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 24px 20px", display: "flex", justifyContent: "flex-end", gap: 10, borderTop: "0.5px solid rgba(28,25,23,0.1)" } }, /* @__PURE__ */ React.createElement("button", { className: "rod-btn", disabled: acting, onClick: () => setShowTrackingInput(false) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "rod-btn rod-btn-primary", disabled: acting, onClick: markShipped }, acting ? "Sending\u2026" : sr.customer_email ? isPickup ? "Notify & mark ready \u2192" : "Notify & mark shipped \u2192" : isPickup ? "Mark ready \u2192" : "Mark shipped \u2192")))), /* @__PURE__ */ React.createElement("div", { className: "qw-grid" }, /* @__PURE__ */ React.createElement("div", { className: "qw-main" }, isOpen && /* @__PURE__ */ React.createElement("div", { className: "qw-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-card-head" }, /* @__PURE__ */ React.createElement("h3", null, "Add samples"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 14, alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { className: "qw-card-hint" }, activeItems.length, " of 5 used"))), /* @__PURE__ */ React.createElement("div", { className: "qw-card-body" }, activeItems.length < 5 && /* @__PURE__ */ React.createElement("button", { onClick: () => setCatalogOpen(true), style: { width: "100%", marginBottom: 12, padding: "12px 14px", cursor: "pointer", textAlign: "left", border: "0.5px solid var(--rod-ink)", background: "var(--rod-ink)", color: "#ece5d8", display: "grid", gridTemplateColumns: "1fr auto", gap: 12, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: '400 15px/1.15 "Cormorant Garamond", serif', letterSpacing: "-0.01em" } }, "\u25A6", " Browse the full catalog"), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "rgba(236,229,216,0.67)", marginTop: 3 } }, "Filter by vendor ", "\xB7", " accessories too")), /* @__PURE__ */ React.createElement("span", { style: { padding: "6px 12px", border: "0.5px solid rgba(236,229,216,0.33)", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase" } }, "Open catalog ", "\u2192")), activeItems.length < 5 ? /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 12, top: 10, font: '400 15px/1 "Cormorant Garamond", serif', color: "var(--rod-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        style: { ...sdField, paddingLeft: 34 },
        placeholder: "Quick add \u2014 search for a sample\u2026",
        value: addSkuSearch,
        onBlur: () => setTimeout(() => {
          setAddSkuResults([]);
          setAddSkuSearching(false);
        }, 200),
        onChange: (e) => searchSkuToAdd(e.target.value)
      }
    ), addSkuSearching && addSkuResults.length === 0 && addSkuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, /* @__PURE__ */ React.createElement("div", { className: "search-loading-indicator" }, "Searching...")), !addSkuSearching && addSkuResults.length === 0 && addSkuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, /* @__PURE__ */ React.createElement("div", { className: "search-no-results" }, "No results found")), addSkuResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, addSkuResults.map((r) => /* @__PURE__ */ React.createElement("div", { key: r.sku_id || r.product_id, className: "search-result-item", onClick: () => !addingItem && addItemToRequest(r) }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { className: "sku-thumb", src: optimizeImg(r.primary_image, 96), alt: "", loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { className: "sku-thumb-placeholder" }, /* @__PURE__ */ React.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5" }, /* @__PURE__ */ React.createElement("rect", { x: "3", y: "3", width: "18", height: "18", rx: "2" }), /* @__PURE__ */ React.createElement("circle", { cx: "8.5", cy: "8.5", r: "1.5" }), /* @__PURE__ */ React.createElement("path", { d: "M21 15l-5-5L5 21" }))), /* @__PURE__ */ React.createElement("div", { className: "sku-info" }, /* @__PURE__ */ React.createElement("div", { className: "sku-name" }, formatLineItem(r).name), /* @__PURE__ */ React.createElement("div", { className: "product-meta" }, r.vendor_name, r.vendor_public_code ? " \xB7 " + r.vendor_public_code : "", r.collection ? " \xB7 " + r.collection : "")), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", style: { fontSize: "0.75rem", padding: "0.2rem 0.5rem", flexShrink: 0 }, disabled: addingItem }, addingItem ? "\u2026" : "Add"))))) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "var(--rod-muted)" } }, "Maximum of 5 samples reached."))), vendorGroups.map((group) => {
      const vid = group.vendor_id || "unknown";
      const shipTo = vendorShipTo[vid] || "store";
      const sendResult = vendorSendResults[vid];
      const isSending = sendingVendorId === vid;
      const notifiedItem = group.items.find((i) => i.vendor_notified_at);
      return /* @__PURE__ */ React.createElement("div", { key: vid, className: "qw-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-card-head" }, /* @__PURE__ */ React.createElement("h3", null, group.vendor_name, group.vendor_public_code ? " \xB7 " + group.vendor_public_code : "", " \xB7 ", group.items.length, " sample", group.items.length !== 1 ? "s" : ""), /* @__PURE__ */ React.createElement("div", { className: "rod-card-actions", style: { alignItems: "center" } }, sendResult && sendResult.sent && /* @__PURE__ */ React.createElement("span", { style: sdBadge("rgba(58,122,78,0.12)", "var(--rod-green)") }, "\u2713 Sent"), sendResult && !sendResult.sent && /* @__PURE__ */ React.createElement("span", { style: sdBadge("rgba(197,71,49,0.12)", "var(--rod-red)") }, "Failed: ", sendResult.error), !sendResult && notifiedItem && /* @__PURE__ */ React.createElement("span", { style: sdBadge("rgba(58,122,78,0.12)", "var(--rod-green)") }, "\u2713 Sent ", new Date(notifiedItem.vendor_notified_at).toLocaleDateString()), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "sd-btn sd-btn-primary sd-btn-sm",
          disabled: isSending || !group.vendor_id,
          onClick: () => {
            setSendVendorTarget(group);
            setSendTo(group.vendor_email || "");
            setSendCc("");
          }
        },
        isSending ? "Sending\u2026" : /* @__PURE__ */ React.createElement(React.Fragment, null, "Send to vendor ", /* @__PURE__ */ React.createElement("span", { style: { opacity: 0.6 } }, "\u2192"))
      ))), /* @__PURE__ */ React.createElement("div", { style: { padding: "11px 20px", background: "var(--rod-warm)", borderBottom: "0.5px solid var(--rod-border)", display: "flex", gap: 12, alignItems: "center", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("span", { style: sdMono }, "Ship to"), isPickup ? /* @__PURE__ */ React.createElement("span", { style: { font: "400 12.5px/1.4 Inter, sans-serif", color: "var(--rod-ink)" } }, "Roma Flooring Designs \xB7 1440 S State College Blvd #6M, Anaheim, CA 92806", /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)" } }, " \xB7 pickup orders always ship to store")) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 4 } }, [["store", "Store"], ["customer", "Customer"]].map(([id, label]) => {
        const on = shipTo === id;
        return /* @__PURE__ */ React.createElement("button", { key: id, onClick: () => setVendorShipTo((prev) => ({ ...prev, [vid]: id })), style: {
          padding: "5px 11px",
          cursor: "pointer",
          font: "500 9px/1 ui-monospace, monospace",
          letterSpacing: "0.14em",
          textTransform: "uppercase",
          border: "0.5px solid " + (on ? "var(--rod-ink)" : "rgba(28,25,23,0.25)"),
          background: on ? "var(--rod-ink)" : "transparent",
          color: on ? "#ece5d8" : "var(--rod-muted)"
        } }, label);
      })), /* @__PURE__ */ React.createElement("span", { style: { font: "400 12.5px/1.4 Inter, sans-serif", color: "var(--rod-ink)" } }, shipTo === "store" ? "Roma Flooring Designs \xB7 1440 S State College Blvd #6M, Anaheim, CA 92806" : sr.customer_name + (addressParts.length ? " \xB7 " + addressParts.join(", ") : ""), shipTo === "customer" && !addressParts.length && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-red)" } }, " \xB7 no customer address on file"))), !group.vendor_email && /* @__PURE__ */ React.createElement("span", { style: { ...sdMono, color: "var(--rod-red)", marginLeft: "auto" } }, "No vendor email configured")), group.items.map((item, idx) => {
        const isCancelled = item.status === "cancelled";
        return /* @__PURE__ */ React.createElement("div", { key: item.id, style: { padding: "12px 20px", borderBottom: idx < group.items.length - 1 ? "0.5px solid rgba(28,25,23,0.06)" : "none", opacity: isCancelled ? 0.55 : 1 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "52px 1fr auto", gap: 12, alignItems: "center" } }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { className: "rod-line-item-thumb", src: optimizeImg(item.primary_image, 120), alt: item.product_name, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 48, height: 44, background: "var(--rod-warm)", display: "flex", alignItems: "center", justifyContent: "center", font: '400 16px/1 "Cormorant Garamond", serif', color: "var(--rod-muted)" } }, "\u25EB"), /* @__PURE__ */ React.createElement("div", null, formatLineItem(item).brand && /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta" }, formatLineItem(item).brand), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-name", style: isCancelled ? { textDecoration: "line-through", color: "var(--rod-muted)" } : void 0 }, formatLineItem(item).name), formatLineItem(item).sku && /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta" }, formatLineItem(item).sku)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, isCancelled ? /* @__PURE__ */ React.createElement("span", { style: sdBadge("rgba(197,71,49,0.12)", "var(--rod-red)") }, "Cancelled") : item.status === "ready" ? /* @__PURE__ */ React.createElement("span", { style: sdBadge("rgba(58,122,78,0.12)", "var(--rod-green)") }, "\u2713 Ready") : /* @__PURE__ */ React.createElement("span", { style: sdBadge("rgba(28,25,23,0.07)", "var(--rod-muted)") }, "Pending"), isOpen && !isCancelled && /* @__PURE__ */ React.createElement("button", { style: sdSmallBtn, onClick: () => toggleItemReady(item.id, item.status) }, item.status === "ready" ? "Undo" : "Mark ready"), isOpen && !isCancelled && /* @__PURE__ */ React.createElement("button", { className: "sd-x", title: "Remove sample", "aria-label": "Remove sample", onClick: () => setRemoveTarget(item) }, "\xD7"))), !isCancelled && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 8, marginLeft: 64 } }, editingNoteId === item.id ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "flex-start" } }, /* @__PURE__ */ React.createElement(
          "textarea",
          {
            value: noteDraft,
            onChange: (e) => setNoteDraft(e.target.value),
            placeholder: "Add a note for this sample\u2026",
            rows: 2,
            style: { ...sdField, flex: 1, resize: "vertical", fontFamily: "inherit", fontSize: 13 }
          }
        ), /* @__PURE__ */ React.createElement("button", { style: { ...sdSmallBtn, background: "var(--rod-ink)", color: "#fff", borderColor: "var(--rod-ink)" }, onClick: () => saveItemNote(item.id) }, "Save"), /* @__PURE__ */ React.createElement("button", { style: sdSmallBtn, onClick: () => setEditingNoteId(null) }, "Cancel")) : /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", gap: 10 } }, item.notes ? /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "400 12.5px/1.5 Inter, sans-serif", color: "rgba(28,25,23,0.7)", whiteSpace: "pre-wrap", flex: 1 } }, item.notes) : /* @__PURE__ */ React.createElement("span", { style: { ...sdMono, flex: 1, textTransform: "none", letterSpacing: "0.04em" } }, "No notes"), isOpen && /* @__PURE__ */ React.createElement("a", { className: "rod-card-action", onClick: () => {
          setEditingNoteId(item.id);
          setNoteDraft(item.notes || "");
        } }, item.notes ? "Edit note" : "+ Add note"))));
      }));
    }), vendorGroups.length === 0 && /* @__PURE__ */ React.createElement("div", { className: "qw-card" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "36px 20px", textAlign: "center", color: "var(--rod-muted)", font: "400 13px/1.5 Inter, sans-serif" } }, "No samples on this request yet.")), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14, font: "500 10px/1.4 ui-monospace, monospace", letterSpacing: "0.08em", textTransform: "uppercase", color: "var(--rod-muted)" } }, "Requested ", fmtTs(sr.created_at))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail" }, /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Sales rep"), sr.rep_id ? /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, sr.rep_id === repInfo.id ? (((repInfo.first_name || "?")[0] || "") + ((repInfo.last_name || "")[0] || "")).toUpperCase() : "\u2022"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name" }, sr.rep_id === repInfo.id ? [repInfo.first_name, repInfo.last_name].filter(Boolean).join(" ") || "Rep" : "Assigned rep", sr.rep_id === repInfo.id && /* @__PURE__ */ React.createElement("span", { className: "ws-you-tag" }, "You")), sr.rep_id === repInfo.id && repInfo.email && /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, repInfo.email))) : /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar", style: { background: "var(--brass-warm, #d8cdb6)", color: "var(--brass-muted, #8a7e68)" } }, "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name", style: { color: "var(--rod-muted)" } }, "Unassigned"), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "No rep owns this request yet")))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Customer"), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "40px 1fr", gap: 12, alignItems: "center", paddingBottom: 12, borderBottom: "0.5px solid rgba(28,25,23,0.08)" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 40, height: 40, background: "var(--rod-warm)", display: "flex", alignItems: "center", justifyContent: "center", font: '400 16px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, initials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 15px/1.2 Inter, sans-serif", color: "var(--rod-ink)" } }, sr.customer_name), /* @__PURE__ */ React.createElement("div", { style: { ...sdMono, marginTop: 3, textTransform: "none", letterSpacing: "0.04em" } }, [sr.customer_email, sr.customer_phone].filter(Boolean).join(" \xB7 ") || "\u2014"))), sr.sidemark && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12 } }, /* @__PURE__ */ React.createElement("div", { style: { ...sdMono, marginBottom: 6 } }, "Sidemark"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "var(--rod-ink)" } }, sr.sidemark)), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12 } }, /* @__PURE__ */ React.createElement("div", { style: { ...sdMono, marginBottom: 6 } }, "Delivery"), isPickup ? /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "var(--rod-green)" } }, "In-store pickup \xB7 Anaheim showroom \xB7 free") : addressParts.length > 0 ? addressParts.map((l, i) => /* @__PURE__ */ React.createElement("div", { key: i, style: { font: "400 13px/1.5 Inter, sans-serif", color: "var(--rod-ink)" } }, l)) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "var(--rod-muted)" } }, "Shipping \xB7 $12 flat rate"), sr.tracking_number && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 10 } }, /* @__PURE__ */ React.createElement("div", { style: { ...sdMono, marginBottom: 4 } }, "Tracking"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 13px/1.3 ui-monospace, monospace", color: "var(--rod-ink)" } }, sr.tracking_number)))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Summary"), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement(RovRow, { l: "Samples", v: activeItems.length + (cancelledCount > 0 ? " \xB7 " + cancelledCount + " cancelled" : "") }), /* @__PURE__ */ React.createElement(
      RovRow,
      {
        l: "Ready",
        v: readyCount + " / " + activeItems.length,
        c: activeItems.length > 0 && readyCount === activeItems.length ? "var(--rod-green)" : void 0
      }
    ), /* @__PURE__ */ React.createElement(RovRow, { l: "Vendors", v: vendorGroups.length })), /* @__PURE__ */ React.createElement("div", { className: "rov-total-row", style: { marginTop: 12 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-ink)" } }, "Shipping"), /* @__PURE__ */ React.createElement("span", { className: "rov-total-value", style: isPickup ? { color: "var(--rod-green)" } : void 0 }, isPickup ? "Free" : "$12"))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Workflow"), steps.map((step, i) => {
      const state = step.done ? "done" : i === firstPendingStep ? "now" : "next";
      return /* @__PURE__ */ React.createElement("div", { key: i, className: "qw-check-row " + state }, /* @__PURE__ */ React.createElement("span", { className: "qw-check-mark" }, step.done ? "\u2713" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", { className: "qw-check-label" }, step.label), step.action === "collect_payment" ? /* @__PURE__ */ React.createElement("span", { style: { display: "flex", gap: 6 } }, /* @__PURE__ */ React.createElement("button", { style: { ...sdSmallBtn, background: "var(--rod-ink)", color: "#fff", borderColor: "var(--rod-ink)" }, disabled: paymentActing, onClick: () => setShowPayModal(true) }, "Card $12"), /* @__PURE__ */ React.createElement("button", { style: sdSmallBtn, disabled: paymentActing, onClick: () => markPaymentCollected(true, "cash") }, paymentActing ? "\u2026" : "Cash")) : step.action === "uncollect_payment" ? /* @__PURE__ */ React.createElement("button", { className: "qw-link muted", disabled: paymentActing, onClick: () => markPaymentCollected(false) }, "Undo") : step.detail && /* @__PURE__ */ React.createElement("span", { className: "qw-check-meta" }, step.detail));
    })), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Tasks"), sampleTasks.length === 0 ? /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.5 Inter, sans-serif", color: "var(--rod-muted)" } }, "No tasks yet.") : sampleTasks.map((t) => {
      const done = t.status === "completed";
      const due = t.due_date ? /* @__PURE__ */ new Date(String(t.due_date).split("T")[0] + "T12:00:00") : null;
      const overdue = !done && due && due < new Date((/* @__PURE__ */ new Date()).toDateString());
      return /* @__PURE__ */ React.createElement("div", { key: t.id, style: { display: "flex", alignItems: "flex-start", gap: 8, padding: "7px 0", borderBottom: "0.5px solid var(--rod-border)" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => toggleTaskDone(t),
          title: done ? "Reopen" : "Complete",
          style: { width: 16, height: 16, marginTop: 1, borderRadius: "50%", flexShrink: 0, cursor: "pointer", padding: 0, border: "1.5px solid " + (done ? "var(--rod-green)" : "var(--rod-border)"), background: done ? "var(--rod-green)" : "transparent", color: "#fff", font: "700 9px/1 Inter, sans-serif", display: "flex", alignItems: "center", justifyContent: "center" }
        },
        done ? "\u2713" : ""
      ), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.35 Inter, sans-serif", color: done ? "var(--rod-muted)" : "var(--rod-ink)", textDecoration: done ? "line-through" : "none" } }, t.title), t.due_date && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.04em", marginTop: 2, color: overdue ? "var(--rod-red)" : "var(--rod-muted)" } }, overdue ? "Overdue \xB7 " : "Due ", due.toLocaleDateString())), /* @__PURE__ */ React.createElement("span", { style: { width: 6, height: 6, borderRadius: "50%", marginTop: 6, flexShrink: 0, background: t.priority === "high" ? "var(--rod-red)" : t.priority === "low" ? "var(--rod-border)" : "var(--rod-accent)" } }));
    })), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Timeline"), [
      ["Created", sr.created_at, "var(--rod-ink)"],
      ["Shipped", sr.shipped_at, "var(--rod-green)"],
      ["Delivered", sr.delivered_at, "var(--rod-green)"],
      ["Cancelled", sr.cancelled_at, "var(--rod-red)"]
    ].filter(([, d]) => d).map(([l, d, c], i, arr) => /* @__PURE__ */ React.createElement("div", { key: l, style: { padding: "9px 0", display: "flex", justifyContent: "space-between", alignItems: "baseline", borderBottom: i < arr.length - 1 ? "0.5px solid rgba(28,25,23,0.06)" : "none" } }, /* @__PURE__ */ React.createElement("span", { style: { ...sdMono, color: c } }, "\u25CF ", l), /* @__PURE__ */ React.createElement("span", { style: { font: "400 12.5px/1.3 Inter, sans-serif", color: "rgba(28,25,23,0.7)" } }, fmtTs(d))))), /* @__PURE__ */ React.createElement(InternalNotesCard, { basePath: "/api/rep/sample-requests/" + editId, bg: "#f5f0e8" }), sr.notes && /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Request notes"), /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "400 13px/1.6 Inter, sans-serif", color: "rgba(28,25,23,0.8)", whiteSpace: "pre-wrap" } }, sr.notes)))), catalogOpen && isOpen && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data: { customer: null, newCustomerName: sr.customer_name },
        showCost: false,
        contextLabel: "Add samples \xB7 " + sr.request_number,
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog
      }
    ), showPayModal && /* @__PURE__ */ React.createElement(
      RofPayCardModal,
      {
        amount: 12,
        customerName: sr.customer_name,
        customerEmail: sr.customer_email,
        orderNumber: sr.request_number,
        tenderIdx: 1,
        tenderCount: 1,
        onSuccess: (res) => {
          setShowPayModal(false);
          markPaymentCollected(true, "card", res);
        },
        onCancel: () => setShowPayModal(false),
        terminalRef,
        terminalStatus,
        connectReader
      }
    ), readyPrompt && /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => !notifying && setReadyPrompt(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation(), style: { width: 460, padding: 0, background: "#f5f0e8", overflow: "hidden" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 28px 18px", borderBottom: "0.5px solid rgba(28,25,23,0.12)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "var(--rod-accent)", marginBottom: 12 } }, "\u25CF", " All samples ready"), /* @__PURE__ */ React.createElement("h3", { style: { margin: 0, font: '300 26px/1.15 "Cormorant Garamond", serif', color: "var(--rod-ink)", letterSpacing: "-0.01em" } }, isPickup ? "Email the customer their samples are ready to collect?" : "Email the customer their samples are ready?")), /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 28px 22px" } }, sr.customer_email ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "400 14px/1.6 Inter, sans-serif", color: "rgba(28,25,23,0.72)" } }, isPickup ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Every sample on ", sr.request_number, " is ready. We", "\u2019", "ll email the customer to come collect at the Anaheim showroom.") : /* @__PURE__ */ React.createElement(React.Fragment, null, "Every sample on ", sr.request_number, " is marked ready. We", "\u2019", "ll let them know their samples are ready and being prepared to ship.")), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 16, padding: "12px 14px", background: "var(--rod-warm)", border: "0.5px solid var(--rod-border)", display: "flex", alignItems: "center", gap: 12 } }, /* @__PURE__ */ React.createElement("div", { style: { width: 34, height: 34, borderRadius: "50%", background: "var(--rod-ink)", color: "#ece5d8", display: "flex", alignItems: "center", justifyContent: "center", font: "500 12px/1 Inter, sans-serif", flexShrink: 0 } }, (sr.customer_name || "?").split(/\s+/).map((w) => w[0]).filter(Boolean).slice(0, 2).join("").toUpperCase()), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.2 Inter, sans-serif", color: "var(--rod-ink)" } }, sr.customer_name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.4 ui-monospace, monospace", letterSpacing: "0.04em", color: "var(--rod-muted)", marginTop: 3, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, sr.customer_email)))) : /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "400 14px/1.6 Inter, sans-serif", color: "rgba(28,25,23,0.72)" } }, isPickup ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Every sample on ", sr.request_number, " is ready, but ", sr.customer_name, " has no email on file \u2014 nothing will be sent.") : /* @__PURE__ */ React.createElement(React.Fragment, null, "Every sample on ", sr.request_number, " is marked ready, but ", sr.customer_name, " has no email on file \u2014 nothing will be sent."))), /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 28px", borderTop: "0.5px solid rgba(28,25,23,0.12)", display: "flex", gap: 8, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("button", { className: "sd-btn", disabled: notifying, onClick: () => setReadyPrompt(false) }, "Not now"), /* @__PURE__ */ React.createElement("button", { className: "sd-btn sd-btn-primary", disabled: notifying, onClick: notifyReady }, notifying ? "Sending\u2026" : sr.customer_email ? "Send email" : "Mark notified")))), removeTarget && /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => !removing && setRemoveTarget(null) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation(), style: { width: 380, padding: 0, background: "#f5f0e8", overflow: "hidden" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 26px 18px" } }, /* @__PURE__ */ React.createElement("h3", { style: { margin: 0, font: '300 22px/1.2 "Cormorant Garamond", serif', color: "var(--rod-ink)", letterSpacing: "-0.01em" } }, "Remove this sample?"), /* @__PURE__ */ React.createElement("p", { style: { margin: "10px 0 0", font: "400 13.5px/1.55 Inter, sans-serif", color: "rgba(28,25,23,0.7)" } }, /* @__PURE__ */ React.createElement("strong", { style: { color: "var(--rod-ink)", fontWeight: 500 } }, formatLineItem(removeTarget).name), " will be removed from this request.")), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 26px", borderTop: "0.5px solid rgba(28,25,23,0.12)", display: "flex", gap: 8, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("button", { className: "sd-btn", disabled: removing, onClick: () => setRemoveTarget(null) }, "Keep"), /* @__PURE__ */ React.createElement("button", { className: "sd-btn sd-btn-primary", disabled: removing, onClick: confirmRemoveItem, style: { background: "var(--rod-red)", borderColor: "var(--rod-red)" } }, removing ? "Removing\u2026" : "Remove")))), sendVendorTarget && (() => {
      const t = sendVendorTarget;
      const tShipTo = isPickup ? "store" : vendorShipTo[t.vendor_id] || "store";
      const sending = sendingVendorId === t.vendor_id;
      const emailOk = (e) => /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/.test(String(e || "").trim());
      const toValid = emailOk(sendTo);
      const ccInvalid = sendCc.split(",").map((s) => s.trim()).filter(Boolean).some((e) => !emailOk(e));
      const canSend = toValid && !ccInvalid && !sending;
      return /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => !sending && setSendVendorTarget(null) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation(), style: { width: 460, padding: 0, background: "#f5f0e8", overflow: "hidden" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 28px 18px", borderBottom: "0.5px solid rgba(28,25,23,0.12)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "var(--rod-accent)", marginBottom: 12 } }, "\u25CF", " Send to vendor"), /* @__PURE__ */ React.createElement("h3", { style: { margin: 0, font: '300 26px/1.15 "Cormorant Garamond", serif', color: "var(--rod-ink)", letterSpacing: "-0.01em" } }, "Email this sample request to ", t.vendor_name, "?")), /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 28px 22px" } }, /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "400 14px/1.6 Inter, sans-serif", color: "rgba(28,25,23,0.72)" } }, t.items.length, " sample", t.items.length !== 1 ? "s" : "", " on ", sr.request_number, " will be emailed with the request PDF attached, shipping to ", tShipTo === "store" ? "the Anaheim showroom" : "the customer", "."), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 15, display: "flex", alignItems: "center", gap: 10 } }, /* @__PURE__ */ React.createElement("div", { style: { width: 30, height: 30, borderRadius: "50%", background: "var(--rod-ink)", color: "#ece5d8", display: "flex", alignItems: "center", justifyContent: "center", font: "500 12px/1 Inter, sans-serif", flexShrink: 0 } }, "\u2709"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.2 Inter, sans-serif", color: "var(--rod-ink)" } }, t.vendor_name, t.vendor_public_code ? " \xB7 " + t.vendor_public_code : "")), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14 } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "Send to"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "email",
          value: sendTo,
          onChange: (e) => setSendTo(e.target.value),
          placeholder: "vendor@example.com",
          style: { width: "100%", boxSizing: "border-box", padding: "9px 11px", font: "400 13px/1.3 Inter, sans-serif", color: "var(--rod-ink)", background: "#fff", border: "0.5px solid " + (sendTo.trim() && !toValid ? "var(--rod-red)" : "var(--rod-border)") }
        }
      ), sendTo.trim() && !toValid && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.06em", color: "var(--rod-red)", marginTop: 4, textTransform: "uppercase" } }, "Enter a valid email"), !t.vendor_email && !sendTo.trim() && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.06em", color: "var(--rod-accent)", marginTop: 4, textTransform: "uppercase" } }, "No email on file \u2014 enter one to send")), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12 } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "CC ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", letterSpacing: 0 } }, "\xB7 comma-separated")), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: sendCc,
          onChange: (e) => setSendCc(e.target.value),
          placeholder: "you@romaflooringdesigns.com",
          style: { width: "100%", boxSizing: "border-box", padding: "9px 11px", font: "400 13px/1.3 Inter, sans-serif", color: "var(--rod-ink)", background: "#fff", border: "0.5px solid " + (ccInvalid ? "var(--rod-red)" : "var(--rod-border)") }
        }
      ), ccInvalid && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.06em", color: "var(--rod-red)", marginTop: 4, textTransform: "uppercase" } }, "One or more CC emails are invalid"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.08em", color: "var(--rod-muted)", marginTop: 6, textTransform: "uppercase" } }, "Your rep email is always CC", "\u2019", "d"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 28px", borderTop: "0.5px solid rgba(28,25,23,0.12)", display: "flex", gap: 8, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("button", { className: "sd-btn", disabled: sending, onClick: () => setSendVendorTarget(null) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "sd-btn sd-btn-primary", disabled: !canSend, onClick: confirmSendToVendor }, sending ? "Sending\u2026" : "Send to vendor"))));
    })());
  }
  function RepNotificationsListView({ navigate, onUnreadChange }) {
    const [notifications, setNotifications] = useState([]);
    const [loading, setLoading] = useState(true);
    const [filter, setFilter] = useState("all");
    const [page, setPage] = useState(1);
    const [total, setTotal] = useState(0);
    const [unreadCount, setUnreadCount] = useState(0);
    const [typeCounts, setTypeCounts] = useState([]);
    const limit = 50;
    const load = useCallback(() => {
      setLoading(true);
      const params = new URLSearchParams();
      if (filter === "unread") params.set("unread_only", "true");
      const activeCat = NOTIF_CATEGORIES.find((c) => c.key === filter);
      if (activeCat) params.set("types", activeCat.types.join(","));
      params.set("limit", limit);
      params.set("offset", (page - 1) * limit);
      repFetch("/api/rep/notifications?" + params.toString()).then((d) => {
        setNotifications(d.notifications || []);
        setTotal(d.total || 0);
        setUnreadCount(d.unread_count || 0);
        setTypeCounts(d.type_counts || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [filter, page]);
    useEffect(load, [load]);
    const countFor = (key) => {
      if (key === "all") return typeCounts.reduce((s, t) => s + t.total, 0);
      if (key === "unread") return unreadCount;
      const cat = NOTIF_CATEGORIES.find((c) => c.key === key);
      return typeCounts.filter((t) => cat.types.includes(t.type)).reduce((s, t) => s + t.total, 0);
    };
    const markAllRead = () => {
      repFetch("/api/rep/notifications/read-all", { method: "PUT" }).then(() => {
        load();
        if (onUnreadChange) onUnreadChange();
      }).catch(() => {
      });
    };
    const clearRead = () => {
      if (!window.confirm("Remove all read notifications? This cannot be undone.")) return;
      repFetch("/api/rep/notifications/read", { method: "DELETE" }).then(() => {
        if (page !== 1) setPage(1);
        else load();
      }).catch(() => {
      });
    };
    const handleClick = (n) => {
      if (!n.is_read) {
        repFetch("/api/rep/notifications/" + n.id + "/read", { method: "PUT" }).then(() => {
          if (onUnreadChange) onUnreadChange();
        }).catch(() => {
        });
        setNotifications((prev) => prev.map((x) => x.id === n.id ? { ...x, is_read: true } : x));
        setUnreadCount((c) => Math.max(0, c - 1));
      }
      const link = notifDeepLink(n);
      if (link) navigate(link.view, link.id || n.entity_id);
    };
    const groups = [];
    notifications.forEach((n) => {
      const label = notifDayGroup(n.created_at);
      let g = groups[groups.length - 1];
      if (!g || g.label !== label) {
        g = { label, items: [] };
        groups.push(g);
      }
      g.items.push(n);
    });
    const totalPages = Math.ceil(total / limit) || 1;
    const chips = [
      { key: "all", label: "All" },
      { key: "unread", label: "Unread" },
      ...NOTIF_CATEGORIES.map((c) => ({ key: c.key, label: c.label }))
    ];
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25CF", " Inbox", unreadCount > 0 ? " \xB7 " + unreadCount + " unread" : " \xB7 all caught up"), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Inbox")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.5 'Inter', sans-serif", color: "rgba(28,25,23,0.7)", marginTop: 6 } }, (/* @__PURE__ */ new Date()).toLocaleDateString("en-US", { weekday: "long", month: "long", day: "numeric" }), " ", "\xB7", " order, payment & vendor activity in one stream")), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: clearRead }, "Clear read"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "ol-header-btn primary",
        onClick: markAllRead,
        disabled: unreadCount === 0,
        style: unreadCount === 0 ? { opacity: 0.4, cursor: "default" } : void 0
      },
      "Mark all read"
    ))), /* @__PURE__ */ React.createElement("div", { className: "nin-filters" }, /* @__PURE__ */ React.createElement("div", { className: "nin-filter-chips" }, chips.map((c) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: c.key,
        className: "nin-chip" + (filter === c.key ? " active" : ""),
        onClick: () => {
          setFilter(c.key);
          setPage(1);
        }
      },
      c.label,
      /* @__PURE__ */ React.createElement("span", { className: "nin-chip-count" }, countFor(c.key))
    ))), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, total, " notification", total !== 1 ? "s" : "")), loading ? /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading notifications\u2026") : notifications.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "nin-list" }, /* @__PURE__ */ React.createElement("div", { className: "nin-empty-state" }, /* @__PURE__ */ React.createElement("div", { className: "nin-empty-glyph" }, "\u25C9"), /* @__PURE__ */ React.createElement("div", { className: "nin-empty-text" }, filter === "unread" ? "No unread notifications \u2014 you're all caught up." : filter !== "all" ? "Nothing in this category yet." : "Nothing here yet. Order, payment and sample activity will land in this stream."))) : /* @__PURE__ */ React.createElement("div", { className: "nin-list" }, groups.map((g) => /* @__PURE__ */ React.createElement("div", { key: g.label }, /* @__PURE__ */ React.createElement("div", { className: "nin-group-label" }, g.label), g.items.map((n) => {
      const cat = notifCategoryFor(n.type);
      const urgent = NOTIF_URGENT_TYPES.includes(n.type);
      const tone = urgent ? "#c54731" : cat.tone;
      const link = notifDeepLink(n);
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: n.id,
          className: "nin-row" + (n.is_read ? "" : " unread"),
          role: "button",
          tabIndex: 0,
          onClick: () => handleClick(n),
          onKeyDown: (e) => {
            if (e.key === "Enter" || e.key === " ") {
              e.preventDefault();
              handleClick(n);
            }
          }
        },
        /* @__PURE__ */ React.createElement("div", { className: "nin-glyph", style: { color: tone } }, cat.glyph),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "nin-row-topline" }, /* @__PURE__ */ React.createElement("span", { className: "nin-kind-chip", style: { background: tone + "1a", color: tone } }, urgent ? "Needs you" : cat.label)), /* @__PURE__ */ React.createElement("div", { className: "nin-row-title" }, n.title), n.message && /* @__PURE__ */ React.createElement("div", { className: "nin-row-message" }, n.message)),
        /* @__PURE__ */ React.createElement("div", { className: "nin-row-right" }, /* @__PURE__ */ React.createElement("div", { className: "nin-row-time" }, !n.is_read && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-accent)" } }, "\u25CF", " "), timeAgo(n.created_at)), link && /* @__PURE__ */ React.createElement("div", { className: "nin-row-entity", style: { color: tone } }, link.label, " ", "\u2192"))
      );
    })))), totalPages > 1 && /* @__PURE__ */ React.createElement("div", { className: "nin-pagination" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "ol-header-btn",
        disabled: page <= 1,
        style: page <= 1 ? { opacity: 0.4 } : void 0,
        onClick: () => setPage((p) => p - 1)
      },
      "\u2190",
      " Previous"
    ), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, "Page ", page, " of ", totalPages), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "ol-header-btn",
        disabled: page >= totalPages,
        style: page >= totalPages ? { opacity: 0.4 } : void 0,
        onClick: () => setPage((p) => p + 1)
      },
      "Next ",
      "\u2192"
    )));
  }
  function RepTeamCommissionsView({ navigate }) {
    const [reps, setReps] = useState(null);
    const [repId, setRepId] = useState("");
    useEffect(() => {
      repFetch("/api/rep/reps").then((d) => setReps(d.reps || [])).catch(() => setReps([]));
    }, []);
    const selected = (reps || []).find((r) => r.id === repId);
    const accent = "#a87935", ink = "#1c1917", muted = "#8a7e68";
    return /* @__PURE__ */ React.createElement("div", { style: { color: ink, fontFamily: "var(--roma-sans)" } }, /* @__PURE__ */ React.createElement("div", { style: { marginBottom: 20 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: accent, marginBottom: 12 } }, "\u25CF Manager view"), /* @__PURE__ */ React.createElement("h1", { style: { font: "300 44px/1 var(--roma-serif)", letterSpacing: "-0.018em", margin: 0, color: ink } }, "Team ", /* @__PURE__ */ React.createElement("em", { style: { color: accent } }, "commissions")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.5 var(--roma-sans)", color: ink + "b3", marginTop: 6 } }, "Select a sales rep to review their commission ledger."), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 18 } }, /* @__PURE__ */ React.createElement(
      "select",
      {
        value: repId,
        onChange: (e) => setRepId(e.target.value),
        style: { font: "400 15px/1 var(--roma-sans)", padding: "10px 14px", border: "0.5px solid " + ink + "44", background: "#faf7f0", color: ink, minWidth: 280, borderRadius: 2 }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, reps === null ? "Loading reps\u2026" : "Choose a rep\u2026"),
      (reps || []).map((r) => /* @__PURE__ */ React.createElement("option", { key: r.id, value: r.id }, r.name, r.is_manager ? " \xB7 manager" : ""))
    ))), repId && selected ? /* @__PURE__ */ React.createElement(
      RepCommissionsView,
      {
        key: repId,
        navigate,
        endpoint: "/api/rep/team/commissions/" + repId,
        viewRepName: selected.name
      }
    ) : /* @__PURE__ */ React.createElement("div", { style: { padding: "48px 22px", textAlign: "center", font: "400 14px/1.5 var(--roma-sans)", color: muted, background: "#faf7f0", border: "0.5px solid " + ink + "22" } }, "No rep selected."));
  }
  function RepCommissionsView({ navigate, endpoint, viewRepName }) {
    const [data, setData] = useState(null);
    const [loading, setLoading] = useState(true);
    const [period, setPeriod] = useState("m0");
    const src = endpoint || "/api/rep/commissions";
    useEffect(() => {
      setLoading(true);
      repFetch(src).then((d) => {
        setData(d);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [src]);
    const theme = { ink: "#1c1917", paper: "#faf7f0", accent: "#a87935", muted: "#8a7e68", warm: "#efe7d6" };
    const { ink, paper, accent, muted, warm } = theme;
    const TONE = { earned: "#5f7a55", pending: "#a87935", paid: "#8a7e68", forfeited: "#b0543a", flag: "#a87935" };
    if (loading || !data) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading commissions\u2026");
    const rows = data.commissions || [];
    const baseRate = parseFloat(data.commission_rate || 0.1);
    const laborRate = parseFloat(data.labor_rate != null ? data.labor_rate : 0.03);
    let repName = "";
    if (viewRepName != null) {
      repName = viewRepName;
    } else {
      try {
        const info = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
        repName = [info.first_name, info.last_name].filter(Boolean).join(" ");
      } catch (e) {
      }
    }
    const now = /* @__PURE__ */ new Date();
    const months = [2, 1, 0].map((back) => {
      const start = new Date(now.getFullYear(), now.getMonth() - back, 1);
      const end = new Date(now.getFullYear(), now.getMonth() - back + 1, 1);
      return {
        key: "m" + back,
        start,
        end,
        label: start.toLocaleString("en-US", { month: "short" }),
        long: start.toLocaleString("en-US", { month: "long", year: "numeric" })
      };
    });
    const ytd = { key: "ytd", label: "YTD", long: now.getFullYear() + " \xB7 year to date", start: new Date(now.getFullYear(), 0, 1), end: new Date(now.getFullYear() + 1, 0, 1) };
    const lastYear = { key: "ly", label: String(now.getFullYear() - 1), long: now.getFullYear() - 1 + " \xB7 full year", start: new Date(now.getFullYear() - 1, 0, 1), end: new Date(now.getFullYear(), 0, 1) };
    const periods = [...months, ytd, lastYear];
    const cur = periods.find((p) => p.key === period) || periods[2];
    const rowDate = (r) => new Date(r.created_at || r.order_date);
    const inRange = (r, p) => {
      const t = rowDate(r);
      return t >= p.start && t < p.end;
    };
    const periodRows = rows.filter((r) => inRange(r, cur));
    const sum = (rs, st) => rs.filter((r) => !st || r.status === st).reduce((a, r) => a + parseFloat(r.commission_amount || 0), 0);
    const earned = sum(periodRows, "earned");
    const pending = sum(periodRows, "pending");
    const paid = sum(periodRows, "paid");
    const forfeited = sum(periodRows, "forfeited");
    const net = earned + pending;
    const ytdRows = rows.filter((r) => rowDate(r) >= new Date(now.getFullYear(), 0, 1));
    const ytdCommission = sum(ytdRows, "earned") + sum(ytdRows, "pending") + sum(ytdRows, "paid");
    const lifetimePaid = sum(rows, "paid");
    const ytdForfeited = sum(ytdRows, "forfeited");
    const money0 = (n) => "$" + Math.round(n).toLocaleString("en-US");
    const money2 = (n) => "$" + Math.abs(n).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    const fmtDate = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "";
    const matBase = (r) => Math.max(0, parseFloat(r.order_total) - parseFloat(r.labor_subtotal || 0));
    const lowMargin = (r) => matBase(r) > 0 && parseFloat(r.margin) / matBase(r) < 0.35 && (r.status === "earned" || r.status === "pending");
    const stateMeta = (r) => {
      if (r.status === "earned") return { label: "Earned \xB7 locked", tone: TONE.earned, when: "Ready for payout" };
      if (r.status === "pending") return { label: "Pending payment", tone: TONE.pending, when: "Unlocks once order is finalized" };
      if (r.status === "paid") return { label: "Paid", tone: TONE.paid, when: "Paid " + fmtDate(r.paid_at) };
      if (r.status === "forfeited") return { label: "Forfeited", tone: TONE.forfeited, when: "Order cancelled" };
      return { label: r.status, tone: muted, when: "" };
    };
    const csvCell = (v) => {
      const s = String(v == null ? "" : v);
      return /[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s;
    };
    const exportCsv = () => {
      const head = ["Order", "Customer", "Order Total", "Vendor Cost", "Margin", "Rate", "Commission", "Status", "Date"];
      const lines = periodRows.map((r) => [
        r.order_number,
        r.customer_name,
        r.order_total,
        r.vendor_cost,
        r.margin,
        (parseFloat(r.commission_rate) * 100).toFixed(2) + "%",
        r.commission_amount,
        r.status,
        fmtDate(r.created_at || r.order_date)
      ].map(csvCell).join(","));
      const csv = [head.join(","), ...lines].join("\n");
      const blob = new Blob([csv], { type: "text/csv" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = "commissions-" + cur.label.toLowerCase() + ".csv";
      a.click();
      URL.revokeObjectURL(url);
    };
    const mono = "500 11px/1 ui-monospace, monospace";
    const kicker = { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase" };
    const gridCols = "6px 118px 1.35fr 104px 62px 108px 1fr 22px";
    const stat = (label, value, sub, tone) => /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 24px", borderRight: "0.5px solid " + ink + "11", minWidth: 150 } }, /* @__PURE__ */ React.createElement("div", { style: { ...kicker, fontSize: 10, color: muted } }, /* @__PURE__ */ React.createElement("span", { style: { color: tone } }, "\u25CF"), " ", label), /* @__PURE__ */ React.createElement("div", { style: { font: "300 30px/1 var(--roma-serif)", color: ink, marginTop: 12, letterSpacing: "-0.014em" } }, value), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.4 var(--roma-sans)", color: ink + "99", marginTop: 6 } }, sub));
    const footStat = (k, v, sub, tone) => /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...kicker, fontSize: 10, color: "#caa97f99" } }, k), /* @__PURE__ */ React.createElement("div", { style: { font: "300 26px/1 var(--roma-serif)", color: tone || "#ece5d8", marginTop: 8, letterSpacing: "-0.012em" } }, v), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.4 var(--roma-sans)", color: "#ece5d899", marginTop: 4 } }, sub));
    return /* @__PURE__ */ React.createElement("div", { style: { color: ink, fontFamily: "var(--roma-sans)" } }, /* @__PURE__ */ React.createElement("div", { style: { marginBottom: 18, display: "flex", justifyContent: "space-between", alignItems: "flex-end", flexWrap: "wrap", gap: 16 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...kicker, color: accent, marginBottom: 12 } }, "\u25CF ", cur.long, " \xB7 ", periodRows.length, " ", periodRows.length === 1 ? "order" : "orders"), /* @__PURE__ */ React.createElement("h1", { style: { font: "300 44px/1 var(--roma-serif)", letterSpacing: "-0.018em", margin: 0, color: ink } }, /* @__PURE__ */ React.createElement("em", { style: { color: accent } }, "Commission")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.5 var(--roma-sans)", color: ink + "b3", marginTop: 6 } }, repName ? repName + " \xB7 " : "", (baseRate * 100).toFixed(0), "% on materials margin \xB7 ", (laborRate * 100).toFixed(0), "% on labor \xB7 paid once finalized")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, flexWrap: "wrap" } }, periods.map((p) => {
      const on = p.key === cur.key;
      return /* @__PURE__ */ React.createElement("span", { key: p.key, onClick: () => setPeriod(p.key), style: {
        padding: "6px 14px",
        background: on ? ink : "transparent",
        color: on ? "#ece5d8" : ink,
        border: on ? "none" : "0.5px solid " + ink + "33",
        font: "500 11px/1 var(--roma-sans)",
        letterSpacing: "0.08em",
        textTransform: "uppercase",
        cursor: "pointer",
        userSelect: "none"
      } }, p.label);
    }))), /* @__PURE__ */ React.createElement("div", { style: { overflowX: "auto", marginBottom: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1.2fr 1fr 1fr 1fr", minWidth: 720, background: paper, border: "0.5px solid " + ink + "22" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "20px 26px", borderRight: "0.5px solid " + ink + "18", background: warm } }, /* @__PURE__ */ React.createElement("div", { style: { ...kicker, color: accent, marginBottom: 8 } }, "Net this period"), /* @__PURE__ */ React.createElement("div", { style: { font: "300 50px/1 var(--roma-serif)", color: ink, letterSpacing: "-0.022em", marginTop: 8 } }, money0(net)), /* @__PURE__ */ React.createElement("div", { style: { font: mono, letterSpacing: "0.1em", color: muted, textTransform: "uppercase", marginTop: 10 } }, "Owed \xB7 not yet paid out")), stat("Earned \xB7 locked", money0(earned), periodRows.filter((r) => r.status === "earned").length + " orders \xB7 paid in full", TONE.earned), stat("Pending", money0(pending), periodRows.filter((r) => r.status === "pending").length + " orders \xB7 awaiting payment", TONE.pending), stat("Paid out", money0(paid), periodRows.filter((r) => r.status === "paid").length + " orders \xB7 disbursed", TONE.paid))), /* @__PURE__ */ React.createElement("div", { style: { background: paper, border: "0.5px solid " + ink + "22", marginBottom: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 22px", borderBottom: "0.5px solid " + ink + "18", display: "flex", justifyContent: "space-between", alignItems: "center" } }, /* @__PURE__ */ React.createElement("h3", { style: { font: "500 13px/1 var(--roma-sans)", letterSpacing: "0.04em", color: ink, margin: 0 } }, "Per-order breakdown \xB7 ", periodRows.length, " ", periodRows.length === 1 ? "entry" : "entries"), periodRows.length > 0 && /* @__PURE__ */ React.createElement("a", { onClick: exportCsv, style: { font: "500 11px/1 var(--roma-sans)", color: accent, letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer" } }, "Export CSV")), /* @__PURE__ */ React.createElement("div", { style: { overflowX: "auto" } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 760 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 22px", borderBottom: "0.5px solid " + ink + "11", display: "grid", gridTemplateColumns: gridCols, gap: 14, alignItems: "center", ...kicker, fontSize: 10, letterSpacing: "0.14em", color: muted } }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Order"), /* @__PURE__ */ React.createElement("span", null, "Customer"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Order total"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Rate"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Commission"), /* @__PURE__ */ React.createElement("span", null, "State \xB7 when"), /* @__PURE__ */ React.createElement("span", null)), periodRows.map((r, i) => {
      const m = stateMeta(r);
      const flag = lowMargin(r);
      const neg = r.status === "forfeited";
      const bar = neg ? TONE.forfeited : flag ? TONE.flag : "transparent";
      return /* @__PURE__ */ React.createElement("div", { key: r.id, onClick: () => navigate("order-detail", r.order_id), style: {
        padding: "12px 22px",
        borderBottom: i < periodRows.length - 1 ? "0.5px solid " + ink + "11" : "none",
        display: "grid",
        gridTemplateColumns: gridCols,
        gap: 14,
        alignItems: "center",
        cursor: "pointer"
      } }, /* @__PURE__ */ React.createElement("span", { style: { width: 4, height: 28, background: bar } }), /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.06em", color: ink } }, r.order_number), /* @__PURE__ */ React.createElement("span", { style: { font: "400 14px/1.3 var(--roma-sans)", color: ink, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, r.customer_name), /* @__PURE__ */ React.createElement("span", { style: { font: "400 14px/1 var(--roma-serif)", color: ink, textAlign: "right" } }, "$", parseFloat(r.order_total).toLocaleString()), /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.06em", color: neg ? TONE.forfeited : muted, textAlign: "right" } }, neg ? "\u2212" : "", (parseFloat(r.commission_rate) * 100).toFixed(1), "%"), /* @__PURE__ */ React.createElement("span", { style: { font: "300 22px/1 var(--roma-serif)", color: neg ? TONE.forfeited : ink, textAlign: "right" } }, neg ? "\u2212" : "", money2(r.commission_amount)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: m.tone, textTransform: "uppercase" } }, "\u25CF ", m.label), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.4 var(--roma-sans)", color: ink + "99", marginTop: 2 } }, m.when, parseFloat(r.labor_commission || 0) > 0 ? " \xB7 incl. " + money2(r.labor_commission) + " labor @ " + (laborRate * 100).toFixed(0) + "%" : "", flag ? " \xB7 margin " + Math.round(parseFloat(r.margin) / matBase(r) * 100) + "% (below floor)" : "")), /* @__PURE__ */ React.createElement("span", { style: { color: muted, font: "400 14px/1 var(--roma-serif)", textAlign: "right" } }, "\u2192"));
    }), periodRows.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "48px 22px", textAlign: "center", font: "400 14px/1.5 var(--roma-sans)", color: muted } }, "No commissions accrued in ", cur.long, ".")))), forfeited > 0 && /* @__PURE__ */ React.createElement("div", { style: { background: paper, border: "0.5px solid " + ink + "22", borderLeft: "3px solid " + TONE.forfeited, padding: "14px 22px", marginBottom: 16, display: "flex", justifyContent: "space-between", alignItems: "center", gap: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 var(--roma-sans)", color: ink + "cc" } }, money2(forfeited), " forfeited this period \u2014 commission on cancelled orders that will not pay out."), /* @__PURE__ */ React.createElement("span", { style: { ...kicker, fontSize: 10, color: TONE.forfeited, whiteSpace: "nowrap" } }, periodRows.filter((r) => r.status === "forfeited").length, " orders")), /* @__PURE__ */ React.createElement("div", { style: { background: "#1c1917", color: paper, padding: "22px 28px", display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 28 } }, footStat("YTD commission", money0(ytdCommission), ytdRows.length + " orders this year"), footStat("Commission rate", (baseRate * 100).toFixed(0) + " / " + (laborRate * 100).toFixed(0) + "%", "materials margin / labor", "#caa97f"), footStat("Paid to date", money0(lifetimePaid), "lifetime payouts"), footStat("Forfeited YTD", money0(ytdForfeited), "cancelled orders")));
  }
  function RepVisitsListView({ navigate }) {
    const [visits, setVisits] = useState([]);
    const [loading, setLoading] = useState(true);
    const [search, setSearch] = useState("");
    const [statusFilter, setStatusFilter] = useState("");
    const [activeView, setActiveView] = useState("all");
    const [scope, setScope] = useState("team");
    const [selected, setSelected] = useState(/* @__PURE__ */ new Set());
    const [page, setPage] = useState(1);
    const perPage = 50;
    const load = useCallback(() => {
      setLoading(true);
      const qs = scope === "team" ? "?scope=team" : "";
      repFetch("/api/rep/visits" + qs).then((d) => {
        setVisits(d.visits || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [scope]);
    useEffect(load, [load]);
    const viewFilters = {
      "engaged": (v) => v.status === "opened" || v.status === "carted",
      "awaiting": (v) => v.status === "sent",
      "drafts": (v) => v.status === "draft",
      "all": () => true
    };
    const filteredVisits = useMemo(() => {
      const fn = viewFilters[activeView] || (() => true);
      const q = search.trim().toLowerCase();
      return visits.filter((v) => fn(v) && (!statusFilter || v.status === statusFilter) && (!q || (v.customer_name || "").toLowerCase().includes(q) || (v.customer_email || "").toLowerCase().includes(q)));
    }, [visits, activeView, statusFilter, search]);
    const viewCounts = useMemo(() => ({
      "engaged": visits.filter(viewFilters["engaged"]).length,
      "awaiting": visits.filter(viewFilters["awaiting"]).length,
      "drafts": visits.filter(viewFilters["drafts"]).length,
      "all": visits.length
    }), [visits]);
    const totalPages = Math.ceil(filteredVisits.length / perPage);
    const pageVisits = filteredVisits.slice((page - 1) * perPage, page * perPage);
    const toggleSelect = (id) => {
      setSelected((prev) => {
        const n = new Set(prev);
        n.has(id) ? n.delete(id) : n.add(id);
        return n;
      });
    };
    const toggleAll = () => {
      if (selected.size === pageVisits.length) setSelected(/* @__PURE__ */ new Set());
      else setSelected(new Set(pageVisits.map((v) => v.id)));
    };
    const getAge = (d) => {
      if (!d) return "\u2014";
      const days = Math.floor((Date.now() - new Date(d).getTime()) / 864e5);
      if (days === 0) return "today";
      return days + "d";
    };
    const fmtDate = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "\u2014";
    const getStage = (v) => {
      if (v.status === "carted") return { label: "Carted", sub: "Added items to cart", tone: "#3a7a4e" };
      if (v.status === "opened") return { label: "Opened", sub: "Customer viewed", tone: "var(--gold, #c9a668)" };
      if (v.status === "sent") return { label: "Sent", sub: "Awaiting open", tone: "#5c6e94" };
      return { label: "Draft", sub: "Not sent yet", tone: "var(--brass-muted)" };
    };
    const VIEWS = [
      { key: "all", label: "All" },
      { key: "engaged", label: "Engaged", tone: "var(--gold, #c9a668)" },
      { key: "awaiting", label: "Awaiting open" },
      { key: "drafts", label: "Drafts" }
    ];
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25CF", " Showroom visits ", "\xB7", " ", viewCounts["engaged"], " engaged ", "\xB7", " ", visits.length, " total"), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Visits"))), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: () => navigate("visit-create") }, "+ New visit"))), /* @__PURE__ */ React.createElement("div", { className: "ol-saved-views" }, VIEWS.map((v) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: v.key,
        className: "ol-view-tab" + (activeView === v.key ? " active" : ""),
        onClick: () => {
          setActiveView(v.key);
          setPage(1);
          setSelected(/* @__PURE__ */ new Set());
        }
      },
      v.label,
      /* @__PURE__ */ React.createElement("span", { className: "ol-tab-count", style: v.tone && viewCounts[v.key] > 0 ? { background: v.tone.startsWith("var") ? "" : v.tone + "1a", color: v.tone } : void 0 }, viewCounts[v.key])
    )), /* @__PURE__ */ React.createElement(ScopeToggle, { scope, setScope, onChange: () => {
      setPage(1);
      setSelected(/* @__PURE__ */ new Set());
    } })), /* @__PURE__ */ React.createElement("div", { className: "ol-filter-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-filter-chips" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        className: "ol-chip",
        value: statusFilter,
        onChange: (e) => {
          setStatusFilter(e.target.value);
          setPage(1);
        },
        style: { appearance: "auto", paddingRight: 24 }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, "Status: Any"),
      /* @__PURE__ */ React.createElement("option", { value: "draft" }, "Draft"),
      /* @__PURE__ */ React.createElement("option", { value: "sent" }, "Sent"),
      /* @__PURE__ */ React.createElement("option", { value: "opened" }, "Opened"),
      /* @__PURE__ */ React.createElement("option", { value: "carted" }, "Carted")
    ), statusFilter && /* @__PURE__ */ React.createElement("span", { className: "ol-chip active", onClick: () => setStatusFilter("") }, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-label" }, "Status:"), " ", statusFilter, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-remove" }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search visits, customers",
        "aria-label": "Search visits",
        value: search,
        onChange: (e) => {
          setSearch(e.target.value);
          setPage(1);
        }
      }
    )), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, filteredVisits.length, " rows"))), selected.size > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-label" }, "\u25CF", " ", selected.size, " visit", selected.size !== 1 ? "s" : "", " selected"), /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: () => setSelected(/* @__PURE__ */ new Set()) }, "\u2715", " Clear"))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 22px", background: "#ece5d8", border: "0.5px solid rgba(28,25,23,0.14)" } }, "Loading visits", "\u2026") : /* @__PURE__ */ React.createElement("div", { className: "vl-table-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "vl-table-head" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "checkbox",
        style: { margin: 0 },
        "aria-label": "Select all visits on page",
        checked: pageVisits.length > 0 && selected.size === pageVisits.length,
        onChange: toggleAll
      }
    ), /* @__PURE__ */ React.createElement("span", null, "Customer"), /* @__PURE__ */ React.createElement("span", null, "Items"), /* @__PURE__ */ React.createElement("span", null, "Sent"), /* @__PURE__ */ React.createElement("span", null, "Opened"), /* @__PURE__ */ React.createElement("span", null, "Stage"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Age"), /* @__PURE__ */ React.createElement("span", null)), pageVisits.map((v) => {
      const stage = getStage(v);
      const isSel = selected.has(v.id);
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: v.id,
          className: "vl-table-row" + (isSel ? " selected" : ""),
          role: "button",
          tabIndex: 0,
          onClick: () => {
            repListNav.visits = filteredVisits.map((x) => x.id);
            navigate("visit-detail", v.id);
          },
          onKeyDown: (e) => {
            if (e.key === "Enter" || e.key === " ") {
              e.preventDefault();
              repListNav.visits = filteredVisits.map((x) => x.id);
              navigate("visit-detail", v.id);
            }
          }
        },
        /* @__PURE__ */ React.createElement(
          "input",
          {
            type: "checkbox",
            style: { margin: 0 },
            checked: isSel,
            "aria-label": "Select visit for " + v.customer_name,
            onChange: () => toggleSelect(v.id),
            onClick: (e) => e.stopPropagation(),
            onKeyDown: (e) => e.stopPropagation()
          }
        ),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "vl-cust-name" }, v.customer_name), v.customer_email && /* @__PURE__ */ React.createElement("div", { className: "vl-cust-sub" }, v.customer_email)),
        /* @__PURE__ */ React.createElement("span", { className: "vl-items" }, v.item_count, " item", v.item_count !== 1 ? "s" : ""),
        /* @__PURE__ */ React.createElement("span", { className: "vl-date" }, fmtDate(v.sent_at)),
        /* @__PURE__ */ React.createElement("span", { className: "vl-date" }, fmtDate(v.opened_at)),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "vl-stage", style: { color: stage.tone } }, "\u25CF", " ", stage.label), /* @__PURE__ */ React.createElement("div", { className: "vl-stage-sub" }, stage.sub)),
        /* @__PURE__ */ React.createElement("span", { className: "vl-age" }, getAge(v.created_at)),
        /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)", textAlign: "right" } }, "\u2192")
      );
    }), pageVisits.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif", marginBottom: 14 } }, "No visits match this view."), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: () => navigate("visit-create") }, "+ New visit")), filteredVisits.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-footer" }, /* @__PURE__ */ React.createElement("span", null, "Showing ", Math.min((page - 1) * perPage + 1, filteredVisits.length), "\u2013", Math.min(page * perPage, filteredVisits.length), " of ", filteredVisits.length), /* @__PURE__ */ React.createElement("span", null, filteredVisits.reduce((s, v) => s + (v.item_count || 0), 0), " items shared")), totalPages > 1 && /* @__PURE__ */ React.createElement("div", { className: "ol-pagination" }, /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, perPage, " per page"), /* @__PURE__ */ React.createElement("div", { className: "ol-page-btns" }, /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page <= 1, onClick: () => setPage((p) => p - 1) }, "\u2190"), Array.from({ length: Math.min(totalPages, 5) }, (_, i) => i + 1).map((p) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: p,
        className: "ol-page-btn" + (page === p ? " active" : ""),
        onClick: () => setPage(p)
      },
      p
    )), /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page >= totalPages, onClick: () => setPage((p) => p + 1) }, "\u2192")))));
  }
  function RepVisitBuilderView({ navigate }) {
    const [pickedCustomer, setPickedCustomer] = useState(null);
    const [customerName, setCustomerName] = useState("");
    const [customerEmail, setCustomerEmail] = useState("");
    const [customerPhone, setCustomerPhone] = useState("");
    const [message, setMessage] = useState("");
    const [sidemark, setSidemark] = useState("");
    const [items, setItems] = useState([]);
    const [skuSearch, setSkuSearch] = useState("");
    const [skuResults, setSkuResults] = useState([]);
    const [skuSearching, setSkuSearching] = useState(false);
    const skuSearchTimer = useRef(null);
    const skuSearchCounter = useRef(0);
    const [saving, setSaving] = useState(false);
    const [catalogOpen, setCatalogOpen] = useState(false);
    const visitSavedRef = useRef(false);
    useRofDraft(
      "visit-builder",
      "visit recap",
      () => !visitSavedRef.current && (items.length > 0 || !!pickedCustomer || !!(customerName.trim() || customerEmail.trim() || customerPhone.trim() || message.trim())),
      () => ({ pickedCustomer, customerName, customerEmail, customerPhone, message, sidemark, items })
    );
    useRofDraftRestore("visit-builder", (s) => {
      if (s.pickedCustomer) setPickedCustomer(s.pickedCustomer);
      setCustomerName(s.customerName || "");
      setCustomerEmail(s.customerEmail || "");
      setCustomerPhone(s.customerPhone || "");
      setMessage(s.message || "");
      setSidemark(s.sidemark || "");
      if (Array.isArray(s.items)) setItems(s.items);
    });
    const searchSku = (q) => {
      setSkuSearch(q);
      if (skuSearchTimer.current) clearTimeout(skuSearchTimer.current);
      const thisSearch = ++skuSearchCounter.current;
      if (q.length < 2) {
        setSkuResults([]);
        setSkuSearching(false);
        return;
      }
      setSkuSearching(true);
      skuSearchTimer.current = setTimeout(async () => {
        try {
          const data = await repFetch("/api/rep/skus/search?q=" + encodeURIComponent(q));
          if (thisSearch !== skuSearchCounter.current) return;
          setSkuResults(data.results || []);
        } catch (e) {
          if (thisSearch !== skuSearchCounter.current) return;
        }
        if (thisSearch === skuSearchCounter.current) setSkuSearching(false);
      }, 250);
    };
    const addProduct = (r) => {
      if (items.find((i) => i.sku_id === r.sku_id)) return;
      setItems((prev) => [...prev, {
        _key: Date.now(),
        product_id: r.product_id || null,
        sku_id: r.sku_id,
        product_name: r.product_name,
        collection: r.collection,
        variant_name: r.variant_name,
        vendor_name: r.vendor_name,
        internal_sku: r.internal_sku,
        primary_image: r.primary_image || null,
        retail_price: r.retail_price ? parseFloat(r.retail_price) : null,
        price_basis: r.price_basis,
        rep_note: ""
      }]);
      setSkuSearch("");
      setSkuResults([]);
    };
    const removeItem = (key) => setItems((prev) => prev.filter((i) => i._key !== key));
    const updateNote = (key, note) => {
      setItems((prev) => prev.map((i) => i._key === key ? { ...i, rep_note: note } : i));
    };
    const addFromCatalog = ({ lines, accessories, customs }) => {
      setItems((prev) => {
        const next = [...prev];
        const seen = new Set(next.map((i) => i.sku_id));
        let key = Date.now();
        const push = (entry) => {
          if (seen.has(entry.sku_id)) return;
          seen.add(entry.sku_id);
          next.push({ _key: key++, rep_note: "", ...entry });
        };
        for (const { item: p } of lines) {
          push({
            product_id: p.id || p.product_id || null,
            sku_id: p.sku_id,
            product_name: p.display_name || p.name || p.product_name || "",
            collection: p.collection || "",
            variant_name: p.variant_name || "",
            vendor_name: p.brand_name || p.vendor_name || "",
            internal_sku: p.vendor_sku || p.internal_sku || "",
            primary_image: p.primary_image || null,
            retail_price: p.price ? parseFloat(p.price) : null,
            price_basis: p.price_basis || null
          });
        }
        for (const { acc, parent } of accessories) {
          push({
            product_id: acc.product_id || null,
            sku_id: acc.sku_id,
            product_name: acc.accessory_label || acc.variant_name || "Accessory",
            collection: "",
            variant_name: acc.variant_name && acc.variant_name !== acc.accessory_label ? acc.variant_name : "",
            vendor_name: parent ? parent.brand_name || parent.vendor_name || "" : "",
            internal_sku: acc.vendor_sku || "",
            primary_image: acc.primary_image || null,
            retail_price: acc.retail_price ? parseFloat(acc.retail_price) : null,
            price_basis: null
          });
        }
        return next;
      });
      if (customs && customs.length) {
        alert("Custom items were skipped \u2014 a visit recap can only link real catalog products.");
      }
    };
    const saveVisit = async (andSend) => {
      if (customerName.trim().split(/\s+/).length < 2) return alert("Customer first and last name are required");
      if (!customerEmail.trim() || !isValidEmail(customerEmail.trim())) return alert("A valid customer email is required");
      if (customerPhone.trim() && !isValidPhone(customerPhone)) return alert("Enter a valid 10-digit phone or leave it blank");
      if (!items.length) return alert("Add at least one product");
      setSaving(true);
      try {
        const body = {
          customer_name: customerName.trim(),
          customer_email: customerEmail.trim(),
          customer_phone: customerPhone.trim() || null,
          message: message.trim() || null,
          sidemark: sidemark.trim() || null,
          items: items.map((i) => ({
            product_id: i.product_id,
            sku_id: i.sku_id,
            rep_note: i.rep_note || null
          }))
        };
        const created = await repFetch("/api/rep/visits", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(body)
        });
        if (andSend && created.visit) {
          await repFetch("/api/rep/visits/" + created.visit.id + "/send", { method: "POST" });
        }
        visitSavedRef.current = true;
        rofDraftStore.clear("visit-builder");
        repToast(andSend ? "Visit sent" : "Visit saved");
        navigate("visit-detail", created.visit.id);
      } catch (err) {
        alert("Error: " + (err.message || "Failed to save"));
      }
      setSaving(false);
    };
    const canSave = customerName.trim().split(/\s+/).length >= 2 && !!customerEmail.trim() && isValidEmail(customerEmail.trim()) && items.length > 0;
    const canSend = canSave;
    const initials = (customerName.trim() || "?").split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase();
    const checklist = [
      { label: "Customer name", done: customerName.trim().split(/\s+/).length >= 2, meta: customerName.trim().split(/\s+/).length >= 2 ? "" : "First and last name required" },
      { label: "Email address", done: !!customerEmail.trim() && isValidEmail(customerEmail.trim()), meta: customerEmail.trim() && isValidEmail(customerEmail.trim()) ? "" : "Required" },
      { label: "Products added", done: items.length > 0, meta: items.length > 0 ? items.length + " item" + (items.length !== 1 ? "s" : "") : "Search or add below" },
      { label: "Personal message", done: !!message.trim(), meta: message.trim() ? "" : "Optional" }
    ];
    const firstTodo = checklist.findIndex((c) => !c.done);
    const STEPS = [
      { label: "Draft", meta: "Compose the recap" },
      { label: "Sent", meta: "Emailed to the customer" },
      { label: "Opened", meta: "Customer views it" },
      { label: "Carted", meta: "Items added to cart" }
    ];
    return /* @__PURE__ */ React.createElement("div", { style: { paddingBottom: 40 } }, /* @__PURE__ */ React.createElement("div", { className: "qw-breadcrumb" }, /* @__PURE__ */ React.createElement("div", { className: "qw-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("visits") }, "Visits"), /* @__PURE__ */ React.createElement("span", { className: "qw-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "qw-crumb-current" }, "New visit")), /* @__PURE__ */ React.createElement("div", { className: "qw-meta-right" }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)" } }, "\u25CF", " Draft \u2014 not sent"))), /* @__PURE__ */ React.createElement("div", { className: "qw-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h1", null, "New ", /* @__PURE__ */ React.createElement("em", null, "visit")), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, initials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, customerName.trim() || "No customer yet"), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, customerEmail.trim() || customerPhone.trim() ? [customerEmail.trim(), customerPhone.trim()].filter(Boolean).join(" \xB7 ") : "Enter the customer\u2019s details below")))), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", disabled: saving || !canSave, onClick: () => saveVisit(false) }, saving ? "Saving\u2026" : "Save draft"), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", disabled: saving || !canSend, onClick: () => saveVisit(true) }, saving ? "Saving\u2026" : "Save & send"))), /* @__PURE__ */ React.createElement("div", { className: "qw-stepper cols-4" }, STEPS.map((s, i) => /* @__PURE__ */ React.createElement("div", { key: s.label, className: "qw-step " + (i === 0 ? "now" : "next") }, /* @__PURE__ */ React.createElement("div", { className: "qw-step-label" }, /* @__PURE__ */ React.createElement("span", { className: "qw-step-mark" }, i === 0 ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", null, s.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-step-meta" }, s.meta)))), /* @__PURE__ */ React.createElement("div", { className: "qw-grid" }, /* @__PURE__ */ React.createElement("div", { className: "qw-main" }, /* @__PURE__ */ React.createElement(WizRepCard, { title: "Customer" }, /* @__PURE__ */ React.createElement(
      RepCustomerEntry,
      {
        selected: pickedCustomer,
        onSelect: (c) => {
          setPickedCustomer(c);
          setCustomerName(c.name || "");
          setCustomerEmail(c.email || "");
          setCustomerPhone(c.phone || "");
        },
        onClear: () => {
          setPickedCustomer(null);
          setCustomerName("");
          setCustomerEmail("");
          setCustomerPhone("");
        },
        name: customerName,
        email: customerEmail,
        phone: customerPhone,
        onName: setCustomerName,
        onEmail: setCustomerEmail,
        onPhone: setCustomerPhone,
        requirePhone: false
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Sidemark ", /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)", fontWeight: 400 } }, "\xB7 job reference (optional)")), /* @__PURE__ */ React.createElement(
      SidemarkSelect,
      {
        value: sidemark,
        onChange: setSidemark,
        customerId: pickedCustomer && pickedCustomer.id,
        email: customerEmail,
        placeholder: "e.g. Smith Residence \u2014 Master Bath"
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Personal Message"), /* @__PURE__ */ React.createElement("textarea", { value: message, onChange: (e) => setMessage(e.target.value), placeholder: "Thanks for visiting! Here are the materials we discussed...", rows: "3" }))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Products \xB7 " + items.length }, /* @__PURE__ */ React.createElement("button", { className: "rof-cat-launch", onClick: () => setCatalogOpen(true) }, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-icon" }, "\u229E"), /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-title" }, "Browse the full catalog"), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-sub" }, "Filter by vendor and add everything the customer viewed in one pass")), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-cta" }, "Open catalog ", "\u2192")), /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap", style: { marginBottom: "0.75rem" } }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: skuSearch,
        placeholder: "Quick add \u2014 search by product name, SKU, or collection...",
        onBlur: () => setTimeout(() => {
          setSkuResults([]);
          setSkuSearching(false);
        }, 200),
        onChange: (e) => searchSku(e.target.value),
        style: { width: "100%", padding: "0.5625rem 0.75rem", border: "0.5px solid rgba(28,25,23,0.18)", background: "#ece5d8", font: "400 0.8125rem/1.4 Inter, sans-serif", boxSizing: "border-box" }
      }
    ), skuSearching && skuResults.length === 0 && skuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results", style: { padding: "0.75rem", color: "#8a7e68", fontSize: "0.8125rem" } }, "Searching..."), !skuSearching && skuResults.length === 0 && skuSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results", style: { padding: "0.75rem", color: "#8a7e68", fontSize: "0.8125rem" } }, "No results found"), skuResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, skuResults.map((r) => /* @__PURE__ */ React.createElement("div", { key: r.sku_id, className: "rof-search-item", onMouseDown: (e) => {
      e.preventDefault();
      addProduct(r);
    } }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(r.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover", flexShrink: 0 }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: r.collection }), 50), flexShrink: 0 } }), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, formatLineItem(r).name), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, r.collection || "", r.vendor_name ? " \xB7 " + r.vendor_name : "", r.internal_sku ? " \xB7 " + r.internal_sku : "", r.retail_price ? " \xB7 $" + pieceRetail(r).toFixed(2) + skuPriceSuffix(r) : "")))))), items.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "rof-items-empty" }, "Search and add the products the customer viewed in the showroom") : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rof-items-header", style: { gridTemplateColumns: "36px 1fr 200px auto 24px" } }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Product"), /* @__PURE__ */ React.createElement("span", null, "Rep note"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Price"), /* @__PURE__ */ React.createElement("span", null)), items.map((item) => /* @__PURE__ */ React.createElement("div", { key: item._key, className: "rof-item-row", style: { gridTemplateColumns: "36px 1fr 200px auto 24px" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-swatch" }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover" }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: item.collection }), 50) } })), /* @__PURE__ */ React.createElement("div", { className: "rof-item-info" }, formatLineItem(item).brand && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, formatLineItem(item).brand), /* @__PURE__ */ React.createElement("div", { className: "rof-item-name" }, formatLineItem(item).name), formatLineItem(item).sku && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, formatLineItem(item).sku)), /* @__PURE__ */ React.createElement(
      "input",
      {
        value: item.rep_note,
        placeholder: "Why they liked it, room, finish...",
        onChange: (e) => updateNote(item._key, e.target.value),
        style: { width: "100%", padding: "0.35rem 0.5rem", border: "0.5px solid rgba(28,25,23,0.18)", background: "transparent", font: "400 0.75rem/1.3 Inter, sans-serif", color: "#1c1917", boxSizing: "border-box" }
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "rof-item-price" }, item.retail_price ? "$" + item.retail_price.toFixed(2) : "\u2014", item.retail_price && item.price_basis === "per_sqft" ? /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.625rem/1 Inter, sans-serif", color: "#8a7e68" } }, "/sqft") : null), /* @__PURE__ */ React.createElement("button", { className: "rof-remove-btn", onClick: () => removeItem(item._key), title: "Remove" }, "\u2715")))))), /* @__PURE__ */ React.createElement("aside", { className: "qw-rail" }, /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Draft checklist"), checklist.map((c, i) => {
      const state = c.done ? "done" : i === firstTodo ? "now" : "next";
      return /* @__PURE__ */ React.createElement("div", { key: c.label, className: "qw-check-row " + state }, /* @__PURE__ */ React.createElement("span", { className: "qw-check-mark" }, c.done ? "\u2713" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", { className: "qw-check-label" }, c.label), /* @__PURE__ */ React.createElement("span", { className: "qw-check-meta" }, c.meta));
    })), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Ready to send"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "Sending emails the customer a personal recap with every product you added \u2014 notes included."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: () => saveVisit(true), disabled: saving || !canSend }, "Save & send ", "\u2192"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-note" }, canSend ? "Goes to " + customerEmail.trim() : "Needs a name, an email, and at least one product")))), catalogOpen && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data: { customer: null, newCustomerName: customerName.trim() },
        showCost: false,
        contextLabel: "Add to visit \xB7 " + (customerName.trim() || "New visit"),
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog
      }
    ));
  }
  function RepVisitDetailView({ navigate, editId }) {
    const [visit, setVisit] = useState(null);
    const [items, setItems] = useState([]);
    const [loading, setLoading] = useState(true);
    const [sending, setSending] = useState(false);
    const [copied, setCopied] = useState(false);
    useEffect(() => {
      repFetch("/api/rep/visits/" + editId).then((d) => {
        setVisit(d.visit);
        setItems(d.items || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [editId]);
    const sendRecap = async () => {
      if (!visit.customer_email) return alert("No customer email set");
      setSending(true);
      try {
        await repFetch("/api/rep/visits/" + editId + "/send", { method: "POST" });
        const d = await repFetch("/api/rep/visits/" + editId);
        setVisit(d.visit);
      } catch (err) {
        alert("Error: " + (err.message || "Failed to send"));
      }
      setSending(false);
    };
    const deleteVisit = async () => {
      if (!confirm("Delete this visit?")) return;
      try {
        await repFetch("/api/rep/visits/" + editId, { method: "DELETE" });
        navigate("visits");
      } catch (err) {
        alert("Error: " + (err.message || "Failed to delete"));
      }
    };
    const copyUrl = () => {
      const url = window.location.origin + "/visit/" + visit.token;
      navigator.clipboard.writeText(url).then(() => {
        setCopied(true);
        setTimeout(() => setCopied(false), 2e3);
      });
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading visit...");
    if (!visit) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Visit not found");
    const priceLabel = (item) => {
      if (!item.retail_price) return "\u2014";
      const p = "$" + parseFloat(item.retail_price).toFixed(2);
      return item.price_basis === "per_sqft" ? p + "/sqft" : p;
    };
    const fmtDT = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" }) + ", " + new Date(d).toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }) : null;
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const st = visit.status === "carted" ? { label: "Carted", tone: "#3a7a4e" } : visit.status === "opened" ? { label: "Opened", tone: "var(--gold, #c9a668)" } : visit.status === "sent" ? { label: "Sent", tone: "#5c6e94" } : { label: "Draft", tone: "var(--brass-muted)" };
    const stepRank = visit.status === "carted" ? 3 : visit.status === "opened" ? 2 : visit.status === "sent" ? 1 : 0;
    const steps = [
      { label: "Draft", meta: fmtDT(visit.created_at) || "Compose the recap" },
      { label: "Sent", meta: fmtDT(visit.sent_at) || "Emailed to the customer" },
      { label: "Opened", meta: fmtDT(visit.opened_at) || "Customer views it" },
      { label: "Carted", meta: fmtDT(visit.items_carted_at) || "Items added to cart" }
    ];
    const initials = (visit.customer_name || "?").split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase();
    const canSend = visit.customer_email && (visit.status === "draft" || visit.status === "sent");
    const visitUrl = window.location.origin + "/visit/" + visit.token;
    const timeline = [
      { label: "Created", at: visit.created_at },
      { label: "Sent", at: visit.sent_at },
      { label: "Opened", at: visit.opened_at },
      ...visit.quote_requested_at ? [{ label: "Quote requested", at: visit.quote_requested_at }] : [],
      { label: "Carted", at: visit.items_carted_at }
    ];
    const firstPending = timeline.findIndex((t) => !t.at);
    const mini = (label, value) => /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 4 } }, label), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 Inter, sans-serif", color: "#1c1917", letterSpacing: "-0.005em", wordBreak: "break-word" } }, value));
    return /* @__PURE__ */ React.createElement("div", { style: { paddingBottom: 40 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("visits") }, "Visits"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, visit.customer_name)), /* @__PURE__ */ React.createElement("div", { className: "qw-meta-right", style: { alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", null, "Created ", fmtDT(visit.created_at)), /* @__PURE__ */ React.createElement(RepPrevNext, { section: "visits", id: editId, view: "visit-detail", navigate, linkStyle: { color: "var(--brass-muted)", textDecoration: "none" } }))), /* @__PURE__ */ React.createElement("div", { className: "ws-head" }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, "Visit ", /* @__PURE__ */ React.createElement("em", null, "recap")), /* @__PURE__ */ React.createElement("span", { className: "rov-chip soft", style: { color: st.tone } }, "\u25CF", " ", st.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, initials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, visit.customer_name), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, [visit.customer_email, visit.customer_phone].filter(Boolean).join(" \xB7 ") || "No contact details on file")))), /* @__PURE__ */ React.createElement("div", { className: "ws-head-actions" }, visit.status === "draft" && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", style: { color: "#c54731" }, onClick: deleteVisit }, "Delete"), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: copyUrl }, copied ? "Copied!" : "Copy link"), canSend && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", disabled: sending, onClick: sendRecap }, sending ? "Sending\u2026" : visit.status === "sent" ? "Resend email" : "Send email"))), /* @__PURE__ */ React.createElement("div", { className: "qw-stepper cols-4" }, steps.map((s, i) => {
      const state = i < stepRank ? "done" : i === stepRank ? "now" : "next";
      return /* @__PURE__ */ React.createElement("div", { key: s.label, className: "qw-step " + state }, /* @__PURE__ */ React.createElement("div", { className: "qw-step-label" }, /* @__PURE__ */ React.createElement("span", { className: "qw-step-mark" }, state === "done" ? "\u2713" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", null, s.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-step-meta" }, s.meta));
    })), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Products"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, items.length)), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Sent"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, visit.sent_at ? fmtDT(visit.sent_at) : "\u2014")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Opened"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, visit.opened_at ? fmtDT(visit.opened_at) : "\u2014")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Carted"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: visit.items_carted_at ? { color: "var(--rod-green)" } : void 0 }, visit.items_carted_at ? fmtDT(visit.items_carted_at) : "\u2014"))), /* @__PURE__ */ React.createElement("div", { className: "qw-grid" }, /* @__PURE__ */ React.createElement("div", { className: "qw-main" }, /* @__PURE__ */ React.createElement(WizRepCard, { title: "Customer" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: "1rem" } }, mini("Name", visit.customer_name), mini("Email", visit.customer_email || "\u2014"), mini("Phone", visit.customer_phone || "\u2014"), visit.sidemark && mini("Sidemark", visit.sidemark)), visit.message && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "1rem", padding: "0.75rem 1rem", background: "rgba(28,25,23,0.03)", borderLeft: "2px solid var(--gold, #c9a668)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 6 } }, "Personal message"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "#1c1917" } }, visit.message))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Products \xB7 " + items.length }, items.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "rof-items-empty" }, "No products in this visit") : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rof-items-header", style: { gridTemplateColumns: "36px 1fr auto" } }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Product"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Price")), items.map((item) => /* @__PURE__ */ React.createElement("div", { key: item.id, className: "rof-item-row", style: { gridTemplateColumns: "36px 1fr auto" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-swatch" }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover" }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: item.collection }), 50) } })), /* @__PURE__ */ React.createElement("div", { className: "rof-item-info" }, formatLineItem(item).brand && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, formatLineItem(item).brand), /* @__PURE__ */ React.createElement("div", { className: "rof-item-name" }, formatLineItem(item).name), formatLineItem(item).sku && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, formatLineItem(item).sku), item.rep_note && /* @__PURE__ */ React.createElement("div", { style: { font: "italic 400 12px/1.4 Inter, sans-serif", color: "#8a7e68", marginTop: 3, whiteSpace: "normal" } }, "\u201C", item.rep_note, "\u201D")), /* @__PURE__ */ React.createElement("div", { className: "rof-item-price" }, priceLabel(item))))))), /* @__PURE__ */ React.createElement("aside", { className: "qw-rail" }, /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Sales rep"), visit.rep_id ? /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, visit.rep_id === repInfo.id ? (((repInfo.first_name || "?")[0] || "") + ((repInfo.last_name || "")[0] || "")).toUpperCase() : "\u2022"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name" }, visit.rep_id === repInfo.id ? [repInfo.first_name, repInfo.last_name].filter(Boolean).join(" ") || "Rep" : "Assigned rep", visit.rep_id === repInfo.id && /* @__PURE__ */ React.createElement("span", { className: "ws-you-tag" }, "You")), visit.rep_id === repInfo.id && repInfo.email && /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, repInfo.email))) : /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar", style: { background: "var(--brass-warm, #d8cdb6)", color: "var(--brass-muted, #8a7e68)" } }, "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name", style: { color: "var(--rod-muted)" } }, "Unassigned"), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "No rep owns this visit yet")))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Activity"), timeline.map((t, i) => {
      const state = t.at ? "done" : i === firstPending ? "now" : "next";
      return /* @__PURE__ */ React.createElement("div", { key: t.label, className: "qw-check-row " + state }, /* @__PURE__ */ React.createElement("span", { className: "qw-check-mark" }, t.at ? "\u2713" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", { className: "qw-check-label" }, t.label), /* @__PURE__ */ React.createElement("span", { className: "qw-check-meta" }, fmtDT(t.at) || "Pending"));
    })), canSend && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, visit.status === "sent" ? "Sent \u2014 nudge again?" : "Ready to send"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, visit.status === "sent" ? "The recap email went out " + (fmtDT(visit.sent_at) || "already") + " but hasn\u2019t been opened yet." : "Sending emails the customer this recap with every product and note."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", disabled: sending, onClick: sendRecap }, sending ? "Sending\u2026" : (visit.status === "sent" ? "Resend email" : "Send email") + " \u2192"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-note" }, "Goes to ", visit.customer_email)), !visit.customer_email && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "No email on file"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "Share the link directly \u2014 copy it below and text it to the customer.")), (visit.quote_requested_at || visit.status === "opened" || visit.status === "carted") && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, visit.quote_requested_at ? "Customer asked for a quote" : visit.status === "carted" ? "Customer carted items" : "Customer is engaged"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, visit.quote_requested_at ? "They requested a quote from the recap " + (fmtDT(visit.quote_requested_at) || "") + "." + (visit.quote_request_message ? " \u201C" + visit.quote_request_message + "\u201D" : "") : visit.status === "carted" ? "They added items from this recap to their cart \u2014 a great time to follow up with a quote." : "They opened the recap " + (fmtDT(visit.opened_at) || "") + ". Consider following up while it\u2019s fresh."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: () => navigate("quote-create") }, "Start a quote ", "\u2192")), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Shareable link"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.5 ui-monospace, monospace", color: "#1c1917", wordBreak: "break-all", marginBottom: 10 } }, visitUrl), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", style: { width: "100%" }, onClick: copyUrl }, copied ? "Copied!" : "Copy link")))));
  }
  function RepDashboardView({ navigate }) {
    const [data, setData] = useState(null);
    const [commissionSummary, setCommissionSummary] = useState(null);
    const [actionItems, setActionItems] = useState([]);
    const [tasksDash, setTasksDash] = useState(null);
    useEffect(() => {
      repFetch("/api/rep/dashboard").then(setData).catch(() => {
      });
      repFetch("/api/rep/commissions/summary").then(setCommissionSummary).catch(() => {
      });
      repFetch("/api/rep/tasks/dashboard").then(setTasksDash).catch(() => {
      });
      repFetch("/api/rep/action-items?scope=mine").then((d) => setActionItems(d.action_items || [])).catch(() => {
      });
    }, []);
    if (!data) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading dashboard...");
    const { stats, metrics, recent_orders } = data;
    if (!stats || !metrics) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Failed to load dashboard");
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const hour = (/* @__PURE__ */ new Date()).getHours();
    const greeting = hour < 12 ? "Morning" : hour < 17 ? "Afternoon" : "Evening";
    const firstName = repInfo.first_name || "there";
    const now = /* @__PURE__ */ new Date();
    const dayNames = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];
    const monthNames = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
    const shortDay = dayNames[now.getDay()] + ", " + monthNames[now.getMonth()] + " " + now.getDate();
    const daysInMonth = new Date(now.getFullYear(), now.getMonth() + 1, 0).getDate();
    const daysLeft = daysInMonth - now.getDate();
    const mtdSales = metrics.sales_this_month || 0;
    const quota = metrics.sales_quota || 0;
    const quotaPct = quota > 0 ? (mtdSales / quota * 100).toFixed(0) : null;
    const pipelineVal = parseFloat(metrics.pipeline_value) || 0;
    const openQuotes = stats.my_quotes || 0;
    const commEarned = commissionSummary ? parseFloat(commissionSummary.total_earned || 0) + parseFloat(commissionSummary.total_pending || 0) : 0;
    const avgMargin = metrics.avg_margin || null;
    const triageCards = actionItems.slice(0, 4).map((item) => {
      const isOrder = item.type === "order";
      const stepCount = item.total_steps > 0 ? item.completed_steps + "/" + item.total_steps : "";
      return {
        tag: isOrder ? "Order" : "Sample",
        ref: item.reference,
        who: item.customer_name,
        what: item.pending_steps.slice(0, 2).join(", ") + (stepCount ? " \xB7 " + stepCount + " done" : ""),
        cta: "Open",
        tone: item.completed_steps / (item.total_steps || 1) < 0.3 ? "#c54731" : "#a87935",
        onClick: () => navigate(isOrder ? "order-detail" : "sample-detail", item.id)
      };
    });
    const overdueIds = new Set((tasksDash && tasksDash.overdue || []).map((o) => o.id));
    const dueNowTasks = tasksDash ? [...tasksDash.overdue || [], ...tasksDash.today || []] : [];
    const upcomingTasks = tasksDash ? tasksDash.upcoming || [] : [];
    const taskTimeMeta = (t, isOverdue) => {
      if (isOverdue) return { time: "Overdue", tone: "#c54731" };
      if (!t.due_date) return { time: "No date", tone: "#8a7e68" };
      const d = /* @__PURE__ */ new Date(String(t.due_date).split("T")[0] + "T00:00:00");
      const t0 = /* @__PURE__ */ new Date();
      t0.setHours(0, 0, 0, 0);
      const diff = Math.round((d - t0) / 864e5);
      if (diff <= 0) return { time: "Today", tone: "#a87935" };
      if (diff === 1) return { time: "Tomorrow", tone: "#3a7a4e" };
      if (diff < 7) return { time: d.toLocaleDateString("en-US", { weekday: "short" }), tone: "#8a7e68" };
      return { time: d.toLocaleDateString("en-US", { month: "short", day: "numeric" }), tone: "#8a7e68" };
    };
    const todayTasks = [...dueNowTasks, ...upcomingTasks].slice(0, 5).map((t) => {
      const meta = taskTimeMeta(t, overdueIds.has(t.id));
      return {
        task: t,
        time: meta.time,
        tone: meta.tone,
        label: t.title + (t.customer_name ? " \xB7 " + t.customer_name : "")
      };
    });
    const dashTaskTitle = dueNowTasks.length > 0 ? "Today \xB7 " + dueNowTasks.length + " thing" + (dueNowTasks.length !== 1 ? "s" : "") : todayTasks.length > 0 ? "Upcoming \xB7 " + todayTasks.length : "Today \xB7 clear";
    const completeDashTask = (id) => {
      repFetch("/api/rep/tasks/" + id + "/complete", { method: "PUT" }).then(() => setTasksDash((prev) => prev ? {
        ...prev,
        overdue: (prev.overdue || []).filter((t) => t.id !== id),
        today: (prev.today || []).filter((t) => t.id !== id),
        upcoming: (prev.upcoming || []).filter((t) => t.id !== id)
      } : prev)).catch(() => {
      });
    };
    const openDashTask = (t) => {
      if (t.linked_order_id) navigate("order-detail", t.linked_order_id);
      else if (t.linked_quote_id) navigate("quote-edit", t.linked_quote_id);
      else if (t.linked_estimate_id) navigate("estimate-edit", t.linked_estimate_id);
      else navigate("tasks");
    };
    const momentumItems = (recent_orders || []).slice(0, 5).map((o) => {
      const ago = Math.floor((Date.now() - new Date(o.created_at).getTime()) / 36e5);
      const lastLabel = ago < 1 ? "Just now" : ago < 24 ? ago + "h ago" : Math.floor(ago / 24) + "d ago";
      const statusLabel = o.status === "pending" ? "Pending \xB7 needs action" : o.status === "confirmed" ? "Confirmed" : o.status === "shipped" ? "Shipped \xB7 in transit" : o.status === "delivered" ? "Delivered" : o.status;
      const statusTone = o.status === "pending" ? "#a87935" : o.status === "shipped" ? "#3a7a4e" : o.status === "cancelled" ? "#c54731" : "#5c6e94";
      return {
        who: o.customer_name + " \xB7 " + o.order_number,
        last: lastLabel,
        signal: statusLabel,
        tone: statusTone,
        action: "Open",
        onClick: () => navigate("order-detail", o.id)
      };
    });
    const progressPct = quota > 0 ? Math.min(100, mtdSales / quota * 100) : 0;
    const dayProgress = quota > 0 ? Math.min(100, now.getDate() / daysInMonth * 100) : 80;
    const paceStr = quotaPct ? quotaPct + "% of pace" : "";
    const paceColor = quotaPct && parseInt(quotaPct) >= 100 ? "#3a7a4e" : "#a87935";
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "td-greeting" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "td-greeting-date" }, "\u25CF", " ", shortDay, " ", "\xB7", " ", daysLeft, " day", daysLeft !== 1 ? "s" : "", " left in the month"), /* @__PURE__ */ React.createElement("h1", null, greeting, ", ", /* @__PURE__ */ React.createElement("em", null, firstName)), /* @__PURE__ */ React.createElement("div", { className: "td-greeting-sub" }, actionItems.length > 0 ? actionItems.length + " thing" + (actionItems.length !== 1 ? "s" : "") + " need" + (actionItems.length === 1 ? "s" : "") + " you today. " : "All clear today. ", pipelineVal > 0 && /* @__PURE__ */ React.createElement("em", null, "$" + (pipelineVal / 1e3).toFixed(0) + "k of quoted pipeline"), pipelineVal > 0 && openQuotes > 0 && " across " + openQuotes + " quotes.")), /* @__PURE__ */ React.createElement("div", { className: "td-greeting-actions" }, /* @__PURE__ */ React.createElement("button", { className: "td-btn", onClick: () => navigate("quote-create") }, "+ New quote"), /* @__PURE__ */ React.createElement("button", { className: "td-btn primary", onClick: () => navigate("order-create") }, "+ New order"))), triageCards.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "td-queue" }, /* @__PURE__ */ React.createElement("div", { className: "td-queue-header" }, /* @__PURE__ */ React.createElement("h2", null, "Needs you ", "\xB7", " ", /* @__PURE__ */ React.createElement("em", null, triageCards.length)), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("notifications") }, "Inbox ", "\u2192")), /* @__PURE__ */ React.createElement("div", { className: "td-queue-grid" }, triageCards.map((it, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "td-queue-card", style: { borderLeft: "3px solid " + it.tone }, onClick: it.onClick }, /* @__PURE__ */ React.createElement("div", { className: "td-queue-card-top" }, /* @__PURE__ */ React.createElement("span", { className: "td-queue-tag", style: { background: it.tone + "1a", color: it.tone } }, it.tag), /* @__PURE__ */ React.createElement("span", { className: "td-queue-ref" }, it.ref)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "td-queue-who" }, it.who), /* @__PURE__ */ React.createElement("div", { className: "td-queue-what" }, it.what)), /* @__PURE__ */ React.createElement("span", { className: "td-queue-cta" }, it.cta, " ", "\u2192"))))), /* @__PURE__ */ React.createElement("div", { className: "td-grid" }, /* @__PURE__ */ React.createElement("div", { className: "td-panel" }, /* @__PURE__ */ React.createElement("div", { className: "td-panel-header" }, /* @__PURE__ */ React.createElement("h3", null, dashTaskTitle), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("tasks") }, "Tasks ", "\u2192")), todayTasks.length > 0 ? todayTasks.map((e) => /* @__PURE__ */ React.createElement("div", { key: e.task.id, className: "td-cal-row", style: { cursor: "pointer" }, onClick: () => openDashTask(e.task) }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement(
      "button",
      {
        title: "Mark complete",
        onClick: (ev) => {
          ev.stopPropagation();
          completeDashTask(e.task.id);
        },
        style: {
          width: 18,
          height: 18,
          flexShrink: 0,
          borderRadius: "50%",
          border: "1.5px solid " + e.tone,
          background: "transparent",
          cursor: "pointer",
          padding: 0,
          display: "inline-flex",
          alignItems: "center",
          justifyContent: "center",
          color: e.tone
        },
        onMouseEnter: (ev) => {
          ev.currentTarget.style.background = e.tone + "22";
        },
        onMouseLeave: (ev) => {
          ev.currentTarget.style.background = "transparent";
        }
      },
      /* @__PURE__ */ React.createElement("svg", { width: "10", height: "10", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "3" }, /* @__PURE__ */ React.createElement("polyline", { points: "20 6 9 17 4 12" }))
    ), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.08em", textTransform: "uppercase", color: e.tone } }, e.time)), /* @__PURE__ */ React.createElement("div", { className: "td-cal-bar", style: { background: e.tone } }), /* @__PURE__ */ React.createElement("div", { className: "td-cal-label" }, e.label), /* @__PURE__ */ React.createElement("span", { className: "td-cal-action" }, "Open ", "\u2192"))) : /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center", color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif" } }, "Nothing on deck. Add a follow-up from any order, quote, or customer, or check your ", /* @__PURE__ */ React.createElement("a", { style: { color: "var(--gold)", cursor: "pointer" }, onClick: () => navigate("tasks") }, "task list"), "."), dueNowTasks.length + upcomingTasks.length > todayTasks.length && /* @__PURE__ */ React.createElement("div", { className: "td-cal-row", style: { cursor: "pointer", justifyContent: "center" }, onClick: () => navigate("tasks") }, /* @__PURE__ */ React.createElement("div", { className: "td-cal-label", style: { gridColumn: "1 / -1", textAlign: "center", color: "var(--gold)", font: "500 12px/1 Inter, sans-serif" } }, "+ " + (dueNowTasks.length + upcomingTasks.length - todayTasks.length) + " more", " ", "\u2192"))), /* @__PURE__ */ React.createElement("div", { className: "td-panel" }, /* @__PURE__ */ React.createElement("div", { className: "td-panel-header" }, /* @__PURE__ */ React.createElement("h3", null, "Your month ", "\xB7", " ", now.getDate(), " of ", daysInMonth, " days in")), /* @__PURE__ */ React.createElement("div", { className: "td-numbers" }, /* @__PURE__ */ React.createElement("div", { className: "td-numbers-label" }, "Booked"), /* @__PURE__ */ React.createElement("div", { className: "td-numbers-row" }, /* @__PURE__ */ React.createElement("span", { className: "td-numbers-big" }, "$", mtdSales >= 1e3 ? (mtdSales / 1e3).toFixed(0) + "K" : mtdSales.toLocaleString(void 0, { maximumFractionDigits: 0 })), quotaPct && /* @__PURE__ */ React.createElement("span", { className: "td-numbers-pace", style: { color: paceColor } }, "\u25CF", " ", quotaPct, "% of pace")), /* @__PURE__ */ React.createElement("div", { className: "td-progress" }, /* @__PURE__ */ React.createElement("div", { className: "td-progress-fill", style: { width: progressPct + "%" } }), /* @__PURE__ */ React.createElement("div", { className: "td-progress-marker", style: { left: dayProgress + "%" } })), /* @__PURE__ */ React.createElement("div", { className: "td-progress-labels" }, /* @__PURE__ */ React.createElement("span", null, "$0"), quota > 0 && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--stone-900)" } }, "Quota $", quota >= 1e3 ? (quota / 1e3).toFixed(0) + "K" : quota), /* @__PURE__ */ React.createElement("span", null, "$", quota > 0 ? quota * 1.25 >= 1e3 ? (quota * 1.25 / 1e3).toFixed(0) + "K" : quota * 1.25 : ""))), /* @__PURE__ */ React.createElement("div", { className: "td-mini-grid" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "td-mini-label" }, "Quoted pipeline"), /* @__PURE__ */ React.createElement("div", { className: "td-mini-value" }, "$", pipelineVal >= 1e3 ? (pipelineVal / 1e3).toFixed(0) + "K" : pipelineVal.toLocaleString(void 0, { maximumFractionDigits: 0 })), /* @__PURE__ */ React.createElement("div", { className: "td-mini-sub" }, openQuotes, " quote", openQuotes !== 1 ? "s" : "", " open")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "td-mini-label" }, "In fulfillment"), /* @__PURE__ */ React.createElement("div", { className: "td-mini-value" }, stats.my_orders || 0), /* @__PURE__ */ React.createElement("div", { className: "td-mini-sub" }, stats.pending_orders || 0, " pending ", "\xB7", " ", stats.shipped_orders || 0, " shipped")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "td-mini-label" }, "Commission MTD"), /* @__PURE__ */ React.createElement("div", { className: "td-mini-value" }, "$", commEarned >= 1e3 ? (commEarned / 1e3).toFixed(1) + "K" : commEarned.toLocaleString(void 0, { maximumFractionDigits: 0 })), /* @__PURE__ */ React.createElement("div", { className: "td-mini-sub" }, "Earned + pending")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "td-mini-label" }, "Margin"), /* @__PURE__ */ React.createElement("div", { className: "td-mini-value", style: { color: "#3a7a4e" } }, avgMargin ? avgMargin.toFixed(1) + "%" : "\u2014"), /* @__PURE__ */ React.createElement("div", { className: "td-mini-sub" }, avgMargin ? "Avg across orders" : "No data yet"))))), momentumItems.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "td-panel" }, /* @__PURE__ */ React.createElement("div", { className: "td-panel-header" }, /* @__PURE__ */ React.createElement("h3", null, "Orders in motion ", "\xB7", " ", momentumItems.length, " recent"), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--brass-muted)" } }, "\u25C7", " Most recent first")), momentumItems.map((it, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "td-momentum-row", onClick: it.onClick }, /* @__PURE__ */ React.createElement("span", { className: "td-momentum-who" }, it.who), /* @__PURE__ */ React.createElement("span", { className: "td-momentum-last" }, "Last ", "\xB7", " ", it.last), /* @__PURE__ */ React.createElement("span", { className: "td-momentum-signal", style: { color: it.tone } }, "\u25CF", " ", it.signal), /* @__PURE__ */ React.createElement("span", { className: "td-momentum-cta" }, it.action, " ", "\u2192")))));
  }
  function RepOrdersListView({ navigate }) {
    const [orders, setOrders] = useState([]);
    const [loading, setLoading] = useState(true);
    const [search, setSearch] = useState("");
    const [statusFilter, setStatusFilter] = useState("");
    const [activeView, setActiveView] = useState("all");
    const [scope, setScope] = useState("team");
    const [selected, setSelected] = useState(/* @__PURE__ */ new Set());
    const [page, setPage] = useState(1);
    const perPage = 50;
    const searchTimer = useRef(null);
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const load = useCallback(() => {
      setLoading(true);
      const params = new URLSearchParams();
      if (search) params.set("search", search);
      if (statusFilter) params.set("status", statusFilter);
      if (scope === "mine") params.set("mine", "true");
      repFetch("/api/rep/orders?" + params.toString()).then((data) => {
        setOrders(data.orders || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [search, statusFilter, scope]);
    useEffect(load, [load]);
    const viewFilters = {
      "needs-me": (o) => ["pending"].includes(o.status) || o.amount_paid && parseFloat(o.amount_paid) === 0 && o.status !== "cancelled",
      "fulfillment": (o) => ["confirmed", "ready_for_pickup", "shipped"].includes(o.status),
      "awaiting-pay": (o) => o.status !== "cancelled" && o.status !== "delivered" && (!o.amount_paid || parseFloat(o.amount_paid) < parseFloat(o.total)),
      "delivered": (o) => o.status === "delivered",
      "all": () => true
    };
    const filteredOrders = useMemo(() => {
      const fn = viewFilters[activeView] || (() => true);
      return orders.filter(fn);
    }, [orders, activeView]);
    const viewCounts = useMemo(() => ({
      "needs-me": orders.filter(viewFilters["needs-me"]).length,
      "fulfillment": orders.filter(viewFilters["fulfillment"]).length,
      "awaiting-pay": orders.filter(viewFilters["awaiting-pay"]).length,
      "delivered": orders.filter(viewFilters["delivered"]).length,
      "all": orders.length
    }), [orders]);
    const totalPages = Math.ceil(filteredOrders.length / perPage);
    const pageOrders = filteredOrders.slice((page - 1) * perPage, page * perPage);
    const toggleSelect = (id) => {
      setSelected((prev) => {
        const next = new Set(prev);
        next.has(id) ? next.delete(id) : next.add(id);
        return next;
      });
    };
    const toggleAll = () => {
      if (selected.size === pageOrders.length) setSelected(/* @__PURE__ */ new Set());
      else setSelected(new Set(pageOrders.map((o) => o.id)));
    };
    const getPayStatus = (o) => {
      const paid = parseFloat(o.amount_paid || 0);
      const total = parseFloat(o.total || 0);
      if (paid >= total && total > 0) return "paid";
      if (paid > 0) return "partial";
      return "unpaid";
    };
    const getStatusColor = (s) => {
      if (s === "pending") return "#a87935";
      if (s === "confirmed" || s === "shipped") return "#3a7a4e";
      if (s === "delivered") return "var(--brass-muted)";
      if (s === "cancelled") return "#c54731";
      return "var(--brass-muted)";
    };
    const getAge = (d) => {
      const days = Math.floor((Date.now() - new Date(d).getTime()) / 864e5);
      if (days === 0) return "today";
      if (days < 1) return Math.floor((Date.now() - new Date(d).getTime()) / 36e5) + "h";
      return days + "d";
    };
    const VIEWS = [
      { key: "all", label: "All" },
      { key: "needs-me", label: "Needs me", tone: "#c54731" },
      { key: "fulfillment", label: "In fulfillment" },
      { key: "awaiting-pay", label: "Awaiting payment" },
      { key: "delivered", label: "Delivered" }
    ];
    const STATUSES = [
      { value: "", label: "Any" },
      { value: "pending", label: "Pending" },
      { value: "confirmed", label: "Confirmed" },
      { value: "shipped", label: "Shipped" },
      { value: "delivered", label: "Delivered" },
      { value: "cancelled", label: "Cancelled" }
    ];
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25CF", " Orders ", "\xB7", " ", orders.filter((o) => o.status !== "delivered" && o.status !== "cancelled").length, " active ", "\xB7", " ", orders.length, " total"), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Orders"))), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn" }, "Export"), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: () => navigate("order-create") }, "+ New order"))), /* @__PURE__ */ React.createElement("div", { className: "ol-saved-views" }, VIEWS.map((v) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: v.key,
        className: "ol-view-tab" + (activeView === v.key ? " active" : ""),
        onClick: () => {
          setActiveView(v.key);
          setPage(1);
          setSelected(/* @__PURE__ */ new Set());
        }
      },
      v.label,
      /* @__PURE__ */ React.createElement("span", { className: "ol-tab-count", style: v.tone && viewCounts[v.key] > 0 ? { background: v.tone + "1a", color: v.tone } : void 0 }, viewCounts[v.key])
    )), /* @__PURE__ */ React.createElement(ScopeToggle, { scope, setScope, onChange: () => {
      setPage(1);
      setSelected(/* @__PURE__ */ new Set());
    } })), /* @__PURE__ */ React.createElement("div", { className: "ol-filter-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-filter-chips" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        className: "ol-chip",
        value: statusFilter,
        onChange: (e) => {
          setStatusFilter(e.target.value);
          setPage(1);
        },
        style: { appearance: "auto", paddingRight: 24 }
      },
      STATUSES.map((s) => /* @__PURE__ */ React.createElement("option", { key: s.value, value: s.value }, s.value ? s.label : "Status: Any"))
    ), statusFilter && /* @__PURE__ */ React.createElement("span", { className: "ol-chip active", onClick: () => setStatusFilter("") }, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-label" }, "Status:"), " ", statusFilter, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-remove" }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search orders, customers",
        "aria-label": "Search orders",
        value: search,
        onChange: (e) => {
          setSearch(e.target.value);
          setPage(1);
        }
      }
    )), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, filteredOrders.length, " rows"))), selected.size > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-label" }, "\u25CF", " ", selected.size, " order", selected.size !== 1 ? "s" : "", " selected"), /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn" }, "Email customers"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn" }, "Export CSV"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: () => setSelected(/* @__PURE__ */ new Set()) }, "\u2715", " Clear"))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 22px", background: "#ece5d8", border: "0.5px solid rgba(28,25,23,0.14)" } }, "Loading orders", "\u2026") : /* @__PURE__ */ React.createElement("div", { className: "ol-table-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "ol-table-head" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "checkbox",
        style: { margin: 0 },
        "aria-label": "Select all orders on page",
        checked: pageOrders.length > 0 && selected.size === pageOrders.length,
        onChange: toggleAll
      }
    ), /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Order ID"), /* @__PURE__ */ React.createElement("span", null, "Customer"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Total"), /* @__PURE__ */ React.createElement("span", null, "Pay"), /* @__PURE__ */ React.createElement("span", null, "State ", "\xB7", " step"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Age"), /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null)), pageOrders.map((o) => {
      const pay = getPayStatus(o);
      const isSel = selected.has(o.id);
      const statusLabel = o.status === "ready_for_pickup" ? "Ready for pickup" : o.delivery_method === "pickup" && o.status === "shipped" ? "Ready for pickup" : o.delivery_method === "pickup" && o.status === "delivered" ? "Picked up" : o.status === "pending" ? "Pending" : o.status === "confirmed" ? "Confirmed" : o.status === "shipped" ? "In transit" : o.status === "delivered" ? "Delivered" : o.status === "cancelled" ? "Cancelled" : o.status;
      const stepText = o.status === "pending" && pay === "unpaid" ? "Awaiting payment" : o.status === "pending" ? "Awaiting confirmation" : o.status === "ready_for_pickup" ? "Customer notified" : o.status === "confirmed" ? o.delivery_method === "pickup" ? "Awaiting stock" : "Processing" : o.status === "shipped" ? o.delivery_method === "pickup" ? "Customer notified" : o.shipping_method === "ltl_freight" ? "LTL freight" : "Parcel shipping" : o.status === "delivered" ? "Closed" : o.status === "cancelled" ? "Cancelled" : o.delivery_method || "";
      const flagColor = o.status === "pending" && pay === "unpaid" ? "#c54731" : o.status === "pending" ? "#a87935" : "transparent";
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: o.id,
          className: "ol-table-row" + (isSel ? " selected" : ""),
          role: "button",
          tabIndex: 0,
          onClick: () => {
            repListNav.orders = filteredOrders.map((x) => x.id);
            navigate("order-detail", o.id);
          },
          onKeyDown: (e) => {
            if (e.key === "Enter" || e.key === " ") {
              e.preventDefault();
              repListNav.orders = filteredOrders.map((x) => x.id);
              navigate("order-detail", o.id);
            }
          }
        },
        /* @__PURE__ */ React.createElement(
          "input",
          {
            type: "checkbox",
            style: { margin: 0 },
            checked: isSel,
            "aria-label": "Select order " + o.order_number,
            onChange: () => toggleSelect(o.id),
            onClick: (e) => e.stopPropagation(),
            onKeyDown: (e) => e.stopPropagation()
          }
        ),
        /* @__PURE__ */ React.createElement("span", { className: "ol-flag", style: { background: flagColor } }),
        /* @__PURE__ */ React.createElement("span", { className: "ol-order-id" }, o.order_number),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-cust-name" }, o.customer_name), o.trade_customer_id && /* @__PURE__ */ React.createElement("div", { className: "ol-cust-tag" }, "Trade")),
        /* @__PURE__ */ React.createElement("span", { className: "ol-total" }, "$", parseFloat(o.total).toFixed(2)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-pay-badge " + pay }, "\u25CF", " ", pay),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-state-label", style: { color: getStatusColor(o.status) } }, "\u25CF", " ", statusLabel), stepText && /* @__PURE__ */ React.createElement("div", { className: "ol-state-step" }, stepText)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-age" }, getAge(o.created_at)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-install" }, new Date(o.created_at).toLocaleDateString("en-US", { month: "short", day: "numeric" })),
        /* @__PURE__ */ React.createElement("span", { className: "ol-arrow" }, "\u2192")
      );
    }), pageOrders.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center", color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif" } }, "No orders match this view."), filteredOrders.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-footer" }, /* @__PURE__ */ React.createElement("span", null, "Showing ", Math.min((page - 1) * perPage + 1, filteredOrders.length), "\u2013", Math.min(page * perPage, filteredOrders.length), " of ", filteredOrders.length, " ", "\xB7", " ", perPage, " per page"), /* @__PURE__ */ React.createElement("span", null, "$", filteredOrders.reduce((s, o) => s + parseFloat(o.total || 0), 0).toLocaleString(void 0, { minimumFractionDigits: 0, maximumFractionDigits: 0 }), " total value")), totalPages > 1 && /* @__PURE__ */ React.createElement("div", { className: "ol-pagination" }, /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, "Page ", page, " of ", totalPages), /* @__PURE__ */ React.createElement("div", { className: "ol-page-btns" }, /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page <= 1, onClick: () => setPage((p) => p - 1) }, "\u2190"), Array.from({ length: Math.min(totalPages, 5) }, (_, i) => i + 1).map((p) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: p,
        className: "ol-page-btn" + (page === p ? " active" : ""),
        onClick: () => setPage(p)
      },
      p
    )), totalPages > 5 && /* @__PURE__ */ React.createElement("span", { className: "ol-meta", style: { padding: "4px 6px" } }, "\xB7\xB7\xB7"), /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page >= totalPages, onClick: () => setPage((p) => p + 1) }, "\u2192")))));
  }
  function PODocumentViewer({ url, po, onClose, onSend, confirmBeforeSend = true }) {
    const iframeRef = React.useRef(null);
    const [sending, setSending] = React.useState(false);
    useEffect(() => {
      return () => {
        if (url && url.startsWith("blob:")) URL.revokeObjectURL(url);
      };
    }, [url]);
    useEffect(() => {
      const handler = (e) => {
        if (e.key === "Escape") onClose();
      };
      document.addEventListener("keydown", handler);
      return () => document.removeEventListener("keydown", handler);
    }, [onClose]);
    const isDraft = po && po.status === "draft";
    const filename = po ? po.po_number + ".pdf" : "document.pdf";
    const handleDownload = () => {
      repOpenDoc("/api/rep/purchase-orders/" + po.id + "/pdf", true).catch(() => alert("Failed to download PDF"));
    };
    const handlePrint = () => {
      try {
        iframeRef.current.contentWindow.print();
      } catch (e) {
        window.open(url, "_blank");
      }
    };
    const handleSend = async () => {
      if (confirmBeforeSend && !confirm("Approve & send this PO to the vendor?")) return;
      setSending(true);
      try {
        await onSend();
      } catch (e) {
        alert(e.message);
      }
      setSending(false);
    };
    return /* @__PURE__ */ React.createElement("div", { style: {
      position: "fixed",
      top: 0,
      left: 0,
      right: 0,
      bottom: 0,
      zIndex: 1e4,
      display: "grid",
      gridTemplateRows: "auto 1fr",
      background: "#3a3835"
    } }, /* @__PURE__ */ React.createElement("div", { style: {
      background: "#22201e",
      color: "#fff",
      padding: "12px 20px",
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      borderBottom: "1px solid #000"
    } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 14 } }, /* @__PURE__ */ React.createElement("span", { style: {
      font: "400 14px/1 'Inter', system-ui, sans-serif",
      color: "#fff",
      display: "flex",
      alignItems: "center",
      gap: 8
    } }, /* @__PURE__ */ React.createElement("span", { style: {
      width: 22,
      height: 26,
      border: "0.5px solid #c7c2b8",
      background: "#fff",
      color: "#22201e",
      font: "500 9px/26px 'Inter', system-ui, sans-serif",
      textAlign: "center",
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center"
    } }, "\u{1F4C4}"), filename), /* @__PURE__ */ React.createElement("span", { style: { color: "#aaa6a0", font: "400 12px/1 'Inter', system-ui, sans-serif" } }, "\xB7", " 1 / 1 page")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("button", { onClick: handleDownload, title: "Download", style: {
      padding: "8px 12px",
      background: "#3a7a4e",
      color: "#fff",
      border: "none",
      cursor: "pointer",
      font: "500 12px/1 'Inter', system-ui, sans-serif",
      letterSpacing: "0.04em"
    } }, "\u2913", " Download"), isDraft && onSend && /* @__PURE__ */ React.createElement("button", { onClick: handleSend, title: "Send to vendor", disabled: sending, style: {
      padding: "8px 12px",
      background: "#a87935",
      color: "#fff",
      border: "none",
      cursor: "pointer",
      font: "500 12px/1 'Inter', system-ui, sans-serif",
      letterSpacing: "0.04em",
      opacity: sending ? 0.6 : 1
    } }, sending ? "Sending\u2026" : "\u2709 Send"), /* @__PURE__ */ React.createElement("button", { onClick: handlePrint, title: "Print", style: {
      padding: "8px 12px",
      background: "transparent",
      color: "#fff",
      border: "0.5px solid #4a4744",
      cursor: "pointer",
      font: "500 12px/1 'Inter', system-ui, sans-serif",
      letterSpacing: "0.04em"
    } }, "\u2399", " Print"), /* @__PURE__ */ React.createElement("button", { onClick: onClose, title: "Close", style: {
      padding: "8px 12px",
      background: "transparent",
      color: "#fff",
      border: "0.5px solid #4a4744",
      cursor: "pointer",
      font: "500 12px/1 'Inter', system-ui, sans-serif",
      letterSpacing: "0.04em"
    } }, "\u2715"))), /* @__PURE__ */ React.createElement("div", { style: {
      display: "flex",
      justifyContent: "center",
      alignItems: "flex-start",
      padding: "28px 0",
      overflow: "auto"
    } }, /* @__PURE__ */ React.createElement("div", { style: {
      width: 816,
      height: 1056,
      flexShrink: 0,
      boxShadow: "0 24px 60px rgba(0,0,0,0.5)"
    } }, /* @__PURE__ */ React.createElement("iframe", { ref: iframeRef, src: url, sandbox: "allow-same-origin allow-modals", style: { width: "100%", height: "100%", border: "none" } }))));
  }
  function RepPdfViewer({ url, filename, downloadPath, onClose }) {
    const iframeRef = React.useRef(null);
    useEffect(() => {
      return () => {
        if (url && url.startsWith("blob:")) URL.revokeObjectURL(url);
      };
    }, [url]);
    useEffect(() => {
      const handler = (e) => {
        if (e.key === "Escape") onClose();
      };
      document.addEventListener("keydown", handler);
      return () => document.removeEventListener("keydown", handler);
    }, [onClose]);
    const handleDownload = () => {
      if (downloadPath) {
        repOpenDoc(downloadPath, true).catch(() => alert("Failed to download PDF"));
      } else if (url) {
        window.open(url, "_blank");
      }
    };
    const handlePrint = () => {
      try {
        iframeRef.current.contentWindow.print();
      } catch (e) {
        window.open(url, "_blank");
      }
    };
    return /* @__PURE__ */ React.createElement("div", { style: {
      position: "fixed",
      top: 0,
      left: 0,
      right: 0,
      bottom: 0,
      zIndex: 1e4,
      display: "grid",
      gridTemplateRows: "auto 1fr",
      background: "#3a3835"
    } }, /* @__PURE__ */ React.createElement("div", { style: {
      background: "#22201e",
      color: "#fff",
      padding: "12px 20px",
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      borderBottom: "1px solid #000"
    } }, /* @__PURE__ */ React.createElement("span", { style: {
      font: "400 14px/1 'Inter', system-ui, sans-serif",
      color: "#fff",
      display: "flex",
      alignItems: "center",
      gap: 8
    } }, /* @__PURE__ */ React.createElement("span", { style: {
      width: 22,
      height: 26,
      border: "0.5px solid #c7c2b8",
      background: "#fff",
      color: "#22201e",
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      font: "500 9px/26px 'Inter', system-ui, sans-serif"
    } }, "\u{1F4C4}"), filename), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 8 } }, /* @__PURE__ */ React.createElement("button", { onClick: handleDownload, title: "Download", style: {
      padding: "8px 12px",
      background: "#a87935",
      color: "#fff",
      border: "none",
      cursor: "pointer",
      font: "500 12px/1 'Inter', system-ui, sans-serif",
      letterSpacing: "0.04em"
    } }, "\u2913", " Download"), /* @__PURE__ */ React.createElement("button", { onClick: handlePrint, title: "Print", style: {
      padding: "8px 12px",
      background: "transparent",
      color: "#fff",
      border: "0.5px solid #4a4744",
      cursor: "pointer",
      font: "500 12px/1 'Inter', system-ui, sans-serif",
      letterSpacing: "0.04em"
    } }, "\u2399", " Print"), /* @__PURE__ */ React.createElement("button", { onClick: onClose, title: "Close", style: {
      padding: "8px 12px",
      background: "transparent",
      color: "#fff",
      border: "0.5px solid #4a4744",
      cursor: "pointer",
      font: "500 12px/1 'Inter', system-ui, sans-serif",
      letterSpacing: "0.04em"
    } }, "\u2715"))), /* @__PURE__ */ React.createElement("div", { style: {
      display: "flex",
      justifyContent: "center",
      alignItems: "flex-start",
      padding: "28px 0",
      overflow: "auto"
    } }, /* @__PURE__ */ React.createElement("div", { style: {
      width: 816,
      height: 1056,
      flexShrink: 0,
      boxShadow: "0 24px 60px rgba(0,0,0,0.5)"
    } }, /* @__PURE__ */ React.createElement("iframe", { ref: iframeRef, src: url, sandbox: "allow-same-origin allow-modals", style: { width: "100%", height: "100%", border: "none" } }))));
  }
  function RodCard({ title, action, action2, onAction, onAction2, children, bg }) {
    return /* @__PURE__ */ React.createElement("section", { className: "rod-card", style: bg ? { background: bg } : void 0 }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-header" }, /* @__PURE__ */ React.createElement("span", { className: "rod-card-title" }, title), /* @__PURE__ */ React.createElement("div", { className: "rod-card-actions" }, action2 && /* @__PURE__ */ React.createElement("button", { className: "rod-card-action", style: { color: "var(--rod-muted)" }, onClick: onAction2 }, action2), action && /* @__PURE__ */ React.createElement("button", { className: "rod-card-action", onClick: onAction }, action))), /* @__PURE__ */ React.createElement("div", { className: "rod-card-body" }, children));
  }
  function InternalNotesCard({ basePath, readOnly, bg, staged, onStaged }) {
    const isStaged = !basePath && !!onStaged;
    const [notes, setNotes] = useState([]);
    const [showAdd, setShowAdd] = useState(false);
    const [text, setText] = useState("");
    const [saving, setSaving] = useState(false);
    const [editingId, setEditingId] = useState(null);
    const [editText, setEditText] = useState("");
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const currentRepId = repInfo.id;
    const currentRepName = [repInfo.first_name, repInfo.last_name].filter(Boolean).join(" ") || "You";
    const entries = isStaged ? staged || [] : notes;
    const load = useCallback(() => {
      if (isStaged || !basePath) return;
      repFetch(basePath + "/notes").then((d) => setNotes(d.notes || [])).catch(() => {
      });
    }, [basePath, isStaged]);
    useEffect(load, [load]);
    const add = async () => {
      const t = text.trim();
      if (!t) return;
      if (isStaged) {
        const entry = {
          id: "staged-" + Date.now() + "-" + Math.round(Math.random() * 1e6),
          note: t,
          staff_id: currentRepId,
          staff_name: currentRepName,
          created_at: (/* @__PURE__ */ new Date()).toISOString()
        };
        onStaged([entry, ...staged || []]);
        setShowAdd(false);
        setText("");
        return;
      }
      setSaving(true);
      try {
        const data = await repFetch(basePath + "/notes", { method: "POST", body: JSON.stringify({ note: t }) });
        if (data.note) setNotes((prev) => [data.note, ...prev]);
        setShowAdd(false);
        setText("");
      } catch (err) {
      }
      setSaving(false);
    };
    const saveEdit = async (n) => {
      const t = editText.trim();
      if (!t) return;
      if (isStaged) {
        onStaged((staged || []).map((x) => x.id === n.id ? { ...x, note: t } : x));
        setEditingId(null);
        setEditText("");
        return;
      }
      try {
        const data = await repFetch(basePath + "/notes/" + n.id, { method: "PUT", body: JSON.stringify({ note: t }) });
        if (data.note) setNotes((prev) => prev.map((x) => x.id === n.id ? data.note : x));
        setEditingId(null);
        setEditText("");
      } catch (err) {
        alert(err.message || "Failed to save note");
      }
    };
    const del = async (n) => {
      if (!confirm("Delete this note? This cannot be undone.")) return;
      if (isStaged) {
        onStaged((staged || []).filter((x) => x.id !== n.id));
        return;
      }
      try {
        await repFetch(basePath + "/notes/" + n.id, { method: "DELETE" });
        setNotes((prev) => prev.filter((x) => x.id !== n.id));
      } catch (err) {
        alert(err.message || "Failed to delete note");
      }
    };
    return /* @__PURE__ */ React.createElement(
      RodCard,
      {
        title: "Internal notes" + (entries.length ? " \xB7 " + entries.length : ""),
        action: readOnly ? void 0 : "+ Add",
        onAction: () => setShowAdd(true),
        bg
      },
      /* @__PURE__ */ React.createElement("div", { style: { padding: "8px 0" } }, entries.length === 0 && !showAdd && /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 18px", fontSize: "0.8125rem", color: "var(--rod-muted)" } }, "No notes yet"), entries.map((n, i) => /* @__PURE__ */ React.createElement("div", { className: "rod-note-entry", key: n.id || i, style: i === entries.length - 1 && !showAdd ? { borderBottom: "none" } : {} }, /* @__PURE__ */ React.createElement("div", { className: "rod-note-avatar" }, (n.staff_name || "?").split(" ").map((w) => w[0]).join("").slice(0, 2).toUpperCase()), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0 } }, editingId === n.id ? /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
        "textarea",
        {
          value: editText,
          onChange: (e) => setEditText(e.target.value),
          autoFocus: true,
          style: { width: "100%", minHeight: 46, padding: 8, background: "transparent", border: "0.5px solid rgba(28,25,23,0.2)", outline: "none", resize: "vertical", boxSizing: "border-box", font: "400 13px/1.4 Inter, sans-serif", color: "var(--rod-ink)" }
        }
      ), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, marginTop: 6, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => {
            setEditingId(null);
            setEditText("");
          },
          style: { padding: "4px 9px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", cursor: "pointer" }
        },
        "Cancel"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          disabled: !editText.trim(),
          onClick: () => saveEdit(n),
          style: { padding: "4px 9px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "none", background: "#1c1917", color: "#ece5d8", cursor: "pointer" }
        },
        "Save"
      ))) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rod-note-text" }, n.note), /* @__PURE__ */ React.createElement("div", { className: "rod-note-ts" }, n.created_at ? new Date(n.created_at).toLocaleDateString("en-US", { month: "short", day: "numeric" }) + " \xB7 " + new Date(n.created_at).toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }) : "", n.staff_name ? " \xB7 " + n.staff_name : "", n.id && !readOnly && n.staff_id === currentRepId && /* @__PURE__ */ React.createElement("span", { style: { marginLeft: 8 } }, /* @__PURE__ */ React.createElement(
        "a",
        {
          onClick: () => {
            setEditingId(n.id);
            setEditText(n.note);
          },
          style: { cursor: "pointer", textDecoration: "underline", color: "var(--rod-muted)" }
        },
        "Edit"
      ), /* @__PURE__ */ React.createElement("span", { style: { margin: "0 5px", color: "var(--rod-muted)" } }, "\xB7"), /* @__PURE__ */ React.createElement(
        "a",
        {
          onClick: () => del(n),
          style: { cursor: "pointer", textDecoration: "underline", color: "var(--rod-red)" }
        },
        "Delete"
      ))))))), showAdd && /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 18px", borderTop: entries.length ? "0.5px solid rgba(28,25,23,0.12)" : "none" } }, /* @__PURE__ */ React.createElement(
        "textarea",
        {
          value: text,
          onChange: (e) => setText(e.target.value),
          placeholder: "Add a note \u2014 visible only to Roma staff\u2026",
          autoFocus: true,
          style: { width: "100%", minHeight: 50, padding: 10, background: "transparent", border: "0.5px solid rgba(28,25,23,0.2)", outline: "none", resize: "vertical", boxSizing: "border-box", font: "400 13px/1.4 Inter, sans-serif", color: "var(--rod-ink)" }
        }
      ), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "6px", marginTop: "6px", justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => {
            setShowAdd(false);
            setText("");
          },
          style: { padding: "5px 10px", fontSize: "10px", fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", cursor: "pointer" }
        },
        "Cancel"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          disabled: !text.trim() || saving,
          onClick: add,
          style: { padding: "5px 10px", fontSize: "10px", fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "none", background: "#1c1917", color: "#ece5d8", cursor: "pointer" }
        },
        saving ? "Saving..." : "Save"
      ))))
    );
  }
  function rovOrderDocLabel(doc) {
    return doc.doc_type === "customer_id" ? "Customer ID" : doc.doc_type === "check_photo" ? "Check photo" : doc.doc_type === "vendor_confirmation" ? "Order confirmation" + (doc.po_number ? " \xB7 " + doc.po_number : "") : doc.file_name || doc.doc_type || "Document";
  }
  function RovChip({ tone, children }) {
    return /* @__PURE__ */ React.createElement("span", { className: "rov-chip " + (tone || "soft") }, "\u25CF ", children);
  }
  function RovMicro({ children, style, color }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rov-micro", style: { ...color ? { color } : {}, ...style } }, children);
  }
  function RovRow({ l, v, c, last }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rov-row", style: last ? { borderBottom: "none" } : {} }, /* @__PURE__ */ React.createElement("span", { className: "rov-row-label" }, l), /* @__PURE__ */ React.createElement("span", { className: "rov-row-value", style: c ? { color: c } : {} }, v));
  }
  function RepOrderDetailView({ navigate, editId, terminalRef, terminalStatus, connectReader, showCost }) {
    const [order, setOrder] = useState(null);
    const [savedMsg, setSavedMsg] = useState(null);
    const savedTimer = useRef(null);
    const flagSaved = (m) => {
      setSavedMsg(m || "Saved");
      if (savedTimer.current) clearTimeout(savedTimer.current);
      savedTimer.current = setTimeout(() => setSavedMsg(null), 4e3);
    };
    const [items, setItems] = useState([]);
    const [adjustments, setAdjustments] = useState([]);
    const [loading, setLoading] = useState(true);
    const [updating, setUpdating] = useState(false);
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const [showReassign, setShowReassign] = useState(false);
    const [reassignSaving, setReassignSaving] = useState(false);
    const [editingCostItem, setEditingCostItem] = useState(null);
    const [costDraft, setCostDraft] = useState("");
    const [purchaseOrders, setPurchaseOrders] = useState([]);
    const [posLoading, setPosLoading] = useState(false);
    const [posGenerating, setPosGenerating] = useState(false);
    const [editingPOItem, setEditingPOItem] = useState(null);
    const [newCost, setNewCost] = useState("");
    const [poUpdating, setPoUpdating] = useState(false);
    const [poEmailModal, setPoEmailModal] = useState(null);
    const [poRepEmail, setPoRepEmail] = useState("");
    const [showTrackingForm, setShowTrackingForm] = useState(false);
    const [trackingData, setTrackingData] = useState({ tracking_number: "", carrier: "", shipped_at: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10) });
    const [showCancelForm, setShowCancelForm] = useState(false);
    const [cancelReason, setCancelReason] = useState("");
    const [showDeliveryForm, setShowDeliveryForm] = useState(false);
    const [deliveryAddress, setDeliveryAddress] = useState({ line1: "", line2: "", city: "", state: "", zip: "" });
    const [deliveryWindow, setDeliveryWindow] = useState("");
    const [carrierPreference, setCarrierPreference] = useState("");
    const [editingJobName, setEditingJobName] = useState(false);
    const [jobNameDraft, setJobNameDraft] = useState("");
    const { addressInputRef, previousAddresses, showPrevAddresses, setShowPrevAddresses, selectPreviousAddress } = useAddressAutofill({
      fieldMap: { line1: "line1", line2: "line2", city: "city", state: "state", zip: "zip" },
      setForm: setDeliveryAddress,
      selectedCustomer: order && order.customer_email ? { email: order.customer_email } : null,
      isReadonly: false
    });
    const [shippingOptions, setShippingOptions] = useState(null);
    const [deliveryUpdating, setDeliveryUpdating] = useState(false);
    const [showFulfillmentMenu, setShowFulfillmentMenu] = useState(false);
    const [freightAmount, setFreightAmount] = useState("");
    const [editingFreight, setEditingFreight] = useState(false);
    const [payments, setPayments] = useState([]);
    const [paymentRequests, setPaymentRequests] = useState([]);
    const [balanceInfo, setBalanceInfo] = useState(null);
    const [showPaymentRequestModal, setShowPaymentRequestModal] = useState(false);
    const [paymentRequestMessage, setPaymentRequestMessage] = useState("");
    const [paymentRequestEmail, setPaymentRequestEmail] = useState("");
    const [orderActivity, setOrderActivity] = useState(null);
    const [poMenuOpen, setPoMenuOpen] = useState(null);
    const [openLines, setOpenLines] = useState({});
    const [receiveItem, setReceiveItem] = useState(null);
    const [receiveQty, setReceiveQty] = useState("");
    const [receiveSaving, setReceiveSaving] = useState(false);
    const [docPreview, setDocPreview] = useState(null);
    const [orderMilestones, setOrderMilestones] = useState([]);
    const [orderDocuments, setOrderDocuments] = useState([]);
    const [showPrintModal, setShowPrintModal] = useState(false);
    const [printSel, setPrintSel] = useState({});
    const [printBusy, setPrintBusy] = useState(null);
    const confirmFileRef = useRef(null);
    const confirmPoIdRef = useRef(null);
    const [confirmUploadingPoId, setConfirmUploadingPoId] = useState(null);
    const [showSendInvoiceModal, setShowSendInvoiceModal] = useState(false);
    const [invoiceMessage, setInvoiceMessage] = useState("");
    const [sendingInvoice, setSendingInvoice] = useState(false);
    const [showScheduleForm, setShowScheduleForm] = useState(false);
    const [scheduleForm, setScheduleForm] = useState({ date: "", window: "", crew: "", notes: "", notify: false });
    const [savingSchedule, setSavingSchedule] = useState(false);
    const [showCollectPayment, setShowCollectPayment] = useState(false);
    const [collectMethod, setCollectMethod] = useState(null);
    const [showCollectCardModal, setShowCollectCardModal] = useState(false);
    const [collectCheckNumber, setCollectCheckNumber] = useState("");
    const [collectCashDrawerOpen, setCollectCashDrawerOpen] = useState(null);
    const [collectAmount, setCollectAmount] = useState("");
    const [collectSaving, setCollectSaving] = useState(false);
    const [refundTarget, setRefundTarget] = useState(null);
    const [refundAmount, setRefundAmount] = useState("");
    const [refundReason, setRefundReason] = useState("");
    const [refundSaving, setRefundSaving] = useState(false);
    const [showRefundConfirm, setShowRefundConfirm] = useState(false);
    const [customerStats, setCustomerStats] = useState(null);
    const [showAddNote, setShowAddNote] = useState(false);
    const [commission, setCommission] = useState(null);
    const [orderInvoices, setOrderInvoices] = useState([]);
    const [releases, setReleases] = useState([]);
    const [releaseUpdating, setReleaseUpdating] = useState(null);
    const [orderNotes, setOrderNotes] = useState([]);
    const [orderNoteText, setOrderNoteText] = useState("");
    const [savingOrderNote, setSavingOrderNote] = useState(false);
    const [editingNoteId, setEditingNoteId] = useState(null);
    const [editNoteText, setEditNoteText] = useState("");
    const [noteForPoId, setNoteForPoId] = useState(null);
    const [poNoteText, setPoNoteText] = useState("");
    const [savingPoNote, setSavingPoNote] = useState(false);
    const [editingInvoiceNote, setEditingInvoiceNote] = useState(false);
    const [invoiceNoteText, setInvoiceNoteText] = useState("");
    const [savingInvoiceNote, setSavingInvoiceNote] = useState(false);
    const [editingPoNoteId, setEditingPoNoteId] = useState(null);
    const [showDiscountEdit, setShowDiscountEdit] = useState(false);
    const [discountType, setDiscountType] = useState("fixed");
    const [discountValue, setDiscountValue] = useState("");
    const [savingDiscount, setSavingDiscount] = useState(false);
    const orderActionLabel = (action) => {
      const labels = {
        order_created: "Order Created",
        status_changed: "Status Changed",
        delivery_method_changed: "Delivery Changed",
        discount_changed: "Discount Changed",
        refund_issued: "Refund Issued",
        item_added: "Item Added",
        item_removed: "Item Removed",
        payment_request_sent: "Payment Request",
        payment_request_cancelled: "Request Cancelled",
        payment_collected: "Payment Collected",
        payment_received: "Payment Received",
        payment_processing: "Payment Processing",
        payment_failed: "Payment Failed",
        rep_assigned: "Rep Assigned",
        price_adjusted: "Price Adjusted",
        cost_updated: "Cost Updated",
        item_ready: "Item Ready",
        item_unready: "Item Unmarked",
        invoice_sent: "Invoice Sent",
        invoice_notes_updated: "Invoice Note Updated",
        po_created: "PO Created",
        po_approved: "PO Sent",
        po_confirmed: "Vendor Confirmed",
        note_added: "Note Added",
        note_edited: "Note Edited",
        note_deleted: "Note Deleted",
        document_uploaded: "Document Added",
        document_deleted: "Document Removed",
        material_released: "Material Released",
        return_processed: "Return Processed",
        release_completed: "Release Completed",
        release_uncompleted: "Release Reopened",
        release_void: "Release Voided",
        install_scheduled: "Install Scheduled",
        order_cancelled: "Order Cancelled",
        job_name_updated: "Job / Sidemark Updated"
      };
      return labels[action] || action.replace(/_/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());
    };
    const orderActivityDetail = (entry) => {
      const d = entry.details || {};
      switch (entry.action) {
        case "order_created": {
          const parts = [];
          if (d.total) parts.push("$" + parseFloat(d.total).toFixed(2));
          if (d.item_count) parts.push(d.item_count + " item" + (d.item_count !== 1 ? "s" : ""));
          if (d.payment_method) parts.push("via " + d.payment_method);
          return parts.join(" \xB7 ") || "";
        }
        case "status_changed":
          return (d.from || "?") + " \u2192 " + (d.to || "?") + (d.tracking_number ? " (tracking: " + d.tracking_number + ")" : "");
        case "delivery_method_changed":
          return (d.from || "?") + " \u2192 " + (d.to || "?") + (d.shipping_cost ? " ($" + d.shipping_cost + ")" : "");
        case "refund_issued":
          return "$" + (d.amount || "0") + (d.is_full ? " (full)" : " (partial)") + (d.reason ? " \u2014 " + d.reason : "");
        case "item_added":
          return (d.product_name || "?") + (d.is_custom ? " (custom)" : "") + " \xD7 " + (d.num_boxes || "?") + " \u2014 $" + (d.subtotal || "0");
        case "item_removed":
          return (d.product_name || "?") + " \xD7 " + (d.num_boxes || "?") + " \u2014 $" + (d.subtotal || "0");
        case "payment_request_sent":
          return "$" + (d.amount || "0") + " to " + (d.sent_to || "?");
        case "payment_request_cancelled":
          return "$" + (d.amount || "0");
        case "rep_assigned":
          return d.unassigned ? "Unassigned" : d.rep_name || "?";
        case "price_adjusted":
          return (d.product_name || "?") + ": $" + (d.previous_price || "?") + " \u2192 $" + (d.new_price || "?") + (d.reason ? " \u2014 " + d.reason : "");
        case "cost_updated":
          return (d.product_name || "?") + " cost \u2192 " + (d.new_cost != null ? "$" + d.new_cost : "cleared");
        case "item_ready":
          return (d.product_name || "?") + " \u2014 at store";
        case "item_unready":
          return (d.product_name || "?") + " \u2014 not ready";
        case "payment_collected":
          return "$" + (d.amount || "0") + " via " + (d.method || "?") + (d.status === "pending" ? " (pending)" : "");
        case "invoice_sent":
          return d.sent_to ? "to " + d.sent_to : "";
        case "po_created":
          return d.po_number ? d.po_number + (d.vendor_name ? " \u2014 " + d.vendor_name : "") : "";
        case "po_approved":
          return (d.po_number || "") + (d.vendor_name ? " \u2014 " + d.vendor_name : "") + (d.sent_via ? " (" + d.sent_via + ")" : "");
        case "po_confirmed":
          return [d.po_number, d.file_name].filter(Boolean).join(" \xB7 ");
        case "discount_changed":
          return (d.previous_discount != null ? d.previous_discount + "%" : "?") + " \u2192 " + (d.new_discount != null ? d.new_discount + "%" : "?") + (d.reason ? " \u2014 " + d.reason : "");
        case "payment_received":
        case "payment_processing":
          return "$" + (d.amount || "0") + (d.method ? " via " + d.method : "") + (entry.action === "payment_processing" ? " (pending)" : "");
        case "payment_failed":
          return (d.method ? d.method + " \u2014 " : "") + (d.error || "failed");
        case "invoice_notes_updated":
          return d.cleared ? "cleared" : d.preview ? "\u201C" + d.preview + "\u201D" : "updated";
        case "note_added":
        case "note_edited":
        case "note_deleted":
          return d.preview ? "\u201C" + d.preview + "\u201D" : "";
        case "document_uploaded":
        case "document_deleted":
          return [d.doc_type, d.file_name].filter(Boolean).join(" \xB7 ");
        case "material_released":
          return (d.release_number || "") + (d.release_method ? " \xB7 " + d.release_method : "") + (d.line_count ? " \xB7 " + d.line_count + " line" + (d.line_count !== 1 ? "s" : "") : "");
        case "return_processed":
          return [d.rma, d.refund_total ? "$" + parseFloat(d.refund_total).toFixed(2) + " refunded" : null, d.store_credit ? "$" + parseFloat(d.store_credit).toFixed(2) + " credit" : null].filter(Boolean).join(" \xB7 ");
        case "install_scheduled":
          return [d.scheduled_at ? new Date(d.scheduled_at).toLocaleDateString() : null, d.window, d.crew].filter(Boolean).join(" \xB7 ");
        case "order_cancelled":
          return d.reason || "";
        default:
          return "";
      }
    };
    const [catalogOpen, setCatalogOpen] = useState(false);
    const [rugModalItem, setRugModalItem] = useState(null);
    const addRugToOrder = async (line) => {
      setRugModalItem(null);
      try {
        const res = await repFetch("/api/rep/orders/" + editId + "/add-item", {
          method: "POST",
          body: JSON.stringify({ is_custom_rug: true, sku_id: line.sku_id, custom_width_ft: line.custom_width_ft, custom_length_ft: line.custom_length_ft, num_boxes: line.num_boxes })
        });
        if (res.order) setOrder(res.order);
        if (res.items) setItems(res.items);
        if (res.balance) setBalanceInfo(res.balance);
        loadPOs();
      } catch (err) {
        alert("Failed to add custom rug: " + (err.message || "Unknown error"));
      }
    };
    const loadOrderActivity = useCallback(() => {
      repFetch("/api/rep/orders/" + editId + "/activity").then((data) => setOrderActivity(data.activity || [])).catch(() => {
      });
    }, [editId]);
    const addFromCatalog = async ({ lines, accessories, customs }) => {
      try {
        let last = null;
        const post = (body) => repFetch("/api/rep/orders/" + editId + "/add-item", { method: "POST", body: JSON.stringify(body) });
        for (const { item: p, qty } of lines) {
          const sellBy = p.sell_by || "box";
          const isUnit = sellBy === "unit" || sellBy === "piece";
          const isCarpet = p.price_basis === "per_sqyd";
          const spb = parseFloat(p.sqft_per_box || 0);
          let body;
          if (isUnit) body = { sku_id: p.sku_id, num_boxes: qty };
          else if (isCarpet) body = { sku_id: p.sku_id, sqft_needed: spb > 0 ? spb * qty : 9 * qty, num_boxes: 1 };
          else if (spb > 0) body = { sku_id: p.sku_id, num_boxes: qty, sqft_needed: spb * qty };
          else body = { sku_id: p.sku_id, num_boxes: qty };
          last = await post(body);
        }
        for (const { acc, qty } of accessories) {
          last = await post({ sku_id: acc.sku_id, num_boxes: qty });
        }
        for (const c of customs) {
          last = await post({
            product_name: c.product_name,
            unit_price: c.unit_price,
            vendor_id: c.vendor_id || void 0,
            custom_vendor: c.vendor_id ? void 0 : c.custom_vendor || void 0,
            custom_vendor_email: c.vendor_id ? void 0 : c.custom_vendor_email || void 0,
            cost: c.cost || void 0,
            description: c.description || null,
            sell_by: c.sell_by,
            num_boxes: c.num_boxes
          });
        }
        if (last) {
          if (last.order) setOrder(last.order);
          if (last.items) setItems(last.items);
          if (last.balance) setBalanceInfo(last.balance);
        }
        loadPOs();
      } catch (err) {
        alert("Failed to add items: " + (err.message || "Unknown error"));
      }
    };
    const loadPOs = useCallback(() => {
      if (!editId) return;
      setPosLoading(true);
      repFetch("/api/rep/orders/" + editId + "/purchase-orders").then((data) => {
        setPurchaseOrders(data.purchase_orders || []);
        setPoRepEmail(data.rep_email || "");
        setPosLoading(false);
      }).catch(() => setPosLoading(false));
    }, [editId]);
    const generatePOs = useCallback(async () => {
      if (!editId) return;
      setPosGenerating(true);
      try {
        const res = await repFetch("/api/rep/orders/" + editId + "/generate-pos", { method: "POST", body: JSON.stringify({}) });
        loadPOs();
        if (res && res.created === 0) alert("No vendor POs to generate \u2014 every line is already on a PO (or has no vendor).");
      } catch (err) {
        alert(err.message || "Failed to generate POs");
      }
      setPosGenerating(false);
    }, [editId, loadPOs]);
    const load = useCallback(() => {
      if (!editId) return;
      repFetch("/api/rep/orders/" + editId).then((data) => {
        setOrder(data.order);
        setItems(data.items || []);
        setAdjustments(data.price_adjustments || []);
        setPayments(data.payments || []);
        setPaymentRequests(data.payment_requests || []);
        setBalanceInfo(data.balance || null);
        setCommission(data.commission || null);
        setOrderInvoices(data.invoices || []);
        setReleases(data.releases || []);
        setOrderMilestones(data.milestones || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [editId]);
    const openScheduleForm = () => {
      const iso = order && order.install_scheduled_at;
      setScheduleForm({
        date: iso ? String(iso).substring(0, 10) : "",
        window: order && order.install_window || "",
        crew: order && order.install_crew || "",
        notes: order && order.install_notes || "",
        notify: false
      });
      setShowScheduleForm(true);
    };
    const saveSchedule = async () => {
      if (!scheduleForm.date) {
        alert("Pick an install date.");
        return;
      }
      setSavingSchedule(true);
      try {
        const data = await repFetch("/api/rep/orders/" + editId + "/install-schedule", {
          method: "PUT",
          body: JSON.stringify({
            install_scheduled_at: scheduleForm.date + "T12:00:00",
            install_window: scheduleForm.window || null,
            install_crew: scheduleForm.crew || null,
            install_notes: scheduleForm.notes || null,
            notify: scheduleForm.notify
          })
        });
        if (data.order) setOrder(data.order);
        setShowScheduleForm(false);
        flagSaved("Install scheduled");
        loadOrderActivity();
      } catch (err) {
        alert(err.message || "Failed to save schedule");
      }
      setSavingSchedule(false);
    };
    const submitReassignOrder = async (repId) => {
      setReassignSaving(true);
      try {
        await repFetch("/api/rep/orders/" + editId + "/assign-rep", {
          method: "PUT",
          body: JSON.stringify({ rep_id: repId })
        });
        setShowReassign(false);
        load();
        loadOrderActivity();
      } catch (err) {
        alert(err.message || "Failed to reassign order");
      } finally {
        setReassignSaving(false);
      }
    };
    const releaseAction = useCallback(async (rel, action) => {
      const verb = rel.release_method === "delivery" ? "delivery" : "pickup";
      if (action === "void" && !confirm("Void release " + rel.release_number + "? Its quantities become releasable again.")) return;
      if (action === "uncomplete" && !confirm("Undo " + verb + " for " + rel.release_number + "? It reverts to Authorized" + (rel.status === "completed" ? " and may reopen the order" : "") + ".")) return;
      setReleaseUpdating(rel.id);
      try {
        const res = await repFetch("/api/rep/releases/" + rel.id + "/" + action, { method: "POST", body: JSON.stringify({}) });
        load();
        loadOrderActivity();
        if (action === "complete" && res && res.order_auto_closed) {
          alert("All materials released and the balance is paid \u2014 this order has been marked " + (rel.release_method === "delivery" ? "Delivered" : "Picked Up") + " and closed.");
        } else if (res && res.order_reopened) {
          alert("This order was reopened \u2014 its materials are no longer fully " + (rel.release_method === "delivery" ? "delivered" : "picked up") + ".");
        }
      } catch (err) {
        alert(err.message || "Failed");
      }
      setReleaseUpdating(null);
    }, [editId, load, loadOrderActivity]);
    const emailRelease = useCallback(async (rel) => {
      if (!confirm("Email release " + rel.release_number + " to the customer?")) return;
      setReleaseUpdating(rel.id);
      try {
        const res = await repFetch("/api/rep/releases/" + rel.id + "/email", { method: "POST", body: JSON.stringify({}) });
        alert("Release " + rel.release_number + " emailed to " + (res && res.to ? res.to : "the customer") + ".");
      } catch (err) {
        alert(err.message || "Failed to email release");
      }
      setReleaseUpdating(null);
    }, []);
    const loadDocuments = useCallback(() => {
      if (!editId) return;
      repFetch("/api/rep/orders/" + editId + "/documents").then((data) => setOrderDocuments(data.documents || [])).catch(() => {
      });
    }, [editId]);
    const pickConfirmationFor = (poId) => {
      confirmPoIdRef.current = poId;
      if (confirmFileRef.current) {
        confirmFileRef.current.value = "";
        confirmFileRef.current.click();
      }
    };
    const handleConfirmationFile = async (e) => {
      const file = e.target.files && e.target.files[0];
      const poId = confirmPoIdRef.current;
      if (!file || !poId) return;
      setConfirmUploadingPoId(poId);
      try {
        const form = new FormData();
        form.append("file", file);
        await repUpload("/api/rep/purchase-orders/" + poId + "/documents", form);
        loadDocuments();
        loadPOs();
      } catch (err) {
        alert(err.message || "Upload failed");
      }
      setConfirmUploadingPoId(null);
      confirmPoIdRef.current = null;
    };
    const deleteOrderDoc = async (doc) => {
      if (!confirm("Delete this document?")) return;
      try {
        await repFetch("/api/rep/orders/documents/" + doc.id, { method: "DELETE" });
        loadDocuments();
        if (doc.doc_type === "vendor_confirmation") loadPOs();
      } catch (err) {
        alert(err.message || "Delete failed");
      }
    };
    const printableDocs = [
      ...orderInvoices.length > 0 ? orderInvoices.map((iv) => ({
        key: "inv-" + iv.id,
        label: "Invoice " + iv.invoice_number,
        sub: iv.issue_date ? "Issued " + new Date(iv.issue_date).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "Draft",
        path: "/api/rep/orders/" + editId + "/invoice?preview=true"
      })) : [{ key: "inv", label: "Invoice", sub: "Customer-facing PDF", path: "/api/rep/orders/" + editId + "/invoice?preview=true" }],
      ...purchaseOrders.map((po) => ({
        key: "po-" + po.id,
        label: po.po_number,
        sub: po.vendor_name + (po.vendor_public_code ? " \xB7 " + po.vendor_public_code : ""),
        path: "/api/rep/purchase-orders/" + po.id + "/pdf?preview=true"
      })),
      ...releases.filter((rel) => rel.status !== "void").map((rel) => ({
        key: "rel-" + rel.id,
        label: "Release " + rel.release_number,
        sub: (rel.release_method === "delivery" ? "Delivery" : rel.release_method === "will_call" ? "Will-call" : "Pickup") + " \xB7 " + rel.line_count + " line" + (rel.line_count === 1 ? "" : "s"),
        path: "/api/rep/releases/" + rel.id + "/release-form?preview=true"
      })),
      ...orderDocuments.filter((doc) => doc.url).map((doc) => ({
        key: "doc-" + doc.id,
        label: rovOrderDocLabel(doc),
        sub: "Uploaded " + (doc.mime_type ? (doc.mime_type.split("/")[1] || "file").toUpperCase() : "file"),
        url: doc.url
      }))
    ];
    const openPrintModal = () => {
      const sel = {};
      printableDocs.forEach((d) => {
        sel[d.key] = true;
      });
      setPrintSel(sel);
      setShowPrintModal(true);
    };
    const printSelectedDocs = async () => {
      const chosen = printableDocs.filter((d) => printSel[d.key]);
      if (!chosen.length || printBusy) return;
      for (let i = 0; i < chosen.length; i++) {
        const d = chosen[i];
        setPrintBusy(chosen.length > 1 ? "Printing " + (i + 1) + " of " + chosen.length + "\u2026" : "Printing\u2026");
        try {
          const url = d.url || await repOpenDoc(d.path);
          await printDocUrl(url, !d.url);
        } catch (e) {
          alert("Failed to load " + d.label);
        }
      }
      setPrintBusy(null);
      setShowPrintModal(false);
    };
    useEffect(load, [load]);
    useEffect(loadPOs, [loadPOs]);
    useEffect(loadDocuments, [loadDocuments]);
    useEffect(loadOrderActivity, [loadOrderActivity]);
    useEffect(() => {
      repFetch("/api/rep/cash-drawer/current").then((d) => setCollectCashDrawerOpen(!!d.drawer)).catch(() => setCollectCashDrawerOpen(false));
    }, []);
    useEffect(() => {
      if (!order) return;
      const custId2 = order.trade_customer_id || order.customer_id;
      const custType2 = order.trade_customer_id ? "trade" : order.customer_id ? "retail" : "guest";
      const handle = (data) => {
        setCustomerStats(data.stats || null);
      };
      if (custId2) {
        repFetch("/api/rep/customers/" + custId2 + "?type=" + custType2).then(handle).catch(() => {
        });
      } else if (order.customer_email) {
        repFetch("/api/rep/customers/" + encodeURIComponent(order.customer_email) + "?type=guest").then(handle).catch(() => {
        });
      }
    }, [order?.id]);
    const loadOrderNotes = useCallback(() => {
      if (!editId) return;
      repFetch("/api/rep/orders/" + editId + "/notes").then((d) => setOrderNotes(d.notes || [])).catch(() => {
      });
    }, [editId]);
    useEffect(loadOrderNotes, [loadOrderNotes]);
    const saveNoteEdit = async (n) => {
      const text = editNoteText.trim();
      if (!text) return;
      try {
        const data = await repFetch("/api/rep/orders/" + editId + "/notes/" + n.id, { method: "PUT", body: JSON.stringify({ note: text }) });
        if (data.note) setOrderNotes((prev) => prev.map((x) => x.id === n.id ? data.note : x));
        setEditingNoteId(null);
        setEditNoteText("");
      } catch (err) {
        alert(err.message || "Failed to save note");
      }
    };
    const deleteNote = async (n) => {
      if (!confirm("Delete this note? This cannot be undone.")) return;
      try {
        await repFetch("/api/rep/orders/" + editId + "/notes/" + n.id, { method: "DELETE" });
        setOrderNotes((prev) => prev.filter((x) => x.id !== n.id));
      } catch (err) {
        alert(err.message || "Failed to delete note");
      }
    };
    const collectPayment = async (method, res) => {
      setCollectSaving(true);
      try {
        await repFetch("/api/rep/orders/" + editId + "/collect-payment", {
          method: "POST",
          body: JSON.stringify({
            payment_method: method,
            amount: parseFloat(collectAmount),
            card_txn: res && res.card_txn || void 0,
            stripe_payment_intent_id: res && res.payment_intent_id || void 0,
            check_number: method === "check" ? collectCheckNumber : void 0
          })
        });
        setShowCollectPayment(false);
        setShowCollectCardModal(false);
        setCollectMethod(null);
        setCollectCheckNumber("");
        setCollectAmount("");
        load();
        loadOrderActivity();
      } catch (err) {
        alert(err.message);
      }
      setCollectSaving(false);
    };
    const handleCollectCard = () => {
      setShowCollectCardModal(true);
    };
    const submitRefund = async () => {
      if (!refundTarget) return;
      setRefundSaving(true);
      try {
        const data = await repFetch("/api/rep/orders/" + editId + "/payments/" + refundTarget.id + "/refund", {
          method: "POST",
          body: JSON.stringify({ amount: parseFloat(refundAmount), reason: refundReason.trim() })
        });
        if (data.order) setOrder(data.order);
        if (data.payments) setPayments(data.payments);
        if (data.balance) setBalanceInfo(data.balance);
        setRefundTarget(null);
        setRefundAmount("");
        setRefundReason("");
        loadOrderActivity();
      } catch (err) {
        alert(err.message);
      }
      setShowRefundConfirm(false);
      setRefundSaving(false);
    };
    const savePOCost = async (poId, itemId) => {
      const val = parseFloat(newCost);
      if (isNaN(val) || val < 0) return alert("Invalid cost");
      setPoUpdating(true);
      try {
        await repFetch("/api/rep/purchase-orders/" + poId + "/items/" + itemId, {
          method: "PUT",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ cost: val })
        });
        setEditingPOItem(null);
        setNewCost("");
        loadPOs();
      } catch (err) {
        alert(err.message);
      }
      setPoUpdating(false);
    };
    const approvePO = (poId) => {
      const po = purchaseOrders.find((p) => p.id === poId);
      if (po) setPoEmailModal(po);
    };
    const doApprovePO = async (po, overrides) => {
      setPoUpdating(true);
      let ok = false;
      try {
        const result = await repFetch("/api/rep/purchase-orders/" + po.id + "/approve", {
          method: "POST",
          body: JSON.stringify({
            method: overrides && (overrides.method === "phone" || overrides.method === "online") ? overrides.method : void 0,
            recipient_email: overrides && overrides.recipient_email || void 0,
            cc_emails: overrides && overrides.cc_emails || void 0,
            notes: overrides && typeof overrides.notes === "string" ? overrides.notes : void 0,
            line_notes: overrides && overrides.line_notes || void 0
          })
        });
        ok = true;
        if (result.resent) {
          if (result.email_sent === false) alert("PO could not be re-emailed. The vendor may not have an email on file, or SMTP is not configured.");
        } else if (result.sent_via === "phone" || result.sent_via === "online") {
          alert("PO marked as ordered " + (result.sent_via === "phone" ? "over the phone" : "online") + ".");
        } else if (result.sent_via === "edi") {
          alert("PO approved and sent via EDI.");
        } else if (result.email_sent === false) {
          alert("PO approved but email could not be sent. The vendor may not have an email on file, or SMTP is not configured.");
        }
        loadPOs();
      } catch (err) {
        alert(err.message);
      }
      setPoUpdating(false);
      return ok;
    };
    const updatePOItemStatus = async (poId, itemId, status) => {
      setPoUpdating(true);
      try {
        await repFetch("/api/rep/purchase-orders/" + poId + "/items/" + itemId + "/status", {
          method: "PUT",
          body: JSON.stringify({ status })
        });
        loadPOs();
      } catch (err) {
        alert(err.message);
      }
      setPoUpdating(false);
    };
    const updateStatus = async (newStatus, extras) => {
      if (newStatus === "shipped" && order.delivery_method === "shipping" && !showTrackingForm) {
        setShowTrackingForm(true);
        return;
      }
      setUpdating(true);
      try {
        const body = { status: newStatus, ...extras };
        if (newStatus === "shipped" && showTrackingForm) {
          body.tracking_number = trackingData.tracking_number;
          body.carrier = trackingData.carrier;
          body.shipped_at = trackingData.shipped_at;
        }
        const data = await repFetch("/api/rep/orders/" + editId + "/status", {
          method: "PUT",
          body: JSON.stringify(body)
        });
        if (data.order) setOrder(data.order);
        flagSaved("Status updated");
        loadPOs();
        setShowTrackingForm(false);
        setShowCancelForm(false);
        setCancelReason("");
        setTrackingData({ tracking_number: "", carrier: "", shipped_at: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10) });
      } catch (err) {
        alert(err.message || "Failed to update status");
      }
      setUpdating(false);
    };
    const saveItemCost = async (itemId) => {
      try {
        const data = await repFetch("/api/rep/orders/" + editId + "/items/" + itemId + "/cost", {
          method: "PUT",
          body: JSON.stringify({ cost: costDraft === "" ? null : costDraft })
        });
        if (data.items) setItems(data.items);
      } catch (err) {
        alert(err.message || "Failed to update cost");
      }
      setEditingCostItem(null);
      setCostDraft("");
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading order...");
    if (!order) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Order not found");
    const qtyLabel = (sellBy) => sellBy === "unit" ? "units" : sellBy === "piece" ? "pcs" : sellBy === "roll" ? "sqft" : "boxes";
    const isPickup = order.delivery_method === "pickup";
    const isWillCall = order.delivery_method === "will_call";
    const noFreight = isPickup || isWillCall;
    const isInstall = items.some((i) => i.item_type === "labor");
    const anyReleased = releases.some((rel) => rel.status !== "void");
    const statusFlow = noFreight ? { pending: "confirmed", confirmed: "ready_for_pickup", ready_for_pickup: "delivered" } : { pending: "confirmed", confirmed: "shipped", shipped: "delivered" };
    const statusLabels = isInstall ? { pending: "Pending", confirmed: "Confirmed", shipped: "Installing", ready_for_pickup: "Installing", delivered: "Job complete", cancelled: "Cancelled", refunded: "Refunded" } : noFreight ? { pending: "Pending", confirmed: "Confirmed", ready_for_pickup: "Ready for Pickup", shipped: "Ready for Pickup", delivered: "Picked Up", cancelled: "Cancelled", refunded: "Refunded" } : { pending: "Pending", confirmed: "Confirmed", shipped: "Shipped", delivered: "Delivered", cancelled: "Cancelled", refunded: "Refunded" };
    const nextStatus = statusFlow[order.status];
    const canCancel = order.status !== "cancelled" && order.status !== "delivered" && order.status !== "refunded";
    const rodFmtDT = (d) => {
      const dt = new Date(d);
      return dt.toLocaleDateString("en-US", { month: "short", day: "numeric" }) + " \xB7 " + dt.toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }).toLowerCase().replace(" ", "");
    };
    const orderIsDead = ["cancelled", "refunded"].includes(order.status);
    const orderTotal = parseFloat(order.total || 0);
    const amountPaid = parseFloat(order.amount_paid || 0);
    const paidDone = balanceInfo ? balanceInfo.balance_status === "paid" || balanceInfo.balance_status === "credit" : orderTotal > 0 && amountPaid >= orderTotal;
    const hasBalanceDue = balanceInfo && balanceInfo.balance_status === "balance_due";
    const openCollectBalance = () => {
      if (!hasBalanceDue) return;
      setCollectAmount(balanceInfo.balance.toFixed(2));
      setPaymentRequestEmail(order.customer_email || "");
      setPaymentRequestMessage("");
      setCollectMethod(null);
      setShowCollectPayment(true);
      setTimeout(() => {
        const el = document.getElementById("rod-collect-anchor");
        if (el) el.scrollIntoView({ behavior: "smooth", block: "center" });
      }, 60);
    };
    const openCollectForMilestone = (m) => {
      if (!hasBalanceDue) return;
      const amt = Math.min(parseFloat(m.remaining != null ? m.remaining : m.amount || 0), balanceInfo.balance);
      if (!(amt > 0)) return;
      setCollectAmount(amt.toFixed(2));
      setPaymentRequestEmail(order.customer_email || "");
      setPaymentRequestMessage("");
      setCollectMethod(null);
      setShowCollectPayment(true);
      setTimeout(() => {
        const el = document.getElementById("rod-collect-anchor");
        if (el) el.scrollIntoView({ behavior: "smooth", block: "center" });
      }, 60);
    };
    const sentPOCount = purchaseOrders.filter((po) => po.status !== "draft").length;
    const posDone = purchaseOrders.length > 0 && sentPOCount === purchaseOrders.length;
    const inFulfillment = ["confirmed", "ready_for_pickup", "shipped"].includes(order.status);
    const isDeliveredOrder = order.status === "delivered";
    const recvLines = items.filter((i) => !i.is_sample && i.item_type !== "labor");
    const recvLinesIn = recvLines.filter((i) => i.ready_at).length;
    const allReceived = recvLines.length === 0 || recvLinesIn === recvLines.length;
    const anyReceived = recvLines.some((i) => i.ready_at || i.qty_received != null && parseFloat(i.qty_received) > 0);
    const relLinesOut = recvLines.filter((it) => parseFloat(it.num_boxes || 0) - parseFloat(it.already_released || 0) <= 1e-3).length;
    const allReleased = recvLines.length > 0 && relLinesOut === recvLines.length;
    const orderStages = [
      { label: "Placed", done: true, meta: rodFmtDT(order.created_at) },
      { label: "Paid", done: paidDone, meta: paidDone ? "$" + amountPaid.toFixed(2) + " collected" : balanceInfo && balanceInfo.balance > 0 ? "Balance $" + balanceInfo.balance.toFixed(2) : "Payment pending" },
      { label: "POs sent", done: posDone, meta: purchaseOrders.length > 0 ? sentPOCount + " of " + purchaseOrders.length + " transmitted" : order.status === "pending" ? "Generated on confirmation" : "No POs" },
      isWillCall ? { label: "Released", done: isDeliveredOrder || allReleased, meta: isDeliveredOrder || allReleased ? "All lines released" : relLinesOut > 0 ? relLinesOut + " of " + recvLines.length + " lines released" : posDone ? "Ready to release" : "Send the PO first" } : { label: "Order received", done: isDeliveredOrder || allReceived, meta: isDeliveredOrder || allReceived ? "All materials in" : recvLinesIn > 0 ? recvLinesIn + " of " + recvLines.length + " lines in" : inFulfillment ? "Awaiting materials" : "Pending" },
      { label: isInstall ? "Job complete" : noFreight ? "Picked up" : "Delivered", done: isDeliveredOrder, meta: isDeliveredOrder ? "Complete" : order.tracking_number ? (order.shipping_carrier ? order.shipping_carrier + " \xB7 " : "") + order.tracking_number : isWillCall ? "Will-call \xB7 distributor" : isPickup ? "Will-call \xB7 Anaheim" : "Pending" }
    ];
    const currentStageIdx = orderStages.findIndex((s) => !s.done);
    const refundedByTender = {};
    payments.forEach((p) => {
      if (p.payment_type === "refund" && p.refund_of_payment_id && (p.status === "completed" || !p.status)) {
        refundedByTender[p.refund_of_payment_id] = (refundedByTender[p.refund_of_payment_id] || 0) + Math.abs(parseFloat(p.amount));
      }
    });
    const tenderRemaining = (p) => parseFloat((parseFloat(p.amount) - (refundedByTender[p.id] || 0)).toFixed(2));
    const canCounterRefund = (p) => ["charge", "additional_charge"].includes(p.payment_type) && parseFloat(p.amount) > 0 && p.status === "completed" && Date.now() - new Date(p.created_at).getTime() <= 24 * 60 * 60 * 1e3 && tenderRemaining(p) > 0;
    const tenderMethod = (p) => p.payment_method || (p.stripe_payment_intent_id ? "card" : "offline");
    const pendingPayment = payments.find((p) => parseFloat(p.amount) > 0 && p.status === "pending");
    const pendingIsCheck = pendingPayment && /check/i.test(pendingPayment.description || pendingPayment.payment_method || "");
    const headChip = order.status === "cancelled" ? { tone: "hot", label: "Cancelled" } : order.status === "refunded" ? { tone: "hot", label: "Refunded" } : isDeliveredOrder ? { tone: "done", label: isInstall ? "Closed \xB7 Job complete" : noFreight ? "Closed \xB7 Picked up" : "Closed \xB7 Delivered" } : order.status === "ready_for_pickup" ? { tone: "live", label: isInstall ? "Installing" : "Ready for pickup" } : order.status === "shipped" ? { tone: "live", label: isInstall ? "Installing" : isPickup ? "Ready for pickup" : "Shipped \xB7 In transit" } : order.status === "confirmed" ? { tone: "live", label: isInstall ? "Booked \xB7 In progress" : "Processed \xB7 In fulfillment" } : { tone: "soft", label: "Pending" };
    const poForItem = (itemId) => purchaseOrders.find((po) => (po.items || []).some((pi) => pi.order_item_id === itemId));
    const lineAwaitingTransmit = (itemId) => {
      const covering = purchaseOrders.filter((po) => po.status !== "cancelled" && (po.items || []).some((pi) => pi.order_item_id === itemId));
      return covering.length > 0 && !covering.some((po) => po.status !== "draft");
    };
    const poChannel = (po) => po.sent_via === "phone" ? "Phone order" : po.sent_via === "online" ? "Online order" : po.sent_via === "edi" || po.edi_interchange_id ? "EDI" : "Email";
    const poAckLine = (po) => {
      if (po.status === "draft") return "Draft \xB7 not yet sent";
      if (po.edi_ack_status === "rejected") return "Rejected \xB7 EDI 855";
      if (po.edi_ack_status === "partial") return "Backordered/changed \xB7 EDI 855";
      if (po.status === "acknowledged" || po.status === "fulfilled") return "Acknowledged by vendor";
      if (po.edi_ack_status === "accepted") return "Acknowledged \xB7 EDI 855";
      if (po.edi_interchange_id) return "Awaiting acknowledgment";
      return "Sent \xB7 awaiting confirmation";
    };
    const poChip = (po) => {
      if (po.status === "cancelled") return { tone: "hot", label: "Cancelled" };
      if (po.status === "received" || po.status === "delivered") return { tone: "done", label: "Closed" };
      if (po.status === "draft") return { tone: "soft", label: "Draft" };
      if (po.edi_ack_status === "rejected") return { tone: "hot", label: "Rejected" };
      if (po.edi_ack_status === "partial") return { tone: "hold", label: "Backordered" };
      if (po.status === "acknowledged" || po.status === "fulfilled") return { tone: "live", label: "Acknowledged" };
      if (po.edi_ack_status === "accepted") return { tone: "live", label: "Acknowledged" };
      return { tone: "soft", label: "Sent" };
    };
    const poLineAckSummary = (po) => {
      const counts = {};
      for (const it of po.items || []) {
        if (it.edi_line_status) counts[it.edi_line_status] = (counts[it.edi_line_status] || 0) + 1;
      }
      const keys = Object.keys(counts);
      if (!keys.length || keys.length === 1 && keys[0] === "accepted") return null;
      return keys.map((k) => counts[k] + " " + k).join(" \xB7 ");
    };
    const custId = order.trade_customer_id || order.customer_id || order.customer_email;
    const custType = order.trade_customer_id ? "trade" : order.customer_id ? "retail" : "guest";
    const showReady = isPickup && ["confirmed", "ready_for_pickup"].includes(order.status);
    const showReceive = !isWillCall && ["confirmed", "shipped", "ready_for_pickup"].includes(order.status);
    const readyTotal = items.filter((i) => !i.is_sample).length;
    const readyCount = items.filter((i) => !i.is_sample && i.ready_at).length;
    const hasUnreleasedMaterials = paidDone && ["confirmed", "shipped", "ready_for_pickup", "delivered"].includes(order.status) && items.some((it) => !it.is_sample && it.item_type !== "labor" && parseFloat(it.num_boxes || 0) - parseFloat(it.already_released || 0) > 1e-3);
    const openReceive = (item) => {
      const ordered = parseFloat(item.num_boxes || 0);
      const current = item.qty_received != null ? parseFloat(item.qty_received) : ordered;
      setReceiveItem(item);
      setReceiveQty(String(current));
    };
    const submitReceive = async () => {
      if (!receiveItem) return;
      const ordered = parseFloat(receiveItem.num_boxes || 0);
      const qty = Math.max(0, Math.min(ordered, parseFloat(receiveQty) || 0));
      setReceiveSaving(true);
      try {
        const data = await repFetch("/api/rep/orders/" + editId + "/items/" + receiveItem.id + "/ready", {
          method: "PUT",
          body: JSON.stringify({ qty_received: qty })
        });
        if (data.order) setOrder(data.order);
        setItems((prev) => prev.map((i) => i.id === receiveItem.id ? { ...i, qty_received: data.qty_received, ready_at: data.ready_at } : i));
        setReceiveItem(null);
        setReceiveQty("");
      } catch (err) {
        alert(err.message || "Failed to update item");
      }
      setReceiveSaving(false);
    };
    return /* @__PURE__ */ React.createElement("div", { style: { paddingBottom: 96 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("orders") }, "Orders"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, order.order_number)), /* @__PURE__ */ React.createElement("div", { className: "rod-breadcrumb-nav" }, /* @__PURE__ */ React.createElement(RepPrevNext, { section: "orders", id: editId, view: "order-detail", navigate }))), /* @__PURE__ */ React.createElement("div", { className: "ws-head" }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, order.order_number), /* @__PURE__ */ React.createElement(RovChip, { tone: headChip.tone }, headChip.label), !orderIsDead && pendingPayment && /* @__PURE__ */ React.createElement(RovChip, { tone: "hold" }, pendingIsCheck ? "Check clearing" : "Payment clearing")), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, (order.customer_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, order.customer_name || "Guest"), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, [
      order.customer_email,
      order.phone,
      custType === "trade" ? "Trade" + (order.trade_tier_name ? " \xB7 " + order.trade_tier_name : "") : custType === "retail" ? "Retail" : "Guest"
    ].filter(Boolean).join(" \xB7 ")))), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 10 } }, order.project_name ? order.project_name + " \xB7 " : "", order.payment_method ? { cash: "Cash", check: "Check", card: "Card", stripe: "Payment Request", offline: "Offline", ach: "Check (ACH)" }[order.payment_method] || order.payment_method : "", order.rep_name ? (order.payment_method || order.project_name ? " \xB7 " : "") + "Rep " + order.rep_name : ""), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 8 } }, editingJobName ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement(
      SidemarkSelect,
      {
        value: jobNameDraft,
        onChange: setJobNameDraft,
        customerId: order.customer_id,
        email: order.customer_email,
        style: { padding: "4px 8px", fontSize: 13, maxWidth: 340 }
      }
    ), /* @__PURE__ */ React.createElement("button", { onClick: async () => {
      const next = jobNameDraft.trim();
      setEditingJobName(false);
      if (next === (order.job_name || "")) return;
      try {
        const data = await repFetch("/api/rep/orders/" + editId + "/job-name", { method: "PUT", body: JSON.stringify({ job_name: next }) });
        if (data.order) setOrder(data.order);
        flagSaved("Saved");
        loadOrderActivity();
      } catch (err) {
        alert(err.message || "Failed to save job name");
      }
    }, style: { padding: "4px 12px", background: "var(--rod-ink)", color: "#fff", border: "0.5px solid var(--rod-ink)", cursor: "pointer", font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.04em", textTransform: "uppercase" } }, "Save"), /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick: () => {
          setEditingJobName(false);
          setJobNameDraft("");
        },
        style: { background: "none", border: "none", cursor: "pointer", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.08em", textTransform: "uppercase", color: "var(--rod-muted)" }
      },
      "Cancel"
    )) : /* @__PURE__ */ React.createElement(
      "span",
      {
        title: "Edit job / sidemark",
        onClick: () => {
          setJobNameDraft(order.job_name || "");
          setEditingJobName(true);
        },
        style: {
          font: "500 10px/1 ui-monospace, monospace",
          letterSpacing: "0.14em",
          textTransform: "uppercase",
          color: order.job_name ? "var(--rod-ink)" : "var(--rod-muted)",
          cursor: "pointer",
          borderBottom: "1px dashed rgba(28,25,23,0.3)",
          paddingBottom: 2
        }
      },
      order.job_name ? "Job \xB7 " + order.job_name : "+ Job / sidemark"
    ))), /* @__PURE__ */ React.createElement("div", { className: "ws-head-actions" }, /* @__PURE__ */ React.createElement(
      AddTaskButton,
      {
        navigate,
        className: "rov-hbtn",
        label: /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "+"), "Task"),
        prefill: {
          title: "Follow up on order " + order.order_number,
          linked_order_id: order.id,
          customer_name: order.customer_name,
          customer_email: order.customer_email,
          customer_phone: order.customer_phone,
          context_label: order.order_number + (order.customer_name ? " \xB7 " + order.customer_name : "")
        }
      }
    ), order.customer_email && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => {
      setInvoiceMessage("");
      setShowSendInvoiceModal(true);
    } }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u21BB"), "Resend invoice"), repInfo.is_manager && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => setShowReassign(true) }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u21C4"), "Reassign rep"), order.status === "pending" && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("change-order", order.id) }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u270E"), "Change order"), paidDone && ["confirmed", "shipped", "ready_for_pickup"].includes(order.status) && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("change-order", order.id) }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u270E"), "Change order"), hasUnreleasedMaterials && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("release-flow", order.id, isWillCall ? { method: "will_call" } : void 0) }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u2192"), "Release materials"), repInfo.is_manager && ["shipped", "delivered", "ready_for_pickup"].includes(order.status) && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("return-flow", order.id) }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u21A9"), "Start return"), !showCancelForm && canCancel && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", { className: "rov-hact-div" }), /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn danger", onClick: () => setShowCancelForm(true), disabled: updating }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u2715"), "Cancel")), order.status === "cancelled" && !order.stripe_refund_id && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => {
      if (confirm("Reopen this order? It will return to Pending status.")) updateStatus("pending");
    }, disabled: updating }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u21BA"), "Reopen"))), /* @__PURE__ */ React.createElement("div", { className: "qw-stepper cols-5" }, orderStages.map((s, i) => {
      const state = orderIsDead ? "dead" : s.done ? "done" : i === currentStageIdx ? "now" : "next";
      return /* @__PURE__ */ React.createElement("div", { key: s.label, className: "qw-step " + state }, /* @__PURE__ */ React.createElement("div", { className: "qw-step-label" }, /* @__PURE__ */ React.createElement("span", { className: "qw-step-mark" }, state === "done" ? "\u2713" : state === "dead" ? "\u2715" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", null, s.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-step-meta" }, s.meta));
    })), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Order total"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", orderTotal.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Paid"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: amountPaid > 0 ? { color: "var(--rod-green)" } : void 0 }, "$", amountPaid.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Balance due"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: (balanceInfo ? balanceInfo.balance > 5e-3 : orderTotal - amountPaid > 5e-3) ? { color: "var(--rod-accent)" } : void 0 }, "$", (balanceInfo ? Math.max(0, balanceInfo.balance) : Math.max(0, orderTotal - amountPaid)).toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Items"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, items.length)), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Placed"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, new Date(order.created_at).toLocaleDateString("en-US", { month: "short", day: "numeric" })))), showReassign && /* @__PURE__ */ React.createElement(
      ReassignRepModal,
      {
        title: "Reassign order " + order.order_number,
        subtitle: "Moves only this order to the chosen rep. The customer\u2019s own rep assignment is unchanged.",
        currentRepId: order.sales_rep_id || "",
        currentRepName: order.rep_name,
        saving: reassignSaving,
        onClose: () => setShowReassign(false),
        onConfirm: submitReassignOrder
      }
    ), order.status === "cancelled" && order.cancel_reason && /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "12px", padding: "0.75rem 1rem", background: "#fee2e2", color: "#991b1b", fontSize: "0.8125rem" } }, /* @__PURE__ */ React.createElement("strong", null, "Cancellation reason:"), " ", order.cancel_reason), order.payment_method === "ach" && order.status === "pending" && /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "12px", padding: "0.75rem 1rem", background: "#eff6ff", border: "1px solid #93c5fd", fontSize: "0.8125rem", color: "#1e40af" } }, "\u{1F3E6}", " ", /* @__PURE__ */ React.createElement("strong", null, "ACH payment pending"), " \u2014 bank transfer typically clears in 4-5 business days. Order will be confirmed automatically when funds settle."), order.status === "refunded" && /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "12px", padding: "0.75rem 1rem", background: "#ede9fe", color: "#6d28d9", fontSize: "0.8125rem" } }, "Refunded $", parseFloat(order.refund_amount || 0).toFixed(2)), hasBalanceDue && !orderIsDead && (() => {
      const dueMilestone = orderMilestones.find((m) => m.status === "due");
      const amt = dueMilestone ? parseFloat(dueMilestone.remaining) : parseFloat(balanceInfo.balance);
      const eyebrow = dueMilestone ? "Next payment due" : "Balance due";
      const caption = dueMilestone ? [dueMilestone.label, dueMilestone.due_label].filter(Boolean).join(" \xB7 ") : isInstall ? "Collect on completion" : null;
      const sub = dueMilestone ? "Balance $" + parseFloat(balanceInfo.balance).toFixed(2) + " of $" + orderTotal.toFixed(2) : amountPaid > 0 ? "$" + amountPaid.toFixed(2) + " collected of $" + orderTotal.toFixed(2) : "of $" + orderTotal.toFixed(2);
      return /* @__PURE__ */ React.createElement("div", { style: {
        margin: "0 0 12px",
        padding: "14px 18px",
        background: "#faf3e4",
        borderLeft: "3px solid var(--gold)",
        border: "0.5px solid var(--rod-border)",
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        gap: 20,
        flexWrap: "wrap"
      } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 220 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 7, marginBottom: 5 } }, /* @__PURE__ */ React.createElement("span", { style: { width: 6, height: 6, borderRadius: "50%", background: "var(--rod-accent)", flexShrink: 0 } }), /* @__PURE__ */ React.createElement("span", { style: { font: "600 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a6d3b" } }, eyebrow)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", gap: 10, flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 24px/1 "Cormorant Garamond", serif', letterSpacing: "-0.01em", color: "var(--rod-ink)" } }, "$", amt.toFixed(2)), caption && /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1.4 Inter, sans-serif", color: "var(--rod-muted)" } }, caption)), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.4 Inter, sans-serif", color: "var(--rod-muted)", marginTop: 3 } }, sub)), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", onClick: () => dueMilestone ? openCollectForMilestone(dueMilestone) : openCollectBalance(), disabled: updating }, dueMilestone ? "Collect " + dueMilestone.label : "Collect / request balance", " ", "\u2192"));
    })(), showTrackingForm && /* @__PURE__ */ React.createElement("div", { className: "rod-inline-panel", style: { marginTop: 0, marginBottom: "12px" } }, /* @__PURE__ */ React.createElement("div", { className: "rod-field-label", style: { marginBottom: "0.75rem" } }, "Shipment Details"), /* @__PURE__ */ React.createElement("div", { className: "resp-grid-3", style: { gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Carrier *"), /* @__PURE__ */ React.createElement("select", { className: "rod-input", value: trackingData.carrier, onChange: (e) => setTrackingData({ ...trackingData, carrier: e.target.value }) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "Select carrier..."), /* @__PURE__ */ React.createElement("option", null, "UPS"), /* @__PURE__ */ React.createElement("option", null, "FedEx"), /* @__PURE__ */ React.createElement("option", null, "USPS"), /* @__PURE__ */ React.createElement("option", null, "FedEx Freight"), /* @__PURE__ */ React.createElement("option", null, "XPO Logistics"), /* @__PURE__ */ React.createElement("option", null, "R+L Carriers"), /* @__PURE__ */ React.createElement("option", null, "SAIA"), /* @__PURE__ */ React.createElement("option", null, "Old Dominion"), /* @__PURE__ */ React.createElement("option", null, "Other"))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Tracking Number *"), /* @__PURE__ */ React.createElement("input", { className: "rod-input", type: "text", maxLength: 80, placeholder: "Enter tracking number", value: trackingData.tracking_number, onChange: (e) => setTrackingData({ ...trackingData, tracking_number: e.target.value }) })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Ship Date"), /* @__PURE__ */ React.createElement("input", { className: "rod-input", type: "date", value: trackingData.shipped_at, onChange: (e) => setTrackingData({ ...trackingData, shipped_at: e.target.value }) }))), /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.75rem", display: "flex", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", onClick: () => updateStatus("shipped"), disabled: updating || !trackingData.carrier || !trackingData.tracking_number }, updating ? "Submitting..." : "Confirm Shipment"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => setShowTrackingForm(false) }, "Cancel"))), showCancelForm && /* @__PURE__ */ React.createElement("div", { className: "rod-inline-panel", style: { marginTop: 0, marginBottom: "12px", borderColor: "rgba(197,71,49,0.35)", background: "rgba(197,71,49,0.04)" } }, /* @__PURE__ */ React.createElement("div", { className: "rod-field-label", style: { marginBottom: "0.75rem", color: "var(--rod-red)" } }, "Cancel Order"), /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Reason for cancellation *"), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        className: "rod-input",
        value: cancelReason,
        onChange: (e) => setCancelReason(e.target.value),
        placeholder: "e.g. Customer requested cancellation, out of stock...",
        rows: 3,
        style: { marginBottom: "0.75rem" }
      }
    ), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rod-mini-btn danger",
        disabled: updating || !cancelReason.trim(),
        onClick: () => updateStatus("cancelled", { cancel_reason: cancelReason.trim() })
      },
      updating ? "Cancelling..." : "Confirm Cancellation"
    ), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => {
      setShowCancelForm(false);
      setCancelReason("");
    }, disabled: updating }, "Go Back"))), /* @__PURE__ */ React.createElement("div", { className: "qw-grid" }, /* @__PURE__ */ React.createElement("div", { className: "qw-main" }, /* @__PURE__ */ React.createElement(
      RodCard,
      {
        title: "Line items \xB7 " + items.length + (showReady ? " \xB7 " + readyCount + "/" + readyTotal + " ready for pickup" : ""),
        action: ["pending", "confirmed"].includes(order.status) ? "Change order" : null,
        onAction: () => navigate("change-order", order.id)
      },
      /* @__PURE__ */ React.createElement("div", { className: "rod-li-head" + (showCost ? "" : " nocost") }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Item"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Qty"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Unit $"), showCost && /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Cost"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Total"), /* @__PURE__ */ React.createElement("span", null)),
      (() => {
        return items.map((item) => {
          const itemAdj = adjustments.filter((a) => a.order_item_id === item.id);
          const isLabor = item.item_type === "labor";
          const isCustom = !isLabor && !item.product_id;
          const fli = formatLineItem(item);
          const open = !!openLines[item.id];
          const po = isLabor ? null : poForItem(item.id);
          const ordered = parseFloat(item.num_boxes || 0);
          const released = parseFloat(item.already_released || 0);
          const badge = (label, accent) => /* @__PURE__ */ React.createElement("span", { style: {
            marginLeft: 6,
            font: '500 8px/1 "Inter", sans-serif',
            letterSpacing: "0.08em",
            textTransform: "uppercase",
            padding: "2px 6px",
            borderRadius: 3,
            background: accent ? "rgba(168,121,53,0.12)" : "rgba(28,25,23,0.08)",
            color: accent ? "var(--rod-accent)" : "var(--rod-muted)",
            verticalAlign: "middle"
          } }, label);
          return /* @__PURE__ */ React.createElement(React.Fragment, { key: item.id }, /* @__PURE__ */ React.createElement("div", { className: "rod-li-sum" + (open ? " open" : "") + (showCost ? "" : " nocost"), onClick: () => setOpenLines((p) => ({ ...p, [item.id]: !p[item.id] })) }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-swatch" }, isLabor ? null : item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover" }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: item.collection || item.current_collection }), 50) } })), /* @__PURE__ */ React.createElement("div", { className: "rof-item-info" }, !isLabor && !isCustom && fli.brand && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, fli.brand), /* @__PURE__ */ React.createElement("div", { className: "rof-item-name", style: { whiteSpace: "normal" } }, isLabor ? item.product_name || "Labor" : fli.name, isLabor && badge("Labor"), !isLabor && isCustom && badge("Custom", true), item.is_sample && badge("Sample")), !isLabor && !isCustom && fli.sku && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, fli.sku), (() => {
            if (isLabor || item.is_sample) return null;
            const recv = item.qty_received != null ? parseFloat(item.qty_received) : null;
            const fullRel = released + 1e-3 >= ordered && ordered > 0;
            const fullRecv = recv != null && recv + 1e-3 >= ordered && ordered > 0;
            let label, color, bg;
            if (fullRel) {
              label = "\u25CF Released";
              color = "var(--rod-green)";
              bg = "rgba(75,138,86,0.1)";
            } else if (released > 0) {
              label = parseFloat(released.toFixed(2)) + "/" + parseFloat(ordered.toFixed(2)) + " released";
              color = "var(--rod-accent)";
              bg = "rgba(168,121,53,0.1)";
            } else if (fullRecv) {
              label = "\u2713 Received";
              color = "var(--rod-green)";
              bg = "rgba(75,138,86,0.1)";
            } else if (recv != null && recv > 0) {
              label = "\u25D1 " + parseFloat(recv.toFixed(2)) + "/" + parseFloat(ordered.toFixed(2)) + " in";
              color = "#a87935";
              bg = "rgba(168,121,53,0.1)";
            } else if (showReceive) {
              label = "On order";
              color = "var(--rod-muted)";
              bg = "transparent";
            } else return null;
            const awaitingT = showReceive && !(recv != null && recv > 0) && lineAwaitingTransmit(item.id);
            if (awaitingT) {
              label = "PO not sent";
              color = "var(--rod-muted)";
              bg = "transparent";
            }
            const canReceive = showReceive && !awaitingT;
            return /* @__PURE__ */ React.createElement("div", { style: { marginTop: 4 } }, canReceive ? /* @__PURE__ */ React.createElement(
              "button",
              {
                className: "rod-li-chip",
                title: "Record received quantity",
                onClick: (e) => {
                  e.stopPropagation();
                  openReceive(item);
                },
                style: { borderColor: label === "On order" ? "rgba(28,25,23,0.22)" : color, background: bg, color }
              },
              label
            ) : /* @__PURE__ */ React.createElement("span", { className: "rod-li-chip", title: awaitingT ? "Send this line\u2019s purchase order to the vendor first" : void 0, style: { background: bg, color, borderColor: awaitingT ? "rgba(28,25,23,0.22)" : void 0 } }, label));
          })()), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-qty", style: { textAlign: "right" } }, isLabor ? item.rate_type === "per_sqft" ? /* @__PURE__ */ React.createElement("span", { style: { font: '400 12px/1.3 "Inter", sans-serif', color: "var(--rod-muted)" } }, "$", parseFloat(item.rate_sqft || 0).toFixed(2), "/sf \xD7 ", parseFloat(item.labor_sqft || 0).toFixed(0)) : parseFloat(item.quantity || 1) > 1 ? /* @__PURE__ */ React.createElement("span", { style: { font: '400 12px/1.3 "Inter", sans-serif', color: "var(--rod-muted)" } }, "\xD7 ", parseFloat(item.quantity).toFixed(0)) : /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)" } }, "\u2014") : /* @__PURE__ */ React.createElement(React.Fragment, null, item.sell_by === "roll" ? item.sqft_needed || 0 : item.num_boxes, " ", item.sell_by === "unit" ? "unit" + (item.num_boxes > 1 ? "s" : "") : item.sell_by === "piece" ? "pc" + (item.num_boxes > 1 ? "s" : "") : item.sell_by === "roll" ? "sf" : "box" + (item.num_boxes > 1 ? "es" : ""))), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-price", style: { textAlign: "right", font: '400 13px/1.2 "Inter", sans-serif' } }, item.is_sample ? "Free" : "$" + parseFloat(item.unit_price || 0).toFixed(2)), showCost && /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" }, onClick: (e) => e.stopPropagation() }, item.is_sample || isLabor ? /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)", fontSize: "0.75rem" } }, "\u2014") : editingCostItem === item.id ? /* @__PURE__ */ React.createElement("div", { style: { display: "inline-flex", alignItems: "center", border: "1px solid var(--rod-accent)", overflow: "hidden" } }, /* @__PURE__ */ React.createElement("span", { style: { padding: "0.15rem 0 0.15rem 0.3rem", fontSize: "0.7rem", color: "var(--stone-400)" } }, "$"), /* @__PURE__ */ React.createElement(
            "input",
            {
              type: "text",
              inputMode: "decimal",
              autoFocus: true,
              value: costDraft,
              onChange: (e) => {
                const v = e.target.value;
                if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) setCostDraft(v);
              },
              onKeyDown: (e) => {
                if (e.key === "Enter") e.target.blur();
                if (e.key === "Escape") {
                  setEditingCostItem(null);
                  setCostDraft("");
                }
              },
              onBlur: () => saveItemCost(item.id),
              style: { width: "52px", padding: "0.15rem 0.3rem", fontSize: "0.7rem", border: "none", outline: "none", background: "transparent" }
            }
          )) : /* @__PURE__ */ React.createElement(
            "button",
            {
              title: "Set vendor cost",
              onClick: () => {
                setEditingCostItem(item.id);
                setCostDraft(item.cost != null ? parseFloat(item.cost).toFixed(2) : item.vendor_cost != null ? parseFloat(item.vendor_cost).toFixed(2) : "");
              },
              style: { padding: "0.1rem 0.3rem", font: '400 13px/1.2 "Inter", sans-serif', border: "none", borderBottom: "1px dashed rgba(28,25,23,0.3)", background: "transparent", cursor: "pointer", color: "var(--rod-ink)" }
            },
            item.cost != null ? "$" + parseFloat(item.cost).toFixed(2) : item.vendor_cost ? "$" + parseFloat(item.vendor_cost).toFixed(2) : "\u2014"
          )), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-subtotal", style: { textAlign: "right", font: '400 14px/1 "Cormorant Garamond", serif', letterSpacing: "-0.008em" } }, item.is_sample ? "Free" : item.subtotal ? "$" + parseFloat(item.subtotal).toFixed(2) : "\u2014"), /* @__PURE__ */ React.createElement("span", { className: "rod-li-chev" + (open ? " open" : "") }, "\u25BE")), open && /* @__PURE__ */ React.createElement("div", { className: "rod-li-detail" }, /* @__PURE__ */ React.createElement("div", { className: "rod-li-detail-grid" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, isLabor ? "Service" : "SKU"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value" }, isLabor ? [item.labor_category ? String(item.labor_category).replace(/_/g, " ") : null, item.source_estimate_area || null].filter(Boolean).join(" \xB7 ") || "Service" : isCustom ? "Custom item" : fli.sku || "\u2014")), !isLabor && item.category_name && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "Category"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value" }, item.category_name)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "Vendor"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value" }, isLabor ? "Roma" : item.vendor_name || (isCustom ? "Custom" : "Roma"), !isLabor && item.vendor_public_code ? " \xB7 " + item.vendor_public_code : "")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "PO"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value" }, isLabor ? "Labor \xB7 no PO" : po ? /* @__PURE__ */ React.createElement("a", { style: { color: "var(--rod-ink)", textDecoration: "underline rgba(28,25,23,0.25)", cursor: "pointer" }, onClick: () => {
            const poEl = document.querySelector(".rod-po-section");
            if (poEl) poEl.scrollIntoView({ behavior: "smooth" });
          } }, po.po_number) : item.is_sample ? "Sample \xB7 no PO" : "No PO")), !isLabor && item.sell_by !== "unit" && item.sell_by !== "piece" && item.sqft_needed && parseFloat(item.sqft_needed) > 0 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "Coverage"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value" }, parseFloat(item.sqft_needed).toFixed(0), " sf")), !item.is_sample && !isLabor && (() => {
            const recv = item.qty_received != null ? parseFloat(item.qty_received) : null;
            const cap = recv != null ? Math.min(recv, ordered) : 0;
            const avail = Math.max(0, cap - released);
            return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "Received"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value", style: { color: recv == null ? "var(--rod-muted)" : void 0 } }, recv == null ? "Not recorded" : parseFloat(recv.toFixed(2)) + " of " + parseFloat(ordered.toFixed(2)))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "To release"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value", style: { color: avail > 0 ? void 0 : "var(--rod-muted)" } }, parseFloat(avail.toFixed(2)))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "Released"), /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-value", style: { color: released > 0 ? released + 1e-3 >= ordered ? "var(--rod-green)" : "var(--rod-accent)" : "var(--rod-muted)" } }, released > 0 ? parseFloat(released.toFixed(2)) + " of " + parseFloat(ordered.toFixed(2)) : "None")));
          })()), itemAdj.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12, fontSize: "0.6875rem", color: "var(--stone-500)" } }, /* @__PURE__ */ React.createElement("div", { className: "rod-li-cell-label" }, "Price history"), itemAdj.map((a) => /* @__PURE__ */ React.createElement("div", { key: a.id, style: { marginBottom: "0.15rem" } }, "$", parseFloat(a.previous_unit_price).toFixed(2), " \u2192 $", parseFloat(a.new_unit_price).toFixed(2), a.reason && /* @__PURE__ */ React.createElement("span", null, " \u2014 ", a.reason), /* @__PURE__ */ React.createElement("span", { style: { marginLeft: "0.5rem", color: "var(--stone-400)" } }, "by ", a.rep_name, " on ", new Date(a.created_at).toLocaleDateString()))))));
        });
      })(),
      /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "1px solid rgba(28,25,23,0.13)" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, marginBottom: 8 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)" } }, "Invoice note"), !editingInvoiceNote && /* @__PURE__ */ React.createElement(
        "a",
        {
          onClick: () => {
            setInvoiceNoteText(order.invoice_notes || "");
            setEditingInvoiceNote(true);
          },
          style: { cursor: "pointer", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--rod-accent)" }
        },
        order.invoice_notes ? "\u270E Edit" : "+ Add"
      )), editingInvoiceNote ? /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
        "textarea",
        {
          value: invoiceNoteText,
          onChange: (e) => setInvoiceNoteText(e.target.value),
          autoFocus: true,
          maxLength: 2e3,
          placeholder: "Note to the customer on " + (order.order_number || "this order") + " \u2014 prints on the invoice\u2026",
          style: { width: "100%", minHeight: 72, padding: 8, background: "transparent", border: "0.5px solid rgba(28,25,23,0.2)", outline: "none", resize: "vertical", boxSizing: "border-box", font: "400 13px/1.4 Inter, sans-serif", color: "var(--rod-ink)" }
        }
      ), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 6, marginTop: 6, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("span", { style: { marginRight: "auto", font: "400 10px/1.3 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-muted)" } }, "Prints on the invoice PDF"), /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => {
            setEditingInvoiceNote(false);
            setInvoiceNoteText("");
          },
          style: { padding: "4px 9px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", cursor: "pointer" }
        },
        "Cancel"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          disabled: savingInvoiceNote,
          onClick: async () => {
            setSavingInvoiceNote(true);
            try {
              const res = await repFetch("/api/rep/orders/" + editId + "/invoice-notes", { method: "PUT", body: JSON.stringify({ invoice_notes: invoiceNoteText.trim() }) });
              if (res && res.order) setOrder((o) => ({ ...o, invoice_notes: res.order.invoice_notes }));
              setEditingInvoiceNote(false);
              setInvoiceNoteText("");
            } catch (err) {
              alert(err.message || "Failed to save note");
            }
            setSavingInvoiceNote(false);
          },
          style: { padding: "4px 9px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "none", background: "#1c1917", color: "#ece5d8", cursor: savingInvoiceNote ? "default" : "pointer" }
        },
        savingInvoiceNote ? "Saving\u2026" : "Save note"
      ))) : /* @__PURE__ */ React.createElement(RovMicro, { style: { whiteSpace: "pre-wrap" } }, order.invoice_notes ? order.invoice_notes : "No note \xB7 prints on the invoice PDF"))
    ), !posLoading && purchaseOrders.length === 0 && order && !["cancelled", "refunded"].includes(order.status) && /* @__PURE__ */ React.createElement(RodCard, { title: "Vendor POs" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 20px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { maxWidth: 460 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 Inter, sans-serif", color: "var(--rod-ink)" } }, "No vendor POs yet."), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11.5px/1.5 Inter, sans-serif", color: "var(--rod-muted)", marginTop: 4 } }, "Generate draft POs to reserve material with the vendor \u2014 you can send them now even if the customer hasn\u2019t paid yet. Drafts don\u2019t transmit until you send each one.")), /* @__PURE__ */ React.createElement(
      "button",
      {
        disabled: posGenerating,
        onClick: generatePOs,
        style: { padding: "9px 16px", border: "none", background: "#1c1917", color: "#ece5d8", font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", cursor: posGenerating ? "default" : "pointer", whiteSpace: "nowrap" }
      },
      posGenerating ? "Generating\u2026" : "Generate POs \xB7 reserve material"
    ))), purchaseOrders.length > 0 && /* @__PURE__ */ React.createElement(RodCard, { title: "Vendor POs \xB7 " + sentPOCount + " of " + purchaseOrders.length + " transmitted" }, /* @__PURE__ */ React.createElement("input", { type: "file", ref: confirmFileRef, onChange: handleConfirmationFile, accept: "application/pdf,image/*", style: { display: "none" } }), /* @__PURE__ */ React.createElement("div", { className: "rod-po-section", style: { padding: "0.875rem 1rem 0.25rem" } }, posLoading ? /* @__PURE__ */ React.createElement("div", { style: { padding: "18px", fontSize: "0.8125rem", color: "var(--rod-muted)" } }, "Loading...") : purchaseOrders.map((po, gi) => {
      const chip = poChip(po);
      const poItems = po.items || [];
      const receivedCount = poItems.filter((i) => i.status === "received").length;
      const isDraft = po.status === "draft";
      const ediSent = po.sent_via === "edi" || !!po.edi_interchange_id;
      const ackLine = poAckLine(po);
      const lineAck = poLineAckSummary(po);
      const poCost = parseFloat(po.subtotal || 0);
      const menuOpen = poMenuOpen === po.id;
      const INK = "#1c1917", MUTED = "#8a7e68", GREEN = "#3a7a4e", WARM = "#d8cdb6";
      const openPdf = async () => {
        try {
          const url = await repOpenDoc("/api/rep/purchase-orders/" + po.id + "/pdf?preview=true");
          setDocPreview({ url, title: "Purchase Order - " + po.po_number, type: "po", po });
        } catch (e) {
          alert("Failed to load PO");
        }
      };
      const qtyUnit = (it) => it.sell_by === "unit" || it.sell_by === "piece" ? "ea" : it.sell_by === "roll" ? "rl" : "bx";
      return /* @__PURE__ */ React.createElement("div", { key: po.id, style: { border: "0.5px solid rgba(28,25,23,0.15)", background: "#f4efe5", marginBottom: "0.875rem" } }, /* @__PURE__ */ React.createElement("div", { style: {
        padding: "0.875rem 1.125rem",
        display: "grid",
        gridTemplateColumns: showCost ? "1fr auto auto" : "1fr auto",
        gap: "1.125rem",
        alignItems: "center",
        borderBottom: "0.5px solid rgba(28,25,23,0.1)"
      } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 4, font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: isDraft ? MUTED : GREEN } }, /* @__PURE__ */ React.createElement("span", null, "PO ", gi + 1, " of ", purchaseOrders.length), ediSent && /* @__PURE__ */ React.createElement("span", { style: { padding: "2px 6px", background: INK, color: "#ece5d8", letterSpacing: "0.12em" } }, "EDI 850")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 18px/1.2 var(--roma-serif)", color: INK, letterSpacing: "-0.012em", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, /* @__PURE__ */ React.createElement("a", { style: { cursor: "pointer", color: INK, textDecoration: "underline rgba(28,25,23,0.2)" }, onClick: openPdf }, po.po_number), " \xB7 ", po.vendor_name, po.vendor_public_code ? /* @__PURE__ */ React.createElement("span", { style: { color: MUTED, fontSize: 14 } }, " \xB7 ", po.vendor_public_code) : null), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: MUTED, textTransform: "uppercase", marginTop: 3 } }, poItems.length, " line", poItems.length !== 1 ? "s" : "", receivedCount > 0 ? " \xB7 " + receivedCount + " received" : "", " ", "\xB7", " ", ackLine), lineAck && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-accent)", textTransform: "uppercase", marginTop: 2 } }, lineAck)), showCost && /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: MUTED } }, "Vendor cost"), /* @__PURE__ */ React.createElement("div", { style: { font: "300 22px/1 var(--roma-serif)", color: INK, letterSpacing: "-0.012em", marginTop: 4 } }, "$", poCost.toFixed(2))), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement(RovChip, { tone: chip.tone }, chip.label))), poItems.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "0.25rem 1.125rem" } }, poItems.map((item, li) => /* @__PURE__ */ React.createElement("div", { key: item.id, style: {
        padding: "0.5rem 0",
        display: "grid",
        gridTemplateColumns: showCost ? "1fr 62px 72px 80px" : "1fr 62px",
        gap: "0.75rem",
        alignItems: "center",
        borderBottom: li < poItems.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none"
      } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.2 var(--roma-sans)", color: INK, letterSpacing: "-0.005em", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, item.product_name || item.description || "Line item"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: MUTED, textTransform: "uppercase", marginTop: 2 } }, item.vendor_sku || "\u2014", item.edi_line_status && item.edi_line_status !== "accepted" ? " \xB7 " + item.edi_line_status : "")), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "400 12px/1.2 var(--roma-serif)", color: INK } }, parseFloat(item.qty || 0), " ", qtyUnit(item), qtyUnit(item) === "bx" && parseFloat(item.sqft_per_box || 0) > 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "400 9px/1.3 ui-monospace, monospace", color: MUTED, letterSpacing: "0.04em", marginTop: 2 } }, (parseFloat(item.qty || 0) * parseFloat(item.sqft_per_box)).toFixed(1), " sf")), showCost && /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "400 12px/1 var(--roma-serif)", color: MUTED } }, "$", parseFloat(item.cost || 0).toFixed(2)), showCost && /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "400 13px/1 var(--roma-serif)", color: INK } }, "$", parseFloat(item.subtotal || 0).toFixed(2))))), /* @__PURE__ */ React.createElement("div", { style: { padding: "0.5rem 1.125rem 0.75rem", borderTop: "0.5px solid rgba(28,25,23,0.07)" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, marginBottom: 6 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: MUTED } }, "Note to vendor ", "\xB7", " printed on this PO"), isDraft && editingPoNoteId !== po.id && /* @__PURE__ */ React.createElement(
        "a",
        {
          onClick: () => {
            setEditingPoNoteId(po.id);
            setPoNoteText(po.notes || "");
          },
          style: { cursor: "pointer", font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--rod-accent)" }
        },
        po.notes ? "\u270E Edit" : "+ Add"
      )), editingPoNoteId === po.id ? /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
        "textarea",
        {
          value: poNoteText,
          onChange: (e) => setPoNoteText(e.target.value),
          autoFocus: true,
          rows: 2,
          maxLength: 2e3,
          placeholder: "e.g. Confirm lead time \xB7 deliver to will-call \xB7 hold for pickup Fri",
          style: { width: "100%", padding: "7px 9px", resize: "vertical", boxSizing: "border-box", font: "400 12px/1.5 var(--roma-sans)", color: INK, border: "0.5px solid rgba(28,25,23,0.2)", background: "#fff" }
        }
      ), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, marginTop: 6, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => {
            setEditingPoNoteId(null);
            setPoNoteText("");
          },
          style: { padding: "4px 9px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", cursor: "pointer" }
        },
        "Cancel"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          disabled: savingPoNote,
          onClick: async () => {
            const v = poNoteText.trim();
            setSavingPoNote(true);
            try {
              await repFetch("/api/rep/purchase-orders/" + po.id, { method: "PUT", body: JSON.stringify({ notes: v }) });
              setEditingPoNoteId(null);
              setPoNoteText("");
              loadPOs();
            } catch (err) {
              alert(err.message || "Failed to save note");
            }
            setSavingPoNote(false);
          },
          style: { padding: "4px 9px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "none", background: "#1c1917", color: "#ece5d8", cursor: savingPoNote ? "default" : "pointer" }
        },
        savingPoNote ? "Saving\u2026" : "Save note"
      ))) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 12px/1.5 var(--roma-sans)", color: po.notes ? INK : MUTED, whiteSpace: "pre-wrap" } }, po.notes || "No note added")), /* @__PURE__ */ React.createElement("div", { style: {
        padding: "0.625rem 1.125rem",
        background: WARM,
        display: "grid",
        gridTemplateColumns: "1fr auto",
        gap: "0.875rem",
        alignItems: "center"
      } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: MUTED, textTransform: "uppercase" } }, isDraft ? "Draft \xB7 not yet transmitted" : poChannel(po) + (po.approved_at ? " \xB7 sent " + rodFmtDT(po.approved_at) : "")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.375rem", alignItems: "center", position: "relative" } }, isDraft ? /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light primary", style: { padding: "0.375rem 0.875rem" }, onClick: () => approvePO(po.id), disabled: poUpdating }, "Review & send \u2192") : /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", style: { padding: "0.375rem 0.875rem" }, onClick: () => setPoEmailModal(po), disabled: poUpdating }, "Resend"), /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", style: { padding: "0.375rem 0.625rem" }, "aria-label": "PO actions", onClick: () => setPoMenuOpen((m) => m === po.id ? null : po.id) }, "\u22EF"), menuOpen && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rov-po-menu-scrim", onClick: () => setPoMenuOpen(null) }), /* @__PURE__ */ React.createElement("div", { className: "rov-po-menu", role: "menu" }, /* @__PURE__ */ React.createElement("div", { className: "rov-po-menu-head" }, po.po_number), /* @__PURE__ */ React.createElement("button", { className: "rov-po-menu-item", role: "menuitem", onClick: () => {
        setPoMenuOpen(null);
        navigate("purchase-order-detail", po.id);
      } }, /* @__PURE__ */ React.createElement("span", { className: "rov-po-menu-ico" }, "\u2197"), "View PO detail"), /* @__PURE__ */ React.createElement("button", { className: "rov-po-menu-item", role: "menuitem", onClick: () => {
        setPoMenuOpen(null);
        openPdf();
      } }, /* @__PURE__ */ React.createElement("span", { className: "rov-po-menu-ico" }, "\u25A4"), "View PDF"), /* @__PURE__ */ React.createElement("button", { className: "rov-po-menu-item", role: "menuitem", disabled: confirmUploadingPoId === po.id, onClick: () => {
        setPoMenuOpen(null);
        pickConfirmationFor(po.id);
      } }, /* @__PURE__ */ React.createElement("span", { className: "rov-po-menu-ico" }, "\u{1F4CE}"), confirmUploadingPoId === po.id ? "Uploading\u2026" : "Attach confirmation"))))));
    }))), releases.length > 0 && /* @__PURE__ */ React.createElement(RodCard, { title: "Material releases \xB7 " + releases.filter((r) => r.status !== "void").length }, /* @__PURE__ */ React.createElement("div", { className: "rod-po-section" }, releases.map((rel) => {
      const isDelivery = rel.release_method === "delivery";
      const isWC = rel.release_method === "will_call";
      const chipTone = rel.status === "void" ? "soft" : rel.status === "completed" ? "live" : "hold";
      const chipLabel = rel.status === "void" ? "Voided" : rel.status === "completed" ? isDelivery ? "Delivered" : "Picked up" : "Authorized";
      const qty = parseFloat(rel.total_qty || 0);
      return /* @__PURE__ */ React.createElement("div", { className: "rov-po-row", key: rel.id, style: rel.status === "void" ? { opacity: 0.55 } : void 0 }, /* @__PURE__ */ React.createElement("div", { className: "rov-po-code" }, isDelivery ? "DLV" : isWC ? "WC" : "PU"), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: '400 14px/1.2 "Inter", sans-serif', color: "var(--rod-ink)" } }, /* @__PURE__ */ React.createElement(
        "a",
        {
          style: { cursor: "pointer", textDecoration: "underline rgba(28,25,23,0.2)", color: "var(--rod-ink)" },
          onClick: () => repOpenDoc("/api/rep/releases/" + rel.id + "/release-form", true).catch((err) => alert(err.message))
        },
        rel.release_number
      ), " \xB7 " + (isDelivery ? "Delivery" : isWC ? "Will-call" : "Pickup"), isWC && rel.po_number ? " \xB7 PO " + rel.po_number : ""), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 3 } }, parseFloat(qty.toFixed(2)), " across ", rel.line_count, " line", rel.line_count === 1 ? "" : "s")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: '400 13px/1.3 "Inter", sans-serif', color: "var(--rod-ink)" } }, rel.recipient_name || "\u2014"), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 3 } }, "Released ", rodFmtDT(rel.released_at), rel.completed_at ? " \xB7 Done " + rodFmtDT(rel.completed_at) : "")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement(RovChip, { tone: chipTone }, chipLabel), rel.status !== "void" && order.customer_email && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", style: { padding: "6px 10px", fontSize: 10 }, disabled: releaseUpdating === rel.id, onClick: () => emailRelease(rel) }, "Email"), rel.status === "released" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", style: { padding: "6px 10px", fontSize: 10 }, disabled: releaseUpdating === rel.id, onClick: () => releaseAction(rel, "complete") }, isDelivery ? "Mark delivered" : "Mark picked up"), /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn danger", style: { padding: "6px 10px", fontSize: 10 }, disabled: releaseUpdating === rel.id, onClick: () => releaseAction(rel, "void") }, "Void")), rel.status === "completed" && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn danger", style: { padding: "6px 10px", fontSize: 10 }, disabled: releaseUpdating === rel.id, onClick: () => releaseAction(rel, "void") }, "Void")));
    }))), isInstall && /* @__PURE__ */ React.createElement(
      RodCard,
      {
        title: "Installation",
        action: !isDeliveredOrder && !orderIsDead && !showScheduleForm ? order.install_scheduled_at ? "Reschedule" : "\uFF0B Schedule" : void 0,
        onAction: openScheduleForm
      },
      showScheduleForm ? /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 20px 16px" } }, /* @__PURE__ */ React.createElement("div", { className: "resp-grid-2", style: { gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Install date *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          className: "rod-input",
          type: "date",
          value: scheduleForm.date,
          onChange: (e) => setScheduleForm({ ...scheduleForm, date: e.target.value })
        }
      )), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Arrival window"), /* @__PURE__ */ React.createElement(
        "input",
        {
          className: "rod-input",
          type: "text",
          maxLength: 50,
          placeholder: "e.g. 8:00 AM \u2013 12:00 PM",
          value: scheduleForm.window,
          onChange: (e) => setScheduleForm({ ...scheduleForm, window: e.target.value })
        }
      ))), /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.75rem" } }, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Crew"), /* @__PURE__ */ React.createElement(
        "input",
        {
          className: "rod-input",
          type: "text",
          maxLength: 120,
          placeholder: "Roma install crew",
          value: scheduleForm.crew,
          onChange: (e) => setScheduleForm({ ...scheduleForm, crew: e.target.value })
        }
      )), /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.75rem" } }, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Notes (crew + customer)"), /* @__PURE__ */ React.createElement(
        "textarea",
        {
          className: "rod-input",
          rows: 2,
          maxLength: 2e3,
          placeholder: "Access, parking, prep, anything the crew or customer should know\u2026",
          value: scheduleForm.notes,
          onChange: (e) => setScheduleForm({ ...scheduleForm, notes: e.target.value })
        }
      )), /* @__PURE__ */ React.createElement("label", { style: { display: "flex", alignItems: "center", gap: 8, marginTop: "0.75rem", font: "400 12px/1.4 Inter, sans-serif", color: "var(--rod-ink)" } }, /* @__PURE__ */ React.createElement("input", { type: "checkbox", checked: scheduleForm.notify, onChange: (e) => setScheduleForm({ ...scheduleForm, notify: e.target.checked }) }), "Email the customer the scheduled date"), /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.85rem", display: "flex", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", onClick: saveSchedule, disabled: savingSchedule || !scheduleForm.date }, savingSchedule ? "Saving\u2026" : scheduleForm.notify ? "Save & notify customer" : "Save schedule"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => setShowScheduleForm(false), disabled: savingSchedule }, "Cancel"))) : order.install_scheduled_at ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rov-fulfill-grid" }, [
        ["Install date", new Date(order.install_scheduled_at).toLocaleDateString("en-US", { weekday: "short", year: "numeric", month: "long", day: "numeric" })],
        ["Window", order.install_window || "\u2014"],
        ["Crew", order.install_crew || "Roma install crew"],
        ["Notes", order.install_notes || "\u2014"]
      ].map(([k, v]) => /* @__PURE__ */ React.createElement("div", { key: k, className: "rov-fulfill-cell" }, /* @__PURE__ */ React.createElement(RovMicro, null, k), /* @__PURE__ */ React.createElement("div", { className: "rov-fulfill-value" }, v)))), /* @__PURE__ */ React.createElement("div", { style: { padding: "2px 20px 16px" } }, /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: async () => {
        try {
          const url = await repOpenDoc("/api/rep/orders/" + editId + "/work-order?preview=true");
          setDocPreview({ url, title: "Work order \xB7 " + order.order_number, downloadPath: "/api/rep/orders/" + editId + "/work-order" });
        } catch (e) {
          alert("Failed to load work order");
        }
      } }, "Crew work order PDF \u2193"))) : /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 20px 18px" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: "var(--rod-muted)", marginBottom: 10 } }, "No install date yet. Book the date to put it on the customer's calendar and generate the crew work order."), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", onClick: openScheduleForm }, "\uFF0B Schedule install"))
    ), /* @__PURE__ */ React.createElement(
      RodCard,
      {
        title: "Fulfillment",
        action: !isInstall && ["pending", "confirmed"].includes(order.status) ? showFulfillmentMenu ? "Close" : "Change method" : void 0,
        onAction: () => setShowFulfillmentMenu((s) => !s)
      },
      showFulfillmentMenu && (() => {
        const applyNoFreight = async (method) => {
          const label = method === "will_call" ? "will-call at the distributor" : "store pickup";
          if (!confirm("Switch this order to " + label + "? Any carrier freight will be set to $0.")) return;
          setDeliveryUpdating(true);
          try {
            const data = await repFetch("/api/rep/orders/" + editId + "/delivery-method", { method: "PUT", body: JSON.stringify({ delivery_method: method }) });
            if (data.order) setOrder(data.order);
            if (data.balance) setBalanceInfo(data.balance);
            setShowFulfillmentMenu(false);
            setShowDeliveryForm(false);
          } catch (err) {
            alert(err.message || "Failed to change method");
          }
          setDeliveryUpdating(false);
        };
        const openShipping = () => {
          setDeliveryAddress({ line1: order.shipping_address_line1 || "", line2: order.shipping_address_line2 || "", city: order.shipping_city || "", state: order.shipping_state || "", zip: order.shipping_zip || "" });
          setShippingOptions(null);
          setShowDeliveryForm(true);
          setShowFulfillmentMenu(false);
        };
        const opts = [
          { key: "shipping", label: "Shipping", sub: "Carrier freight to the delivery address", active: !noFreight, onClick: openShipping },
          { key: "pickup", label: "Store pickup", sub: "Customer collects at Roma \xB7 Anaheim", active: isPickup, onClick: () => applyNoFreight("pickup") },
          // Will-call = pickup at the distributor. Lock it once Roma has
          // received any material — the goods are already in the showroom, so
          // the customer can't collect them at the distributor. [[will-call-distributor]]
          {
            key: "will_call",
            label: "Will-call at distributor",
            sub: "Customer collects at the distributor",
            active: isWillCall,
            locked: !isWillCall && anyReceived,
            lockReason: "Materials already received at the showroom",
            onClick: () => applyNoFreight("will_call")
          }
        ];
        return /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 20px 4px", display: "flex", flexDirection: "column", gap: 8 } }, opts.map((o) => {
          const disabled = deliveryUpdating || o.active || o.locked;
          return /* @__PURE__ */ React.createElement(
            "button",
            {
              key: o.key,
              type: "button",
              disabled,
              onClick: o.locked ? void 0 : o.onClick,
              title: o.locked ? o.lockReason : void 0,
              style: {
                textAlign: "left",
                padding: "10px 14px",
                border: "0.5px solid " + (o.active ? "var(--gold)" : "var(--rod-border)"),
                background: o.active ? "#faf3e4" : o.locked ? "#f6f5f3" : "#fff",
                cursor: o.locked ? "not-allowed" : o.active ? "default" : "pointer",
                display: "flex",
                alignItems: "center",
                justifyContent: "space-between",
                gap: 12,
                opacity: o.locked ? 0.55 : deliveryUpdating && !o.active ? 0.5 : 1
              }
            },
            /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { font: "500 13px/1.3 Inter, sans-serif", color: "var(--rod-ink)" } }, o.label), /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 11.5px/1.4 Inter, sans-serif", color: "var(--rod-muted)", marginTop: 2 } }, o.locked ? o.lockReason : o.sub)),
            o.active ? /* @__PURE__ */ React.createElement("span", { style: { font: "600 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a6d3b", flexShrink: 0 } }, "Current") : o.locked ? /* @__PURE__ */ React.createElement("span", { style: { font: "600 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--rod-muted)", flexShrink: 0 } }, "Unavailable") : null
          );
        }));
      })(),
      /* @__PURE__ */ React.createElement("div", { className: "rov-fulfill-grid" }, [
        ["Method", isWillCall ? "Will-call at distributor" : isPickup ? "Store pickup \xB7 will-call" : isInstall ? "Installation \xB7 job site" : "Shipping" + (order.shipping_method === "quote_pending" ? " \xB7 freight quote pending" : order.shipping_method ? " \xB7 " + order.shipping_method : "")],
        [isInstall ? "Crew" : "Carrier", order.shipping_carrier || (isWillCall ? "Customer collects at distributor" : isPickup ? "Customer collects" : isInstall ? "Roma install crew" : "TBD")],
        [order.status === "delivered" ? isInstall ? "Job complete" : noFreight ? "Picked up" : "Delivered" : isInstall ? "Install started" : "Shipped", order.shipped_at ? rodFmtDT(order.shipped_at) : "\u2014"],
        [noFreight ? "Pickup at" : isInstall ? "Job site" : "Deliver to", isWillCall ? "Distributor will-call \xB7 pickup location & PO on the release form" : isPickup ? "Roma Flooring Designs \xB7 1440 S. State College Blvd., Suite 6M \xB7 Anaheim, CA 92806" : [order.shipping_address_line1, order.shipping_address_line2, [order.shipping_city, order.shipping_state].filter(Boolean).join(", ") + (order.shipping_zip ? " " + order.shipping_zip : "")].filter(Boolean).join(" \xB7 ") || "TBD"],
        ["Site contact", [order.customer_name, order.phone].filter(Boolean).join(" \xB7 ") || "\u2014"],
        ["Status", statusLabels[order.status] || order.status]
      ].map(([k, v]) => /* @__PURE__ */ React.createElement("div", { key: k, className: "rov-fulfill-cell" }, /* @__PURE__ */ React.createElement(RovMicro, null, k), /* @__PURE__ */ React.createElement("div", { className: "rov-fulfill-value" }, v)))),
      order.tracking_number && /* @__PURE__ */ React.createElement("div", { className: "rov-card-band" }, /* @__PURE__ */ React.createElement(RovMicro, null, "Tracking ", order.tracking_number, order.shipping_carrier ? " \xB7 " + order.shipping_carrier : "")),
      (() => {
        const showShip = !noFreight && order.status === "confirmed";
        const showDelivered = !noFreight && order.status === "shipped";
        if (!showShip && !showDelivered) return null;
        return /* @__PURE__ */ React.createElement("div", { style: { padding: "2px 20px 16px", display: "flex", gap: 8, flexWrap: "wrap", alignItems: "center" } }, showShip && !showTrackingForm && /* @__PURE__ */ React.createElement(
          "button",
          {
            className: "rod-mini-btn primary",
            disabled: updating || !allReceived,
            title: !allReceived ? isInstall ? "Receive all materials before starting the install" : "Receive all materials before shipping" : void 0,
            onClick: () => {
              if (isInstall && !anyReleased && !confirm("No materials have been released to the job site yet. Start the install anyway?")) return;
              window.scrollTo({ top: 0, behavior: "smooth" });
              updateStatus("shipped");
            }
          },
          isInstall ? "Start install" : "Add tracking & mark shipped"
        ), showShip && !allReceived && /* @__PURE__ */ React.createElement("span", { style: { font: '400 11px/1.4 "Inter", sans-serif', color: "var(--rod-muted)" } }, recvLinesIn, " of ", recvLines.length, " lines received"), showDelivered && /* @__PURE__ */ React.createElement(
          "button",
          {
            className: "rod-mini-btn primary",
            disabled: updating,
            onClick: () => {
              const done = isInstall ? "Mark this job complete" : "Mark this order as delivered";
              const msg = hasBalanceDue ? done + "?\n\nThere is still a balance of $" + balanceInfo.balance.toFixed(2) + " due \u2014 you can collect it or send a payment request after." : done + "?";
              if (confirm(msg)) updateStatus("delivered");
            }
          },
          isInstall ? "Mark job complete" : "Mark delivered"
        ));
      })(),
      !noFreight && !isInstall && ["pending", "confirmed"].includes(order.status) && (() => {
        const freightSet = order.shipping_method && order.shipping_method !== "quote_pending";
        const showForm = !freightSet || editingFreight;
        const currentFreight = parseFloat(order.shipping || 0);
        const accent = freightSet ? "#3f7d5a" : "var(--gold)";
        const labelColor = freightSet ? "#356349" : "#8a6d3b";
        const submitFreight = async () => {
          setDeliveryUpdating(true);
          try {
            const data = await repFetch("/api/rep/orders/" + editId + "/delivery-method", { method: "PUT", body: JSON.stringify({ delivery_method: "shipping", manual_shipping_amount: parseFloat(freightAmount) }) });
            if (data.order) {
              setOrder(data.order);
              setFreightAmount("");
              setEditingFreight(false);
              flagSaved("Freight saved");
            }
            if (data.balance) setBalanceInfo(data.balance);
          } catch (err) {
          }
          setDeliveryUpdating(false);
        };
        return /* @__PURE__ */ React.createElement("div", { style: { margin: "0 20px 16px", padding: "14px 16px", background: freightSet ? "#f1f5f0" : "#faf3e4", borderLeft: "3px solid " + accent, border: "0.5px solid var(--rod-border)", display: "flex", alignItems: "center", justifyContent: "space-between", gap: "20px", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 200, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "7px", marginBottom: "5px" } }, /* @__PURE__ */ React.createElement("span", { style: { width: 6, height: 6, borderRadius: "50%", background: accent, flexShrink: 0 } }), /* @__PURE__ */ React.createElement("span", { style: { font: "600 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: labelColor, whiteSpace: "nowrap" } }, freightSet ? "Freight quoted" : "Freight quote needed")), /* @__PURE__ */ React.createElement("div", { style: { fontSize: 12.5, lineHeight: 1.5, color: "var(--rod-muted)" } }, freightSet ? "Carrier freight is on this order. Adjust it if the quote changes, then send or resend the payment request." : "Materials are paid. Enter the carrier-quoted freight to add it to this order, then send a payment request to collect.")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "8px", alignItems: "center", flexShrink: 0 } }, showForm ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { style: { position: "relative", display: "flex", alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, fontSize: 13, color: "var(--rod-muted)", pointerEvents: "none" } }, "$"), /* @__PURE__ */ React.createElement(
          "input",
          {
            className: "rod-input",
            style: { width: 116, paddingLeft: 22, textAlign: "right" },
            type: "number",
            min: "0",
            step: "0.01",
            placeholder: "0.00",
            value: freightAmount,
            onChange: (e) => setFreightAmount(e.target.value)
          }
        )), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", disabled: deliveryUpdating || !(parseFloat(freightAmount) >= 0), onClick: submitFreight }, deliveryUpdating ? "Saving\u2026" : freightSet ? "Update" : "Set freight"), freightSet && /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", disabled: deliveryUpdating, onClick: () => {
          setEditingFreight(false);
          setFreightAmount("");
        } }, "Cancel")) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", { style: { font: '400 22px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, "$" + currentFreight.toFixed(2)), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", disabled: deliveryUpdating, onClick: () => {
          setFreightAmount(currentFreight ? String(currentFreight) : "");
          setEditingFreight(true);
        } }, "Adjust"))));
      })(),
      isWillCall && paidDone && ["confirmed", "ready_for_pickup"].includes(order.status) && hasUnreleasedMaterials && (() => {
        const poReady = sentPOCount > 0;
        const accent = poReady ? "#3f7d5a" : "var(--gold)";
        const labelColor = poReady ? "#356349" : "#8a6d3b";
        return /* @__PURE__ */ React.createElement("div", { style: { margin: "0 20px 16px", padding: "14px 16px", background: poReady ? "#f1f5f0" : "#faf3e4", borderLeft: "3px solid " + accent, border: "0.5px solid var(--rod-border)", display: "flex", alignItems: "center", justifyContent: "space-between", gap: "20px", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 200, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "7px", marginBottom: "5px" } }, /* @__PURE__ */ React.createElement("span", { style: { width: 6, height: 6, borderRadius: "50%", background: accent, flexShrink: 0 } }), /* @__PURE__ */ React.createElement("span", { style: { font: "600 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: labelColor, whiteSpace: "nowrap" } }, poReady ? "Ready for will-call" : "Send the PO first")), /* @__PURE__ */ React.createElement("div", { style: { fontSize: 12.5, lineHeight: 1.5, color: "var(--rod-muted)" } }, poReady ? "Issue the customer\u2019s will-call form \u2014 it carries the distributor PO number they release against." : sentPOCount + " of " + purchaseOrders.length + " POs transmitted. Send the distributor\u2019s PO, then issue the will-call form.")), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", disabled: !poReady, onClick: () => navigate("release-flow", order.id, { method: "will_call" }) }, "Issue will-call form"));
      })(),
      showDeliveryForm && /* @__PURE__ */ React.createElement("div", { style: { padding: "0 20px 14px" } }, /* @__PURE__ */ React.createElement("div", { className: "rod-inline-panel", style: { overflow: "hidden" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-field full" }, /* @__PURE__ */ React.createElement("label", null, "Address Line 1 *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          ref: addressInputRef,
          type: "text",
          placeholder: "Start typing an address...",
          value: deliveryAddress.line1,
          onChange: (e) => setDeliveryAddress({ ...deliveryAddress, line1: e.target.value })
        }
      ), previousAddresses.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.3rem" } }, /* @__PURE__ */ React.createElement(
        "span",
        {
          style: { font: "400 0.6875rem/1 Inter, sans-serif", color: "#8a7e68", cursor: "pointer", textDecoration: "underline" },
          onClick: () => setShowPrevAddresses((s) => !s)
        },
        showPrevAddresses ? "Hide" : "Use",
        " previous address (",
        previousAddresses.length,
        ")"
      ), showPrevAddresses && /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.12)", marginTop: "0.3rem" } }, previousAddresses.map((addr, i) => /* @__PURE__ */ React.createElement(
        "div",
        {
          key: i,
          style: { padding: "0.4rem 0.75rem", cursor: "pointer", borderBottom: "0.5px solid rgba(28,25,23,0.06)" },
          onClick: () => selectPreviousAddress(addr),
          onMouseEnter: (e) => e.currentTarget.style.background = "rgba(28,25,23,0.04)",
          onMouseLeave: (e) => e.currentTarget.style.background = "transparent"
        },
        /* @__PURE__ */ React.createElement("div", { style: { font: "500 0.8125rem/1.3 Inter, sans-serif" } }, addr.line1),
        /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.6875rem/1.3 Inter, sans-serif", color: "#8a7e68" } }, [addr.city, addr.state, addr.zip].filter(Boolean).join(", "))
      ))))), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Address Line 2"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: deliveryAddress.line2,
          onChange: (e) => setDeliveryAddress({ ...deliveryAddress, line2: e.target.value })
        }
      )), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row-3" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "City *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: deliveryAddress.city,
          onChange: (e) => setDeliveryAddress({ ...deliveryAddress, city: e.target.value })
        }
      )), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "State *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: deliveryAddress.state,
          maxLength: 2,
          onChange: (e) => setDeliveryAddress({ ...deliveryAddress, state: e.target.value })
        }
      )), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "ZIP *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: deliveryAddress.zip,
          maxLength: 10,
          onChange: (e) => setDeliveryAddress({ ...deliveryAddress, zip: e.target.value })
        }
      ))), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Delivery Window"), /* @__PURE__ */ React.createElement("select", { value: deliveryWindow, onChange: (e) => setDeliveryWindow(e.target.value) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "Anytime"), /* @__PURE__ */ React.createElement("option", { value: "Morning \xB7 8a\u201312p" }, "Morning \xB7 8a\u201312p"), /* @__PURE__ */ React.createElement("option", { value: "Afternoon \xB7 12p\u20134p" }, "Afternoon \xB7 12p\u20134p"), /* @__PURE__ */ React.createElement("option", { value: "Evening \xB7 4p\u20137p" }, "Evening \xB7 4p\u20137p"))), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Preferred Carrier"), /* @__PURE__ */ React.createElement("select", { value: carrierPreference, onChange: (e) => setCarrierPreference(e.target.value) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "No preference"), /* @__PURE__ */ React.createElement("option", { value: "fedex_freight" }, "FedEx Freight"), /* @__PURE__ */ React.createElement("option", { value: "ups_freight" }, "UPS Freight"), /* @__PURE__ */ React.createElement("option", { value: "estes" }, "Estes Express"), /* @__PURE__ */ React.createElement("option", { value: "old_dominion" }, "Old Dominion"), /* @__PURE__ */ React.createElement("option", { value: "xpo" }, "XPO Logistics"), /* @__PURE__ */ React.createElement("option", { value: "roma_truck" }, "Roma Truck"), /* @__PURE__ */ React.createElement("option", { value: "other" }, "Other")))), /* @__PURE__ */ React.createElement("div", { style: {
        margin: "0.25rem -16px -14px",
        padding: "0.75rem 16px",
        background: "#d8cdb6",
        borderTop: "0.5px solid rgba(28,25,23,0.12)",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        gap: "1rem"
      } }, /* @__PURE__ */ React.createElement("span", { style: {
        font: "500 9px/1.4 ui-monospace, monospace",
        letterSpacing: "0.14em",
        textTransform: "uppercase",
        color: "#8a7e68"
      } }, "\u25CF", " ", deliveryWindow ? "Delivery window \xB7 " + deliveryWindow : "Charged to the customer \xB7 added to the order total"), /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "baseline", gap: "0.625rem", flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68" } }, "Freight estimate"), /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 16px/1 "Cormorant Garamond", serif', color: "#1c1917" } }, "$"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          inputMode: "decimal",
          value: freightAmount,
          placeholder: "0.00",
          onChange: (e) => {
            if (e.target.value === "" || /^\d*\.?\d{0,2}$/.test(e.target.value)) setFreightAmount(e.target.value);
          },
          style: {
            width: 76,
            border: "none",
            borderBottom: "0.5px solid rgba(28,25,23,0.33)",
            background: "transparent",
            outline: "none",
            font: '400 18px/1 "Cormorant Garamond", serif',
            color: "#1c1917",
            padding: "0 0 2px",
            textAlign: "right"
          }
        }
      ))))), /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.75rem", display: "flex", gap: "0.5rem", justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => {
        setShowDeliveryForm(false);
        setShippingOptions(null);
      } }, "Cancel"), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rod-mini-btn primary",
          disabled: deliveryUpdating || !deliveryAddress.line1 || !deliveryAddress.city || !deliveryAddress.state || !deliveryAddress.zip,
          onClick: async () => {
            setDeliveryUpdating(true);
            try {
              const carrierLabels = { fedex_freight: "FedEx Freight", ups_freight: "UPS Freight", estes: "Estes Express", old_dominion: "Old Dominion", xpo: "XPO Logistics", roma_truck: "Roma Truck", other: "Other" };
              const data = await repFetch("/api/rep/orders/" + editId + "/delivery-method", { method: "PUT", body: JSON.stringify({
                delivery_method: "shipping",
                shipping_address: deliveryAddress,
                manual_shipping_amount: parseFloat(freightAmount) || 0,
                shipping_carrier: carrierPreference ? carrierLabels[carrierPreference] : void 0,
                delivery_window: deliveryWindow || void 0
              }) });
              if (data.order) {
                setOrder(data.order);
                setShowDeliveryForm(false);
                setShippingOptions(null);
                setFreightAmount("");
              }
              if (data.balance) setBalanceInfo(data.balance);
            } catch (err) {
              alert(err.message || "Failed to save shipping");
            }
            setDeliveryUpdating(false);
          }
        },
        deliveryUpdating ? "Saving\u2026" : "Save shipping"
      )))
    ), (payments.length > 0 || paymentRequests.length > 0) && /* @__PURE__ */ React.createElement(RodCard, { title: "Payments \xB7 " + payments.length + " tender" + (payments.length !== 1 ? "s" : "") }, payments.map((p) => {
      const amt = parseFloat(p.amount);
      const state = p.status === "pending" ? "Clearing" : p.status === "failed" ? "Failed" : p.payment_type === "refund" || amt < 0 ? "Refunded" : "Captured";
      const stateColor = state === "Clearing" ? "#5c6e94" : state === "Failed" || state === "Refunded" ? "var(--rod-red)" : "var(--rod-green)";
      return /* @__PURE__ */ React.createElement("div", { className: "rov-pay-row", key: p.id }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, p.description || ({ card: "Card", cash: "Cash", check: "Check", ach: "ACH" }[p.payment_method] || p.payment_method || "Payment")), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 3 } }, rodFmtDT(p.created_at), p.stripe_payment_intent_id ? " \xB7 " + p.stripe_payment_intent_id.slice(0, 18) : p.initiated_by_name ? " \xB7 by " + p.initiated_by_name : "")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 16px/1 "Cormorant Garamond", serif', color: amt < 0 ? "var(--rod-red)" : "var(--rod-ink)" } }, amt < 0 ? "-" : "", "$", Math.abs(amt).toFixed(2)), /* @__PURE__ */ React.createElement(RovMicro, { color: stateColor, style: { marginTop: 3 } }, state)));
    }), refundTarget && /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 20px", borderTop: "0.5px solid rgba(28,25,23,0.13)", background: "var(--rod-warm)" } }, /* @__PURE__ */ React.createElement(RovMicro, null, "Refund \xB7 ", refundTarget.description || tenderMethod(refundTarget)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, marginTop: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 16px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rod-input",
        type: "number",
        min: "0.01",
        max: tenderRemaining(refundTarget),
        step: "0.01",
        value: refundAmount,
        onChange: (e) => setRefundAmount(e.target.value),
        style: { flex: 1, width: "auto" }
      }
    ), /* @__PURE__ */ React.createElement(RovMicro, { style: { whiteSpace: "nowrap" } }, "OF $", tenderRemaining(refundTarget).toFixed(2))), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rod-input",
        placeholder: "Reason (required)",
        maxLength: 200,
        value: refundReason,
        onChange: (e) => setRefundReason(e.target.value),
        style: { marginTop: 8 }
      }
    ), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 6 } }, tenderMethod(refundTarget) === "cash" ? "Cash comes out of your open drawer" : tenderMethod(refundTarget) === "check" ? "Records the check as returned \u2014 hand it back or void it" : "Refunds to the customer's card via Stripe"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, justifyContent: "flex-end", marginTop: 10 } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => setRefundTarget(null), disabled: refundSaving }, "Cancel"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rod-mini-btn primary",
        onClick: () => setShowRefundConfirm(true),
        disabled: refundSaving || !refundReason.trim() || !(parseFloat(refundAmount) > 0) || parseFloat(refundAmount) > tenderRemaining(refundTarget) + 5e-3
      },
      "Refund $" + (parseFloat(refundAmount) > 0 ? parseFloat(refundAmount).toFixed(2) : "0.00")
    ))), paymentRequests.map((pr) => /* @__PURE__ */ React.createElement("div", { className: "rov-pay-row", key: pr.id }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, "Payment link \xB7 ", pr.sent_to_email), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 3 } }, rodFmtDT(pr.created_at))), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 16px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, "$", parseFloat(pr.amount).toFixed(2)), /* @__PURE__ */ React.createElement(RovMicro, { color: pr.status === "paid" ? "var(--rod-green)" : pr.status === "pending" ? "var(--rod-accent)" : "var(--rod-red)", style: { marginTop: 3 } }, pr.status))))), /* @__PURE__ */ React.createElement(RodCard, { title: "Activity" + (orderActivity ? " \xB7 " + orderActivity.length : "") }, orderActivity === null ? /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 18px", fontSize: "0.8125rem", color: "var(--rod-muted)" } }, "Loading activity...") : orderActivity.length === 0 ? /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 18px", fontSize: "0.8125rem", color: "var(--stone-400)" } }, "No activity recorded yet") : /* @__PURE__ */ React.createElement("div", { style: { padding: "6px 20px 14px" } }, orderActivity.map((entry) => /* @__PURE__ */ React.createElement("div", { className: "rov-activity-row", key: entry.id }, /* @__PURE__ */ React.createElement(RovMicro, null, rodFmtDT(entry.created_at)), /* @__PURE__ */ React.createElement("span", { className: "rov-activity-dot" }), /* @__PURE__ */ React.createElement("span", { style: { font: "400 13px/1.4 Inter, sans-serif", color: "var(--rod-ink)" } }, orderActionLabel(entry.action), orderActivityDetail(entry) ? " \xB7 " + orderActivityDetail(entry) : ""), /* @__PURE__ */ React.createElement(RovMicro, null, entry.performer_name || "System")))))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail" }, /* @__PURE__ */ React.createElement(RodCard, { title: "Sales rep" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded" }, order.sales_rep_id ? /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, (order.rep_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name" }, order.rep_name || "Rep", order.sales_rep_id === repInfo.id && /* @__PURE__ */ React.createElement("span", { className: "ws-you-tag" }, "You")), order.sales_rep_id === repInfo.id && repInfo.email && /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, repInfo.email))) : /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar", style: { background: "var(--brass-warm, #d8cdb6)", color: "var(--brass-muted, #8a7e68)" } }, "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name", style: { color: "var(--rod-muted)" } }, "Unassigned"), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "No rep owns this order yet"))))), /* @__PURE__ */ React.createElement(RodCard, { title: "Customer" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 12 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "38px 1fr", gap: 12, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { className: "rov-cust-avatar" }, order.customer_name ? order.customer_name.split(" ").map((n) => n[0]).join("").slice(0, 2).toUpperCase() : "?"), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.2 Inter, sans-serif", color: "var(--rod-ink)" } }, order.customer_name), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 3 } }, custType === "trade" ? "Trade pro" : custType === "retail" ? "Retail" : "Guest", customerStats ? " \xB7 " + (customerStats.total_orders || 0) + " order" + (customerStats.total_orders !== 1 ? "s" : "") + " \xB7 $" + (parseFloat(customerStats.total_spent || 0) / 1e3).toFixed(1) + "k lifetime" : ""))), /* @__PURE__ */ React.createElement(RovMicro, null, [order.customer_email, order.phone].filter(Boolean).join(" \xB7 ") || "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 16, alignItems: "baseline", flexWrap: "wrap" } }, order.customer_email && /* @__PURE__ */ React.createElement("a", { className: "rov-textlink", href: "mailto:" + order.customer_email }, "Email"), order.phone && /* @__PURE__ */ React.createElement("a", { className: "rov-textlink", href: "tel:" + order.phone }, "Call")))), /* @__PURE__ */ React.createElement(RodCard, { title: "Totals" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 12 } }, /* @__PURE__ */ React.createElement(RovRow, { l: "Subtotal \xB7 " + items.length + " line" + (items.length !== 1 ? "s" : ""), v: "$" + parseFloat(order.subtotal).toFixed(2) }), isPickup ? /* @__PURE__ */ React.createElement(RovRow, { l: "Shipping \xB7 store pickup", v: "Free", c: "var(--rod-green)" }) : /* @__PURE__ */ React.createElement(RovRow, { l: "Shipping", v: "$" + parseFloat(order.shipping || 0).toFixed(2) }), parseFloat(order.sample_shipping || 0) > 0 && /* @__PURE__ */ React.createElement(RovRow, { l: "Sample shipping", v: "$" + parseFloat(order.sample_shipping).toFixed(2) }), parseFloat(order.discount_amount || 0) > 0 && /* @__PURE__ */ React.createElement(RovRow, { l: "Discount", v: "\u2212$" + parseFloat(order.discount_amount).toFixed(2), c: "var(--rod-green)" }), order.status !== "cancelled" && !showDiscountEdit && /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rov-textlink",
        style: { background: "none", border: "none", padding: 0, justifySelf: "start", width: "fit-content", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-muted)", cursor: "pointer" },
        onClick: () => {
          setDiscountType("fixed");
          setDiscountValue("");
          setShowDiscountEdit(true);
        }
      },
      parseFloat(order.discount_amount || 0) > 0 ? "Edit discount" : "+ Add discount"
    ), showDiscountEdit && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px", border: "0.5px solid rgba(28,25,23,0.15)", background: "rgba(28,25,23,0.02)", display: "grid", gap: 8 } }, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Order discount"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, alignItems: "center" } }, /* @__PURE__ */ React.createElement("select", { className: "rod-input", value: discountType, onChange: (e) => setDiscountType(e.target.value), style: { width: "auto", flex: "0 0 64px" } }, /* @__PURE__ */ React.createElement("option", { value: "fixed" }, "$"), /* @__PURE__ */ React.createElement("option", { value: "percent" }, "%")), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rod-input",
        type: "number",
        min: "0",
        step: "0.01",
        value: discountValue,
        autoFocus: true,
        onChange: (e) => setDiscountValue(e.target.value),
        placeholder: discountType === "percent" ? "% off subtotal" : "$ off total",
        style: { flex: 1 }
      }
    )), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick: () => {
          setShowDiscountEdit(false);
          setDiscountValue("");
        },
        style: { padding: "5px 10px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", cursor: "pointer" }
      },
      "Cancel"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        disabled: savingDiscount || discountValue === "" || parseFloat(discountValue) < 0,
        onClick: async () => {
          setSavingDiscount(true);
          try {
            await repFetch("/api/rep/orders/" + editId + "/discount", { method: "PUT", body: JSON.stringify({ discount_type: discountType, discount_value: parseFloat(discountValue) || 0 }) });
            setShowDiscountEdit(false);
            setDiscountValue("");
            load();
          } catch (err) {
            alert(err.message || "Failed to apply discount");
          }
          setSavingDiscount(false);
        },
        style: { padding: "5px 10px", fontSize: 10, fontFamily: "Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", border: "none", background: "#1c1917", color: "#ece5d8", cursor: savingDiscount ? "default" : "pointer" }
      },
      savingDiscount ? "Saving\u2026" : "Apply"
    )), /* @__PURE__ */ React.createElement("div", { style: { fontSize: 10, lineHeight: 1.4, color: "var(--rod-muted)" } }, "Set to 0 to remove. Re-issues the invoice as revised if it was already sent.")), /* @__PURE__ */ React.createElement(RovRow, { l: "Amount paid", v: "$" + amountPaid.toFixed(2), c: "var(--rod-green)" }), /* @__PURE__ */ React.createElement("div", { className: "rov-total-row" }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-ink)" } }, "Total"), /* @__PURE__ */ React.createElement("span", { className: "rov-total-value" }, "$", parseFloat(order.total).toFixed(2))), balanceInfo && balanceInfo.balance_status === "paid" && /* @__PURE__ */ React.createElement("div", { className: "rov-band " + (isDeliveredOrder ? "ink" : "green") }, "\u25CF Paid in full \xB7 $0.00 balance"), balanceInfo && balanceInfo.balance_status === "credit" && /* @__PURE__ */ React.createElement("div", { className: "rov-band blue" }, "\u25CF Credit available \xB7 $", Math.abs(balanceInfo.balance).toFixed(2)), balanceInfo && balanceInfo.balance_status === "balance_due" && /* @__PURE__ */ React.createElement("div", { id: "rod-collect-anchor", className: "rov-band amber" }, "\u25CF Balance due \xB7 $", balanceInfo.balance.toFixed(2)), balanceInfo && balanceInfo.balance_status === "balance_due" && !showCollectPayment && /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rov-textlink",
        style: { background: "none", border: "none", borderBottom: "1px solid rgba(28,25,23,0.33)", justifySelf: "center", width: "fit-content" },
        onClick: () => {
          setCollectAmount(balanceInfo.balance.toFixed(2));
          setPaymentRequestEmail(order.customer_email || "");
          setPaymentRequestMessage("");
          setCollectMethod(null);
          setShowCollectPayment(true);
        }
      },
      "Collect payment"
    ), showCollectPayment && balanceInfo && balanceInfo.balance_status === "balance_due" && /* @__PURE__ */ React.createElement("div", { className: "rod-collect-wrap" }, collectMethod !== "request" ? /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Amount to collect"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.375rem" } }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 16px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, "$"), /* @__PURE__ */ React.createElement("input", { className: "rod-input", type: "number", min: "0.01", max: balanceInfo.balance, step: "0.01", value: collectAmount, onChange: (e) => setCollectAmount(e.target.value), style: { flex: 1, width: "auto" } }), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.1em", color: "var(--rod-muted)", whiteSpace: "nowrap" } }, "of $", balanceInfo.balance.toFixed(2))), parseFloat(collectAmount) > 0 && parseFloat(collectAmount) < balanceInfo.balance && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "var(--rod-accent)", marginTop: "0.375rem" } }, "Remaining after: $", (balanceInfo.balance - parseFloat(collectAmount)).toFixed(2)), (parseFloat(collectAmount) <= 0 || parseFloat(collectAmount) > balanceInfo.balance || isNaN(parseFloat(collectAmount))) && collectAmount !== "" && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "var(--rod-red)", marginTop: "0.375rem" } }, "Amount must be between $0.01 and $", balanceInfo.balance.toFixed(2))) : /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 10 } }, /* @__PURE__ */ React.createElement("span", { className: "rod-field-label", style: { marginBottom: 0 } }, "Amount requested"), /* @__PURE__ */ React.createElement("span", { style: { font: '400 16px/1 "Cormorant Garamond", serif', color: "var(--rod-ink)" } }, "$", balanceInfo.balance.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "rod-collect-methods" }, [["card", "Card"], ["cash", "Cash"], ["check", "Check"], ["request", "Request"]].map(([m, label]) => /* @__PURE__ */ React.createElement("button", { key: m, className: "rod-mini-btn" + (collectMethod === m ? " selected" : ""), onClick: () => setCollectMethod(m) }, label))), collectMethod === "card" && /* @__PURE__ */ React.createElement("div", { className: "rod-collect-note" }, terminalStatus === "connected" ? "\u2713 Valor terminal ready" : "No terminal set \u2014 manual card entry available"), collectMethod === "cash" && collectCashDrawerOpen === false && /* @__PURE__ */ React.createElement("div", { className: "rod-banner amber", style: { marginTop: 0, textAlign: "left" } }, "No cash drawer open. Open one from the Cash Drawer tab first."), collectMethod === "check" && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Check Number *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rod-input",
        type: "text",
        maxLength: 20,
        value: collectCheckNumber,
        onChange: (e) => setCollectCheckNumber(e.target.value),
        placeholder: "Enter check number"
      }
    )), collectMethod === "request" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Email link to *"), /* @__PURE__ */ React.createElement("input", { className: "rod-input", type: "email", value: paymentRequestEmail, onChange: (e) => setPaymentRequestEmail(e.target.value), placeholder: "customer@example.com" })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Message ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", color: "var(--rod-muted)" } }, "\xB7 optional")), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        className: "rod-input",
        rows: 2,
        maxLength: 2e3,
        value: paymentRequestMessage,
        onChange: (e) => setPaymentRequestMessage(e.target.value),
        placeholder: "e.g. Here's the balance for your order."
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rod-collect-note" }, "Secure Stripe link \xB7 invoice PDF attached \xB7 sends from your rep email")), collectMethod && collectMethod !== "request" && (() => {
      const amt = parseFloat(collectAmount);
      const validAmt = !isNaN(amt) && amt > 0 && amt <= balanceInfo.balance;
      const amtLabel = validAmt ? " \xB7 $" + amt.toFixed(2) : "";
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rod-mini-btn primary",
          style: { width: "100%", padding: "11px 12px" },
          disabled: collectSaving || !validAmt || collectMethod === "cash" && collectCashDrawerOpen === false || collectMethod === "check" && !collectCheckNumber.trim(),
          onClick: () => {
            if (collectMethod === "card") handleCollectCard();
            else if (collectMethod === "cash") collectPayment("cash", null);
            else if (collectMethod === "check") collectPayment("check", null);
          }
        },
        collectSaving ? "Processing..." : collectMethod === "card" ? "Pay with Card" + amtLabel : collectMethod === "cash" ? "Record Cash Payment" + amtLabel : "Record Check Payment" + amtLabel
      );
    })(), collectMethod === "request" && (() => {
      const email = (paymentRequestEmail || "").trim();
      const emailValid = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rod-mini-btn primary",
          style: { width: "100%", padding: "11px 12px" },
          disabled: updating || !emailValid,
          onClick: async () => {
            setUpdating(true);
            try {
              await repFetch("/api/rep/orders/" + editId + "/payment-request", { method: "POST", body: JSON.stringify({ message: paymentRequestMessage || void 0, recipient_email: email }) });
              alert("Payment request sent to " + email);
              const refreshed = await repFetch("/api/rep/orders/" + editId);
              setPaymentRequests(refreshed.payment_requests || []);
              setShowCollectPayment(false);
              setCollectMethod(null);
            } catch (err) {
              alert(err.message);
            }
            setUpdating(false);
          }
        },
        updating ? "Sending\u2026" : "Send payment request \xB7 $" + balanceInfo.balance.toFixed(2)
      );
    })(), /* @__PURE__ */ React.createElement("button", { className: "rov-textlink", style: { justifySelf: "center" }, onClick: () => {
      setShowCollectPayment(false);
      setCollectMethod(null);
    } }, "Cancel")))), orderMilestones.length > 0 && /* @__PURE__ */ React.createElement(RodCard, { title: "Payment schedule" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "4px 20px 14px" } }, orderMilestones.map((m, i) => {
      const amount = parseFloat(m.amount || 0);
      const remaining = parseFloat(m.remaining != null ? m.remaining : amount);
      const paidApplied = parseFloat(m.paid_applied || 0);
      const isPaid = m.status === "paid";
      const isDue = m.status === "due";
      const tone = isPaid ? "var(--rod-green)" : isDue ? "#a87935" : "var(--rod-muted)";
      return /* @__PURE__ */ React.createElement("div", { className: "rov-pay-row", key: i }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, m.label, m.percent ? " \xB7 " + parseFloat(m.percent) + "%" : ""), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, m.due_label ? m.due_label + " \xB7 " : "", isPaid ? "Paid" : isDue ? paidApplied > 0 ? "$" + paidApplied.toFixed(2) + " paid \xB7 $" + remaining.toFixed(2) + " due" : "Due now" : "Upcoming")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 12 } }, /* @__PURE__ */ React.createElement("span", { style: { font: '500 15px/1 "Cormorant Garamond", serif', color: tone } }, "$", amount.toFixed(2)), isPaid ? /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-green)", font: "500 12px/1 Inter, sans-serif" } }, "\u2713") : isDue && hasBalanceDue ? /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: () => openCollectForMilestone(m) }, "Collect \u2193") : /* @__PURE__ */ React.createElement("span", { style: { width: 8 } })));
    }))), commission && /* @__PURE__ */ React.createElement(RodCard, { title: "Margin & commission" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 10 } }, /* @__PURE__ */ React.createElement(RovRow, { l: "Vendor cost", v: "$" + parseFloat(commission.vendor_cost || 0).toFixed(2) }), /* @__PURE__ */ React.createElement(RovRow, { l: "Gross margin", v: "$" + parseFloat(commission.margin || 0).toFixed(2), c: "var(--rod-green)" }), /* @__PURE__ */ React.createElement(RovRow, { l: "Margin %", v: (parseFloat(commission.order_total || 0) > 0 ? (parseFloat(commission.margin || 0) / parseFloat(commission.order_total) * 100).toFixed(1) : "0.0") + "%", c: "var(--rod-green)" }), /* @__PURE__ */ React.createElement(RovRow, { l: "Commission \xB7 " + (parseFloat(commission.commission_rate || 0) * 100).toFixed(1) + "% of margin", v: "$" + parseFloat(commission.commission_amount || 0).toFixed(2), last: true })), /* @__PURE__ */ React.createElement("div", { className: "rov-card-band" }, /* @__PURE__ */ React.createElement(RovMicro, { color: commission.status === "forfeited" ? "var(--rod-red)" : void 0 }, commission.status === "paid" ? "\u2713 Paid out on commission statement" : commission.status === "earned" ? "\u2713 Earned \xB7 pays on next statement" : commission.status === "forfeited" ? "Forfeited" : "Releases when delivered & all tenders clear"))), /* @__PURE__ */ React.createElement(RodCard, { title: "Documents", action: "\u2399 Print", onAction: openPrintModal }, /* @__PURE__ */ React.createElement("div", { style: { padding: "8px 20px 14px" } }, isInstall && /* @__PURE__ */ React.createElement("div", { className: "rov-doc-row2" }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, "Work order"), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, "Crew job sheet \xB7 materials + scope")), /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/orders/" + editId + "/work-order?preview=true");
        setDocPreview({ url, title: "Work order \xB7 " + order.order_number, downloadPath: "/api/rep/orders/" + editId + "/work-order" });
      } catch (e) {
        alert("Failed to load work order");
      }
    } }, "PDF \u2193")), orderInvoices.length > 0 ? orderInvoices.map((iv) => /* @__PURE__ */ React.createElement("div", { className: "rov-doc-row2", key: iv.id }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, "Invoice ", iv.invoice_number), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, iv.issue_date ? "Issued " + new Date(iv.issue_date).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "Draft", " \xB7 paid $" + parseFloat(iv.amount_paid || 0).toFixed(2) + " of $" + parseFloat(iv.total || 0).toFixed(2))), /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/orders/" + editId + "/invoice?preview=true");
        setDocPreview({ url, title: "Invoice " + iv.invoice_number, downloadPath: "/api/rep/orders/" + editId + "/invoice" });
      } catch (e) {
        alert("Failed to load invoice");
      }
    } }, "PDF \u2193"))) : /* @__PURE__ */ React.createElement("div", { className: "rov-doc-row2" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, "Invoice"), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, "Customer-facing PDF")), /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/orders/" + editId + "/invoice?preview=true");
        setDocPreview({ url, title: "Invoice", downloadPath: "/api/rep/orders/" + editId + "/invoice" });
      } catch (e) {
        alert("Failed to load invoice");
      }
    } }, "PDF \u2193")), payments.filter((p) => p.valor_tran_no || p.stripe_receipt_url).map((p) => /* @__PURE__ */ React.createElement("div", { className: "rov-doc-row2", key: "rcpt-" + p.id }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, "Card receipt"), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, p.card_brand ? p.card_brand.charAt(0).toUpperCase() + p.card_brand.slice(1) + (p.card_last4 ? " \xB7\xB7\xB7\xB7" + p.card_last4 : "") : (p.payment_method || "card").toUpperCase(), " \xB7 $" + parseFloat(p.amount || 0).toFixed(2), p.valor_tran_no ? " \xB7 Valor terminal" : "")), /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/orders/" + editId + "/receipt?payment_id=" + p.id + "&preview=true");
        setDocPreview({ url, title: "Card receipt", downloadPath: "/api/rep/orders/" + editId + "/receipt?payment_id=" + p.id });
      } catch (e) {
        alert("Failed to load receipt");
      }
    } }, "PDF \u2193"))), purchaseOrders.map((po) => /* @__PURE__ */ React.createElement("div", { className: "rov-doc-row2", key: po.id }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, po.po_number), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, po.vendor_name, po.vendor_public_code ? " \xB7 " + po.vendor_public_code : "", " \xB7 $", parseFloat(po.subtotal || 0).toFixed(2))), /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/purchase-orders/" + po.id + "/pdf?preview=true");
        setDocPreview({ url, title: "Purchase Order - " + po.po_number, type: "po", po });
      } catch (e) {
        alert("Failed to load PO");
      }
    } }, "PDF \u2193"))), releases.filter((rel) => rel.status !== "void").map((rel) => /* @__PURE__ */ React.createElement("div", { className: "rov-doc-row2", key: "rel-" + rel.id }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, "Release ", rel.release_number), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, rel.release_method === "delivery" ? "Delivery" : rel.release_method === "will_call" ? "Will-call" : "Pickup", " \xB7 " + parseFloat(rel.total_qty || 0) + " across " + rel.line_count + " line" + (rel.line_count === 1 ? "" : "s"), rel.released_at ? " \xB7 " + new Date(rel.released_at).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "")), /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/releases/" + rel.id + "/release-form?preview=true");
        setDocPreview({ url, title: "Release " + rel.release_number, downloadPath: "/api/rep/releases/" + rel.id + "/release-form" });
      } catch (e) {
        alert("Failed to load release");
      }
    } }, "PDF \u2193"))), orderDocuments.map((doc) => {
      const docLabel = rovOrderDocLabel(doc);
      return /* @__PURE__ */ React.createElement("div", { className: "rov-doc-row2", key: doc.id }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)" } }, docLabel), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, doc.doc_type === "vendor_confirmation" ? (doc.file_name || "Vendor confirmation") + " \xB7 " : "", doc.mime_type ? (doc.mime_type.split("/")[1] || "file").toUpperCase() : "FILE", doc.file_size ? " \xB7 " + (doc.file_size > 1024 * 1024 ? (doc.file_size / 1024 / 1024).toFixed(1) + " MB" : Math.round(doc.file_size / 1024) + " KB") : "")), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 12, flexShrink: 0 } }, doc.url ? /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", onClick: () => setDocPreview({ url: doc.url, title: docLabel }) }, "Open \u2193") : /* @__PURE__ */ React.createElement("span", { className: "rov-doc-link" }, "\u2014"), /* @__PURE__ */ React.createElement("button", { className: "rov-doc-link", style: { color: "var(--rod-red)" }, onClick: () => deleteOrderDoc(doc), title: "Delete document" }, "Delete")));
    }))), /* @__PURE__ */ React.createElement(InternalNotesCard, { basePath: "/api/rep/orders/" + editId, bg: "#f5f0e8" }))), /* @__PURE__ */ React.createElement("div", { className: "rof-action-bar" }, /* @__PURE__ */ React.createElement("div", { className: "rof-action-left" }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)" } }, order.order_number, " \xB7 ", statusLabels[order.status] || order.status), /* @__PURE__ */ React.createElement("span", { className: "rof-action-total", style: { display: "block", marginTop: 4 } }, "$", parseFloat(order.total).toLocaleString(void 0, { minimumFractionDigits: 2, maximumFractionDigits: 2 }))), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "rgba(245,240,232,0.6)" } }, [
      order.customer_name,
      items.length + " line" + (items.length !== 1 ? "s" : ""),
      isWillCall ? "Will-call" : isPickup ? "Pickup" : "Shipping",
      balanceInfo && balanceInfo.balance > 0 ? "Balance $" + balanceInfo.balance.toLocaleString(void 0, { minimumFractionDigits: 2, maximumFractionDigits: 2 }) : "Paid in full"
    ].filter(Boolean).join(" \xB7 ")), savedMsg && /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#7fb98f" } }, "\u2713", " ", savedMsg)), !hasUnreleasedMaterials && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: () => navigate("orders") }, "Save & close"), order.status === "cancelled" && !order.stripe_refund_id && /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => {
      if (confirm("Reopen this order? It will return to Pending status.")) updateStatus("pending");
    }, disabled: updating }, "Reopen")), catalogOpen && ["pending", "confirmed"].includes(order.status) && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data: { customer: parseFloat(order.trade_discount_percent) > 0 ? { type: "trade", discount_percent: parseFloat(order.trade_discount_percent), tier_name: order.trade_tier_name } : null, newCustomerName: order.customer_name },
        showCost,
        contextLabel: "Add to order \xB7 " + order.order_number,
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog,
        onAddRug: addRugToOrder
      }
    ), rugModalItem && /* @__PURE__ */ React.createElement(
      RugConfigModal,
      {
        carpet: rugModalItem,
        showCost,
        onClose: () => setRugModalItem(null),
        onAdd: addRugToOrder
      }
    ), showSendInvoiceModal && (() => {
      const hasBalance = balanceInfo && balanceInfo.balance > 0.01;
      const noEmail = !(order.customer_email && String(order.customer_email).trim());
      return /* @__PURE__ */ React.createElement("div", { className: "rof-overlay", onMouseDown: (e) => {
        if (e.target === e.currentTarget && !sendingInvoice) setShowSendInvoiceModal(false);
      } }, /* @__PURE__ */ React.createElement("div", { className: "rof-modal-card rof-pop", style: { maxWidth: 500 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "22px 24px 16px", borderBottom: "0.5px solid rgba(28,25,23,0.1)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 8 } }, hasBalance ? "Invoice \xB7 payment request" : "Resend invoice"), /* @__PURE__ */ React.createElement("h2", { style: { margin: 0, font: '300 24px/1.15 "Cormorant Garamond", serif', letterSpacing: "-0.012em", color: "#1c1917" } }, "Email invoice for ", /* @__PURE__ */ React.createElement("em", null, order.order_number)), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: noEmail ? "#c54731" : "#8a7e68", marginTop: 8 } }, noEmail ? "No email on file for this customer" : "To \xB7 " + order.customer_email)), /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 24px" } }, hasBalance && /* @__PURE__ */ React.createElement("div", { style: { marginBottom: 16, padding: "11px 13px", background: "rgba(168,121,53,0.1)", border: "0.5px solid rgba(168,121,53,0.35)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 8.5px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#a87935", marginBottom: 5 } }, "Balance due"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.45 Inter, sans-serif", color: "#1c1917" } }, /* @__PURE__ */ React.createElement("strong", null, "$", balanceInfo.balance.toFixed(2)), " outstanding \u2014 a secure payment link is included so the customer can pay from the email.")), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Message to customer ", /* @__PURE__ */ React.createElement("span", { style: { textTransform: "none", color: "#8a7e68" } }, "\xB7 optional")), /* @__PURE__ */ React.createElement(
        "textarea",
        {
          value: invoiceMessage,
          onChange: (e) => setInvoiceMessage(e.target.value),
          rows: 3,
          maxLength: 2e3,
          placeholder: "e.g. Here is the invoice for your recent order \u2014 thank you!"
        }
      ), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", color: "#8a7e68", marginTop: 6, textTransform: "uppercase" } }, "The invoice PDF is attached \xB7 sends from your rep email"))), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 24px", borderTop: "0.5px solid rgba(28,25,23,0.1)", display: "flex", gap: 8, justifyContent: "flex-end", background: "#d8cdb6" } }, /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", onClick: () => setShowSendInvoiceModal(false), disabled: sendingInvoice, style: { padding: "0.5rem 1rem" } }, "Cancel"), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light primary",
          disabled: sendingInvoice || noEmail,
          style: { padding: "0.5rem 1.25rem" },
          onClick: async () => {
            setSendingInvoice(true);
            try {
              const data = await repFetch("/api/rep/orders/" + editId + "/send-invoice", { method: "POST", body: JSON.stringify({ message: invoiceMessage || void 0 }) });
              setShowSendInvoiceModal(false);
              const msg = data.payment_requested ? "Invoice with payment request sent to " + data.sent_to : "Invoice sent to " + data.sent_to;
              alert(msg);
              if (data.payment_requested) {
                const refreshed = await repFetch("/api/rep/orders/" + editId);
                setPaymentRequests(refreshed.payment_requests || []);
                setBalanceInfo(refreshed.balance || null);
              }
            } catch (err) {
              alert("Failed to send: " + err.message);
            }
            setSendingInvoice(false);
          }
        },
        sendingInvoice ? "Sending\u2026" : hasBalance ? "Send invoice & request" : "Send invoice"
      ))));
    })(), showRefundConfirm && refundTarget && /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => !refundSaving && setShowRefundConfirm(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation(), style: { maxWidth: 420 } }, /* @__PURE__ */ React.createElement("h3", null, "Confirm refund"), /* @__PURE__ */ React.createElement("p", { style: { margin: "0 0 0.75rem" } }, "Refund ", /* @__PURE__ */ React.createElement("strong", { style: { font: '400 20px/1 "Cormorant Garamond", serif' } }, "$", (parseFloat(refundAmount) || 0).toFixed(2)), " of the ", /* @__PURE__ */ React.createElement("strong", null, "$", parseFloat(refundTarget.amount).toFixed(2)), " ", tenderMethod(refundTarget) === "cash" ? "cash payment" : tenderMethod(refundTarget) === "check" ? "check payment" : "card payment", " on ", order.order_number, "?"), /* @__PURE__ */ React.createElement("div", { style: { padding: "0.6rem 0.75rem", background: "var(--rod-warm)", font: "400 13px/1.45 Inter, sans-serif", color: "var(--rod-ink)", marginBottom: "0.75rem" } }, tenderMethod(refundTarget) === "cash" ? "Take the cash from your drawer and hand it to the customer \u2014 the drawer\u2019s expected balance is adjusted automatically." : tenderMethod(refundTarget) === "check" ? "The check is recorded as returned \u2014 hand it back to the customer or void it." : "The amount is refunded to the customer\u2019s card through Stripe. This cannot be undone."), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: "1rem" } }, "Reason \xB7 ", refundReason.trim()), /* @__PURE__ */ React.createElement("div", { className: "modal-actions" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowRefundConfirm(false), disabled: refundSaving }, "Go back"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: submitRefund, disabled: refundSaving }, refundSaving ? "Refunding\u2026" : "Confirm refund")))), showCollectCardModal && balanceInfo && /* @__PURE__ */ React.createElement(
      RofPayCardModal,
      {
        amount: parseFloat(collectAmount),
        customerName: order.customer_name,
        customerEmail: order.customer_email,
        orderNumber: order.order_number,
        tenderIdx: payments.filter((p) => parseFloat(p.amount) > 0).length + 1,
        tenderCount: payments.filter((p) => parseFloat(p.amount) > 0).length + 1,
        onSuccess: (res) => {
          setShowCollectCardModal(false);
          collectPayment("card", res);
        },
        onCancel: () => setShowCollectCardModal(false),
        terminalRef,
        terminalStatus,
        connectReader
      }
    ), docPreview && (docPreview.type === "po" ? /* @__PURE__ */ React.createElement(
      PODocumentViewer,
      {
        url: docPreview.url,
        po: docPreview.po,
        confirmBeforeSend: false,
        onClose: () => setDocPreview(null),
        onSend: () => {
          const fresh = purchaseOrders.find((p) => p.id === docPreview.po.id) || docPreview.po;
          setDocPreview(null);
          setPoEmailModal(fresh);
        }
      }
    ) : /* @__PURE__ */ React.createElement(RepPdfViewer, { url: docPreview.url, filename: docPreview.title, downloadPath: docPreview.downloadPath, onClose: () => setDocPreview(null) })), showPrintModal && (() => {
      const selCount = printableDocs.filter((d) => printSel[d.key]).length;
      const allChecked = selCount === printableDocs.length;
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          onClick: () => !printBusy && setShowPrintModal(false),
          style: { position: "fixed", inset: 0, zIndex: 1400, background: "rgba(28,25,23,0.4)", display: "flex", alignItems: "center", justifyContent: "center", padding: 20 }
        },
        /* @__PURE__ */ React.createElement(
          "div",
          {
            onClick: (e) => e.stopPropagation(),
            style: { width: "100%", maxWidth: 424, background: "#f5f1e8", border: "0.5px solid rgba(28,25,23,0.25)", boxShadow: "0 20px 48px rgba(28,25,23,0.22)" }
          },
          /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 20px", borderBottom: "0.5px solid rgba(28,25,23,0.12)", display: "flex", alignItems: "baseline", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "Print documents"), /* @__PURE__ */ React.createElement("div", { style: { font: '400 16px/1.3 "Inter", sans-serif', color: "var(--rod-ink)" } }, order ? order.order_number : "")), /* @__PURE__ */ React.createElement(
            "button",
            {
              className: "rod-mini-btn",
              disabled: !!printBusy,
              onClick: () => {
                const sel = {};
                printableDocs.forEach((d) => {
                  sel[d.key] = !allChecked;
                });
                setPrintSel(sel);
              }
            },
            allChecked ? "None" : "All"
          )),
          /* @__PURE__ */ React.createElement("div", { style: { maxHeight: "52vh", overflowY: "auto", padding: "6px 0" } }, printableDocs.map((d) => /* @__PURE__ */ React.createElement("label", { key: d.key, style: { display: "flex", alignItems: "center", gap: 12, padding: "10px 20px", cursor: printBusy ? "default" : "pointer", borderBottom: "0.5px solid rgba(28,25,23,0.07)" } }, /* @__PURE__ */ React.createElement(
            "input",
            {
              type: "checkbox",
              checked: !!printSel[d.key],
              disabled: !!printBusy,
              onChange: (e) => setPrintSel((prev) => ({ ...prev, [d.key]: e.target.checked })),
              style: { accentColor: "#1c1917", width: 15, height: 15, flexShrink: 0, margin: 0 }
            }
          ), /* @__PURE__ */ React.createElement("span", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 13px/1.25 Inter, sans-serif", color: "var(--rod-ink)", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, d.label), d.sub && /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 2 } }, d.sub)))), printableDocs.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 20px", font: "400 13px/1.4 Inter, sans-serif", color: "var(--rod-muted)" } }, "No documents on this order yet")),
          /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "0.5px solid rgba(28,25,23,0.12)", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 8 } }, /* @__PURE__ */ React.createElement(RovMicro, null, selCount, " selected \xB7 one print job each"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8 } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", disabled: !!printBusy, onClick: () => setShowPrintModal(false) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", disabled: !!printBusy || selCount === 0, onClick: printSelectedDocs }, printBusy || "\u2399 Print" + (selCount > 1 ? " " + selCount : ""))))
        )
      );
    })(), poEmailModal && /* @__PURE__ */ React.createElement(
      RofPoEmailModal,
      {
        vendorName: poEmailModal.vendor_name,
        ediOn: !!poEmailModal.edi_enabled,
        isResend: poEmailModal.status && poEmailModal.status !== "draft",
        defaultTo: poEmailModal.recipient_email || poEmailModal.vendor_email || "",
        defaultCc: poEmailModal.cc_emails && poEmailModal.cc_emails.length ? poEmailModal.cc_emails : [poRepEmail].filter(Boolean),
        defaultNotes: poEmailModal.notes || "",
        lines: (poEmailModal.items || []).map((it) => ({
          id: it.id,
          label: [it.vendor_sku, it.product_name].filter(Boolean).join(" \xB7 ") || "Line",
          note: it.line_note || ""
        })),
        sending: poUpdating,
        onCancel: () => setPoEmailModal(null),
        onSend: async (overrides) => {
          const ok = await doApprovePO(poEmailModal, overrides);
          if (ok) setPoEmailModal(null);
        }
      }
    ), receiveItem && (() => {
      const ordered = parseFloat(receiveItem.num_boxes || 0);
      const released = parseFloat(receiveItem.already_released || 0);
      const q = Math.max(0, Math.min(ordered, parseFloat(receiveQty) || 0));
      const avail = Math.max(0, q - released);
      const unit = receiveItem.sell_by === "unit" ? "units" : receiveItem.sell_by === "roll" ? "rolls" : receiveItem.sell_by === "piece" ? "pcs" : "boxes";
      const belowReleased = q + 1e-3 < released;
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          onClick: () => !receiveSaving && setReceiveItem(null),
          style: { position: "fixed", inset: 0, zIndex: 1400, background: "rgba(28,25,23,0.4)", display: "flex", alignItems: "center", justifyContent: "center", padding: 20 }
        },
        /* @__PURE__ */ React.createElement(
          "div",
          {
            onClick: (e) => e.stopPropagation(),
            style: { width: "100%", maxWidth: 424, background: "#f5f1e8", border: "0.5px solid rgba(28,25,23,0.25)", boxShadow: "0 20px 48px rgba(28,25,23,0.22)" }
          },
          /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 20px", borderBottom: "0.5px solid rgba(28,25,23,0.12)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "Mark received"), /* @__PURE__ */ React.createElement("div", { style: { font: '400 16px/1.3 "Inter", sans-serif', color: "var(--rod-ink)" } }, formatLineItem(receiveItem).name || receiveItem.product_name)),
          /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 20px" } }, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "How many ", unit, " were received?"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 10, marginTop: 6 } }, /* @__PURE__ */ React.createElement(
            "input",
            {
              className: "rod-input",
              type: "number",
              min: "0",
              max: ordered,
              step: "0.01",
              autoFocus: true,
              value: receiveQty,
              onChange: (e) => setReceiveQty(e.target.value),
              onKeyDown: (e) => {
                if (e.key === "Enter" && !receiveSaving) submitReceive();
              },
              style: { width: 120, textAlign: "right" }
            }
          ), /* @__PURE__ */ React.createElement("span", { style: { font: '400 13px/1 "Inter", sans-serif', color: "var(--rod-muted)" } }, "of ", parseFloat(ordered.toFixed(2)), " ordered"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => setReceiveQty(String(ordered)) }, "All")), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14, padding: "10px 12px", background: "rgba(28,25,23,0.04)", border: "0.5px solid rgba(28,25,23,0.08)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 12.5px/1.5 "Inter", sans-serif', color: "var(--rod-ink)" } }, released > 0 ? parseFloat(released.toFixed(2)) + " already released \xB7 " : "", /* @__PURE__ */ React.createElement("strong", null, parseFloat(avail.toFixed(2)), " available to release")), belowReleased && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 6, font: '400 12px/1.4 "Inter", sans-serif', color: "var(--rod-red)" } }, "Below the ", parseFloat(released.toFixed(2)), " already released \u2014 nothing more can be released until more arrives."))),
          /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "0.5px solid rgba(28,25,23,0.12)", display: "flex", justifyContent: "flex-end", gap: 8 } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", disabled: receiveSaving, onClick: () => setReceiveItem(null) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", disabled: receiveSaving, onClick: submitReceive }, receiveSaving ? "Saving\u2026" : "Save"))
        )
      );
    })());
  }
  const RET_STEPS = [
    { num: "01", label: "Return", sub: "Items \xB7 reason \xB7 condition", key: "return" },
    { num: "02", label: "Refund", sub: "Credit memo \xB7 tenders", key: "refund" },
    { num: "03", label: "Vendor RMAs", sub: "Restock vs return", key: "rmas" },
    { num: "04", label: "Done", sub: "Receipt \xB7 numbers", key: "done" }
  ];
  const RET_REASONS = [
    { id: "preference", label: "Buyer's preference", restock: 20 },
    { id: "vendor_error", label: "Wrong product \xB7 vendor error", restock: 0 },
    { id: "damaged", label: "Damaged on arrival", restock: 0 },
    { id: "refused", label: "Refused at delivery", restock: 0 },
    { id: "late", label: "Late delivery", restock: 0 },
    { id: "other", label: "Other", restock: 15 }
  ];
  const RET_CONDITIONS = [
    { id: "saleable", label: "Saleable", col: "var(--rod-green)" },
    { id: "damaged", label: "Damaged", col: "var(--rod-red)" },
    { id: "defective", label: "Defective", col: "var(--rod-red)" }
  ];
  function RetStepRail({ step, setStep }) {
    return /* @__PURE__ */ React.createElement("div", { className: "rof-rail" }, RET_STEPS.map((s, i) => {
      const stepNum = i + 1;
      const isActive = step === stepNum;
      const isCompleted = step > stepNum;
      const canJump = isCompleted;
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: s.key,
          className: "rof-rail-step" + (isActive ? " active" : "") + (isCompleted ? " completed" : "") + (!isActive && !isCompleted ? " future" : ""),
          onClick: () => canJump ? setStep(stepNum) : null
        },
        /* @__PURE__ */ React.createElement("span", { className: "rof-rail-num" }, isCompleted ? "\u2713" : s.num),
        /* @__PURE__ */ React.createElement("div", { className: "rof-rail-text" }, /* @__PURE__ */ React.createElement("div", { className: "rof-rail-label" }, s.label), /* @__PURE__ */ React.createElement("div", { className: "rof-rail-sub" }, s.sub)),
        /* @__PURE__ */ React.createElement("span", { className: "rof-rail-chev" }, isCompleted ? "\u2713" : "\u203A")
      );
    }));
  }
  function RetActionBar({ step, setStep, canContinue, onCommit, committing, navigate, refundTotal, rmaNumber, customerName, lineCount, unitCount }) {
    if (step === 4) return null;
    const labels = { 1: "Continue to Refund", 2: "Issue Refund", 3: "Continue to Done" };
    const nextLabel = labels[step] || "Continue";
    const context = [
      customerName,
      lineCount > 0 ? lineCount + " line" + (lineCount !== 1 ? "s" : "") : null,
      unitCount > 0 ? unitCount + " unit" + (unitCount !== 1 ? "s" : "") : null
    ].filter(Boolean).join(" \xB7 ");
    const isCommitStep = step === 3;
    return /* @__PURE__ */ React.createElement("div", { className: "rof-action-bar" }, /* @__PURE__ */ React.createElement("div", { className: "rof-action-left" }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)" } }, rmaNumber || "New return", " ", "\xB7", " Step ", step, " of 4"), /* @__PURE__ */ React.createElement("span", { className: "rof-action-total", style: { display: "block", marginTop: 4 } }, "\u2212", "$", refundTotal.toFixed(2), /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.4375rem/1 ui-monospace, monospace", opacity: 0.5, marginLeft: "0.3rem" } }, "REFUND"))), context && /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "rgba(245,240,232,0.6)", marginLeft: "1rem" } }, context)), /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => navigate("orders") }, "Save & Close"), step > 1 && /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => setStep(step - 1) }, "Back"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn " + (isCommitStep ? "" : "rof-btn-primary"),
        style: isCommitStep && canContinue && !committing ? { background: "#c54731", borderColor: "#c54731", color: "#f5f0e8" } : void 0,
        disabled: !canContinue || committing,
        onClick: () => {
          if (isCommitStep) {
            onCommit();
          } else {
            setStep(step + 1);
          }
        }
      },
      committing ? "Processing\u2026" : nextLabel
    ));
  }
  function RetSumRow({ l, v, c }) {
    return /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", padding: "6px 0", borderBottom: "0.5px solid rgba(28,25,23,0.07)", font: "400 13px/1.3 'Inter', sans-serif" } }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)" } }, l), /* @__PURE__ */ React.createElement("span", { style: { font: "400 14px/1 'Cormorant Garamond', serif", color: c || "var(--rod-ink)" } }, v));
  }
  function RepReturnFlowView({ navigate, editId, showCost }) {
    const [step, setStep] = useState(1);
    const [loading, setLoading] = useState(true);
    const [ctx, setCtx] = useState(null);
    const [committing, setCommitting] = useState(false);
    const [result, setResult] = useState(null);
    const [error, setError] = useState("");
    const [lineState, setLineState] = useState({});
    const [splits, setSplits] = useState({});
    const [storeCredit, setStoreCredit] = useState("");
    const [internalNote, setInternalNote] = useState("");
    const [customerNote, setCustomerNote] = useState("");
    useEffect(() => {
      if (!editId) return;
      repFetch("/api/rep/orders/" + editId + "/return-context").then((data) => {
        setCtx(data);
        const seed = {};
        (data.items || []).forEach((it) => {
          const maxQty = (parseInt(it.num_boxes, 10) || 0) - (parseInt(it.already_returned, 10) || 0);
          seed[it.id] = {
            selected: false,
            return_qty: maxQty > 0 ? maxQty : 0,
            reason: "",
            reason_id: "",
            condition: "saleable",
            restock_pct: 0,
            vendor_action: "restock_local",
            replacement_requested: false
          };
        });
        setLineState(seed);
        setLoading(false);
      }).catch((err) => {
        setError(err.message || "Failed to load return context");
        setLoading(false);
      });
    }, [editId]);
    const items = ctx && ctx.items || [];
    const tenders = ctx && ctx.tenders || [];
    const taxRate = ctx ? parseFloat(ctx.tax_rate) || 0 : 0;
    const setLine = (id, patch) => setLineState((prev) => ({ ...prev, [id]: { ...prev[id], ...patch } }));
    const lineMax = (it) => Math.max(0, (parseInt(it.num_boxes, 10) || 0) - (parseInt(it.already_returned, 10) || 0));
    const lineCalc = (it) => {
      const st = lineState[it.id] || {};
      const qty = st.selected ? parseInt(st.return_qty, 10) || 0 : 0;
      const gross = qty * (parseFloat(it.unit_price) || 0);
      const isSaleable = st.condition === "saleable";
      const pct = isSaleable ? parseFloat(st.restock_pct) || 0 : 0;
      const restockFee = isSaleable ? gross * pct / 100 : 0;
      const refundLine = gross - restockFee;
      return { qty, gross, restockFee, refundLine };
    };
    const selectedItems = items.filter((it) => lineState[it.id] && lineState[it.id].selected && lineCalc(it).qty > 0);
    const totals = (() => {
      let subtotal = 0, restock = 0, merch = 0, units = 0;
      selectedItems.forEach((it) => {
        const c = lineCalc(it);
        subtotal += c.gross;
        restock += c.restockFee;
        merch += c.refundLine;
        units += c.qty;
      });
      const taxRefund = merch * taxRate;
      const refundTotal = merch + taxRefund;
      return { subtotal, restock, merch, taxRefund, refundTotal, lineCount: selectedItems.length, units };
    })();
    const allocated = tenders.reduce((s, t) => s + (parseFloat(splits[t.id]) || 0), 0) + (parseFloat(storeCredit) || 0);
    const remaining = totals.refundTotal - allocated;
    const balanced = Math.abs(remaining) < 0.01;
    const step1Valid = selectedItems.length > 0 && selectedItems.every((it) => {
      const st = lineState[it.id];
      const c = lineCalc(it);
      return c.qty > 0 && c.qty <= lineMax(it) && st.reason_id;
    });
    const step2Valid = balanced && totals.refundTotal > 5e-3;
    const canContinue = step === 1 ? step1Valid : step === 2 ? step2Valid : true;
    const commit = async () => {
      setCommitting(true);
      setError("");
      try {
        const lines = selectedItems.map((it) => {
          const st = lineState[it.id];
          const c = lineCalc(it);
          return {
            order_item_id: it.id,
            return_qty: c.qty,
            reason: st.reason || (RET_REASONS.find((r) => r.id === st.reason_id) || {}).label || "",
            reason_id: st.reason_id,
            condition: st.condition,
            restock_pct: st.condition === "saleable" ? parseFloat(st.restock_pct) || 0 : 0,
            vendor_action: st.vendor_action,
            vendor_id: it.line_vendor_id,
            replacement_requested: !!st.replacement_requested
          };
        });
        const refund_splits = tenders.filter((t) => (parseFloat(splits[t.id]) || 0) > 0).map((t) => ({ payment_id: t.id, amount: parseFloat(splits[t.id]) }));
        const body = {
          lines,
          refund_splits,
          store_credit_amount: parseFloat(storeCredit) || 0,
          customer_note: customerNote,
          reason_summary: internalNote
        };
        const data = await repFetch("/api/rep/orders/" + editId + "/returns", { method: "POST", body: JSON.stringify(body) });
        setResult(data);
        setStep(4);
      } catch (err) {
        setError(err.message || "Failed to process return");
      }
      setCommitting(false);
    };
    const order = ctx && ctx.order;
    const custName = order ? order.customer_name || "Customer" : "";
    const custInitials = (custName || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?";
    const rmaNumber = result && result.return ? result.return.rma_number : null;
    const retMaterialKind = (it) => {
      const s = ((it.category_name || "") + " " + (it.variant_type || "")).toLowerCase();
      if (/hardwood|wood/.test(s)) return "wood";
      if (/lvp|lvt|vinyl|spc/.test(s)) return "lvp";
      if (/laminate/.test(s)) return "laminate";
      if (/marble/.test(s)) return "marble";
      if (/tile|porcelain|ceramic|mosaic/.test(s)) return "tile";
      if (/stone|travertine|slate/.test(s)) return "stone";
      if (/quartz/.test(s)) return "quartz";
      if (/carpet/.test(s)) return "carpet";
      return "tile";
    };
    const retTone = (name) => {
      let h = 0;
      for (let i = 0; i < (name || "").length; i++) h = (h << 5) - h + name.charCodeAt(i) | 0;
      h = Math.abs(h);
      const hue = 20 + h % 30, satA = 20 + (h >> 4) % 25, litA = 60 + (h >> 8) % 18, litB = 35 + (h >> 12) % 15, litC = 14 + (h >> 16) % 12;
      const hsl2hex = (hh, s, l) => {
        s /= 100;
        l /= 100;
        const a = s * Math.min(l, 1 - l);
        const f = (n) => {
          const k = (n + hh / 30) % 12;
          return l - a * Math.max(-1, Math.min(k - 3, 9 - k, 1));
        };
        return "#" + [f(0), f(8), f(4)].map((x) => Math.round(x * 255).toString(16).padStart(2, "0")).join("");
      };
      return { a: hsl2hex(hue, satA, litA), b: hsl2hex(hue, satA + 5, litB), c: hsl2hex(hue, satA + 8, litC) };
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading return\u2026");
    if (error && !ctx) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, error);
    if (!ctx) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Return not available");
    return /* @__PURE__ */ React.createElement("div", { className: "rof-container" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar", style: { paddingTop: "1rem" } }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("orders") }, "Orders"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("order-detail", editId) }, order.order_number), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, "Return"))), /* @__PURE__ */ React.createElement("div", { className: "ws-head", style: { marginBottom: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, "Return ", rmaNumber ? /* @__PURE__ */ React.createElement("em", null, rmaNumber) : /* @__PURE__ */ React.createElement("em", null, "against ", order.order_number)), /* @__PURE__ */ React.createElement(RovChip, { tone: rmaNumber ? "done" : "hold" }, rmaNumber ? "Processed" : "In progress")), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, custInitials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, custName), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, "Order ", order.order_number, " \xB7 ", order.customer_email || ""))))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Order total"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", parseFloat(order.total || 0).toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Refund total"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: totals.refundTotal > 5e-3 ? { color: "var(--rod-red)" } : void 0 }, "$", totals.refundTotal.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Lines selected"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, totals.lineCount, " of ", items.length)), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Restock fee"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", totals.restock.toFixed(2)))), /* @__PURE__ */ React.createElement(RetStepRail, { step, setStep }), step <= 3 ? /* @__PURE__ */ React.createElement("div", { className: "rof-body" }, /* @__PURE__ */ React.createElement("div", { className: "rof-main" }, step === 1 && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 16 } }, /* @__PURE__ */ React.createElement(WizRepCard, { title: "What's coming back?", actions: /* @__PURE__ */ React.createElement("span", { className: "rof-card-action", style: { color: "var(--rod-muted)" } }, totals.lineCount, " of ", items.length, " lines") }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 0 } }, items.map((it, idx) => {
      const st = lineState[it.id] || {};
      const max = lineMax(it);
      const c = lineCalc(it);
      const disabled = max <= 0;
      return /* @__PURE__ */ React.createElement("div", { key: it.id, style: { padding: "14px 0", borderBottom: idx < items.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none", opacity: disabled ? 0.5 : 1 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "22px 44px 1fr 90px 90px", gap: 12, alignItems: "center" } }, /* @__PURE__ */ React.createElement("label", { style: { cursor: disabled ? "not-allowed" : "pointer" } }, /* @__PURE__ */ React.createElement("span", { onClick: () => !disabled && setLine(it.id, { selected: !st.selected }), style: { display: "flex", width: 16, height: 16, border: "1px solid var(--rod-ink)", background: st.selected ? "var(--rod-ink)" : "transparent", alignItems: "center", justifyContent: "center", color: "#ece5d8", font: "500 10px/1 'Inter', sans-serif" } }, st.selected ? "\u2713" : "")), /* @__PURE__ */ React.createElement("div", { style: { height: 36, ...materialFace(retMaterialKind(it), retTone(it.product_name)) } }), /* @__PURE__ */ React.createElement("div", null, formatLineItem(it).brand && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginBottom: 3 } }, formatLineItem(it).brand), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.3 'Cormorant Garamond', serif", color: "var(--rod-ink)" } }, formatLineItem(it).name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginTop: 3 } }, [formatLineItem(it).sku || (it.vendor_name || "Roma"), it.num_boxes + " box", (parseInt(it.already_returned, 10) || 0) > 0 ? it.already_returned + " returned" : "none returned"].join("  \xB7  "))), st.selected ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", padding: "4px 8px" } }, /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "1",
          max,
          value: st.return_qty,
          onChange: (e) => {
            let v = parseInt(e.target.value, 10) || 0;
            if (v > max) v = max;
            if (v < 0) v = 0;
            setLine(it.id, { return_qty: v });
          },
          style: { width: "100%", border: "none", outline: "none", background: "transparent", textAlign: "right", font: "400 14px/1 'Cormorant Garamond', serif", color: "var(--rod-ink)" }
        }
      ), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", color: "var(--rod-muted)" } }, "/", max)) : /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "500 10px/1 ui-monospace, monospace", color: "var(--rod-muted)" } }, "0 / ", max), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", font: "400 14px/1 'Cormorant Garamond', serif", color: st.selected ? "var(--rod-red)" : "var(--rod-muted)" } }, st.selected ? "\u2212$" + c.refundLine.toFixed(2) : "$" + parseFloat(it.unit_price).toFixed(2))), st.selected && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12, padding: "12px 14px", background: "rgba(216,205,182,0.35)", display: "grid", gap: 12 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "Reason"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, flexWrap: "wrap" } }, RET_REASONS.map((r) => {
        const on = st.reason_id === r.id;
        return /* @__PURE__ */ React.createElement(
          "span",
          {
            key: r.id,
            onClick: () => setLine(it.id, { reason_id: r.id, restock_pct: st.condition === "saleable" ? r.restock : 0 }),
            style: { padding: "6px 10px", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", cursor: "pointer", border: "0.5px solid " + (on ? "var(--rod-ink)" : "rgba(28,25,23,0.2)"), background: on ? "var(--rod-ink)" : "transparent", color: on ? "#ece5d8" : "var(--rod-muted)" }
          },
          r.label
        );
      })), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          placeholder: "Add detail (optional)",
          value: st.reason || "",
          onChange: (e) => setLine(it.id, { reason: e.target.value }),
          style: { marginTop: 8, width: "100%", boxSizing: "border-box", padding: "6px 8px", border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", outline: "none", font: "400 12px/1.3 'Inter', sans-serif", color: "var(--rod-ink)" }
        }
      )), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1.3fr 1fr", gap: 14 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "Condition"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, flexWrap: "wrap" } }, RET_CONDITIONS.map((cd) => {
        const on = st.condition === cd.id;
        return /* @__PURE__ */ React.createElement(
          "span",
          {
            key: cd.id,
            onClick: () => setLine(it.id, { condition: cd.id, restock_pct: cd.id === "saleable" ? st.restock_pct : 0 }),
            style: { padding: "6px 10px", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", cursor: "pointer", border: "0.5px solid " + (on ? cd.col : "rgba(28,25,23,0.2)"), background: on ? cd.col : "transparent", color: on ? "#ece5d8" : "var(--rod-muted)" }
          },
          cd.label
        );
      }))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "Restock %"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", padding: "4px 8px", background: st.condition !== "saleable" ? "rgba(28,25,23,0.05)" : "transparent" } }, /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "0",
          max: "100",
          disabled: st.condition !== "saleable",
          value: st.condition === "saleable" ? st.restock_pct : 0,
          onChange: (e) => {
            let v = parseFloat(e.target.value);
            if (isNaN(v) || v < 0) v = 0;
            if (v > 100) v = 100;
            setLine(it.id, { restock_pct: v });
          },
          style: { width: "100%", border: "none", outline: "none", background: "transparent", font: "400 14px/1 'Cormorant Garamond', serif", color: "var(--rod-ink)" }
        }
      ), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", color: "var(--rod-muted)" } }, "%")))), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr auto", gap: 14, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6 } }, [{ id: "restock_local", l: "Restock local" }, { id: "return_to_vendor", l: "Return to vendor" }].map((va) => {
        const on = st.vendor_action === va.id;
        return /* @__PURE__ */ React.createElement(
          "span",
          {
            key: va.id,
            onClick: () => setLine(it.id, { vendor_action: va.id }),
            style: { padding: "6px 10px", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", cursor: "pointer", border: "0.5px solid " + (on ? "var(--rod-accent)" : "rgba(28,25,23,0.2)"), background: on ? "rgba(168,121,53,0.12)" : "transparent", color: on ? "var(--rod-accent)" : "var(--rod-muted)" }
          },
          va.l
        );
      })), /* @__PURE__ */ React.createElement(
        "label",
        {
          style: { display: "flex", alignItems: "center", gap: 6, cursor: "pointer", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-muted)" },
          onClick: () => setLine(it.id, { replacement_requested: !st.replacement_requested })
        },
        /* @__PURE__ */ React.createElement("span", { style: { display: "flex", width: 14, height: 14, border: "1px solid var(--rod-ink)", background: st.replacement_requested ? "var(--rod-ink)" : "transparent", alignItems: "center", justifyContent: "center", color: "#ece5d8", font: "500 9px/1 'Inter', sans-serif" } }, st.replacement_requested ? "\u2713" : ""),
        "Replacement"
      ))));
    }))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Notes" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-muted)", marginBottom: 6 } }, "Internal note"), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        value: internalNote,
        onChange: (e) => setInternalNote(e.target.value),
        rows: 4,
        style: { width: "100%", boxSizing: "border-box", padding: 10, border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", outline: "none", resize: "vertical", font: "400 13px/1.5 'Inter', sans-serif", color: "var(--rod-ink)" }
      }
    )), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--rod-accent)", marginBottom: 6 } }, "Customer note \xB7 prints on credit memo"), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        value: customerNote,
        onChange: (e) => setCustomerNote(e.target.value),
        rows: 4,
        style: { width: "100%", boxSizing: "border-box", padding: 10, border: "0.5px solid rgba(28,25,23,0.2)", background: "transparent", outline: "none", resize: "vertical", font: "400 13px/1.5 'Inter', sans-serif", color: "var(--rod-ink)" }
      }
    ))))), step === 2 && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 16 } }, /* @__PURE__ */ React.createElement(WizRepCard, { title: "Refund breakdown" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 0 } }, /* @__PURE__ */ React.createElement(RetSumRow, { l: "Items subtotal", v: "$" + totals.subtotal.toFixed(2) }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Restocking fees", v: totals.restock ? "\u2212$" + totals.restock.toFixed(2) : "\u2014", c: totals.restock ? "var(--rod-red)" : "var(--rod-muted)" }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Merchandise refund", v: "$" + totals.merch.toFixed(2) }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Tax refund \xB7 " + (taxRate * 100).toFixed(2) + "%", v: "$" + totals.taxRefund.toFixed(2) }), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", paddingTop: 10 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-ink)" } }, "Refund total"), /* @__PURE__ */ React.createElement("span", { style: { font: "300 30px/1 'Cormorant Garamond', serif", color: "var(--rod-red)", letterSpacing: "-0.014em" } }, "\u2212", "$", totals.refundTotal.toFixed(2))))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Where does the refund go?" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 0 } }, tenders.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 0", font: "400 13px/1.4 'Inter', sans-serif", color: "var(--rod-muted)" } }, "No refundable tenders on this order \u2014 allocate to store credit below."), tenders.map((t, i) => {
      const refundable = parseFloat(t.refundable) || 0;
      const method = { card: "Card", check: "Check", cash: "Cash", stripe: "Payment Request", offline: "Offline", ach: "Check (ACH)" }[t.payment_method] || t.payment_method;
      return /* @__PURE__ */ React.createElement("div", { key: t.id, style: { padding: "14px 0", display: "grid", gridTemplateColumns: "1.5fr 1fr 130px", gap: 14, alignItems: "center", borderBottom: "0.5px solid rgba(28,25,23,0.07)" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.3 'Inter', sans-serif", color: "var(--rod-ink)" } }, method, t.check_number ? " #" + t.check_number : ""), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginTop: 3 } }, "Paid $", parseFloat(t.amount).toFixed(2))), /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase" } }, "Refundable $", refundable.toFixed(2)), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", padding: "4px 8px" } }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)", fontSize: 12 } }, "$"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "0",
          max: refundable,
          step: "0.01",
          value: splits[t.id] || "",
          placeholder: "0.00",
          onChange: (e) => {
            let v = parseFloat(e.target.value);
            if (isNaN(v) || v < 0) {
              setSplits((p) => ({ ...p, [t.id]: e.target.value === "" ? "" : 0 }));
              return;
            }
            if (v > refundable) v = refundable;
            setSplits((p) => ({ ...p, [t.id]: v }));
          },
          style: { width: "100%", border: "none", outline: "none", background: "transparent", textAlign: "right", font: "400 16px/1 'Cormorant Garamond', serif", color: "var(--rod-red)" }
        }
      )));
    }), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 0", display: "grid", gridTemplateColumns: "1.5fr 1fr 130px", gap: 14, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.3 'Inter', sans-serif", color: "var(--rod-ink)" } }, "Store credit"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginTop: 3 } }, "Applies to a future order")), /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", padding: "4px 8px" } }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)", fontSize: 12 } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "number",
        min: "0",
        step: "0.01",
        value: storeCredit,
        placeholder: "0.00",
        onChange: (e) => {
          const v = parseFloat(e.target.value);
          setStoreCredit(e.target.value === "" ? "" : isNaN(v) || v < 0 ? 0 : v);
        },
        style: { width: "100%", border: "none", outline: "none", background: "transparent", textAlign: "right", font: "400 16px/1 'Cormorant Garamond', serif", color: "var(--rod-accent)" }
      }
    )))), /* @__PURE__ */ React.createElement("div", { style: { padding: "16px 0 0", display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 20, borderTop: "0.5px solid var(--rod-border)", marginTop: 4 } }, [
      { l: "Refund due", v: "$" + totals.refundTotal.toFixed(2), col: "var(--rod-ink)" },
      { l: "Allocated", v: "$" + allocated.toFixed(2), col: "var(--rod-ink)" },
      { l: balanced ? "\u25CF Balanced" : remaining > 0 ? "\u25CF Remaining" : "\u25CF Over", v: "$" + Math.abs(remaining).toFixed(2), col: balanced ? "var(--rod-green)" : "var(--rod-red)" }
    ].map((m) => /* @__PURE__ */ React.createElement("div", { key: m.l }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: m.col, marginBottom: 6 } }, m.l), /* @__PURE__ */ React.createElement("div", { style: { font: "300 26px/1 'Cormorant Garamond', serif", color: m.col === "var(--rod-green)" ? "var(--rod-ink)" : m.col, letterSpacing: "-0.014em" } }, m.v))))), error && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 14px", background: "#fee2e2", color: "#991b1b", fontSize: 13 } }, error)), step === 3 && /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 16 } }, /* @__PURE__ */ React.createElement(WizRepCard, { title: "Vendor handling \xB7 recorded for your reference" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "0 0 10px", font: "400 13px/1.5 'Inter', sans-serif", color: "rgba(28,25,23,0.8)" } }, "Recorded for your reference \u2014 no automatic transmission. Toggle how each returned line is handled."), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 0 } }, selectedItems.map((it, i) => {
      const st = lineState[it.id];
      const c = lineCalc(it);
      return /* @__PURE__ */ React.createElement("div", { key: it.id, style: { padding: "14px 0", display: "grid", gridTemplateColumns: "40px 1fr auto", gap: 14, alignItems: "center", borderBottom: i < selectedItems.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none" } }, /* @__PURE__ */ React.createElement("div", { style: { height: 34, ...materialFace(retMaterialKind(it), retTone(it.product_name)) } }), /* @__PURE__ */ React.createElement("div", null, formatLineItem(it).brand && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginBottom: 3 } }, formatLineItem(it).brand), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.3 'Cormorant Garamond', serif", color: "var(--rod-ink)" } }, formatLineItem(it).name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginTop: 3 } }, [c.qty + " box", formatLineItem(it).sku || (it.vendor_name || "Roma"), st.condition].join("  \xB7  "))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6 } }, [{ id: "restock_local", l: "Restock local" }, { id: "return_to_vendor", l: "Return to vendor" }].map((va) => {
        const on = st.vendor_action === va.id;
        return /* @__PURE__ */ React.createElement(
          "span",
          {
            key: va.id,
            onClick: () => setLine(it.id, { vendor_action: va.id }),
            style: { padding: "6px 10px", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", cursor: "pointer", border: "0.5px solid " + (on ? "var(--rod-accent)" : "rgba(28,25,23,0.2)"), background: on ? "rgba(168,121,53,0.12)" : "transparent", color: on ? "var(--rod-accent)" : "var(--rod-muted)" }
          },
          va.l
        );
      })));
    })), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 12, padding: "10px 12px", background: "rgba(216,205,182,0.4)", font: "500 10px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase" } }, "\u25CF Recorded for your reference \u2014 no automatic transmission")), error && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 14px", background: "#fee2e2", color: "#991b1b", fontSize: 13 } }, error))), /* @__PURE__ */ React.createElement("div", { className: "rof-sidebar" }, /* @__PURE__ */ React.createElement(RodCard, { title: "Return summary" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement(RetSumRow, { l: "Lines \xB7 " + totals.lineCount, v: totals.units + " units" }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Items subtotal", v: "$" + totals.subtotal.toFixed(2) }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Restock", v: totals.restock ? "\u2212$" + totals.restock.toFixed(2) : "\u2014", c: totals.restock ? "var(--rod-red)" : "var(--rod-muted)" }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Merch refund", v: "$" + totals.merch.toFixed(2) }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Tax \xB7 " + (taxRate * 100).toFixed(2) + "%", v: "$" + totals.taxRefund.toFixed(2) }), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", paddingTop: 8, borderTop: "0.5px solid var(--rod-border)" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-ink)" } }, "Refund"), /* @__PURE__ */ React.createElement("span", { style: { font: "300 28px/1 'Cormorant Garamond', serif", color: "var(--rod-red)", letterSpacing: "-0.014em" } }, "\u2212", "$", totals.refundTotal.toFixed(2))), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 6, padding: "8px 10px", textAlign: "center", background: step >= 2 ? "var(--rod-ink)" : "var(--rod-warm)", color: step >= 2 ? "#ece5d8" : "var(--rod-muted)", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase" } }, "\u25CF ", step === 1 ? "Drafting return" : step === 2 ? "Allocating refund" : "Ready to commit"))))) : (
      /* STEP 04 — Done */
      /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "32px 28px", background: "var(--rod-ink)", color: "#ece5d8", display: "grid", gridTemplateColumns: "1fr auto", gap: 24, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "var(--rod-green)", marginBottom: 12 } }, "\u25CF Refund issued \xB7 credit memo created"), /* @__PURE__ */ React.createElement("h1", { style: { font: "300 42px/1 'Cormorant Garamond', serif", letterSpacing: "-0.018em", margin: 0, color: "#ece5d8" } }, /* @__PURE__ */ React.createElement("em", { style: { fontStyle: "normal", color: "var(--rod-accent)" } }, result && result.return ? result.return.rma_number : ""), " \xB7 the loop is closed."), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.6 'Inter', sans-serif", color: "rgba(236,229,216,0.8)", marginTop: 12, maxWidth: 560 } }, "Credit memo ", result && result.credit_memo ? result.credit_memo.credit_memo_number : "", " issued.", result && result.is_full ? " This return closed the order." : "", result && result.balance != null ? " Remaining order balance $" + parseFloat(result.balance).toFixed(2) + "." : "")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(236,229,216,0.47)", marginBottom: 6 } }, "Refunded"), /* @__PURE__ */ React.createElement("div", { style: { font: "300 52px/1 'Cormorant Garamond', serif", color: "var(--rod-red)", letterSpacing: "-0.018em" } }, "\u2212", "$", totals.refundTotal.toFixed(2)))), /* @__PURE__ */ React.createElement(RodCard, { title: "Return receipt" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 10 } }, /* @__PURE__ */ React.createElement(RetSumRow, { l: "RMA number", v: result && result.return ? result.return.rma_number : "\u2014" }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Credit memo", v: result && result.credit_memo ? result.credit_memo.credit_memo_number : "\u2014" }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Refund total", v: "$" + totals.refundTotal.toFixed(2), c: "var(--rod-red)" }), result && result.balance != null && /* @__PURE__ */ React.createElement(RetSumRow, { l: "Order balance", v: "$" + parseFloat(result.balance).toFixed(2) }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Closed order", v: result && result.is_full ? "Yes \xB7 fully returned" : "No \xB7 partial" })), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "0.5px solid var(--rod-border)", display: "flex", gap: 8, flexWrap: "wrap" } }, result && result.return && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => repOpenDoc("/api/rep/returns/" + result.return.id + "/credit-memo", true).catch((err) => alert(err.message)) }, "View credit memo (PDF)"), /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("order-detail", editId) }, "Back to order"), /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("dashboard") }, "Dashboard"))))
    ), /* @__PURE__ */ React.createElement(
      RetActionBar,
      {
        step,
        setStep,
        canContinue,
        onCommit: commit,
        committing,
        navigate,
        refundTotal: totals.refundTotal,
        rmaNumber,
        customerName: custName,
        lineCount: totals.lineCount,
        unitCount: totals.units
      }
    ));
  }
  function relMaterialKind(it) {
    const s = ((it.category_name || "") + " " + (it.variant_type || "")).toLowerCase();
    if (/hardwood|wood/.test(s)) return "wood";
    if (/lvp|lvt|vinyl|spc/.test(s)) return "lvp";
    if (/laminate/.test(s)) return "laminate";
    if (/marble/.test(s)) return "marble";
    if (/tile|porcelain|ceramic|mosaic/.test(s)) return "tile";
    if (/stone|travertine|slate/.test(s)) return "stone";
    if (/quartz/.test(s)) return "quartz";
    if (/carpet/.test(s)) return "carpet";
    return "tile";
  }
  function relTone(name) {
    let h = 0;
    for (let i = 0; i < (name || "").length; i++) h = (h << 5) - h + name.charCodeAt(i) | 0;
    h = Math.abs(h);
    const hue = 20 + h % 30, satA = 20 + (h >> 4) % 25, litA = 60 + (h >> 8) % 18, litB = 35 + (h >> 12) % 15, litC = 14 + (h >> 16) % 12;
    const hsl2hex = (hh, s, l) => {
      s /= 100;
      l /= 100;
      const a = s * Math.min(l, 1 - l);
      const f = (n) => {
        const k = (n + hh / 30) % 12;
        return l - a * Math.max(-1, Math.min(k - 3, 9 - k, 1));
      };
      return "#" + [f(0), f(8), f(4)].map((x) => Math.round(x * 255).toString(16).padStart(2, "0")).join("");
    };
    return { a: hsl2hex(hue, satA, litA), b: hsl2hex(hue, satA + 5, litB), c: hsl2hex(hue, satA + 8, litC) };
  }
  function RepReleaseFlowView({ navigate, editId, initialMethod }) {
    const [loading, setLoading] = useState(true);
    const [ctx, setCtx] = useState(null);
    const [lineState, setLineState] = useState({});
    const [method, setMethod] = useState(initialMethod === "will_call" || initialMethod === "delivery" ? initialMethod : "pickup");
    const [distVendor, setDistVendor] = useState("");
    const [recipient, setRecipient] = useState("");
    const [notes, setNotes] = useState("");
    const [committing, setCommitting] = useState(false);
    const [result, setResult] = useState(null);
    const [error, setError] = useState("");
    useEffect(() => {
      if (!editId) return;
      repFetch("/api/rep/orders/" + editId + "/release-context").then((data) => {
        setCtx(data);
        if (data.is_install && !initialMethod) setMethod("delivery");
        const seed = {};
        (data.items || []).forEach((it) => {
          const ordered = parseFloat(it.num_boxes || 0);
          const cap = it.qty_received != null ? Math.min(parseFloat(it.qty_received), ordered) : 0;
          const remaining = Math.max(0, cap - parseFloat(it.already_released || 0));
          seed[it.id] = { selected: remaining > 0, qty: remaining };
        });
        setLineState(seed);
        if (data.order && data.order.customer_name) setRecipient(data.order.customer_name);
        setLoading(false);
      }).catch((err) => {
        setError(err.message || "Failed to load");
        setLoading(false);
      });
    }, [editId]);
    const order = ctx && ctx.order;
    const items = ctx && ctx.items || [];
    const distributors = ctx && ctx.distributors || [];
    const isWillCall = method === "will_call";
    const selectedDist = distributors.find((d) => String(d.vendor_id) === String(distVendor)) || null;
    const setLine = (id, patch) => setLineState((prev) => ({ ...prev, [id]: { ...prev[id], ...patch } }));
    const receivedCap = (it) => {
      const ordered = parseFloat(it.num_boxes || 0);
      return it.qty_received != null ? Math.min(parseFloat(it.qty_received), ordered) : 0;
    };
    const capFor = (it) => isWillCall ? parseFloat(it.num_boxes || 0) : receivedCap(it);
    const lineMatchesDist = (it) => !isWillCall || distVendor && String(it.vendor_id) === String(distVendor);
    const remainingFor = (it) => Math.max(0, capFor(it) - parseFloat(it.already_released || 0));
    const unitFor = (it) => it.sell_by === "unit" ? "unit" : it.sell_by === "roll" ? "roll" : it.sell_by === "piece" ? "pc" : "box";
    useEffect(() => {
      if (!ctx) return;
      if (isWillCall && !distVendor && distributors.length) {
        const firstSent = distributors.find((d) => d.po_sent) || distributors[0];
        if (firstSent) {
          setDistVendor(String(firstSent.vendor_id));
          return;
        }
      }
      setLineState((prev) => {
        const next = { ...prev };
        items.forEach((it) => {
          const rem = remainingFor(it);
          const match = lineMatchesDist(it);
          next[it.id] = { selected: match && rem > 0, qty: match ? rem : 0 };
        });
        return next;
      });
    }, [method, distVendor, ctx]);
    const releasable = items.filter((it) => remainingFor(it) > 0 && lineMatchesDist(it));
    const checkedLines = items.filter((it) => lineState[it.id] && lineState[it.id].selected);
    const selectedLines = items.filter((it) => {
      const st = lineState[it.id];
      return st && st.selected && parseFloat(st.qty) > 0 && parseFloat(st.qty) <= remainingFor(it) + 1e-3;
    });
    const totalQty = selectedLines.reduce((s, it) => s + parseFloat(lineState[it.id].qty || 0), 0);
    const anyRemaining = releasable.length > 0;
    const overSelected = checkedLines.some((it) => parseFloat(lineState[it.id].qty) > remainingFor(it) + 1e-3 || !(parseFloat(lineState[it.id].qty) > 0));
    const willCallReady = !isWillCall || !!(selectedDist && selectedDist.po_sent);
    const valid = selectedLines.length > 0 && !overSelected && willCallReady;
    const allSelected = releasable.length > 0 && releasable.every((it) => lineState[it.id] && lineState[it.id].selected);
    const toggleAll = () => {
      setLineState((prev) => {
        const next = { ...prev };
        releasable.forEach((it) => {
          next[it.id] = { ...next[it.id], selected: !allSelected, qty: !allSelected ? parseFloat(next[it.id] && next[it.id].qty) > 0 ? next[it.id].qty : remainingFor(it) : next[it.id].qty };
        });
        return next;
      });
    };
    const commit = async () => {
      setCommitting(true);
      setError("");
      try {
        const lines = selectedLines.map((it) => ({ order_item_id: it.id, release_qty: parseFloat(lineState[it.id].qty) }));
        const data = await repFetch("/api/rep/orders/" + editId + "/releases", {
          method: "POST",
          body: JSON.stringify({ lines, release_method: method, recipient_name: recipient.trim() || null, notes: notes.trim() || null, vendor_id: isWillCall ? distVendor : null })
        });
        setResult(data);
      } catch (err) {
        setError(err.message || "Failed to create release");
      }
      setCommitting(false);
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading release\u2026");
    if (error && !ctx) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, error);
    if (!ctx) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Order not available");
    const custName = order ? order.customer_name || "Customer" : "";
    const methodLabel = method === "delivery" ? "Delivery" : method === "will_call" ? "Will-call at distributor" : "Warehouse pickup";
    const LI_GRID = { display: "grid", gridTemplateColumns: "24px 48px 1fr 80px 84px 130px", gap: 12, alignItems: "center" };
    return /* @__PURE__ */ React.createElement("div", { style: { paddingBottom: 96 } }, /* @__PURE__ */ React.createElement("div", { className: "rod-breadcrumb" }, /* @__PURE__ */ React.createElement("div", { className: "rod-breadcrumb-trail" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "rod-breadcrumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("orders") }, "Orders"), /* @__PURE__ */ React.createElement("span", { className: "rod-breadcrumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("order-detail", editId) }, order.order_number), /* @__PURE__ */ React.createElement("span", { className: "rod-breadcrumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "rod-current" }, "Release"))), /* @__PURE__ */ React.createElement("div", { className: "rov-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(RovMicro, null, "Orders \xB7 ", order.order_number), /* @__PURE__ */ React.createElement("div", { className: "rov-header-title-row" }, /* @__PURE__ */ React.createElement("h1", null, "Release materials"), result ? /* @__PURE__ */ React.createElement(RovChip, { tone: "done" }, result.release.release_number) : /* @__PURE__ */ React.createElement(RovChip, { tone: "soft" }, methodLabel), result && result.is_full && /* @__PURE__ */ React.createElement(RovChip, { tone: "live" }, "Fully released")), /* @__PURE__ */ React.createElement(RovMicro, { style: { marginTop: 10 } }, custName, order.customer_email ? " \xB7 " + order.customer_email : "", order.rep_name ? " \xB7 Rep " + order.rep_name : "")), /* @__PURE__ */ React.createElement("div", { className: "rov-header-actions" }, result && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => repOpenDoc("/api/rep/releases/" + result.release.id + "/release-form", true).catch((err) => alert(err.message)) }, "Release form (PDF)"), /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("order-detail", editId) }, "Back to order"))), error && /* @__PURE__ */ React.createElement("div", { className: "rod-inline-panel", style: { marginTop: 0, marginBottom: 20, borderColor: "rgba(197,71,49,0.35)", background: "rgba(197,71,49,0.05)", color: "var(--rod-red)", font: "500 12px/1.4 'Inter', sans-serif" } }, error), result ? (
      /* ── Done ── */
      /* @__PURE__ */ React.createElement("div", { className: "rod-body" }, /* @__PURE__ */ React.createElement("div", { className: "rod-col" }, /* @__PURE__ */ React.createElement(RodCard, { title: "Release receipt" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "4px 20px 8px" } }, /* @__PURE__ */ React.createElement(RovRow, { l: "Release number", v: result.release.release_number }), /* @__PURE__ */ React.createElement(RovRow, { l: "Method", v: methodLabel }), /* @__PURE__ */ React.createElement(RovRow, { l: method === "delivery" ? "Recipient" : "Picked up by", v: recipient.trim() || custName }), /* @__PURE__ */ React.createElement(RovRow, { l: "Lines \xB7 quantity", v: selectedLines.length + " \xB7 " + parseFloat(totalQty.toFixed(2)) }), /* @__PURE__ */ React.createElement(RovRow, { l: "Order fully released", v: result.is_full ? "Yes" : "No \xB7 partial", c: result.is_full ? "var(--rod-green)" : void 0, last: true })), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "0.5px solid var(--rod-border)", display: "flex", gap: 8, flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", onClick: () => repOpenDoc("/api/rep/releases/" + result.release.id + "/release-form", true).catch((err) => alert(err.message)) }, "View release form"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => navigate("order-detail", editId) }, "Back to order"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: () => navigate("dashboard") }, "Dashboard")))), /* @__PURE__ */ React.createElement("div", { className: "rod-col" }, /* @__PURE__ */ React.createElement(RodCard, { title: method === "delivery" ? "Out for delivery" : method === "will_call" ? "Will-call authorized" : "Ready for pickup" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { font: "400 13px/1.6 'Inter', sans-serif", color: "var(--rod-muted)" } }, method === "delivery" ? "These materials are staged for delivery." : method === "will_call" ? "The customer can pick these up at " + (selectedDist ? selectedDist.vendor_name : "the distributor") + (selectedDist && selectedDist.po_number ? " against PO " + selectedDist.po_number : "") + "." : "These materials are ready for pickup at the Anaheim warehouse.", " ", order.customer_email ? "A release notice was emailed to " + order.customer_email + "." : "No customer email on file \u2014 email was skipped."))))
    ) : !ctx.releasable ? /* @__PURE__ */ React.createElement(RodCard, { title: "Not releasable yet" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { font: "400 13px/1.6 'Inter', sans-serif", color: "var(--rod-muted)" } }, "Materials can only be released once the order is confirmed (paid). This order is ", /* @__PURE__ */ React.createElement("strong", { style: { color: "var(--rod-ink)" } }, order.status), ".")) : !ctx.paid ? /* @__PURE__ */ React.createElement(RodCard, { title: "Payment required" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { font: "400 13px/1.6 'Inter', sans-serif", color: "var(--rod-muted)" } }, "Materials can\u2019t be released until this order is ", /* @__PURE__ */ React.createElement("strong", { style: { color: "var(--rod-ink)" } }, "paid in full"), ".", ctx.balance != null && parseFloat(ctx.balance) > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, " Outstanding balance: ", /* @__PURE__ */ React.createElement("strong", { style: { color: "var(--rod-red)" } }, "$", parseFloat(ctx.balance).toFixed(2)), ".") : "", " ", "Collect the balance from the order, then release."), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "0.5px solid var(--rod-border)" } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", onClick: () => navigate("order-detail", editId) }, "Back to order"))) : !anyRemaining ? /* @__PURE__ */ React.createElement(RodCard, { title: "Fully released" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { font: "400 13px/1.6 'Inter', sans-serif", color: "var(--rod-muted)" } }, "Every line on this order has already been released. Void an existing release from the order detail if you need to redo one.")) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "rod-body" }, /* @__PURE__ */ React.createElement("div", { className: "rod-col" }, /* @__PURE__ */ React.createElement(
      RodCard,
      {
        title: "Line items \xB7 " + items.length,
        action: allSelected ? "Clear all" : "Select all",
        onAction: toggleAll
      },
      /* @__PURE__ */ React.createElement("div", { className: "rod-line-items-header", style: LI_GRID }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Item"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Ordered"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Released"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Release now")),
      items.map((it) => {
        const st = lineState[it.id] || {};
        const remaining = remainingFor(it);
        const released = parseFloat(it.already_released || 0);
        const ordered = parseFloat(it.num_boxes || 0);
        const unit = unitFor(it);
        const fli = formatLineItem(it);
        const name = fli.name;
        const done = remaining <= 0;
        const awaitingReceipt = receivedCap(it) <= 0;
        const over = st.selected && (parseFloat(st.qty) > remaining + 1e-3 || !(parseFloat(st.qty) > 0));
        return /* @__PURE__ */ React.createElement("div", { key: it.id, className: "rod-line-item-row", style: { ...LI_GRID, opacity: done ? 0.5 : 1 } }, /* @__PURE__ */ React.createElement(
          "span",
          {
            onClick: () => !done && setLine(it.id, { selected: !st.selected }),
            style: { display: "flex", width: 16, height: 16, border: "1px solid var(--rod-ink)", background: st.selected && !done ? "var(--rod-ink)" : "transparent", alignItems: "center", justifyContent: "center", color: "#ece5d8", font: "500 10px/1 'Inter', sans-serif", cursor: done ? "default" : "pointer" }
          },
          st.selected && !done ? "\u2713" : ""
        ), it.primary_image ? /* @__PURE__ */ React.createElement("img", { className: "rod-line-item-thumb", src: optimizeImg(it.primary_image, 96), alt: "" }) : /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-thumb", style: materialFace(relMaterialKind(it), relTone(it.product_name || name)) }), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, fli.brand && /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta" }, fli.brand), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-name", style: { overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" } }, name), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta" }, fli.sku || (it.vendor_name || "Roma")), it.qty_received == null ? /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta", style: { color: "#a87935" } }, "Not yet received") : parseFloat(it.qty_received) < ordered && /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-meta", style: { color: "#a87935" } }, parseFloat(parseFloat(it.qty_received).toFixed(2)), " of ", ordered, " received")), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-qty" }, ordered, " ", /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)" } }, ordered === 1 ? unit : unit + "s")), /* @__PURE__ */ React.createElement("div", { className: "rod-line-item-qty", style: { color: released > 0 ? "var(--rod-accent)" : "var(--rod-muted)" } }, released > 0 ? released : "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "flex-end" } }, done ? /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: awaitingReceipt ? "var(--rod-muted)" : "var(--rod-green)" } }, awaitingReceipt ? "\u25CF Awaiting receipt" : "\u25CF Released") : /* @__PURE__ */ React.createElement("div", { style: { display: "inline-flex", alignItems: "center", border: "0.5px solid " + (over ? "var(--rod-red)" : "rgba(28,25,23,0.3)"), background: st.selected ? "#fff" : "rgba(28,25,23,0.03)" } }, /* @__PURE__ */ React.createElement(
          "input",
          {
            type: "number",
            min: "0",
            max: remaining,
            step: "1",
            value: st.qty,
            disabled: !st.selected,
            onChange: (e) => setLine(it.id, { qty: e.target.value }),
            style: { width: 54, border: "none", outline: "none", background: "transparent", textAlign: "right", padding: "7px 3px 7px 8px", font: "400 15px/1 'Cormorant Garamond', serif", color: over ? "var(--rod-red)" : "var(--rod-ink)" }
          }
        ), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", color: "var(--rod-muted)", padding: "0 8px 0 2px" } }, "/", remaining))));
      })
    )), /* @__PURE__ */ React.createElement("div", { className: "rod-col" }, /* @__PURE__ */ React.createElement(RodCard, { title: "Fulfillment" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 16 } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Method"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6 } }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn" + (method === "pickup" ? " selected" : ""), style: { flex: 1 }, onClick: () => setMethod("pickup") }, "Pickup"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn" + (method === "delivery" ? " selected" : ""), style: { flex: 1 }, onClick: () => setMethod("delivery") }, "Delivery"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn" + (method === "will_call" ? " selected" : ""), style: { flex: 1 }, onClick: () => setMethod("will_call") }, "Will-call"))), isWillCall && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Distributor"), distributors.length === 0 ? /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.75rem/1.4 Inter, sans-serif", color: "var(--rod-muted)", padding: "6px 0" } }, "No distributor POs on this order yet. Create a purchase order first.") : /* @__PURE__ */ React.createElement("select", { className: "rod-input", value: distVendor, onChange: (e) => setDistVendor(e.target.value) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "Select distributor\u2026"), distributors.map((d) => /* @__PURE__ */ React.createElement("option", { key: d.vendor_id, value: d.vendor_id }, d.vendor_name, d.vendor_public_code ? " \xB7 " + d.vendor_public_code : "", " \xB7 ", d.po_number, d.po_sent ? "" : " (PO not sent)"))), selectedDist && !selectedDist.po_sent && /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.7rem/1.4 Inter, sans-serif", color: "#a13b32", marginTop: 4 } }, "Send PO ", selectedDist.po_number, " to the distributor before authorizing will-call \u2014 they release against a sent PO."), selectedDist && selectedDist.po_sent && /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.7rem/1.4 Inter, sans-serif", color: "var(--rod-muted)", marginTop: 4 } }, "Releases against PO ", selectedDist.po_number, ". Customer picks up at ", selectedDist.vendor_name, selectedDist.vendor_public_code ? " \xB7 " + selectedDist.vendor_public_code : "", ".")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, method === "delivery" ? "Recipient" : "Picked up by"), /* @__PURE__ */ React.createElement("input", { className: "rod-input", type: "text", maxLength: 120, value: recipient, onChange: (e) => setRecipient(e.target.value), placeholder: "Name of person receiving" })), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Notes (optional)"), /* @__PURE__ */ React.createElement("textarea", { className: "rod-input", rows: 2, value: notes, onChange: (e) => setNotes(e.target.value), placeholder: "e.g. forklift needed, will-call counter" })))), /* @__PURE__ */ React.createElement(RodCard, { title: "Summary" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "4px 20px 8px" } }, /* @__PURE__ */ React.createElement(RovRow, { l: "Lines selected", v: selectedLines.length + " of " + releasable.length }), /* @__PURE__ */ React.createElement(RovRow, { l: "Total quantity", v: parseFloat(totalQty.toFixed(2)) + "" }), /* @__PURE__ */ React.createElement(RovRow, { l: "Method", v: methodLabel, last: true })), /* @__PURE__ */ React.createElement("div", { style: { padding: "0 20px 16px" } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "9px 12px", textAlign: "center", background: valid ? "var(--rod-ink)" : "var(--rod-warm)", color: valid ? "#ece5d8" : "var(--rod-muted)", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase" } }, "\u25CF ", overSelected ? "Fix quantities" : isWillCall && !distVendor ? "Pick a distributor" : isWillCall && !willCallReady ? "Send the PO first" : valid ? "Ready to release" : "Select a line"))))), /* @__PURE__ */ React.createElement("div", { className: "rof-action-bar" }, /* @__PURE__ */ React.createElement("div", { className: "rof-action-left" }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)" } }, "New release ", "\xB7", " ", order.order_number), /* @__PURE__ */ React.createElement("span", { className: "rof-action-total", style: { display: "block", marginTop: 4 } }, parseFloat(totalQty.toFixed(2)), /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.4375rem/1 ui-monospace, monospace", opacity: 0.5, marginLeft: "0.3rem" } }, method === "delivery" ? "TO DELIVER" : "TO PICK UP"))), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "rgba(245,240,232,0.6)", marginLeft: "1rem" } }, custName, selectedLines.length ? " \xB7 " + selectedLines.length + " line" + (selectedLines.length !== 1 ? "s" : "") : "")), /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => navigate("order-detail", editId) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", disabled: !valid || committing || !(ctx && ctx.paid), onClick: commit }, committing ? "Releasing\u2026" : "Release " + (method === "delivery" ? "for delivery" : method === "will_call" ? "for will-call" : "for pickup")))));
  }
  function RepChangeOrderView({ navigate, editId, showCost }) {
    const [order, setOrder] = useState(null);
    const [items, setItems] = useState([]);
    const [balanceInfo, setBalanceInfo] = useState(null);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState("");
    const [catalogOpen, setCatalogOpen] = useState(false);
    const [committing, setCommitting] = useState(false);
    const [committed, setCommitted] = useState(false);
    const [newBalance, setNewBalance] = useState(null);
    const [diffs, setDiffs] = useState([]);
    const [priceDraft, setPriceDraft] = useState({});
    const [qtyDraft, setQtyDraft] = useState({});
    const load = useCallback(() => {
      if (!editId) return;
      repFetch("/api/rep/orders/" + editId).then((data) => {
        setOrder(data.order);
        setItems(data.items || []);
        setBalanceInfo(data.balance || null);
        setLoading(false);
      }).catch((err) => {
        setError(err.message || "Failed to load order");
        setLoading(false);
      });
    }, [editId]);
    useEffect(load, [load]);
    const custName = order ? order.customer_name || "Customer" : "";
    const custInitials = (custName || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?";
    const removeDiff = (itemId) => diffs.find((d) => d.itemId === itemId && d.type === "remove");
    const qtyDiff = (itemId) => diffs.find((d) => d.itemId === itemId && d.type === "qty");
    const priceDiff = (itemId) => diffs.find((d) => d.itemId === itemId && d.type === "price");
    const dropDiff = (key) => setDiffs((prev) => prev.filter((d) => d.key !== key));
    const dropItemDiffs = (itemId, type) => setDiffs((prev) => prev.filter((d) => !(d.itemId === itemId && d.type === type)));
    const genKey = () => "d" + Date.now().toString(36) + Math.random().toString(36).slice(2, 7);
    const itemQty = (it) => parseInt(it.num_boxes, 10) || 0;
    const itemName = (it) => formatLineItem(it).name || "\u2014";
    const HD_LOCKED_DISCOUNT_CAP = 10;
    const effTradeDisc = (discount, retailLocked) => retailLocked ? Math.min(discount, HD_LOCKED_DISCOUNT_CAP) : discount;
    const orderTradeDiscount = order && parseFloat(order.trade_discount_percent) > 0 ? parseFloat(order.trade_discount_percent) : 0;
    const tradePrice = (retail, retailLocked) => orderTradeDiscount > 0 ? retail * (1 - effTradeDisc(orderTradeDiscount, retailLocked) / 100) : retail;
    const rebuildPayload = (it, qty) => {
      const isCustom = !it.product_id;
      if (isCustom) {
        return {
          product_name: it.product_name,
          unit_price: parseFloat(it.unit_price) || 0,
          vendor_id: it.vendor_id || void 0,
          description: it.description || null,
          sell_by: it.sell_by || "box",
          num_boxes: qty
        };
      }
      if (it.is_custom_rug) {
        return { sku_id: it.sku_id, num_boxes: qty, is_custom_rug: true, custom_width_ft: it.custom_width_ft, custom_length_ft: it.custom_length_ft };
      }
      const sellBy = it.sell_by || "box";
      const isUnit = sellBy === "unit" || sellBy === "piece";
      const spb = parseFloat(it.sqft_per_box || 0);
      const perBoxSqft = itemQty(it) > 0 && parseFloat(it.sqft_needed) > 0 ? parseFloat(it.sqft_needed) / itemQty(it) : spb > 0 ? spb : 0;
      if (isUnit) return { sku_id: it.sku_id, num_boxes: qty };
      if (sellBy === "roll") return { sku_id: it.sku_id, num_boxes: 1, sqft_needed: perBoxSqft > 0 ? perBoxSqft * qty : parseFloat(it.sqft_needed) || 9 };
      if (perBoxSqft > 0) return { sku_id: it.sku_id, num_boxes: qty, sqft_needed: perBoxSqft * qty };
      return { sku_id: it.sku_id, num_boxes: qty };
    };
    const stageRemove = (it) => {
      dropItemDiffs(it.id, "qty");
      dropItemDiffs(it.id, "price");
      setDiffs((prev) => [
        ...prev.filter((d) => !(d.itemId === it.id && d.type === "remove")),
        { key: genKey(), type: "remove", itemId: it.id, name: itemName(it), wasSubtotal: parseFloat(it.subtotal) || 0 }
      ]);
    };
    const stageQty = (it) => {
      const raw = qtyDraft[it.id];
      const newQty = parseInt(raw, 10);
      if (!newQty || newQty < 1 || newQty === itemQty(it)) {
        setQtyDraft((p) => ({ ...p, [it.id]: "" }));
        return;
      }
      const unitPrice = parseFloat(it.unit_price) || 0;
      dropItemDiffs(it.id, "qty");
      setDiffs((prev) => [...prev, {
        key: genKey(),
        type: "qty",
        itemId: it.id,
        name: itemName(it),
        wasQty: itemQty(it),
        newQty,
        unitPrice,
        wasSubtotal: parseFloat(it.subtotal) || 0,
        addPayload: rebuildPayload(it, newQty)
      }]);
      setQtyDraft((p) => ({ ...p, [it.id]: "" }));
    };
    const stagePrice = (it) => {
      const raw = priceDraft[it.id];
      const newPrice = parseFloat(raw);
      if (isNaN(newPrice) || newPrice < 0 || newPrice === (parseFloat(it.unit_price) || 0)) {
        setPriceDraft((p) => ({ ...p, [it.id]: "" }));
        return;
      }
      dropItemDiffs(it.id, "price");
      setDiffs((prev) => [...prev, {
        key: genKey(),
        type: "price",
        itemId: it.id,
        name: itemName(it),
        wasPrice: parseFloat(it.unit_price) || 0,
        newPrice,
        qty: itemQty(it),
        wasSubtotal: parseFloat(it.subtotal) || 0
      }]);
      setPriceDraft((p) => ({ ...p, [it.id]: "" }));
    };
    const addFromCatalog = ({ lines, accessories, customs }) => {
      const staged = [];
      for (const { item: p, qty } of lines) {
        const sellBy = p.sell_by || "box";
        const isUnit = sellBy === "unit" || sellBy === "piece";
        const isCarpet = p.price_basis === "per_sqyd";
        const perSqft = p.price_basis === "per_sqft" || p.price_basis === "sqft";
        const spb = parseFloat(p.sqft_per_box || 0);
        const mult = isUnit ? 1 : isCarpet ? spb > 0 ? spb / 9 : 1 : perSqft && spb > 0 ? spb : 1;
        const skuCost = p.cost != null && p.cost !== "" && parseFloat(p.cost) > 0 ? parseFloat(p.cost) : null;
        staged.push({
          key: genKey(),
          type: "add",
          name: formatLineItem({ ...p, product_name: p.display_name || p.name || p.product_name || p.collection }).name || "Item",
          vendorName: p.vendor_name || "Roma",
          image: p.primary_image || null,
          isCustom: false,
          skuId: p.sku_id,
          sellBy,
          isCarpet,
          spb,
          mult,
          qty,
          unitPrice: tradePrice(parseFloat(p.unit_price || p.price || 0), p.retail_locked),
          cost: skuCost
        });
      }
      for (const { acc, qty } of accessories) {
        const accCost = acc.cost != null && parseFloat(acc.cost) > 0 ? parseFloat(acc.cost) : acc.vendor_cost != null && parseFloat(acc.vendor_cost) > 0 ? parseFloat(acc.vendor_cost) : null;
        staged.push({
          key: genKey(),
          type: "add",
          name: formatLineItem({ ...acc, product_name: acc.display_name || acc.product_name || acc.name || acc.accessory_label }).name || "Accessory",
          vendorName: acc.vendor_name || "Roma",
          image: acc.primary_image || null,
          isCustom: false,
          skuId: acc.sku_id,
          sellBy: acc.sell_by || "unit",
          isCarpet: false,
          spb: 0,
          mult: 1,
          qty,
          unitPrice: tradePrice(parseFloat(acc.unit_price || acc.price || 0), acc.retail_locked),
          cost: accCost
        });
      }
      for (const c of customs) {
        const sellBy = c.sell_by || "box";
        staged.push({
          key: genKey(),
          type: "add",
          name: c.product_name || "Custom item",
          image: null,
          vendorName: c.custom_vendor || "Custom",
          isCustom: true,
          product_name: c.product_name,
          vendor_id: c.vendor_id || null,
          custom_vendor: c.custom_vendor || null,
          custom_vendor_email: c.custom_vendor_email || null,
          description: c.description || null,
          sellBy,
          isCarpet: sellBy === "roll",
          spb: 0,
          mult: sellBy === "roll" ? 1 / 9 : 1,
          qty: parseInt(c.num_boxes, 10) || 1,
          unitPrice: parseFloat(c.unit_price) || 0,
          cost: c.cost != null ? parseFloat(c.cost) : null
        });
      }
      setDiffs((prev) => [...prev, ...staged]);
      setCatalogOpen(false);
    };
    const numOr = (v, dflt) => {
      const n = parseFloat(v);
      return isNaN(n) ? dflt : n;
    };
    const addLineSubtotal = (d) => numOr(d.unitPrice, 0) * Math.max(0, parseInt(d.qty, 10) || 0) * (d.mult || 1);
    const buildAddPayload = (d) => {
      const qty = Math.max(1, parseInt(d.qty, 10) || 1);
      const price = numOr(d.unitPrice, 0);
      const cost = d.cost === "" || d.cost == null ? null : numOr(d.cost, null);
      if (d.isCustom) {
        return {
          product_name: d.product_name,
          unit_price: price,
          vendor_id: d.vendor_id || void 0,
          custom_vendor: d.vendor_id ? void 0 : d.custom_vendor || void 0,
          custom_vendor_email: d.vendor_id ? void 0 : d.custom_vendor_email || void 0,
          cost: cost != null ? cost : void 0,
          description: d.description || null,
          sell_by: d.sellBy,
          num_boxes: qty
        };
      }
      const base = { sku_id: d.skuId, num_boxes: qty, unit_price: price };
      if (cost != null) base.cost = cost;
      if (d.isCarpet) {
        base.sqft_needed = d.spb > 0 ? d.spb * qty : 9 * qty;
        base.num_boxes = 1;
      } else if (d.spb > 0) base.sqft_needed = d.spb * qty;
      return base;
    };
    const updateAddDiff = (key, patch) => setDiffs((prev) => prev.map((d) => d.key === key && d.type === "add" ? { ...d, ...patch } : d));
    const netChange = diffs.reduce((sum, d) => {
      if (d.type === "remove") return sum - d.wasSubtotal;
      if (d.type === "qty") return sum + (d.newQty * d.unitPrice - d.wasSubtotal);
      if (d.type === "price") return sum + (d.newPrice * d.qty - d.wasSubtotal);
      if (d.type === "add") return sum + addLineSubtotal(d);
      return sum;
    }, 0);
    const oldTotal = order ? parseFloat(order.total || 0) : 0;
    const taxFactor = order && parseFloat(order.subtotal || 0) > 0 ? parseFloat(order.total || 0) / parseFloat(order.subtotal || 0) : 1;
    const estNewTotal = oldTotal + netChange * taxFactor;
    const commit = async () => {
      setCommitting(true);
      setError("");
      try {
        for (const d of diffs.filter((x) => x.type === "price")) {
          await repFetch("/api/rep/orders/" + editId + "/items/" + d.itemId + "/price", { method: "PUT", body: JSON.stringify({ unit_price: d.newPrice, reason: "Change order" }) });
        }
        for (const d of diffs.filter((x) => x.type === "qty")) {
          await repFetch("/api/rep/orders/" + editId + "/items/" + d.itemId, { method: "DELETE" });
          await repFetch("/api/rep/orders/" + editId + "/add-item", { method: "POST", body: JSON.stringify(d.addPayload) });
        }
        for (const d of diffs.filter((x) => x.type === "add")) {
          await repFetch("/api/rep/orders/" + editId + "/add-item", { method: "POST", body: JSON.stringify(buildAddPayload(d)) });
        }
        let last = null;
        for (const d of diffs.filter((x) => x.type === "remove")) {
          last = await repFetch("/api/rep/orders/" + editId + "/items/" + d.itemId, { method: "DELETE" });
        }
        const fresh = await repFetch("/api/rep/orders/" + editId);
        setOrder(fresh.order);
        setItems(fresh.items || []);
        setBalanceInfo(fresh.balance || null);
        setNewBalance(fresh.balance || null);
        setDiffs([]);
        setCommitted(true);
      } catch (err) {
        setError(err.message || "Failed to apply changes");
      }
      setCommitting(false);
    };
    const changeMaterialKind = (it) => {
      const s = ((it.category_name || "") + " " + (it.variant_type || "")).toLowerCase();
      if (/hardwood|wood/.test(s)) return "wood";
      if (/lvp|lvt|vinyl|spc/.test(s)) return "lvp";
      if (/laminate/.test(s)) return "laminate";
      if (/marble/.test(s)) return "marble";
      if (/tile|porcelain|ceramic|mosaic/.test(s)) return "tile";
      if (/stone|travertine|slate/.test(s)) return "stone";
      if (/quartz/.test(s)) return "quartz";
      if (/carpet/.test(s)) return "carpet";
      return "tile";
    };
    const changeTone = (name) => {
      let h = 0;
      for (let i = 0; i < (name || "").length; i++) h = (h << 5) - h + name.charCodeAt(i) | 0;
      h = Math.abs(h);
      const hue = 20 + h % 30, satA = 20 + (h >> 4) % 25, litA = 60 + (h >> 8) % 18, litB = 35 + (h >> 12) % 15, litC = 14 + (h >> 16) % 12;
      const hsl2hex = (hh, s, l) => {
        s /= 100;
        l /= 100;
        const a = s * Math.min(l, 1 - l);
        const f = (n) => {
          const k = (n + hh / 30) % 12;
          return l - a * Math.max(-1, Math.min(k - 3, 9 - k, 1));
        };
        return "#" + [f(0), f(8), f(4)].map((x) => Math.round(x * 255).toString(16).padStart(2, "0")).join("");
      };
      return { a: hsl2hex(hue, satA, litA), b: hsl2hex(hue, satA + 5, litB), c: hsl2hex(hue, satA + 8, litC) };
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading order\u2026");
    if (error && !order) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, error);
    if (!order) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Order not found");
    const balanceAmt = (committed ? newBalance : balanceInfo) ? parseFloat((committed ? newBalance : balanceInfo).balance || 0) : 0;
    return /* @__PURE__ */ React.createElement("div", { className: "rof-container" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar", style: { paddingTop: "1rem" } }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("orders") }, "Orders"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("order-detail", editId) }, order.order_number), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, "Change order"))), /* @__PURE__ */ React.createElement("div", { className: "ws-head", style: { marginBottom: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, "Change ", /* @__PURE__ */ React.createElement("em", null, order.order_number)), /* @__PURE__ */ React.createElement(RovChip, { tone: { pending: "hold", confirmed: "live", shipped: "live", ready_for_pickup: "live", delivered: "done", cancelled: "hot" }[order.status] || "soft" }, (order.status || "").replace(/_/g, " "))), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, custInitials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, custName), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, [order.customer_email, order.phone].filter(Boolean).join(" \xB7 ") || "Order " + order.order_number))))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Order total"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", oldTotal.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Net change"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: netChange < -5e-3 ? { color: "var(--rod-red)" } : netChange > 5e-3 ? { color: "var(--rod-green)" } : void 0 }, (netChange < 0 ? "\u2212" : "+") + "$" + Math.abs(netChange).toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "New total \xB7 est."), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", estNewTotal.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Staged edits"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, diffs.length))), /* @__PURE__ */ React.createElement("div", { style: { marginBottom: 16, padding: "12px 16px", background: "rgba(168,121,53,0.1)", border: "0.5px solid rgba(168,121,53,0.4)", font: "400 13px/1.5 'Inter', sans-serif", color: "var(--rod-ink)" } }, /* @__PURE__ */ React.createElement("strong", null, "This order is already paid."), " Staged edits recompute the balance \u2014 after committing you'll either collect the balance owed or refund the difference using the order's existing payment controls. Purchase orders are affected for SKU lines."), committed ? /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 16 } }, /* @__PURE__ */ React.createElement("div", { style: { padding: "28px", background: "var(--rod-ink)", color: "#ece5d8", display: "grid", gridTemplateColumns: "1fr auto", gap: 24, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.2em", textTransform: "uppercase", color: "var(--rod-green)", marginBottom: 12 } }, "\u25CF Changes applied"), /* @__PURE__ */ React.createElement("h1", { style: { font: "300 36px/1 'Cormorant Garamond', serif", letterSpacing: "-0.018em", margin: 0, color: "#ece5d8" } }, "Order updated."), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.6 'Inter', sans-serif", color: "rgba(236,229,216,0.8)", marginTop: 12, maxWidth: 560 } }, "New order total $", parseFloat(order.total || 0).toFixed(2), ".", balanceAmt > 5e-3 ? " There is a balance of $" + balanceAmt.toFixed(2) + " owed." : balanceAmt < -5e-3 ? " The customer overpaid by $" + Math.abs(balanceAmt).toFixed(2) + "." : " The order is balanced.")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(236,229,216,0.47)", marginBottom: 6 } }, "New total"), /* @__PURE__ */ React.createElement("div", { style: { font: "300 48px/1 'Cormorant Garamond', serif", color: "#ece5d8", letterSpacing: "-0.018em" } }, "$", parseFloat(order.total || 0).toFixed(2)))), /* @__PURE__ */ React.createElement(RodCard, { title: "Next step" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "flex", gap: 8, flexWrap: "wrap" } }, balanceAmt > 5e-3 && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("order-detail", editId) }, "Collect balance ($", balanceAmt.toFixed(2), ") \u2192"), balanceAmt < -5e-3 && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("order-detail", editId) }, "Refund difference ($", Math.abs(balanceAmt).toFixed(2), ") \u2192"), /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => navigate("order-detail", editId) }, "Back to order")))) : /* @__PURE__ */ React.createElement("div", { className: "rof-body" }, /* @__PURE__ */ React.createElement("div", { className: "rof-main", style: { display: "grid", gap: 16 } }, /* @__PURE__ */ React.createElement(RodCard, { title: "Order lines", action: "+ Add / swap item", onAction: () => setCatalogOpen(true) }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 0 } }, items.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 'Inter', sans-serif", color: "var(--rod-muted)" } }, "No line items."), items.map((it, idx) => {
      const rd = removeDiff(it.id), qd = qtyDiff(it.id), pd = priceDiff(it.id);
      const staged = rd || qd || pd;
      return /* @__PURE__ */ React.createElement("div", { key: it.id, style: { padding: "14px 0", borderBottom: idx < items.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none", opacity: rd ? 0.5 : 1 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "44px 1fr auto", gap: 12, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { height: 38, ...materialFace(changeMaterialKind(it), changeTone(it.product_name)), textDecoration: rd ? "line-through" : "none" } }), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.3 'Cormorant Garamond', serif", color: "var(--rod-ink)", textDecoration: rd ? "line-through" : "none" } }, itemName(it)), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginTop: 3 } }, it.vendor_name || (!it.product_id ? "Custom" : "Roma"), it.vendor_public_code ? " \xB7 " + it.vendor_public_code : "", " \xB7 ", itemQty(it), " ", it.sell_by === "unit" ? "unit" : it.sell_by === "piece" ? "pc" : "box", " \xB7 $", parseFloat(it.unit_price).toFixed(2), " ea \xB7 $", parseFloat(it.subtotal || 0).toFixed(2))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", padding: "3px 6px" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginRight: 4 } }, "Qty"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "1",
          placeholder: String(itemQty(it)),
          value: qtyDraft[it.id] || "",
          onChange: (e) => setQtyDraft((p) => ({ ...p, [it.id]: e.target.value })),
          onBlur: () => stageQty(it),
          onKeyDown: (e) => {
            if (e.key === "Enter") e.target.blur();
          },
          disabled: !!rd,
          style: { width: 44, border: "none", outline: "none", background: "transparent", textAlign: "right", font: "400 13px/1 'Inter', sans-serif", color: "var(--rod-ink)" }
        }
      )), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", padding: "3px 6px" } }, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)", fontSize: 11 } }, "$"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          inputMode: "decimal",
          placeholder: parseFloat(it.unit_price).toFixed(2),
          value: priceDraft[it.id] || "",
          onChange: (e) => {
            const v = e.target.value;
            if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) setPriceDraft((p) => ({ ...p, [it.id]: v }));
          },
          onBlur: () => stagePrice(it),
          onKeyDown: (e) => {
            if (e.key === "Enter") e.target.blur();
          },
          disabled: !!rd,
          style: { width: 56, border: "none", outline: "none", background: "transparent", textAlign: "right", font: "400 13px/1 'Inter', sans-serif", color: "var(--rod-ink)" }
        }
      )), rd ? /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", style: { padding: "5px 8px", fontSize: 10 }, onClick: () => dropDiff(rd.key) }, "Undo") : /* @__PURE__ */ React.createElement(
        "button",
        {
          title: "Remove line",
          onClick: () => stageRemove(it),
          style: { padding: "4px 8px", border: "0.5px solid rgba(197,71,49,0.35)", background: "transparent", color: "var(--rod-red)", cursor: "pointer", font: "400 12px/1 'Inter', sans-serif" }
        },
        "\xD7"
      ))), staged && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 6, font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--rod-accent)" } }, "\u25CF Staged: ", rd ? "remove" : qd ? "qty " + qd.wasQty + " \u2192 " + qd.newQty : "price $" + pd.wasPrice.toFixed(2) + " \u2192 $" + pd.newPrice.toFixed(2)));
    }))), /* @__PURE__ */ React.createElement(RodCard, { title: "Modifications \xB7 " + diffs.length }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 0 } }, diffs.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 'Inter', sans-serif", color: "var(--rod-muted)" } }, "No changes staged yet. Edit qty/price, remove a line, or add an item above."), diffs.map((d, i) => {
      let was = "", now = "", delta = 0, poImpact = "";
      if (d.type === "remove") {
        was = "$" + d.wasSubtotal.toFixed(2);
        now = "Removed";
        delta = -d.wasSubtotal;
        poImpact = "PO line cancelled";
      }
      if (d.type === "qty") {
        was = d.wasQty + " \xD7 $" + d.unitPrice.toFixed(2);
        now = d.newQty + " \xD7 $" + d.unitPrice.toFixed(2);
        delta = d.newQty * d.unitPrice - d.wasSubtotal;
        poImpact = "PO qty updated";
      }
      if (d.type === "price") {
        was = "$" + d.wasPrice.toFixed(2) + " ea";
        now = "$" + d.newPrice.toFixed(2) + " ea";
        delta = d.newPrice * d.qty - d.wasSubtotal;
        poImpact = "Price only \xB7 PO cost unchanged";
      }
      if (d.type === "add") {
        was = "\u2014";
        delta = addLineSubtotal(d);
        poImpact = "New PO line";
      }
      const coBox = { display: "flex", alignItems: "center", border: "0.5px solid rgba(28,25,23,0.2)", padding: "3px 6px" };
      const coBoxLbl = { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.12em", color: "var(--rod-muted)", textTransform: "uppercase", marginRight: 4 };
      const coBoxInput = (w) => ({ width: w, border: "none", outline: "none", background: "transparent", textAlign: "right", font: "400 13px/1 'Inter', sans-serif", color: "var(--rod-ink)" });
      const rowBorder = i < diffs.length - 1 ? "0.5px solid rgba(28,25,23,0.07)" : "none";
      const removeBtn = /* @__PURE__ */ React.createElement("button", { onClick: () => dropDiff(d.key), style: { border: "none", background: "transparent", cursor: "pointer", color: "var(--rod-muted)", font: "400 14px/1 'Inter', sans-serif" } }, "\xD7");
      if (d.type === "add") {
        return /* @__PURE__ */ React.createElement("div", { key: d.key, style: { padding: "14px 0", display: "grid", gridTemplateColumns: "44px 1fr auto 24px", gap: 12, alignItems: "center", borderBottom: rowBorder } }, d.image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(d.image, 96), alt: "", loading: "lazy", style: { height: 38, width: 44, objectFit: "cover", border: "0.5px solid rgba(28,25,23,0.1)" } }) : /* @__PURE__ */ React.createElement("div", { style: { height: 38, ...materialFace(changeMaterialKind({}), changeTone(d.name)) } }), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.3 'Cormorant Garamond', serif", color: "var(--rod-ink)" } }, d.name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.14em", color: "var(--rod-green)", textTransform: "uppercase", margin: "4px 0 9px" } }, "\u25CF New line \xB7 ", d.vendorName), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, alignItems: "center", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: coBox }, /* @__PURE__ */ React.createElement("span", { style: coBoxLbl }, "Qty"), /* @__PURE__ */ React.createElement("input", { type: "number", min: "1", value: d.qty, onChange: (e) => updateAddDiff(d.key, { qty: e.target.value }), style: coBoxInput(40) })), /* @__PURE__ */ React.createElement("div", { style: coBox }, /* @__PURE__ */ React.createElement("span", { style: coBoxLbl }, "Price"), /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)", fontSize: 11, marginRight: 2 } }, "$"), /* @__PURE__ */ React.createElement("input", { type: "text", inputMode: "decimal", value: d.unitPrice, onChange: (e) => {
          const v = e.target.value;
          if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) updateAddDiff(d.key, { unitPrice: v });
        }, style: coBoxInput(56) })), showCost && /* @__PURE__ */ React.createElement("div", { style: coBox }, /* @__PURE__ */ React.createElement("span", { style: coBoxLbl }, "Cost"), /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-muted)", fontSize: 11, marginRight: 2 } }, "$"), /* @__PURE__ */ React.createElement("input", { type: "text", inputMode: "decimal", placeholder: "\u2014", value: d.cost == null ? "" : d.cost, onChange: (e) => {
          const v = e.target.value;
          if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) updateAddDiff(d.key, { cost: v });
        }, style: coBoxInput(56) })))), /* @__PURE__ */ React.createElement("span", { style: { font: "400 15px/1 'Cormorant Garamond', serif", color: "var(--rod-green)", textAlign: "right" } }, "+$", addLineSubtotal(d).toFixed(2)), removeBtn);
      }
      return /* @__PURE__ */ React.createElement("div", { key: d.key, style: { padding: "12px 0", display: "grid", gridTemplateColumns: "90px 1fr auto 24px", gap: 12, alignItems: "center", borderBottom: rowBorder } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: d.type === "remove" ? "var(--rod-red)" : "var(--rod-accent)" } }, "\u25CF ", d.type), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 'Inter', sans-serif", color: "var(--rod-ink)" } }, d.name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.1em", color: "var(--rod-muted)", textTransform: "uppercase", marginTop: 3 } }, was, " \u2192 ", now, " \xB7 ", poImpact)), /* @__PURE__ */ React.createElement("span", { style: { font: "400 15px/1 'Cormorant Garamond', serif", color: delta < 0 ? "var(--rod-red)" : "var(--rod-green)", textAlign: "right" } }, delta < 0 ? "\u2212" : "+", "$", Math.abs(delta).toFixed(2)), removeBtn);
    }))), error && /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 14px", background: "#fee2e2", color: "#991b1b", fontSize: 13 } }, error)), /* @__PURE__ */ React.createElement("div", { className: "rof-sidebar" }, /* @__PURE__ */ React.createElement(RodCard, { title: "Change summary" }, /* @__PURE__ */ React.createElement("div", { className: "rod-card-body-padded", style: { display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement(RetSumRow, { l: "Before \xB7 total", v: "$" + oldTotal.toFixed(2) }), /* @__PURE__ */ React.createElement(RetSumRow, { l: "Net change", v: (netChange < 0 ? "\u2212" : "+") + "$" + Math.abs(netChange).toFixed(2), c: netChange < 0 ? "var(--rod-red)" : "var(--rod-green)" }), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", paddingTop: 8, borderTop: "0.5px solid var(--rod-border)" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-ink)" } }, "After \xB7 est."), /* @__PURE__ */ React.createElement("span", { style: { font: "300 28px/1 'Cormorant Garamond', serif", color: "var(--rod-ink)", letterSpacing: "-0.014em" } }, "$", estNewTotal.toFixed(2))), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 4, font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", color: "var(--rod-muted)", textTransform: "uppercase" } }, "Tax recalculated on commit"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rod-btn rod-btn-primary",
        disabled: diffs.length === 0 || committing,
        onClick: commit,
        style: { marginTop: 10, padding: "12px 14px", background: diffs.length === 0 ? "rgba(28,25,23,0.3)" : "var(--rod-ink)", color: "#fff", border: "none", cursor: diffs.length === 0 ? "not-allowed" : "pointer", font: "500 12px/1 'Inter', sans-serif", letterSpacing: "0.06em", textTransform: "uppercase" }
      },
      committing ? "Applying\u2026" : "Commit change"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick: () => navigate("order-detail", editId),
        style: { padding: "10px 14px", background: "transparent", color: "var(--rod-ink)", border: "0.5px solid rgba(28,25,23,0.2)", cursor: "pointer", font: "500 11px/1 'Inter', sans-serif", letterSpacing: "0.06em", textTransform: "uppercase" }
      },
      "Cancel"
    ))))), catalogOpen && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data: { customer: orderTradeDiscount > 0 ? { type: "trade", discount_percent: orderTradeDiscount, tier_name: order.trade_tier_name } : null, newCustomerName: order.customer_name },
        showCost,
        contextLabel: "Change order \xB7 " + order.order_number,
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog
      }
    ));
  }
  function RepQuotesListView({ navigate }) {
    const [quotes, setQuotes] = useState([]);
    const [loading, setLoading] = useState(true);
    const [search, setSearch] = useState("");
    const [statusFilter, setStatusFilter] = useState("");
    const [activeView, setActiveView] = useState("all");
    const [scope, setScope] = useState("team");
    const [selected, setSelected] = useState(/* @__PURE__ */ new Set());
    const [page, setPage] = useState(1);
    const perPage = 50;
    const load = useCallback(() => {
      setLoading(true);
      const params = new URLSearchParams();
      if (search) params.set("search", search);
      if (statusFilter) params.set("status", statusFilter);
      if (scope === "team") params.set("scope", "team");
      repFetch("/api/rep/quotes?" + params.toString()).then((data) => {
        setQuotes(data.quotes || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [search, statusFilter, scope]);
    useEffect(load, [load]);
    const viewFilters = {
      "hot": (q) => q.status === "sent" && q.view_count >= 3,
      "awaiting": (q) => q.status === "sent" && (!q.view_count || q.view_count < 3),
      "expiring": (q) => {
        if (!q.expires_at) return false;
        const daysLeft = Math.ceil((new Date(q.expires_at) - Date.now()) / 864e5);
        return daysLeft >= 0 && daysLeft <= 7 && q.status !== "converted" && q.status !== "declined" && q.status !== "expired";
      },
      "drafts": (q) => q.status === "draft",
      "converted": (q) => q.status === "converted",
      "declined": (q) => q.status === "declined" || q.status === "expired",
      "all": () => true
    };
    const filteredQuotes = useMemo(() => {
      const fn = viewFilters[activeView] || (() => true);
      return quotes.filter(fn);
    }, [quotes, activeView]);
    const viewCounts = useMemo(() => ({
      "hot": quotes.filter(viewFilters["hot"]).length,
      "awaiting": quotes.filter(viewFilters["awaiting"]).length,
      "expiring": quotes.filter(viewFilters["expiring"]).length,
      "drafts": quotes.filter(viewFilters["drafts"]).length,
      "converted": quotes.filter(viewFilters["converted"]).length,
      "declined": quotes.filter(viewFilters["declined"]).length,
      "all": quotes.length
    }), [quotes]);
    const totalPages = Math.ceil(filteredQuotes.length / perPage);
    const pageQuotes = filteredQuotes.slice((page - 1) * perPage, page * perPage);
    const pipelineTotal = filteredQuotes.reduce((s, q) => s + (q.status !== "converted" && q.status !== "declined" && q.status !== "expired" ? parseFloat(q.total || 0) : 0), 0);
    const toggleSelect = (id) => {
      setSelected((prev) => {
        const n = new Set(prev);
        n.has(id) ? n.delete(id) : n.add(id);
        return n;
      });
    };
    const toggleAll = () => {
      if (selected.size === pageQuotes.length) setSelected(/* @__PURE__ */ new Set());
      else setSelected(new Set(pageQuotes.map((q) => q.id)));
    };
    const [bulkBusy, setBulkBusy] = useState(null);
    const selectedQuotes = useMemo(() => quotes.filter((q) => selected.has(q.id)), [quotes, selected]);
    const bulkExportCsv = () => {
      const rows = selectedQuotes.length ? selectedQuotes : filteredQuotes;
      if (!rows.length) {
        repToast("Nothing to export");
        return;
      }
      const cols = ["Quote", "Customer", "Type", "Items", "Total", "Stage", "Opens", "Expires", "Age", "Created"];
      const esc = (v) => '"' + String(v == null ? "" : v).replace(/"/g, '""') + '"';
      const lines = [cols.join(",")];
      rows.forEach((q) => {
        lines.push([
          q.quote_number,
          q.customer_name,
          q.customer_type === "trade" ? "Trade" : "Retail",
          q.item_count,
          parseFloat(q.total || 0).toFixed(2),
          getStage(q).label.split(" \xB7 ")[0],
          q.view_count || 0,
          getExpires(q).label,
          getAge(q.created_at),
          q.created_at ? new Date(q.created_at).toLocaleDateString() : ""
        ].map(esc).join(","));
      });
      const blob = new Blob([lines.join("\r\n")], { type: "text/csv;charset=utf-8;" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = "roma-quotes-" + (/* @__PURE__ */ new Date()).toISOString().slice(0, 10) + ".csv";
      document.body.appendChild(a);
      a.click();
      a.remove();
      setTimeout(() => URL.revokeObjectURL(url), 1e3);
      repToast("Exported " + rows.length + " quote" + (rows.length !== 1 ? "s" : ""));
    };
    const bulkReinstate = async () => {
      const eligible = selectedQuotes.filter((q) => q.status !== "converted");
      if (!eligible.length) {
        repToast("No eligible quotes (converted can\u2019t be reinstated)");
        return;
      }
      setBulkBusy("reinstate");
      let ok = 0, fail = 0;
      for (const q of eligible) {
        try {
          await repFetch("/api/rep/quotes/" + q.id + "/reinstate", { method: "POST" });
          ok++;
        } catch (e) {
          fail++;
        }
      }
      setBulkBusy(null);
      setSelected(/* @__PURE__ */ new Set());
      load();
      repToast("Extended " + ok + " quote" + (ok !== 1 ? "s" : "") + (fail ? ", " + fail + " failed" : ""));
    };
    const bulkResend = async () => {
      const eligible = selectedQuotes.filter((q) => ["draft", "sent"].includes(q.status) && q.customer_email);
      const skipped = selectedQuotes.length - eligible.length;
      if (!eligible.length) {
        repToast("No sendable quotes (need draft/sent + email)");
        return;
      }
      const ok0 = window.confirm("Email " + eligible.length + " quote" + (eligible.length !== 1 ? "s" : "") + " to " + (eligible.length === 1 ? "its customer" : "their customers") + "?" + (skipped ? "\n\n" + skipped + " selected quote" + (skipped !== 1 ? "s" : "") + " will be skipped (no email or not sendable)." : ""));
      if (!ok0) return;
      setBulkBusy("resend");
      let ok = 0, fail = 0;
      for (const q of eligible) {
        try {
          await repFetch("/api/rep/quotes/" + q.id + "/send", { method: "POST", body: JSON.stringify({}) });
          ok++;
        } catch (e) {
          fail++;
        }
      }
      setBulkBusy(null);
      setSelected(/* @__PURE__ */ new Set());
      load();
      repToast("Sent " + ok + " quote" + (ok !== 1 ? "s" : "") + (fail ? ", " + fail + " failed" : ""));
    };
    const getAge = (d) => {
      if (!d) return "\u2014";
      const days = Math.floor((Date.now() - new Date(d).getTime()) / 864e5);
      if (days === 0) return "today";
      return days + "d";
    };
    const getExpires = (q) => {
      if (!q.expires_at) return { label: "\u2014", tone: "var(--brass-muted)" };
      const days = Math.ceil((new Date(q.expires_at) - Date.now()) / 864e5);
      if (days < 0) return { label: "Expired", tone: "var(--brass-muted)" };
      if (days === 0) return { label: "Today", tone: "#c54731" };
      if (days <= 2) return { label: days + " days", tone: "#a87935" };
      return { label: days + " days", tone: "var(--stone-900)" };
    };
    const getStage = (q) => {
      if (q.status === "draft") return { label: "Draft", tone: "var(--brass-muted)" };
      if (q.status === "converted") return { label: "Converted", tone: "#3a7a4e" };
      if (q.status === "accepted") return { label: "Accepted", tone: "#3a7a4e" };
      if (q.status === "declined") return { label: "Declined", tone: "var(--brass-muted)" };
      if (q.status === "expired") return { label: "Expired", tone: "var(--brass-muted)" };
      if (q.view_count >= 3) return { label: "Hot \xB7 " + q.view_count + "\xD7 views", tone: "var(--gold, #c9a668)" };
      return { label: "Awaiting customer", tone: "#5c6e94" };
    };
    const VIEWS = [
      { key: "all", label: "All" },
      { key: "hot", label: "Hot", tone: "var(--gold, #c9a668)" },
      { key: "awaiting", label: "Awaiting customer" },
      { key: "expiring", label: "Expiring this week", tone: "#c54731" },
      { key: "drafts", label: "Drafts" },
      { key: "converted", label: "Converted" },
      { key: "declined", label: "Declined", tone: "var(--brass-muted)" }
    ];
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25CF", " Quotes ", "\xB7", " ", filteredQuotes.length, " showing ", "\xB7", " ", quotes.length, " total"), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Quotes"))), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: () => navigate("quote-create") }, "+ New quote"))), /* @__PURE__ */ React.createElement("div", { className: "ol-saved-views" }, VIEWS.map((v) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: v.key,
        className: "ol-view-tab" + (activeView === v.key ? " active" : ""),
        onClick: () => {
          setActiveView(v.key);
          setPage(1);
          setSelected(/* @__PURE__ */ new Set());
        }
      },
      v.label,
      /* @__PURE__ */ React.createElement("span", { className: "ol-tab-count", style: v.tone && viewCounts[v.key] > 0 ? { background: v.tone.startsWith("var") ? "" : v.tone + "1a", color: v.tone } : void 0 }, viewCounts[v.key])
    )), /* @__PURE__ */ React.createElement(ScopeToggle, { scope, setScope, onChange: () => {
      setPage(1);
      setSelected(/* @__PURE__ */ new Set());
    } })), /* @__PURE__ */ React.createElement("div", { className: "ol-filter-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-filter-chips" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        className: "ol-chip",
        value: statusFilter,
        onChange: (e) => {
          setStatusFilter(e.target.value);
          setPage(1);
        },
        style: { appearance: "auto", paddingRight: 24 }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, "Status: Any"),
      /* @__PURE__ */ React.createElement("option", { value: "draft" }, "Draft"),
      /* @__PURE__ */ React.createElement("option", { value: "sent" }, "Sent"),
      /* @__PURE__ */ React.createElement("option", { value: "accepted" }, "Accepted"),
      /* @__PURE__ */ React.createElement("option", { value: "converted" }, "Converted"),
      /* @__PURE__ */ React.createElement("option", { value: "expired" }, "Expired"),
      /* @__PURE__ */ React.createElement("option", { value: "declined" }, "Declined")
    ), statusFilter && /* @__PURE__ */ React.createElement("span", { className: "ol-chip active", onClick: () => setStatusFilter("") }, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-label" }, "Status:"), " ", statusFilter, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-remove" }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search quotes, customers",
        "aria-label": "Search quotes",
        value: search,
        onChange: (e) => {
          setSearch(e.target.value);
          setPage(1);
        }
      }
    )), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, filteredQuotes.length, " rows"))), selected.size > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-label" }, "\u25CF", " ", selected.size, " quote", selected.size !== 1 ? "s" : "", " selected"), /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: bulkResend, disabled: !!bulkBusy }, bulkBusy === "resend" ? "Sending\u2026" : "\u2709 Resend"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: bulkReinstate, disabled: !!bulkBusy }, bulkBusy === "reinstate" ? "Extending\u2026" : "\u21BB Extend expiry"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: bulkExportCsv, disabled: !!bulkBusy }, "\u2913", " Export CSV"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: () => setSelected(/* @__PURE__ */ new Set()), disabled: !!bulkBusy }, "\u2715", " Clear"))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 22px", background: "#ece5d8", border: "0.5px solid rgba(28,25,23,0.14)" } }, "Loading quotes", "\u2026") : /* @__PURE__ */ React.createElement("div", { className: "ql-table-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "ql-table-head" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "checkbox",
        style: { margin: 0 },
        "aria-label": "Select all quotes on page",
        checked: pageQuotes.length > 0 && selected.size === pageQuotes.length,
        onChange: toggleAll
      }
    ), /* @__PURE__ */ React.createElement("span", null, "Quote ID"), /* @__PURE__ */ React.createElement("span", null, "Customer"), /* @__PURE__ */ React.createElement("span", null, "Items"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Total"), /* @__PURE__ */ React.createElement("span", null, "Engagement"), /* @__PURE__ */ React.createElement("span", null, "Expires"), /* @__PURE__ */ React.createElement("span", null, "Stage"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Age"), /* @__PURE__ */ React.createElement("span", null)), pageQuotes.map((q) => {
      const expires = getExpires(q);
      const stage = getStage(q);
      const isSel = selected.has(q.id);
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: q.id,
          className: "ql-table-row" + (isSel ? " selected" : ""),
          style: isSel ? { background: "var(--brass-warm, #d8cdb6)" } : void 0,
          role: "button",
          tabIndex: 0,
          onClick: () => {
            repListNav.quotes = filteredQuotes.map((x) => x.id);
            navigate("quote-edit", q.id);
          },
          onKeyDown: (e) => {
            if (e.key === "Enter" || e.key === " ") {
              e.preventDefault();
              repListNav.quotes = filteredQuotes.map((x) => x.id);
              navigate("quote-edit", q.id);
            }
          }
        },
        /* @__PURE__ */ React.createElement(
          "input",
          {
            type: "checkbox",
            style: { margin: 0 },
            checked: isSel,
            "aria-label": "Select quote " + q.quote_number,
            onChange: () => toggleSelect(q.id),
            onClick: (e) => e.stopPropagation(),
            onKeyDown: (e) => e.stopPropagation()
          }
        ),
        /* @__PURE__ */ React.createElement("span", { className: "ql-quote-id" }, q.quote_number),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ql-cust-name" }, q.customer_name), q.customer_type === "trade" && /* @__PURE__ */ React.createElement("div", { className: "ql-cust-tag" }, "Trade")),
        /* @__PURE__ */ React.createElement("span", { className: "ql-project" }, q.item_count, " item", q.item_count !== 1 ? "s" : ""),
        /* @__PURE__ */ React.createElement("span", { className: "ql-total" }, "$", parseFloat(q.total).toLocaleString(void 0, { minimumFractionDigits: 2, maximumFractionDigits: 2 })),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ql-engagement" }, q.view_count || 0, "\xD7", " opens", q.customer_replied && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--gold)", marginLeft: 6 } }, "\u2709"))),
        /* @__PURE__ */ React.createElement("span", { className: "ql-expires", style: { color: expires.tone } }, "\u25CF", " ", expires.label),
        /* @__PURE__ */ React.createElement("span", { className: "ql-stage", style: { color: stage.tone } }, "\u25CF", " ", stage.label),
        /* @__PURE__ */ React.createElement("span", { className: "ql-age" }, getAge(q.created_at)),
        /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)", textAlign: "right" } }, "\u2192")
      );
    }), pageQuotes.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif", marginBottom: 14 } }, "No quotes match this view."), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: () => navigate("quote-create") }, "+ New quote")), /* @__PURE__ */ React.createElement("div", { className: "ql-footer" }, /* @__PURE__ */ React.createElement("span", null, "Showing ", Math.min((page - 1) * perPage + 1, filteredQuotes.length), "\u2013", Math.min(page * perPage, filteredQuotes.length), " of ", filteredQuotes.length), /* @__PURE__ */ React.createElement("span", null, "$", pipelineTotal.toLocaleString(void 0, { minimumFractionDigits: 0, maximumFractionDigits: 0 }), " in open pipeline")), totalPages > 1 && /* @__PURE__ */ React.createElement("div", { className: "ol-pagination" }, /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, perPage, " per page"), /* @__PURE__ */ React.createElement("div", { className: "ol-page-btns" }, /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page <= 1, onClick: () => setPage((p) => p - 1) }, "\u2190"), Array.from({ length: Math.min(totalPages, 5) }, (_, i) => i + 1).map((p) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: p,
        className: "ol-page-btn" + (page === p ? " active" : ""),
        onClick: () => setPage(p)
      },
      p
    )), /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page >= totalPages, onClick: () => setPage((p) => p + 1) }, "\u2192")))));
  }
  function RepQuoteFormView({ navigate, editId, terminalRef, terminalStatus, connectReader, showCost }) {
    const [quote, setQuote] = useState(null);
    const [quoteItems, setQuoteItems] = useState([]);
    const [loading, setLoading] = useState(!!editId);
    const [saving, setSaving] = useState(false);
    const [savedFlash, setSavedFlash] = useState(false);
    const [form, setForm] = useState({
      customer_name: "",
      customer_first: "",
      customer_middle: "",
      customer_last: "",
      customer_email: "",
      phone: "",
      company_name: "",
      shipping_address_line1: "",
      shipping_address_line2: "",
      shipping_city: "",
      shipping_state: "",
      shipping_zip: "",
      notes: "",
      delivery_method: "pickup",
      shipping: "",
      sidemark: ""
    });
    useEffect(() => {
      setSavedFlash(false);
    }, [form]);
    const quoteSavedRef = useRef(false);
    useRofDraft(
      editId ? null : "quote-form",
      "quote",
      () => !quoteSavedRef.current && (quoteItems.length > 0 || !!selectedCustomer || !!(form.customer_name || form.customer_email || form.phone || form.notes)),
      () => ({ form, quoteItems, selectedCustomer })
    );
    useRofDraftRestore(editId ? null : "quote-form", (s) => {
      if (s.form) setForm((f) => ({ ...f, ...s.form }));
      if (Array.isArray(s.quoteItems)) setQuoteItems(s.quoteItems);
      if (s.selectedCustomer) setSelectedCustomer(s.selectedCustomer);
    });
    const [customerSearch, setCustomerSearch] = useState("");
    const [customerResults, setCustomerResults] = useState([]);
    const [customerSearching, setCustomerSearching] = useState(false);
    const [selectedCustomer, setSelectedCustomer] = useState(null);
    const [customerMatch, setCustomerMatch] = useState(null);
    const [customerMode, setCustomerMode] = useState("search");
    const customerSearchTimer = useRef(null);
    const [emailError, setEmailError] = useState("");
    const [phoneError, setPhoneError] = useState("");
    const [productSearch, setProductSearch] = useState("");
    const [searchResults, setSearchResults] = useState([]);
    const [showResults, setShowResults] = useState(false);
    const searchTimeout = useRef(null);
    const [addQty, setAddQty] = useState(1);
    const [catalogOpen, setCatalogOpen] = useState(false);
    const [rugModalItem, setRugModalItem] = useState(null);
    const [editingCell, setEditingCell] = useState(null);
    const [cellValue, setCellValue] = useState("");
    const startCellEdit = (idx, field, val) => {
      setEditingCell({ idx, field });
      setCellValue(String(val));
    };
    const cancelCellEdit = () => {
      setEditingCell(null);
      setCellValue("");
    };
    const [showConvertModal, setShowConvertModal] = useState(false);
    const [convertMethod, setConvertMethod] = useState("card");
    const [convertCheckNumber, setConvertCheckNumber] = useState("");
    const [convertCashDrawerOpen, setConvertCashDrawerOpen] = useState(null);
    const [showConvertTapModal, setShowConvertTapModal] = useState(false);
    const [showConvertCardChoice, setShowConvertCardChoice] = useState(false);
    const [showConvertManualCard, setShowConvertManualCard] = useState(false);
    const [showConvertAchModal, setShowConvertAchModal] = useState(false);
    const [showPreview, setShowPreview] = useState(false);
    const [previewHtml, setPreviewHtml] = useState("");
    const [previewMeta, setPreviewMeta] = useState(null);
    const [previewRecipients, setPreviewRecipients] = useState([]);
    const [recipientDraft, setRecipientDraft] = useState("");
    const addRecipient = (raw) => {
      const found = String(raw).split(/[\s,;]+/).map((s) => s.trim()).filter((s) => s && isValidEmail(s));
      if (found.length) setPreviewRecipients((prev) => [.../* @__PURE__ */ new Set([...prev, ...found])]);
      setRecipientDraft("");
    };
    const removeRecipient = (email) => setPreviewRecipients((prev) => prev.filter((e) => e !== email));
    const [previewLoading, setPreviewLoading] = useState(false);
    const [sendFeedback, setSendFeedback] = useState(null);
    const [events, setEvents] = useState([]);
    const [engagement, setEngagement] = useState(null);
    const [threadText, setThreadText] = useState("");
    const [threadPosting, setThreadPosting] = useState(false);
    const [statusBusy, setStatusBusy] = useState(false);
    const [promoCode, setPromoCode] = useState("");
    const [promoResult, setPromoResult] = useState(null);
    const [promoLoading, setPromoLoading] = useState(false);
    const [promoError, setPromoError] = useState("");
    const quoteIsReadonly = quote && !["draft", "sent"].includes(quote.status);
    const { addressInputRef: quoteAddrRef, previousAddresses: quotePrevAddrs, showPrevAddresses: showQuotePrevAddrs, setShowPrevAddresses: setShowQuotePrevAddrs, selectPreviousAddress: selectQuotePrevAddr } = useAddressAutofill({
      fieldMap: { line1: "shipping_address_line1", line2: "shipping_address_line2", city: "shipping_city", state: "shipping_state", zip: "shipping_zip" },
      setForm,
      selectedCustomer,
      isReadonly: !!quoteIsReadonly
    });
    const hydrateItems = (items) => (items || []).map((i) => ({
      ...i,
      _sqft_per_box: i._sqft_per_box || (i.sell_by === "box" && parseFloat(i.sqft_needed) > 0 && parseInt(i.num_boxes) > 0 ? parseFloat(i.sqft_needed) / parseInt(i.num_boxes) : null)
    }));
    useEffect(() => {
      if (!editId) return;
      repFetch("/api/rep/quotes/" + editId).then((data) => {
        setQuote(data.quote);
        setQuoteItems(hydrateItems(data.items));
        setForm({
          customer_name: data.quote.customer_name || "",
          customer_email: data.quote.customer_email || "",
          phone: data.quote.phone || "",
          company_name: data.quote.company_name || "",
          shipping_address_line1: data.quote.shipping_address_line1 || "",
          shipping_address_line2: data.quote.shipping_address_line2 || "",
          shipping_city: data.quote.shipping_city || "",
          shipping_state: data.quote.shipping_state || "",
          shipping_zip: data.quote.shipping_zip || "",
          notes: data.quote.notes || "",
          delivery_method: data.quote.delivery_method || "pickup",
          shipping: data.quote.shipping != null && parseFloat(data.quote.shipping) > 0 ? String(parseFloat(data.quote.shipping)) : "",
          sidemark: data.quote.sidemark || ""
        });
        if (data.quote.promo_code) {
          setPromoCode(data.quote.promo_code);
          setPromoResult({
            code: data.quote.promo_code,
            discount_amount: parseFloat(data.quote.discount_amount || 0)
          });
        }
        if (data.customer) {
          setSelectedCustomer(data.customer);
          setCustomerMode("search");
        } else {
          setCustomerMode("new");
        }
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [editId]);
    useEffect(() => {
      repFetch("/api/rep/cash-drawer/current").then((d) => setConvertCashDrawerOpen(!!d.drawer)).catch(() => setConvertCashDrawerOpen(false));
    }, []);
    const searchCustomers = (q) => {
      setCustomerSearch(q);
      if (customerSearchTimer.current) clearTimeout(customerSearchTimer.current);
      if (q.trim().length < 2) {
        setCustomerResults([]);
        setCustomerSearching(false);
        return;
      }
      setCustomerSearching(true);
      customerSearchTimer.current = setTimeout(async () => {
        try {
          const data = await repFetch("/api/rep/customers/search?q=" + encodeURIComponent(q.trim()));
          setCustomerResults(data.results || []);
        } catch (e) {
          setCustomerResults([]);
        }
        setCustomerSearching(false);
      }, 300);
    };
    const HD_LOCKED_DISCOUNT_CAP = 10;
    const effTradeDisc = (d, locked) => locked ? Math.min(d, HD_LOCKED_DISCOUNT_CAP) : d;
    const tradeDiscountFor = (c) => c && c.type === "trade" ? parseFloat(c.discount_percent || 0) || 0 : 0;
    const tradeUnit = (retail, locked, discount) => discount > 0 ? retail * (1 - effTradeDisc(discount, locked) / 100) : retail;
    const applyQuoteReprice = (discount) => {
      const next = quoteItems.map((it) => {
        if (!it.sku_id || !it.retail_price) return it;
        const retail = parseFloat(it.retail_price);
        const oldUnit = parseFloat(it.unit_price) || retail;
        const newUnit = tradeUnit(retail, it.retail_locked, discount);
        const oldSub = parseFloat(it.subtotal) || 0;
        const newSub = oldUnit > 0 ? oldSub * (newUnit / oldUnit) : newUnit;
        return { ...it, unit_price: newUnit.toFixed(2), subtotal: newSub.toFixed(2) };
      });
      setQuoteItems(next);
      if (editId) {
        next.forEach((it, i) => {
          const before = quoteItems[i];
          if (it.id && before && (it.unit_price !== before.unit_price || it.subtotal !== before.subtotal)) {
            repFetch("/api/rep/quotes/" + editId + "/items/" + it.id, {
              method: "PUT",
              body: JSON.stringify({ unit_price: it.unit_price, subtotal: it.subtotal })
            }).catch(() => {
            });
          }
        });
      }
    };
    const selectCustomer = (c) => {
      setSelectedCustomer(c);
      applyQuoteReprice(tradeDiscountFor(c));
      setForm((f) => ({
        ...f,
        customer_name: c.name || "",
        customer_email: c.email || "",
        phone: c.phone || "",
        company_name: c.company_name || "",
        shipping_address_line1: c.address_line1 || f.shipping_address_line1,
        shipping_address_line2: c.address_line2 || f.shipping_address_line2,
        shipping_city: c.city || f.shipping_city,
        shipping_state: c.state || f.shipping_state,
        shipping_zip: c.zip || f.shipping_zip
      }));
      setCustomerSearch("");
      setCustomerResults([]);
      setCustomerMatch(null);
    };
    const clearCustomer = () => {
      setSelectedCustomer(null);
      applyQuoteReprice(0);
      setForm((f) => ({ ...f, customer_name: "", customer_first: "", customer_middle: "", customer_last: "", customer_email: "", phone: "", company_name: "" }));
      setCustomerMatch(null);
      setEmailError("");
      setPhoneError("");
    };
    const checkCustomerMatch = async () => {
      if (selectedCustomer) return;
      const email = form.customer_email || "";
      const phone = form.phone || "";
      const hasEmail = email.trim().length >= 3;
      const hasPhone = phone.replace(/\D/g, "").length >= 10;
      if (!hasEmail && !hasPhone) {
        setCustomerMatch(null);
        return;
      }
      try {
        const data = await repFetch("/api/rep/customers/check-duplicate?email=" + encodeURIComponent(hasEmail ? email : "") + "&phone=" + encodeURIComponent(hasPhone ? phone : ""));
        setCustomerMatch(data.match || null);
      } catch (e) {
        setCustomerMatch(null);
      }
    };
    const handleProductSearch = (term) => {
      setProductSearch(term);
      if (searchTimeout.current) clearTimeout(searchTimeout.current);
      if (term.length < 2) {
        setSearchResults([]);
        setShowResults(false);
        return;
      }
      setShowResults(true);
      searchTimeout.current = setTimeout(async () => {
        try {
          const data = await repFetch("/api/rep/skus/search?q=" + encodeURIComponent(term));
          setSearchResults((data.results || []).map((r) => ({
            sku_id: r.sku_id,
            product_id: r.product_id,
            product_name: r.product_name,
            collection: r.collection,
            variant_name: r.variant_name,
            variant_type: r.variant_type,
            accessory_label: r.accessory_label,
            color: r.color,
            vendor_name: r.vendor_name,
            vendor_sku: r.vendor_sku,
            internal_sku: r.internal_sku,
            price: r.retail_price,
            sqft_per_box: r.sqft_per_box,
            sell_by: r.sell_by || "box",
            vendor_id: r.vendor_id,
            price_basis: r.price_basis || "per_sqft",
            cut_price: r.cut_price,
            roll_price: r.roll_price,
            roll_width_ft: r.roll_width_ft,
            primary_image: r.primary_image || null,
            vendor_cost: r.vendor_cost || null
          })));
        } catch (e) {
          setSearchResults([]);
        }
        setShowResults(true);
      }, 300);
    };
    const addProductToQuote = async (sku) => {
      setShowResults(false);
      setProductSearch("");
      try {
        const isCarpet = sku.price_basis === "per_sqyd" || sku.sell_by === "roll";
        const retail = isCarpet ? parseFloat(sku.cut_price || sku.price || 0) : pieceRetail(sku, sku.price);
        const price = tradeUnit(retail, sku.retail_locked, tradeDiscountFor(selectedCustomer));
        const sqftPerBox = parseFloat(sku.sqft_per_box || 0);
        const ppCost = (isPerPieceRow(sku) && sku.vendor_cost != null ? parseFloat(sku.vendor_cost) * sqftPerBox : sku.vendor_cost) || null;
        const sellBy = isCarpet ? "roll" : sku.sell_by || "box";
        const qty = Math.max(1, parseInt(addQty) || 1);
        const defaultSqft = isCarpet ? 9 * qty : (sqftPerBox || 0) * qty;
        const subtotal = isCarpet ? price * qty : sellBy === "unit" || sellBy === "piece" ? price * qty : defaultSqft > 0 ? price * defaultSqft : price * qty;
        const newItem = {
          sku_id: sku.sku_id,
          product_id: sku.product_id || null,
          product_name: sku.product_name || "",
          collection: sku.collection || "",
          color: sku.color || "",
          variant_name: sku.variant_name || "",
          variant_type: sku.variant_type || "",
          accessory_label: sku.accessory_label || "",
          vendor_name: sku.vendor_name || null,
          vendor_sku: sku.vendor_sku || sku.internal_sku || null,
          sqft_needed: defaultSqft || null,
          num_boxes: isCarpet ? 1 : qty,
          unit_price: price.toFixed(2),
          subtotal: subtotal.toFixed(2),
          is_sample: false,
          sell_by: sellBy,
          _sqft_per_box: sqftPerBox,
          retail_price: retail,
          retail_locked: !!sku.retail_locked,
          // Carpet-only: kept so the "Custom Area Rug" popup can price the cut.
          cut_price: sku.cut_price != null ? sku.cut_price : null,
          cut_cost: sku.cut_cost != null ? sku.cut_cost : null,
          roll_width_ft: sku.roll_width_ft != null ? sku.roll_width_ft : null,
          price_basis: sku.price_basis || null,
          vendor_id: sku.vendor_id || null,
          vendor_cost: ppCost,
          primary_image: sku.primary_image || null
        };
        setAddQty(1);
        if (editId) {
          const result = await repFetch("/api/rep/quotes/" + editId + "/items", {
            method: "POST",
            body: JSON.stringify(newItem)
          });
          if (result.item) {
            result.item._sqft_per_box = sqftPerBox;
            result.item.vendor_cost = ppCost;
            result.item.retail_price = retail;
            result.item.retail_locked = !!sku.retail_locked;
            setQuoteItems((prev) => [...prev, result.item]);
            const qData = await repFetch("/api/rep/quotes/" + editId);
            setQuote(qData.quote);
          }
        } else {
          setQuoteItems((prev) => [...prev, { ...newItem, _temp_id: "q" + Date.now() + "-" + (sku.sku_id || prev.length), vendor_cost: ppCost }]);
        }
      } catch (err) {
        alert("Failed to add product: " + (err.message || "Unknown error"));
      }
    };
    useEffect(() => {
      const pending = consumePendingAdd("quote");
      if (!pending) return;
      const list = pending.skus && pending.skus.length ? pending.skus : pending.sku ? [pending.sku] : [];
      if (!list.length) return;
      const targetId = pending.quoteId ? String(pending.quoteId) : null;
      if (targetId ? String(editId || "") === targetId : !editId) {
        (async () => {
          for (const r of list) {
            await addProductToQuote({
              sku_id: r.sku_id,
              product_id: r.product_id,
              product_name: r.product_name,
              collection: r.collection,
              variant_name: r.variant_name,
              variant_type: r.variant_type,
              accessory_label: r.accessory_label,
              color: r.color,
              vendor_name: r.vendor_name,
              vendor_sku: r.vendor_sku,
              internal_sku: r.internal_sku,
              price: r.retail_price,
              sqft_per_box: r.sqft_per_box,
              sell_by: r.sell_by || "box",
              vendor_id: r.vendor_id,
              price_basis: r.price_basis || "per_sqft",
              cut_price: r.cut_price,
              roll_price: r.roll_price,
              roll_width_ft: r.roll_width_ft,
              primary_image: r.primary_image || null,
              vendor_cost: r.vendor_cost || null
            });
          }
        })();
      }
    }, []);
    const addRugToQuote = async (line) => {
      setRugModalItem(null);
      try {
        if (editId) {
          const result = await repFetch("/api/rep/quotes/" + editId + "/items", {
            method: "POST",
            body: JSON.stringify({ is_custom_rug: true, sku_id: line.sku_id, custom_width_ft: line.custom_width_ft, custom_length_ft: line.custom_length_ft, num_boxes: line.num_boxes })
          });
          if (result.item) {
            setQuoteItems((prev) => [...prev, result.item]);
            const qData = await repFetch("/api/rep/quotes/" + editId);
            setQuote(qData.quote);
          }
        } else {
          setQuoteItems((prev) => [...prev, { ...line, _temp_id: "rug" + Date.now() }]);
        }
      } catch (err) {
        alert("Failed to add custom rug: " + (err.message || "Unknown error"));
      }
    };
    const updateItemLocal = (index, field, value) => {
      setQuoteItems((prev) => {
        const updated = [...prev];
        const item = { ...updated[index] };
        item[field] = value;
        if (field === "num_boxes" || field === "unit_price" || field === "sqft_needed") {
          const price = field === "unit_price" ? parseFloat(value) || 0 : parseFloat(item.unit_price) || 0;
          if (item.sell_by === "roll") {
            const sqft = field === "sqft_needed" ? parseFloat(value) || 0 : parseFloat(item.sqft_needed) || 0;
            item.sqft_needed = sqft;
            item.subtotal = (price * (sqft / 9)).toFixed(2);
          } else {
            const boxes = field === "num_boxes" ? parseInt(value) || 0 : parseInt(item.num_boxes) || 0;
            if (field === "num_boxes" && item._sqft_per_box) {
              item.sqft_needed = (boxes * parseFloat(item._sqft_per_box)).toFixed(2);
            }
            const sqft = parseFloat(item.sqft_needed) || 0;
            if ((item.sell_by === "box" || item.sell_by === "sqft") && sqft > 0) {
              item.subtotal = (price * sqft).toFixed(2);
            } else {
              item.subtotal = (price * boxes).toFixed(2);
            }
          }
        }
        updated[index] = item;
        return updated;
      });
    };
    const saveItemEdit = async (item, index, includeCost = false) => {
      if (editId && item.id) {
        await repFetch("/api/rep/quotes/" + editId + "/items/" + item.id, {
          method: "PUT",
          body: JSON.stringify({
            num_boxes: item.num_boxes,
            unit_price: item.unit_price,
            sqft_needed: item.sqft_needed,
            subtotal: item.subtotal,
            product_name: item.product_name,
            collection: item.collection,
            description: item.description,
            sell_by: item.sell_by,
            // Only persist cost when the cost cell itself was edited, so
            // editing qty/price never freezes a catalog item's live cost.
            ...includeCost ? { cost: item.vendor_cost } : {}
          })
        });
        const qData = await repFetch("/api/rep/quotes/" + editId);
        setQuote(qData.quote);
      }
    };
    const removeItem = async (item, index) => {
      if (editId && item.id) {
        await repFetch("/api/rep/quotes/" + editId + "/items/" + item.id, { method: "DELETE" });
        const qData = await repFetch("/api/rep/quotes/" + editId);
        setQuote(qData.quote);
      }
      setQuoteItems((prev) => prev.filter((_, i) => i !== index));
    };
    const applyRepPromo = () => {
      const code = promoCode.trim();
      if (!code) return;
      setPromoLoading(true);
      setPromoError("");
      const items = quoteItems.map((i) => ({
        product_id: i.product_id,
        subtotal: i.subtotal,
        is_sample: i.is_sample || false
      }));
      repFetch("/api/rep/promo-codes/validate", {
        method: "POST",
        body: JSON.stringify({ code, items, customer_email: form.customer_email })
      }).then((data) => {
        if (data.valid) {
          setPromoResult(data);
          setPromoError("");
          setPromoCode(data.code);
        } else {
          setPromoResult(null);
          setPromoError(data.error || "Invalid promo code");
        }
        setPromoLoading(false);
      }).catch(() => {
        setPromoError("Unable to validate");
        setPromoLoading(false);
      });
    };
    const removeRepPromo = () => {
      setPromoResult(null);
      setPromoCode("");
      setPromoError("");
    };
    const saveQuote = async () => {
      setSaving(true);
      try {
        const promoPayload = promoResult ? promoResult.code : "";
        const shippingPayload = form.delivery_method === "shipping" ? parseFloat(form.shipping) || 0 : 0;
        if (editId) {
          await repFetch("/api/rep/quotes/" + editId, {
            method: "PUT",
            body: JSON.stringify({ ...form, shipping: shippingPayload, promo_code: promoPayload })
          });
        } else {
          const itemsPayload = quoteItems.map((i) => ({
            product_id: i.product_id,
            sku_id: i.sku_id,
            product_name: i.product_name,
            collection: i.collection,
            description: i.description,
            sqft_needed: i.sqft_needed,
            num_boxes: i.num_boxes,
            unit_price: i.unit_price,
            subtotal: i.subtotal,
            sell_by: i.sell_by,
            is_sample: i.is_sample,
            is_custom_rug: i.is_custom_rug || false,
            custom_width_ft: i.custom_width_ft || null,
            custom_length_ft: i.custom_length_ft || null
          }));
          const data = await repFetch("/api/rep/quotes", {
            method: "POST",
            body: JSON.stringify({ ...form, shipping: shippingPayload, items: itemsPayload, promo_code: promoPayload })
          });
          if (data.quote) {
            quoteSavedRef.current = true;
            rofDraftStore.clear("quote-form");
            navigate("quote-edit", data.quote.id);
            return;
          }
        }
        if (editId) {
          const data = await repFetch("/api/rep/quotes/" + editId);
          setQuote(data.quote);
          setQuoteItems(hydrateItems(data.items));
          if (data.quote.promo_code) {
            setPromoCode(data.quote.promo_code);
            setPromoResult({ code: data.quote.promo_code, discount_amount: parseFloat(data.quote.discount_amount || 0) });
          } else {
            setPromoResult(null);
            setPromoCode("");
          }
          setSavedFlash(true);
        }
      } catch (err) {
        alert(err.message || "Failed to save quote");
      }
      setSaving(false);
    };
    const openPreview = async () => {
      if (!editId) return;
      setPreviewLoading(true);
      setShowPreview(true);
      setPreviewHtml("");
      setPreviewMeta(null);
      try {
        const data = await repFetch("/api/rep/quotes/" + editId + "/preview");
        setPreviewHtml(data.html || "");
        setPreviewMeta({ to: data.to, subject: data.subject, reply_to: data.reply_to });
        setPreviewRecipients(data.to ? [data.to] : []);
        setRecipientDraft("");
      } catch (err) {
        setPreviewHtml('<p style="padding:2rem;color:#dc2626;">Failed to load email preview.</p>');
      }
      setPreviewLoading(false);
    };
    const sendQuote = async () => {
      if (!editId) return;
      const draft = recipientDraft.trim();
      const recipients = [.../* @__PURE__ */ new Set([...previewRecipients, ...isValidEmail(draft) ? [draft] : []])];
      if (!recipients.length) return;
      setShowPreview(false);
      setSaving(true);
      setSendFeedback(null);
      try {
        const result = await repFetch("/api/rep/quotes/" + editId + "/send", {
          method: "POST",
          body: JSON.stringify({ to: recipients })
        });
        const data = await repFetch("/api/rep/quotes/" + editId);
        setQuote(data.quote);
        loadEvents();
        setSendFeedback({ emailed: result.emailed, message: result.message });
        setTimeout(() => setSendFeedback(null), 6e3);
      } catch (err) {
        alert(err.message || "Failed to send quote");
      }
      setSaving(false);
    };
    const convertQuoteWithPI = async (res, documentIds) => {
      if (!editId) return;
      setSaving(true);
      try {
        const convertBody = { payment_method: convertMethod };
        if (res && res.card_txn) convertBody.card_txn = res.card_txn;
        else if (res && res.payment_intent_id) convertBody.stripe_payment_intent_id = res.payment_intent_id;
        if (convertMethod === "check" && convertCheckNumber) convertBody.check_number = convertCheckNumber;
        const data = await repFetch("/api/rep/quotes/" + editId + "/convert", {
          method: "POST",
          body: JSON.stringify(convertBody)
        });
        if (data.order) {
          navigate("order-detail", data.order.id);
          return;
        }
      } catch (err) {
        alert(err.message);
      }
      setSaving(false);
      setShowConvertModal(false);
    };
    const convertQuote = async () => {
      if (!editId) return;
      if (convertMethod === "cash" && !convertCashDrawerOpen) {
        alert("No cash drawer open");
        return;
      }
      if (convertMethod === "card") {
        setShowConvertModal(false);
        setShowConvertCardChoice(true);
        return;
      }
      convertQuoteWithPI(null);
    };
    const [reinstating, setReinstating] = useState(false);
    const reinstateQuote = async () => {
      if (!editId) return;
      setReinstating(true);
      try {
        const data = await repFetch("/api/rep/quotes/" + editId + "/reinstate", { method: "POST" });
        if (data.quote) {
          setQuote(data.quote);
          loadEvents();
        }
      } catch (err) {
        alert(err.message || "Failed to reinstate quote");
      }
      setReinstating(false);
    };
    const loadEvents = () => {
      if (!editId) {
        setEvents([]);
        setEngagement(null);
        return;
      }
      repFetch("/api/rep/quotes/" + editId + "/events").then((d) => {
        setEvents(d.events || []);
        setEngagement(d.engagement || null);
      }).catch(() => {
      });
    };
    useEffect(loadEvents, [editId]);
    const postThreadEntry = async (kind) => {
      const body = threadText.trim();
      if (!body || !editId) return;
      setThreadPosting(true);
      try {
        await repFetch("/api/rep/quotes/" + editId + "/events", {
          method: "POST",
          body: JSON.stringify({ event_type: kind, body })
        });
        setThreadText("");
        loadEvents();
      } catch (err) {
        alert(err.message || "Failed to log entry");
      }
      setThreadPosting(false);
    };
    const markStatus = async (status) => {
      if (!editId) return;
      let reason;
      if (status === "declined") {
        reason = window.prompt("Mark this quote lost \u2014 why? (optional)");
        if (reason === null) return;
      }
      setStatusBusy(true);
      try {
        await repFetch("/api/rep/quotes/" + editId + "/status", {
          method: "POST",
          body: JSON.stringify({ status, reason: reason || void 0 })
        });
        const data = await repFetch("/api/rep/quotes/" + editId);
        setQuote(data.quote);
        loadEvents();
      } catch (err) {
        alert(err.message || "Failed to update status");
      }
      setStatusBusy(false);
    };
    const duplicateQuote = async () => {
      if (!quote) return;
      setSaving(true);
      try {
        const itemsPayload = quoteItems.map((i) => ({
          product_id: i.product_id,
          sku_id: i.sku_id,
          product_name: i.product_name,
          collection: i.collection,
          description: i.description,
          sqft_needed: i.sqft_needed,
          num_boxes: i.num_boxes,
          unit_price: i.unit_price,
          subtotal: i.subtotal,
          sell_by: i.sell_by,
          is_sample: i.is_sample,
          is_custom_rug: i.is_custom_rug || false,
          custom_width_ft: i.custom_width_ft || null,
          custom_length_ft: i.custom_length_ft || null
        }));
        const data = await repFetch("/api/rep/quotes", {
          method: "POST",
          body: JSON.stringify({ ...form, items: itemsPayload })
        });
        if (data.quote) {
          navigate("quote-edit", data.quote.id);
          return;
        }
      } catch (err) {
        alert(err.message || "Failed to duplicate quote");
      }
      setSaving(false);
    };
    const [pdfViewer, setPdfViewer] = useState(null);
    const openPdf = async () => {
      if (!editId) return;
      try {
        const url = await repOpenDoc("/api/rep/quotes/" + editId + "/pdf?preview=true");
        setPdfViewer({ url, filename: (quote && quote.quote_number ? quote.quote_number : "quote") + ".pdf" });
      } catch (e) {
        alert("Failed to load PDF");
      }
    };
    const addFromCatalog = async ({ lines, accessories, customs }) => {
      const toAdd = [];
      const discount = tradeDiscountFor(selectedCustomer);
      for (const { item: p, qty } of lines) {
        const isCarpet = p.price_basis === "per_sqyd";
        const retail = isCarpet ? parseFloat(p.cut_price || p.price || 0) : pieceRetail(p, p.price);
        const price = tradeUnit(retail, p.retail_locked, discount);
        const sqftPerBox = parseFloat(p.sqft_per_box || 0);
        const sellBy = isCarpet ? "roll" : p.sell_by || "box";
        const productName = p.display_name || p.name || p.product_name || "";
        const sqftNeeded = isCarpet ? 9 * qty : sqftPerBox * qty;
        const subtotal = isCarpet ? price * qty : sellBy === "unit" || sellBy === "piece" ? price * qty : sqftNeeded > 0 ? price * sqftNeeded : price * qty;
        toAdd.push({
          item: {
            sku_id: p.sku_id,
            product_id: p.id || p.product_id || null,
            product_name: productName,
            collection: p.collection || "",
            sqft_needed: sqftNeeded || null,
            num_boxes: isCarpet ? 1 : qty,
            unit_price: price.toFixed(2),
            subtotal: subtotal.toFixed(2),
            is_sample: false,
            sell_by: sellBy
          },
          extra: {
            _sqft_per_box: sqftPerBox,
            vendor_cost: (isPerPieceRow(p) && p.cost != null ? parseFloat(p.cost) * sqftPerBox : p.cost) || null,
            primary_image: p.primary_image || null,
            vendor_name: p.brand_name || p.vendor_name || null,
            vendor_sku: p.vendor_sku || p.internal_sku || null,
            color: p.color || null,
            variant_name: p.variant_name || null,
            variant_type: p.variant_type || null,
            accessory_label: p.accessory_label || null,
            retail_price: retail,
            retail_locked: !!p.retail_locked
          }
        });
      }
      for (const { acc, parent, qty } of accessories) {
        const accRetail = parseFloat(acc.retail_price || 0);
        const price = tradeUnit(accRetail, acc.retail_locked, discount);
        toAdd.push({
          item: {
            sku_id: acc.sku_id,
            product_id: acc.product_id || null,
            product_name: parent ? parent.product_name || parent.display_name || parent.name || acc.product_name || "Accessory" : acc.product_name || "Accessory",
            collection: acc.collection || (parent ? parent.collection : "") || "",
            color: acc.color || "",
            variant_name: acc.variant_name || "",
            variant_type: "accessory",
            accessory_label: acc.accessory_label || "",
            sqft_needed: null,
            num_boxes: qty,
            unit_price: price.toFixed(2),
            subtotal: (price * qty).toFixed(2),
            is_sample: false,
            sell_by: acc.sell_by || "unit"
          },
          extra: {
            vendor_cost: acc.cost || null,
            primary_image: acc.primary_image || null,
            vendor_name: parent ? parent.brand_name || parent.vendor_name || null : null,
            vendor_sku: acc.vendor_sku || acc.internal_sku || null,
            color: acc.color || null,
            variant_name: acc.variant_name || null,
            variant_type: "accessory",
            accessory_label: acc.accessory_label || null,
            retail_price: accRetail,
            retail_locked: !!acc.retail_locked
          }
        });
      }
      for (const c of customs) {
        toAdd.push({
          item: {
            sku_id: null,
            product_id: null,
            product_name: c.product_name,
            collection: "",
            description: c.description || "",
            sqft_needed: null,
            num_boxes: c.num_boxes,
            unit_price: parseFloat(c.unit_price || 0).toFixed(2),
            subtotal: parseFloat(c.subtotal || 0).toFixed(2),
            is_sample: false,
            sell_by: c.sell_by || "box",
            vendor_id: c.vendor_id || null,
            custom_vendor: c.custom_vendor || null,
            custom_vendor_email: c.custom_vendor_email || null,
            cost: c.cost || null
          },
          extra: { _is_custom: true, vendor_name: c.vendor_name || null, vendor_cost: c.cost || null }
        });
      }
      try {
        if (editId) {
          for (const { item, extra } of toAdd) {
            const result = await repFetch("/api/rep/quotes/" + editId + "/items", {
              method: "POST",
              body: JSON.stringify(item)
            });
            if (result.item) setQuoteItems((prev) => [...prev, { ...result.item, ...extra }]);
          }
          const qData = await repFetch("/api/rep/quotes/" + editId);
          setQuote(qData.quote);
        } else {
          const stamp = Date.now();
          setQuoteItems((prev) => [...prev, ...toAdd.map(({ item, extra }, i) => ({ ...item, ...extra, _temp_id: stamp + i }))]);
        }
      } catch (err) {
        alert("Failed to add items: " + (err.message || "Unknown error"));
      }
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading quote...");
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const isReadonly = quote && !["draft", "sent"].includes(quote.status);
    const calcSubtotal = quoteItems.reduce((sum, i) => sum + parseFloat(i.subtotal || 0), 0);
    const qtyLabel = (sellBy) => sellBy === "unit" ? "units" : sellBy === "piece" ? "pcs" : sellBy === "roll" ? "sqft" : "boxes";
    const unitSuffix = (sellBy) => sellBy === "unit" ? "/ea" : sellBy === "roll" ? "/sqyd" : "/sqft";
    const fmtD = (d) => new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" });
    const STATUS_META = {
      draft: { label: "Draft", tone: "#a87935" },
      sent: { label: "Sent \xB7 awaiting customer", tone: "#5c6e94" },
      accepted: { label: "Accepted", tone: "#3a7a4e" },
      converted: { label: "Converted", tone: "#3a7a4e" },
      declined: { label: "Lost", tone: "#c54731" },
      expired: { label: "Expired", tone: "var(--brass-muted)" }
    };
    const st = quote ? STATUS_META[quote.status] || { label: quote.status, tone: "var(--brass-muted)" } : { label: "New draft", tone: "#a87935" };
    const expiresAt = quote && quote.expires_at ? new Date(quote.expires_at) : null;
    const isExpired = quote && (quote.status === "expired" || quote.status === "sent" && expiresAt && expiresAt.getTime() < Date.now());
    const isDead = quote && (quote.status === "declined" || isExpired);
    const daysLeft = expiresAt ? Math.ceil((expiresAt.getTime() - Date.now()) / 864e5) : null;
    const expiryPct = expiresAt && quote.created_at && expiresAt.getTime() > new Date(quote.created_at).getTime() ? Math.min(100, Math.max(0, (Date.now() - new Date(quote.created_at).getTime()) / (expiresAt.getTime() - new Date(quote.created_at).getTime()) * 100)) : 0;
    const fmtDT = (d) => {
      const dt = new Date(d);
      return dt.toLocaleDateString("en-US", { month: "short", day: "numeric" }) + " \xB7 " + dt.toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }).toLowerCase().replace(" ", "");
    };
    const engOpens = engagement ? engagement.opens : 0;
    const engViewers = engagement ? engagement.viewers : 0;
    const engReplies = engagement ? engagement.replies : 0;
    const lastViewedAt = engagement && engagement.last_viewed_at ? engagement.last_viewed_at : null;
    const lastReplyAt = engagement && engagement.last_reply_at ? engagement.last_reply_at : null;
    const lastSentEvent = [...events].reverse().find((e) => e.event_type === "sent");
    const threadHot = !!(lastReplyAt && lastSentEvent && new Date(lastReplyAt) > new Date(lastSentEvent.created_at) && new Date(lastReplyAt) - new Date(lastSentEvent.created_at) < 864e5);
    let stepRank = 0;
    if (quote) {
      if (quote.status === "converted") stepRank = 5;
      else if (quote.status === "accepted" || isDead) stepRank = 3;
      else if (quote.status === "sent") stepRank = engOpens > 0 ? 2 : 1;
    }
    const steps = [
      { label: "Drafted", meta: quote ? fmtD(quote.created_at) : "not saved yet" },
      { label: "Sent", meta: quote && quote.status !== "draft" ? lastSentEvent ? fmtDT(lastSentEvent.created_at) : ["accepted", "converted"].includes(quote.status) ? "skipped \u2014 direct " + quote.status : "emailed to customer" : "preview & send" },
      { label: "Viewed", meta: engOpens > 0 ? engOpens + (engOpens === 1 ? " open \xB7 " : " opens \xB7 ") + engViewers + (engViewers === 1 ? " viewer" : " viewers") : "no opens yet" },
      { label: "Accepted", meta: isDead ? quote.status === "declined" ? "marked lost" : "quote expired" : quote && ["accepted", "converted"].includes(quote.status) ? "customer committed" : expiresAt ? "expires " + fmtD(expiresAt) : "awaiting customer" },
      { label: "Converted", meta: quote && quote.converted_order_number ? quote.converted_order_number : quote && quote.converted_order_id ? "order created" : "locks pricing + payment" }
    ];
    const deadStepIndex = 3;
    const stepState = (i) => {
      if (isDead) {
        if (i === deadStepIndex) return "dead";
        if (i > deadStepIndex) return "next";
        if (i === 2) return engOpens > 0 ? "done" : "next";
        return "done";
      }
      return i < stepRank ? "done" : i === stepRank ? "now" : "next";
    };
    const costedItems = quoteItems.filter((i) => parseFloat(i.vendor_cost) > 0 && parseFloat(i.unit_price) > 0 && !i.is_sample);
    const costedSell = costedItems.reduce((s, i) => s + parseFloat(i.subtotal || 0), 0);
    const grossProfit = costedItems.reduce((s, i) => s + (1 - parseFloat(i.vendor_cost) / parseFloat(i.unit_price)) * parseFloat(i.subtotal || 0), 0);
    const blendedMargin = costedSell > 0 ? grossProfit / costedSell * 100 : null;
    const lineMargin = (item) => {
      const p = parseFloat(item.unit_price || 0);
      if (!(parseFloat(item.vendor_cost) > 0) || p <= 0) return null;
      return (1 - parseFloat(item.vendor_cost) / p) * 100;
    };
    const displaySubtotal = quote ? parseFloat(quote.subtotal) : calcSubtotal;
    const displayDiscount = promoResult && promoResult.discount_amount > 0 ? promoResult.discount_amount : 0;
    const displayShipping = quote ? parseFloat(quote.shipping || 0) : 0;
    const displayTotal = (quote ? parseFloat(quote.total) : calcSubtotal) - (promoResult && !quote ? promoResult.discount_amount : 0);
    const initials = (form.customer_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?";
    const canConvert = editId && quote && ["draft", "sent", "accepted"].includes(quote.status) && !isExpired;
    const saveDisabled = saving || !form.customer_name || form.customer_name.trim().split(/\s+/).length < 2 || !form.customer_email || !isValidEmail(form.customer_email) || !form.phone || !isValidPhone(form.phone) || !!customerMatch;
    const customerDone = !!form.customer_name && form.customer_name.trim().split(/\s+/).length >= 2 && isValidEmail(form.customer_email) && isValidPhone(form.phone) && !customerMatch;
    const deliveryDone = form.delivery_method === "pickup" || form.delivery_method === "will_call" || !!(form.shipping_address_line1 && form.shipping_city && form.shipping_zip);
    const linesDone = quoteItems.length > 0;
    const checklist = [
      { label: "Customer", done: customerDone, meta: customerDone ? form.customer_name : "name \xB7 email \xB7 phone" },
      { label: "Delivery", done: deliveryDone, meta: form.delivery_method === "pickup" ? "Store pickup" : form.delivery_method === "will_call" ? "Will-call at distributor" : deliveryDone ? [form.shipping_city, form.shipping_state].filter(Boolean).join(", ") : "address optional" },
      { label: "Lines", done: linesDone, meta: linesDone ? quoteItems.length + (quoteItems.length === 1 ? " line \xB7 $" : " lines \xB7 $") + calcSubtotal.toFixed(2) : "add from catalog" }
    ];
    const firstTodo = checklist.findIndex((c) => !c.done);
    return /* @__PURE__ */ React.createElement("div", { style: { paddingBottom: 96 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("quotes") }, "Quotes"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, editId && quote ? quote.quote_number : "New quote")), /* @__PURE__ */ React.createElement("div", { className: "qw-meta-right", style: { alignItems: "center" } }, quote && /* @__PURE__ */ React.createElement("span", null, "Updated ", fmtD(quote.updated_at)), editId && /* @__PURE__ */ React.createElement(RepPrevNext, { section: "quotes", id: editId, view: "quote-edit", navigate, linkStyle: { color: "var(--brass-muted)", textDecoration: "none" } }))), /* @__PURE__ */ React.createElement("div", { className: "ws-head" }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, editId && quote ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Quote ", /* @__PURE__ */ React.createElement("em", null, quote.quote_number)) : /* @__PURE__ */ React.createElement(React.Fragment, null, "New ", /* @__PURE__ */ React.createElement("em", null, "quote"))), /* @__PURE__ */ React.createElement("span", { className: "rov-chip soft", style: { color: st.tone } }, "\u25CF", " ", st.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, initials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, form.customer_name || "No customer yet"), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, form.customer_email || form.phone ? /* @__PURE__ */ React.createElement(React.Fragment, null, quote && quote.trade_customer_id ? "Trade \xB7 " : "", form.customer_email, form.customer_email && form.phone ? " \xB7 " : "", form.phone) : "Pick an existing customer or enter details below")))), /* @__PURE__ */ React.createElement("div", { className: "ws-head-actions" }, editId && quote && /* @__PURE__ */ React.createElement(
      AddTaskButton,
      {
        navigate,
        className: "ol-header-btn",
        label: "+ Task",
        prefill: {
          title: "Follow up on quote " + quote.quote_number,
          linked_quote_id: quote.id,
          customer_name: form.customer_name,
          customer_email: form.customer_email,
          customer_phone: form.phone,
          context_label: quote.quote_number + (form.customer_name ? " \xB7 " + form.customer_name : "")
        }
      }
    ), editId && quote && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: duplicateQuote, disabled: saving }, "Duplicate"), editId && quote && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: openPdf }, "Preview PDF"), quote && quote.status === "draft" && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: openPreview, disabled: saving }, "Preview & send"), quote && quote.status === "sent" && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: openPreview, disabled: saving }, "Send revision"), savedFlash && !saving && /* @__PURE__ */ React.createElement("span", { style: { alignSelf: "center", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "#3f7d5a" } }, "\u2713", " Saved"), !isReadonly && editId && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: saveQuote, disabled: saveDisabled }, saving ? "Saving\u2026" : "Save changes"))), /* @__PURE__ */ React.createElement("div", { className: "qw-stepper cols-5" }, steps.map((s, i) => {
      const state = stepState(i);
      return /* @__PURE__ */ React.createElement("div", { key: s.label, className: "qw-step " + state }, /* @__PURE__ */ React.createElement("div", { className: "qw-step-label" }, /* @__PURE__ */ React.createElement("span", { className: "qw-step-mark" }, state === "done" ? "\u2713" : state === "dead" ? "\u2715" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", null, s.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-step-meta" }, s.meta));
    })), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Total"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", displayTotal.toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Lines"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, quoteItems.length)), showCost && blendedMargin !== null && /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Margin \xB7 rep only"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, blendedMargin.toFixed(1), "%")), quote && expiresAt && ["draft", "sent", "accepted", "expired"].includes(quote.status) && /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Expires"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: isExpired ? { color: "#c54731" } : daysLeft !== null && daysLeft < 7 ? { color: "var(--gold, #c9a668)" } : void 0 }, isExpired ? "Expired " + fmtD(expiresAt) : fmtD(expiresAt) + " \xB7 " + daysLeft + "d")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, lastSentEvent ? "Sent" : "Drafted"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, lastSentEvent ? fmtD(lastSentEvent.created_at) : quote ? fmtD(quote.created_at) : "\u2014"))), sendFeedback && /* @__PURE__ */ React.createElement("div", { style: {
      padding: "0.75rem 1rem",
      marginBottom: "1rem",
      border: "1px solid",
      borderColor: sendFeedback.emailed ? "#86efac" : "#fde68a",
      background: sendFeedback.emailed ? "#f0fdf4" : "#fffbeb",
      color: sendFeedback.emailed ? "#166534" : "#92400e",
      fontSize: "0.875rem"
    } }, sendFeedback.message), /* @__PURE__ */ React.createElement("div", { className: "qw-grid" }, /* @__PURE__ */ React.createElement("div", { className: "qw-main" }, /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Customer",
        actions: !isReadonly && selectedCustomer ? /* @__PURE__ */ React.createElement("button", { className: "rof-card-action", onClick: clearCustomer }, "Change") : null
      },
      !isReadonly && !selectedCustomer && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light" + (customerMode === "search" ? " primary" : ""),
          onClick: () => {
            setCustomerMode("search");
            setCustomerMatch(null);
          },
          style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
        },
        "Search Existing"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light" + (customerMode === "new" ? " primary" : ""),
          onClick: () => {
            setCustomerMode("new");
            setCustomerResults([]);
            setCustomerSearch("");
          },
          style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
        },
        "+ New Customer"
      )),
      !isReadonly && customerMode === "search" && !selectedCustomer && /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Search"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: customerSearch,
          onChange: (e) => searchCustomers(e.target.value),
          placeholder: "Name, email, or phone..."
        }
      )), customerSearching && !customerResults.length && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "Searching..."), !customerSearching && customerSearch.trim().length >= 2 && customerResults.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "No customers found"), customerResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, customerResults.map((c, i) => /* @__PURE__ */ React.createElement("div", { key: c.id || i, className: "rof-search-item", onMouseDown: (e) => {
        e.preventDefault();
        selectCustomer(c);
      } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, c.name, c.company_name ? " \u2014 " + c.company_name : ""), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, c.email, c.phone ? " \xB7 " + c.phone : "")), /* @__PURE__ */ React.createElement("span", { className: "rof-search-tag " + (c.type === "trade" ? "trade" : c.type === "order" ? "order" : "new") }, c.type === "order" ? "prev order" : c.type))))),
      selectedCustomer && (() => {
        const c = selectedCustomer;
        const isTrade = c.type === "trade";
        const disc = parseFloat(c.discount_percent || 0);
        const addr = [c.address_line1, [c.city, c.state].filter(Boolean).join(", "), c.zip].filter(Boolean).join(" \xB7 ");
        const custInitials = (c.name || "?").split(" ").map((w) => w[0]).slice(0, 2).join("").toUpperCase();
        const mini = (label, value, color) => /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 4 } }, label), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 Inter, sans-serif", color: color || "#1c1917", letterSpacing: "-0.005em" } }, value));
        return /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.13)", background: "#ece5d8" } }, /* @__PURE__ */ React.createElement("div", { style: {
          padding: "1rem 1.125rem",
          display: "grid",
          gridTemplateColumns: "54px 1.4fr 1fr",
          gap: "1.25rem",
          alignItems: "flex-start"
        } }, /* @__PURE__ */ React.createElement("div", { style: {
          width: 54,
          height: 54,
          background: "#d8cdb6",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          font: '400 20px/1 "Cormorant Garamond", serif',
          color: "#1c1917",
          letterSpacing: "-0.012em"
        } }, custInitials), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 3, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 19px/1.2 "Cormorant Garamond", serif', color: "#1c1917", letterSpacing: "-0.012em" } }, c.name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, [c.company_name, isTrade ? "Trade" : "Retail", c.tier_name].filter(Boolean).join(" \xB7 ")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.5 Inter, sans-serif", color: "#1c1917", marginTop: 4 } }, c.email, c.phone ? " \xB7 " + c.phone : ""), addr && /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.4 Inter, sans-serif", color: "rgba(28,25,23,0.7)" } }, addr)), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 12 } }, mini("Discount tier", isTrade && disc > 0 ? "Trade \xB7 " + disc + "% off" : isTrade ? "Trade \xB7 list" : "Retail \xB7 list", disc > 0 ? "#a87935" : void 0), mini("Lifetime", "$" + parseFloat(c.lifetime_value || 0).toLocaleString("en-US", { maximumFractionDigits: 0 }) + " \xB7 " + (c.order_count || 0) + " order" + ((c.order_count || 0) !== 1 ? "s" : "")))), /* @__PURE__ */ React.createElement("div", { style: {
          padding: "0.625rem 1.125rem",
          borderTop: "0.5px solid rgba(28,25,23,0.1)",
          background: "#d8cdb6",
          display: "flex",
          justifyContent: "space-between",
          alignItems: "baseline",
          gap: 12
        } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, "\u25CF", " Drives pricing & terms on this quote"), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "#8a7e68" } }, "Avg order ", /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "#1c1917", letterSpacing: "0", textTransform: "none", marginLeft: 4 } }, (c.order_count || 0) > 0 ? "$" + (parseFloat(c.lifetime_value || 0) / c.order_count).toLocaleString("en-US", { maximumFractionDigits: 0 }) : "\u2014"))));
      })(),
      !isReadonly && customerMode === "new" && customerMatch && !selectedCustomer && /* @__PURE__ */ React.createElement("div", { className: "rof-alert error", style: { display: "flex", alignItems: "center", gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }, "Customer already exists: ", /* @__PURE__ */ React.createElement("strong", null, customerMatch.name), " (", customerMatch.email, ")"), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light",
          onClick: () => {
            selectCustomer(customerMatch);
            setCustomerMode("search");
          },
          style: { padding: "0.25rem 0.5rem", fontSize: "0.5rem", flexShrink: 0, whiteSpace: "nowrap" }
        },
        "Use this customer"
      )),
      !selectedCustomer && isReadonly && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Full Name *"), /* @__PURE__ */ React.createElement("input", { type: "text", value: form.customer_name, disabled: true, placeholder: "John Smith" })), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Email *"), /* @__PURE__ */ React.createElement("input", { type: "email", value: form.customer_email, disabled: true, placeholder: "email@example.com" }))), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Phone *"), /* @__PURE__ */ React.createElement("input", { type: "text", value: form.phone, disabled: true, placeholder: "(555) 123-4567" })), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Company (optional)"), /* @__PURE__ */ React.createElement("input", { type: "text", value: form.company_name, disabled: true, placeholder: "Company name" })))),
      !selectedCustomer && !isReadonly && customerMode === "new" && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
        CustomerNameFields,
        {
          first: form.customer_first,
          middle: form.customer_middle,
          last: form.customer_last,
          onFirst: (v) => setForm((f) => ({ ...f, customer_first: v, customer_name: composeCustomerName(v, f.customer_middle, f.customer_last) })),
          onMiddle: (v) => setForm((f) => ({ ...f, customer_middle: v, customer_name: composeCustomerName(f.customer_first, v, f.customer_last) })),
          onLast: (v) => setForm((f) => ({ ...f, customer_last: v, customer_name: composeCustomerName(f.customer_first, f.customer_middle, v) }))
        }
      ), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Email *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "email",
          value: form.customer_email,
          onChange: (e) => {
            setForm((f) => ({ ...f, customer_email: e.target.value }));
            if (emailError) setEmailError("");
          },
          onBlur: () => {
            if (form.customer_email && !isValidEmail(form.customer_email)) setEmailError("Enter a valid email");
            else setEmailError("");
            checkCustomerMatch("email");
          },
          placeholder: "email@example.com",
          style: emailError ? { borderColor: "var(--red, #dc2626)" } : {}
        }
      ), emailError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red, #dc2626)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, emailError)), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Phone *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: form.phone,
          onChange: (e) => {
            const v = formatPhone(e.target.value);
            setForm((f) => ({ ...f, phone: v }));
            if (phoneError) setPhoneError("");
          },
          onBlur: () => {
            if (!form.phone || !isValidPhone(form.phone)) setPhoneError("Enter a valid 10-digit phone");
            else setPhoneError("");
            checkCustomerMatch("phone");
          },
          placeholder: "(555) 123-4567",
          style: phoneError ? { borderColor: "var(--red, #dc2626)" } : {}
        }
      ), phoneError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red, #dc2626)", fontSize: "0.6875rem", marginTop: "0.25rem" } }, phoneError))), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Company (optional)"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: form.company_name,
          onChange: (e) => setForm((f) => ({ ...f, company_name: e.target.value })),
          placeholder: "Company name"
        }
      )))
    ), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Sidemark" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Sidemark / job reference"), /* @__PURE__ */ React.createElement(
      SidemarkSelect,
      {
        value: form.sidemark,
        disabled: isReadonly,
        onChange: (v) => setForm((f) => ({ ...f, sidemark: v })),
        customerId: selectedCustomer && selectedCustomer.id,
        email: form.customer_email,
        placeholder: "e.g. Smith Residence \u2014 Master Bath"
      }
    ), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.6875rem/1.4 Inter, sans-serif", color: "#8a7e68", marginTop: "0.4rem" } }, "Prints on the order, vendor POs, and documents. Carried over when the quote converts to an order."))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "How is the customer receiving these items?" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: "0.625rem", marginBottom: "0.875rem" } }, [
      {
        id: "pickup",
        icon: "\u25EB",
        label: "In-store pickup",
        sub: "Collect at the Anaheim showroom.",
        cost: "No freight"
      },
      {
        id: "will_call",
        icon: "\u22A1",
        label: "Will-call at distributor",
        sub: "Customer picks up at the distributor.",
        cost: "No freight"
      },
      {
        id: "shipping",
        icon: "\u2933",
        label: "Freight / delivery",
        sub: "Roma truck (local) or LTL freight.",
        cost: "Estimate below"
      }
    ].map((opt) => {
      const on = form.delivery_method === opt.id;
      return /* @__PURE__ */ React.createElement(
        "button",
        {
          key: opt.id,
          disabled: isReadonly,
          onClick: () => setForm((f) => ({ ...f, delivery_method: opt.id })),
          style: {
            padding: "0.875rem",
            cursor: isReadonly ? "default" : "pointer",
            textAlign: "left",
            background: on ? "#d8cdb6" : "transparent",
            border: "0.5px solid " + (on ? "#1c1917" : "rgba(28,25,23,0.2)"),
            display: "flex",
            flexDirection: "column",
            gap: "0.5rem",
            minHeight: 138,
            transition: "background 120ms, border-color 120ms",
            opacity: isReadonly && !on ? 0.5 : 1
          }
        },
        /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("span", { style: {
          width: 38,
          height: 38,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          font: "400 19px/1 var(--roma-serif)",
          color: "#1c1917",
          border: "0.5px solid " + (on ? "rgba(28,25,23,0.4)" : "rgba(28,25,23,0.2)")
        } }, opt.icon), /* @__PURE__ */ React.createElement("span", { style: {
          width: 15,
          height: 15,
          borderRadius: "50%",
          flexShrink: 0,
          border: "1px solid " + (on ? "#1c1917" : "rgba(28,25,23,0.33)"),
          background: on ? "#1c1917" : "transparent",
          display: "flex",
          alignItems: "center",
          justifyContent: "center"
        } }, on && /* @__PURE__ */ React.createElement("span", { style: { width: 5, height: 5, borderRadius: "50%", background: "#ece5d8" } }))),
        /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 16px/1.2 var(--roma-serif)", color: "#1c1917", letterSpacing: "-0.012em" } }, opt.label),
        /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 11.5px/1.5 var(--roma-sans)", color: "rgba(28,25,23,0.7)" } }, opt.sub),
        /* @__PURE__ */ React.createElement("span", { style: {
          marginTop: "auto",
          paddingTop: "0.25rem",
          font: "500 9px/1 ui-monospace, monospace",
          letterSpacing: "0.16em",
          textTransform: "uppercase",
          color: on ? "#a87935" : "#8a7e68"
        } }, "\u25CF", " ", opt.cost)
      );
    })), form.delivery_method === "pickup" && /* @__PURE__ */ React.createElement("div", { style: { padding: "0.75rem 1rem", border: "0.5px solid rgba(28,25,23,0.08)", background: "rgba(28,25,23,0.02)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: "0.45rem" } }, "Pickup location"), /* @__PURE__ */ React.createElement("div", { style: { font: "500 0.8125rem/1.3 Inter, sans-serif", color: "#1c1917" } }, "Roma Flooring Designs"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.8125rem/1.5 Inter, sans-serif", color: "#8a7e68", marginTop: "0.2rem" } }, "1440 S. State College Blvd., Suite 6M", /* @__PURE__ */ React.createElement("br", null), "Anaheim, CA 92806")), form.delivery_method === "will_call" && /* @__PURE__ */ React.createElement("div", { style: { padding: "0.75rem 1rem", border: "0.5px solid rgba(28,25,23,0.08)", background: "rgba(28,25,23,0.02)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: "0.45rem" } }, "How will-call works"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.8125rem/1.5 Inter, sans-serif", color: "#8a7e68" } }, "No freight is charged. Once the order is paid and the distributor PO is sent, open the order and use ", /* @__PURE__ */ React.createElement("b", { style: { color: "#1c1917" } }, "Release materials"), " to print the customer\u2019s will-call form \u2014 it carries the PO number the distributor releases against.")), form.delivery_method === "shipping" && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-field full" }, /* @__PURE__ */ React.createElement("label", null, "Address Line 1"), /* @__PURE__ */ React.createElement(
      "input",
      {
        ref: quoteAddrRef,
        type: "text",
        placeholder: isReadonly ? "" : "Street address",
        value: form.shipping_address_line1,
        onChange: (e) => setForm((f) => ({ ...f, shipping_address_line1: e.target.value })),
        disabled: isReadonly
      }
    ), !isReadonly && quotePrevAddrs.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.3rem" } }, /* @__PURE__ */ React.createElement(
      "span",
      {
        style: { font: "400 0.6875rem/1 Inter, sans-serif", color: "#8a7e68", cursor: "pointer", textDecoration: "underline" },
        onClick: () => setShowQuotePrevAddrs((s) => !s)
      },
      showQuotePrevAddrs ? "Hide" : "Use",
      " previous address (",
      quotePrevAddrs.length,
      ")"
    ), showQuotePrevAddrs && /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.12)", marginTop: "0.3rem" } }, quotePrevAddrs.map((addr, i) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: i,
        style: { padding: "0.4rem 0.75rem", cursor: "pointer", borderBottom: "0.5px solid rgba(28,25,23,0.06)" },
        onClick: () => selectQuotePrevAddr(addr),
        onMouseEnter: (e) => e.currentTarget.style.background = "rgba(28,25,23,0.04)",
        onMouseLeave: (e) => e.currentTarget.style.background = "transparent"
      },
      /* @__PURE__ */ React.createElement("div", { style: { font: "500 0.8125rem/1.3 Inter, sans-serif" } }, addr.line1),
      /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.6875rem/1.3 Inter, sans-serif", color: "#8a7e68" } }, [addr.city, addr.state, addr.zip].filter(Boolean).join(", "))
    ))))), /* @__PURE__ */ React.createElement("div", { className: "rof-field full" }, /* @__PURE__ */ React.createElement("label", null, "Address Line 2"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: form.shipping_address_line2,
        onChange: (e) => setForm((f) => ({ ...f, shipping_address_line2: e.target.value })),
        disabled: isReadonly
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row-3" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "City"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: form.shipping_city,
        onChange: (e) => setForm((f) => ({ ...f, shipping_city: e.target.value })),
        disabled: isReadonly
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "State"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 2,
        value: form.shipping_state,
        onChange: (e) => setForm((f) => ({ ...f, shipping_state: e.target.value })),
        disabled: isReadonly
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "ZIP Code"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 10,
        value: form.shipping_zip,
        onChange: (e) => setForm((f) => ({ ...f, shipping_zip: e.target.value })),
        disabled: isReadonly
      }
    ))), /* @__PURE__ */ React.createElement("div", { style: {
      marginTop: "0.75rem",
      padding: "0.75rem 0.875rem",
      background: "rgba(28,25,23,0.03)",
      border: "0.5px solid rgba(28,25,23,0.12)",
      display: "flex",
      justifyContent: "space-between",
      alignItems: "center",
      gap: "1rem",
      flexWrap: "wrap"
    } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.6875rem/1.4 Inter, sans-serif", color: "#8a7e68" } }, "Charged to the customer ", "\xB7", " added to the quote total"), /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", gap: "0.75rem", flexShrink: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "baseline", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, "Freight estimate"), /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 15px/1 var(--roma-serif)", color: "#1c1917" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        inputMode: "decimal",
        value: form.shipping,
        placeholder: "0.00",
        disabled: isReadonly,
        onChange: (e) => {
          if (e.target.value === "" || /^\d*\.?\d{0,2}$/.test(e.target.value)) setForm((f) => ({ ...f, shipping: e.target.value }));
        },
        onKeyDown: (e) => {
          if (e.key === "Enter" && !saveDisabled) saveQuote();
        },
        style: { width: 72, border: "none", borderBottom: "0.5px solid rgba(28,25,23,0.33)", background: "transparent", outline: "none", font: "400 17px/1 var(--roma-serif)", color: "#1c1917", padding: "0 0 2px", textAlign: "right" }
      }
    ))))))), /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Lines \xB7 " + quoteItems.length,
        actions: showCost ? /* @__PURE__ */ React.createElement("span", { style: { font: "500 0.5625rem/1 ui-monospace, monospace", letterSpacing: "0.08em", textTransform: "uppercase", color: "#a87935" } }, "Cost + margin \xB7 rep only") : null
      },
      !isReadonly && /* @__PURE__ */ React.createElement("button", { className: "rof-cat-launch", onClick: () => setCatalogOpen(true) }, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-icon" }, "\u229E"), /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-title" }, "Browse the full catalog"), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-sub" }, "Filter by vendor, set quantities & add accessories in one pass")), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-cta" }, "Open catalog ", "\u2192")),
      !isReadonly && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.625rem", marginBottom: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap", style: { flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: productSearch,
          placeholder: "Quick add \u2014 search SKU or product name...",
          onFocus: () => {
            if (searchResults.length > 0) setShowResults(true);
          },
          onBlur: () => setTimeout(() => setShowResults(false), 200),
          onChange: (e) => handleProductSearch(e.target.value),
          style: { width: "100%", padding: "0.5625rem 0.75rem", border: "0.5px solid rgba(28,25,23,0.18)", background: "#ece5d8", font: "400 0.8125rem/1.4 Inter, sans-serif", boxSizing: "border-box" }
        }
      ), showResults && searchResults.length === 0 && productSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results", style: { padding: "0.75rem", color: "#8a7e68", fontSize: "0.8125rem" } }, "Searching..."), showResults && searchResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, searchResults.slice(0, 20).map((r, i) => {
        const rSuffix = skuPriceSuffix(r);
        const rRetail = pieceRetail(r, r.price);
        const rTrade = rofTradeUnit(rRetail, r.retail_locked, tradeDiscountFor(selectedCustomer));
        const rDisc = rRetail > 0 && rTrade < rRetail;
        return /* @__PURE__ */ React.createElement("div", { key: r.sku_id || i, className: "rof-search-item", onMouseDown: () => addProductToQuote(r) }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(r.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover", flexShrink: 0 }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: r.collection }), 50), flexShrink: 0 } }), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0 } }, formatLineItem(r).brand && /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, formatLineItem(r).brand), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, formatLineItem(r).name), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, formatLineItem(r).sku, rRetail > 0 && "  \xB7  ", rRetail > 0 && rDisc && /* @__PURE__ */ React.createElement("span", { style: { textDecoration: "line-through", color: "#8a7e68" } }, "$", rRetail.toFixed(2)), rRetail > 0 && /* @__PURE__ */ React.createElement("span", { style: { color: rDisc ? "#a87935" : void 0 } }, rDisc ? " " : "", "$", rTrade.toFixed(2), rSuffix))));
      }))), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "1",
          value: addQty,
          onChange: (e) => setAddQty(parseInt(e.target.value) || 1),
          placeholder: "Qty",
          title: "Quantity to add",
          style: { width: "60px", padding: "0.5625rem 0.375rem", border: "0.5px solid rgba(28,25,23,0.18)", background: "#ece5d8", font: "400 0.8125rem/1 Inter, sans-serif", textAlign: "center" }
        }
      )),
      quoteItems.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-items-header" + (showCost ? "" : " rof-nocost") }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Product"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Qty"), showCost && /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Cost \xB7 Mgn"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Price"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Subtotal")),
      quoteItems.map((item, idx) => {
        const isCustom = !item.product_id;
        const mgn = lineMargin(item);
        const qtyField = item.sell_by === "roll" ? "sqft_needed" : "num_boxes";
        const qtyVal = item.sell_by === "roll" ? item.sqft_needed || 0 : item.num_boxes;
        const isEditingQty = editingCell && editingCell.idx === idx && editingCell.field === "qty";
        const isEditingPrice = editingCell && editingCell.idx === idx && editingCell.field === "price";
        const isEditingCost = editingCell && editingCell.idx === idx && editingCell.field === "cost";
        const fli = formatLineItem(item);
        const brandBit = isCustom ? "" : fli.brand;
        const metaBits = [
          isCustom ? "Custom" : fli.sku || null,
          item.sell_by !== "unit" && item.sell_by !== "piece" && item.sqft_needed ? parseFloat(item.sqft_needed).toFixed(1) + " sqft" : null
        ].filter(Boolean).join("  \xB7  ");
        return /* @__PURE__ */ React.createElement("div", { key: item.id || item._temp_id || idx, className: "rof-item-row" + (showCost ? "" : " rof-nocost") }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-swatch" }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover" }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: item.collection || item.current_collection }), 50) } })), /* @__PURE__ */ React.createElement("div", { className: "rof-item-info" }, brandBit && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, brandBit), /* @__PURE__ */ React.createElement("div", { className: "rof-item-name" }, isCustom ? item.product_name || fli.name : fli.name), !isReadonly && isCustom && (item.sell_by === "box" || item.sell_by === "sqft") ? /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta", style: { display: "flex", alignItems: "center", gap: 4, marginTop: 2 } }, /* @__PURE__ */ React.createElement("span", null, "Custom ", "\xB7", " "), /* @__PURE__ */ React.createElement(
          "input",
          {
            type: "text",
            inputMode: "decimal",
            value: item.sqft_needed != null && item.sqft_needed !== "" ? item.sqft_needed : "",
            onChange: (e) => {
              const v = e.target.value;
              if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) updateItemLocal(idx, "sqft_needed", v);
            },
            onBlur: () => saveItemEdit(item, idx),
            placeholder: "0",
            style: { width: 52, padding: "1px 5px", border: "0.5px solid rgba(28,25,23,0.25)", background: "transparent", font: "inherit", color: "inherit", boxSizing: "border-box" }
          }
        ), /* @__PURE__ */ React.createElement("span", null, "sqft")) : metaBits && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, metaBits)), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, !isReadonly && isEditingQty ? /* @__PURE__ */ React.createElement(
          "input",
          {
            className: "rof-edit-input",
            type: "text",
            inputMode: "numeric",
            autoFocus: true,
            value: cellValue,
            onChange: (e) => {
              const v = e.target.value;
              if (v === "" || /^\d+$/.test(v)) {
                setCellValue(v);
                updateItemLocal(idx, qtyField, v || "1");
              }
            },
            onKeyDown: (e) => {
              if (e.key === "Enter") e.target.blur();
              if (e.key === "Escape") cancelCellEdit();
            },
            onBlur: () => {
              saveItemEdit(item, idx);
              cancelCellEdit();
            }
          }
        ) : !isReadonly ? /* @__PURE__ */ React.createElement("button", { className: "rof-edit-cell", onClick: () => startCellEdit(idx, "qty", qtyVal) }, qtyVal, " ", /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5625rem", color: "#8a7e68", marginLeft: "0.1rem" } }, qtyLabel(item.sell_by))) : /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.8125rem/1 Inter, sans-serif", color: "#1c1917" } }, qtyVal, " ", /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5625rem", color: "#8a7e68" } }, qtyLabel(item.sell_by)))), showCost && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", alignItems: "baseline", justifyContent: "flex-end", gap: "0.35rem" } }, !isReadonly && isEditingCost ? /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.8125rem", color: "#8a7e68", marginRight: "0.1rem" } }, "$"), /* @__PURE__ */ React.createElement(
          "input",
          {
            className: "rof-edit-input",
            type: "text",
            inputMode: "decimal",
            autoFocus: true,
            value: cellValue,
            onChange: (e) => {
              const v = e.target.value;
              if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) {
                setCellValue(v);
                updateItemLocal(idx, "vendor_cost", v);
              }
            },
            onKeyDown: (e) => {
              if (e.key === "Enter") e.target.blur();
              if (e.key === "Escape") cancelCellEdit();
            },
            onBlur: () => {
              saveItemEdit(item, idx, true);
              cancelCellEdit();
            }
          }
        )) : !isReadonly ? /* @__PURE__ */ React.createElement(
          "button",
          {
            className: "rof-edit-cell",
            style: { color: "#8a7e68" },
            onClick: () => startCellEdit(idx, "cost", parseFloat(item.vendor_cost || 0).toFixed(2))
          },
          parseFloat(item.vendor_cost) > 0 ? "$" + parseFloat(item.vendor_cost).toFixed(2) : "\u2014"
        ) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.75rem/1.3 Inter, sans-serif", color: "#8a7e68" } }, parseFloat(item.vendor_cost) > 0 ? "$" + parseFloat(item.vendor_cost).toFixed(2) : "\u2014"), mgn !== null && /* @__PURE__ */ React.createElement("span", { style: { font: "500 0.5625rem/1.3 ui-monospace, monospace", letterSpacing: "0.04em", color: "#a87935" } }, mgn.toFixed(1) + "%")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, !isReadonly && isEditingPrice ? /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.8125rem", color: "#8a7e68", marginRight: "0.1rem" } }, "$"), /* @__PURE__ */ React.createElement(
          "input",
          {
            className: "rof-edit-input",
            type: "text",
            inputMode: "decimal",
            autoFocus: true,
            value: cellValue,
            onChange: (e) => {
              const v = e.target.value;
              if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) {
                setCellValue(v);
                updateItemLocal(idx, "unit_price", v || "0");
              }
            },
            onKeyDown: (e) => {
              if (e.key === "Enter") e.target.blur();
              if (e.key === "Escape") cancelCellEdit();
            },
            onBlur: () => {
              saveItemEdit(item, idx);
              cancelCellEdit();
            }
          }
        )) : !isReadonly ? /* @__PURE__ */ React.createElement("button", { className: "rof-edit-cell", onClick: () => startCellEdit(idx, "price", parseFloat(item.unit_price || 0).toFixed(2)) }, "$", parseFloat(item.unit_price || 0).toFixed(2), /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5rem", color: "#8a7e68", marginLeft: "0.15rem" } }, unitSuffix(item.sell_by))) : /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.8125rem/1 Inter, sans-serif", color: "#1c1917" } }, "$", parseFloat(item.unit_price || 0).toFixed(2), /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5rem", color: "#8a7e68", marginLeft: "0.15rem" } }, unitSuffix(item.sell_by)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.375rem", justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("span", { className: "rof-item-price" }, "$", parseFloat(item.subtotal || 0).toFixed(2)), !isReadonly && /* @__PURE__ */ React.createElement(
          "button",
          {
            className: "rof-remove-btn",
            onClick: () => removeItem(item, idx),
            title: "Remove",
            "aria-label": "Remove " + (item.product_name || "line " + (idx + 1))
          },
          "\u2715"
        )));
      }),
      quoteItems.length === 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-items-empty" }, "No lines yet. Search the catalog or add a custom item above.")
    ), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Notes" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Customer Notes (shown on PDF)"), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        rows: "3",
        value: form.notes,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, notes: e.target.value })),
        placeholder: "Notes visible to customer..."
      }
    ))), rugModalItem && /* @__PURE__ */ React.createElement(
      RugConfigModal,
      {
        carpet: rugModalItem,
        showCost,
        onClose: () => setRugModalItem(null),
        onAdd: addRugToQuote
      }
    )), /* @__PURE__ */ React.createElement("aside", { className: "qw-rail" }, editId && quote && /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Sales rep"), quote.sales_rep_id ? /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, (quote.rep_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name" }, quote.rep_name || "Rep", quote.sales_rep_id === repInfo.id && /* @__PURE__ */ React.createElement("span", { className: "ws-you-tag" }, "You")), quote.sales_rep_id === repInfo.id && repInfo.email && /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, repInfo.email))) : /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar", style: { background: "var(--brass-warm, #d8cdb6)", color: "var(--brass-muted, #8a7e68)" } }, "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name", style: { color: "var(--rod-muted)" } }, "Unassigned"), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "No rep owns this quote yet")))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Totals"), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement(RovRow, { l: "Subtotal \xB7 " + quoteItems.length + " line" + (quoteItems.length !== 1 ? "s" : ""), v: "$" + displaySubtotal.toFixed(2) }), displayDiscount > 0 && /* @__PURE__ */ React.createElement(RovRow, { l: "Discount" + (promoResult && promoResult.code ? " \xB7 " + promoResult.code : ""), v: "\u2212$" + displayDiscount.toFixed(2), c: "var(--rod-green)" }), displayShipping > 0 && /* @__PURE__ */ React.createElement(RovRow, { l: "Shipping", v: "$" + displayShipping.toFixed(2) })), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 10, paddingTop: 10, borderTop: "0.5px solid rgba(28,25,23,0.1)" } }, isReadonly ? promoResult ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "600 0.75rem/1 ui-monospace, monospace", color: "var(--green, #16a34a)", letterSpacing: "0.04em" } }, promoResult.code), /* @__PURE__ */ React.createElement("span", { style: { font: '300 1rem/1 "Cormorant Garamond", serif', color: "var(--green, #16a34a)" } }, "-$", promoResult.discount_amount.toFixed(2))) : /* @__PURE__ */ React.createElement("span", { style: { color: "#8a7e68", fontSize: "0.8125rem" } }, "No promo code applied") : promoResult ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "600 0.75rem/1 ui-monospace, monospace", color: "var(--green, #16a34a)", letterSpacing: "0.04em" } }, promoResult.code), /* @__PURE__ */ React.createElement("span", { style: { display: "flex", alignItems: "center", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("span", { style: { font: '300 1rem/1 "Cormorant Garamond", serif', color: "var(--green, #16a34a)" } }, "-$", promoResult.discount_amount.toFixed(2)), /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light danger", onClick: removeRepPromo, style: { padding: "0.2rem 0.5rem", fontSize: "0.5rem" } }, "Remove"))) : /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { flex: 1, marginBottom: 0 } }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 50,
        value: promoCode,
        onChange: (e) => {
          setPromoCode(e.target.value.toUpperCase());
          setPromoError("");
        },
        onKeyDown: (e) => e.key === "Enter" && applyRepPromo(),
        placeholder: "Promo code"
      }
    )), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn-light primary",
        onClick: applyRepPromo,
        disabled: promoLoading || !promoCode.trim(),
        style: { padding: "0.4rem 0.75rem", alignSelf: "flex-start" }
      },
      promoLoading ? "..." : "Apply"
    )), promoError && /* @__PURE__ */ React.createElement("div", { style: { color: "var(--red, #dc2626)", fontSize: "0.75rem", marginTop: "0.375rem" } }, promoError))), /* @__PURE__ */ React.createElement("div", { className: "rov-total-row", style: { marginTop: 12 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-ink)" } }, "Total"), /* @__PURE__ */ React.createElement("span", { className: "rov-total-value" }, "$", displayTotal.toFixed(2))), quote && expiresAt && ["draft", "sent", "accepted"].includes(quote.status) && /* @__PURE__ */ React.createElement("div", { className: "qw-commission", style: isExpired ? { color: "#c54731" } : void 0 }, isExpired ? "Expired " : "Expires ", expiresAt.toLocaleDateString("en-US", { month: "short", day: "numeric" }), !isExpired && daysLeft !== null ? " \xB7 " + daysLeft + "d left" : ""), showCost && blendedMargin !== null && /* @__PURE__ */ React.createElement("div", { className: "qw-commission" }, "Blended margin ", blendedMargin.toFixed(1), "% \xB7 rep only"), showCost && blendedMargin !== null && /* @__PURE__ */ React.createElement("div", { className: "qw-commission" }, "Est. commission $", (grossProfit * 0.1).toFixed(2))), !editId && /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Draft checklist"), checklist.map((c, i) => {
      const state = c.done ? "done" : i === firstTodo ? "now" : "next";
      return /* @__PURE__ */ React.createElement("div", { key: c.label, className: "qw-check-row " + state }, /* @__PURE__ */ React.createElement("span", { className: "qw-check-mark" }, c.done ? "\u2713" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", { className: "qw-check-label" }, c.label), /* @__PURE__ */ React.createElement("span", { className: "qw-check-meta" }, c.meta));
    })), editId && quote && /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Engagement"), /* @__PURE__ */ React.createElement("div", { className: "qw-engage-grid" }, [[engOpens, engOpens === 1 ? "open" : "opens"], [engViewers, engViewers === 1 ? "viewer" : "viewers"], [engReplies, engReplies === 1 ? "reply" : "replies"]].map(([n, l]) => /* @__PURE__ */ React.createElement("div", { key: l }, /* @__PURE__ */ React.createElement("div", { className: "qw-engage-n" }, n), /* @__PURE__ */ React.createElement("div", { className: "qw-engage-l" }, l)))), /* @__PURE__ */ React.createElement("div", { className: "qw-engage-foot" }, engOpens > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Last opened ", fmtDT(lastViewedAt), lastReplyAt ? " \xB7 last reply " + fmtDT(lastReplyAt) : "", ".") : quote.status === "draft" ? "Send the quote to start tracking opens." : "No opens yet \u2014 tracking starts when the customer opens the email.")), canConvert && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Ready to commit"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "Converting creates the order at quoted pricing, takes payment, and drafts the vendor POs."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: () => setShowConvertModal(true), disabled: saving || quoteItems.length === 0 }, "Convert to order ", "\u2192"), expiresAt && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-note" }, "Quoted pricing held until ", fmtD(expiresAt)), !expiresAt && quoteItems.length === 0 && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-note" }, "Add lines before converting")), editId && isExpired && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Quote expired"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "Payment is blocked on an expired quote. Reinstate it to refresh the pricing window (10 days) and convert."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: reinstateQuote, disabled: reinstating }, reinstating ? "Reinstating\u2026" : /* @__PURE__ */ React.createElement(React.Fragment, null, "Reinstate quote ", "\u2192"))), quote && quote.status === "converted" && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Converted"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "This quote became an order", quote.payment_method ? " \xB7 paid by " + quote.payment_method : "", "."), quote.converted_order_id && /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: () => navigate("order-detail", quote.converted_order_id) }, "View order ", "\u2192")), !editId && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Ready to draft"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "Creating saves the draft and unlocks preview, send, and convert-to-order."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: saveQuote, disabled: saveDisabled }, saving ? "Creating\u2026" : /* @__PURE__ */ React.createElement(React.Fragment, null, "Create quote ", "\u2192")), saveDisabled && !saving && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-note" }, "Complete customer details to create")), editId ? /* @__PURE__ */ React.createElement(InternalNotesCard, { basePath: "/api/rep/quotes/" + editId, readOnly: isReadonly, bg: "#f5f0e8" }) : /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Internal notes"), /* @__PURE__ */ React.createElement("p", { style: { margin: "4px 0 0", fontSize: "0.8125rem", color: "#8a7e68" } }, "Save the quote to start adding internal notes.")), /* @__PURE__ */ React.createElement("div", { className: "qw-quick-links" }, quote && quote.status === "sent" && /* @__PURE__ */ React.createElement("button", { className: "qw-link muted", onClick: openPreview }, "Resend email"), /* @__PURE__ */ React.createElement("button", { className: "qw-link muted", onClick: () => navigate("quotes") }, "Back to quotes")))), /* @__PURE__ */ React.createElement("div", { className: "rof-action-bar" }, /* @__PURE__ */ React.createElement("div", { className: "rof-action-left" }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)" } }, editId && quote ? quote.quote_number + " \xB7 " + st.label + (quote.converted_order_number ? " \xB7 " + quote.converted_order_number : "") : "New quote \xB7 draft"), /* @__PURE__ */ React.createElement("span", { className: "rof-action-total", style: { display: "block", marginTop: 4 } }, "$", displayTotal.toFixed(2))), (form.customer_name || quoteItems.length > 0) && /* @__PURE__ */ React.createElement("span", { className: "qw-action-context", style: { font: "500 9px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "rgba(245,240,232,0.6)" } }, [
      form.customer_name,
      quoteItems.length > 0 ? quoteItems.length + " line" + (quoteItems.length !== 1 ? "s" : "") : null,
      form.delivery_method === "pickup" ? "Pickup" : form.delivery_method === "will_call" ? "Will-call" : "Shipping"
    ].filter(Boolean).join(" \xB7 ")), savedFlash && !saving && /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#7fb98f" } }, "\u2713", " Saved")), !editId ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => navigate("quotes") }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: saveQuote, disabled: saveDisabled }, saving ? "Creating\u2026" : "Create Quote")) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => navigate("quotes") }, "Close"), !isReadonly && /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: saveQuote, disabled: saveDisabled }, saving ? "Saving\u2026" : "Save"), quote && quote.status === "draft" && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: openPreview, disabled: saving }, "Preview & Send"), quote && quote.status === "sent" && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: openPreview, disabled: saving }, "Send Revision"), quote && quote.status === "accepted" && quoteItems.length > 0 && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: () => setShowConvertModal(true), disabled: saving }, "Convert to Order ", "\u2192"), quote && quote.status === "converted" && quote.converted_order_id && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: () => navigate("order-detail", quote.converted_order_id) }, "View Order ", "\u2192"))), catalogOpen && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data: { customer: selectedCustomer, newCustomerName: form.customer_name },
        showCost,
        contextLabel: "Add to quote \xB7 " + (quote ? quote.quote_number : "New quote"),
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog,
        onAddRug: addRugToQuote
      }
    ), pdfViewer && /* @__PURE__ */ React.createElement(
      RepPdfViewer,
      {
        url: pdfViewer.url,
        filename: pdfViewer.filename,
        downloadPath: "/api/rep/quotes/" + editId + "/pdf",
        onClose: () => setPdfViewer(null)
      }
    ), showPreview && /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => setShowPreview(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box modal-wide qw-email-modal", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("div", { className: "qw-email-head" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-email-eyebrow" }, "\u25CF", " Email preview"), /* @__PURE__ */ React.createElement("h3", { style: { margin: "3px 0 0" } }, previewMeta ? previewMeta.subject : "Quote email")), /* @__PURE__ */ React.createElement("button", { className: "qw-email-x", onClick: () => setShowPreview(false), "aria-label": "Close" }, "\u2715")), previewLoading ? /* @__PURE__ */ React.createElement("div", { className: "qw-email-loading" }, "Loading preview", "\u2026") : /* @__PURE__ */ React.createElement(React.Fragment, null, previewMeta && /* @__PURE__ */ React.createElement("div", { className: "qw-email-meta" }, /* @__PURE__ */ React.createElement("div", { className: "qw-email-row" }, /* @__PURE__ */ React.createElement("span", { className: "qw-email-k" }, "To"), /* @__PURE__ */ React.createElement("div", { className: "qw-recip-field", onClick: (e) => {
      const el = e.currentTarget.querySelector("input");
      if (el) el.focus();
    } }, previewRecipients.map((r) => /* @__PURE__ */ React.createElement("span", { className: "qw-recip-chip", key: r }, r, /* @__PURE__ */ React.createElement("button", { type: "button", onClick: () => removeRecipient(r), "aria-label": "Remove " + r }, "\u2715"))), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        value: recipientDraft,
        placeholder: previewRecipients.length ? "Add another\u2026" : "name@email.com",
        onChange: (e) => setRecipientDraft(e.target.value),
        onKeyDown: (e) => {
          if ((e.key === "Enter" || e.key === "," || e.key === ";") && recipientDraft.trim()) {
            e.preventDefault();
            addRecipient(recipientDraft);
          } else if (e.key === "Backspace" && !recipientDraft && previewRecipients.length) {
            removeRecipient(previewRecipients[previewRecipients.length - 1]);
          }
        },
        onBlur: () => {
          if (recipientDraft.trim()) addRecipient(recipientDraft);
        }
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "qw-email-row" }, /* @__PURE__ */ React.createElement("span", { className: "qw-email-k" }, "Reply-To"), /* @__PURE__ */ React.createElement("span", { className: "qw-email-v" }, previewMeta.reply_to))), /* @__PURE__ */ React.createElement("iframe", { className: "qw-email-frame", srcDoc: previewHtml, sandbox: "", title: "Email preview" })), /* @__PURE__ */ React.createElement("div", { className: "qw-email-actions" }, /* @__PURE__ */ React.createElement("span", { className: "qw-email-count" }, (() => {
      const n = previewRecipients.length + (isValidEmail(recipientDraft.trim()) ? 1 : 0);
      return n + " recipient" + (n === 1 ? "" : "s");
    })()), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowPreview(false) }, "Cancel"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "btn btn-primary",
        onClick: sendQuote,
        disabled: saving || previewLoading || previewRecipients.length === 0 && !isValidEmail(recipientDraft.trim())
      },
      saving ? "Sending\u2026" : "Confirm & Send"
    ))))), showConvertModal && /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => setShowConvertModal(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("h3", null, "Convert Quote to Order"), /* @__PURE__ */ React.createElement("p", null, "Choose a payment method for this order:"), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Payment Method"), /* @__PURE__ */ React.createElement("div", { className: "mobile-stack", style: { display: "flex", gap: "0.5rem", flexWrap: "wrap" } }, [
      { value: "card", label: "Card", icon: "\u{1F4B3}" },
      { value: "cash", label: "Cash", icon: "\u{1F4B5}" },
      { value: "check", label: "Check", icon: "\u{1F4C4}" },
      { value: "stripe", label: "Payment Request", icon: "\u{1F4E9}" }
    ].map((opt) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: opt.value,
        type: "button",
        className: convertMethod === opt.value ? "btn btn-primary btn-sm" : "btn btn-secondary btn-sm",
        style: { padding: "0.5rem 1rem", fontSize: "0.8125rem", flex: 1 },
        onClick: () => setConvertMethod(opt.value)
      },
      opt.icon,
      " ",
      opt.label
    ))), convertMethod === "check" && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.5rem" } }, /* @__PURE__ */ React.createElement("label", { style: { fontSize: "0.8125rem", fontWeight: 500, display: "block", marginBottom: "0.25rem" } }, "Check Number *"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 20,
        value: convertCheckNumber || "",
        onChange: (e) => setConvertCheckNumber(e.target.value),
        placeholder: "Enter check number",
        style: { width: "100%", padding: "0.6rem 0.75rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem" }
      }
    )), convertMethod === "cash" && convertCashDrawerOpen === false && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.5rem", padding: "0.5rem 0.75rem", background: "#fef3c7", border: "1px solid #f59e0b", borderRadius: "6px", fontSize: "0.8125rem", color: "#92400e" } }, "No cash drawer open. Open one from the Cash Drawer tab first."), convertMethod === "card" && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.5rem", padding: "0.5rem 0.75rem", background: "#eff6ff", border: "1px solid #93c5fd", borderRadius: "6px", fontSize: "0.8125rem", color: "#1e40af" } }, "Tap to pay or enter card details")), /* @__PURE__ */ React.createElement("div", { className: "modal-actions" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowConvertModal(false) }, "Cancel"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "btn btn-primary",
        onClick: convertQuote,
        disabled: saving || convertMethod === "cash" && !convertCashDrawerOpen || convertMethod === "check" && !(convertCheckNumber || "").trim()
      },
      saving ? "Converting..." : convertMethod === "card" ? "Pay with Card" : "Convert to Order"
    )))), showConvertTapModal && /* @__PURE__ */ React.createElement(
      TapToPayModal,
      {
        amount: parseFloat(quote.total || 0),
        onSuccess: (res) => {
          setShowConvertTapModal(false);
          convertQuoteWithPI(res);
        },
        onCancel: () => setShowConvertTapModal(false)
      }
    ), showConvertCardChoice && /* @__PURE__ */ React.createElement(
      CardMethodChoiceModal,
      {
        amount: parseFloat(quote.total || 0),
        terminalConnected: terminalStatus === "connected",
        onTap: () => {
          setShowConvertCardChoice(false);
          if (terminalStatus !== "connected") {
            const confirmed = window.confirm("No Valor terminal set up. Set it up now?");
            if (confirmed) connectReader();
            return;
          }
          setShowConvertTapModal(true);
        },
        onManual: () => {
          setShowConvertCardChoice(false);
          setShowConvertManualCard(true);
        },
        onCancel: () => setShowConvertCardChoice(false)
      }
    ), showConvertManualCard && /* @__PURE__ */ React.createElement(
      ManualCardEntryModal,
      {
        amount: parseFloat(quote.total || 0),
        onSuccess: (piId) => {
          setShowConvertManualCard(false);
          convertQuoteWithPI({ payment_intent_id: piId });
        },
        onCancel: () => setShowConvertManualCard(false)
      }
    ), showConvertAchModal && /* @__PURE__ */ React.createElement(
      AchPaymentModal,
      {
        amount: parseFloat(quote.total || 0),
        customerName: quote.customer_name,
        customerEmail: quote.customer_email,
        onSuccess: (piId, docIds) => {
          setShowConvertAchModal(false);
          convertQuoteWithPI({ payment_intent_id: piId }, docIds);
        },
        onCancel: () => setShowConvertAchModal(false)
      }
    ));
  }
  function RepEstimatesListView({ navigate }) {
    const [estimates, setEstimates] = useState([]);
    const [loading, setLoading] = useState(true);
    const [search, setSearch] = useState("");
    const [statusFilter, setStatusFilter] = useState("");
    const [activeView, setActiveView] = useState("all");
    const [scope, setScope] = useState("team");
    const [selected, setSelected] = useState(/* @__PURE__ */ new Set());
    const [page, setPage] = useState(1);
    const perPage = 50;
    const [bulkSending, setBulkSending] = useState(false);
    const [bulkMsg, setBulkMsg] = useState(null);
    const load = useCallback(() => {
      setLoading(true);
      const params = new URLSearchParams();
      if (search) params.set("search", search);
      if (statusFilter) params.set("status", statusFilter);
      if (scope === "team") params.set("scope", "team");
      repFetch("/api/rep/estimates?" + params.toString()).then((data) => {
        setEstimates(data.estimates || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [search, statusFilter, scope]);
    useEffect(load, [load]);
    const effOf = (e) => e.effective_status || e.status;
    const viewFilters = {
      "open": (e) => ["draft", "sent", "accepted"].includes(effOf(e)),
      "hot": (e) => effOf(e) === "sent" && (e.view_count || 0) >= 3,
      "drafts": (e) => effOf(e) === "draft",
      "awaiting": (e) => effOf(e) === "sent",
      "expiring": (e) => {
        if (!e.expires_at) return false;
        const d = Math.ceil((new Date(e.expires_at) - Date.now()) / 864e5);
        return d >= 0 && d <= 7 && ["draft", "sent", "accepted"].includes(effOf(e));
      },
      "accepted": (e) => effOf(e) === "accepted",
      "declined": (e) => effOf(e) === "declined",
      "converted": (e) => effOf(e) === "converted",
      "expired": (e) => effOf(e) === "expired",
      "all": () => true
    };
    const filteredEstimates = useMemo(() => {
      const fn = viewFilters[activeView] || (() => true);
      return estimates.filter(fn);
    }, [estimates, activeView]);
    const viewCounts = useMemo(() => ({
      "open": estimates.filter(viewFilters["open"]).length,
      "hot": estimates.filter(viewFilters["hot"]).length,
      "drafts": estimates.filter(viewFilters["drafts"]).length,
      "awaiting": estimates.filter(viewFilters["awaiting"]).length,
      "expiring": estimates.filter(viewFilters["expiring"]).length,
      "accepted": estimates.filter(viewFilters["accepted"]).length,
      "declined": estimates.filter(viewFilters["declined"]).length,
      "converted": estimates.filter(viewFilters["converted"]).length,
      "expired": estimates.filter(viewFilters["expired"]).length,
      "all": estimates.length
    }), [estimates]);
    const totalPages = Math.ceil(filteredEstimates.length / perPage);
    const pageEstimates = filteredEstimates.slice((page - 1) * perPage, page * perPage);
    const toggleSelect = (id) => {
      setSelected((prev) => {
        const next = new Set(prev);
        next.has(id) ? next.delete(id) : next.add(id);
        return next;
      });
    };
    const toggleAll = () => {
      if (selected.size === pageEstimates.length) setSelected(/* @__PURE__ */ new Set());
      else setSelected(new Set(pageEstimates.map((e) => e.id)));
    };
    const bulkResend = async () => {
      const eligible = estimates.filter((e) => selected.has(e.id) && ["sent", "expired"].includes(effOf(e)));
      if (!eligible.length) {
        setBulkMsg({ type: "warn", text: "Select sent or expired estimates to re-send." });
        return;
      }
      setBulkSending(true);
      setBulkMsg(null);
      try {
        const result = await repFetch("/api/rep/estimates/bulk-resend", {
          method: "POST",
          body: JSON.stringify({ ids: eligible.map((e) => e.id) })
        });
        const sent = result.sent || 0, failed = result.failed || 0;
        setBulkMsg({
          type: failed ? "warn" : "success",
          text: "Re-sent " + sent + " estimate" + (sent !== 1 ? "s" : "") + (failed ? ", " + failed + " failed" : "") + "."
        });
        setSelected(/* @__PURE__ */ new Set());
        load();
      } catch (err) {
        setBulkMsg({ type: "error", text: "Bulk re-send failed. Please try again." });
      }
      setBulkSending(false);
    };
    const bulkExtend = async () => {
      const eligible = estimates.filter((e) => selected.has(e.id) && !["converted", "declined"].includes(effOf(e)));
      if (!eligible.length) {
        setBulkMsg({ type: "warn", text: "No eligible estimates (converted/declined can\u2019t be extended)." });
        return;
      }
      setBulkSending(true);
      setBulkMsg(null);
      let ok = 0, fail = 0;
      for (const e of eligible) {
        try {
          await repFetch("/api/rep/estimates/" + e.id + "/extend", { method: "POST" });
          ok++;
        } catch (err) {
          fail++;
        }
      }
      setBulkSending(false);
      setSelected(/* @__PURE__ */ new Set());
      load();
      setBulkMsg({
        type: fail ? "warn" : "success",
        text: "Extended " + ok + " estimate" + (ok !== 1 ? "s" : "") + (fail ? ", " + fail + " failed" : "") + "."
      });
    };
    const bulkExportCsv = () => {
      const rows = selected.size ? estimates.filter((e) => selected.has(e.id)) : filteredEstimates;
      if (!rows.length) {
        setBulkMsg({ type: "warn", text: "Nothing to export." });
        return;
      }
      const cols = ["Estimate", "Customer", "Project", "Items", "Materials", "Labor", "Total", "Status", "Expires", "Age", "Created"];
      const esc = (v) => '"' + String(v == null ? "" : v).replace(/"/g, '""') + '"';
      const lines = [cols.join(",")];
      rows.forEach((e) => {
        lines.push([
          e.estimate_number,
          e.customer_name,
          e.project_name || "",
          e.item_count || 0,
          parseFloat(e.materials_subtotal || 0).toFixed(2),
          parseFloat(e.labor_subtotal || 0).toFixed(2),
          parseFloat(e.total || 0).toFixed(2),
          effOf(e),
          getExpires(e).label,
          getAge(e.created_at),
          e.created_at ? new Date(e.created_at).toLocaleDateString() : ""
        ].map(esc).join(","));
      });
      const blob = new Blob([lines.join("\r\n")], { type: "text/csv;charset=utf-8;" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = "roma-estimates-" + (/* @__PURE__ */ new Date()).toISOString().slice(0, 10) + ".csv";
      document.body.appendChild(a);
      a.click();
      a.remove();
      setTimeout(() => URL.revokeObjectURL(url), 1e3);
      setBulkMsg({ type: "success", text: "Exported " + rows.length + " estimate" + (rows.length !== 1 ? "s" : "") + "." });
    };
    const getStatusColor = (s) => {
      if (s === "draft") return "#a87935";
      if (s === "sent") return "#5c6e94";
      if (s === "accepted") return "#3a7a4e";
      if (s === "converted") return "#3a7a4e";
      if (s === "declined") return "#c54731";
      if (s === "expired") return "var(--brass-muted)";
      return "var(--brass-muted)";
    };
    const getAge = (d) => {
      const days = Math.floor((Date.now() - new Date(d).getTime()) / 864e5);
      if (days === 0) return "today";
      if (days < 1) return Math.floor((Date.now() - new Date(d).getTime()) / 36e5) + "h";
      return days + "d";
    };
    const getExpires = (e) => {
      if (!e.expires_at) return { label: "\u2014", tone: "var(--brass-muted)" };
      if (["converted", "declined"].includes(effOf(e))) return { label: "\u2014", tone: "var(--brass-muted)" };
      const days = Math.ceil((new Date(e.expires_at) - Date.now()) / 864e5);
      if (days < 0) return { label: "Expired", tone: "var(--brass-muted)" };
      if (days === 0) return { label: "Today", tone: "#c54731" };
      if (days <= 2) return { label: days + " days", tone: "#a87935" };
      return { label: days + " days", tone: "var(--stone-900)" };
    };
    const VIEWS = [
      { key: "all", label: "All" },
      { key: "open", label: "Open", tone: "#a87935" },
      { key: "hot", label: "Hot", tone: "var(--gold, #c9a668)" },
      { key: "drafts", label: "Drafts" },
      { key: "awaiting", label: "Awaiting customer" },
      { key: "expiring", label: "Expiring this week", tone: "#c54731" },
      { key: "accepted", label: "Accepted", tone: "#3a7a4e" },
      { key: "declined", label: "Declined", tone: "#c54731" },
      { key: "converted", label: "Converted" },
      { key: "expired", label: "Expired" }
    ];
    const STATUSES = [
      { value: "", label: "Any" },
      { value: "draft", label: "Draft" },
      { value: "sent", label: "Sent" },
      { value: "accepted", label: "Accepted" },
      { value: "declined", label: "Declined" },
      { value: "converted", label: "Converted" },
      { value: "expired", label: "Expired" }
    ];
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25CF", " Estimates ", "\xB7", " ", estimates.filter((e) => ["draft", "sent", "accepted"].includes(effOf(e))).length, " open ", "\xB7", " ", estimates.length, " total"), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Estimates"))), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: bulkExportCsv }, "Export"), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: () => navigate("estimate-create") }, "+ New estimate"))), /* @__PURE__ */ React.createElement("div", { className: "ol-saved-views" }, VIEWS.map((v) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: v.key,
        className: "ol-view-tab" + (activeView === v.key ? " active" : ""),
        onClick: () => {
          setActiveView(v.key);
          setPage(1);
          setSelected(/* @__PURE__ */ new Set());
        }
      },
      v.label,
      /* @__PURE__ */ React.createElement("span", { className: "ol-tab-count", style: v.tone && viewCounts[v.key] > 0 ? { background: v.tone + "1a", color: v.tone } : void 0 }, viewCounts[v.key])
    )), /* @__PURE__ */ React.createElement(ScopeToggle, { scope, setScope, onChange: () => {
      setPage(1);
      setSelected(/* @__PURE__ */ new Set());
    } })), /* @__PURE__ */ React.createElement("div", { className: "ol-filter-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-filter-chips" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        className: "ol-chip",
        value: statusFilter,
        onChange: (e) => {
          setStatusFilter(e.target.value);
          setPage(1);
        },
        style: { appearance: "auto", paddingRight: 24 }
      },
      STATUSES.map((s) => /* @__PURE__ */ React.createElement("option", { key: s.value, value: s.value }, s.value ? s.label : "Status: Any"))
    ), statusFilter && /* @__PURE__ */ React.createElement("span", { className: "ol-chip active", onClick: () => setStatusFilter("") }, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-label" }, "Status:"), " ", statusFilter, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-remove" }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search estimates, customers",
        "aria-label": "Search estimates",
        value: search,
        onChange: (e) => {
          setSearch(e.target.value);
          setPage(1);
        }
      }
    )), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, filteredEstimates.length, " rows"))), selected.size > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-label" }, "\u25CF", " ", selected.size, " estimate", selected.size !== 1 ? "s" : "", " selected"), /* @__PURE__ */ React.createElement("div", { className: "ol-bulk-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: bulkResend, disabled: bulkSending }, bulkSending ? "Working\u2026" : "\u21BB Re-send"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: bulkExtend, disabled: bulkSending }, "Extend expiry"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: bulkExportCsv }, "Export CSV"), /* @__PURE__ */ React.createElement("button", { className: "ol-bulk-btn", onClick: () => setSelected(/* @__PURE__ */ new Set()) }, "\u2715", " Clear"))), bulkMsg && /* @__PURE__ */ React.createElement("div", { className: "rof-alert " + bulkMsg.type, style: { margin: "0 0 12px" } }, bulkMsg.text), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 22px", background: "#ece5d8", border: "0.5px solid rgba(28,25,23,0.14)" } }, "Loading estimates", "\u2026") : /* @__PURE__ */ React.createElement("div", { className: "ol-table-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "ol-table-head ol-table-est" }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "checkbox",
        style: { margin: 0 },
        "aria-label": "Select all estimates on page",
        checked: pageEstimates.length > 0 && selected.size === pageEstimates.length,
        onChange: toggleAll
      }
    ), /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Estimate #"), /* @__PURE__ */ React.createElement("span", null, "Customer"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Total"), /* @__PURE__ */ React.createElement("span", null, "Items"), /* @__PURE__ */ React.createElement("span", null, "Status ", "\xB7", " breakdown"), /* @__PURE__ */ React.createElement("span", null, "Engagement"), /* @__PURE__ */ React.createElement("span", null, "Expires"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Age"), /* @__PURE__ */ React.createElement("span", null, "Date"), /* @__PURE__ */ React.createElement("span", null)), pageEstimates.map((e) => {
      const isSel = selected.has(e.id);
      const es = effOf(e);
      const statusLabel = es === "draft" ? "Draft" : es === "sent" ? "Sent" : es === "accepted" ? "Accepted" : es === "declined" ? "Declined" : es === "converted" ? "Converted" : es === "expired" ? "Expired" : es;
      const stepText = "Mat $" + parseFloat(e.materials_subtotal || 0).toFixed(2) + " \xB7 Labor $" + parseFloat(e.labor_subtotal || 0).toFixed(2);
      const isConverted = es === "converted" && e.converted_order_id;
      const balDue = parseFloat(e.order_balance_due || 0);
      const goTo = () => {
        repListNav.estimates = filteredEstimates.map((x) => x.id);
        return navigate("estimate-edit", e.id);
      };
      const flagColor = es === "draft" ? "#a87935" : es === "declined" ? "#c54731" : es === "accepted" ? "#3a7a4e" : "transparent";
      return /* @__PURE__ */ React.createElement(
        "div",
        {
          key: e.id,
          className: "ol-table-row ol-table-est" + (isSel ? " selected" : ""),
          role: "button",
          tabIndex: 0,
          onClick: goTo,
          onKeyDown: (ev) => {
            if (ev.key === "Enter" || ev.key === " ") {
              ev.preventDefault();
              goTo();
            }
          }
        },
        /* @__PURE__ */ React.createElement(
          "input",
          {
            type: "checkbox",
            style: { margin: 0 },
            checked: isSel,
            "aria-label": "Select estimate " + e.estimate_number,
            onChange: () => toggleSelect(e.id),
            onClick: (ev) => ev.stopPropagation(),
            onKeyDown: (ev) => ev.stopPropagation()
          }
        ),
        /* @__PURE__ */ React.createElement("span", { className: "ol-flag", style: { background: flagColor } }),
        /* @__PURE__ */ React.createElement("span", { className: "ol-order-id" }, e.estimate_number),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-cust-name" }, e.customer_name), e.project_name && /* @__PURE__ */ React.createElement("div", { className: "ol-cust-tag" }, e.project_name)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-total" }, "$", parseFloat(e.total || 0).toFixed(2)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-install" }, e.item_count || 0, " item", parseInt(e.item_count, 10) !== 1 ? "s" : ""),
        /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-state-label", style: { color: getStatusColor(es) } }, "\u25CF", " ", statusLabel), /* @__PURE__ */ React.createElement("div", { className: "ol-state-step" }, isConverted ? /* @__PURE__ */ React.createElement(React.Fragment, null, e.converted_order_number || "Order", " \xB7 ", /* @__PURE__ */ React.createElement("span", { style: { color: balDue > 0.01 ? "#a87935" : "#3a7a4e", fontWeight: 600 } }, balDue > 0.01 ? "Balance $" + balDue.toFixed(2) : "Paid")) : stepText)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-cust-tag", style: { margin: 0 } }, e.view_count || 0, "\xD7", " opens", e.reply_count ? " \u2709" : ""),
        /* @__PURE__ */ React.createElement("span", { className: "ol-state-label", style: { color: getExpires(e).tone } }, "\u25CF", " ", getExpires(e).label),
        /* @__PURE__ */ React.createElement("span", { className: "ol-age" }, getAge(e.created_at)),
        /* @__PURE__ */ React.createElement("span", { className: "ol-install" }, new Date(e.created_at).toLocaleDateString("en-US", { month: "short", day: "numeric" })),
        /* @__PURE__ */ React.createElement("span", { className: "ol-arrow" }, "\u2192")
      );
    }), pageEstimates.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif", marginBottom: 14 } }, "No estimates match this view."), /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: () => navigate("estimate-create") }, "+ New estimate")), filteredEstimates.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "ol-footer" }, /* @__PURE__ */ React.createElement("span", null, "Showing ", Math.min((page - 1) * perPage + 1, filteredEstimates.length), "\u2013", Math.min(page * perPage, filteredEstimates.length), " of ", filteredEstimates.length, " ", "\xB7", " ", perPage, " per page"), /* @__PURE__ */ React.createElement("span", null, "$", filteredEstimates.reduce((s, e) => s + parseFloat(e.total || 0), 0).toLocaleString(void 0, { minimumFractionDigits: 0, maximumFractionDigits: 0 }), " total value")), totalPages > 1 && /* @__PURE__ */ React.createElement("div", { className: "ol-pagination" }, /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, "Page ", page, " of ", totalPages), /* @__PURE__ */ React.createElement("div", { className: "ol-page-btns" }, /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page <= 1, onClick: () => setPage((p) => p - 1) }, "\u2190"), Array.from({ length: Math.min(totalPages, 5) }, (_, i) => i + 1).map((p) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: p,
        className: "ol-page-btn" + (page === p ? " active" : ""),
        onClick: () => setPage(p)
      },
      p
    )), totalPages > 5 && /* @__PURE__ */ React.createElement("span", { className: "ol-meta", style: { padding: "4px 6px" } }, "\xB7\xB7\xB7"), /* @__PURE__ */ React.createElement("button", { className: "ol-page-btn", disabled: page >= totalPages, onClick: () => setPage((p) => p + 1) }, "\u2192")))));
  }
  function RepEstimateFormView({ navigate, editId, showCost }) {
    const [estimate, setEstimate] = useState(null);
    const [items, setItems] = useState([]);
    const [areas, setAreas] = useState([]);
    const [areaSubtotals, setAreaSubtotals] = useState({});
    const [milestones, setMilestones] = useState([]);
    const [savingSchedule, setSavingSchedule] = useState(false);
    const [editingSchedule, setEditingSchedule] = useState(false);
    const scheduleSnapshotRef = useRef([]);
    const [loading, setLoading] = useState(!!editId);
    const [saving, setSaving] = useState(false);
    const [editingAreaId, setEditingAreaId] = useState(null);
    const [areaDraft, setAreaDraft] = useState({ name: "", area_sqft: "" });
    const [addingArea, setAddingArea] = useState(false);
    const [newAreaName, setNewAreaName] = useState("");
    const [scopeOpen, setScopeOpen] = useState({});
    const [scopeDraft, setScopeDraft] = useState({});
    const scopeTimers = useRef({});
    const [confirmDeleteArea, setConfirmDeleteArea] = useState(null);
    const [areaBusy, setAreaBusy] = useState(false);
    const [events, setEvents] = useState([]);
    const [engagement, setEngagement] = useState(null);
    const [threadText, setThreadText] = useState("");
    const [threadPosting, setThreadPosting] = useState(false);
    const [copied, setCopied] = useState(false);
    const [actionError, setActionError] = useState(null);
    const [savedFlash, setSavedFlash] = useState(false);
    const [form, setForm] = useState({
      customer_name: "",
      customer_first: "",
      customer_middle: "",
      customer_last: "",
      customer_email: "",
      phone: "",
      project_name: "",
      project_address_line1: "",
      project_address_line2: "",
      project_city: "",
      project_state: "",
      project_zip: "",
      notes: "",
      internal_notes: "",
      sidemark: ""
    });
    useEffect(() => {
      setSavedFlash(false);
    }, [form]);
    useRofDraft(
      editId ? null : "estimate-form",
      "estimate",
      () => !estimate && !!(form.customer_name || form.customer_email || form.phone || form.project_name || form.project_address_line1 || form.notes),
      () => ({ form })
    );
    useRofDraftRestore(editId ? null : "estimate-form", (s) => {
      if (s.form) setForm((f) => ({ ...f, ...s.form }));
    });
    useEffect(() => {
      if (estimate) rofDraftStore.clear("estimate-form");
    }, [!!estimate]);
    const [depositType, setDepositType] = useState("percent");
    const [depositValue, setDepositValue] = useState("50");
    const [customerSearch, setCustomerSearch] = useState("");
    const [customerResults, setCustomerResults] = useState([]);
    const [customerSearching, setCustomerSearching] = useState(false);
    const [selectedCustomer, setSelectedCustomer] = useState(null);
    const [customerMatch, setCustomerMatch] = useState(null);
    const [customerMode, setCustomerMode] = useState("search");
    const customerSearchTimer = useRef(null);
    const [productSearch, setProductSearch] = useState("");
    const [searchResults, setSearchResults] = useState([]);
    const [showResults, setShowResults] = useState(false);
    const searchTimeout = useRef(null);
    const [catalogOpen, setCatalogOpen] = useState(false);
    const [targetAreaId, setTargetAreaId] = useState(null);
    const [rugModalItem, setRugModalItem] = useState(null);
    const [activeAddArea, setActiveAddArea] = useState(null);
    const [editingCell, setEditingCell] = useState(null);
    const [cellValue, setCellValue] = useState("");
    const startCellEdit = (id, field, val) => {
      setEditingCell({ id, field });
      setCellValue(String(val));
    };
    const cancelCellEdit = () => {
      setEditingCell(null);
      setCellValue("");
    };
    const [showLaborForm, setShowLaborForm] = useState(false);
    const [laborItem, setLaborItem] = useState({
      labor_category: "installation",
      custom_name: "",
      description: "",
      rate_type: "per_sqft",
      rate_sqft: "",
      labor_sqft: "",
      unit_price: "",
      quantity: "1",
      area_id: null
    });
    const [addPanelTab, setAddPanelTab] = useState("material");
    const openAddPanel = (areaId, tab) => {
      const key = "add-" + (areaId || "general");
      setTargetAreaId(areaId);
      setProductSearch("");
      setSearchResults([]);
      setShowResults(false);
      setLaborItem((l) => ({ ...l, area_id: areaId }));
      setAddPanelTab(tab || "material");
      setActiveAddArea(key);
    };
    const closeAddPanel = () => {
      setActiveAddArea(null);
      setShowResults(false);
    };
    const [showPreview, setShowPreview] = useState(false);
    const [previewHtml, setPreviewHtml] = useState("");
    const [previewMeta, setPreviewMeta] = useState(null);
    const [previewLoading, setPreviewLoading] = useState(false);
    const [sendFeedback, setSendFeedback] = useState(null);
    const [pdfViewer, setPdfViewer] = useState(null);
    const [showConvertQuoteModal, setShowConvertQuoteModal] = useState(false);
    const [showConvertOrderModal, setShowConvertOrderModal] = useState(false);
    const [convertMethod, setConvertMethod] = useState("cash");
    const [convertOnTerms, setConvertOnTerms] = useState(false);
    const [convertDeposit, setConvertDeposit] = useState("");
    const [converting, setConverting] = useState(false);
    const isReadonly = estimate && !["draft", "sent"].includes(estimate.status);
    const { addressInputRef: estAddrRef, previousAddresses: estPrevAddrs, showPrevAddresses: showEstPrevAddrs, setShowPrevAddresses: setShowEstPrevAddrs, selectPreviousAddress: selectEstPrevAddr } = useAddressAutofill({
      fieldMap: { line1: "project_address_line1", line2: "project_address_line2", city: "project_city", state: "project_state", zip: "project_zip" },
      setForm,
      selectedCustomer,
      isReadonly: !!isReadonly
    });
    useEffect(() => {
      if (!editId) return;
      repFetch("/api/rep/estimates/" + editId).then((data) => {
        setEstimate(data.estimate);
        setItems(data.items || []);
        setAreas(data.areas || []);
        setAreaSubtotals(data.area_subtotals || {});
        setMilestones(data.milestones || []);
        setForm({
          customer_name: data.estimate.customer_name || "",
          customer_email: data.estimate.customer_email || "",
          phone: data.estimate.phone || "",
          project_name: data.estimate.project_name || "",
          project_address_line1: data.estimate.project_address_line1 || "",
          project_address_line2: data.estimate.project_address_line2 || "",
          project_city: data.estimate.project_city || "",
          project_state: data.estimate.project_state || "",
          project_zip: data.estimate.project_zip || "",
          notes: data.estimate.notes || "",
          internal_notes: data.estimate.internal_notes || "",
          sidemark: data.estimate.sidemark || ""
        });
        const dt = data.estimate.deposit_type;
        const dv = parseFloat(data.estimate.deposit_value || 0);
        if ((!dt || dt === "none") && !dv) {
          setDepositType("percent");
          setDepositValue("50");
        } else {
          setDepositType(dt || "none");
          setDepositValue(String(dv || 0));
        }
        if (data.customer) {
          setSelectedCustomer(data.customer);
          setCustomerMode("search");
        } else {
          setCustomerMode("new");
        }
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [editId]);
    const reloadEstimate = useCallback(async () => {
      if (!editId) return;
      try {
        const data = await repFetch("/api/rep/estimates/" + editId);
        setEstimate(data.estimate);
        setItems(data.items || []);
        setAreas(data.areas || []);
        setAreaSubtotals(data.area_subtotals || {});
      } catch (e) {
      }
    }, [editId]);
    const loadEvents = useCallback(() => {
      if (!editId) {
        setEvents([]);
        setEngagement(null);
        return;
      }
      repFetch("/api/rep/estimates/" + editId + "/events").then((d) => {
        setEvents(d.events || []);
        setEngagement(d.engagement || null);
      }).catch(() => {
      });
    }, [editId]);
    useEffect(() => {
      loadEvents();
    }, [loadEvents]);
    const postThreadEntry = async (kind) => {
      const body = threadText.trim();
      if (!body || !editId) return;
      setThreadPosting(true);
      try {
        await repFetch("/api/rep/estimates/" + editId + "/events", {
          method: "POST",
          body: JSON.stringify({ event_type: kind, body })
        });
        setThreadText("");
        loadEvents();
      } catch (err) {
        alert(err.message || "Failed to log entry");
      }
      setThreadPosting(false);
    };
    const searchCustomers = (q) => {
      setCustomerSearch(q);
      if (customerSearchTimer.current) clearTimeout(customerSearchTimer.current);
      if (q.trim().length < 2) {
        setCustomerResults([]);
        setCustomerSearching(false);
        return;
      }
      setCustomerSearching(true);
      customerSearchTimer.current = setTimeout(async () => {
        try {
          const data = await repFetch("/api/rep/customers/search?q=" + encodeURIComponent(q.trim()));
          setCustomerResults(data.results || []);
        } catch (e) {
          setCustomerResults([]);
        }
        setCustomerSearching(false);
      }, 300);
    };
    const HD_LOCKED_DISCOUNT_CAP = 10;
    const effTradeDisc = (d, locked) => locked ? Math.min(d, HD_LOCKED_DISCOUNT_CAP) : d;
    const tradeDiscountFor = (c) => c && c.type === "trade" ? parseFloat(c.discount_percent || 0) || 0 : 0;
    const tradeUnit = (retail, locked, discount) => discount > 0 ? retail * (1 - effTradeDisc(discount, locked) / 100) : retail;
    const applyEstimateReprice = (discount) => {
      const next = items.map((it) => {
        if (!it.sku_id || !it.retail_price) return it;
        const retail = parseFloat(it.retail_price);
        const oldUnit = parseFloat(it.unit_price) || retail;
        const newUnit = tradeUnit(retail, it.retail_locked, discount);
        const oldSub = parseFloat(it.subtotal) || 0;
        const newSub = oldUnit > 0 ? oldSub * (newUnit / oldUnit) : newUnit;
        return { ...it, unit_price: newUnit.toFixed(2), subtotal: newSub.toFixed(2) };
      });
      setItems(next);
      if (estimate) {
        next.forEach((it, i) => {
          const before = items[i];
          if (it.id && before && (it.unit_price !== before.unit_price || it.subtotal !== before.subtotal)) {
            repFetch("/api/rep/estimates/" + estimate.id + "/items/" + it.id, {
              method: "PUT",
              body: JSON.stringify({ unit_price: it.unit_price, subtotal: it.subtotal })
            }).catch(() => {
            });
          }
        });
      }
    };
    const persistCustomer = (fields) => {
      if (!estimate) return;
      repFetch("/api/rep/estimates/" + estimate.id, { method: "PUT", body: JSON.stringify(fields) }).then((d) => {
        if (d.estimate) setEstimate(d.estimate);
      }).catch(() => {
      });
    };
    const selectCustomer = (c) => {
      setSelectedCustomer(c);
      applyEstimateReprice(tradeDiscountFor(c));
      const patch = {
        customer_name: c.name || "",
        customer_email: c.email || "",
        phone: c.phone || ""
      };
      setForm((f) => ({
        ...f,
        ...patch,
        project_address_line1: c.address_line1 || f.project_address_line1,
        project_address_line2: c.address_line2 || f.project_address_line2,
        project_city: c.city || f.project_city,
        project_state: c.state || f.project_state,
        project_zip: c.zip || f.project_zip
      }));
      setCustomerSearch("");
      setCustomerResults([]);
      setCustomerMatch(null);
      persistCustomer(patch);
    };
    const clearCustomer = () => {
      setSelectedCustomer(null);
      applyEstimateReprice(0);
      setForm((f) => ({ ...f, customer_name: "", customer_first: "", customer_middle: "", customer_last: "", customer_email: "", phone: "" }));
      setCustomerMatch(null);
      persistCustomer({ customer_name: "", customer_email: "", phone: "" });
    };
    const checkCustomerMatch = async (field) => {
      if (selectedCustomer) return;
      const val = field === "email" ? form.customer_email : form.phone;
      if (!val || val.trim().length < 3) {
        setCustomerMatch(null);
        return;
      }
      try {
        const data = await repFetch("/api/rep/customers/search?q=" + encodeURIComponent(val));
        if (data.results && data.results.length > 0) setCustomerMatch(data.results[0]);
        else setCustomerMatch(null);
      } catch (e) {
        setCustomerMatch(null);
      }
    };
    const creatingRef = useRef(null);
    const ensureEstimate = async () => {
      if (estimate) return estimate;
      if (creatingRef.current) return creatingRef.current;
      const p = (async () => {
        const data = await repFetch("/api/rep/estimates", { method: "POST", body: JSON.stringify(form) });
        setEstimate(data.estimate);
        setItems(data.items || []);
        return data.estimate;
      })();
      creatingRef.current = p;
      return p;
    };
    const validateCustomerFields = () => {
      if (form.customer_name && form.customer_name.trim().split(/\s+/).length < 2) {
        alert("Enter the customer\u2019s first and last name.");
        return false;
      }
      if (form.customer_email && !isValidEmail(form.customer_email)) {
        alert("Enter a valid customer email.");
        return false;
      }
      if (form.phone && !isValidPhone(form.phone)) {
        alert("Enter a valid 10-digit phone number.");
        return false;
      }
      return true;
    };
    const saveEstimate = async () => {
      if (!validateCustomerFields()) return;
      setSaving(true);
      try {
        if (editId && estimate) {
          const data = await repFetch("/api/rep/estimates/" + editId, {
            method: "PUT",
            body: JSON.stringify({
              ...form,
              deposit_type: depositType,
              deposit_value: depositType === "none" ? 0 : parseFloat(depositValue) || 0
            })
          });
          setEstimate(data.estimate);
          setSavedFlash(true);
        } else {
          const est = await ensureEstimate();
          navigate("estimate-edit", est.id);
        }
      } catch (err) {
        alert("Save failed: " + err.message);
      }
      setSaving(false);
    };
    const handleProductSearch = (term) => {
      setProductSearch(term);
      if (searchTimeout.current) clearTimeout(searchTimeout.current);
      if (term.length < 2) {
        setSearchResults([]);
        setShowResults(false);
        return;
      }
      setShowResults(true);
      searchTimeout.current = setTimeout(async () => {
        try {
          const data = await repFetch("/api/rep/skus/search?q=" + encodeURIComponent(term));
          setSearchResults((data.results || []).map((r) => ({
            sku_id: r.sku_id,
            product_name: r.product_name,
            collection: r.collection,
            variant_name: r.variant_name,
            variant_type: r.variant_type,
            accessory_label: r.accessory_label,
            color: r.color,
            vendor_name: r.vendor_name,
            vendor_sku: r.vendor_sku,
            internal_sku: r.internal_sku,
            price: r.retail_price,
            sqft_per_box: r.sqft_per_box,
            sell_by: r.sell_by || "box",
            vendor_id: r.vendor_id,
            price_basis: r.price_basis || "per_sqft",
            cut_price: r.cut_price,
            roll_price: r.roll_price,
            roll_width_ft: r.roll_width_ft,
            primary_image: r.primary_image || null,
            vendor_cost: r.vendor_cost || null
          })));
        } catch (e) {
          setSearchResults([]);
        }
        setShowResults(true);
      }, 300);
    };
    const addProductToEstimate = async (sku) => {
      setShowResults(false);
      setProductSearch("");
      const isCarpet = sku.price_basis === "per_sqyd" || sku.sell_by === "roll";
      const retail = isCarpet ? parseFloat(sku.cut_price || sku.price || 0) : pieceRetail(sku, sku.price);
      const price = tradeUnit(retail, sku.retail_locked, tradeDiscountFor(selectedCustomer));
      const sqftPerBox = parseFloat(sku.sqft_per_box || 0);
      const sellBy = isCarpet ? "roll" : sku.sell_by || "box";
      const numBoxes = 1;
      const sqftNeeded = isCarpet ? 9 : sqftPerBox || 0;
      const ovFactor = 1;
      const sub = isCarpet ? parseFloat((price * (sqftNeeded / 9) * ovFactor).toFixed(2)) : sellBy === "box" ? parseFloat((sqftNeeded * ovFactor * price).toFixed(2)) : parseFloat((numBoxes * price).toFixed(2));
      const newItem = {
        item_type: "material",
        sku_id: sku.sku_id,
        product_name: sku.product_name || "",
        collection: sku.collection || "",
        color: sku.color || "",
        variant_name: sku.variant_name || "",
        variant_type: sku.variant_type || "",
        accessory_label: sku.accessory_label || "",
        vendor_name: sku.vendor_name || null,
        vendor_sku: sku.vendor_sku || sku.internal_sku || null,
        sqft_needed: sqftNeeded,
        num_boxes: numBoxes,
        unit_price: price.toFixed(2),
        subtotal: sub.toFixed(2),
        sell_by: sellBy,
        area_id: targetAreaId,
        vendor_cost: (isPerPieceRow(sku) && sku.vendor_cost != null ? parseFloat(sku.vendor_cost) * sqftPerBox : sku.vendor_cost) || null,
        _sqft_per_box: sqftPerBox
      };
      const existed = !!estimate;
      let est;
      try {
        est = await ensureEstimate();
      } catch (err) {
        setActionError(err.message || "Failed to start estimate");
        return;
      }
      try {
        const result = await repFetch("/api/rep/estimates/" + est.id + "/items", {
          method: "POST",
          body: JSON.stringify(newItem)
        });
        if (result.item) {
          result.item._sqft_per_box = sqftPerBox;
          result.item.vendor_cost = (isPerPieceRow(sku) && sku.vendor_cost != null ? parseFloat(sku.vendor_cost) * sqftPerBox : sku.vendor_cost) || null;
          result.item.retail_price = retail;
          result.item.retail_locked = !!sku.retail_locked;
          if (result.item.primary_image == null) result.item.primary_image = sku.primary_image || null;
          if (result.item.vendor_name == null) result.item.vendor_name = sku.vendor_name || null;
          if (result.item.vendor_sku == null) result.item.vendor_sku = sku.vendor_sku || null;
          if (result.item.color == null) result.item.color = sku.color || null;
          if (result.item.collection == null) result.item.collection = sku.collection || null;
          if (result.item.variant_name == null) result.item.variant_name = sku.variant_name || null;
          if (result.item.variant_type == null) result.item.variant_type = sku.variant_type || null;
          if (result.item.accessory_label == null) result.item.accessory_label = sku.accessory_label || null;
          setItems((prev) => [...prev, result.item]);
          if (result.estimate) setEstimate(result.estimate);
        }
      } catch (err) {
        setActionError(err.message || "Failed to add item");
        return;
      }
      if (!existed) navigate("estimate-edit", est.id);
    };
    const addRugToEstimate = async (line) => {
      const carpetLine = rugModalItem;
      setRugModalItem(null);
      let est;
      try {
        est = await ensureEstimate();
      } catch (err) {
        setActionError(err.message || "Failed to start estimate");
        return;
      }
      try {
        const result = await repFetch("/api/rep/estimates/" + est.id + "/items", {
          method: "POST",
          body: JSON.stringify({ is_custom_rug: true, sku_id: line.sku_id, custom_width_ft: line.custom_width_ft, custom_length_ft: line.custom_length_ft, num_boxes: line.num_boxes, item_type: "material", area_id: carpetLine ? carpetLine.area_id : null })
        });
        if (result.item) {
          setItems((prev) => [...prev, result.item]);
          if (result.estimate) setEstimate(result.estimate);
        }
      } catch (err) {
        setActionError(err.message || "Failed to add custom rug");
        return;
      }
    };
    const addFromCatalog = async ({ lines, accessories, customs }) => {
      const existed = !!estimate;
      let est;
      try {
        est = await ensureEstimate();
      } catch (err) {
        setActionError(err.message || "Failed to start estimate");
        return;
      }
      const ovFactor = 1;
      const discount = tradeDiscountFor(selectedCustomer);
      const toAdd = [];
      for (const { item: p, qty } of lines) {
        const isCarpet = p.price_basis === "per_sqyd";
        const retail = isCarpet ? parseFloat(p.cut_price || p.price || 0) : pieceRetail(p, p.price);
        const price = tradeUnit(retail, p.retail_locked, discount);
        const sqftPerBox = parseFloat(p.sqft_per_box || 0);
        const sellBy = isCarpet ? "roll" : p.sell_by || "box";
        const productName = p.display_name || p.name || p.product_name || "";
        const sqftNeeded = isCarpet ? 9 * qty : sqftPerBox * qty;
        const sub = isCarpet ? price * (sqftNeeded / 9) * ovFactor : sellBy === "unit" || sellBy === "piece" ? price * qty : sqftNeeded > 0 ? sqftNeeded * ovFactor * price : price * qty;
        toAdd.push({
          item: {
            item_type: "material",
            sku_id: p.sku_id,
            product_id: p.id || p.product_id || null,
            product_name: productName,
            collection: p.collection || "",
            sqft_needed: sqftNeeded || null,
            num_boxes: isCarpet ? 1 : qty,
            unit_price: price.toFixed(2),
            subtotal: parseFloat(sub.toFixed(2)).toFixed(2),
            sell_by: sellBy,
            area_id: targetAreaId
          },
          extra: {
            _sqft_per_box: sqftPerBox,
            vendor_cost: (isPerPieceRow(p) && p.cost != null ? parseFloat(p.cost) * sqftPerBox : p.cost) || null,
            primary_image: p.primary_image || null,
            vendor_name: p.vendor_name || p.brand_name || null,
            vendor_sku: p.vendor_sku || p.internal_sku || null,
            color: p.color || null,
            variant_name: p.variant_name || null,
            variant_type: p.variant_type || null,
            accessory_label: p.accessory_label || null,
            retail_price: retail,
            retail_locked: !!p.retail_locked
          }
        });
      }
      for (const { acc, parent, qty } of accessories) {
        const accRetail = parseFloat(acc.retail_price || 0);
        const price = tradeUnit(accRetail, acc.retail_locked, discount);
        toAdd.push({
          item: {
            item_type: "material",
            sku_id: acc.sku_id,
            product_id: acc.product_id || null,
            product_name: parent ? parent.product_name || parent.display_name || parent.name || acc.product_name || "Accessory" : acc.product_name || "Accessory",
            collection: acc.collection || (parent ? parent.collection : "") || "",
            color: acc.color || "",
            variant_name: acc.variant_name || "",
            variant_type: "accessory",
            accessory_label: acc.accessory_label || "",
            sqft_needed: null,
            num_boxes: qty,
            unit_price: price.toFixed(2),
            subtotal: (price * qty).toFixed(2),
            sell_by: acc.sell_by || "unit",
            area_id: targetAreaId
          },
          extra: {
            vendor_cost: acc.cost || null,
            primary_image: acc.primary_image || null,
            vendor_name: parent ? parent.vendor_name || parent.brand_name || null : acc.vendor_name || null,
            vendor_sku: acc.vendor_sku || acc.internal_sku || null,
            color: acc.color || null,
            variant_name: acc.variant_name || null,
            variant_type: "accessory",
            accessory_label: acc.accessory_label || null,
            retail_price: accRetail,
            retail_locked: !!acc.retail_locked
          }
        });
      }
      for (const c of customs) {
        toAdd.push({
          item: {
            item_type: "material",
            sku_id: null,
            product_id: null,
            product_name: c.product_name,
            collection: "",
            description: c.description || "",
            sqft_needed: null,
            num_boxes: c.num_boxes,
            unit_price: parseFloat(c.unit_price || 0).toFixed(2),
            subtotal: parseFloat(c.subtotal || 0).toFixed(2),
            sell_by: c.sell_by || "box",
            area_id: targetAreaId
          },
          extra: {}
        });
      }
      try {
        for (const { item, extra } of toAdd) {
          const result = await repFetch("/api/rep/estimates/" + est.id + "/items", {
            method: "POST",
            body: JSON.stringify(item)
          });
          if (result.item) {
            setItems((prev) => [...prev, { ...result.item, ...extra }]);
            if (result.estimate) setEstimate(result.estimate);
          }
        }
      } catch (err) {
        alert("Failed to add items: " + (err.message || "Unknown error"));
      }
      if (!existed) navigate("estimate-edit", est.id);
    };
    const [localEdits, setLocalEdits] = useState({});
    const onMaterialChange = (item, field, value) => {
      setLocalEdits((prev) => ({ ...prev, [item.id]: { ...prev[item.id] || {}, [field]: value } }));
    };
    const getMaterialValue = (item, field) => {
      const local = localEdits[item.id];
      if (local && local[field] !== void 0) return local[field];
      return item[field];
    };
    const commitMaterialItem = async (item) => {
      const local = localEdits[item.id];
      if (!local || !estimate) return;
      const isUnit = item.sell_by === "unit" || item.sell_by === "piece";
      const isCarpet = item.sell_by === "roll";
      const sqftPerBox = item._sqft_per_box || parseFloat(item.sqft_needed || 0) / Math.max(item.num_boxes || 1, 1);
      const merged = { ...item, ...local };
      const price = parseFloat(merged.unit_price || 0);
      const ovFactor = 1;
      const sqft = parseFloat(merged.sqft_needed || 0);
      const boxes = parseInt(merged.num_boxes || 1);
      if (isUnit) {
        merged.subtotal = (boxes * price).toFixed(2);
        merged.sqft_needed = null;
      } else if (isCarpet) {
        merged.subtotal = (price * (sqft / 9) * ovFactor).toFixed(2);
        merged.num_boxes = 1;
      } else if (local.sqft_needed !== void 0) {
        if (sqftPerBox > 0) merged.num_boxes = Math.ceil(sqft * ovFactor / sqftPerBox);
        merged.subtotal = (sqft * ovFactor * price).toFixed(2);
      } else if (local.num_boxes !== void 0) {
        if (sqftPerBox > 0) merged.sqft_needed = (boxes * sqftPerBox).toFixed(0);
        const newSqft = parseFloat(merged.sqft_needed || 0);
        merged.subtotal = newSqft > 0 ? (newSqft * ovFactor * price).toFixed(2) : (boxes * price).toFixed(2);
      } else {
        merged.subtotal = sqft > 0 ? (sqft * ovFactor * price).toFixed(2) : (boxes * price).toFixed(2);
      }
      let result;
      try {
        result = await repFetch("/api/rep/estimates/" + estimate.id + "/items/" + item.id, {
          method: "PUT",
          body: JSON.stringify({
            sqft_needed: merged.sqft_needed,
            num_boxes: merged.num_boxes,
            unit_price: merged.unit_price,
            subtotal: merged.subtotal,
            ...local.vendor_cost !== void 0 ? { vendor_cost: merged.vendor_cost } : {}
          })
        });
      } catch (err) {
        setActionError(err.message || "Failed to update item");
        setLocalEdits((prev) => {
          const n = { ...prev };
          delete n[item.id];
          return n;
        });
        return;
      }
      if (result.item) {
        const kept = {
          _sqft_per_box: sqftPerBox,
          vendor_cost: merged.vendor_cost,
          retail_price: item.retail_price,
          retail_locked: item.retail_locked,
          primary_image: item.primary_image
        };
        setItems((prev) => prev.map((i) => i.id === item.id ? { ...kept, ...result.item } : i));
        if (result.estimate) setEstimate(result.estimate);
      }
      setLocalEdits((prev) => {
        const n = { ...prev };
        delete n[item.id];
        return n;
      });
    };
    const deleteItem = async (itemId) => {
      if (!estimate) return;
      try {
        const result = await repFetch("/api/rep/estimates/" + estimate.id + "/items/" + itemId, { method: "DELETE" });
        setItems((prev) => prev.filter((i) => i.id !== itemId));
        if (result.estimate) setEstimate(result.estimate);
      } catch (err) {
        setActionError(err.message || "Failed to remove item");
      }
    };
    const addLaborItem = async () => {
      const li = laborItem;
      let unitPrice, qty, sub;
      if (li.rate_type === "per_sqft") {
        unitPrice = parseFloat(li.rate_sqft || 0);
        qty = parseFloat(li.labor_sqft || 0);
        sub = parseFloat((unitPrice * qty).toFixed(2));
      } else {
        unitPrice = parseFloat(li.unit_price || 0);
        qty = parseFloat(li.quantity || 1);
        sub = parseFloat((unitPrice * qty).toFixed(2));
      }
      if (sub <= 0) return alert("Labor subtotal must be greater than zero.");
      const newItem = {
        item_type: "labor",
        labor_category: li.labor_category,
        product_name: li.labor_category === "other" ? (li.custom_name || "").trim() || null : null,
        description: li.description,
        rate_type: li.rate_type,
        rate_sqft: li.rate_type === "per_sqft" ? unitPrice : null,
        labor_sqft: li.rate_type === "per_sqft" ? qty : null,
        unit_price: unitPrice.toFixed(2),
        quantity: qty,
        subtotal: sub.toFixed(2),
        area_id: li.area_id || null
      };
      const existed = !!estimate;
      let est;
      try {
        est = await ensureEstimate();
      } catch (err) {
        setActionError(err.message || "Failed to start estimate");
        return;
      }
      try {
        const result = await repFetch("/api/rep/estimates/" + est.id + "/items", {
          method: "POST",
          body: JSON.stringify(newItem)
        });
        if (result.item) {
          setItems((prev) => [...prev, result.item]);
          if (result.estimate) setEstimate(result.estimate);
        }
      } catch (err) {
        setActionError(err.message || "Failed to add labor");
        return;
      }
      setLaborItem({ labor_category: "installation", custom_name: "", description: "", rate_type: "per_sqft", rate_sqft: "", labor_sqft: "", unit_price: "", quantity: "1", area_id: li.area_id || null });
      if (!existed) navigate("estimate-edit", est.id);
    };
    const addArea = async (name) => {
      if (!name || !name.trim()) return;
      const existed = !!estimate;
      setAreaBusy(true);
      let est;
      try {
        est = await ensureEstimate();
      } catch (err) {
        setActionError(err.message || "Failed to start estimate");
        setAreaBusy(false);
        return;
      }
      try {
        const data = await repFetch("/api/rep/estimates/" + est.id + "/areas", {
          method: "POST",
          body: JSON.stringify({ name: name.trim() })
        });
        if (data.area) setAreas((prev) => [...prev, data.area]);
        setNewAreaName("");
        setAddingArea(false);
      } catch (err) {
        setActionError(err.message || "Failed to add section");
      }
      setAreaBusy(false);
      if (!existed) navigate("estimate-edit", est.id);
    };
    const saveAreaField = async (areaId, patch) => {
      if (!estimate) return;
      try {
        const data = await repFetch("/api/rep/estimates/" + estimate.id + "/areas/" + areaId, {
          method: "PUT",
          body: JSON.stringify(patch)
        });
        if (data.area) setAreas((prev) => prev.map((a) => a.id === areaId ? data.area : a));
      } catch (err) {
        setActionError(err.message || "Failed to update area");
      }
    };
    const commitAreaHeader = async (areaId) => {
      const name = (areaDraft.name || "").trim();
      const patch = {};
      if (name) patch.name = name;
      patch.area_sqft = areaDraft.area_sqft === "" ? null : parseFloat(areaDraft.area_sqft);
      setEditingAreaId(null);
      await saveAreaField(areaId, patch);
    };
    const onScopeChange = (areaId, val) => {
      setScopeDraft((prev) => ({ ...prev, [areaId]: val }));
      if (scopeTimers.current[areaId]) clearTimeout(scopeTimers.current[areaId]);
      scopeTimers.current[areaId] = setTimeout(() => {
        saveAreaField(areaId, { scope_notes: val });
      }, 800);
    };
    const commitScope = (areaId) => {
      if (scopeTimers.current[areaId]) clearTimeout(scopeTimers.current[areaId]);
      const val = scopeDraft[areaId];
      if (val !== void 0) saveAreaField(areaId, { scope_notes: val });
    };
    const moveArea = async (areaId, dir) => {
      const idx = areas.findIndex((a) => a.id === areaId);
      if (idx < 0) return;
      const swap = idx + dir;
      if (swap < 0 || swap >= areas.length) return;
      const next = areas.slice();
      const [row] = next.splice(idx, 1);
      next.splice(swap, 0, row);
      setAreas(next);
      try {
        const data = await repFetch("/api/rep/estimates/" + estimate.id + "/areas/reorder", {
          method: "POST",
          body: JSON.stringify({ area_ids: next.map((a) => a.id) })
        });
        if (data.areas) setAreas(data.areas);
      } catch (err) {
        setActionError(err.message || "Failed to reorder");
        reloadEstimate();
      }
    };
    const removeArea = async (areaId, mode) => {
      if (!estimate) return;
      setAreaBusy(true);
      try {
        await repFetch("/api/rep/estimates/" + estimate.id + "/areas/" + areaId + "?mode=" + mode, {
          method: "DELETE"
        });
        setConfirmDeleteArea(null);
        await reloadEstimate();
      } catch (err) {
        setActionError(err.message || "Failed to delete area");
      }
      setAreaBusy(false);
    };
    const moveItemToArea = async (item, areaId) => {
      if (!estimate) return;
      try {
        const result = await repFetch("/api/rep/estimates/" + estimate.id + "/items/" + item.id, {
          method: "PUT",
          body: JSON.stringify({ area_id: areaId })
        });
        const merged = result.item ? { ...item, ...result.item } : { ...item, area_id: areaId };
        setItems((prev) => prev.map((i) => i.id === item.id ? merged : i));
        if (result.estimate) setEstimate(result.estimate);
      } catch (err) {
        setActionError(err.message || "Failed to move item");
      }
    };
    const sendEstimate = async () => {
      if (!estimate) return;
      setSendFeedback(null);
      const result = await repFetch("/api/rep/estimates/" + estimate.id + "/send", { method: "POST" });
      setEstimate((prev) => ({ ...prev, status: "sent" }));
      setSendFeedback(result.emailed ? "Estimate emailed to " + estimate.customer_email : "Estimate marked as sent");
      setShowPreview(false);
    };
    const schedulePctSum = milestones.reduce((s, m) => s + (parseFloat(m.percent) || 0), 0);
    const startEditSchedule = () => {
      scheduleSnapshotRef.current = milestones;
      setEditingSchedule(true);
    };
    const cancelEditSchedule = () => {
      setMilestones(scheduleSnapshotRef.current || []);
      setEditingSchedule(false);
    };
    const applyScheduleTemplate = () => {
      scheduleSnapshotRef.current = milestones;
      setMilestones([
        { label: "Deposit", percent: 50, due_label: "at booking" },
        { label: "Progress payment", percent: 30, due_label: "at install start" },
        { label: "Final payment", percent: 20, due_label: "on completion" }
      ]);
      setEditingSchedule(true);
    };
    const addMilestone = () => setMilestones((ms) => [...ms, { label: "", percent: "", due_label: "" }]);
    const startNewSchedule = () => {
      scheduleSnapshotRef.current = milestones;
      addMilestone();
      setEditingSchedule(true);
    };
    const updateMilestone = (i, patch) => setMilestones((ms) => ms.map((m, idx) => idx === i ? { ...m, ...patch } : m));
    const removeMilestone = (i) => setMilestones((ms) => ms.filter((_, idx) => idx !== i));
    const saveSchedule = async () => {
      for (const m of milestones) {
        if (!String(m.label || "").trim()) {
          alert("Each milestone needs a label.");
          return;
        }
        const p = parseFloat(m.percent);
        if (isNaN(p) || p <= 0 || p > 100) {
          alert("Each milestone needs a percent between 0 and 100.");
          return;
        }
      }
      setSavingSchedule(true);
      try {
        const est = await ensureEstimate();
        const data = await repFetch("/api/rep/estimates/" + est.id + "/payment-schedule", {
          method: "PUT",
          body: JSON.stringify({ milestones: milestones.map((m) => ({ label: m.label, percent: parseFloat(m.percent), due_label: m.due_label || null })) })
        });
        setMilestones(data.milestones || []);
        setEditingSchedule(false);
      } catch (err) {
        alert(err.message || "Failed to save schedule");
      }
      setSavingSchedule(false);
    };
    const loadPreview = async () => {
      if (!estimate) return;
      if (!form.customer_email || !isValidEmail(form.customer_email)) {
        setActionError("Add a customer with a valid email before sending this estimate.");
        return;
      }
      setPreviewLoading(true);
      try {
        const data = await repFetch("/api/rep/estimates/" + estimate.id + "/preview");
        setPreviewHtml(data.html);
        setPreviewMeta(data);
        setShowPreview(true);
      } catch (e) {
      }
      setPreviewLoading(false);
    };
    const convertToQuote = async (force) => {
      if (!estimate) return;
      setConverting(true);
      try {
        const data = await repFetch("/api/rep/estimates/" + estimate.id + "/convert-to-quote", {
          method: "POST",
          body: JSON.stringify(force ? { force: true } : {})
        });
        if (data.quote) navigate("quote-edit", data.quote.id);
      } catch (err) {
        if (err.needs_force || err.data && err.data.needs_force) {
          if (confirm((err.message || "This estimate is declined or expired.") + "\n\nConvert it anyway?")) {
            setConverting(false);
            return convertToQuote(true);
          }
        } else {
          alert("Conversion failed: " + err.message);
        }
      }
      setConverting(false);
    };
    const openConvertModal = () => {
      if (!estimate) return;
      setConvertOnTerms(false);
      setConvertDeposit(parseFloat(estimate.total || 0).toFixed(2));
      setShowConvertOrderModal(true);
    };
    const convertToOrder = async (force) => {
      if (!estimate) return;
      setConverting(true);
      try {
        const body = { payment_method: convertOnTerms ? "offline" : convertMethod };
        if (convertOnTerms) body.on_terms = true;
        else body.deposit_amount = parseFloat(convertDeposit);
        if (force) body.force = true;
        const data = await repFetch("/api/rep/estimates/" + estimate.id + "/convert-to-order", {
          method: "POST",
          body: JSON.stringify(body)
        });
        if (data.order) navigate("order-detail", data.order.id);
      } catch (err) {
        if (err.needs_force || err.data && err.data.needs_force) {
          if (confirm((err.message || "This estimate is declined or expired.") + "\n\nConvert it anyway?")) {
            setConverting(false);
            return convertToOrder(true);
          }
        } else {
          alert("Conversion failed: " + err.message);
        }
      }
      setConverting(false);
    };
    const deleteEstimate = async () => {
      if (!estimate || estimate.status !== "draft") return;
      if (!confirm("Delete this draft estimate?")) return;
      await repFetch("/api/rep/estimates/" + estimate.id, { method: "DELETE" });
      navigate("estimates");
    };
    const materialItems = items.filter((i) => i.item_type === "material");
    const laborItems = items.filter((i) => i.item_type === "labor");
    const itemsForArea = (areaId) => items.filter((i) => (i.area_id || null) === areaId);
    const generalItems = itemsForArea(null);
    const subtotalFor = (key) => {
      const be = areaSubtotals && areaSubtotals[key];
      if (be) return { materials: parseFloat(be.materials || 0), labor: parseFloat(be.labor || 0), total: parseFloat(be.total || 0) };
      const areaId = key === "general" ? null : key;
      const list = itemsForArea(areaId);
      const materials = list.filter((i) => i.item_type === "material").reduce((s, i) => s + parseFloat(i.subtotal || 0), 0);
      const labor = list.filter((i) => i.item_type === "labor").reduce((s, i) => s + parseFloat(i.subtotal || 0), 0);
      return { materials, labor, total: materials + labor };
    };
    const showGeneral = generalItems.length > 0 || areas.length === 0;
    const laborCategoryLabels = {
      installation: "Installation",
      tearout: "Tearout",
      underlayment: "Underlayment",
      transitions: "Transitions",
      baseboards: "Baseboards",
      floor_leveling: "Floor Leveling",
      moisture_barrier: "Moisture Barrier",
      furniture_moving: "Furniture Moving",
      other: "Other"
    };
    const linearLaborCats = { baseboards: true };
    const laborUnitShort = (cat) => linearLaborCats[cat] ? "lin ft" : "sqft";
    const laborDisplayName = (item) => item.labor_category === "other" && item.product_name ? item.product_name : laborCategoryLabels[item.labor_category] || item.labor_category;
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading estimate...");
    const fmtD = (d) => new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" });
    const fmtDT = (d) => {
      if (!d) return null;
      const dt = new Date(d);
      return dt.toLocaleDateString("en-US", { month: "short", day: "numeric" }) + " \xB7 " + dt.toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }).toLowerCase().replace(" ", "");
    };
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const effStatus = estimate ? estimate.effective_status || estimate.status : "draft";
    const isTerminalNeg = effStatus === "declined" || effStatus === "expired";
    const EST_STATUS_META = {
      draft: { label: "Draft", tone: "#a87935" },
      sent: { label: "Sent \xB7 awaiting customer", tone: "#5c6e94" },
      accepted: { label: "Accepted", tone: "#3a7a4e" },
      declined: { label: "Declined", tone: "#c54731" },
      expired: { label: "Expired", tone: "var(--brass-muted)" },
      converted: { label: "Converted", tone: "#3a7a4e" }
    };
    const st = estimate ? EST_STATUS_META[effStatus] || { label: effStatus, tone: "var(--brass-muted)" } : { label: "New draft", tone: "#a87935" };
    const publicLink = estimate && estimate.public_token ? window.location.origin + "/estimate/" + estimate.public_token : null;
    const copyPublicLink = () => {
      if (!publicLink) return;
      const done = () => {
        setCopied(true);
        setTimeout(() => setCopied(false), 1600);
      };
      if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(publicLink).then(done, done);
      } else {
        const ta = document.createElement("textarea");
        ta.value = publicLink;
        document.body.appendChild(ta);
        ta.select();
        try {
          document.execCommand("copy");
        } catch (e) {
        }
        document.body.removeChild(ta);
        done();
      }
    };
    const engOpens = engagement ? engagement.opens : 0;
    const engViewers = engagement ? engagement.viewers : 0;
    const engReplies = engagement ? engagement.replies : 0;
    const lastViewedAt = engagement && engagement.last_viewed_at ? engagement.last_viewed_at : null;
    const lastReplyAt = engagement && engagement.last_reply_at ? engagement.last_reply_at : null;
    const initials = (form.customer_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?";
    const estExpiresAt = estimate && estimate.expires_at ? new Date(estimate.expires_at) : null;
    const estIsExpired = effStatus === "expired";
    const estWinStart = estimate ? new Date(estimate.sent_at || estimate.created_at) : null;
    const estDaysLeft = estExpiresAt ? Math.ceil((estExpiresAt.getTime() - Date.now()) / 864e5) : null;
    const estExpiryPct = estExpiresAt && estWinStart && estExpiresAt.getTime() > estWinStart.getTime() ? Math.min(100, Math.max(0, (Date.now() - estWinStart.getTime()) / (estExpiresAt.getTime() - estWinStart.getTime()) * 100)) : 0;
    const estCustomerDone = !!form.customer_name && form.customer_name.trim().split(/\s+/).length >= 2 && isValidEmail(form.customer_email);
    const estLinesDone = items.length > 0;
    const estChecklist = [
      { label: "Customer", done: estCustomerDone, meta: estCustomerDone ? form.customer_name : "name \xB7 email" },
      { label: "Lines", done: estLinesDone, meta: estLinesDone ? materialItems.length + " mat \xB7 " + laborItems.length + " labor" : "add materials or labor" },
      { label: "Ready to send", done: estCustomerDone && estLinesDone, meta: estCustomerDone && estLinesDone ? "preview & send" : "complete the above" }
    ];
    const estFirstTodo = estChecklist.findIndex((c) => !c.done);
    let estStepRank = 0;
    if (estimate) {
      if (effStatus === "converted") estStepRank = 5;
      else if (effStatus === "accepted" || isTerminalNeg) estStepRank = 3;
      else if (effStatus === "sent") estStepRank = engOpens > 0 ? 2 : 1;
    }
    const estSteps = [
      { label: "Draft", meta: estimate ? fmtD(estimate.created_at) : "not saved yet" },
      { label: "Sent", meta: estimate && ["sent", "accepted", "declined", "converted"].includes(estimate.status) ? estimate.sent_at ? fmtDT(estimate.sent_at) : "emailed to customer" : "preview & send" },
      { label: "Viewed", meta: engOpens > 0 ? engOpens + (engOpens === 1 ? " open \xB7 " : " opens \xB7 ") + engViewers + (engViewers === 1 ? " viewer" : " viewers") : estimate && estimate.first_viewed_at ? "viewed " + fmtD(estimate.first_viewed_at) : "no opens yet" },
      { label: "Accepted", meta: isTerminalNeg ? effStatus === "declined" ? "declined" : "expired" : estimate && estimate.accepted_at ? "by " + (estimate.accepted_by_name || "customer") : ["accepted", "converted"].includes(effStatus) ? "customer committed" : estExpiresAt ? "expires " + fmtD(estExpiresAt) : "customer approval" },
      { label: "Converted", meta: estimate && estimate.converted_order_number ? estimate.converted_order_number : estimate && estimate.converted_quote_id ? "quote created" : estimate && estimate.converted_order_id ? "order created" : "becomes an order" }
    ];
    const estDeadStepIndex = 3;
    const estStepState = (i) => {
      if (isTerminalNeg) {
        if (i === estDeadStepIndex) return "dead";
        if (i > estDeadStepIndex) return "next";
        if (i === 2) return engOpens > 0 ? "done" : "next";
        return "done";
      }
      return i < estStepRank ? "done" : i === estStepRank ? "now" : "next";
    };
    const estCosted = materialItems.filter((i) => parseFloat(i.vendor_cost) > 0 && parseFloat(i.unit_price) > 0);
    const estCostedSell = estCosted.reduce((s, i) => s + parseFloat(i.subtotal || 0), 0);
    const estGrossProfit = estCosted.reduce((s, i) => s + (1 - parseFloat(i.vendor_cost) / parseFloat(i.unit_price)) * parseFloat(i.subtotal || 0), 0);
    const estBlendedMargin = estCostedSell > 0 ? estGrossProfit / estCostedSell * 100 : null;
    const canConvert = estimate && !isReadonly && (materialItems.length > 0 || laborItems.length > 0);
    const createDisabled = saving;
    const moveSelect = (item) => {
      if (isReadonly || areas.length === 0) return null;
      return /* @__PURE__ */ React.createElement(
        "select",
        {
          value: item.area_id || "",
          title: "Move to section",
          onChange: (e) => moveItemToArea(item, e.target.value || null),
          style: { font: "400 0.625rem/1 Inter, sans-serif", color: "#8a7e68", border: "0.5px solid rgba(28,25,23,0.16)", background: "#ece5d8", padding: "2px 4px", maxWidth: 96 }
        },
        /* @__PURE__ */ React.createElement("option", { value: "" }, "General"),
        areas.map((a) => /* @__PURE__ */ React.createElement("option", { key: a.id, value: a.id }, a.name))
      );
    };
    const renderMaterialRow = (item) => {
      const isUnit = item.sell_by === "unit" || item.sell_by === "piece";
      const isCarpet = item.sell_by === "roll";
      const liveCost = parseFloat(getMaterialValue(item, "vendor_cost") || 0);
      const livePrice = parseFloat(getMaterialValue(item, "unit_price") || 0);
      const mgn = liveCost > 0 && livePrice > 0 ? (1 - liveCost / livePrice) * 100 : null;
      const editCell = (field, rawVal, display, numericOnly) => {
        const isEditing = editingCell && editingCell.id === item.id && editingCell.field === field;
        const targetField = field === "sqft" ? "sqft_needed" : field === "qty" ? "num_boxes" : field === "cost" ? "vendor_cost" : "unit_price";
        if (isReadonly) return /* @__PURE__ */ React.createElement("span", { style: { font: "400 0.8125rem/1 Inter, sans-serif", color: "#1c1917" } }, display);
        return isEditing ? /* @__PURE__ */ React.createElement(
          "input",
          {
            className: "rof-edit-input",
            type: "text",
            inputMode: numericOnly ? "numeric" : "decimal",
            autoFocus: true,
            value: cellValue,
            onChange: (e) => {
              const v = e.target.value;
              const ok = numericOnly ? v === "" || /^\d+$/.test(v) : v === "" || /^\d*\.?\d{0,2}$/.test(v);
              if (ok) {
                setCellValue(v);
                onMaterialChange(item, targetField, v || "0");
              }
            },
            onKeyDown: (e) => {
              if (e.key === "Enter") e.target.blur();
              if (e.key === "Escape") cancelCellEdit();
            },
            onBlur: () => {
              commitMaterialItem(item);
              cancelCellEdit();
            }
          }
        ) : /* @__PURE__ */ React.createElement("button", { className: "rof-edit-cell", onClick: () => startCellEdit(item.id, field, rawVal) }, display);
      };
      const isCustom = !item.sku_id && !item.product_id;
      const nb = getMaterialValue(item, "num_boxes") || 1;
      const sqftVal = parseFloat(getMaterialValue(item, "sqft_needed") || 0);
      const unitLabel = isUnit ? item.sell_by === "piece" ? "pc" + (nb > 1 ? "s" : "") : "unit" + (nb > 1 ? "s" : "") : "box" + (nb > 1 ? "es" : "");
      const fli = formatLineItem(item);
      const brandBit = isCustom ? "" : fli.brand;
      const metaBits = [
        isCustom ? "Custom" : fli.sku || "Roma",
        !isCustom && item.category_name ? item.category_name : null
      ].filter(Boolean).join("  \xB7  ");
      return /* @__PURE__ */ React.createElement("div", { key: item.id, className: "rof-item-row" + (showCost ? "" : " rof-nocost") }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-swatch" }, item.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(item.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover" }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: item.collection || item.current_collection }), 50) } })), /* @__PURE__ */ React.createElement("div", { className: "rof-item-info" }, brandBit && /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta" }, brandBit), /* @__PURE__ */ React.createElement("div", { className: "rof-item-name" }, fli.name), /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta", style: { display: "flex", alignItems: "center", gap: "0.5rem", flexWrap: "wrap", marginTop: 2 } }, metaBits && /* @__PURE__ */ React.createElement("span", null, metaBits), moveSelect(item))), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, isCarpet ? editCell("sqft", sqftVal, /* @__PURE__ */ React.createElement(React.Fragment, null, sqftVal.toFixed(0), " sf")) : editCell("qty", nb, /* @__PURE__ */ React.createElement(React.Fragment, null, nb, " ", /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.5625rem", color: "#8a7e68" } }, unitLabel)), true), !isUnit && !isCarpet && sqftVal > 0 && /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.3 ui-monospace, monospace", letterSpacing: "0.12em", color: "#8a7e68", marginTop: 2 } }, sqftVal.toFixed(0), " sf")), showCost && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexWrap: "wrap", alignItems: "baseline", justifyContent: "flex-end", gap: "0.35rem" } }, editCell("cost", liveCost.toFixed(2), /* @__PURE__ */ React.createElement("span", { style: { color: "#8a7e68" } }, liveCost > 0 ? "$" + liveCost.toFixed(2) : "\u2014")), mgn !== null && /* @__PURE__ */ React.createElement("span", { style: { font: "500 0.5625rem/1.3 ui-monospace, monospace", letterSpacing: "0.04em", color: "#a87935" } }, mgn.toFixed(1) + "%")), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, editCell(
        "price",
        parseFloat(getMaterialValue(item, "unit_price") || 0).toFixed(2),
        /* @__PURE__ */ React.createElement(React.Fragment, null, "$", parseFloat(getMaterialValue(item, "unit_price") || 0).toFixed(2))
      )), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.375rem", justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("span", { className: "rof-item-price" }, "$", parseFloat(item.subtotal || 0).toFixed(2)), !isReadonly && /* @__PURE__ */ React.createElement("button", { className: "rof-remove-btn", onClick: () => deleteItem(item.id), title: "Remove" }, "\u2715")));
    };
    const laborCols = "36px 1.5fr 2fr 1.1fr 112px";
    const renderLaborRow = (item) => /* @__PURE__ */ React.createElement("div", { key: item.id, className: "rof-item-row", style: { gridTemplateColumns: laborCols } }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-swatch" }), /* @__PURE__ */ React.createElement("div", { className: "rof-item-info" }, /* @__PURE__ */ React.createElement("div", { className: "rof-item-name" }, laborDisplayName(item), /* @__PURE__ */ React.createElement("span", { style: {
      marginLeft: 6,
      font: '500 8px/1 "Inter", sans-serif',
      letterSpacing: "0.08em",
      textTransform: "uppercase",
      padding: "2px 6px",
      borderRadius: 3,
      background: "rgba(28,25,23,0.08)",
      color: "#8a7e68",
      verticalAlign: "middle"
    } }, "Labor")), /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta", style: { marginTop: 4 } }, moveSelect(item))), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.75rem/1.5 Inter, sans-serif", color: "rgba(28,25,23,0.7)" } }, item.description ? item.description.split("\n").map((line, i) => /* @__PURE__ */ React.createElement("div", { key: i }, i > 0 ? "\u2014 " : "", line)) : "\u2014"), /* @__PURE__ */ React.createElement("div", { className: "rof-item-meta", style: { marginTop: 0, textAlign: "right", whiteSpace: "nowrap" } }, item.rate_type === "per_sqft" ? "$" + parseFloat(item.rate_sqft || 0).toFixed(2) + "/" + laborUnitShort(item.labor_category) + " \xD7 " + parseFloat(item.labor_sqft || 0).toFixed(0) : parseFloat(item.quantity || 1) > 1 ? "$" + parseFloat(item.unit_price || 0).toFixed(2) + " \xD7 " + parseFloat(item.quantity).toFixed(0) : "Flat rate"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.375rem", justifyContent: "flex-end" } }, /* @__PURE__ */ React.createElement("span", { className: "rof-item-price" }, "$", parseFloat(item.subtotal || 0).toFixed(2)), !isReadonly && /* @__PURE__ */ React.createElement("button", { className: "rof-remove-btn", onClick: () => deleteItem(item.id), title: "Remove" }, "\u2715")));
    const renderMaterialAdd = () => {
      if (isReadonly) return null;
      return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { className: "rof-cat-launch", onClick: () => {
        setTargetAreaId(null);
        setCatalogOpen(true);
      } }, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-icon" }, "\u229E"), /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-title" }, "Browse the full catalog"), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-sub" }, "Filter by vendor, set quantities & add accessories in one pass")), /* @__PURE__ */ React.createElement("span", { className: "rof-cat-launch-cta" }, "Open catalog ", "\u2192")), /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap", style: { marginBottom: "0.75rem" } }, /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: productSearch,
          placeholder: "Quick add \u2014 search SKU or product name...",
          onFocus: () => {
            if (searchResults.length > 0) setShowResults(true);
          },
          onBlur: () => setTimeout(() => setShowResults(false), 200),
          onChange: (e) => {
            setTargetAreaId(null);
            handleProductSearch(e.target.value);
          },
          style: { width: "100%", padding: "0.5625rem 0.75rem", border: "0.5px solid rgba(28,25,23,0.18)", background: "#ece5d8", font: "400 0.8125rem/1.4 Inter, sans-serif", boxSizing: "border-box" }
        }
      ), showResults && searchResults.length === 0 && productSearch.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results", style: { padding: "0.75rem", color: "#8a7e68", fontSize: "0.8125rem" } }, "Searching..."), showResults && searchResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, searchResults.slice(0, 20).map((r, i) => {
        const rSuffix = skuPriceSuffix(r);
        const rRetail = pieceRetail(r, r.price);
        const rTrade = rofTradeUnit(rRetail, r.retail_locked, tradeDiscountFor(selectedCustomer));
        const rDisc = rRetail > 0 && rTrade < rRetail;
        return /* @__PURE__ */ React.createElement("div", { key: r.sku_id || i, className: "rof-search-item", onMouseDown: () => {
          setTargetAreaId(null);
          addProductToEstimate(r);
        } }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { src: optimizeImg(r.primary_image, 96), alt: "", style: { width: 36, height: 36, objectFit: "cover", flexShrink: 0 }, loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { style: { width: 36, height: 36, ...materialFace(rofCatKind({ collection: r.collection }), 50), flexShrink: 0 } }), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, formatLineItem(r).name), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, r.vendor_name, r.vendor_public_code ? " \xB7 " + r.vendor_public_code : "", r.vendor_sku ? " \xB7 " + r.vendor_sku : r.internal_sku ? " \xB7 " + r.internal_sku : "", rRetail > 0 && " \xB7 ", rRetail > 0 && rDisc && /* @__PURE__ */ React.createElement("span", { style: { textDecoration: "line-through", color: "#8a7e68" } }, "$", rRetail.toFixed(2)), rRetail > 0 && /* @__PURE__ */ React.createElement("span", { style: { color: rDisc ? "#a87935" : void 0 } }, rDisc ? " " : "", "$", rTrade.toFixed(2), rSuffix))));
      }))));
    };
    const renderLaborAdd = () => {
      if (isReadonly) return null;
      if (activeAddArea !== "labor") return /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.75rem" } }, /* @__PURE__ */ React.createElement("button", { className: "rof-card-action", onClick: () => {
        setLaborItem((l) => ({ ...l, area_id: null }));
        setActiveAddArea("labor");
      } }, "+ Add labor"));
      const isSqft = laborItem.rate_type === "per_sqft";
      const isOther = laborItem.labor_category === "other";
      const unit = laborUnitShort(laborItem.labor_category);
      const isLinear = unit === "lin ft";
      const lineTotal = isSqft ? parseFloat(laborItem.rate_sqft || 0) * parseFloat(laborItem.labor_sqft || 0) : parseFloat(laborItem.unit_price || 0) * parseFloat(laborItem.quantity || 1);
      const needsName = isOther && !(laborItem.custom_name || "").trim();
      const canAdd = lineTotal > 0 && !needsName;
      const matSqft = Math.round(items.filter((i) => i.item_type === "material").reduce((s, i) => s + parseFloat(i.sqft_needed || 0), 0));
      const fieldLabel = { display: "block", font: "500 0.5rem/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 5 };
      const numInput = (val, onCh) => /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          inputMode: "decimal",
          placeholder: "0",
          value: val,
          onChange: (e) => {
            const v = e.target.value;
            if (v === "" || /^\d*\.?\d{0,2}$/.test(v)) onCh(v);
          },
          style: { width: "100%", padding: "0.5rem 0.625rem", border: "0.5px solid rgba(28,25,23,0.2)", background: "#f5f0e8", font: "400 0.875rem/1 Inter, sans-serif", boxSizing: "border-box" }
        }
      );
      return /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.75rem", border: "0.5px solid rgba(28,25,23,0.16)", borderLeft: "2px solid #a87935", background: "rgba(28,25,23,0.025)" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", gap: "0.75rem", padding: "0.75rem 1rem", borderBottom: "0.5px solid rgba(28,25,23,0.1)", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 0.5625rem/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, "New labor line"), /* @__PURE__ */ React.createElement("div", { style: { display: "inline-flex", border: "0.5px solid rgba(28,25,23,0.2)" } }, [["per_sqft", isLinear ? "Per lin ft" : "Per sq ft"], ["flat", "Flat rate"]].map(([rt, lbl]) => /* @__PURE__ */ React.createElement(
        "button",
        {
          key: rt,
          onClick: () => setLaborItem((l) => ({ ...l, rate_type: rt })),
          style: {
            padding: "0.4rem 0.85rem",
            cursor: "pointer",
            border: "none",
            font: "500 0.5625rem/1 ui-monospace, monospace",
            letterSpacing: "0.1em",
            textTransform: "uppercase",
            background: laborItem.rate_type === rt ? "#1c1917" : "transparent",
            color: laborItem.rate_type === rt ? "#f5f0e8" : "#8a7e68"
          }
        },
        lbl
      )))), /* @__PURE__ */ React.createElement("div", { style: { padding: "1rem" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-field-row", style: { marginBottom: "0.875rem" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Category"), /* @__PURE__ */ React.createElement(
        "select",
        {
          value: laborItem.labor_category,
          onChange: (e) => setLaborItem((l) => ({ ...l, labor_category: e.target.value }))
        },
        Object.entries(laborCategoryLabels).map(([v, lbl]) => /* @__PURE__ */ React.createElement("option", { key: v, value: v }, lbl))
      )), isOther ? /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Specify ", "\u2014", " name this labor line *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          maxLength: 60,
          autoFocus: true,
          placeholder: "e.g. Stair nosing install, Curbless shower prep",
          value: laborItem.custom_name,
          onChange: (e) => setLaborItem((l) => ({ ...l, custom_name: e.target.value }))
        }
      )) : /* @__PURE__ */ React.createElement("div", null)), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Description ", "\u2014", " one detail per line, shown on the estimate"), /* @__PURE__ */ React.createElement(
        "textarea",
        {
          rows: "2",
          placeholder: "e.g.\nInstall porcelain tile with thinset + grout\nSeal and haul away debris",
          value: laborItem.description,
          onChange: (e) => setLaborItem((l) => ({ ...l, description: e.target.value }))
        }
      )), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "flex-end", gap: "0.625rem", flexWrap: "wrap", marginTop: "1rem", paddingTop: "0.875rem", borderTop: "0.5px solid rgba(28,25,23,0.08)" } }, isSqft ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { style: { width: 118 } }, /* @__PURE__ */ React.createElement("label", { style: fieldLabel }, "Rate / ", unit), numInput(laborItem.rate_sqft, (v) => setLaborItem((l) => ({ ...l, rate_sqft: v })))), /* @__PURE__ */ React.createElement("span", { style: { font: '300 1.125rem/1 "Cormorant Garamond", serif', color: "#8a7e68", paddingBottom: "0.45rem" } }, "\xD7"), /* @__PURE__ */ React.createElement("div", { style: { width: 132 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", gap: 6 } }, /* @__PURE__ */ React.createElement("label", { style: fieldLabel }, isLinear ? "Length (lin ft)" : "Area (sqft)"), !isLinear && matSqft > 0 && parseFloat(laborItem.labor_sqft || 0) !== matSqft && /* @__PURE__ */ React.createElement(
        "button",
        {
          onClick: () => setLaborItem((l) => ({ ...l, labor_sqft: String(matSqft) })),
          title: "Match the total material sqft on this estimate",
          style: { background: "none", border: "none", padding: 0, cursor: "pointer", font: "500 0.5rem/1 ui-monospace, monospace", letterSpacing: "0.06em", textTransform: "uppercase", color: "#a87935" }
        },
        "Use ",
        matSqft
      )), numInput(laborItem.labor_sqft, (v) => setLaborItem((l) => ({ ...l, labor_sqft: v }))))) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { style: { width: 118 } }, /* @__PURE__ */ React.createElement("label", { style: fieldLabel }, "Price"), numInput(laborItem.unit_price, (v) => setLaborItem((l) => ({ ...l, unit_price: v })))), /* @__PURE__ */ React.createElement("span", { style: { font: '300 1.125rem/1 "Cormorant Garamond", serif', color: "#8a7e68", paddingBottom: "0.45rem" } }, "\xD7"), /* @__PURE__ */ React.createElement("div", { style: { width: 90 } }, /* @__PURE__ */ React.createElement("label", { style: fieldLabel }, "Qty"), numInput(laborItem.quantity, (v) => setLaborItem((l) => ({ ...l, quantity: v }))))), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: 90, textAlign: "right" } }, /* @__PURE__ */ React.createElement("div", { style: { ...fieldLabel, marginBottom: 3 } }, "Line total"), /* @__PURE__ */ React.createElement("div", { style: { font: '300 1.5rem/1 "Cormorant Garamond", serif', color: canAdd ? "#1c1917" : "#b8ad97", letterSpacing: "-0.01em" } }, "$", lineTotal.toFixed(2)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "flex-end", gap: "0.5rem", marginTop: "1rem" } }, /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", style: { padding: "0.5rem 0.85rem" }, onClick: () => setActiveAddArea(null) }, "Cancel"), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light primary",
          onClick: addLaborItem,
          disabled: !canAdd,
          style: { padding: "0.5rem 1.1rem", opacity: canAdd ? 1 : 0.5, cursor: canAdd ? "pointer" : "not-allowed" }
        },
        "Add labor"
      ))));
    };
    const renderMaterialsBody = () => {
      const mats = items.filter((i) => i.item_type === "material");
      return /* @__PURE__ */ React.createElement(React.Fragment, null, renderMaterialAdd(), mats.length > 0 ? /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-items-header" + (showCost ? "" : " rof-nocost") }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Item"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Qty"), showCost && /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Cost \xB7 Mgn"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Price"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Subtotal")), mats.map(renderMaterialRow)) : /* @__PURE__ */ React.createElement("div", { className: "rof-items-empty" }, "No materials yet \u2014 add flooring, trim, or accessories."));
    };
    const renderLaborBody = () => {
      const labor = items.filter((i) => i.item_type === "labor");
      return /* @__PURE__ */ React.createElement(React.Fragment, null, labor.length > 0 ? /* @__PURE__ */ React.createElement("div", { style: { marginTop: "1rem" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-items-header", style: { gridTemplateColumns: laborCols } }, /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement("span", null, "Labor"), /* @__PURE__ */ React.createElement("span", null, "Description"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Rate"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Subtotal")), labor.map(renderLaborRow)) : /* @__PURE__ */ React.createElement("div", { className: "rof-items-empty" }, "No labor lines yet \u2014 add installation, tearout, prep, etc."), renderLaborAdd());
    };
    return /* @__PURE__ */ React.createElement("div", { style: { paddingBottom: 96 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("dashboard") }, "Workspace"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("estimates") }, "Estimates"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, editId && estimate ? estimate.estimate_number : "New estimate")), /* @__PURE__ */ React.createElement("div", { className: "qw-meta-right", style: { alignItems: "center" } }, estimate && /* @__PURE__ */ React.createElement("span", null, "Updated ", fmtD(estimate.updated_at)), editId && /* @__PURE__ */ React.createElement(RepPrevNext, { section: "estimates", id: editId, view: "estimate-edit", navigate, linkStyle: { color: "var(--brass-muted)", textDecoration: "none" } }))), /* @__PURE__ */ React.createElement("div", { className: "ws-head" }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, editId && estimate ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Estimate ", /* @__PURE__ */ React.createElement("em", null, estimate.estimate_number)) : /* @__PURE__ */ React.createElement(React.Fragment, null, "New ", /* @__PURE__ */ React.createElement("em", null, "estimate"))), /* @__PURE__ */ React.createElement("span", { className: "rov-chip soft", style: { color: st.tone } }, "\u25CF", " ", st.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-identity" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, initials), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "qw-cust-line" }, form.customer_name || "No customer yet"), /* @__PURE__ */ React.createElement("div", { className: "qw-cust-sub" }, form.customer_email || form.phone ? /* @__PURE__ */ React.createElement(React.Fragment, null, [form.customer_email, form.phone].filter(Boolean).join(" \xB7 "), form.project_name ? " \xB7 " + form.project_name : "") : "Pick an existing customer or enter details below")))), /* @__PURE__ */ React.createElement("div", { className: "ws-head-actions" }, estimate && /* @__PURE__ */ React.createElement(
      AddTaskButton,
      {
        navigate,
        className: "ol-header-btn",
        label: "+ Task",
        prefill: {
          title: "Follow up on estimate " + estimate.estimate_number,
          linked_estimate_id: estimate.id,
          customer_name: form.customer_name,
          customer_email: form.customer_email,
          customer_phone: form.phone,
          context_label: estimate.estimate_number + (form.customer_name ? " \xB7 " + form.customer_name : "")
        }
      }
    ), estimate && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/estimates/" + estimate.id + "/pdf?preview=true");
        setPdfViewer({ url, filename: (estimate.estimate_number || "estimate") + ".pdf" });
      } catch (e) {
        alert("Failed to load PDF");
      }
    } }, "Preview PDF"), estimate && !isReadonly && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: loadPreview, disabled: previewLoading }, previewLoading ? "Loading\u2026" : effStatus === "expired" ? "Re-send (renew 14 days)" : estimate.status === "sent" ? "Resend estimate" : "Preview & send"), savedFlash && !saving && /* @__PURE__ */ React.createElement("span", { style: { alignSelf: "center", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "#3f7d5a" } }, "\u2713", " Saved"), !isReadonly && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: saveEstimate, disabled: saving }, saving ? "Saving\u2026" : estimate ? "Save details" : "Save draft"), estimate && estimate.status === "draft" && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: deleteEstimate }, "Delete draft"))), /* @__PURE__ */ React.createElement("div", { className: "qw-stepper cols-5" }, estSteps.map((s, i) => {
      const state = estStepState(i);
      return /* @__PURE__ */ React.createElement("div", { key: s.label, className: "qw-step " + state }, /* @__PURE__ */ React.createElement("div", { className: "qw-step-label" }, /* @__PURE__ */ React.createElement("span", { className: "qw-step-mark" }, state === "done" ? "\u2713" : state === "dead" ? "\u2715" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", null, s.label)), /* @__PURE__ */ React.createElement("div", { className: "qw-step-meta" }, s.meta));
    })), isTerminalNeg && /* @__PURE__ */ React.createElement("div", { style: {
      marginTop: "-12px",
      marginBottom: "24px",
      padding: "0.75rem 1rem",
      background: effStatus === "declined" ? "rgba(197,71,49,0.08)" : "rgba(28,25,23,0.05)",
      border: "0.5px solid " + (effStatus === "declined" ? "rgba(197,71,49,0.35)" : "rgba(28,25,23,0.18)"),
      font: "400 0.8125rem/1.5 Inter, sans-serif",
      color: "rgba(28,25,23,0.75)"
    } }, /* @__PURE__ */ React.createElement("strong", { style: { color: effStatus === "declined" ? "#c54731" : "#8a7e68" } }, effStatus === "declined" ? "Declined" : "Expired", ":"), " ", effStatus === "declined" ? estimate.decline_reason ? "Reason: " + estimate.decline_reason : "The customer declined this estimate." : "Re-send to renew for 14 days."), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Total"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", parseFloat(estimate ? estimate.total || 0 : 0).toFixed(2))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Lines"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, materialItems.length + laborItems.length), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-s" }, materialItems.length, " mat \xB7 ", laborItems.length, " labor")), (() => {
      const estTotal = parseFloat(estimate ? estimate.total || 0 : 0) || 0;
      const depAmt = milestones.length > 0 ? estTotal * (parseFloat(milestones[0].percent) || 0) / 100 : depositType === "percent" ? Math.min(estTotal, Math.round(estTotal * ((parseFloat(depositValue) || 0) / 100) * 100) / 100) : depositType === "fixed" ? Math.min(estTotal, parseFloat(depositValue) || 0) : 0;
      if (!(depAmt > 0)) return null;
      return /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Deposit due"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", depAmt.toFixed(2)));
    })(), showCost && estBlendedMargin !== null && /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Margin \xB7 rep only"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, estBlendedMargin.toFixed(1), "%")), estimate && estExpiresAt && ["draft", "sent", "accepted"].includes(estimate.status) && /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Expires"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: estIsExpired ? { color: "#c54731" } : estDaysLeft !== null && estDaysLeft < 7 ? { color: "var(--gold, #c9a668)" } : void 0 }, estIsExpired ? "Expired " + fmtD(estExpiresAt) : fmtD(estExpiresAt) + " \xB7 " + estDaysLeft + "d")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, estimate && estimate.sent_at ? "Sent" : "Drafted"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, estimate ? fmtD(estimate.sent_at || estimate.created_at) : "\u2014"))), pdfViewer && /* @__PURE__ */ React.createElement(
      RepPdfViewer,
      {
        url: pdfViewer.url,
        filename: pdfViewer.filename,
        downloadPath: "/api/rep/estimates/" + estimate.id + "/pdf",
        onClose: () => setPdfViewer(null)
      }
    ), sendFeedback && /* @__PURE__ */ React.createElement("div", { className: "rof-alert success", style: { marginBottom: "1rem" } }, sendFeedback), estimate && estimate.status === "accepted" && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "1rem", padding: "0.875rem 1.125rem", marginBottom: "1rem", background: "rgba(58,122,78,0.1)", border: "0.5px solid rgba(58,122,78,0.35)", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("span", { style: { font: '400 1rem/1.3 "Cormorant Garamond", serif', color: "#2f6340", flex: 1 } }, "\u2713", " Accepted", estimate.accepted_by_name ? " by " + estimate.accepted_by_name : "", estimate.accepted_at ? " on " + fmtD(estimate.accepted_at) : ""), canConvert && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", style: { padding: "0.5rem 1rem" }, onClick: openConvertModal, disabled: converting }, "Convert to Order ", "\u2192")), estimate && estimate.status !== "draft" && publicLink && /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", style: { padding: "0.4rem 0.8rem" }, onClick: copyPublicLink }, copied ? "\u2713 Copied" : "Copy customer link")), /* @__PURE__ */ React.createElement("div", { className: "qw-grid" }, /* @__PURE__ */ React.createElement("div", { className: "qw-main" }, /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Customer",
        actions: !isReadonly && selectedCustomer ? /* @__PURE__ */ React.createElement("button", { className: "rof-card-action", onClick: clearCustomer }, "Change") : null
      },
      !isReadonly && !selectedCustomer && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light" + (customerMode === "search" ? " primary" : ""),
          onClick: () => {
            setCustomerMode("search");
            setCustomerMatch(null);
          },
          style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
        },
        "Search Existing"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light" + (customerMode === "new" ? " primary" : ""),
          onClick: () => {
            setCustomerMode("new");
            setCustomerResults([]);
            setCustomerSearch("");
          },
          style: { padding: "0.35rem 0.75rem", fontSize: "0.5625rem" }
        },
        "+ New Customer"
      )),
      !isReadonly && customerMode === "search" && !selectedCustomer && /* @__PURE__ */ React.createElement("div", { className: "rof-search-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Search"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: customerSearch,
          onChange: (e) => searchCustomers(e.target.value),
          placeholder: "Name, email, or phone..."
        }
      )), customerSearching && !customerResults.length && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "Searching..."), !customerSearching && customerSearch.trim().length >= 2 && customerResults.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", color: "#8a7e68", marginTop: "-0.5rem", marginBottom: "0.5rem" } }, "No customers found"), customerResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "rof-search-results" }, customerResults.map((c, i) => /* @__PURE__ */ React.createElement("div", { key: c.id || i, className: "rof-search-item", onMouseDown: (e) => {
        e.preventDefault();
        selectCustomer(c);
        setCustomerMode("new");
      } }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0, flex: 1 } }, /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-name" }, c.name, c.company_name ? " \u2014 " + c.company_name : ""), /* @__PURE__ */ React.createElement("div", { className: "rof-search-item-sub" }, c.email, c.phone ? " \xB7 " + c.phone : "")), /* @__PURE__ */ React.createElement("span", { className: "rof-search-tag " + (c.type === "trade" ? "trade" : c.type === "order" ? "order" : "new") }, c.type === "order" ? "prev order" : c.type))))),
      selectedCustomer && (() => {
        const c = selectedCustomer;
        const isTrade = c.type === "trade";
        const disc = parseFloat(c.discount_percent || 0);
        const addr = [c.address_line1, [c.city, c.state].filter(Boolean).join(", "), c.zip].filter(Boolean).join(" \xB7 ");
        const custInitials = (c.name || "?").split(" ").map((w) => w[0]).slice(0, 2).join("").toUpperCase();
        const mini = (label, value, color) => /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "#8a7e68", marginBottom: 4 } }, label), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 Inter, sans-serif", color: color || "#1c1917", letterSpacing: "-0.005em" } }, value));
        return /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.13)", background: "#ece5d8" } }, /* @__PURE__ */ React.createElement("div", { style: {
          padding: "1rem 1.125rem",
          display: "grid",
          gridTemplateColumns: "54px 1.4fr 1fr",
          gap: "1.25rem",
          alignItems: "flex-start"
        } }, /* @__PURE__ */ React.createElement("div", { style: {
          width: 54,
          height: 54,
          background: "#d8cdb6",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          font: '400 20px/1 "Cormorant Garamond", serif',
          color: "#1c1917",
          letterSpacing: "-0.012em"
        } }, custInitials), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 3, minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 19px/1.2 "Cormorant Garamond", serif', color: "#1c1917", letterSpacing: "-0.012em" } }, c.name), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, [c.company_name, isTrade ? "Trade" : "Retail", c.tier_name].filter(Boolean).join(" \xB7 ")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.5 Inter, sans-serif", color: "#1c1917", marginTop: 4 } }, c.email, c.phone ? " \xB7 " + c.phone : ""), addr && /* @__PURE__ */ React.createElement("div", { style: { font: "400 12.5px/1.4 Inter, sans-serif", color: "rgba(28,25,23,0.7)" } }, addr)), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 12 } }, mini("Discount tier", isTrade && disc > 0 ? "Trade \xB7 " + disc + "% off" : isTrade ? "Trade \xB7 list" : "Retail \xB7 list", disc > 0 ? "#a87935" : void 0), mini("Lifetime", "$" + parseFloat(c.lifetime_value || 0).toLocaleString("en-US", { maximumFractionDigits: 0 }) + " \xB7 " + (c.order_count || 0) + " order" + ((c.order_count || 0) !== 1 ? "s" : "")))), /* @__PURE__ */ React.createElement("div", { style: {
          padding: "0.625rem 1.125rem",
          borderTop: "0.5px solid rgba(28,25,23,0.1)",
          background: "#d8cdb6",
          display: "flex",
          justifyContent: "space-between",
          alignItems: "baseline",
          gap: 12
        } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#8a7e68" } }, "\u25CF", " Drives pricing & terms on this estimate"), /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "#8a7e68" } }, "Avg order ", /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "#1c1917", letterSpacing: "0", textTransform: "none", marginLeft: 4 } }, (c.order_count || 0) > 0 ? "$" + (parseFloat(c.lifetime_value || 0) / c.order_count).toLocaleString("en-US", { maximumFractionDigits: 0 }) : "\u2014"))));
      })(),
      !isReadonly && customerMatch && !selectedCustomer && /* @__PURE__ */ React.createElement("div", { className: "rof-alert error", style: { display: "flex", alignItems: "center", gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }, "Customer already exists: ", /* @__PURE__ */ React.createElement("strong", null, customerMatch.name), " (", customerMatch.email, ")"), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "rof-btn-light",
          onClick: () => {
            selectCustomer(customerMatch);
            setCustomerMode("search");
          },
          style: { padding: "0.25rem 0.5rem", fontSize: "0.5rem", flexShrink: 0, whiteSpace: "nowrap" }
        },
        "Use this customer"
      )),
      !selectedCustomer && isReadonly && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Full Name *"), /* @__PURE__ */ React.createElement("input", { type: "text", value: form.customer_name, disabled: true, placeholder: "John Smith" })), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Email *"), /* @__PURE__ */ React.createElement("input", { type: "email", value: form.customer_email, disabled: true, placeholder: "email@example.com" }))), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Phone *"), /* @__PURE__ */ React.createElement("input", { type: "tel", value: form.phone, disabled: true, placeholder: "(555) 123-4567" })), /* @__PURE__ */ React.createElement("div", null))),
      !selectedCustomer && !isReadonly && customerMode === "new" && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
        CustomerNameFields,
        {
          first: form.customer_first,
          middle: form.customer_middle,
          last: form.customer_last,
          onFirst: (v) => setForm((f) => ({ ...f, customer_first: v, customer_name: composeCustomerName(v, f.customer_middle, f.customer_last) })),
          onMiddle: (v) => setForm((f) => ({ ...f, customer_middle: v, customer_name: composeCustomerName(f.customer_first, v, f.customer_last) })),
          onLast: (v) => setForm((f) => ({ ...f, customer_last: v, customer_name: composeCustomerName(f.customer_first, f.customer_middle, v) }))
        }
      ), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Email *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "email",
          value: form.customer_email,
          onChange: (e) => setForm((f) => ({ ...f, customer_email: e.target.value })),
          onBlur: () => checkCustomerMatch("email"),
          placeholder: "email@example.com"
        }
      )), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Phone *"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "tel",
          value: form.phone,
          onChange: (e) => setForm((f) => ({ ...f, phone: formatPhone(e.target.value) })),
          onBlur: () => checkCustomerMatch("phone"),
          placeholder: "(555) 123-4567"
        }
      ))))
    ), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Project" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Project Name"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        placeholder: "e.g. Kitchen Renovation",
        value: form.project_name,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, project_name: e.target.value }))
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Project Address"), /* @__PURE__ */ React.createElement(
      "input",
      {
        ref: estAddrRef,
        type: "text",
        placeholder: isReadonly ? "" : "Street address",
        value: form.project_address_line1,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, project_address_line1: e.target.value }))
      }
    ), !isReadonly && estPrevAddrs.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.3rem" } }, /* @__PURE__ */ React.createElement(
      "span",
      {
        style: { font: "400 0.6875rem/1 Inter, sans-serif", color: "#8a7e68", cursor: "pointer", textDecoration: "underline" },
        onClick: () => setShowEstPrevAddrs((s) => !s)
      },
      showEstPrevAddrs ? "Hide" : "Use",
      " previous address (",
      estPrevAddrs.length,
      ")"
    ), showEstPrevAddrs && /* @__PURE__ */ React.createElement("div", { style: { border: "0.5px solid rgba(28,25,23,0.12)", marginTop: "0.3rem" } }, estPrevAddrs.map((a, i) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: i,
        style: { padding: "0.4rem 0.75rem", cursor: "pointer", borderBottom: "0.5px solid rgba(28,25,23,0.06)" },
        onClick: () => {
          selectEstPrevAddr(a);
          setShowEstPrevAddrs(false);
        },
        onMouseEnter: (e) => e.currentTarget.style.background = "rgba(28,25,23,0.04)",
        onMouseLeave: (e) => e.currentTarget.style.background = "transparent"
      },
      /* @__PURE__ */ React.createElement("div", { style: { font: "500 0.8125rem/1.3 Inter, sans-serif" } }, a.line1),
      /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.6875rem/1.3 Inter, sans-serif", color: "#8a7e68" } }, [a.city, a.state, a.zip].filter(Boolean).join(", "))
    ))))), /* @__PURE__ */ React.createElement("div", { className: "rof-field" }, /* @__PURE__ */ React.createElement("label", null, "Suite / Unit"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: form.project_address_line2,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, project_address_line2: e.target.value }))
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "rof-field-row-3" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "City"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: form.project_city,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, project_city: e.target.value }))
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "State"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 2,
        value: form.project_state,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, project_state: e.target.value }))
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "ZIP"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        maxLength: 10,
        value: form.project_zip,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, project_zip: e.target.value }))
      }
    )))), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Sidemark" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Sidemark / job reference"), /* @__PURE__ */ React.createElement(
      SidemarkSelect,
      {
        value: form.sidemark,
        disabled: isReadonly,
        onChange: (v) => setForm((f) => ({ ...f, sidemark: v })),
        customerId: selectedCustomer && selectedCustomer.id,
        email: form.customer_email,
        placeholder: "e.g. Smith Residence \u2014 Master Bath"
      }
    ), /* @__PURE__ */ React.createElement("div", { style: { font: "400 0.6875rem/1.4 Inter, sans-serif", color: "#8a7e68", marginTop: "0.4rem" } }, "Prints on the order, vendor POs, and documents. Carried over when the estimate converts to an order."))), actionError && /* @__PURE__ */ React.createElement("div", { className: "rof-alert error", style: { marginBottom: "1rem", display: "flex", justifyContent: "space-between", gap: 12 } }, /* @__PURE__ */ React.createElement("span", null, actionError), /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", style: { padding: "0.15rem 0.5rem", fontSize: "0.5rem" }, onClick: () => setActionError(null) }, "Dismiss")), /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Materials",
        actions: /* @__PURE__ */ React.createElement(React.Fragment, null, showCost && /* @__PURE__ */ React.createElement("span", { style: { font: "500 0.5625rem/1 ui-monospace, monospace", letterSpacing: "0.08em", textTransform: "uppercase", color: "#a87935" } }, "Cost + margin \xB7 rep only"), /* @__PURE__ */ React.createElement("span", { style: { font: '400 1rem/1 "Cormorant Garamond", serif', color: "#1c1917" } }, "$", items.filter((i) => i.item_type === "material").reduce((s, i) => s + parseFloat(i.subtotal || 0), 0).toFixed(2)))
      },
      renderMaterialsBody()
    ), /* @__PURE__ */ React.createElement(
      WizRepCard,
      {
        title: "Labor",
        actions: /* @__PURE__ */ React.createElement("span", { style: { font: '400 1rem/1 "Cormorant Garamond", serif', color: "#1c1917" } }, "$", items.filter((i) => i.item_type === "labor").reduce((s, i) => s + parseFloat(i.subtotal || 0), 0).toFixed(2))
      },
      renderLaborBody()
    ), /* @__PURE__ */ React.createElement(WizRepCard, { title: "Notes" }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: 0 } }, /* @__PURE__ */ React.createElement("label", null, "Customer Notes (shown on PDF)"), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        rows: "3",
        value: form.notes,
        disabled: isReadonly,
        onChange: (e) => setForm((f) => ({ ...f, notes: e.target.value })),
        placeholder: "Notes visible to customer..."
      }
    ))), estimate && estimate.status !== "draft" && /* @__PURE__ */ React.createElement("section", { className: "qw-card", style: { marginTop: "1rem" } }, /* @__PURE__ */ React.createElement("div", { className: "qw-card-head" }, /* @__PURE__ */ React.createElement("h3", null, "Engagement"), engOpens > 0 && /* @__PURE__ */ React.createElement("span", { className: "qw-card-hint" }, "\u25CF", " ", engOpens === 1 ? "1 open" : engOpens + " opens")), /* @__PURE__ */ React.createElement("div", { style: { padding: "12px 20px 0", display: "flex", gap: 20, flexWrap: "wrap" } }, [[engOpens, engOpens === 1 ? "open" : "opens"], [engViewers, engViewers === 1 ? "viewer" : "viewers"], [engReplies, engReplies === 1 ? "reply" : "replies"]].map(([n, l]) => /* @__PURE__ */ React.createElement("div", { key: l }, /* @__PURE__ */ React.createElement("div", { style: { font: '400 22px/1 "Cormorant Garamond", serif', color: "#1c1917" } }, n), /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--brass-muted)", marginTop: 3 } }, l))), lastViewedAt && /* @__PURE__ */ React.createElement("div", { style: { marginLeft: "auto", alignSelf: "flex-end", font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--brass-muted)" } }, "Last viewed ", fmtDT(lastViewedAt))), (() => {
      const entries = [];
      events.forEach((e) => {
        const prev = entries[entries.length - 1];
        if (e.event_type === "viewed" && prev && prev.event_type === "viewed") {
          prev._count += 1;
          prev.created_at = e.created_at;
        } else {
          entries.push({ ...e, _count: 1 });
        }
      });
      if (entries.length === 0) {
        return /* @__PURE__ */ React.createElement("div", { className: "qw-thread-empty" }, "No activity yet \u2014 opens, replies, and accept/decline land here.");
      }
      return /* @__PURE__ */ React.createElement("div", { className: "qw-thread-list" }, entries.map((e) => {
        const isRep = e.actor === "rep";
        const isSys = ["sent", "viewed", "accepted", "declined", "converted"].includes(e.event_type);
        const name = e.actor_name || (e.actor === "customer" ? form.customer_name : "System");
        const initials2 = (name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?";
        let label = e.body || "";
        if (e.event_type === "viewed") {
          const src = e.meta && e.meta.source === "page" ? "page view" : "email open";
          label = e._count > 1 ? e._count + " " + src + "s" : src;
        } else if (e.event_type === "sent") label = e.body || "Estimate sent";
        else if (e.event_type === "accepted") label = e.body || "Accepted by customer";
        else if (e.event_type === "declined") label = e.body || "Declined by customer";
        else if (e.event_type === "converted") label = e.body || "Converted";
        return /* @__PURE__ */ React.createElement("div", { key: e.id, className: "qw-msg" + (isRep ? " rep" : "") }, /* @__PURE__ */ React.createElement("div", { className: "qw-msg-avatar" }, initials2), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "qw-msg-head" }, /* @__PURE__ */ React.createElement("span", { className: "qw-msg-name" }, name), /* @__PURE__ */ React.createElement("span", { className: "qw-msg-time" }, fmtDT(e.created_at)), e.event_type === "note" && /* @__PURE__ */ React.createElement("span", { className: "qw-msg-tag" }, "Internal note"), e.event_type === "reply" && e.meta && e.meta.logged_by && /* @__PURE__ */ React.createElement("span", { className: "qw-msg-tag" }, "Logged by ", e.meta.logged_by)), /* @__PURE__ */ React.createElement("div", { className: "qw-msg-body" + (isSys ? " sys" : e.event_type === "note" ? " note" : "") }, e.event_type === "reply" ? "\u201C" + label + "\u201D" : label)));
      }));
    })(), /* @__PURE__ */ React.createElement("div", { className: "qw-composer" }, /* @__PURE__ */ React.createElement(
      "textarea",
      {
        rows: 2,
        maxLength: 2e3,
        placeholder: "Log a customer reply or an internal note\u2026",
        value: threadText,
        onChange: (e) => setThreadText(e.target.value),
        disabled: threadPosting
      }
    ), /* @__PURE__ */ React.createElement("div", { className: "qw-composer-bar" }, /* @__PURE__ */ React.createElement("span", { className: "qw-composer-hint" }, "Internal record \u2014 nothing here emails the customer"), /* @__PURE__ */ React.createElement("div", { className: "qw-composer-actions" }, /* @__PURE__ */ React.createElement("button", { className: "qw-composer-btn ghost", onClick: () => postThreadEntry("note"), disabled: threadPosting || !threadText.trim() }, "+ Internal note"), /* @__PURE__ */ React.createElement("button", { className: "qw-composer-btn", onClick: () => postThreadEntry("reply"), disabled: threadPosting || !threadText.trim() }, "Log customer reply")))))), /* @__PURE__ */ React.createElement("aside", { className: "qw-rail" }, editId && estimate && /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Sales rep"), estimate.sales_rep_id ? /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, (estimate.rep_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name" }, estimate.rep_name || "Rep", estimate.sales_rep_id === repInfo.id && /* @__PURE__ */ React.createElement("span", { className: "ws-you-tag" }, "You")), estimate.rep_email && /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, estimate.rep_email))) : /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar", style: { background: "var(--brass-warm, #d8cdb6)", color: "var(--brass-muted, #8a7e68)" } }, "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name", style: { color: "var(--rod-muted)" } }, "Unassigned"), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "No rep owns this estimate yet")))), /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Totals"), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement(RovRow, { l: "Materials \xB7 " + materialItems.length + " line" + (materialItems.length !== 1 ? "s" : ""), v: "$" + parseFloat(estimate ? estimate.materials_subtotal || 0 : 0).toFixed(2) }), /* @__PURE__ */ React.createElement(RovRow, { l: "Labor & services", v: "$" + parseFloat(estimate ? estimate.labor_subtotal || 0 : 0).toFixed(2) }), estimate && parseFloat(estimate.tax_amount || 0) > 0 && /* @__PURE__ */ React.createElement(RovRow, { l: "Tax \xB7 materials only", v: "$" + parseFloat(estimate.tax_amount).toFixed(2) })), /* @__PURE__ */ React.createElement("div", { className: "rov-total-row", style: { marginTop: 12 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", color: "var(--rod-ink)" } }, "Total"), /* @__PURE__ */ React.createElement("span", { className: "rov-total-value" }, "$", parseFloat(estimate ? estimate.total || 0 : 0).toFixed(2))), estimate && estExpiresAt && ["draft", "sent", "accepted"].includes(estimate.status) && /* @__PURE__ */ React.createElement("div", { className: "qw-commission", style: estIsExpired ? { color: "#c54731" } : void 0 }, estIsExpired ? "Expired " : "Expires ", estExpiresAt.toLocaleDateString("en-US", { month: "short", day: "numeric" }), !estIsExpired && estDaysLeft !== null ? " \xB7 " + estDaysLeft + "d left" : ""), showCost && estBlendedMargin !== null && /* @__PURE__ */ React.createElement("div", { className: "qw-commission" }, "Materials margin ", estBlendedMargin.toFixed(1), "% \xB7 rep only"), (() => {
      const estTotal = parseFloat(estimate ? estimate.total || 0 : 0) || 0;
      if (milestones.length > 0) {
        const m0 = milestones[0];
        const m0amt = estTotal * (parseFloat(m0.percent) || 0) / 100;
        return /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14, paddingTop: 14, borderTop: "0.5px solid rgba(28,25,23,0.14)" } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: "var(--brass-muted)", marginBottom: 6 } }, "Deposit"), /* @__PURE__ */ React.createElement("div", { style: { font: '400 11px/1.5 "Inter", sans-serif', color: "var(--brass-muted)" } }, "The schedule's first milestone \u2014 ", /* @__PURE__ */ React.createElement("strong", null, m0.label || "Deposit", " \xB7 $", m0amt.toFixed(2)), " \u2014 is what the customer pays online at acceptance."));
      }
      const depAmt = depositType === "percent" ? Math.min(estTotal, Math.round(estTotal * ((parseFloat(depositValue) || 0) / 100) * 100) / 100) : depositType === "fixed" ? Math.min(estTotal, parseFloat(depositValue) || 0) : 0;
      return /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14, paddingTop: 14, borderTop: "0.5px solid rgba(28,25,23,0.14)" } }, /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: depositType === "none" ? 0 : "0.5rem" } }, /* @__PURE__ */ React.createElement("label", null, "Deposit"), /* @__PURE__ */ React.createElement(
        "select",
        {
          value: depositType,
          disabled: isReadonly,
          onChange: (e) => setDepositType(e.target.value)
        },
        /* @__PURE__ */ React.createElement("option", { value: "none" }, "No deposit"),
        /* @__PURE__ */ React.createElement("option", { value: "percent" }, "Percentage"),
        /* @__PURE__ */ React.createElement("option", { value: "fixed" }, "Fixed amount")
      )), depositType === "percent" && /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: "0.4rem" } }, /* @__PURE__ */ React.createElement("label", null, "Percent of total"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "0",
          max: "100",
          step: "1",
          value: depositValue,
          disabled: isReadonly,
          onChange: (e) => setDepositValue(e.target.value),
          placeholder: "50"
        }
      )), depositType === "fixed" && /* @__PURE__ */ React.createElement("div", { className: "rof-field", style: { marginBottom: "0.4rem" } }, /* @__PURE__ */ React.createElement("label", null, "Amount ($)"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "0",
          max: estTotal || void 0,
          step: "0.01",
          value: depositValue,
          disabled: isReadonly,
          onChange: (e) => setDepositValue(e.target.value),
          placeholder: "0.00"
        }
      )), depositType !== "none" && /* @__PURE__ */ React.createElement("div", { style: { font: '400 11px/1.4 "Inter", sans-serif', color: "var(--brass-muted)" } }, "Customer can pay a ", /* @__PURE__ */ React.createElement("strong", null, "$", depAmt.toFixed(2)), " deposit online at acceptance."));
    })()), (() => {
      const estTotal = estimate ? parseFloat(estimate.total || 0) || 0 : 0;
      const sumOff = milestones.length > 0 && Math.abs(schedulePctSum - 100) > 0.01;
      const miniInput = { width: "100%", padding: "5px 7px", border: "0.5px solid rgba(28,25,23,0.25)", borderRadius: 4, font: "400 12px/1.2 Inter, sans-serif", background: isReadonly ? "#f3efe7" : "#fff", color: "var(--ink)" };
      const linkBtn = { background: "none", border: "none", padding: 0, cursor: "pointer", font: "500 11px/1 Inter, sans-serif", color: "var(--brass)", textDecoration: "underline" };
      return /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Payment schedule"), /* @__PURE__ */ React.createElement("div", { style: { font: '400 11px/1.5 "Inter", sans-serif', color: "var(--brass-muted)", marginBottom: 10 } }, "Progress billing (e.g. 50 / 30 / 20). Shown to the customer on the estimate and tracked on the order."), editingSchedule ? /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 10 } }, milestones.map((m, i) => /* @__PURE__ */ React.createElement("div", { key: i, style: { display: "grid", gridTemplateColumns: "1fr 58px 18px", gap: 6, alignItems: "center" } }, /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: m.label,
          disabled: isReadonly,
          style: miniInput,
          placeholder: "Deposit",
          onChange: (e) => updateMilestone(i, { label: e.target.value })
        }
      ), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "0",
          max: "100",
          step: "1",
          value: m.percent,
          disabled: isReadonly,
          style: { ...miniInput, paddingRight: 16, textAlign: "right" },
          placeholder: "0",
          onChange: (e) => updateMilestone(i, { percent: e.target.value })
        }
      ), /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", right: 5, top: "50%", transform: "translateY(-50%)", font: "400 11px/1 Inter, sans-serif", color: "var(--brass-muted)" } }, "%")), !isReadonly ? /* @__PURE__ */ React.createElement("button", { title: "Remove", onClick: () => removeMilestone(i), style: { background: "none", border: "none", cursor: "pointer", font: "400 15px/1 Inter, sans-serif", color: "var(--brass-muted)", padding: 0 } }, "\xD7") : /* @__PURE__ */ React.createElement("span", null), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          value: m.due_label || "",
          disabled: isReadonly,
          style: { ...miniInput, gridColumn: "1 / 2" },
          placeholder: "e.g. at install start",
          onChange: (e) => updateMilestone(i, { due_label: e.target.value })
        }
      ), /* @__PURE__ */ React.createElement("span", { style: { gridColumn: "2 / 4", textAlign: "right", font: '500 13px/1 "Cormorant Garamond", serif', color: "var(--ink)" } }, "$", (estTotal * (parseFloat(m.percent) || 0) / 100).toFixed(2)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", paddingTop: 6, borderTop: "0.5px solid rgba(28,25,23,0.14)" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: sumOff ? "#c05621" : "#3a7a4e" } }, schedulePctSum, "% of total", sumOff ? " \xB7 should be 100%" : " \u2713"), !isReadonly && /* @__PURE__ */ React.createElement("button", { style: linkBtn, onClick: addMilestone }, "+ Add")), !isReadonly && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8 } }, /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light primary", style: { flex: 1 }, onClick: saveSchedule, disabled: savingSchedule }, savingSchedule ? "Saving\u2026" : "Save schedule"), /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", onClick: cancelEditSchedule, disabled: savingSchedule }, "Cancel"))) : milestones.length > 0 ? /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 6 } }, milestones.map((m, i) => /* @__PURE__ */ React.createElement("div", { key: i, style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", padding: "5px 0", borderBottom: i < milestones.length - 1 ? "0.5px solid rgba(28,25,23,0.1)" : "none" } }, /* @__PURE__ */ React.createElement("span", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("span", { style: { font: "400 12px/1.3 Inter, sans-serif", color: "var(--ink)" } }, m.label, /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)" } }, " \xB7 ", parseFloat(m.percent), "%")), m.due_label && /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "400 10px/1.4 Inter, sans-serif", color: "var(--brass-muted)", marginTop: 1 } }, m.due_label)), /* @__PURE__ */ React.createElement("span", { style: { font: '500 13px/1 "Cormorant Garamond", serif', color: "var(--ink)", whiteSpace: "nowrap" } }, "$", (estTotal * (parseFloat(m.percent) || 0) / 100).toFixed(2)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "center", paddingTop: 6, borderTop: "0.5px solid rgba(28,25,23,0.14)" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: sumOff ? "#c05621" : "#3a7a4e" } }, schedulePctSum, "% of total", sumOff ? " \xB7 should be 100%" : " \u2713"), !isReadonly && /* @__PURE__ */ React.createElement("button", { style: linkBtn, onClick: startEditSchedule }, "Edit"))) : /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 8 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.5 Inter, sans-serif", color: "var(--brass-muted)" } }, "No schedule \u2014 the full total is due at conversion."), !isReadonly && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8 } }, /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light primary", style: { flex: 1 }, onClick: applyScheduleTemplate }, "Use 50 / 30 / 20"), /* @__PURE__ */ React.createElement("button", { className: "rof-btn-light", onClick: startNewSchedule }, "+ Add"))));
    })(), !editId && /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Draft checklist"), estChecklist.map((c, i) => {
      const state = c.done ? "done" : i === estFirstTodo ? "now" : "next";
      return /* @__PURE__ */ React.createElement("div", { key: c.label, className: "qw-check-row " + state }, /* @__PURE__ */ React.createElement("span", { className: "qw-check-mark" }, c.done ? "\u2713" : state === "now" ? "\u25CF" : "\u25CB"), /* @__PURE__ */ React.createElement("span", { className: "qw-check-label" }, c.label), /* @__PURE__ */ React.createElement("span", { className: "qw-check-meta" }, c.meta));
    })), editId && estimate && /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Engagement"), /* @__PURE__ */ React.createElement("div", { className: "qw-engage-grid" }, [[engOpens, engOpens === 1 ? "open" : "opens"], [engViewers, engViewers === 1 ? "viewer" : "viewers"], [engReplies, engReplies === 1 ? "reply" : "replies"]].map(([n, l]) => /* @__PURE__ */ React.createElement("div", { key: l }, /* @__PURE__ */ React.createElement("div", { className: "qw-engage-n" }, n), /* @__PURE__ */ React.createElement("div", { className: "qw-engage-l" }, l)))), /* @__PURE__ */ React.createElement("div", { className: "qw-engage-foot" }, engOpens > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, "Last opened ", fmtDT(lastViewedAt), lastReplyAt ? " \xB7 last reply " + fmtDT(lastReplyAt) : "", ".") : estimate.status === "draft" ? "Send the estimate to start tracking opens." : "No opens yet \u2014 tracking starts when the customer opens the email.")), canConvert && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Ready to commit"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "Convert to an ", /* @__PURE__ */ React.createElement("strong", null, "order"), " to bill materials ", /* @__PURE__ */ React.createElement("em", null, "and"), " labor (tax on materials only). Convert to a ", /* @__PURE__ */ React.createElement("strong", null, "quote"), " for materials only \u2014 labor is summarized in the quote notes."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: openConvertModal, disabled: converting }, "Convert to order ", "\u2192"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "qw-link",
        onClick: () => setShowConvertQuoteModal(true),
        disabled: converting,
        style: { display: "block", margin: "12px auto 0", color: "#caa97f" }
      },
      "Convert to quote instead"
    )), estimate && estimate.status === "converted" && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Converted"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "This estimate became ", estimate.converted_quote_id ? "a quote" : "an order", "."), estimate.converted_quote_id && /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: () => navigate("quote-edit", estimate.converted_quote_id) }, "View quote ", "\u2192"), estimate.converted_order_id && /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: () => navigate("order-detail", estimate.converted_order_id) }, "View order ", "\u2192")), editId && estIsExpired && /* @__PURE__ */ React.createElement("div", { className: "qw-convert-card" }, /* @__PURE__ */ React.createElement("div", { className: "qw-convert-label" }, "Estimate expired"), /* @__PURE__ */ React.createElement("div", { className: "qw-convert-copy" }, "Re-send to renew the 14-day window, then convert."), /* @__PURE__ */ React.createElement("button", { className: "qw-convert-btn", onClick: loadPreview, disabled: previewLoading }, previewLoading ? "Loading\u2026" : /* @__PURE__ */ React.createElement(React.Fragment, null, "Re-send & renew ", "\u2192"))), editId ? /* @__PURE__ */ React.createElement(InternalNotesCard, { basePath: "/api/rep/estimates/" + editId, readOnly: isReadonly, bg: "#f5f0e8" }) : /* @__PURE__ */ React.createElement("div", { className: "qw-rail-card" }, /* @__PURE__ */ React.createElement("h3", null, "Internal notes"), /* @__PURE__ */ React.createElement("p", { style: { margin: "4px 0 0", fontSize: "0.8125rem", color: "#8a7e68" } }, "Save the estimate to start adding internal notes.")), /* @__PURE__ */ React.createElement("div", { className: "qw-quick-links" }, estimate && estimate.status === "draft" && /* @__PURE__ */ React.createElement("button", { className: "qw-link muted", onClick: deleteEstimate }, "Delete draft"), /* @__PURE__ */ React.createElement("button", { className: "qw-link muted", onClick: () => navigate("estimates") }, "Back to estimates")))), /* @__PURE__ */ React.createElement("div", { className: "rof-action-bar" }, /* @__PURE__ */ React.createElement("div", { className: "rof-action-left" }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "block", font: "500 8px/1 ui-monospace, monospace", letterSpacing: "0.18em", textTransform: "uppercase", color: "rgba(245,240,232,0.47)" } }, estimate ? estimate.estimate_number + " \xB7 " + st.label : "New estimate \xB7 draft"), /* @__PURE__ */ React.createElement("span", { className: "rof-action-total", style: { display: "block", marginTop: 4 } }, "$", parseFloat(estimate ? estimate.total || 0 : 0).toFixed(2))), (form.customer_name || items.length > 0) && /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: "rgba(245,240,232,0.6)" } }, [
      form.customer_name,
      materialItems.length > 0 ? materialItems.length + " material" + (materialItems.length !== 1 ? "s" : "") : null,
      laborItems.length > 0 ? laborItems.length + " labor" : null
    ].filter(Boolean).join(" \xB7 ")), savedFlash && !saving && /* @__PURE__ */ React.createElement("span", { style: { font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: "#7fb98f" } }, "\u2713", " Saved")), !estimate ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => navigate("estimates") }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: saveEstimate, disabled: saving }, saving ? "Saving\u2026" : "Save draft")) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: () => navigate("estimates") }, "Close"), !isReadonly && /* @__PURE__ */ React.createElement("button", { className: "rof-btn", onClick: saveEstimate, disabled: createDisabled }, saving ? "Saving\u2026" : "Save"), estimate.status === "draft" && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: loadPreview, disabled: previewLoading }, previewLoading ? "Loading\u2026" : "Preview & Send"), ["sent", "accepted"].includes(estimate.status) && canConvert && /* @__PURE__ */ React.createElement("button", { className: "rof-btn rof-btn-primary", onClick: openConvertModal, disabled: converting }, "Convert to Order ", "\u2192"), estimate.status === "converted" && (estimate.converted_quote_id || estimate.converted_order_id) && /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "rof-btn rof-btn-primary",
        onClick: () => estimate.converted_quote_id ? navigate("quote-edit", estimate.converted_quote_id) : navigate("order-detail", estimate.converted_order_id)
      },
      "View ",
      estimate.converted_quote_id ? "Quote" : "Order",
      " ",
      "\u2192"
    ))), catalogOpen && /* @__PURE__ */ React.createElement(
      RofCatalogModal,
      {
        data: { customer: selectedCustomer, newCustomerName: form.customer_name },
        showCost,
        contextLabel: "Add to estimate \xB7 " + (estimate ? estimate.estimate_number : "New estimate"),
        onClose: () => setCatalogOpen(false),
        onAdd: addFromCatalog,
        onAddRug: addRugToEstimate
      }
    ), rugModalItem && /* @__PURE__ */ React.createElement(
      RugConfigModal,
      {
        carpet: rugModalItem,
        showCost,
        onClose: () => setRugModalItem(null),
        onAdd: addRugToEstimate
      }
    ), showPreview && /* @__PURE__ */ React.createElement("div", { className: "modal-backdrop", onClick: () => setShowPreview(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-content", onClick: (e) => e.stopPropagation(), style: { maxWidth: 700 } }, /* @__PURE__ */ React.createElement("div", { className: "modal-header" }, /* @__PURE__ */ React.createElement("h2", null, "Email Preview"), /* @__PURE__ */ React.createElement("button", { className: "modal-close", onClick: () => setShowPreview(false) }, "x")), /* @__PURE__ */ React.createElement("div", { className: "modal-body" }, previewMeta && /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "1rem", fontSize: "0.8125rem", color: "var(--stone-500)" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("strong", null, "To:"), " ", previewMeta.to), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("strong", null, "Subject:"), " ", previewMeta.subject), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("strong", null, "Reply-To:"), " ", previewMeta.reply_to)), /* @__PURE__ */ React.createElement("div", { style: { border: "1px solid var(--stone-200)", borderRadius: "var(--radius-sm)", maxHeight: 400, overflowY: "auto" } }, /* @__PURE__ */ React.createElement("iframe", { srcDoc: previewHtml, sandbox: "", style: { width: "100%", height: 400, border: "none" } }))), /* @__PURE__ */ React.createElement("div", { className: "modal-footer" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowPreview(false) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: sendEstimate }, "Send to Customer")))), showConvertQuoteModal && /* @__PURE__ */ React.createElement("div", { className: "modal-backdrop", onClick: () => setShowConvertQuoteModal(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-content", onClick: (e) => e.stopPropagation(), style: { maxWidth: 500 } }, /* @__PURE__ */ React.createElement("div", { className: "modal-header" }, /* @__PURE__ */ React.createElement("h2", null, "Convert to Quote"), /* @__PURE__ */ React.createElement("button", { className: "modal-close", onClick: () => setShowConvertQuoteModal(false) }, "x")), /* @__PURE__ */ React.createElement("div", { className: "modal-body" }, /* @__PURE__ */ React.createElement("p", { style: { marginBottom: "1rem", fontSize: "0.875rem", color: "var(--stone-600)" } }, "This will create a new quote with ", /* @__PURE__ */ React.createElement("strong", null, materialItems.length, " material item", materialItems.length !== 1 ? "s" : ""), " totaling ", /* @__PURE__ */ React.createElement("strong", null, "$", parseFloat(estimate.materials_subtotal || 0).toFixed(2)), "."), laborItems.length > 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "0.75rem", background: "#fffbeb", border: "1px solid #fde68a", borderRadius: "var(--radius-sm)", fontSize: "0.8125rem", marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement("strong", null, laborItems.length, " labor item", laborItems.length !== 1 ? "s" : ""), " totaling ", /* @__PURE__ */ React.createElement("strong", null, "$", parseFloat(estimate.labor_subtotal || 0).toFixed(2)), " will be excluded from the quote but summarized in the quote notes.", /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.5rem", color: "#8a7e68" } }, "Tip: convert to an ", /* @__PURE__ */ React.createElement("strong", null, "order"), " instead to bill labor alongside materials."))), /* @__PURE__ */ React.createElement("div", { className: "modal-footer" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowConvertQuoteModal(false) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: () => convertToQuote(), disabled: converting }, converting ? "Converting..." : "Convert to Quote")))), showConvertOrderModal && (() => {
      const total = parseFloat(estimate.total || 0);
      const depositNum = Math.max(0, Math.min(parseFloat(convertDeposit) || 0, total));
      const balance = Math.max(0, parseFloat((total - depositNum).toFixed(2)));
      const isFull = depositNum >= total - 5e-3;
      const primaryLabel = converting ? "Working\u2026" : convertOnTerms ? "Start on terms" : isFull ? "Collect $" + total.toFixed(2) + " & create order" : "Collect $" + depositNum.toFixed(2) + " & create order";
      return /* @__PURE__ */ React.createElement("div", { className: "modal-backdrop", onClick: () => setShowConvertOrderModal(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-content", onClick: (e) => e.stopPropagation(), style: { maxWidth: 500 } }, /* @__PURE__ */ React.createElement("div", { className: "modal-header" }, /* @__PURE__ */ React.createElement("h2", null, "Create order"), /* @__PURE__ */ React.createElement("button", { className: "modal-close", onClick: () => setShowConvertOrderModal(false) }, "x")), /* @__PURE__ */ React.createElement("div", { className: "modal-body" }, /* @__PURE__ */ React.createElement("p", { style: { marginBottom: "0.85rem", fontSize: "0.8125rem", color: "var(--stone-600)", lineHeight: 1.5 } }, "Creates an order at the estimate pricing \u2014 ", [
        materialItems.length > 0 ? materialItems.length + " material item" + (materialItems.length !== 1 ? "s" : "") : null,
        laborItems.length > 0 ? laborItems.length + " labor item" + (laborItems.length !== 1 ? "s" : "") : null
      ].filter(Boolean).join(" and "), materialItems.length > 0 && laborItems.length > 0 ? " (tax on materials only)" : "", ". Order total ", /* @__PURE__ */ React.createElement("strong", null, "$", total.toFixed(2)), "."), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, marginBottom: "1rem" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: !convertOnTerms ? "btn btn-primary btn-sm" : "btn btn-secondary btn-sm",
          style: { flex: 1 },
          onClick: () => setConvertOnTerms(false)
        },
        "Collect payment now"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: convertOnTerms ? "btn btn-primary btn-sm" : "btn btn-secondary btn-sm",
          style: { flex: 1 },
          onClick: () => setConvertOnTerms(true)
        },
        "Start on terms"
      )), convertOnTerms ? /* @__PURE__ */ React.createElement("div", { style: { padding: "0.75rem", background: "#fffbeb", border: "1px solid #fde68a", borderRadius: "var(--radius-sm)", fontSize: "0.8125rem" } }, "No payment collected now. The order opens with the full ", /* @__PURE__ */ React.createElement("strong", null, "$", total.toFixed(2)), " as balance due \u2014 bill it later with a payment request.") : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Deposit collected now"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative", flex: 1 } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: "50%", transform: "translateY(-50%)", color: "var(--stone-500)" } }, "$"), /* @__PURE__ */ React.createElement(
        "input",
        {
          className: "form-input",
          type: "number",
          min: "0",
          step: "0.01",
          value: convertDeposit,
          style: { paddingLeft: 20 },
          onChange: (e) => setConvertDeposit(e.target.value)
        }
      )), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: "btn btn-secondary btn-sm",
          onClick: () => setConvertDeposit((total / 2).toFixed(2))
        },
        "50%"
      ), /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          className: "btn btn-secondary btn-sm",
          onClick: () => setConvertDeposit(total.toFixed(2))
        },
        "Full"
      )), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 6, fontSize: "0.75rem", color: "var(--stone-500)" } }, isFull ? "Paid in full \u2014 no balance due." : /* @__PURE__ */ React.createElement(React.Fragment, null, "Balance due after: ", /* @__PURE__ */ React.createElement("strong", null, "$", balance.toFixed(2)), " (bill later with a payment request)."))), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Payment Method"), /* @__PURE__ */ React.createElement("select", { className: "form-input", value: convertMethod, onChange: (e) => setConvertMethod(e.target.value) }, /* @__PURE__ */ React.createElement("option", { value: "cash" }, "Cash"), /* @__PURE__ */ React.createElement("option", { value: "check" }, "Check"), /* @__PURE__ */ React.createElement("option", { value: "card" }, "Card"), /* @__PURE__ */ React.createElement("option", { value: "offline" }, "Offline / Other"))))), /* @__PURE__ */ React.createElement("div", { className: "modal-footer" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowConvertOrderModal(false) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: () => convertToOrder(), disabled: converting }, primaryLabel))));
    })());
  }
  function RepCashDrawerView({ navigate }) {
    const [drawer, setDrawer] = useState(null);
    const [transactions, setTransactions] = useState([]);
    const [history2, setHistory] = useState([]);
    const [loading, setLoading] = useState(true);
    const [openingBalance, setOpeningBalance] = useState("200.00");
    const [opening, setOpening] = useState(false);
    const [showCloseModal, setShowCloseModal] = useState(false);
    const [actualBalance, setActualBalance] = useState("");
    const [closeNotes, setCloseNotes] = useState("");
    const [closing, setClosing] = useState(false);
    const [closeResult, setCloseResult] = useState(null);
    const [showTxnModal, setShowTxnModal] = useState(null);
    const [txnAmount, setTxnAmount] = useState("");
    const [txnDescription, setTxnDescription] = useState("");
    const [txnSaving, setTxnSaving] = useState(false);
    const load = async () => {
      try {
        const [currentData, historyData] = await Promise.all([
          repFetch("/api/rep/cash-drawer/current"),
          repFetch("/api/rep/cash-drawer/history")
        ]);
        setDrawer(currentData.drawer);
        setTransactions(currentData.transactions || []);
        setHistory(historyData.drawers || []);
      } catch (err) {
      }
      setLoading(false);
    };
    useEffect(() => {
      load();
    }, []);
    const openDrawer = async () => {
      const bal = parseFloat(openingBalance);
      if (isNaN(bal) || bal < 0) return;
      setOpening(true);
      try {
        const data = await repFetch("/api/rep/cash-drawer/open", {
          method: "POST",
          body: JSON.stringify({ opening_balance: bal })
        });
        setDrawer(data.drawer);
        setTransactions([]);
      } catch (err) {
        alert(err.message);
      }
      setOpening(false);
    };
    const closeDrawer = async () => {
      const actual = parseFloat(actualBalance);
      if (isNaN(actual) || actual < 0) return;
      setClosing(true);
      try {
        const data = await repFetch("/api/rep/cash-drawer/close", {
          method: "POST",
          body: JSON.stringify({ actual_balance: actual, notes: closeNotes || void 0 })
        });
        setCloseResult(data.drawer);
        setDrawer(null);
        setTransactions([]);
        const historyData = await repFetch("/api/rep/cash-drawer/history");
        setHistory(historyData.drawers || []);
      } catch (err) {
        alert(err.message);
      }
      setClosing(false);
    };
    const recordTxn = async () => {
      const amt = parseFloat(txnAmount);
      if (isNaN(amt) || amt <= 0 || !showTxnModal) return;
      setTxnSaving(true);
      try {
        const data = await repFetch("/api/rep/cash-drawer/transaction", {
          method: "POST",
          body: JSON.stringify({ type: showTxnModal, amount: amt, description: txnDescription || void 0 })
        });
        setDrawer(data.drawer);
        setTransactions((prev) => [...prev, data.transaction]);
        setShowTxnModal(null);
        setTxnAmount("");
        setTxnDescription("");
      } catch (err) {
        alert(err.message);
      }
      setTxnSaving(false);
    };
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading...");
    const fmtTime = (ts) => {
      if (!ts) return "";
      const d = new Date(ts);
      return d.toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" });
    };
    const fmtDate = (ts) => {
      if (!ts) return "";
      return new Date(ts).toLocaleDateString("en-US", { month: "short", day: "numeric" });
    };
    const fmtMoney = (v) => "$" + parseFloat(v || 0).toFixed(2);
    const cashSales = transactions.filter((t) => t.type === "sale").reduce((s, t) => s + parseFloat(t.amount), 0);
    const cashRefunds = transactions.filter((t) => t.type === "refund").reduce((s, t) => s + parseFloat(t.amount), 0);
    const cashIn = transactions.filter((t) => t.type === "cash_in").reduce((s, t) => s + parseFloat(t.amount), 0);
    const cashOut = transactions.filter((t) => t.type === "cash_out").reduce((s, t) => s + parseFloat(t.amount), 0);
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "content-header" }, /* @__PURE__ */ React.createElement("h1", null, "Cash Drawer"), drawer && /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.875rem", color: "var(--stone-500)" } }, "Opened ", fmtTime(drawer.opened_at))), closeResult && /* @__PURE__ */ React.createElement("div", { style: {
      padding: "1rem 1.25rem",
      marginBottom: "1.5rem",
      borderRadius: "8px",
      background: parseFloat(closeResult.over_short) === 0 ? "#dcfce7" : parseFloat(closeResult.over_short) > 0 ? "#dbeafe" : "#fef3c7",
      border: "1px solid " + (parseFloat(closeResult.over_short) === 0 ? "#86efac" : parseFloat(closeResult.over_short) > 0 ? "#93c5fd" : "#fcd34d")
    } }, /* @__PURE__ */ React.createElement("div", { style: { fontWeight: 600, marginBottom: "0.25rem" } }, "Drawer Closed"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.875rem" } }, "Expected: ", fmtMoney(closeResult.expected_balance), " | Actual: ", fmtMoney(closeResult.actual_balance), " |", " ", parseFloat(closeResult.over_short) === 0 ? "Exact" : parseFloat(closeResult.over_short) > 0 ? "+" + fmtMoney(closeResult.over_short) + " over" : fmtMoney(Math.abs(parseFloat(closeResult.over_short))) + " short"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary btn-sm", style: { marginTop: "0.5rem" }, onClick: () => setCloseResult(null) }, "Dismiss")), !drawer ? (
      /* ===== Drawer Closed State ===== */
      /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "form-card" }, /* @__PURE__ */ React.createElement("h3", null, "Open Cash Drawer"), /* @__PURE__ */ React.createElement("p", { style: { color: "var(--stone-500)", fontSize: "0.875rem", marginBottom: "1rem" } }, "No cash drawer is currently open."), /* @__PURE__ */ React.createElement("div", { className: "form-group", style: { maxWidth: "240px" } }, /* @__PURE__ */ React.createElement("label", null, "Opening Balance"), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: "0.75rem", top: "50%", transform: "translateY(-50%)", color: "var(--stone-400)" } }, "$"), /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "0",
          step: "0.01",
          value: openingBalance,
          onChange: (e) => setOpeningBalance(e.target.value),
          style: { width: "100%", padding: "0.75rem 1rem 0.75rem 1.5rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem" }
        }
      ))), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: openDrawer, disabled: opening }, opening ? "Opening..." : "Open Cash Drawer")), history2.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "form-card", style: { marginTop: "1.5rem" } }, /* @__PURE__ */ React.createElement("h3", null, "Recent History"), /* @__PURE__ */ React.createElement("div", { className: "table-scroll-wrapper" }, /* @__PURE__ */ React.createElement("table", { className: "data-table" }, /* @__PURE__ */ React.createElement("thead", null, /* @__PURE__ */ React.createElement("tr", null, /* @__PURE__ */ React.createElement("th", null, "Date"), /* @__PURE__ */ React.createElement("th", null, "Opening"), /* @__PURE__ */ React.createElement("th", null, "Expected"), /* @__PURE__ */ React.createElement("th", null, "Actual"), /* @__PURE__ */ React.createElement("th", null, "Over/Short"))), /* @__PURE__ */ React.createElement("tbody", null, history2.map((h) => {
        const os = parseFloat(h.over_short || 0);
        return /* @__PURE__ */ React.createElement("tr", { key: h.id }, /* @__PURE__ */ React.createElement("td", { "data-label": "Date" }, fmtDate(h.closed_at)), /* @__PURE__ */ React.createElement("td", { "data-label": "Opening" }, fmtMoney(h.opening_balance)), /* @__PURE__ */ React.createElement("td", { "data-label": "Expected" }, fmtMoney(h.expected_balance)), /* @__PURE__ */ React.createElement("td", { "data-label": "Actual" }, fmtMoney(h.actual_balance)), /* @__PURE__ */ React.createElement("td", { "data-label": "Over/Short", style: { color: os === 0 ? "var(--stone-500)" : os > 0 ? "#2563eb" : "#dc2626", fontWeight: 500 } }, os === 0 ? "Exact" : (os > 0 ? "+" : "-") + "$" + Math.abs(os).toFixed(2)));
      }))))))
    ) : (
      /* ===== Drawer Open State ===== */
      /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "form-card" }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))", gap: "1rem", marginBottom: "1.5rem" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", textTransform: "uppercase", color: "var(--stone-400)", marginBottom: "0.25rem" } }, "Opening Balance"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1.25rem", fontWeight: 600 } }, fmtMoney(drawer.opening_balance))), cashSales > 0 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", textTransform: "uppercase", color: "var(--stone-400)", marginBottom: "0.25rem" } }, "Cash Sales"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1.25rem", fontWeight: 600, color: "#16a34a" } }, "+", fmtMoney(cashSales))), cashRefunds > 0 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", textTransform: "uppercase", color: "var(--stone-400)", marginBottom: "0.25rem" } }, "Refunds"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1.25rem", fontWeight: 600, color: "#dc2626" } }, "-", fmtMoney(cashRefunds))), cashIn > 0 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", textTransform: "uppercase", color: "var(--stone-400)", marginBottom: "0.25rem" } }, "Cash In"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1.25rem", fontWeight: 600, color: "#16a34a" } }, "+", fmtMoney(cashIn))), cashOut > 0 && /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", textTransform: "uppercase", color: "var(--stone-400)", marginBottom: "0.25rem" } }, "Cash Out"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1.25rem", fontWeight: 600, color: "#dc2626" } }, "-", fmtMoney(cashOut))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.75rem", textTransform: "uppercase", color: "var(--stone-400)", marginBottom: "0.25rem" } }, "Expected Balance"), /* @__PURE__ */ React.createElement("div", { style: { fontSize: "1.25rem", fontWeight: 700 } }, fmtMoney(drawer.expected_balance)))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.75rem", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => {
        setShowTxnModal("cash_in");
        setTxnAmount("");
        setTxnDescription("");
      } }, "+ Cash In"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => {
        setShowTxnModal("cash_out");
        setTxnAmount("");
        setTxnDescription("");
      } }, "- Cash Out"), /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "btn btn-primary",
          style: { marginLeft: "auto" },
          onClick: () => {
            setShowCloseModal(true);
            setActualBalance("");
            setCloseNotes("");
          }
        },
        "Close Drawer"
      ))), transactions.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "form-card", style: { marginTop: "1.5rem" } }, /* @__PURE__ */ React.createElement("h3", null, "Today's Transactions"), /* @__PURE__ */ React.createElement("div", { className: "table-scroll-wrapper" }, /* @__PURE__ */ React.createElement("table", { className: "data-table" }, /* @__PURE__ */ React.createElement("thead", null, /* @__PURE__ */ React.createElement("tr", null, /* @__PURE__ */ React.createElement("th", null, "Time"), /* @__PURE__ */ React.createElement("th", null, "Type"), /* @__PURE__ */ React.createElement("th", null, "Description"), /* @__PURE__ */ React.createElement("th", { style: { textAlign: "right" } }, "Amount"))), /* @__PURE__ */ React.createElement("tbody", null, transactions.map((t) => {
        const isCredit = ["sale", "cash_in"].includes(t.type);
        const typeLabels = { sale: "Sale", refund: "Refund", cash_in: "Cash In", cash_out: "Cash Out" };
        return /* @__PURE__ */ React.createElement("tr", { key: t.id }, /* @__PURE__ */ React.createElement("td", { "data-label": "Time" }, fmtTime(t.created_at)), /* @__PURE__ */ React.createElement("td", { "data-label": "Type" }, /* @__PURE__ */ React.createElement("span", { className: "badge badge-" + (isCredit ? "confirmed" : "cancelled"), style: { fontSize: "0.75rem" } }, typeLabels[t.type] || t.type)), /* @__PURE__ */ React.createElement("td", { "data-label": "Description", style: { fontSize: "0.8125rem", color: "var(--stone-600)" } }, t.description || "\u2014"), /* @__PURE__ */ React.createElement("td", { "data-label": "Amount", style: { textAlign: "right", fontWeight: 500, color: isCredit ? "#16a34a" : "#dc2626" } }, isCredit ? "+" : "-", fmtMoney(t.amount)));
      }))))))
    ), showTxnModal && /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => setShowTxnModal(null) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("h3", null, showTxnModal === "cash_in" ? "Cash In" : "Cash Out"), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Amount"), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: "0.75rem", top: "50%", transform: "translateY(-50%)", color: "var(--stone-400)" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "number",
        min: "0.01",
        step: "0.01",
        value: txnAmount,
        onChange: (e) => setTxnAmount(e.target.value),
        placeholder: "0.00",
        autoFocus: true,
        style: { width: "100%", padding: "0.75rem 1rem 0.75rem 1.5rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem" }
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Reason"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: txnDescription,
        onChange: (e) => setTxnDescription(e.target.value),
        placeholder: showTxnModal === "cash_in" ? "e.g. Change added" : "e.g. Bank deposit",
        style: { width: "100%", padding: "0.75rem 1rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem" }
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "modal-actions" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowTxnModal(null) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", onClick: recordTxn, disabled: txnSaving || !txnAmount || parseFloat(txnAmount) <= 0 }, txnSaving ? "Recording..." : "Record")))), showCloseModal && /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: () => setShowCloseModal(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-box", onClick: (e) => e.stopPropagation() }, /* @__PURE__ */ React.createElement("h3", null, "Close Cash Drawer"), /* @__PURE__ */ React.createElement("div", { style: { marginBottom: "1rem", padding: "0.75rem", background: "var(--stone-50)", borderRadius: "6px" } }, /* @__PURE__ */ React.createElement("span", { style: { fontSize: "0.8125rem", color: "var(--stone-500)" } }, "Expected Balance:"), /* @__PURE__ */ React.createElement("span", { style: { fontSize: "1.1rem", fontWeight: 600, marginLeft: "0.5rem" } }, fmtMoney(drawer.expected_balance))), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Actual Count"), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: "0.75rem", top: "50%", transform: "translateY(-50%)", color: "var(--stone-400)" } }, "$"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "number",
        min: "0",
        step: "0.01",
        value: actualBalance,
        onChange: (e) => setActualBalance(e.target.value),
        placeholder: "0.00",
        autoFocus: true,
        style: { width: "100%", padding: "0.75rem 1rem 0.75rem 1.5rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem" }
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Notes (optional)"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: closeNotes,
        onChange: (e) => setCloseNotes(e.target.value),
        placeholder: "Any notes about the count",
        style: { width: "100%", padding: "0.75rem 1rem", border: "1px solid var(--stone-300)", fontSize: "0.875rem" }
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "modal-actions" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowCloseModal(false) }, "Cancel"), /* @__PURE__ */ React.createElement(
      "button",
      {
        className: "btn btn-primary",
        onClick: () => {
          closeDrawer();
          setShowCloseModal(false);
        },
        disabled: closing || !actualBalance || parseFloat(actualBalance) < 0
      },
      closing ? "Closing..." : "Close & Reconcile"
    )))));
  }
  const TK_VIEWS = [
    { key: "open", label: "Open" },
    { key: "today", label: "Today" },
    { key: "overdue", label: "Overdue" },
    { key: "snoozed", label: "Snoozed" }
  ];
  function RepTasksListView({ navigate, onChanged }) {
    const [tasks, setTasks] = useState([]);
    const [counts, setCounts] = useState(null);
    const [loading, setLoading] = useState(true);
    const [viewKey, setViewKey] = useState("open");
    const [priorityFilter, setPriorityFilter] = useState("");
    const [sourceFilter, setSourceFilter] = useState("");
    const [search, setSearch] = useState("");
    const [showModal, setShowModal] = useState(false);
    const [editingTask, setEditingTask] = useState(null);
    const [snoozeMenuId, setSnoozeMenuId] = useState(null);
    const load = useCallback(() => {
      setLoading(true);
      const params = new URLSearchParams();
      const todayStr = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
      if (viewKey === "open") params.set("status", "open");
      else if (viewKey === "today") {
        params.set("status", "open");
        params.set("due_from", todayStr);
        params.set("due_to", todayStr);
      } else if (viewKey === "overdue") {
        params.set("status", "open");
        params.set("due_to", new Date(Date.now() - 864e5).toISOString().split("T")[0]);
      } else if (viewKey === "snoozed") params.set("snoozed", "true");
      if (priorityFilter) params.set("priority", priorityFilter);
      if (sourceFilter) params.set("source", sourceFilter);
      if (search) params.set("search", search);
      repFetch("/api/rep/tasks?" + params.toString()).then((d) => {
        setTasks(d.tasks || []);
        setCounts(d.counts || null);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [viewKey, priorityFilter, sourceFilter, search]);
    useEffect(load, [load]);
    useEffect(() => {
      if (!snoozeMenuId) return;
      const handler = () => setSnoozeMenuId(null);
      document.addEventListener("click", handler);
      return () => document.removeEventListener("click", handler);
    }, [snoozeMenuId]);
    const [showCompleted, setShowCompleted] = useState(false);
    const [completedTasks, setCompletedTasks] = useState([]);
    const [completedLoading, setCompletedLoading] = useState(false);
    const loadCompleted = useCallback(() => {
      setCompletedLoading(true);
      repFetch("/api/rep/tasks?status=completed").then((d) => {
        setCompletedTasks(d.tasks || []);
        setCompletedLoading(false);
      }).catch(() => setCompletedLoading(false));
    }, []);
    useEffect(() => {
      if (showCompleted) loadCompleted();
    }, [showCompleted, loadCompleted]);
    const afterChange = () => {
      load();
      if (showCompleted) loadCompleted();
      if (onChanged) onChanged();
    };
    const [confirmingId, setConfirmingId] = useState(null);
    const confirmTimerRef = useRef(null);
    useEffect(() => () => {
      if (confirmTimerRef.current) clearTimeout(confirmTimerRef.current);
    }, []);
    const toggleTask = async (t) => {
      if (t.status === "completed") {
        await repFetch("/api/rep/tasks/" + t.id + "/reopen", { method: "PUT" });
        afterChange();
        return;
      }
      if (confirmingId !== t.id) {
        if (confirmTimerRef.current) clearTimeout(confirmTimerRef.current);
        setConfirmingId(t.id);
        confirmTimerRef.current = setTimeout(() => setConfirmingId(null), 2500);
        return;
      }
      clearTimeout(confirmTimerRef.current);
      setConfirmingId(null);
      await repFetch("/api/rep/tasks/" + t.id + "/complete", { method: "PUT" });
      afterChange();
    };
    const deleteTask = async (id) => {
      if (!confirm("Delete this task?")) return;
      await repFetch("/api/rep/tasks/" + id, { method: "DELETE" });
      afterChange();
    };
    const dismissTask = async (id) => {
      await repFetch("/api/rep/tasks/" + id + "/dismiss", { method: "PUT" });
      afterChange();
    };
    const snoozeTask = async (id, days) => {
      const dateStr = new Date(Date.now() + days * 864e5).toISOString().split("T")[0];
      await repFetch("/api/rep/tasks/" + id + "/snooze", { method: "PUT", body: JSON.stringify({ snooze_until: dateStr }) });
      setSnoozeMenuId(null);
      afterChange();
    };
    const wakeTask = async (id) => {
      await repFetch("/api/rep/tasks/" + id + "/snooze", { method: "PUT", body: JSON.stringify({ snooze_until: null }) });
      afterChange();
    };
    const dueMeta = (dateStr) => {
      if (!dateStr) return null;
      const d = /* @__PURE__ */ new Date(String(dateStr).split("T")[0] + "T00:00:00");
      const today = /* @__PURE__ */ new Date();
      today.setHours(0, 0, 0, 0);
      const diff = Math.round((d - today) / 864e5);
      if (diff < 0) return { text: Math.abs(diff) + "d overdue", tone: "#c54731", group: "Overdue" };
      if (diff === 0) return { text: "Today", tone: "#a87935", group: "Today" };
      if (diff === 1) return { text: "Tomorrow", tone: "#3a7a4e", group: "Tomorrow" };
      if (diff < 7) return { text: d.toLocaleDateString("en-US", { weekday: "short" }), tone: "#8a7e68", group: "This week" };
      return { text: d.toLocaleDateString("en-US", { month: "short", day: "numeric" }), tone: "#8a7e68", group: "Later" };
    };
    const fmtCompletedAt = (ts) => ts ? new Date(ts).toLocaleDateString("en-US", { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" }) : "";
    const fmtWake = (dateStr) => (/* @__PURE__ */ new Date(String(dateStr).split("T")[0] + "T00:00:00")).toLocaleDateString("en-US", { month: "short", day: "numeric" });
    const groups = [];
    if (viewKey === "open") {
      tasks.forEach((t) => {
        const label = t.due_date ? dueMeta(t.due_date).group : "No due date";
        let g = groups[groups.length - 1];
        if (!g || g.label !== label) {
          g = { label, items: [] };
          groups.push(g);
        }
        g.items.push(t);
      });
    } else {
      groups.push({ label: null, items: tasks });
    }
    const chipCount = (k) => {
      if (!counts) return null;
      return { open: counts.open, today: counts.due_today, overdue: counts.overdue, snoozed: counts.snoozed }[k];
    };
    const emptyText = {
      open: "No open tasks \u2014 you're all caught up.",
      today: "Nothing due today.",
      overdue: "Nothing overdue. Nice.",
      snoozed: "No snoozed tasks."
    }[viewKey];
    const renderRow = (t) => {
      const isDone = t.status === "completed";
      const isAuto = t.source === "auto";
      const due = dueMeta(t.due_date);
      return /* @__PURE__ */ React.createElement("div", { className: "tk-row", key: t.id }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "tk-check" + (isDone ? " done" : "") + (confirmingId === t.id ? " arming" : ""),
          onClick: () => toggleTask(t),
          title: isDone ? "Reopen" : confirmingId === t.id ? "Click again to complete" : "Mark complete"
        },
        isDone || confirmingId === t.id ? "\u2713" : ""
      ), confirmingId === t.id && !isDone && /* @__PURE__ */ React.createElement("span", { className: "tk-check-hint" }, "Again to complete")), /* @__PURE__ */ React.createElement("div", { className: "tk-priority-bar " + t.priority, title: t.priority + " priority" }), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement(
        "div",
        {
          className: "tk-title" + (isDone ? " done" : "") + (!isAuto && !isDone ? " editable" : ""),
          onClick: () => {
            if (!isAuto && !isDone) {
              setEditingTask(t);
              setShowModal(true);
            }
          }
        },
        t.title,
        isAuto && /* @__PURE__ */ React.createElement("span", { className: "tk-auto-chip" }, "Auto")
      ), t.description && /* @__PURE__ */ React.createElement("div", { className: "tk-desc" }, t.description), (t.customer_name || t.customer_email || t.customer_phone) && /* @__PURE__ */ React.createElement("div", { className: "tk-contact" }, t.customer_name && /* @__PURE__ */ React.createElement("span", null, t.customer_name), t.customer_email && /* @__PURE__ */ React.createElement("a", { href: "mailto:" + t.customer_email, onClick: (e) => e.stopPropagation() }, t.customer_email), t.customer_phone && /* @__PURE__ */ React.createElement("a", { href: "tel:" + t.customer_phone, onClick: (e) => e.stopPropagation() }, t.customer_phone))), /* @__PURE__ */ React.createElement("div", { className: "tk-row-right" }, t.linked_order_number && /* @__PURE__ */ React.createElement("span", { className: "tk-ref", onClick: () => navigate("order-detail", t.linked_order_id) }, t.linked_order_number), t.linked_quote_number && /* @__PURE__ */ React.createElement("span", { className: "tk-ref", onClick: () => navigate("quote-edit", t.linked_quote_id) }, t.linked_quote_number), t.linked_estimate_number && /* @__PURE__ */ React.createElement("span", { className: "tk-ref", onClick: () => navigate("estimate-edit", t.linked_estimate_id) }, t.linked_estimate_number), t.linked_deal_title && /* @__PURE__ */ React.createElement("span", { className: "tk-ref", style: { cursor: "default" } }, t.linked_deal_title), t.linked_sample_request_number && /* @__PURE__ */ React.createElement("span", { className: "tk-ref", onClick: () => navigate("sample-detail", t.linked_sample_request_id) }, t.linked_sample_request_number), viewKey === "snoozed" && t.snoozed_until && /* @__PURE__ */ React.createElement("span", { className: "tk-due", style: { color: "#8a7e68", background: "rgba(28,25,23,0.06)" } }, "Wakes ", fmtWake(t.snoozed_until)), !isDone && viewKey !== "snoozed" && due && /* @__PURE__ */ React.createElement("span", { className: "tk-due", style: { color: due.tone, background: due.tone + "1a" } }, due.text), isDone && t.completed_at && /* @__PURE__ */ React.createElement("span", { className: "tk-completed-at" }, "Done ", fmtCompletedAt(t.completed_at)), viewKey === "snoozed" && /* @__PURE__ */ React.createElement(
        "button",
        {
          className: "tk-icon-btn",
          style: { width: "auto", padding: "0 10px", font: "500 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase" },
          onClick: () => wakeTask(t.id)
        },
        "Wake now"
      ), !isDone && viewKey !== "snoozed" && /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("button", { className: "tk-icon-btn", title: "Snooze", onClick: (e) => {
        e.stopPropagation();
        setSnoozeMenuId(snoozeMenuId === t.id ? null : t.id);
      } }, /* @__PURE__ */ React.createElement("svg", { width: "13", height: "13", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2" }, /* @__PURE__ */ React.createElement("circle", { cx: "12", cy: "12", r: "10" }), /* @__PURE__ */ React.createElement("path", { d: "M12 6v6l4 2" }))), snoozeMenuId === t.id && /* @__PURE__ */ React.createElement("div", { className: "tk-menu", onClick: (e) => e.stopPropagation() }, [{ label: "Tomorrow", days: 1 }, { label: "In 3 days", days: 3 }, { label: "Next week", days: 7 }, { label: "In 2 weeks", days: 14 }].map((opt) => /* @__PURE__ */ React.createElement("button", { key: opt.days, onClick: () => snoozeTask(t.id, opt.days) }, opt.label)))), !isDone && (isAuto ? /* @__PURE__ */ React.createElement("button", { className: "tk-icon-btn", title: "Dismiss", onClick: () => dismissTask(t.id) }, /* @__PURE__ */ React.createElement("svg", { width: "13", height: "13", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2" }, /* @__PURE__ */ React.createElement("path", { d: "M18 6L6 18M6 6l12 12" }))) : /* @__PURE__ */ React.createElement("button", { className: "tk-icon-btn", title: "Delete", onClick: () => deleteTask(t.id) }, /* @__PURE__ */ React.createElement("svg", { width: "13", height: "13", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2" }, /* @__PURE__ */ React.createElement("path", { d: "M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" }))))));
    };
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25C7", " Tasks", counts ? " \xB7 " + counts.open + " open" : "", counts && counts.overdue > 0 && /* @__PURE__ */ React.createElement("span", { style: { color: "var(--rod-red)" } }, " \xB7 " + counts.overdue + " overdue")), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Tasks")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.5 'Inter', sans-serif", color: "rgba(28,25,23,0.7)", marginTop: 6 } }, (/* @__PURE__ */ new Date()).toLocaleDateString("en-US", { weekday: "long", month: "long", day: "numeric" }), " ", "\xB7", " follow-ups and to-dos, sorted by urgency")), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: () => {
      setEditingTask(null);
      setShowModal(true);
    } }, "+ New task"))), /* @__PURE__ */ React.createElement("div", { className: "nin-filters" }, /* @__PURE__ */ React.createElement("div", { className: "nin-filter-chips" }, TK_VIEWS.map((v) => /* @__PURE__ */ React.createElement("button", { key: v.key, className: "nin-chip" + (viewKey === v.key ? " active" : ""), onClick: () => setViewKey(v.key) }, v.label, chipCount(v.key) != null && /* @__PURE__ */ React.createElement("span", { className: "nin-chip-count" }, chipCount(v.key))))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("select", { className: "rod-input", style: { width: "auto", padding: "6px 8px" }, value: priorityFilter, onChange: (e) => setPriorityFilter(e.target.value) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "All priorities"), /* @__PURE__ */ React.createElement("option", { value: "high" }, "High"), /* @__PURE__ */ React.createElement("option", { value: "medium" }, "Medium"), /* @__PURE__ */ React.createElement("option", { value: "low" }, "Low")), /* @__PURE__ */ React.createElement("select", { className: "rod-input", style: { width: "auto", padding: "6px 8px" }, value: sourceFilter, onChange: (e) => setSourceFilter(e.target.value) }, /* @__PURE__ */ React.createElement("option", { value: "" }, "All sources"), /* @__PURE__ */ React.createElement("option", { value: "auto" }, "Auto"), /* @__PURE__ */ React.createElement("option", { value: "manual" }, "Manual")), /* @__PURE__ */ React.createElement("input", { className: "ol-search-inline", style: { paddingLeft: 12, minWidth: 170 }, placeholder: "Search tasks\u2026", value: search, onChange: (e) => setSearch(e.target.value) }))), loading ? /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading tasks\u2026") : tasks.length === 0 ? /* @__PURE__ */ React.createElement("div", { className: "nin-list" }, /* @__PURE__ */ React.createElement("div", { className: "nin-empty-state" }, /* @__PURE__ */ React.createElement("div", { className: "nin-empty-glyph" }, "\u25C7"), /* @__PURE__ */ React.createElement("div", { className: "nin-empty-text" }, emptyText))) : /* @__PURE__ */ React.createElement("div", { className: "nin-list" }, groups.map((g, gi) => /* @__PURE__ */ React.createElement("div", { key: g.label || gi }, g.label && /* @__PURE__ */ React.createElement("div", { className: "nin-group-label", style: g.label === "Overdue" ? { color: "var(--rod-red)" } : void 0 }, g.label), g.items.map(renderRow)))), viewKey === "open" && counts && counts.completed > 0 && /* @__PURE__ */ React.createElement("div", { className: "nin-list" }, /* @__PURE__ */ React.createElement("button", { className: "tk-completed-toggle", onClick: () => setShowCompleted((s) => !s) }, /* @__PURE__ */ React.createElement("span", null, /* @__PURE__ */ React.createElement("span", { style: { display: "inline-block", transform: showCompleted ? "rotate(90deg)" : "none", transition: "transform 0.15s", marginRight: 8 } }, "\u25B8"), "Completed this week"), /* @__PURE__ */ React.createElement("span", { className: "nin-chip-count" }, counts.completed)), showCompleted && (completedLoading ? /* @__PURE__ */ React.createElement("div", { className: "nin-empty" }, "Loading\u2026") : completedTasks.map(renderRow))), showModal && /* @__PURE__ */ React.createElement(TaskCreateEditModal, { task: editingTask, onClose: () => {
      setShowModal(false);
      setEditingTask(null);
    }, onSaved: afterChange, navigate }));
  }
  function TaskCreateEditModal({ task, onClose, onSaved, navigate, prefill }) {
    const [title, setTitle] = useState(task ? task.title : prefill && prefill.title || "");
    const [description, setDescription] = useState(task ? task.description || "" : "");
    const [dueDate, setDueDate] = useState(task && task.due_date ? String(task.due_date).split("T")[0] : "");
    const [priority, setPriority] = useState(task ? task.priority : "medium");
    const [linkedOrderId] = useState(task ? task.linked_order_id : prefill && prefill.linked_order_id || null);
    const [linkedQuoteId] = useState(task ? task.linked_quote_id : prefill && prefill.linked_quote_id || null);
    const [linkedEstimateId] = useState(task ? task.linked_estimate_id : prefill && prefill.linked_estimate_id || null);
    const [linkedDealId] = useState(task ? task.linked_deal_id : prefill && prefill.linked_deal_id || null);
    const [linkedSampleRequestId] = useState(task ? task.linked_sample_request_id : prefill && prefill.linked_sample_request_id || null);
    const [linkedCustomerType] = useState(task ? task.linked_customer_type : prefill && prefill.linked_customer_type || null);
    const [linkedCustomerRef] = useState(task ? task.linked_customer_ref : prefill && prefill.linked_customer_ref || null);
    const [customerName] = useState(task ? task.customer_name : prefill && prefill.customer_name || null);
    const [customerEmail] = useState(task ? task.customer_email : prefill && prefill.customer_email || null);
    const [customerPhone] = useState(task ? task.customer_phone : prefill && prefill.customer_phone || null);
    const [saving, setSaving] = useState(false);
    const contextLabel = prefill && prefill.context_label || task && (task.linked_order_number || task.linked_quote_number || task.linked_estimate_number || task.linked_deal_title || task.linked_sample_request_number ? [task.linked_order_number, task.linked_quote_number, task.linked_estimate_number, task.linked_deal_title, task.linked_sample_request_number].filter(Boolean).join(" \xB7 ") : null) || customerName || null;
    const setQuickDate = (days) => setDueDate(new Date(Date.now() + days * 864e5).toISOString().split("T")[0]);
    const quickDateMatch = (days) => !!dueDate && dueDate === new Date(Date.now() + days * 864e5).toISOString().split("T")[0];
    const priorities = [
      { key: "low", label: "Low", tone: "#8a7e68" },
      { key: "medium", label: "Medium", tone: "#a87935" },
      { key: "high", label: "High", tone: "#c54731" }
    ];
    const save = async () => {
      if (!title.trim() || saving) return;
      setSaving(true);
      const body = {
        title: title.trim(),
        description: description || null,
        due_date: dueDate || null,
        priority,
        customer_name: customerName,
        customer_email: customerEmail,
        customer_phone: customerPhone,
        linked_customer_type: linkedCustomerType,
        linked_customer_ref: linkedCustomerRef,
        linked_order_id: linkedOrderId,
        linked_quote_id: linkedQuoteId,
        linked_estimate_id: linkedEstimateId,
        linked_deal_id: linkedDealId,
        linked_sample_request_id: linkedSampleRequestId
      };
      try {
        if (task) await repFetch("/api/rep/tasks/" + task.id, { method: "PUT", body: JSON.stringify(body) });
        else await repFetch("/api/rep/tasks", { method: "POST", body: JSON.stringify(body) });
        onSaved();
        onClose();
      } catch (e) {
      }
      setSaving(false);
    };
    return /* @__PURE__ */ React.createElement("div", { className: "modal-overlay", onClick: (e) => {
      if (e.target === e.currentTarget) onClose();
    } }, /* @__PURE__ */ React.createElement("div", { className: "tk-modal" }, /* @__PURE__ */ React.createElement("div", { className: "tk-modal-header" }, /* @__PURE__ */ React.createElement("span", { className: "tk-modal-title" }, task ? "Edit task" : "New task"), /* @__PURE__ */ React.createElement("button", { className: "tk-modal-close", onClick: onClose }, "\xD7")), /* @__PURE__ */ React.createElement("div", { className: "tk-modal-body" }, contextLabel && /* @__PURE__ */ React.createElement("div", { style: {
      display: "flex",
      alignItems: "center",
      gap: 8,
      padding: "8px 12px",
      marginBottom: 14,
      background: "rgba(168,121,53,0.08)",
      border: "1px solid rgba(168,121,53,0.2)",
      borderRadius: 6,
      font: "400 12px/1.3 'Inter', sans-serif",
      color: "#8a6d2f"
    } }, /* @__PURE__ */ React.createElement("span", { style: { font: "600 9px/1 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", opacity: 0.7 } }, "For"), /* @__PURE__ */ React.createElement("span", { style: { fontWeight: 500 } }, contextLabel)), /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Task"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "rod-input",
        value: title,
        onChange: (e) => setTitle(e.target.value),
        autoFocus: true,
        placeholder: "What needs to be done?",
        onKeyDown: (e) => {
          if (e.key === "Enter" && !e.shiftKey) {
            e.preventDefault();
            save();
          }
        }
      }
    ), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14 } }, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Notes"), /* @__PURE__ */ React.createElement("textarea", { className: "rod-input", rows: "2", value: description, onChange: (e) => setDescription(e.target.value), placeholder: "Details or context (optional)" })), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14 } }, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Priority"), /* @__PURE__ */ React.createElement("div", { className: "tk-seg" }, priorities.map((p) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: p.key,
        onClick: () => setPriority(p.key),
        style: priority === p.key ? { borderColor: p.tone, color: p.tone, background: p.tone + "14" } : void 0
      },
      /* @__PURE__ */ React.createElement("span", { className: "tk-seg-dot", style: { background: p.tone, opacity: priority === p.key ? 1 : 0.35 } }),
      p.label
    )))), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 14 } }, /* @__PURE__ */ React.createElement("label", { className: "rod-field-label" }, "Due date"), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, flexWrap: "wrap", marginBottom: 8 } }, [{ label: "Today", days: 0 }, { label: "Tomorrow", days: 1 }, { label: "In 3 days", days: 3 }, { label: "Next week", days: 7 }].map((q) => /* @__PURE__ */ React.createElement("button", { key: q.days, className: "nin-chip" + (quickDateMatch(q.days) ? " active" : ""), onClick: () => setQuickDate(q.days) }, q.label)), dueDate && /* @__PURE__ */ React.createElement("button", { className: "nin-chip", onClick: () => setDueDate("") }, "Clear")), /* @__PURE__ */ React.createElement("input", { type: "date", className: "rod-input", value: dueDate, onChange: (e) => setDueDate(e.target.value) }))), /* @__PURE__ */ React.createElement("div", { className: "tk-modal-actions" }, /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn", onClick: onClose }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "rod-mini-btn primary", onClick: save, disabled: saving || !title.trim() }, saving ? "Saving\u2026" : task ? "Save changes" : "Create task"))));
  }
  function AddTaskButton({ prefill, navigate, onSaved, label = "+ Follow-up task", className = "rod-mini-btn", style }) {
    const [open, setOpen] = useState(false);
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("button", { className, style, onClick: () => setOpen(true) }, label), open && /* @__PURE__ */ React.createElement(
      TaskCreateEditModal,
      {
        prefill,
        navigate,
        onClose: () => setOpen(false),
        onSaved: () => {
          if (onSaved) onSaved();
        }
      }
    ));
  }
  function RepPurchaseOrdersListView({ navigate }) {
    const [pos, setPOs] = useState([]);
    const [loading, setLoading] = useState(true);
    const [statusFilter, setStatusFilter] = useState("");
    const [vendorFilter, setVendorFilter] = useState("");
    const [search, setSearch] = useState("");
    const [vendors, setVendors] = useState([]);
    const [showCreate, setShowCreate] = useState(false);
    const [newVendorId, setNewVendorId] = useState("");
    const [newNotes, setNewNotes] = useState("");
    const [newShipTo, setNewShipTo] = useState("Roma Anaheim Warehouse\n1440 S. State College Blvd #6M\nAnaheim, CA 92806");
    const [newExpectedDelivery, setNewExpectedDelivery] = useState("");
    const [newRecipientEmail, setNewRecipientEmail] = useState("");
    const [newCcEmails, setNewCcEmails] = useState("");
    const [creating, setCreating] = useState(false);
    const [tab, setTab] = useState("order");
    const load = useCallback(() => {
      setLoading(true);
      const params = new URLSearchParams();
      if (statusFilter) params.set("status", statusFilter);
      if (vendorFilter) params.set("vendor_id", vendorFilter);
      if (search) params.set("search", search);
      repFetch("/api/rep/purchase-orders?" + params.toString()).then((data) => {
        setPOs(data.purchase_orders || []);
        setLoading(false);
      }).catch(() => setLoading(false));
    }, [statusFilter, vendorFilter, search]);
    useEffect(load, [load]);
    useEffect(() => {
      repFetch("/api/rep/vendors").then((data) => setVendors(data.vendors || [])).catch(() => {
      });
    }, []);
    const createPO = async () => {
      if (!newVendorId) return;
      setCreating(true);
      try {
        const data = await repFetch("/api/rep/purchase-orders", {
          method: "POST",
          body: JSON.stringify({
            vendor_id: newVendorId,
            notes: newNotes || null,
            ship_to: newShipTo || null,
            expected_delivery: newExpectedDelivery || null,
            recipient_email: newRecipientEmail || null,
            cc_emails: newCcEmails ? newCcEmails.split(",").map((e) => e.trim()).filter(Boolean) : null
          })
        });
        if (data.error) throw new Error(data.error);
        setShowCreate(false);
        setNewVendorId("");
        setNewNotes("");
        setNewShipTo("Roma Anaheim Warehouse\n1440 S. State College Blvd #6M\nAnaheim, CA 92806");
        setNewExpectedDelivery("");
        setNewRecipientEmail("");
        setNewCcEmails("");
        navigate("purchase-order-detail", data.purchase_order.id);
      } catch (err) {
        alert(err.message);
      }
      setCreating(false);
    };
    const getStatusTone = (s) => {
      if (s === "sent") return "#5c6e94";
      if (s === "acknowledged") return "#a87935";
      if (s === "fulfilled") return "#3a7a4e";
      if (s === "cancelled") return "#c54731";
      return "var(--brass-muted)";
    };
    const getAge = (d) => {
      if (!d) return "\u2014";
      const days = Math.floor((Date.now() - new Date(d).getTime()) / 864e5);
      if (days === 0) return "today";
      return days + "d";
    };
    const filteredPOs = pos.filter((po) => tab === "order" ? po.order_id != null : po.order_id == null);
    const orderCount = pos.filter((po) => po.order_id != null).length;
    const openPO = (po) => {
      if (tab === "order") {
        navigate("order-detail", po.order_id);
        return;
      }
      repListNav["purchase-orders"] = filteredPOs.map((x) => x.id);
      navigate("purchase-order-detail", po.id);
    };
    const viewSubtotal = filteredPOs.reduce((sum, po) => sum + parseFloat(po.subtotal || 0), 0);
    const VIEWS = [
      { key: "order", label: "Order POs", count: orderCount },
      { key: "standalone", label: "Standalone", count: pos.length - orderCount }
    ];
    return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header" }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ol-header-label" }, "\u25CF", " Purchase orders ", "\xB7", " ", filteredPOs.length, " showing ", "\xB7", " ", pos.length, " total"), /* @__PURE__ */ React.createElement("h1", null, /* @__PURE__ */ React.createElement("em", null, "Purchase orders"))), /* @__PURE__ */ React.createElement("div", { className: "ol-header-actions" }, tab === "standalone" && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn primary", onClick: () => setShowCreate(true) }, "+ New purchase order"))), /* @__PURE__ */ React.createElement("div", { className: "ol-saved-views" }, VIEWS.map((v) => /* @__PURE__ */ React.createElement("button", { key: v.key, className: "ol-view-tab" + (tab === v.key ? " active" : ""), onClick: () => setTab(v.key) }, v.label, /* @__PURE__ */ React.createElement("span", { className: "ol-tab-count" }, v.count)))), /* @__PURE__ */ React.createElement("div", { className: "ol-filter-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ol-filter-chips" }, /* @__PURE__ */ React.createElement(
      "select",
      {
        className: "ol-chip",
        value: statusFilter,
        onChange: (e) => setStatusFilter(e.target.value),
        style: { appearance: "auto", paddingRight: 24 }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, "Status: Any"),
      ["draft", "sent", "acknowledged", "fulfilled", "cancelled"].map((s) => /* @__PURE__ */ React.createElement("option", { key: s, value: s }, s.charAt(0).toUpperCase() + s.slice(1)))
    ), statusFilter && /* @__PURE__ */ React.createElement("span", { className: "ol-chip active", onClick: () => setStatusFilter("") }, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-label" }, "Status:"), " ", statusFilter, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-remove" }, "\xD7")), /* @__PURE__ */ React.createElement(
      "select",
      {
        className: "ol-chip",
        value: vendorFilter,
        onChange: (e) => setVendorFilter(e.target.value),
        style: { appearance: "auto", paddingRight: 24 }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, "Vendor: Any"),
      vendors.map((v) => /* @__PURE__ */ React.createElement("option", { key: v.id, value: v.id }, v.name))
    ), vendorFilter && /* @__PURE__ */ React.createElement("span", { className: "ol-chip active", onClick: () => setVendorFilter("") }, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-label" }, "Vendor:"), " ", (vendors.find((v) => String(v.id) === String(vendorFilter)) || {}).name || vendorFilter, /* @__PURE__ */ React.createElement("span", { className: "ol-chip-remove" }, "\xD7"))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 8, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: 10, top: 7, font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)" } }, "\u2315"), /* @__PURE__ */ React.createElement(
      "input",
      {
        className: "ol-search-inline",
        placeholder: "Search PO#, vendor, order#",
        "aria-label": "Search purchase orders",
        value: search,
        onChange: (e) => setSearch(e.target.value)
      }
    )), /* @__PURE__ */ React.createElement("span", { className: "ol-meta" }, filteredPOs.length, " rows"))), loading ? /* @__PURE__ */ React.createElement("div", { className: "gs-loading", style: { padding: "40px 22px", background: "#ece5d8", border: "0.5px solid rgba(28,25,23,0.14)" } }, "Loading purchase orders", "\u2026") : /* @__PURE__ */ React.createElement("div", { className: "pl-table-wrap" }, /* @__PURE__ */ React.createElement("div", { className: "pl-table-head" + (tab === "standalone" ? " standalone" : "") }, /* @__PURE__ */ React.createElement("span", null, "PO #"), /* @__PURE__ */ React.createElement("span", null, "Vendor"), tab === "order" && /* @__PURE__ */ React.createElement("span", null, "Order #"), /* @__PURE__ */ React.createElement("span", null, "Items"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Subtotal"), /* @__PURE__ */ React.createElement("span", null, "Status"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Age"), /* @__PURE__ */ React.createElement("span", null)), filteredPOs.map((po) => /* @__PURE__ */ React.createElement(
      "div",
      {
        key: po.id,
        className: "pl-table-row" + (tab === "standalone" ? " standalone" : ""),
        role: "button",
        tabIndex: 0,
        onClick: () => openPO(po),
        onKeyDown: (e) => {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            openPO(po);
          }
        }
      },
      /* @__PURE__ */ React.createElement("span", { className: "pl-po-id" }, po.po_number),
      /* @__PURE__ */ React.createElement("span", { className: "pl-vendor-name" }, po.vendor_name, po.vendor_public_code ? /* @__PURE__ */ React.createElement("span", { style: { color: "var(--brass-muted)", fontWeight: 400 } }, " \xB7 ", po.vendor_public_code) : null),
      tab === "order" && /* @__PURE__ */ React.createElement("span", { className: "pl-order-ref" }, po.order_number),
      /* @__PURE__ */ React.createElement("span", { className: "pl-items" }, po.item_count, " item", po.item_count !== 1 ? "s" : ""),
      /* @__PURE__ */ React.createElement("span", { className: "pl-total" }, "$", parseFloat(po.subtotal || 0).toLocaleString(void 0, { minimumFractionDigits: 2, maximumFractionDigits: 2 })),
      /* @__PURE__ */ React.createElement("span", { className: "pl-status", style: { color: getStatusTone(po.status) } }, "\u25CF", " ", po.status),
      /* @__PURE__ */ React.createElement("span", { className: "pl-age" }, getAge(po.created_at)),
      /* @__PURE__ */ React.createElement("span", { style: { font: '400 14px/1 "Cormorant Garamond", serif', color: "var(--brass-muted)", textAlign: "right" } }, "\u2192")
    )), filteredPOs.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "40px 22px", textAlign: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { color: "var(--brass-muted)", font: "400 13px/1.4 Inter, sans-serif", marginBottom: tab === "standalone" ? 14 : 0 } }, tab === "order" ? "No order-linked purchase orders match this view." : "No standalone purchase orders match this view."), tab === "standalone" && /* @__PURE__ */ React.createElement("button", { className: "ol-header-btn", onClick: () => setShowCreate(true) }, "+ New purchase order")), filteredPOs.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "pl-footer" }, /* @__PURE__ */ React.createElement("span", null, filteredPOs.length, " purchase order", filteredPOs.length !== 1 ? "s" : ""), /* @__PURE__ */ React.createElement("span", null, "$", viewSubtotal.toLocaleString(void 0, { minimumFractionDigits: 0, maximumFractionDigits: 0 }), " combined subtotal"))), showCreate && /* @__PURE__ */ React.createElement("div", { className: "modal-backdrop", onClick: () => setShowCreate(false) }, /* @__PURE__ */ React.createElement("div", { className: "modal-content", onClick: (e) => e.stopPropagation(), style: { maxWidth: "480px" } }, /* @__PURE__ */ React.createElement("div", { className: "modal-header" }, /* @__PURE__ */ React.createElement("h2", null, "New Purchase Order")), /* @__PURE__ */ React.createElement("div", { className: "modal-body" }, /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Vendor *"), /* @__PURE__ */ React.createElement(
      "select",
      {
        value: newVendorId,
        onChange: (e) => setNewVendorId(e.target.value),
        style: { width: "100%", padding: "0.5rem", border: "1px solid var(--stone-300)", borderRadius: "6px" }
      },
      /* @__PURE__ */ React.createElement("option", { value: "" }, "Select vendor..."),
      vendors.map((v) => /* @__PURE__ */ React.createElement("option", { key: v.id, value: v.id }, v.name, v.public_code ? " \xB7 " + v.public_code : ""))
    )), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Ship To"), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        value: newShipTo,
        onChange: (e) => setNewShipTo(e.target.value),
        placeholder: "Ship-to address...",
        style: { width: "100%", minHeight: "68px", padding: "0.5rem", border: "1px solid var(--stone-300)", borderRadius: "6px", fontFamily: "Inter, sans-serif", resize: "vertical", fontSize: "13px" }
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Expected Delivery"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "date",
        value: newExpectedDelivery,
        onChange: (e) => setNewExpectedDelivery(e.target.value),
        style: { width: "100%", padding: "0.5rem", border: "1px solid var(--stone-300)", borderRadius: "6px", fontFamily: "Inter, sans-serif" }
      }
    )), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: "0.75rem" } }, /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Recipient Email"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        value: newRecipientEmail,
        onChange: (e) => setNewRecipientEmail(e.target.value),
        placeholder: "vendor@example.com",
        style: { width: "100%", padding: "0.5rem", border: "1px solid var(--stone-300)", borderRadius: "6px", fontFamily: "Inter, sans-serif", boxSizing: "border-box" }
      }
    )), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "CC Emails"), /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: newCcEmails,
        onChange: (e) => setNewCcEmails(e.target.value),
        placeholder: "a@co.com, b@co.com",
        style: { width: "100%", padding: "0.5rem", border: "1px solid var(--stone-300)", borderRadius: "6px", fontFamily: "Inter, sans-serif", boxSizing: "border-box" }
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "form-group" }, /* @__PURE__ */ React.createElement("label", null, "Notes"), /* @__PURE__ */ React.createElement(
      "textarea",
      {
        value: newNotes,
        onChange: (e) => setNewNotes(e.target.value),
        placeholder: "Optional notes...",
        style: { width: "100%", minHeight: "80px", padding: "0.5rem", border: "1px solid var(--stone-300)", borderRadius: "6px", fontFamily: "Inter, sans-serif", resize: "vertical" }
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "modal-footer" }, /* @__PURE__ */ React.createElement("button", { className: "btn btn-secondary", onClick: () => setShowCreate(false) }, "Cancel"), /* @__PURE__ */ React.createElement("button", { className: "btn btn-primary", disabled: !newVendorId || creating, onClick: createPO }, creating ? "Creating..." : "Create PO")))));
  }
  const PD_INK = "#1c1917";
  const PD_PAPER = "#ece5d8";
  const PD_ACCENT = "#a87935";
  const PD_MUTED = "#8a7e68";
  const PD_WARM = "#d8cdb6";
  const PD_GREEN = "#3a7a4e";
  const PD_AMBER = "#a87935";
  const PD_RED = "#c54731";
  const PD_BLUE = "#5c6e94";
  const pdMonoLabel = { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: PD_MUTED };
  const pdSerifNum = (size) => ({ fontFamily: "'Cormorant Garamond', serif", fontWeight: 300, fontSize: size, letterSpacing: "-0.01em", lineHeight: 1 });
  function PdCard({ title, action, actionClick, action2, children }) {
    return /* @__PURE__ */ React.createElement("section", { style: { background: PD_PAPER, border: "0.5px solid " + PD_INK + "22" } }, /* @__PURE__ */ React.createElement("header", { style: { padding: "14px 20px", borderBottom: "0.5px solid " + PD_INK + "18", display: "flex", alignItems: "center", justifyContent: "space-between" } }, /* @__PURE__ */ React.createElement("h3", { style: { font: "500 13px/1 Inter, sans-serif", letterSpacing: "0.04em", color: PD_INK, margin: 0 } }, title), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 14 } }, action2 && /* @__PURE__ */ React.createElement("span", { style: { ...pdMonoLabel, fontSize: 11, letterSpacing: "0.12em", cursor: "default" } }, action2), action && /* @__PURE__ */ React.createElement("span", { style: { ...pdMonoLabel, fontSize: 11, letterSpacing: "0.12em", color: PD_ACCENT, cursor: actionClick ? "pointer" : "default" }, onClick: actionClick }, action))), children);
  }
  function RepPurchaseOrderDetailView({ navigate, editId }) {
    const [po, setPO] = useState(null);
    const [items, setItems] = useState([]);
    const [activity, setActivity] = useState([]);
    const [loading, setLoading] = useState(true);
    const [poUpdating, setPoUpdating] = useState(false);
    const [addingItem, setAddingItem] = useState(false);
    const [addItemMode, setAddItemMode] = useState("sku");
    const [addItemSku, setAddItemSku] = useState("");
    const [skuSearchResults, setSkuSearchResults] = useState([]);
    const [skuSearching, setSkuSearching] = useState(false);
    const skuSearchTimer = useRef(null);
    const skuSearchCounter = useRef(0);
    const [newPOItem, setNewPOItem] = useState({ product_name: "", vendor_sku: "", qty: 1, cost: "", sell_by: "box", description: "", retail_price: "" });
    const [poDocPreview, setPoDocPreview] = useState(null);
    const repInfo = JSON.parse(sessionStorage.getItem("rep_info") || "{}");
    const load = async () => {
      setLoading(true);
      try {
        const data = await repFetch("/api/rep/purchase-orders/" + editId + "/detail");
        setPO(data.purchase_order);
        setItems(data.purchase_order.items || []);
        setActivity(data.activity || []);
      } catch (err) {
      }
      setLoading(false);
    };
    useEffect(() => {
      load();
    }, [editId]);
    const isDraft = po && po.status === "draft";
    const isSentOrLater = po && ["sent", "acknowledged", "fulfilled"].includes(po.status);
    const isOrderLinked = po && !!po.order_number;
    const editable = isDraft && !isOrderLinked;
    const INK = PD_INK, PAPER = PD_PAPER, ACCENT = PD_ACCENT, MUTED = PD_MUTED, WARM = PD_WARM;
    const GREEN = PD_GREEN, AMBER = PD_AMBER, RED = PD_RED, BLUE = PD_BLUE;
    const statusColor = { draft: MUTED, sent: AMBER, acknowledged: AMBER, fulfilled: GREEN, cancelled: RED };
    const statusLabel = { draft: "Draft", sent: "Sent \xB7 awaiting acknowledgement", acknowledged: "Acknowledged", fulfilled: "Fulfilled \xB7 complete", cancelled: "Cancelled" };
    const updatePO = async (updates) => {
      try {
        const data = await repFetch("/api/rep/purchase-orders/" + editId, {
          method: "PUT",
          body: JSON.stringify(updates)
        });
        if (data.purchase_order) setPO((prev) => ({ ...prev, ...data.purchase_order }));
      } catch (err) {
        alert(err.message);
      }
    };
    const updatePOItem = async (itemId, updates) => {
      try {
        await repFetch("/api/rep/purchase-orders/" + editId + "/items/" + itemId, {
          method: "PUT",
          body: JSON.stringify(updates)
        });
        load();
      } catch (err) {
        alert(err.message);
      }
    };
    const addPOItem = async (itemData) => {
      try {
        await repFetch("/api/rep/purchase-orders/" + editId + "/items", {
          method: "POST",
          body: JSON.stringify(itemData)
        });
        load();
      } catch (err) {
        alert(err.message);
      }
    };
    const deletePOItem = async (itemId) => {
      if (!confirm("Delete this line item?")) return;
      try {
        await repFetch("/api/rep/purchase-orders/" + editId + "/items/" + itemId, {
          method: "DELETE"
        });
        load();
      } catch (err) {
        alert(err.message);
      }
    };
    const approveAndSend = async () => {
      if (!confirm("Approve & send this PO to the vendor?")) return;
      setPoUpdating(true);
      try {
        await repFetch("/api/rep/purchase-orders/" + editId + "/approve", { method: "POST" });
        load();
      } catch (err) {
        alert(err.message);
      }
      setPoUpdating(false);
    };
    const updatePOItemStatus = async (itemId, status) => {
      try {
        await repFetch("/api/rep/purchase-orders/" + editId + "/items/" + itemId + "/status", {
          method: "PUT",
          body: JSON.stringify({ status })
        });
        load();
      } catch (err) {
        alert(err.message);
      }
    };
    const bulkUpdatePOItemStatus = async (status) => {
      if (!confirm("Set all non-terminal items to " + status + "?")) return;
      try {
        await repFetch("/api/rep/purchase-orders/" + editId + "/items/bulk-status", {
          method: "PUT",
          body: JSON.stringify({ status })
        });
        load();
      } catch (err) {
        alert(err.message);
      }
    };
    const handleSkuSearch = (q) => {
      setAddItemSku(q);
      if (skuSearchTimer.current) clearTimeout(skuSearchTimer.current);
      const thisSearch = ++skuSearchCounter.current;
      if (q.length >= 2) {
        setSkuSearching(true);
        skuSearchTimer.current = setTimeout(async () => {
          try {
            const data = await repFetch("/api/rep/skus/search?q=" + encodeURIComponent(q));
            if (thisSearch !== skuSearchCounter.current) return;
            setSkuSearchResults((data.results || []).map((r) => ({
              sku_id: r.sku_id,
              product_name: r.product_name || r.collection || "",
              variant_name: r.variant_name || "",
              color: r.color || "",
              vendor_sku: r.vendor_sku || "",
              internal_sku: r.internal_sku || "",
              vendor_name: r.vendor_name || "",
              sell_by: r.sell_by || "box",
              cost: r.vendor_cost || 0,
              retail_price: r.retail_price || 0,
              price_basis: r.price_basis || "per_sqft",
              sqft_per_box: r.sqft_per_box || 1,
              primary_image: r.primary_image || null
            })));
          } catch (e) {
            if (thisSearch !== skuSearchCounter.current) return;
          }
          if (thisSearch === skuSearchCounter.current) setSkuSearching(false);
        }, 250);
      } else {
        setSkuSearchResults([]);
        setSkuSearching(false);
      }
    };
    const selectSkuResult = async (r) => {
      const isPerSqft = r.price_basis === "per_sqft" || r.price_basis === "sqft";
      const sqftPerBox = parseFloat(r.sqft_per_box || 1);
      const rawCost = parseFloat(r.cost || 0);
      const rawRetail = parseFloat(r.retail_price || 0);
      const itemCost = isPerSqft ? rawCost * sqftPerBox : rawCost;
      const itemRetail = isPerSqft ? rawRetail * sqftPerBox : rawRetail;
      await addPOItem({
        sku_id: r.sku_id,
        product_name: formatLineItem(r).name || r.product_name,
        vendor_sku: r.vendor_sku || r.internal_sku,
        qty: 1,
        cost: itemCost.toFixed(2),
        retail_price: itemRetail > 0 ? itemRetail.toFixed(2) : null,
        sell_by: r.sell_by
      });
      setAddItemSku("");
      setSkuSearchResults([]);
      setAddingItem(false);
    };
    const actionLabel = (action) => {
      const labels = { sent: "Sent", resent: "Resent", revised_and_sent: "Revised & Sent", acknowledged: "Acknowledged", fulfilled: "Fulfilled", cancelled: "Cancelled", reverted: "Reverted to Draft", created: "Created", notes_updated: "Notes Updated", edi_sent: "Sent via EDI", edi_acknowledged: "EDI Acknowledged", edi_ack_rejected: "EDI Rejected", edi_ack_partial: "EDI Partial Ack", confirmation_attached: "Confirmation Attached", edi_shipped: "EDI Ship Notice", edi_invoiced: "EDI Invoice Received" };
      return labels[action] || action;
    };
    const actionColor = (action) => {
      if (["sent", "resent", "revised_and_sent", "edi_sent"].includes(action)) return BLUE;
      if (["acknowledged", "edi_acknowledged", "fulfilled", "created"].includes(action)) return GREEN;
      if (["cancelled"].includes(action)) return RED;
      return AMBER;
    };
    const subtotal = items.reduce((s, it) => s + parseFloat(it.cost || 0) * (it.qty || 0), 0);
    const retail = items.reduce((s, it) => s + parseFloat(it.retail_price || 0) * (it.qty || 0), 0);
    const margin = retail > 0 ? retail - subtotal : 0;
    const marginPct = retail > 0 ? (margin / retail * 100).toFixed(1) : "0.0";
    const receivedCount = items.filter((it) => it.status === "received").length;
    const findAct = (...actions) => {
      for (let i = activity.length - 1; i >= 0; i--) {
        if (actions.includes(activity[i].action)) return activity[i];
      }
      return null;
    };
    const sentAct = po ? findAct("sent", "edi_sent", "resent", "revised_and_sent") : null;
    const ackAct = po ? findAct("acknowledged", "edi_acknowledged") : null;
    const shipAct = po ? findAct("edi_shipped") : null;
    const sentAt = po && (po.approved_at || sentAct && sentAct.created_at);
    const ackAt = po && (po.edi_ack_received_at || ackAct && ackAct.created_at);
    const shippedLines = items.filter((it) => ["shipped", "received"].includes(it.status) || parseFloat(it.qty_shipped || 0) > 0).length;
    const fmtDT = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" }) + " \xB7 " + new Date(d).toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }) : null;
    const fmtDay = (d) => d ? new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : null;
    const sentDone = po && (!!sentAt || isSentOrLater);
    const ackFlagged = po && (po.edi_ack_status === "rejected" || po.edi_ack_status === "partial");
    const ackDone = po && !ackFlagged && (po.edi_ack_status === "accepted" || ["acknowledged", "fulfilled"].includes(po.status));
    const ackLineSummary = (() => {
      const c = {};
      for (const it of items) {
        if (it.edi_line_status) c[it.edi_line_status] = (c[it.edi_line_status] || 0) + 1;
      }
      const k = Object.keys(c);
      return !k.length || k.length === 1 && k[0] === "accepted" ? null : k.map((x) => c[x] + " " + x).join(" \xB7 ");
    })();
    const shipDone = items.length > 0 && shippedLines === items.length;
    const recvDone = items.length > 0 && receivedCount === items.length;
    const timeline = !po ? [] : [
      {
        label: "Created",
        done: true,
        t: fmtDT(po.created_at),
        detail: po.order_number ? "From order " + po.order_number : "Standalone PO"
      },
      {
        label: "Sent",
        done: sentDone,
        t: fmtDT(sentAt) || (sentDone ? "\u2014" : "Not yet sent"),
        detail: sentDone ? (sentAct && sentAct.action === "edi_sent" ? "EDI 850" : po.recipient_email || "Email to vendor") + (po.approved_by_name ? " \xB7 " + po.approved_by_name : "") + (po.revision > 0 ? " \xB7 Rev " + po.revision : "") : "Approve & send to transmit"
      },
      {
        label: "Acknowledged",
        done: ackDone,
        t: fmtDT(ackAt) || (ackDone ? "\u2014" : sentDone ? "Awaiting vendor" : "\u2014"),
        detail: ackFlagged ? "EDI 855 \xB7 " + po.edi_ack_status + (ackLineSummary ? " \xB7 " + ackLineSummary : "") : po.edi_ack_status === "accepted" ? "EDI 855 \xB7 accepted" + (ackLineSummary ? " \xB7 " + ackLineSummary : "") : ackDone ? "Vendor acknowledged" : sentDone ? "Awaiting vendor" : "No acknowledgement yet"
      },
      {
        label: "Shipped",
        done: shipDone,
        t: shipAct ? fmtDT(shipAct.created_at) : "\u2014",
        detail: items.length ? shippedLines + " of " + items.length + " lines" + (shipAct ? " \xB7 ASN 856" : "") : "No lines"
      },
      {
        label: "Received",
        done: recvDone,
        t: recvDone ? "\u2014" : po.expected_delivery ? "Est. " + fmtDay(po.expected_delivery) : "\u2014",
        detail: items.length ? receivedCount + " of " + items.length + " lines" + (recvDone ? " \xB7 complete" : "") : "No lines"
      }
    ];
    const timelineCurrent = timeline.findIndex((s) => !s.done);
    const monoLabel = pdMonoLabel;
    const serifNum = pdSerifNum;
    if (loading) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Loading purchase order...");
    if (!po) return /* @__PURE__ */ React.createElement("div", { className: "loading" }, "Purchase order not found");
    const sCol = statusColor[po.status] || MUTED;
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs-bar" }, /* @__PURE__ */ React.createElement("div", { className: "ws-crumbs" }, /* @__PURE__ */ React.createElement("a", { onClick: () => navigate("purchase-orders") }, "PO Tracking"), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" }), isOrderLinked && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("a", { style: { color: ACCENT }, onClick: () => navigate("order-detail", po.order_id) }, po.order_number), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-sep" })), /* @__PURE__ */ React.createElement("span", { className: "ws-crumb-cur" }, po.po_number)), /* @__PURE__ */ React.createElement("div", { className: "rod-breadcrumb-nav" }, /* @__PURE__ */ React.createElement(
      RepPrevNext,
      {
        section: "purchase-orders",
        id: editId,
        view: "purchase-order-detail",
        navigate,
        linkStyle: { color: MUTED, textDecoration: "none" }
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: "ws-head" }, /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-title-row" }, /* @__PURE__ */ React.createElement("h1", { className: "ws-title" }, "Purchase order ", /* @__PURE__ */ React.createElement("em", null, po.po_number)), /* @__PURE__ */ React.createElement(RovChip, { tone: { draft: "soft", sent: "hold", acknowledged: "hold", fulfilled: "done", cancelled: "hot" }[po.status] || "soft" }, statusLabel[po.status] || po.status), isOrderLinked ? /* @__PURE__ */ React.createElement("span", { style: { padding: "5px 10px", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: ACCENT, border: "0.5px solid " + ACCENT + "55", cursor: "pointer" }, onClick: () => navigate("order-detail", po.order_id) }, "\u25CF Linked to ", po.order_number) : /* @__PURE__ */ React.createElement("span", { style: { padding: "5px 10px", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: INK, border: "0.5px solid " + INK + "33" } }, "\u25CB Standalone PO"), /* @__PURE__ */ React.createElement("span", { style: { display: "inline-flex", alignItems: "center", gap: 6, padding: "5px 10px", font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: editable ? GREEN : MUTED, background: editable ? "#e8f0e8" : INK + "0a" } }, editable ? "\u270E Adjustable" : "\u{1F512} View only")), /* @__PURE__ */ React.createElement("div", { style: { font: "400 14px/1.5 Inter, sans-serif", color: INK + "b3", marginTop: 8 } }, po.vendor_name, po.vendor_public_code ? " \xB7 " + po.vendor_public_code : "", po.recipient_email || po.vendor_email ? /* @__PURE__ */ React.createElement(React.Fragment, null, " \xB7 ", /* @__PURE__ */ React.createElement("em", null, po.recipient_email || po.vendor_email)) : null, " \xB7 ", po.edi_config ? "via EDI" : "via email", po.order_number ? " \xB7 for order " + po.order_number : "", " \xB7 placed ", new Date(po.created_at).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }))), !isOrderLinked && /* @__PURE__ */ React.createElement("div", { className: "ws-head-actions" }, isDraft && items.length > 0 && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", style: { background: INK, color: PAPER, borderColor: INK }, disabled: poUpdating, onClick: approveAndSend }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u2913"), poUpdating ? "Sending..." : "Approve & send"), isDraft && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => {
      setAddingItem(true);
      setAddItemMode("sku");
    } }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "+"), "Add line"), isSentOrLater && /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: () => bulkUpdatePOItemStatus("received") }, /* @__PURE__ */ React.createElement("span", { className: "rov-hico" }, "\u2398"), "Mark all received"), /* @__PURE__ */ React.createElement("button", { className: "rov-hbtn", onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/purchase-orders/" + editId + "/pdf?preview=true");
        setPoDocPreview({ url, po });
      } catch (e) {
        alert("Failed to load PDF");
      }
    } }, "View PDF"))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpis" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "PO total"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, "$", subtotal.toLocaleString(void 0, { minimumFractionDigits: 2 }))), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Resale value"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v", style: retail > 0 ? { color: GREEN } : void 0 }, retail > 0 ? "$" + retail.toLocaleString(void 0, { minimumFractionDigits: 2 }) : "\u2014")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Lines"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, items.length)), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Vendor"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, po.vendor_name, po.vendor_public_code ? " \xB7 " + po.vendor_public_code : "")), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi" }, /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-l" }, "Sent"), /* @__PURE__ */ React.createElement("div", { className: "ws-kpi-v" }, sentDone ? fmtDay(sentAt) || "\u2014" : "Not sent"))), isOrderLinked && /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 18, alignItems: "center", justifyContent: "space-between", padding: "14px 20px", background: WARM, border: "0.5px solid " + ACCENT + "44", borderLeft: "3px solid " + ACCENT, marginBottom: 22 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 14, alignItems: "center" } }, /* @__PURE__ */ React.createElement("span", { style: { width: 34, height: 34, flexShrink: 0, background: PAPER, border: "0.5px solid " + INK + "22", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 16 } }, "\u{1F512}"), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: ACCENT, marginBottom: 5 } }, "View only \xB7 managed by order ", po.order_number), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.5 Inter, sans-serif", color: INK + "cc", maxWidth: 620 } }, "This PO was generated from a customer order. Line items, quantities and pricing are driven by the order \u2014 edit them from the order, then re-issue. You can still receive against it or download the PDF."))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: 6, flexShrink: 0 } }, /* @__PURE__ */ React.createElement("button", { style: { padding: "9px 14px", background: INK, color: PAPER, font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", border: "none" }, onClick: () => navigate("order-detail", po.order_id) }, "Edit on order \u2192"), /* @__PURE__ */ React.createElement("button", { style: { padding: "9px 14px", border: "0.5px solid " + INK + "55", color: INK, font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.08em", textTransform: "uppercase", cursor: "pointer", background: "transparent" }, onClick: async () => {
      try {
        const url = await repOpenDoc("/api/rep/purchase-orders/" + editId + "/pdf?preview=true");
        setPoDocPreview({ url, po });
      } catch (e) {
        alert("Failed to load PDF");
      }
    } }, "View PDF"))), /* @__PURE__ */ React.createElement("div", { style: { background: PAPER, border: "0.5px solid " + INK + "22", padding: "26px 22px 18px", marginBottom: 24 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(160px, 1fr))", rowGap: 30 } }, timeline.map((s, i) => {
      const current = i === timelineCurrent && po.status !== "cancelled";
      return /* @__PURE__ */ React.createElement("div", { key: s.label, style: { position: "relative", paddingTop: 18, paddingRight: 14 } }, /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", top: 0, left: 0, right: i === timeline.length - 1 ? "auto" : 0, width: i === timeline.length - 1 ? 14 : "auto", height: 2, background: s.done ? ACCENT : INK + "22" } }), /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", top: -6, left: 0, width: 14, height: 14, borderRadius: "50%", boxSizing: "border-box", background: s.done ? ACCENT : current ? PAPER : INK + "11", border: "2px solid " + (s.done || current ? ACCENT : INK + "33") } }), /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.14em", textTransform: "uppercase", color: current ? ACCENT : MUTED, marginBottom: 4 } }, s.label), /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "'Cormorant Garamond', serif", fontWeight: 400, fontSize: 13, lineHeight: 1.3, color: INK } }, s.t || "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { font: "400 11px/1.4 Inter, sans-serif", color: INK + "80", marginTop: 4 } }, s.detail));
    }))), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "1.55fr 1fr", gap: 20 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 20 } }, /* @__PURE__ */ React.createElement(PdCard, { title: "Line items \xB7 " + items.length, action: editable ? "+ Add line" : isOrderLinked ? "Read-only" : null, actionClick: editable ? () => {
      setAddingItem(true);
      setAddItemMode("sku");
    } : null }, editable && addingItem && /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderBottom: "0.5px solid " + INK + "18", background: WARM + "80" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", marginBottom: "0.75rem" } }, /* @__PURE__ */ React.createElement("button", { className: addItemMode === "sku" ? "btn btn-primary btn-sm" : "btn btn-secondary btn-sm", onClick: () => setAddItemMode("sku") }, "SKU Search"), /* @__PURE__ */ React.createElement("button", { className: addItemMode === "manual" ? "btn btn-primary btn-sm" : "btn btn-secondary btn-sm", onClick: () => setAddItemMode("manual") }, "Manual Entry"), /* @__PURE__ */ React.createElement("div", { style: { flex: 1 } }), /* @__PURE__ */ React.createElement("button", { className: "btn btn-sm btn-secondary", onClick: () => {
      setAddingItem(false);
      setSkuSearchResults([]);
      setAddItemSku("");
    } }, "Cancel")), addItemMode === "sku" ? /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        value: addItemSku,
        placeholder: "Type SKU or product name...",
        onBlur: () => setTimeout(() => {
          setSkuSearchResults([]);
          setSkuSearching(false);
        }, 200),
        onChange: (e) => handleSkuSearch(e.target.value),
        style: { width: "100%", padding: "0.5rem", border: "0.5px solid " + INK + "33", borderRadius: 0, background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" }
      }
    ), skuSearching && skuSearchResults.length === 0 && addItemSku.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, /* @__PURE__ */ React.createElement("div", { className: "search-loading-indicator" }, "Searching...")), !skuSearching && skuSearchResults.length === 0 && addItemSku.length >= 2 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, /* @__PURE__ */ React.createElement("div", { className: "search-no-results" }, "No results found")), skuSearchResults.length > 0 && /* @__PURE__ */ React.createElement("div", { className: "search-results" }, skuSearchResults.map((r, i) => /* @__PURE__ */ React.createElement("div", { key: i, className: "search-result-item", onClick: () => selectSkuResult(r) }, r.primary_image ? /* @__PURE__ */ React.createElement("img", { className: "sku-thumb", src: optimizeImg(r.primary_image, 96), alt: "", loading: "lazy" }) : /* @__PURE__ */ React.createElement("div", { className: "sku-thumb-placeholder" }, /* @__PURE__ */ React.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "1.5" }, /* @__PURE__ */ React.createElement("rect", { x: "3", y: "3", width: "18", height: "18", rx: "2" }), /* @__PURE__ */ React.createElement("circle", { cx: "8.5", cy: "8.5", r: "1.5" }), /* @__PURE__ */ React.createElement("path", { d: "M21 15l-5-5L5 21" }))), /* @__PURE__ */ React.createElement("div", { className: "sku-info" }, /* @__PURE__ */ React.createElement("div", { className: "sku-name" }, formatLineItem(r).name), /* @__PURE__ */ React.createElement("div", { className: "product-meta" }, r.vendor_name, r.vendor_public_code ? " \xB7 " + r.vendor_public_code : "", r.vendor_sku ? " \xB7 " + r.vendor_sku : r.internal_sku ? " \xB7 " + r.internal_sku : "", r.cost ? " \xB7 Cost $" + parseFloat(r.cost).toFixed(2) + skuPriceSuffix(r) : "")))))) : /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: "0.5rem" } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", alignItems: "flex-end", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 2, minWidth: "120px" } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", ...monoLabel, fontSize: 10, marginBottom: 4 } }, "Product Name *"), /* @__PURE__ */ React.createElement("input", { type: "text", maxLength: 200, value: newPOItem.product_name, onChange: (e) => setNewPOItem({ ...newPOItem, product_name: e.target.value }), style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" } })), /* @__PURE__ */ React.createElement("div", { style: { flex: 1, minWidth: "80px" } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", ...monoLabel, fontSize: 10, marginBottom: 4 } }, "Vendor SKU"), /* @__PURE__ */ React.createElement("input", { type: "text", maxLength: 100, value: newPOItem.vendor_sku, onChange: (e) => setNewPOItem({ ...newPOItem, vendor_sku: e.target.value }), style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" } })), /* @__PURE__ */ React.createElement("div", { style: { width: 80 } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", ...monoLabel, fontSize: 10, marginBottom: 4 } }, "Sell By"), /* @__PURE__ */ React.createElement("select", { value: newPOItem.sell_by, onChange: (e) => setNewPOItem({ ...newPOItem, sell_by: e.target.value }), style: { width: "100%", padding: "6px 4px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 12px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" } }, /* @__PURE__ */ React.createElement("option", { value: "box" }, "Box"), /* @__PURE__ */ React.createElement("option", { value: "unit" }, "Unit"), /* @__PURE__ */ React.createElement("option", { value: "sqft" }, "Sqft"), /* @__PURE__ */ React.createElement("option", { value: "pallet" }, "Pallet")))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.5rem", alignItems: "flex-end", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("div", { style: { flex: 2, minWidth: "120px" } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", ...monoLabel, fontSize: 10, marginBottom: 4 } }, "Description"), /* @__PURE__ */ React.createElement("input", { type: "text", maxLength: 500, value: newPOItem.description, onChange: (e) => setNewPOItem({ ...newPOItem, description: e.target.value }), placeholder: "Optional description", style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" } })), /* @__PURE__ */ React.createElement("div", { style: { width: 60 } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", ...monoLabel, fontSize: 10, marginBottom: 4 } }, "Qty *"), /* @__PURE__ */ React.createElement("input", { type: "number", min: "1", value: newPOItem.qty, onChange: (e) => setNewPOItem({ ...newPOItem, qty: parseInt(e.target.value) || 1 }), style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" } })), /* @__PURE__ */ React.createElement("div", { style: { width: 80 } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", ...monoLabel, fontSize: 10, marginBottom: 4 } }, "Cost *"), /* @__PURE__ */ React.createElement("input", { type: "number", step: "0.01", min: "0", value: newPOItem.cost, onChange: (e) => setNewPOItem({ ...newPOItem, cost: e.target.value }), placeholder: "0.00", style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" } })), /* @__PURE__ */ React.createElement("div", { style: { width: 80 } }, /* @__PURE__ */ React.createElement("label", { style: { display: "block", ...monoLabel, fontSize: 10, marginBottom: 4 } }, "Retail"), /* @__PURE__ */ React.createElement("input", { type: "number", step: "0.01", min: "0", value: newPOItem.retail_price, onChange: (e) => setNewPOItem({ ...newPOItem, retail_price: e.target.value }), placeholder: "0.00", style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" } })), /* @__PURE__ */ React.createElement(
      "button",
      {
        style: { padding: "7px 14px", background: INK, color: PAPER, border: "none", font: "500 12px/1 Inter, sans-serif", cursor: "pointer" },
        disabled: !newPOItem.product_name || !newPOItem.cost,
        onClick: () => {
          addPOItem({ ...newPOItem, retail_price: newPOItem.retail_price || null });
          setNewPOItem({ product_name: "", vendor_sku: "", qty: 1, cost: "", sell_by: "box", description: "", retail_price: "" });
          setAddingItem(false);
        }
      },
      "Add"
    )))), /* @__PURE__ */ React.createElement("div", { style: { padding: "10px 20px", display: "grid", gridTemplateColumns: editable ? "1fr 100px 56px 84px 80px 22px" : "1fr 100px 56px 84px 80px", gap: 8, background: WARM, borderBottom: "0.5px solid " + INK + "22", ...monoLabel, fontSize: 9, letterSpacing: "0.16em" } }, /* @__PURE__ */ React.createElement("span", null, "Description"), /* @__PURE__ */ React.createElement("span", null, "Vendor SKU"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Qty"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Cost"), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right" } }, "Line $"), editable && /* @__PURE__ */ React.createElement("span", null)), items.map((item, i) => {
      const costVal = parseFloat(item.cost || 0);
      const lineTotal = costVal * (item.qty || 0);
      return /* @__PURE__ */ React.createElement("div", { key: item.id, style: { padding: "14px 20px", display: "grid", gridTemplateColumns: editable ? "1fr 100px 56px 84px 80px 22px" : "1fr 100px 56px 84px 80px", gap: 8, alignItems: "center", borderBottom: i < items.length - 1 ? "0.5px solid " + INK + "11" : "none" } }, /* @__PURE__ */ React.createElement("div", null, formatLineItem(item).brand && /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginBottom: 2 } }, formatLineItem(item).brand), /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.3 Inter, sans-serif", color: INK } }, formatLineItem(item).name), formatLineItem(item).sku && /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginTop: 2 } }, formatLineItem(item).sku), item.description && /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginTop: 2 } }, item.description), editable ? /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "text",
          defaultValue: item.dye_lot || "",
          placeholder: "Dye lot",
          onBlur: (e) => {
            const v = e.target.value;
            if (v !== (item.dye_lot || "")) updatePOItem(item.id, { dye_lot: v || null });
          },
          style: { marginTop: 4, width: "120px", padding: "2px 4px", border: "0.5px solid " + INK + "22", background: "transparent", font: "400 10px/1 ui-monospace, monospace", color: MUTED, letterSpacing: "0.04em", outline: "none" }
        }
      ) : item.dye_lot ? /* @__PURE__ */ React.createElement("div", { style: { marginTop: 4, font: "400 10px/1 ui-monospace, monospace", color: MUTED, letterSpacing: "0.04em" } }, "Lot: ", item.dye_lot) : null, isSentOrLater && (() => {
        const stCol = item.status === "received" ? GREEN : item.status === "shipped" ? AMBER : item.status === "ordered" ? BLUE : item.status === "cancelled" ? RED : MUTED;
        return /* @__PURE__ */ React.createElement("div", { style: { marginTop: 4, font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: stCol } }, "\u25CF ", item.status || "pending", parseFloat(item.qty_shipped || 0) > 0 ? " \xB7 " + item.qty_shipped + " shipped" : "", item.edi_line_status ? " \xB7 EDI " + item.edi_line_status : "");
      })()), /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 10, letterSpacing: "0.06em" } }, item.vendor_sku || "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, editable ? /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          min: "0",
          step: "any",
          defaultValue: item.qty,
          onBlur: (e) => {
            const v = parseFloat(e.target.value);
            if (v > 0 && v !== parseFloat(item.qty)) updatePOItem(item.id, { qty: v });
          },
          style: { width: "100%", border: "none", borderBottom: "1px solid " + INK + "26", background: "transparent", outline: "none", padding: "2px 0", fontFamily: "'Cormorant Garamond', serif", fontSize: 14, color: INK, textAlign: "right" }
        }
      ) : /* @__PURE__ */ React.createElement("span", { style: { fontFamily: "'Cormorant Garamond', serif", fontSize: 14, color: INK } }, item.qty)), /* @__PURE__ */ React.createElement("div", { style: { textAlign: "right" } }, editable ? /* @__PURE__ */ React.createElement(
        "input",
        {
          type: "number",
          step: "0.01",
          min: "0",
          defaultValue: costVal.toFixed(2),
          onBlur: (e) => {
            const v = parseFloat(e.target.value);
            if (!isNaN(v) && v !== costVal) updatePOItem(item.id, { cost: v });
          },
          style: { width: "100%", border: "none", borderBottom: "1px solid " + INK + "26", background: "transparent", outline: "none", padding: "2px 0", fontFamily: "'Cormorant Garamond', serif", fontSize: 14, color: INK, textAlign: "right" }
        }
      ) : /* @__PURE__ */ React.createElement("span", { style: { fontFamily: "'Cormorant Garamond', serif", fontSize: 14, color: INK } }, "$", costVal.toFixed(2))), /* @__PURE__ */ React.createElement("span", { style: { textAlign: "right", fontFamily: "'Cormorant Garamond', serif", fontSize: 14, color: INK } }, "$", lineTotal.toFixed(2)), editable && /* @__PURE__ */ React.createElement("span", { style: { color: MUTED, fontFamily: "'Cormorant Garamond', serif", fontSize: 14, textAlign: "right", cursor: "pointer" }, onClick: () => deletePOItem(item.id) }, "\xD7"));
    }), items.length === 0 && /* @__PURE__ */ React.createElement("div", { style: { padding: "24px 20px", textAlign: "center", color: MUTED, font: "400 13px/1.4 Inter, sans-serif" } }, "No items yet"), /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px", borderTop: "0.5px solid " + INK + "22", display: "flex", justifyContent: "flex-end", alignItems: "baseline", gap: 14 } }, /* @__PURE__ */ React.createElement("span", { style: { ...monoLabel, fontSize: 10, letterSpacing: "0.16em" } }, "Subtotal"), /* @__PURE__ */ React.createElement("span", { style: { ...serifNum(26), color: INK } }, "$", subtotal.toLocaleString(void 0, { minimumFractionDigits: 2 })))), isSentOrLater && /* @__PURE__ */ React.createElement(PdCard, { title: "Receiving", action: "Mark all received", actionClick: () => bulkUpdatePOItemStatus("received") }, items.map((item, i) => {
      const isReceived = item.status === "received";
      const itemStatusCol = isReceived ? GREEN : ["shipped", "ordered"].includes(item.status) ? AMBER : MUTED;
      return /* @__PURE__ */ React.createElement("div", { key: item.id, style: { padding: "13px 20px", display: "grid", gridTemplateColumns: "1fr 120px 110px", gap: 14, alignItems: "center", borderBottom: i < items.length - 1 ? "0.5px solid " + INK + "11" : "none" } }, /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "'Cormorant Garamond', serif", fontSize: 13, lineHeight: 1.3, color: INK } }, formatLineItem(item).name || item.product_name), /* @__PURE__ */ React.createElement("div", { style: { height: 4, background: INK + "14", marginTop: 8, position: "relative" } }, /* @__PURE__ */ React.createElement("div", { style: { position: "absolute", inset: 0, right: isReceived ? 0 : "100%", background: isReceived ? GREEN : AMBER } }))), /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 ui-monospace, monospace", letterSpacing: "0.08em", color: INK, textAlign: "right" } }, isReceived ? item.qty : 0, " / ", item.qty, " ", item.sell_by || "box"), /* @__PURE__ */ React.createElement(
        "select",
        {
          value: item.status || "pending",
          onChange: (e) => updatePOItemStatus(item.id, e.target.value),
          style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.1em", textTransform: "uppercase", color: itemStatusCol, textAlign: "right", border: "0.5px solid " + INK + "22", background: "transparent", padding: "4px 6px", cursor: "pointer" }
        },
        ["pending", "ordered", "shipped", "received", "cancelled"].map((s) => /* @__PURE__ */ React.createElement("option", { key: s, value: s }, s.charAt(0).toUpperCase() + s.slice(1)))
      ));
    })), /* @__PURE__ */ React.createElement(PdCard, { title: "Activity \xB7 " + activity.length }, activity.length > 0 ? activity.map((a, i) => /* @__PURE__ */ React.createElement("div", { key: i, style: { padding: "11px 20px", display: "grid", gridTemplateColumns: "110px 60px 1fr", gap: 12, alignItems: "flex-start", borderBottom: i < activity.length - 1 ? "0.5px solid " + INK + "11" : "none" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 10px/1.5 ui-monospace, monospace", letterSpacing: "0.12em", color: MUTED, textTransform: "uppercase" } }, new Date(a.created_at).toLocaleDateString("en-US", { month: "short", day: "numeric" }), " \xB7 ", new Date(a.created_at).toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" })), /* @__PURE__ */ React.createElement("span", { style: { display: "inline-block", padding: "2px 6px", font: "500 9px/1.4 ui-monospace, monospace", letterSpacing: "0.12em", textTransform: "uppercase", color: actionColor(a.action), border: "0.5px solid " + actionColor(a.action) + "55", textAlign: "center" } }, actionLabel(a.action).split(" ")[0]), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 Inter, sans-serif", color: INK } }, actionLabel(a.action), a.recipient_email ? " to " + a.recipient_email : "", a.revision > 0 ? " (Rev. " + a.revision + ")" : ""), a.performer_name && /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginTop: 2 } }, a.performer_name)))) : /* @__PURE__ */ React.createElement("div", { style: { padding: "20px", color: MUTED, font: "400 13px/1.4 Inter, sans-serif" } }, "No activity yet"))), /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gap: 20, alignContent: "start" } }, /* @__PURE__ */ React.createElement(PdCard, { title: "Approved by" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 20px" } }, po.approved_by_name ? /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar" }, (po.approved_by_name || "?").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "?"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name" }, po.approved_by_name, po.approved_by === repInfo.id && /* @__PURE__ */ React.createElement("span", { className: "ws-you-tag" }, "You")), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "Approved & sent this PO", sentAt ? " \xB7 " + fmtDay(sentAt) : ""))) : /* @__PURE__ */ React.createElement("div", { className: "ws-owner" }, /* @__PURE__ */ React.createElement("div", { className: "qw-avatar", style: { background: "var(--brass-warm, #d8cdb6)", color: "var(--brass-muted, #8a7e68)" } }, "\u2014"), /* @__PURE__ */ React.createElement("div", { style: { minWidth: 0 } }, /* @__PURE__ */ React.createElement("div", { className: "ws-owner-name", style: { color: "var(--rod-muted)" } }, "Not yet approved"), /* @__PURE__ */ React.createElement("div", { className: "ws-owner-meta" }, "Approve & send records the owner"))))), /* @__PURE__ */ React.createElement(PdCard, { title: "Vendor" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 20px", display: "grid", gap: 12 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "grid", gridTemplateColumns: "44px 1fr", gap: 12, alignItems: "center" } }, /* @__PURE__ */ React.createElement("div", { style: { width: 44, height: 44, background: INK, color: PAPER, display: "flex", alignItems: "center", justifyContent: "center", font: "500 12px/1 ui-monospace, monospace", letterSpacing: "0.08em" } }, (po.vendor_name || "??").substring(0, 3).toUpperCase()), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { fontFamily: "'Cormorant Garamond', serif", fontSize: 18, lineHeight: 1.1, color: INK, letterSpacing: "-0.012em" } }, po.vendor_name, po.vendor_public_code ? /* @__PURE__ */ React.createElement("span", { style: { color: MUTED, fontSize: 14 } }, " \xB7 ", po.vendor_public_code) : null), /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginTop: 4 } }, po.vendor_code || "\u2014", po.edi_config ? " \xB7 EDI enabled" : " \xB7 Email PO"))), (po.recipient_email || po.vendor_email) && /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 Inter, sans-serif", color: INK + "cc", wordBreak: "break-word" } }, po.recipient_email || po.vendor_email), (po.recipient_email || po.vendor_email) && /* @__PURE__ */ React.createElement(
      "a",
      {
        href: "mailto:" + (po.recipient_email || po.vendor_email) + "?subject=" + encodeURIComponent("PO " + po.po_number + " \u2014 Roma Flooring Designs"),
        style: { padding: "10px 0", textAlign: "center", border: "0.5px solid " + INK + "55", color: INK, font: "500 10px/1 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", textDecoration: "none", display: "block" }
      },
      "Message vendor"
    ))), /* @__PURE__ */ React.createElement(PdCard, { title: "Shipping & delivery" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 20px", display: "grid", gap: 14 } }, (() => {
      const isPickup = po.fulfillment_method === "pickup";
      return /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginBottom: 6 } }, "Fulfillment"), editable ? /* @__PURE__ */ React.createElement("div", { style: { display: "flex", border: "0.5px solid " + INK + "33" } }, [["ship", "Ship to Roma"], ["pickup", "Pick up at distributor"]].map(([val, label], i) => /* @__PURE__ */ React.createElement(
        "button",
        {
          key: val,
          type: "button",
          onClick: () => {
            if (po.fulfillment_method !== val) updatePO({ fulfillment_method: val });
          },
          style: {
            flex: 1,
            padding: "8px 6px",
            border: "none",
            borderLeft: i ? "0.5px solid " + INK + "33" : "none",
            cursor: "pointer",
            background: (isPickup ? val === "pickup" : val === "ship") ? ACCENT : "transparent",
            color: (isPickup ? val === "pickup" : val === "ship") ? "#fff" : INK + "cc",
            font: "500 11px/1.2 Inter, sans-serif"
          }
        },
        label
      ))) : /* @__PURE__ */ React.createElement("div", { style: { font: "500 12px/1.4 Inter, sans-serif", color: INK } }, isPickup ? "Roma picks up at distributor" : "Ship to Roma"));
    })(), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginBottom: 6 } }, po.fulfillment_method === "pickup" ? "Pickup Location" : "Ship To"), editable ? /* @__PURE__ */ React.createElement(
      "textarea",
      {
        defaultValue: po.ship_to || "",
        placeholder: po.fulfillment_method === "pickup" ? "Defaults to the distributor address" : "",
        onBlur: (e) => {
          const v = e.target.value;
          if (v !== (po.ship_to || "")) updatePO({ ship_to: v });
        },
        style: { width: "100%", minHeight: "54px", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1.4 Inter, sans-serif", color: INK, boxSizing: "border-box", resize: "vertical" }
      }
    ) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 Inter, sans-serif", color: INK, whiteSpace: "pre-line" } }, po.ship_to || (po.fulfillment_method === "pickup" ? "Distributor address" : "\u2014"))), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginBottom: 6 } }, "Expected Delivery"), editable ? /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "date",
        defaultValue: po.expected_delivery ? po.expected_delivery.substring(0, 10) : "",
        onBlur: (e) => {
          const v = e.target.value;
          if (v !== (po.expected_delivery || "").substring(0, 10)) updatePO({ expected_delivery: v || null });
        },
        style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" }
      }
    ) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 Inter, sans-serif", color: INK } }, po.expected_delivery ? new Date(po.expected_delivery).toLocaleDateString() : "\u2014")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginBottom: 6 } }, "Recipient Email"), editable ? /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "email",
        defaultValue: po.recipient_email || "",
        onBlur: (e) => {
          const v = e.target.value;
          if (v !== (po.recipient_email || "")) updatePO({ recipient_email: v || null });
        },
        placeholder: "vendor@example.com",
        style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" }
      }
    ) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 Inter, sans-serif", color: INK } }, po.recipient_email || "\u2014")), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { style: { ...monoLabel, fontSize: 9, marginBottom: 6 } }, "CC Emails"), editable ? /* @__PURE__ */ React.createElement(
      "input",
      {
        type: "text",
        defaultValue: Array.isArray(po.cc_emails) ? po.cc_emails.join(", ") : po.cc_emails || "",
        onBlur: (e) => {
          const v = e.target.value;
          updatePO({ cc_emails: v ? v.split(",").map((s) => s.trim()).filter(Boolean) : [] });
        },
        placeholder: "a@co.com, b@co.com",
        style: { width: "100%", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1 Inter, sans-serif", color: INK, boxSizing: "border-box" }
      }
    ) : /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.4 Inter, sans-serif", color: INK } }, Array.isArray(po.cc_emails) && po.cc_emails.length ? po.cc_emails.join(", ") : "\u2014")))), /* @__PURE__ */ React.createElement(PdCard, { title: "PO summary" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 20px", display: "grid", gap: 10 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline", paddingBottom: 8, borderBottom: "0.5px solid " + INK + "22" } }, /* @__PURE__ */ React.createElement("span", { style: { font: "500 11px/1 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", color: INK } }, "PO cost"), /* @__PURE__ */ React.createElement("span", { style: { ...serifNum(30), color: INK } }, "$", subtotal.toLocaleString(void 0, { minimumFractionDigits: 2 }))), retail > 0 && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 10, padding: "12px 14px", background: WARM, display: "grid", gap: 6 } }, /* @__PURE__ */ React.createElement("div", { style: { font: "500 10px/1 ui-monospace, monospace", letterSpacing: "0.16em", textTransform: "uppercase", color: ACCENT } }, "Resale margin", isOrderLinked ? " \xB7 " + po.order_number : " \xB7 at list"), [["Resale value", "$" + retail.toFixed(2)], ["PO cost", "$" + subtotal.toFixed(2)]].map(([k, val]) => /* @__PURE__ */ React.createElement("div", { key: k, style: { display: "flex", justifyContent: "space-between", font: "400 13px/1.3 Inter, sans-serif" } }, /* @__PURE__ */ React.createElement("span", { style: { color: INK + "b3" } }, k), /* @__PURE__ */ React.createElement("span", { style: { color: INK, fontFamily: "'Cormorant Garamond', serif", fontSize: 14 } }, val))), /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", paddingTop: 8, borderTop: "0.5px solid " + INK + "33", font: "500 13px/1 Inter, sans-serif" } }, /* @__PURE__ */ React.createElement("span", { style: { color: INK } }, "Margin \xB7 ", marginPct, "%"), /* @__PURE__ */ React.createElement("span", { style: { color: GREEN, fontFamily: "'Cormorant Garamond', serif", fontSize: 14 } }, "$", margin.toFixed(2)))))), isOrderLinked && /* @__PURE__ */ React.createElement(PdCard, { title: "Linked order", action: "Open order \u2192", actionClick: () => navigate("order-detail", po.order_id) }, /* @__PURE__ */ React.createElement("div", { style: { padding: "18px 20px", display: "grid", gap: 12 } }, /* @__PURE__ */ React.createElement("div", { style: { display: "flex", justifyContent: "space-between", alignItems: "baseline" } }, /* @__PURE__ */ React.createElement("span", { style: { fontFamily: "'Cormorant Garamond', serif", fontSize: 20, color: INK, letterSpacing: "-0.012em" } }, po.order_number)), /* @__PURE__ */ React.createElement("button", { style: { padding: "10px 0", textAlign: "center", background: INK, color: PAPER, font: "500 10px/1 Inter, sans-serif", letterSpacing: "0.1em", textTransform: "uppercase", cursor: "pointer", border: "none", width: "100%" }, onClick: () => navigate("order-detail", po.order_id) }, "Open order ", po.order_number, " \u2192"))), /* @__PURE__ */ React.createElement(PdCard, { title: "Notes to vendor" }, /* @__PURE__ */ React.createElement("div", { style: { padding: "14px 20px" } }, editable ? /* @__PURE__ */ React.createElement(
      "textarea",
      {
        defaultValue: po.notes || "",
        onBlur: (e) => {
          const v = e.target.value;
          if (v !== (po.notes || "")) updatePO({ notes: v || null });
        },
        placeholder: "Add notes for the vendor...",
        style: { width: "100%", minHeight: "68px", padding: "6px 8px", border: "0.5px solid " + INK + "33", background: "transparent", font: "400 13px/1.6 Inter, sans-serif", color: INK, boxSizing: "border-box", resize: "vertical" }
      }
    ) : po.notes ? /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.6 Inter, sans-serif", color: INK + "cc", fontStyle: "italic" } }, '"', po.notes, '"') : /* @__PURE__ */ React.createElement("div", { style: { font: "400 13px/1.6 Inter, sans-serif", color: MUTED } }, "No notes"), /* @__PURE__ */ React.createElement("div", { style: { marginTop: 8, ...monoLabel, fontSize: 10, letterSpacing: "0.14em" } }, "Printed on the PO PDF \xB7 sent in the vendor email body")))))), poDocPreview && /* @__PURE__ */ React.createElement(
      PODocumentViewer,
      {
        url: poDocPreview.url,
        po: poDocPreview.po,
        onClose: () => setPoDocPreview(null),
        onSend: async () => {
          await repFetch("/api/rep/purchase-orders/" + editId + "/approve", { method: "POST" });
          setPoDocPreview(null);
          load();
        }
      }
    ));
  }
  ReactDOM.createRoot(document.getElementById("root")).render(/* @__PURE__ */ React.createElement(App, null));
})();
